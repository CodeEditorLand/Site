const _astro_dataLayerContent = [["Map", 1, 2, 375, 376, 3012, 3013], "blog", ["Map", 3, 4, 49, 50, 98, 99, 142, 143, 186, 187, 243, 244, 282, 283, 326, 327], "ApplicationStartupHandshake", { id: 3, data: 5, body: 17, filePath: 18, digest: 19, rendered: 20 }, { title: 6, summary: 7, publishedAt: 8, tags: 9, author: 15, readTime: 16 }, "Application Startup Handshake: How Mountain, Cocoon, Sky, and Wind Bootstrap", "The complete end-to-end process from launching the native binary to a fully initialized editor with running extensions.", "2026-05-15", [10, 11, 12, 13, 14], "Architecture", "Bootstrap", "Mountain", "Cocoon", "IPC", "CodeEditorLand", 8, "# Application Startup Handshake\n\nWhen you double-click the Land editor, four components boot in a carefully\nordered sequence. Each must finish its phase before the next can proceed. This\ndocument traces the exact path from process launch to a stable, interactive\neditor.\n\n## Phase 1: Mountain Starts the Native Process\n\nThe entry point is `Mountain/Source/Binary/Main/Entry.rs`. The `main` function\ncreates a Tauri `Builder` and configures a `.setup()` hook. Inside that hook:\n\n1. **`AppState` is created** and registered with Tauri as the central state\n   store.\n2. **`MountainEnvironment`** is instantiated. This struct implements every\n   abstract service trait from `Common` -- filesystem, terminal, clipboard,\n   configuration, and more.\n3. **`AppRuntime`** wraps the environment. This is the engine that executes\n   `ActionEffect`s: declarative descriptions of work with typed error channels\n   and deterministic resource cleanup.\n4. A **`tokio` background task** is spawned to handle post-setup initialization.\n\nThe background task then runs three operations in sequence:\n\n- `handlers::config::InitializeConfiguration` loads all `settings.json` files\n  from disk into `AppState`.\n- `ExtensionManagement` scans extension directories, reads manifests, and\n  populates the extension list in `AppState`.\n- `vine::server::Initialize` starts the gRPC server that will listen for\n  connections from Cocoon.\n\nFinally, `handlers::process_management::InitializeCocoon` is called. This\nexecutes `LaunchAndManageCocoonSidecar` from\n`ProcessManagement/CocoonManagement.rs`.\n\n## Phase 2: The Cocoon Sidecar Spawns\n\n`LaunchAndManageCocoonSidecar` constructs a detailed environment block for the\nchild process, including `VSCODE_PARENT_PID` for automatic shutdown if Mountain\nexits. It then spawns:\n\n```\nnode ./Element/Cocoon/Scripts/cocoon/bootstrap-fork.js\n```\n\nCocoon is now a separate Node.js process. Its first action is hardening:\n`PatchProcess` patches `process.exit`, pipes `console.log` to Mountain, and\nregisters an uncaught exception handler. Then `IpcProvider` starts Cocoon's gRPC\nclient.\n\nUpon successful connection to Mountain's Vine gRPC server, Cocoon sends the\n`$initialHandshake` notification and waits for the response.\n\n## Phase 3: The Handshake\n\nMountain receives `$initialHandshake`. This is the signal to proceed. Mountain\ncalls `InitializationData` from `ProcessManagement/InitializationData.rs`,\ngathering:\n\n- Workspace path and folder list\n- Extension manifest data\n- Configuration snapshots\n- Feature flags and tier settings\n\nThis payload is sent back to Cocoon via `initExtensionHost` gRPC request. Cocoon\nreceives it and creates the `InitDataLayer`, then runs `FullAppInitialization`.\nTwo critical things happen inside that effect:\n\n1. **`RequireInterceptor` is installed.** This patches both CJS `require()` and\n   ESM `import` so calls to the `vscode` module are intercepted and routed to\n   the correct, extension-specific API instance.\n2. **`ExtensionHostProvider`** is resolved. It activates extensions matching the\n   `*` (workspace) activation event.\n\nAt this point, extensions can begin registering language providers, tree views,\nand commands.\n\n## Phase 4: The UI Loads\n\nWhile the handshake is happening, the main Tauri window opens and loads\n`index.html`. The preload script at `Wind/Source/Preload.ts` executes first. It\nshims the `window.vscode` global with Tauri-backed implementations for\n`ipcRenderer` and `process`. This is the bridge that allows the VS Code\nworkbench code to run inside Tauri's WebView instead of Electron.\n\nThe main UI script then:\n\n1. Waits for the DOM to be ready.\n2. Creates the master `AppLayer`, which composes all Wind services\n   (`LiveClipboardService`, `LiveDialogService`, `LiveEditorService`, etc.).\n3. Converts the `Layer` to a `Runtime` and resolves core services.\n4. Instantiates the VS Code `Workbench` class: `new Workbench(...)`.\n5. Calls `Workbench.startup()`, which renders the Activity Bar, Status Bar, Side\n   Bar, Editor Part, and all other UI components.\n\nThe three processes are now running and communicating:\n\n| Process  | Technology | IPC Path                      |\n| -------- | ---------- | ----------------------------- |\n| Mountain | Rust/Tauri | gRPC server + Tauri commands  |\n| Cocoon   | Node.js    | gRPC client (`Vine` protocol) |\n| Wind/Sky | TypeScript | Tauri IPC (`invoke` system)   |\n\nTwo independent IPC paths exist by design. Tauri IPC handles native work: files,\nterminals, clipboard, search, and configuration work. Vine gRPC handles\nextension-host startup, document notifications, language provider requests,\ncancellation, and streaming routes.\n\n## Why This Sequence Matters\n\nThe startup order is not arbitrary. Mountain must be fully initialized before\nCocoon spawns, because Cocoon needs configuration and extension data to\nbootstrap. The handshake ensures Cocoon does not start activating extensions\nuntil Mountain is ready to receive proxied API calls. The UI loads in parallel\nwith the handshake, but the workbench cannot render useful state until both\nMountain and Cocoon have completed their initialization.\n\nThis is the foundational workflow. Every user action -- opening a file, running\na command, triggering a language feature -- flows through the state established\nhere.", "Source/Content/blog/ApplicationStartupHandshake.md", "48f2fec465d37233", { html: 21, metadata: 22 }, '<h1 id="application-startup-handshake">Application Startup Handshake</h1>\n<p>When you double-click the Land editor, four components boot in a carefully\nordered sequence. Each must finish its phase before the next can proceed. This\ndocument traces the exact path from process launch to a stable, interactive\neditor.</p>\n<h2 id="phase-1-mountain-starts-the-native-process">Phase 1: Mountain Starts the Native Process</h2>\n<p>The entry point is <code>Mountain/Source/Binary/Main/Entry.rs</code>. The <code>main</code> function\ncreates a Tauri <code>Builder</code> and configures a <code>.setup()</code> hook. Inside that hook:</p>\n<ol>\n<li><strong><code>AppState</code> is created</strong> and registered with Tauri as the central state\nstore.</li>\n<li><strong><code>MountainEnvironment</code></strong> is instantiated. This struct implements every\nabstract service trait from <code>Common</code> — filesystem, terminal, clipboard,\nconfiguration, and more.</li>\n<li><strong><code>AppRuntime</code></strong> wraps the environment. This is the engine that executes\n<code>ActionEffect</code>s: declarative descriptions of work with typed error channels\nand deterministic resource cleanup.</li>\n<li>A <strong><code>tokio</code> background task</strong> is spawned to handle post-setup initialization.</li>\n</ol>\n<p>The background task then runs three operations in sequence:</p>\n<ul>\n<li><code>handlers::config::InitializeConfiguration</code> loads all <code>settings.json</code> files\nfrom disk into <code>AppState</code>.</li>\n<li><code>ExtensionManagement</code> scans extension directories, reads manifests, and\npopulates the extension list in <code>AppState</code>.</li>\n<li><code>vine::server::Initialize</code> starts the gRPC server that will listen for\nconnections from Cocoon.</li>\n</ul>\n<p>Finally, <code>handlers::process_management::InitializeCocoon</code> is called. This\nexecutes <code>LaunchAndManageCocoonSidecar</code> from\n<code>ProcessManagement/CocoonManagement.rs</code>.</p>\n<h2 id="phase-2-the-cocoon-sidecar-spawns">Phase 2: The Cocoon Sidecar Spawns</h2>\n<p><code>LaunchAndManageCocoonSidecar</code> constructs a detailed environment block for the\nchild process, including <code>VSCODE_PARENT_PID</code> for automatic shutdown if Mountain\nexits. It then spawns:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>node ./Element/Cocoon/Scripts/cocoon/bootstrap-fork.js</span></span></code></pre>\n<p>Cocoon is now a separate Node.js process. Its first action is hardening:\n<code>PatchProcess</code> patches <code>process.exit</code>, pipes <code>console.log</code> to Mountain, and\nregisters an uncaught exception handler. Then <code>IpcProvider</code> starts Cocoon’s gRPC\nclient.</p>\n<p>Upon successful connection to Mountain’s Vine gRPC server, Cocoon sends the\n<code>$initialHandshake</code> notification and waits for the response.</p>\n<h2 id="phase-3-the-handshake">Phase 3: The Handshake</h2>\n<p>Mountain receives <code>$initialHandshake</code>. This is the signal to proceed. Mountain\ncalls <code>InitializationData</code> from <code>ProcessManagement/InitializationData.rs</code>,\ngathering:</p>\n<ul>\n<li>Workspace path and folder list</li>\n<li>Extension manifest data</li>\n<li>Configuration snapshots</li>\n<li>Feature flags and tier settings</li>\n</ul>\n<p>This payload is sent back to Cocoon via <code>initExtensionHost</code> gRPC request. Cocoon\nreceives it and creates the <code>InitDataLayer</code>, then runs <code>FullAppInitialization</code>.\nTwo critical things happen inside that effect:</p>\n<ol>\n<li><strong><code>RequireInterceptor</code> is installed.</strong> This patches both CJS <code>require()</code> and\nESM <code>import</code> so calls to the <code>vscode</code> module are intercepted and routed to\nthe correct, extension-specific API instance.</li>\n<li><strong><code>ExtensionHostProvider</code></strong> is resolved. It activates extensions matching the\n<code>*</code> (workspace) activation event.</li>\n</ol>\n<p>At this point, extensions can begin registering language providers, tree views,\nand commands.</p>\n<h2 id="phase-4-the-ui-loads">Phase 4: The UI Loads</h2>\n<p>While the handshake is happening, the main Tauri window opens and loads\n<code>index.html</code>. The preload script at <code>Wind/Source/Preload.ts</code> executes first. It\nshims the <code>window.vscode</code> global with Tauri-backed implementations for\n<code>ipcRenderer</code> and <code>process</code>. This is the bridge that allows the VS Code\nworkbench code to run inside Tauri’s WebView instead of Electron.</p>\n<p>The main UI script then:</p>\n<ol>\n<li>Waits for the DOM to be ready.</li>\n<li>Creates the master <code>AppLayer</code>, which composes all Wind services\n(<code>LiveClipboardService</code>, <code>LiveDialogService</code>, <code>LiveEditorService</code>, etc.).</li>\n<li>Converts the <code>Layer</code> to a <code>Runtime</code> and resolves core services.</li>\n<li>Instantiates the VS Code <code>Workbench</code> class: <code>new Workbench(...)</code>.</li>\n<li>Calls <code>Workbench.startup()</code>, which renders the Activity Bar, Status Bar, Side\nBar, Editor Part, and all other UI components.</li>\n</ol>\n<p>The three processes are now running and communicating:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Process</th><th>Technology</th><th>IPC Path</th></tr></thead><tbody><tr><td>Mountain</td><td>Rust/Tauri</td><td>gRPC server + Tauri commands</td></tr><tr><td>Cocoon</td><td>Node.js</td><td>gRPC client (<code>Vine</code> protocol)</td></tr><tr><td>Wind/Sky</td><td>TypeScript</td><td>Tauri IPC (<code>invoke</code> system)</td></tr></tbody></table>\n<p>Two independent IPC paths exist by design. Tauri IPC handles native work: files,\nterminals, clipboard, search, and configuration work. Vine gRPC handles\nextension-host startup, document notifications, language provider requests,\ncancellation, and streaming routes.</p>\n<h2 id="why-this-sequence-matters">Why This Sequence Matters</h2>\n<p>The startup order is not arbitrary. Mountain must be fully initialized before\nCocoon spawns, because Cocoon needs configuration and extension data to\nbootstrap. The handshake ensures Cocoon does not start activating extensions\nuntil Mountain is ready to receive proxied API calls. The UI loads in parallel\nwith the handshake, but the workbench cannot render useful state until both\nMountain and Cocoon have completed their initialization.</p>\n<p>This is the foundational workflow. Every user action — opening a file, running\na command, triggering a language feature — flows through the state established\nhere.</p>', { headings: 23, localImagePaths: 44, remoteImagePaths: 45, frontmatter: 46, imagePaths: 48 }, [24, 28, 32, 35, 38, 41], { depth: 25, slug: 26, text: 27 }, 1, "application-startup-handshake", "Application Startup Handshake", { depth: 29, slug: 30, text: 31 }, 2, "phase-1-mountain-starts-the-native-process", "Phase 1: Mountain Starts the Native Process", { depth: 29, slug: 33, text: 34 }, "phase-2-the-cocoon-sidecar-spawns", "Phase 2: The Cocoon Sidecar Spawns", { depth: 29, slug: 36, text: 37 }, "phase-3-the-handshake", "Phase 3: The Handshake", { depth: 29, slug: 39, text: 40 }, "phase-4-the-ui-loads", "Phase 4: The UI Loads", { depth: 29, slug: 42, text: 43 }, "why-this-sequence-matters", "Why This Sequence Matters", [], [], { title: 6, summary: 7, publishedAt: 8, tags: 47, author: 15, readTime: 16 }, [10, 11, 12, 13, 14], [], "BuildSystemDesign", { id: 49, data: 51, body: 60, filePath: 61, digest: 62, rendered: 63 }, { title: 52, summary: 53, publishedAt: 8, tags: 54, author: 15, readTime: 59 }, "Build System Design: Rhai Scripting, TOML/JSON5, and Deterministic Builds", "How Maintain orchestrates cross-element builds with embedded scripting and type-safe configuration editing.", [55, 56, 57, 58], "Build System", "Rhai", "CI/CD", "Maintain", 7, "# Build System Design\n\nDeterministic builds are hard. The same commit should produce identical output\non every machine, regardless of installed tool versions, environment variables,\nor implicit toolchain state. Maintain, Land's build system, is a Rust binary\ndesigned to eliminate that class of failures entirely.\n\n## Architecture\n\nMaintain is both a library and a CLI. The entry point at `Source/Library.rs`\nwires together three subsystems:\n\n1. **CLI layer** (`Source/Build/CLI.rs`) -- a `clap`-based interface accepting\n   subcommands for debug, release, profile, and dev operations.\n2. **Rhai scripting engine** (`Source/Build/Rhai/`) -- an embedded interpreter\n   that executes build configuration scripts with full environment access.\n3. **Configuration editing** (`Source/Build/TomlEdit.rs`,\n   `Source/Build/JsonEdit.rs`) -- non-lossy TOML and JSON5 editing for\n   `Cargo.toml` and `package.json`.\n\nBuild dispatch flows through `Source/Build/Fn.rs`, which coordinates clean,\ncompile, link, and post-process stages. Child process management\n(`Source/Build/Process.rs`) handles stdout/stderr capture and exit code\npropagation.\n\n## The Shell Entry Points\n\nEvery build starts from a shell script in `Maintain/`:\n\n| Script            | Purpose                                      |\n| ----------------- | -------------------------------------------- |\n| `Debug.sh`        | Full debug build of all elements             |\n| `Dev-Mountain.sh` | Hot-reload development mode for Mountain     |\n| `Release.sh`      | Optimized release build with all elements    |\n| `Profile.sh`      | Release build with profiling instrumentation |\n\nThese scripts call the Maintain CLI with appropriate flags. For example, the\ndebug path invokes:\n\n```\nMaintain debug --target aarch64-apple-darwin\n```\n\nThe CLI loads the Rhai build configuration, resolves environment variables,\nreads `Cargo.toml` for version and dependency information, and then iterates\nthrough the build group defined in `Source/Build/Definition.rs`. Each element is\ncompiled in order via subprocess:\n\n```\ncargo build -p Mountain\ncargo build -p Air\ncargo build -p Echo\n```\n\nTypeScript elements (Cocoon, Wind, Sky) are built through\n`pnpm run prepublishOnly` subprocesses, coordinated via Turborepo.\n\n## Tier Gating via Environment Overlay\n\nThe most complex part of the build system is tier-gating. The file\n`Maintain/Script/TierEnvironment.sh` acts as the master configuration fan-out.\nIt reads `.env.Land` (or `.env.Land.Sample`) and exports every `Tier*`,\n`Product*`, and `Network*` variable so all elements see the same configuration.\n\nThe overlay cascade resolves in order:\n\n1. Root `.env.Land` values\n2. Domain-specific overlays: `.env.Land.Node`, `.env.Land.Extensions`,\n   `.env.Land.PostHog`, `.env.Land.Diagnostics`, `.env.Land.Bundled`\n3. Production overlays when `NODE_ENV=production` or the profile contains\n   `release`\n\nFrom these values, the script derives two outputs:\n\n- **`CargoFeatures`** -- a comma-separated list of Rust feature flags. For\n  example, `TierRemoteProcedureCall=SharedMemory` becomes\n  `TierRemoteProcedureCallSharedMemory`, which Mountain's `Cargo.toml` gates on.\n- **`CocoonEsbuildDefine`** -- a JSON object of `__LandTier_*__`,\n  `__LandProduct_*__`, and `__LandNetwork_*__` replacement tokens that Cocoon's\n  `TargetConfig.ts` merges into esbuild options.\n\nThis means every capability with multiple implementations (gRPC vs shared\nmemory, native glob vs `globset`, JS file watcher vs `notify`) lives in the\ncodebase simultaneously and gets selected at build time. No duplicated call\nsites, no preprocessor macros in source files.\n\n## Configuration Editing\n\nMaintain provides two configuration editors:\n\n**TomlEdit** (`Source/Build/TomlEdit.rs`) uses the `toml_edit` crate for\nnon-lossy TOML modifications. This means version bumps and dependency changes\npreserve formatting, comments, and structure. Critical for CI that modifies\n`Cargo.toml` without destroying developer-added documentation.\n\n**JsonEdit** (`Source/Build/JsonEdit.rs`) provides JSON5-aware editing for\n`package.json` and `.json5` files. JSON5 allows comments and trailing commas,\nboth common in Land's configuration files.\n\n## Route Manifest Generation\n\n`Maintain/Script/GenerateRouteManifest.sh` is a code generation step hooked into\n`pnpm run prepublishOnly`. It scans three routing tiers and emits a TypeScript\nmanifest at `Element/Cocoon/Source/Generated/RouteManifest.ts`:\n\n- **Tier 1**: Mountain Rust handlers extracted from\n  `Element/Mountain/Source/Track/Effect/CreateEffectForRequest/*.rs` via regex\n  matching of `\"Name\" =>` arms.\n- **Tier 2**: Stock VS Code lifted functions from\n  `Element/Cocoon/Source/Services/Handler/VscodeAPI/StockLift.ts`.\n- **Tier 3**: Cocoon bespoke Node fallbacks discovered in `*Fallback.ts` files.\n\nThe manifest produces three `ReadonlySet<string>` values consumed by\n`Services/DualTrack.ts` at runtime. The dual-track dispatcher skips Mountain's\ngRPC round-trip for methods Mountain does not handle, and raises a typed error\nfor methods no tier covers. A coverage report is emitted at\n`RouteManifest.Report.md`.\n\n## Rhai Scripting Engine\n\nFor build logic that cannot be expressed in TOML or shell, Maintain embeds Rhai.\nThe scripting engine lives in three modules:\n\n| Module                                     | Role                                    |\n| ------------------------------------------ | --------------------------------------- |\n| `Source/Build/Rhai/ConfigLoader.rs`        | Loads and parses `.rhai` build scripts  |\n| `Source/Build/Rhai/ScriptRunner.rs`        | Executes scripts with Land API bindings |\n| `Source/Build/Rhai/EnvironmentResolver.rs` | Exposes env vars to scripts             |\n\nRhai scripts have access to the full build context: environment variables,\nelement paths, and build phase information. This allows custom build steps\nwithout modifying the Rust binary or adding new shell scripts.\n\n## Key Dependencies\n\n| Rust Crate         | Purpose                            |\n| ------------------ | ---------------------------------- |\n| `rhai`             | Embedded scripting engine          |\n| `clap`             | CLI argument parsing               |\n| `toml_edit`        | Non-lossy TOML parsing and editing |\n| `json5`            | JSON5 configuration support        |\n| `colored`          | Colored terminal output            |\n| `log`/`env_logger` | Logging framework                  |\n\n## Why Rust for a Build System?\n\nSpeed and type safety are the surface reasons. The deeper reason is that the\nbuild system shares a language with Mountain, Air, and Echo. Type definitions,\nDTOs, and error types flow naturally between the build pipeline and the elements\nit compiles. There is no impedance mismatch between build-time configuration and\nruntime behavior.", "Source/Content/blog/BuildSystemDesign.md", "6fd6b445f6492dfb", { html: 64, metadata: 65 }, '<h1 id="build-system-design">Build System Design</h1>\n<p>Deterministic builds are hard. The same commit should produce identical output\non every machine, regardless of installed tool versions, environment variables,\nor implicit toolchain state. Maintain, Land’s build system, is a Rust binary\ndesigned to eliminate that class of failures entirely.</p>\n<h2 id="architecture">Architecture</h2>\n<p>Maintain is both a library and a CLI. The entry point at <code>Source/Library.rs</code>\nwires together three subsystems:</p>\n<ol>\n<li><strong>CLI layer</strong> (<code>Source/Build/CLI.rs</code>) — a <code>clap</code>-based interface accepting\nsubcommands for debug, release, profile, and dev operations.</li>\n<li><strong>Rhai scripting engine</strong> (<code>Source/Build/Rhai/</code>) — an embedded interpreter\nthat executes build configuration scripts with full environment access.</li>\n<li><strong>Configuration editing</strong> (<code>Source/Build/TomlEdit.rs</code>,\n<code>Source/Build/JsonEdit.rs</code>) — non-lossy TOML and JSON5 editing for\n<code>Cargo.toml</code> and <code>package.json</code>.</li>\n</ol>\n<p>Build dispatch flows through <code>Source/Build/Fn.rs</code>, which coordinates clean,\ncompile, link, and post-process stages. Child process management\n(<code>Source/Build/Process.rs</code>) handles stdout/stderr capture and exit code\npropagation.</p>\n<h2 id="the-shell-entry-points">The Shell Entry Points</h2>\n<p>Every build starts from a shell script in <code>Maintain/</code>:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Script</th><th>Purpose</th></tr></thead><tbody><tr><td><code>Debug.sh</code></td><td>Full debug build of all elements</td></tr><tr><td><code>Dev-Mountain.sh</code></td><td>Hot-reload development mode for Mountain</td></tr><tr><td><code>Release.sh</code></td><td>Optimized release build with all elements</td></tr><tr><td><code>Profile.sh</code></td><td>Release build with profiling instrumentation</td></tr></tbody></table>\n<p>These scripts call the Maintain CLI with appropriate flags. For example, the\ndebug path invokes:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>Maintain debug --target aarch64-apple-darwin</span></span></code></pre>\n<p>The CLI loads the Rhai build configuration, resolves environment variables,\nreads <code>Cargo.toml</code> for version and dependency information, and then iterates\nthrough the build group defined in <code>Source/Build/Definition.rs</code>. Each element is\ncompiled in order via subprocess:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>cargo build -p Mountain</span></span>\n<span class="line"><span>cargo build -p Air</span></span>\n<span class="line"><span>cargo build -p Echo</span></span></code></pre>\n<p>TypeScript elements (Cocoon, Wind, Sky) are built through\n<code>pnpm run prepublishOnly</code> subprocesses, coordinated via Turborepo.</p>\n<h2 id="tier-gating-via-environment-overlay">Tier Gating via Environment Overlay</h2>\n<p>The most complex part of the build system is tier-gating. The file\n<code>Maintain/Script/TierEnvironment.sh</code> acts as the master configuration fan-out.\nIt reads <code>.env.Land</code> (or <code>.env.Land.Sample</code>) and exports every <code>Tier*</code>,\n<code>Product*</code>, and <code>Network*</code> variable so all elements see the same configuration.</p>\n<p>The overlay cascade resolves in order:</p>\n<ol>\n<li>Root <code>.env.Land</code> values</li>\n<li>Domain-specific overlays: <code>.env.Land.Node</code>, <code>.env.Land.Extensions</code>,\n<code>.env.Land.PostHog</code>, <code>.env.Land.Diagnostics</code>, <code>.env.Land.Bundled</code></li>\n<li>Production overlays when <code>NODE_ENV=production</code> or the profile contains\n<code>release</code></li>\n</ol>\n<p>From these values, the script derives two outputs:</p>\n<ul>\n<li><strong><code>CargoFeatures</code></strong> — a comma-separated list of Rust feature flags. For\nexample, <code>TierRemoteProcedureCall=SharedMemory</code> becomes\n<code>TierRemoteProcedureCallSharedMemory</code>, which Mountain’s <code>Cargo.toml</code> gates on.</li>\n<li><strong><code>CocoonEsbuildDefine</code></strong> — a JSON object of <code>__LandTier_*__</code>,\n<code>__LandProduct_*__</code>, and <code>__LandNetwork_*__</code> replacement tokens that Cocoon’s\n<code>TargetConfig.ts</code> merges into esbuild options.</li>\n</ul>\n<p>This means every capability with multiple implementations (gRPC vs shared\nmemory, native glob vs <code>globset</code>, JS file watcher vs <code>notify</code>) lives in the\ncodebase simultaneously and gets selected at build time. No duplicated call\nsites, no preprocessor macros in source files.</p>\n<h2 id="configuration-editing">Configuration Editing</h2>\n<p>Maintain provides two configuration editors:</p>\n<p><strong>TomlEdit</strong> (<code>Source/Build/TomlEdit.rs</code>) uses the <code>toml_edit</code> crate for\nnon-lossy TOML modifications. This means version bumps and dependency changes\npreserve formatting, comments, and structure. Critical for CI that modifies\n<code>Cargo.toml</code> without destroying developer-added documentation.</p>\n<p><strong>JsonEdit</strong> (<code>Source/Build/JsonEdit.rs</code>) provides JSON5-aware editing for\n<code>package.json</code> and <code>.json5</code> files. JSON5 allows comments and trailing commas,\nboth common in Land’s configuration files.</p>\n<h2 id="route-manifest-generation">Route Manifest Generation</h2>\n<p><code>Maintain/Script/GenerateRouteManifest.sh</code> is a code generation step hooked into\n<code>pnpm run prepublishOnly</code>. It scans three routing tiers and emits a TypeScript\nmanifest at <code>Element/Cocoon/Source/Generated/RouteManifest.ts</code>:</p>\n<ul>\n<li><strong>Tier 1</strong>: Mountain Rust handlers extracted from\n<code>Element/Mountain/Source/Track/Effect/CreateEffectForRequest/*.rs</code> via regex\nmatching of <code>"Name" =></code> arms.</li>\n<li><strong>Tier 2</strong>: Stock VS Code lifted functions from\n<code>Element/Cocoon/Source/Services/Handler/VscodeAPI/StockLift.ts</code>.</li>\n<li><strong>Tier 3</strong>: Cocoon bespoke Node fallbacks discovered in <code>*Fallback.ts</code> files.</li>\n</ul>\n<p>The manifest produces three <code>ReadonlySet&#x3C;string></code> values consumed by\n<code>Services/DualTrack.ts</code> at runtime. The dual-track dispatcher skips Mountain’s\ngRPC round-trip for methods Mountain does not handle, and raises a typed error\nfor methods no tier covers. A coverage report is emitted at\n<code>RouteManifest.Report.md</code>.</p>\n<h2 id="rhai-scripting-engine">Rhai Scripting Engine</h2>\n<p>For build logic that cannot be expressed in TOML or shell, Maintain embeds Rhai.\nThe scripting engine lives in three modules:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Module</th><th>Role</th></tr></thead><tbody><tr><td><code>Source/Build/Rhai/ConfigLoader.rs</code></td><td>Loads and parses <code>.rhai</code> build scripts</td></tr><tr><td><code>Source/Build/Rhai/ScriptRunner.rs</code></td><td>Executes scripts with Land API bindings</td></tr><tr><td><code>Source/Build/Rhai/EnvironmentResolver.rs</code></td><td>Exposes env vars to scripts</td></tr></tbody></table>\n<p>Rhai scripts have access to the full build context: environment variables,\nelement paths, and build phase information. This allows custom build steps\nwithout modifying the Rust binary or adding new shell scripts.</p>\n<h2 id="key-dependencies">Key Dependencies</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Rust Crate</th><th>Purpose</th></tr></thead><tbody><tr><td><code>rhai</code></td><td>Embedded scripting engine</td></tr><tr><td><code>clap</code></td><td>CLI argument parsing</td></tr><tr><td><code>toml_edit</code></td><td>Non-lossy TOML parsing and editing</td></tr><tr><td><code>json5</code></td><td>JSON5 configuration support</td></tr><tr><td><code>colored</code></td><td>Colored terminal output</td></tr><tr><td><code>log</code>/<code>env_logger</code></td><td>Logging framework</td></tr></tbody></table>\n<h2 id="why-rust-for-a-build-system">Why Rust for a Build System?</h2>\n<p>Speed and type safety are the surface reasons. The deeper reason is that the\nbuild system shares a language with Mountain, Air, and Echo. Type definitions,\nDTOs, and error types flow naturally between the build pipeline and the elements\nit compiles. There is no impedance mismatch between build-time configuration and\nruntime behavior.</p>', { headings: 66, localImagePaths: 93, remoteImagePaths: 94, frontmatter: 95, imagePaths: 97 }, [67, 70, 72, 75, 78, 81, 84, 87, 90], { depth: 25, slug: 68, text: 69 }, "build-system-design", "Build System Design", { depth: 29, slug: 71, text: 10 }, "architecture", { depth: 29, slug: 73, text: 74 }, "the-shell-entry-points", "The Shell Entry Points", { depth: 29, slug: 76, text: 77 }, "tier-gating-via-environment-overlay", "Tier Gating via Environment Overlay", { depth: 29, slug: 79, text: 80 }, "configuration-editing", "Configuration Editing", { depth: 29, slug: 82, text: 83 }, "route-manifest-generation", "Route Manifest Generation", { depth: 29, slug: 85, text: 86 }, "rhai-scripting-engine", "Rhai Scripting Engine", { depth: 29, slug: 88, text: 89 }, "key-dependencies", "Key Dependencies", { depth: 29, slug: 91, text: 92 }, "why-rust-for-a-build-system", "Why Rust for a Build System?", [], [], { title: 52, summary: 53, publishedAt: 8, tags: 96, author: 15, readTime: 59 }, [55, 56, 57, 58], [], "CC0OpenSource", { id: 98, data: 100, body: 110, filePath: 111, digest: 112, rendered: 113 }, { title: 101, summary: 102, publishedAt: 103, tags: 104, author: 15, readTime: 109 }, "Why We Chose CC0 1.0 Universal for a Code Editor", "Public domain licensing, no CLA, no copyright assignment - maximum freedom.", "2026-04-03", [105, 106, 107, 108], "Open Source", "Licensing", "CC0", "Community", 6, "# Why We Chose CC0 1.0 Universal for a Code Editor\n\nCode Editor Land is released under\n[CC0 1.0 Universal](https://creativecommons.org/publicdomain/zero/1.0/) - a\npublic domain dedication. This means the entire codebase, including every `Rust`\ncrate, `TypeScript` package, and build script, carries no copyright restrictions\nwhatsoever.\n\n## What CC0 Actually Means\n\nCC0 is not a license in the traditional sense. It is a waiver. The copyright\nholder (CodeEditorLand Foundation, operated by PlayForm, Sofia, Bulgaria)\nirrevocably surrenders all copyright and related rights to the work, to the\nfullest extent permitted by law. Where waiver is not legally possible (as in\nsome jurisdictions), CC0 includes a permissive fallback license.\n\nIn practical terms:\n\n- You can use Land's code in proprietary software.\n- You can modify it without publishing your changes.\n- You do not need to include attribution (though it is appreciated).\n- You can sell products built on top of it.\n- No patent claims are made or reserved.\n\n## No CLA, No Copyright Assignment\n\nMany open-source projects require contributors to sign a Contributor License\nAgreement (CLA) that transfers or grants broad rights over their contributions.\nOthers require full copyright assignment to a corporate entity.\n\nLand requires neither.\n\nWhen you submit a pull request to any CodeEditorLand repository, your\ncontribution is accepted under the same CC0 dedication. There is no paperwork,\nno legal review, no waiting period. You retain the ability to use your own code\nhowever you wish - CC0 does not restrict the contributor, only the project's\nability to restrict others.\n\n## Why Not MIT, Apache 2.0, or GPL?\n\nEach of these licenses carries conditions that create friction:\n\n| License    | Condition                                        |\n| ---------- | ------------------------------------------------ |\n| MIT        | Must include copyright notice and license text   |\n| Apache 2.0 | Must include notice, state changes, patent grant |\n| GPL 3.0    | Must distribute source for derivative works      |\n\nThese conditions are reasonable and well-intentioned. But for a code editor - a\ntool that developers use to build everything else - we wanted zero friction. You\nshould not need a lawyer to decide whether you can embed a snippet from your\neditor's source into your project.\n\nCC0 eliminates that question entirely.\n\n## The Architecture Decision\n\nA CC0 dedication is a deliberate architectural choice, not just a legal one. It\nshapes how the codebase is organized. When every component carries no\nrestrictions, there is no need for licensing headers on every file, no license\ncompliance tooling in CI, and no per-component license auditing. The build\npipeline stays lean. The `Maintain` element focuses on code quality and\nrefactoring (GritQL queries, automated fixes) rather than license compliance.\n\nEvery Land element -- `Mountain`, `Cocoon`, `Sky`, `Wind`, `Vine`, `Air`,\n`Echo`, and the rest -- shares the same public domain status. This means\ncomponents can be extracted, combined, or embedded without license compatibility\nanalysis.\n\n## How NLnet Makes This Possible\n\nDeveloping a full-featured code editor without a commercial license or a SaaS\nrevenue model requires funding that does not depend on restricting the output.\nThe [NGI0 Commons Fund](https://nlnet.nl/commonsfund/) (grant No. 101135429),\nadministered by the NLnet Foundation, provides exactly this. The grant funds\ndevelopment of open internet infrastructure without requiring proprietary\nlock-in.\n\nPlayForm (Sofia, Bulgaria) operates the CodeEditorLand Foundation under this\ngrant. The funding model ensures that CC0 licensing is sustainable -\ncontributors and maintainers are compensated through public interest funding,\nnot through monetizing user data or restricting distribution.\n\n## What This Means for You\n\nIf you are an individual developer, CC0 means Land is yours. Fork it, ship it,\nbreak it apart, rebuild it.\n\nIf you are a company, CC0 means there is no license compliance burden. Your\nlegal team does not need to audit it. Your procurement process does not need to\nflag it.\n\nIf you are a contributor, CC0 means your code reaches the widest possible\naudience without gatekeepers.\n\nThat is the kind of freedom a code editor should provide.", "Source/Content/blog/CC0OpenSource.md", "51df6cf55e847222", { html: 114, metadata: 115 }, '<h1 id="why-we-chose-cc0-10-universal-for-a-code-editor">Why We Chose CC0 1.0 Universal for a Code Editor</h1>\n<p>Code Editor Land is released under\n<a href="https://creativecommons.org/publicdomain/zero/1.0/">CC0 1.0 Universal</a> - a\npublic domain dedication. This means the entire codebase, including every <code>Rust</code>\ncrate, <code>TypeScript</code> package, and build script, carries no copyright restrictions\nwhatsoever.</p>\n<h2 id="what-cc0-actually-means">What CC0 Actually Means</h2>\n<p>CC0 is not a license in the traditional sense. It is a waiver. The copyright\nholder (CodeEditorLand Foundation, operated by PlayForm, Sofia, Bulgaria)\nirrevocably surrenders all copyright and related rights to the work, to the\nfullest extent permitted by law. Where waiver is not legally possible (as in\nsome jurisdictions), CC0 includes a permissive fallback license.</p>\n<p>In practical terms:</p>\n<ul>\n<li>You can use Land’s code in proprietary software.</li>\n<li>You can modify it without publishing your changes.</li>\n<li>You do not need to include attribution (though it is appreciated).</li>\n<li>You can sell products built on top of it.</li>\n<li>No patent claims are made or reserved.</li>\n</ul>\n<h2 id="no-cla-no-copyright-assignment">No CLA, No Copyright Assignment</h2>\n<p>Many open-source projects require contributors to sign a Contributor License\nAgreement (CLA) that transfers or grants broad rights over their contributions.\nOthers require full copyright assignment to a corporate entity.</p>\n<p>Land requires neither.</p>\n<p>When you submit a pull request to any CodeEditorLand repository, your\ncontribution is accepted under the same CC0 dedication. There is no paperwork,\nno legal review, no waiting period. You retain the ability to use your own code\nhowever you wish - CC0 does not restrict the contributor, only the project’s\nability to restrict others.</p>\n<h2 id="why-not-mit-apache-20-or-gpl">Why Not MIT, Apache 2.0, or GPL?</h2>\n<p>Each of these licenses carries conditions that create friction:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>License</th><th>Condition</th></tr></thead><tbody><tr><td>MIT</td><td>Must include copyright notice and license text</td></tr><tr><td>Apache 2.0</td><td>Must include notice, state changes, patent grant</td></tr><tr><td>GPL 3.0</td><td>Must distribute source for derivative works</td></tr></tbody></table>\n<p>These conditions are reasonable and well-intentioned. But for a code editor - a\ntool that developers use to build everything else - we wanted zero friction. You\nshould not need a lawyer to decide whether you can embed a snippet from your\neditor’s source into your project.</p>\n<p>CC0 eliminates that question entirely.</p>\n<h2 id="the-architecture-decision">The Architecture Decision</h2>\n<p>A CC0 dedication is a deliberate architectural choice, not just a legal one. It\nshapes how the codebase is organized. When every component carries no\nrestrictions, there is no need for licensing headers on every file, no license\ncompliance tooling in CI, and no per-component license auditing. The build\npipeline stays lean. The <code>Maintain</code> element focuses on code quality and\nrefactoring (GritQL queries, automated fixes) rather than license compliance.</p>\n<p>Every Land element — <code>Mountain</code>, <code>Cocoon</code>, <code>Sky</code>, <code>Wind</code>, <code>Vine</code>, <code>Air</code>,\n<code>Echo</code>, and the rest — shares the same public domain status. This means\ncomponents can be extracted, combined, or embedded without license compatibility\nanalysis.</p>\n<h2 id="how-nlnet-makes-this-possible">How NLnet Makes This Possible</h2>\n<p>Developing a full-featured code editor without a commercial license or a SaaS\nrevenue model requires funding that does not depend on restricting the output.\nThe <a href="https://nlnet.nl/commonsfund/">NGI0 Commons Fund</a> (grant No. 101135429),\nadministered by the NLnet Foundation, provides exactly this. The grant funds\ndevelopment of open internet infrastructure without requiring proprietary\nlock-in.</p>\n<p>PlayForm (Sofia, Bulgaria) operates the CodeEditorLand Foundation under this\ngrant. The funding model ensures that CC0 licensing is sustainable -\ncontributors and maintainers are compensated through public interest funding,\nnot through monetizing user data or restricting distribution.</p>\n<h2 id="what-this-means-for-you">What This Means for You</h2>\n<p>If you are an individual developer, CC0 means Land is yours. Fork it, ship it,\nbreak it apart, rebuild it.</p>\n<p>If you are a company, CC0 means there is no license compliance burden. Your\nlegal team does not need to audit it. Your procurement process does not need to\nflag it.</p>\n<p>If you are a contributor, CC0 means your code reaches the widest possible\naudience without gatekeepers.</p>\n<p>That is the kind of freedom a code editor should provide.</p>', { headings: 116, localImagePaths: 137, remoteImagePaths: 138, frontmatter: 139, imagePaths: 141 }, [117, 119, 122, 125, 128, 131, 134], { depth: 25, slug: 118, text: 101 }, "why-we-chose-cc0-10-universal-for-a-code-editor", { depth: 29, slug: 120, text: 121 }, "what-cc0-actually-means", "What CC0 Actually Means", { depth: 29, slug: 123, text: 124 }, "no-cla-no-copyright-assignment", "No CLA, No Copyright Assignment", { depth: 29, slug: 126, text: 127 }, "why-not-mit-apache-20-or-gpl", "Why Not MIT, Apache 2.0, or GPL?", { depth: 29, slug: 129, text: 130 }, "the-architecture-decision", "The Architecture Decision", { depth: 29, slug: 132, text: 133 }, "how-nlnet-makes-this-possible", "How NLnet Makes This Possible", { depth: 29, slug: 135, text: 136 }, "what-this-means-for-you", "What This Means for You", [], [], { title: 101, summary: 102, publishedAt: 103, tags: 140, author: 15, readTime: 109 }, [105, 106, 107, 108], [], "EffectTypeScriptArchitecture", { id: 142, data: 144, body: 151, filePath: 152, digest: 153, rendered: 154 }, { title: 145, summary: 146, publishedAt: 147, tags: 148, author: 15, readTime: 16 }, "Effect-TS Architecture in the Land Editor", "How we use Effect-TS for typed error handling across the editor core.", "2026-04-01", [10, 149, 150], "TypeScript", "Effect-TS", "# Effect-TS Architecture in the Land Editor\n\nCode Editor Land uses [`Effect-TS`](https://effect.website) as the foundational\nabstraction for typed error handling, structured concurrency, and dependency\ninjection across the `TypeScript` layer of the editor. `Cocoon`, `Wind`, and the\nshared `Vine` IPC contracts are all built on Effect-TS from the ground up.\n\n## Why Effect-TS?\n\nTraditional `TypeScript` error handling relies on `try/catch` blocks and\n`Promise` chains, which lose type information at every boundary. `Effect-TS`\nencodes errors and requirements directly in the function signature, making it\nimpossible to accidentally swallow an error or forget a dependency.\n\nEvery service in the `Wind` UI layer returns an `Effect<Result, AppError, Env>`.\nThe three type parameters represent:\n\n1. **Result** - the success value\n2. **AppError** - the union of all errors that can occur\n3. **Env** - the services this Effect depends on\n\nThis makes refactoring safe: the compiler tells you exactly what changed at\nevery call site. When you rename a service tag or change its interface, every\nconsuming Effect fails to compile.\n\n## The Effect Layer Pattern\n\nEach service in `Wind` follows an atomic directory structure:\n\n```text\nEffect/\n  ServiceName/\n    Tag.ts      -- Context.Tag for dependency injection\n    Interface.ts -- TypeScript interface the service implements\n    Implementation.ts -- Concrete implementation using Effect primitives\n    Layer.ts    -- Effect Layer that binds Interface to Implementation\n    Type.ts     -- Shared types, error definitions\n```\n\nThese compose into `TauriLiveLayer`, `ElectronLiveLayer`, and `TestLayer`\nstacks. In production, `Wind` uses `TauriLiveLayer`, which routes service calls\nthrough `Tauri` commands into `Mountain`'s Rust handlers. For testing,\n`TestLayer` provides mock implementations. The consuming code never knows the\ndifference.\n\n## How It Powers the Extension Host (Cocoon)\n\nThe `Cocoon` element runs `VS Code` extensions inside an Effect-managed runtime.\nEach extension lifecycle hook (`activate`, `deactivate`) is modeled as an Effect\nprogram with explicit error types and resource cleanup via `Scope`.\n\nThe core architecture:\n\n1. **Bootstrap** (`Bootstrap/Implementation/Cocoon/Main.ts`) composes all\n   Effect-TS layers, establishes the gRPC connection to `Mountain`, and starts\n   the extension host services.\n\n2. **Module Interception** (`Effect/Module/Interceptor.ts`) patches both CJS\n   `require()` and ESM `import` statements so that calls to the `vscode` module\n   are correctly sandboxed and routed to the appropriate, extension-specific API\n   instance.\n\n3. **API Factory** (`Services/API/Factory/Service.ts`) constructs the `vscode`\n   API object that extensions receive. Every method call is wired through the\n   Effect-TS service stack, not through raw callbacks.\n\n4. **Extension Host Service** (`Services/Extension/Host/Service.ts`) manages\n   extension activation, lifecycle, and supervision. One extension crashing does\n   not take down the rest.\n\nAll of this runs under `Effect-TS`'s structured concurrency model. Each\nextension runs in its own supervised fiber with `Scope`-based resource\nmanagement -- file handles, network connections, and spawned processes are all\ntracked and cleaned up on extension deactivation or failure.\n\n## gRPC Integration via Vine\n\nCommunication between `Cocoon` and `Mountain` happens over the `Vine` gRPC\nprotocol defined in `.proto` files. `Cocoon` wraps the gRPC client in Effect-TS\nservices:\n\n```typescript\n// Services/Mountain/gRPC/Client.ts -- Effect-wrapped gRPC operations\ninterface MountainClient {\n	window: WindowOperations;\n	workspace: WorkspaceOperations;\n	commands: CommandOperations;\n	secrets: SecretStorageOperations;\n}\n```\n\nThe `Services/Mountain/gRPC/Client.ts` module provides Effect-wrapped methods\nfor window, workspace, command, and secret storage calls. The gRPC server\n(`Services/gRPC/Server/Service.ts`) implements the Vine protocol with\nbidirectional streaming for real-time event communication.\n\n## Process Hardening\n\n`Cocoon` applies process-level hardening before any other code runs:\n\n- Patches `process.exit` to prevent extensions from killing the host\n- Handles uncaught exceptions and unhandled promise rejections\n- Pipes logs to the host `Mountain` process\n- Automatically terminates if the parent `Mountain` process exits\n\nThis is the `PatchProcess/` module, the first thing that runs on bootstrap.\n\n## Structured Concurrency\n\n`Effect-TS` provides `Effect.fork`, `Effect.race`, and `Effect.timeout`\nprimitives that integrate with the `Echo` work-stealing executor in the Rust\nlayer, giving the editor predictable resource usage even under heavy extension\nload.\n\nThe `Echo` element in `Land/Element/Echo` provides a work-stealing task\nscheduler for bounded background work. In the TypeScript layer, `Effect.fork`\ncreates fibers that the scheduler can preempt. This replaces the traditional\nNode.js event-loop model where one hung Promise blocks every other extension.\n\n## Typed IPC: Two Layers\n\nThe editor has two independent IPC paths, each typed differently:\n\n- **Tauri IPC** (`Wind` to `Mountain`): Typed `Tauri` commands with Rust\n  handlers in `Mountain`'s `IPC/` module. `Preload.ts` shims the Electron\n  `ipcRenderer` so the VS Code workbench code communicates through Tauri's\n  `invoke` system instead.\n\n- **Vine gRPC** (`Cocoon` to `Mountain`): Strongly-typed protocol described by\n  `.proto` files, with generated Rust and TypeScript stubs. Bidirectional\n  streaming for real-time events like document notifications, language provider\n  requests, terminal I/O, and extension health.\n\nThese paths are independent. The website should not describe every service call\nas flowing through one universal bus. Each has its own job and its own error\nsurface.", "Source/Content/blog/EffectTypeScriptArchitecture.md", "81cba512f7ccf2ca", { html: 155, metadata: 156 }, '<h1 id="effect-ts-architecture-in-the-land-editor">Effect-TS Architecture in the Land Editor</h1>\n<p>Code Editor Land uses <a href="https://effect.website"><code>Effect-TS</code></a> as the foundational\nabstraction for typed error handling, structured concurrency, and dependency\ninjection across the <code>TypeScript</code> layer of the editor. <code>Cocoon</code>, <code>Wind</code>, and the\nshared <code>Vine</code> IPC contracts are all built on Effect-TS from the ground up.</p>\n<h2 id="why-effect-ts">Why Effect-TS?</h2>\n<p>Traditional <code>TypeScript</code> error handling relies on <code>try/catch</code> blocks and\n<code>Promise</code> chains, which lose type information at every boundary. <code>Effect-TS</code>\nencodes errors and requirements directly in the function signature, making it\nimpossible to accidentally swallow an error or forget a dependency.</p>\n<p>Every service in the <code>Wind</code> UI layer returns an <code>Effect&#x3C;Result, AppError, Env></code>.\nThe three type parameters represent:</p>\n<ol>\n<li><strong>Result</strong> - the success value</li>\n<li><strong>AppError</strong> - the union of all errors that can occur</li>\n<li><strong>Env</strong> - the services this Effect depends on</li>\n</ol>\n<p>This makes refactoring safe: the compiler tells you exactly what changed at\nevery call site. When you rename a service tag or change its interface, every\nconsuming Effect fails to compile.</p>\n<h2 id="the-effect-layer-pattern">The Effect Layer Pattern</h2>\n<p>Each service in <code>Wind</code> follows an atomic directory structure:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="text"><code><span class="line"><span>Effect/</span></span>\n<span class="line"><span>  ServiceName/</span></span>\n<span class="line"><span>    Tag.ts      -- Context.Tag for dependency injection</span></span>\n<span class="line"><span>    Interface.ts -- TypeScript interface the service implements</span></span>\n<span class="line"><span>    Implementation.ts -- Concrete implementation using Effect primitives</span></span>\n<span class="line"><span>    Layer.ts    -- Effect Layer that binds Interface to Implementation</span></span>\n<span class="line"><span>    Type.ts     -- Shared types, error definitions</span></span></code></pre>\n<p>These compose into <code>TauriLiveLayer</code>, <code>ElectronLiveLayer</code>, and <code>TestLayer</code>\nstacks. In production, <code>Wind</code> uses <code>TauriLiveLayer</code>, which routes service calls\nthrough <code>Tauri</code> commands into <code>Mountain</code>’s Rust handlers. For testing,\n<code>TestLayer</code> provides mock implementations. The consuming code never knows the\ndifference.</p>\n<h2 id="how-it-powers-the-extension-host-cocoon">How It Powers the Extension Host (Cocoon)</h2>\n<p>The <code>Cocoon</code> element runs <code>VS Code</code> extensions inside an Effect-managed runtime.\nEach extension lifecycle hook (<code>activate</code>, <code>deactivate</code>) is modeled as an Effect\nprogram with explicit error types and resource cleanup via <code>Scope</code>.</p>\n<p>The core architecture:</p>\n<ol>\n<li>\n<p><strong>Bootstrap</strong> (<code>Bootstrap/Implementation/Cocoon/Main.ts</code>) composes all\nEffect-TS layers, establishes the gRPC connection to <code>Mountain</code>, and starts\nthe extension host services.</p>\n</li>\n<li>\n<p><strong>Module Interception</strong> (<code>Effect/Module/Interceptor.ts</code>) patches both CJS\n<code>require()</code> and ESM <code>import</code> statements so that calls to the <code>vscode</code> module\nare correctly sandboxed and routed to the appropriate, extension-specific API\ninstance.</p>\n</li>\n<li>\n<p><strong>API Factory</strong> (<code>Services/API/Factory/Service.ts</code>) constructs the <code>vscode</code>\nAPI object that extensions receive. Every method call is wired through the\nEffect-TS service stack, not through raw callbacks.</p>\n</li>\n<li>\n<p><strong>Extension Host Service</strong> (<code>Services/Extension/Host/Service.ts</code>) manages\nextension activation, lifecycle, and supervision. One extension crashing does\nnot take down the rest.</p>\n</li>\n</ol>\n<p>All of this runs under <code>Effect-TS</code>’s structured concurrency model. Each\nextension runs in its own supervised fiber with <code>Scope</code>-based resource\nmanagement — file handles, network connections, and spawned processes are all\ntracked and cleaned up on extension deactivation or failure.</p>\n<h2 id="grpc-integration-via-vine">gRPC Integration via Vine</h2>\n<p>Communication between <code>Cocoon</code> and <code>Mountain</code> happens over the <code>Vine</code> gRPC\nprotocol defined in <code>.proto</code> files. <code>Cocoon</code> wraps the gRPC client in Effect-TS\nservices:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="typescript"><code><span class="line"><span style="color:#6A737D">// Services/Mountain/gRPC/Client.ts -- Effect-wrapped gRPC operations</span></span>\n<span class="line"><span style="color:#F97583">interface</span><span style="color:#B392F0"> MountainClient</span><span style="color:#E1E4E8"> {</span></span>\n<span class="line"><span style="color:#FFAB70">	window</span><span style="color:#F97583">:</span><span style="color:#B392F0"> WindowOperations</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"><span style="color:#FFAB70">	workspace</span><span style="color:#F97583">:</span><span style="color:#B392F0"> WorkspaceOperations</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"><span style="color:#FFAB70">	commands</span><span style="color:#F97583">:</span><span style="color:#B392F0"> CommandOperations</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"><span style="color:#FFAB70">	secrets</span><span style="color:#F97583">:</span><span style="color:#B392F0"> SecretStorageOperations</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>\n<p>The <code>Services/Mountain/gRPC/Client.ts</code> module provides Effect-wrapped methods\nfor window, workspace, command, and secret storage calls. The gRPC server\n(<code>Services/gRPC/Server/Service.ts</code>) implements the Vine protocol with\nbidirectional streaming for real-time event communication.</p>\n<h2 id="process-hardening">Process Hardening</h2>\n<p><code>Cocoon</code> applies process-level hardening before any other code runs:</p>\n<ul>\n<li>Patches <code>process.exit</code> to prevent extensions from killing the host</li>\n<li>Handles uncaught exceptions and unhandled promise rejections</li>\n<li>Pipes logs to the host <code>Mountain</code> process</li>\n<li>Automatically terminates if the parent <code>Mountain</code> process exits</li>\n</ul>\n<p>This is the <code>PatchProcess/</code> module, the first thing that runs on bootstrap.</p>\n<h2 id="structured-concurrency">Structured Concurrency</h2>\n<p><code>Effect-TS</code> provides <code>Effect.fork</code>, <code>Effect.race</code>, and <code>Effect.timeout</code>\nprimitives that integrate with the <code>Echo</code> work-stealing executor in the Rust\nlayer, giving the editor predictable resource usage even under heavy extension\nload.</p>\n<p>The <code>Echo</code> element in <code>Land/Element/Echo</code> provides a work-stealing task\nscheduler for bounded background work. In the TypeScript layer, <code>Effect.fork</code>\ncreates fibers that the scheduler can preempt. This replaces the traditional\nNode.js event-loop model where one hung Promise blocks every other extension.</p>\n<h2 id="typed-ipc-two-layers">Typed IPC: Two Layers</h2>\n<p>The editor has two independent IPC paths, each typed differently:</p>\n<ul>\n<li>\n<p><strong>Tauri IPC</strong> (<code>Wind</code> to <code>Mountain</code>): Typed <code>Tauri</code> commands with Rust\nhandlers in <code>Mountain</code>’s <code>IPC/</code> module. <code>Preload.ts</code> shims the Electron\n<code>ipcRenderer</code> so the VS Code workbench code communicates through Tauri’s\n<code>invoke</code> system instead.</p>\n</li>\n<li>\n<p><strong>Vine gRPC</strong> (<code>Cocoon</code> to <code>Mountain</code>): Strongly-typed protocol described by\n<code>.proto</code> files, with generated Rust and TypeScript stubs. Bidirectional\nstreaming for real-time events like document notifications, language provider\nrequests, terminal I/O, and extension health.</p>\n</li>\n</ul>\n<p>These paths are independent. The website should not describe every service call\nas flowing through one universal bus. Each has its own job and its own error\nsurface.</p>', { headings: 157, localImagePaths: 181, remoteImagePaths: 182, frontmatter: 183, imagePaths: 185 }, [158, 160, 163, 166, 169, 172, 175, 178], { depth: 25, slug: 159, text: 145 }, "effect-ts-architecture-in-the-land-editor", { depth: 29, slug: 161, text: 162 }, "why-effect-ts", "Why Effect-TS?", { depth: 29, slug: 164, text: 165 }, "the-effect-layer-pattern", "The Effect Layer Pattern", { depth: 29, slug: 167, text: 168 }, "how-it-powers-the-extension-host-cocoon", "How It Powers the Extension Host (Cocoon)", { depth: 29, slug: 170, text: 171 }, "grpc-integration-via-vine", "gRPC Integration via Vine", { depth: 29, slug: 173, text: 174 }, "process-hardening", "Process Hardening", { depth: 29, slug: 176, text: 177 }, "structured-concurrency", "Structured Concurrency", { depth: 29, slug: 179, text: 180 }, "typed-ipc-two-layers", "Typed IPC: Two Layers", [], [], { title: 145, summary: 146, publishedAt: 147, tags: 184, author: 15, readTime: 16 }, [10, 149, 150], [], "ExtensionHostDeepDive", { id: 186, data: 188, body: 195, filePath: 196, digest: 197, rendered: 198 }, { title: 189, summary: 190, publishedAt: 8, tags: 191, author: 15, readTime: 194 }, "Extension Host Architecture: Cocoon's Design for Module Interception, API Shims, and Fiber Supervision", "How Cocoon runs VS Code extensions in supervised Effect-TS fibers with module interception and gRPC-backed API shims.", [192, 13, 150, 10, 193], "Extensions", "Modules", 10, "# Extension Host Deep Dive: Cocoon Architecture\n\nVS Code's extension host runs on a single Node.js event loop. One hung Promise\nblocks every other extension. There is no way to cancel an in-flight operation,\nno back-pressure, and no preemption. Cocoon solves this by running each\nextension in its own supervised Effect-TS fiber.\n\n## What Cocoon Is\n\nCocoon is a Node.js sidecar process spawned and managed by Mountain. It provides\na comprehensive VS Code Extension Host API surface so existing extensions run\nunmodified. All communication with Mountain happens over gRPC through the Vine\nprotocol.\n\nThe key distinction: Cocoon does not ask extension authors to change their code.\nExtensions written for VS Code call `require('vscode')` and get back an API\nobject. Cocoon builds that object from Effect-TS services.\n\n## Bootstrap Sequence\n\nThe entry point is `Bootstrap/Implementation/Cocoon/Main.ts`. It composes all\nEffect-TS layers into the master `AppLayer`:\n\n```\nPatchProcess -> Effect/Bootstrap -> IpcProvider -> AppLayer -> ExtensionHost\n```\n\n`PatchProcess` runs first and is non-negotiable. Before any other code executes,\nit:\n\n- Patches `process.exit` so extensions cannot kill the host\n- Captures `SIGTERM`, `SIGINT` for graceful shutdown\n- Redirects `stdout` and `stderr` to Mountain via the established channel\n- Sets up uncaught exception and unhandled rejection handlers\n- Monitors the parent Mountain process PID; if it exits, Cocoon terminates\n  automatically\n\nAfter hardening, `Effect/Bootstrap.ts` coordinates the initialization stages in\norder:\n\n1. Environment detection (OS, Node version, platform data)\n2. Configuration loading\n3. gRPC connection establishment\n4. Module interceptor installation\n5. Extension registry population\n6. Health check setup\n\nWhen the gRPC connection to Mountain's Vine server succeeds, Cocoon sends the\n`$initialHandshake` notification and waits for `initExtensionHost` to return\nwith the initialization payload. That payload contains workspace info, extension\nlists, configuration, and feature flags.\n\n## Module Interception System\n\nWhen an extension calls `require('vscode')`, Cocoon intercepts the module\nrequest before the extension code ever sees the module. This is the core\ncompatibility mechanism.\n\nThe `RequireInterceptor.ts` module patches both CommonJS `require()` and ESM\n`import` at Node.js module resolution level. The interception flow:\n\n1. Extension calls `require('vscode')` or `import 'vscode'`.\n2. The interceptor detects the vscode module pattern.\n3. It requests an API instance from `ApiFactory` for the calling extension.\n4. `ApiFactory` creates an isolated vscode API object wired through Effect-TS\n   services.\n5. The interceptor returns this instance to the extension.\n\nEach extension gets its own API object. One extension's API calls do not\ninterfere with another's state. This instance isolation prevents the global\nstate pollution that plagues shared extension hosts.\n\nThe VS Code upstream uses a similar pattern in\n`src/vs/workbench/api/common/extHostRequireInterceptor.ts`. Cocoon's\nimplementation extends it with ESM interception, which VS Code does not support.\n\n## API Factory and Service Shims\n\n`Services/API/Factory/Service.ts` constructs the vscode API object that\nextensions receive. Every method is wired through the Effect-TS service stack,\nnot through raw callbacks.\n\nThe service layer implements VS Code extension host interfaces as Effect-TS\n`Layer`s:\n\n| VS Code Service     | Cocoon Implementation | VS Code Upstream Reference |\n| ------------------- | --------------------- | -------------------------- |\n| `vscode.commands`   | `CommandsProvider`    | `IExtHostCommands`         |\n| `vscode.workspace`  | `WorkspaceProvider`   | `IExtHostWorkspace`        |\n| `vscode.window`     | `WindowProvider`      | `IExtHostWindow`           |\n| `vscode.extensions` | `ExtensionProvider`   | `IExtHostExtensions`       |\n| `vscode.languages`  | `LanguageProvider`    | `IExtHostLanguages`        |\n\nEach provider is an Effect-TS service. This means every API call carries typed\nerror information, resource cleanup guarantees, and dependency declarations in\nits function signature. An API method cannot silently swallow an error or forget\na dependency.\n\n## Walking an API Call\n\nWhen an extension calls `vscode.window.showInformationMessage(\"Hello\")`:\n\n1. The extension's `vscode` object is the instance created by `ApiFactory`.\n2. `showInformationMessage` is implemented by `WindowProvider`.\n3. `WindowProvider` creates an Effect that serializes the message into a\n   gRPC-compatible format and sends a `ShowMessageRequest` to Mountain via\n   `Services/Mountain/gRPC/Client.ts`.\n4. The request flows through the bidirectional gRPC channel to Mountain's Vine\n   server.\n5. Mountain's `Track` dispatcher routes the request to the native dialog\n   handler.\n6. Mountain displays a native OS information dialog and waits for user\n   interaction.\n7. The user's choice flows back: Mountain -> gRPC -> Cocoon -> WindowProvider ->\n   extension promise resolution.\n\nTotal latency for this flow breaks down as approximately 0.42ms of processing\noverhead plus network latency and Mountain processing time.\n\n## Extension Host Service and Fiber Supervision\n\n`Services/Extension/Host/Service.ts` manages extension activation and lifecycle.\nThe key design decision: each extension runs in its own supervised fiber.\n\nEffect-TS fibers are lightweight, preemptible execution units managed by the\nruntime scheduler. When an extension is activated:\n\n1. A new `Scope` is created for that extension.\n2. The extension's `activate()` function runs inside this scope as a supervised\n   Effect.\n3. All resources acquired during activation (file handles, network connections,\n   spawned processes) are registered with the scope.\n\nIf an extension crashes:\n\n1. The fiber is interrupted.\n2. The `Scope` runs deterministic cleanup: every registered resource is\n   released.\n3. The supervisor logs the error and reports it to Mountain.\n4. Other extensions continue running unaffected.\n\nThis is fundamentally different from the VS Code model where unhandled\nexceptions in one extension can cause cascading failures.\n\n## gRPC Communication: Client and Server\n\nCocoon maintains a dual gRPC presence:\n\n**`Services/Mountain/gRPC/Client.ts`** provides Effect-wrapped methods for\ncalling Mountain back. The interface groups operations into logical categories:\n\n- `window` operations (dialogs, notifications, status bar)\n- `workspace` operations (file system, configuration, workspace folders)\n- `commands` operations (registration, execution, keybindings)\n- `secrets` operations (secure credential storage)\n\n**`Services/gRPC/Server/Service.ts`** implements Cocoon's side of the Vine\nprotocol. It handles incoming requests from Mountain with bidirectional\nstreaming for real-time event communication. Incoming requests are delegated to\ndomain-specific handler modules: `ExtensionHostHandler`,\n`LanguageProviderHandler`, `NotificationHandler`, and `RequestRoutingHandler`.\n\nThe protocol supports request batching (aggregating small requests into batched\noperations), connection resiliency (automatic reconnection with exponential\nbackoff), and request queuing during connection outages.\n\n## Type Conversion\n\nVS Code types like `Range`, `Position`, `URI`, `Dialog`, `TreeView`, `Webview`,\n`Task`, and `WorkspaceEdit` cannot be serialized naively across a process\nboundary. The `TypeConverter/` module provides pure conversion functions that\ntransform each rich type into a plain DTO suitable for gRPC transport.\n\nEach conversion path is documented and tested. The converter organizes itself by\nfeature domain: `Main` (URI, Range), `Dialog`, `TreeView`, `Webview`, `Task`,\nand `WorkspaceEdit`.\n\n## Code Generation Pipeline\n\nCocoon includes a `Codegen/` pipeline that walks the VS Code extension-host\nsource subtree and emits `IExtHost*Upstream` schemas grounded in real upstream\ndefinitions. This reuses Wind's extractor and resolver infrastructure. The\ngenerated schemas ensure that Cocoon's API shims stay aligned with actual VS\nCode extension-host types, reducing drift as VS Code evolves.\n\nThis is a practical response to a real problem: VS Code's extension host API\nchanges between versions. Without automated schema generation, manual shim\nupdates would lag behind and introduce compatibility gaps.\n\n## What Works and What Does Not\n\nSource-backed compatibility today:\n\n- Core extension activation and lifecycle\n- Commands, workspace, window, terminals, webviews, language providers,\n  diagnostics, output channels, and document events\n- ESM module interception (beyond what VS Code's own extension host supports)\n- Typed error handling and resource cleanup through Effect-TS fibers\n\nNot yet implemented:\n\n- `vscode.lm.*` (language model API)\n- `vscode.chat.*` (chat API)\n- `vscode.notebook.*` (notebook API)\n- `vscode.tests.*` (testing API)\n\nExtensions using these APIs activate but the specific features silently no-op. A\npublic extension validation matrix naming specific extensions, versions,\nplatforms, and exercised APIs is pending.\n\n## The Architecture Decision: Effect-TS Over Traditional OOP\n\nVS Code's extension host uses traditional object-oriented service instantiation.\nCocoon uses Effect-TS Layer composition. The difference is not cosmetic:\n\n| Aspect               | VS Code Approach   | Cocoon Approach    | Result                     |\n| -------------------- | ------------------ | ------------------ | -------------------------- |\n| Dependency injection | Service collection | Effect-TS Layers   | Compile-time guarantees    |\n| Error handling       | try/catch          | Effect error types | No swallowed errors        |\n| Async operations     | Promises           | Effect pipelines   | Cancellable, preemptible   |\n| Service composition  | Instantiation      | Layer composition  | Deterministic availability |\n\nThe Effect-TS approach shifts correctness from runtime testing to compile-time\nverification. When a service tag is renamed or an interface changes, the\ncompiler fails at every consuming site. This is expensive to adopt but pays for\nitself in refactoring safety.", "Source/Content/blog/ExtensionHostDeepDive.md", "aeff6c4dd2f2fbf9", { html: 199, metadata: 200 }, `<h1 id="extension-host-deep-dive-cocoon-architecture">Extension Host Deep Dive: Cocoon Architecture</h1>
<p>VS Code’s extension host runs on a single Node.js event loop. One hung Promise
blocks every other extension. There is no way to cancel an in-flight operation,
no back-pressure, and no preemption. Cocoon solves this by running each
extension in its own supervised Effect-TS fiber.</p>
<h2 id="what-cocoon-is">What Cocoon Is</h2>
<p>Cocoon is a Node.js sidecar process spawned and managed by Mountain. It provides
a comprehensive VS Code Extension Host API surface so existing extensions run
unmodified. All communication with Mountain happens over gRPC through the Vine
protocol.</p>
<p>The key distinction: Cocoon does not ask extension authors to change their code.
Extensions written for VS Code call <code>require('vscode')</code> and get back an API
object. Cocoon builds that object from Effect-TS services.</p>
<h2 id="bootstrap-sequence">Bootstrap Sequence</h2>
<p>The entry point is <code>Bootstrap/Implementation/Cocoon/Main.ts</code>. It composes all
Effect-TS layers into the master <code>AppLayer</code>:</p>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>PatchProcess -> Effect/Bootstrap -> IpcProvider -> AppLayer -> ExtensionHost</span></span></code></pre>
<p><code>PatchProcess</code> runs first and is non-negotiable. Before any other code executes,
it:</p>
<ul>
<li>Patches <code>process.exit</code> so extensions cannot kill the host</li>
<li>Captures <code>SIGTERM</code>, <code>SIGINT</code> for graceful shutdown</li>
<li>Redirects <code>stdout</code> and <code>stderr</code> to Mountain via the established channel</li>
<li>Sets up uncaught exception and unhandled rejection handlers</li>
<li>Monitors the parent Mountain process PID; if it exits, Cocoon terminates
automatically</li>
</ul>
<p>After hardening, <code>Effect/Bootstrap.ts</code> coordinates the initialization stages in
order:</p>
<ol>
<li>Environment detection (OS, Node version, platform data)</li>
<li>Configuration loading</li>
<li>gRPC connection establishment</li>
<li>Module interceptor installation</li>
<li>Extension registry population</li>
<li>Health check setup</li>
</ol>
<p>When the gRPC connection to Mountain’s Vine server succeeds, Cocoon sends the
<code>$initialHandshake</code> notification and waits for <code>initExtensionHost</code> to return
with the initialization payload. That payload contains workspace info, extension
lists, configuration, and feature flags.</p>
<h2 id="module-interception-system">Module Interception System</h2>
<p>When an extension calls <code>require('vscode')</code>, Cocoon intercepts the module
request before the extension code ever sees the module. This is the core
compatibility mechanism.</p>
<p>The <code>RequireInterceptor.ts</code> module patches both CommonJS <code>require()</code> and ESM
<code>import</code> at Node.js module resolution level. The interception flow:</p>
<ol>
<li>Extension calls <code>require('vscode')</code> or <code>import 'vscode'</code>.</li>
<li>The interceptor detects the vscode module pattern.</li>
<li>It requests an API instance from <code>ApiFactory</code> for the calling extension.</li>
<li><code>ApiFactory</code> creates an isolated vscode API object wired through Effect-TS
services.</li>
<li>The interceptor returns this instance to the extension.</li>
</ol>
<p>Each extension gets its own API object. One extension’s API calls do not
interfere with another’s state. This instance isolation prevents the global
state pollution that plagues shared extension hosts.</p>
<p>The VS Code upstream uses a similar pattern in
<code>src/vs/workbench/api/common/extHostRequireInterceptor.ts</code>. Cocoon’s
implementation extends it with ESM interception, which VS Code does not support.</p>
<h2 id="api-factory-and-service-shims">API Factory and Service Shims</h2>
<p><code>Services/API/Factory/Service.ts</code> constructs the vscode API object that
extensions receive. Every method is wired through the Effect-TS service stack,
not through raw callbacks.</p>
<p>The service layer implements VS Code extension host interfaces as Effect-TS
<code>Layer</code>s:</p>



































<table><thead><tr><th>VS Code Service</th><th>Cocoon Implementation</th><th>VS Code Upstream Reference</th></tr></thead><tbody><tr><td><code>vscode.commands</code></td><td><code>CommandsProvider</code></td><td><code>IExtHostCommands</code></td></tr><tr><td><code>vscode.workspace</code></td><td><code>WorkspaceProvider</code></td><td><code>IExtHostWorkspace</code></td></tr><tr><td><code>vscode.window</code></td><td><code>WindowProvider</code></td><td><code>IExtHostWindow</code></td></tr><tr><td><code>vscode.extensions</code></td><td><code>ExtensionProvider</code></td><td><code>IExtHostExtensions</code></td></tr><tr><td><code>vscode.languages</code></td><td><code>LanguageProvider</code></td><td><code>IExtHostLanguages</code></td></tr></tbody></table>
<p>Each provider is an Effect-TS service. This means every API call carries typed
error information, resource cleanup guarantees, and dependency declarations in
its function signature. An API method cannot silently swallow an error or forget
a dependency.</p>
<h2 id="walking-an-api-call">Walking an API Call</h2>
<p>When an extension calls <code>vscode.window.showInformationMessage("Hello")</code>:</p>
<ol>
<li>The extension’s <code>vscode</code> object is the instance created by <code>ApiFactory</code>.</li>
<li><code>showInformationMessage</code> is implemented by <code>WindowProvider</code>.</li>
<li><code>WindowProvider</code> creates an Effect that serializes the message into a
gRPC-compatible format and sends a <code>ShowMessageRequest</code> to Mountain via
<code>Services/Mountain/gRPC/Client.ts</code>.</li>
<li>The request flows through the bidirectional gRPC channel to Mountain’s Vine
server.</li>
<li>Mountain’s <code>Track</code> dispatcher routes the request to the native dialog
handler.</li>
<li>Mountain displays a native OS information dialog and waits for user
interaction.</li>
<li>The user’s choice flows back: Mountain -> gRPC -> Cocoon -> WindowProvider ->
extension promise resolution.</li>
</ol>
<p>Total latency for this flow breaks down as approximately 0.42ms of processing
overhead plus network latency and Mountain processing time.</p>
<h2 id="extension-host-service-and-fiber-supervision">Extension Host Service and Fiber Supervision</h2>
<p><code>Services/Extension/Host/Service.ts</code> manages extension activation and lifecycle.
The key design decision: each extension runs in its own supervised fiber.</p>
<p>Effect-TS fibers are lightweight, preemptible execution units managed by the
runtime scheduler. When an extension is activated:</p>
<ol>
<li>A new <code>Scope</code> is created for that extension.</li>
<li>The extension’s <code>activate()</code> function runs inside this scope as a supervised
Effect.</li>
<li>All resources acquired during activation (file handles, network connections,
spawned processes) are registered with the scope.</li>
</ol>
<p>If an extension crashes:</p>
<ol>
<li>The fiber is interrupted.</li>
<li>The <code>Scope</code> runs deterministic cleanup: every registered resource is
released.</li>
<li>The supervisor logs the error and reports it to Mountain.</li>
<li>Other extensions continue running unaffected.</li>
</ol>
<p>This is fundamentally different from the VS Code model where unhandled
exceptions in one extension can cause cascading failures.</p>
<h2 id="grpc-communication-client-and-server">gRPC Communication: Client and Server</h2>
<p>Cocoon maintains a dual gRPC presence:</p>
<p><strong><code>Services/Mountain/gRPC/Client.ts</code></strong> provides Effect-wrapped methods for
calling Mountain back. The interface groups operations into logical categories:</p>
<ul>
<li><code>window</code> operations (dialogs, notifications, status bar)</li>
<li><code>workspace</code> operations (file system, configuration, workspace folders)</li>
<li><code>commands</code> operations (registration, execution, keybindings)</li>
<li><code>secrets</code> operations (secure credential storage)</li>
</ul>
<p><strong><code>Services/gRPC/Server/Service.ts</code></strong> implements Cocoon’s side of the Vine
protocol. It handles incoming requests from Mountain with bidirectional
streaming for real-time event communication. Incoming requests are delegated to
domain-specific handler modules: <code>ExtensionHostHandler</code>,
<code>LanguageProviderHandler</code>, <code>NotificationHandler</code>, and <code>RequestRoutingHandler</code>.</p>
<p>The protocol supports request batching (aggregating small requests into batched
operations), connection resiliency (automatic reconnection with exponential
backoff), and request queuing during connection outages.</p>
<h2 id="type-conversion">Type Conversion</h2>
<p>VS Code types like <code>Range</code>, <code>Position</code>, <code>URI</code>, <code>Dialog</code>, <code>TreeView</code>, <code>Webview</code>,
<code>Task</code>, and <code>WorkspaceEdit</code> cannot be serialized naively across a process
boundary. The <code>TypeConverter/</code> module provides pure conversion functions that
transform each rich type into a plain DTO suitable for gRPC transport.</p>
<p>Each conversion path is documented and tested. The converter organizes itself by
feature domain: <code>Main</code> (URI, Range), <code>Dialog</code>, <code>TreeView</code>, <code>Webview</code>, <code>Task</code>,
and <code>WorkspaceEdit</code>.</p>
<h2 id="code-generation-pipeline">Code Generation Pipeline</h2>
<p>Cocoon includes a <code>Codegen/</code> pipeline that walks the VS Code extension-host
source subtree and emits <code>IExtHost*Upstream</code> schemas grounded in real upstream
definitions. This reuses Wind’s extractor and resolver infrastructure. The
generated schemas ensure that Cocoon’s API shims stay aligned with actual VS
Code extension-host types, reducing drift as VS Code evolves.</p>
<p>This is a practical response to a real problem: VS Code’s extension host API
changes between versions. Without automated schema generation, manual shim
updates would lag behind and introduce compatibility gaps.</p>
<h2 id="what-works-and-what-does-not">What Works and What Does Not</h2>
<p>Source-backed compatibility today:</p>
<ul>
<li>Core extension activation and lifecycle</li>
<li>Commands, workspace, window, terminals, webviews, language providers,
diagnostics, output channels, and document events</li>
<li>ESM module interception (beyond what VS Code’s own extension host supports)</li>
<li>Typed error handling and resource cleanup through Effect-TS fibers</li>
</ul>
<p>Not yet implemented:</p>
<ul>
<li><code>vscode.lm.*</code> (language model API)</li>
<li><code>vscode.chat.*</code> (chat API)</li>
<li><code>vscode.notebook.*</code> (notebook API)</li>
<li><code>vscode.tests.*</code> (testing API)</li>
</ul>
<p>Extensions using these APIs activate but the specific features silently no-op. A
public extension validation matrix naming specific extensions, versions,
platforms, and exercised APIs is pending.</p>
<h2 id="the-architecture-decision-effect-ts-over-traditional-oop">The Architecture Decision: Effect-TS Over Traditional OOP</h2>
<p>VS Code’s extension host uses traditional object-oriented service instantiation.
Cocoon uses Effect-TS Layer composition. The difference is not cosmetic:</p>



































<table><thead><tr><th>Aspect</th><th>VS Code Approach</th><th>Cocoon Approach</th><th>Result</th></tr></thead><tbody><tr><td>Dependency injection</td><td>Service collection</td><td>Effect-TS Layers</td><td>Compile-time guarantees</td></tr><tr><td>Error handling</td><td>try/catch</td><td>Effect error types</td><td>No swallowed errors</td></tr><tr><td>Async operations</td><td>Promises</td><td>Effect pipelines</td><td>Cancellable, preemptible</td></tr><tr><td>Service composition</td><td>Instantiation</td><td>Layer composition</td><td>Deterministic availability</td></tr></tbody></table>
<p>The Effect-TS approach shifts correctness from runtime testing to compile-time
verification. When a service tag is renamed or an interface changes, the
compiler fails at every consuming site. This is expensive to adopt but pays for
itself in refactoring safety.</p>`, { headings: 201, localImagePaths: 238, remoteImagePaths: 239, frontmatter: 240, imagePaths: 242 }, [202, 205, 208, 211, 214, 217, 220, 223, 226, 229, 232, 235], { depth: 25, slug: 203, text: 204 }, "extension-host-deep-dive-cocoon-architecture", "Extension Host Deep Dive: Cocoon Architecture", { depth: 29, slug: 206, text: 207 }, "what-cocoon-is", "What Cocoon Is", { depth: 29, slug: 209, text: 210 }, "bootstrap-sequence", "Bootstrap Sequence", { depth: 29, slug: 212, text: 213 }, "module-interception-system", "Module Interception System", { depth: 29, slug: 215, text: 216 }, "api-factory-and-service-shims", "API Factory and Service Shims", { depth: 29, slug: 218, text: 219 }, "walking-an-api-call", "Walking an API Call", { depth: 29, slug: 221, text: 222 }, "extension-host-service-and-fiber-supervision", "Extension Host Service and Fiber Supervision", { depth: 29, slug: 224, text: 225 }, "grpc-communication-client-and-server", "gRPC Communication: Client and Server", { depth: 29, slug: 227, text: 228 }, "type-conversion", "Type Conversion", { depth: 29, slug: 230, text: 231 }, "code-generation-pipeline", "Code Generation Pipeline", { depth: 29, slug: 233, text: 234 }, "what-works-and-what-does-not", "What Works and What Does Not", { depth: 29, slug: 236, text: 237 }, "the-architecture-decision-effect-ts-over-traditional-oop", "The Architecture Decision: Effect-TS Over Traditional OOP", [], [], { title: 189, summary: 190, publishedAt: 8, tags: 241, author: 15, readTime: 194 }, [192, 13, 150, 10, 193], [], "LocalFirstPhilosophy", { id: 243, data: 245, body: 253, filePath: 254, digest: 255, rendered: 256 }, { title: 246, summary: 247, publishedAt: 248, tags: 249, author: 15, readTime: 252 }, "Local-First Philosophy", "How Land keeps the editor path local-first while sync and portal features stay explicit.", "2026-04-02", [250, 10, 251], "Local-First", "Privacy", 5, "# Local-First Philosophy\n\nLand's editor direction is local-first: the core editor should remain useful\nwithout a portal account or cloud sync. That is different from saying every\nplanned sync feature is already shipped.\n\n## What Local-First Means architecturally\n\n\"Local-first\" is not a marketing term here. It is a design constraint that\nshapes every architectural decision:\n\n- **Mountain** owns native file, terminal, process, clipboard, IPC, and\n  extension integration paths -- all local OS operations, no network dependency.\n- **Cocoon** runs installed `VS Code` extensions without rewriting their source\n  when the APIs they use are implemented.\n- **Air** contains background service code for updates, downloads,\n  authentication, indexing, health, security, and `Vine` IPC.\n\nThe editor opens a native window (`Tauri`), loads the workbench UI (`Sky` in the\nOS WebView), starts the extension host (`Cocoon` over `Vine` gRPC), and is fully\nfunctional. No account, no network, no cloud round-trip required to open a file,\nrun a terminal, or activate extensions.\n\n## The Network Boundary\n\n`Mist` handles DNS isolation, resolver, and secure service-boundary code. It can\nbe implemented either as a native module within `Mountain` or as a separate\nsidecar. The point is intentional: network-dependent features are isolated at\nthe service boundary. Core editing never depends on them.\n\n`Air`'s background services (updates, downloads, auth, indexing, health) are\nprofile-dependent. Whether Air is part of a given build depends on the selected\nprofile and feature flags. The default development configuration makes the\neditor functional without any remote service.\n\n## Source-Backed Today\n\nHere is what works right now without any network dependency:\n\n- `Mountain` provides native-speed file I/O, process management (PTY for\n  integrated terminal), clipboard access, search, and configuration persistence.\n- `Cocoon` activates extensions from disk. The `vscode` API object is built with\n  Effect-TS, with privileged calls proxied to `Mountain` via gRPC.\n- `Wind` runs the workbench services in the Tauri WebView, routing UI requests\n  through typed Tauri commands into Rust handlers.\n- `Sky` renders the UI components driven by `Wind`'s state.\n- `Rest` bundles the VS Code platform code from the `Dependency/Editor`\n  submodule for `Cocoon` to consume at runtime.\n- `Output` contains the bundled JavaScript artifacts that `Cocoon` loads.\n- `Echo` provides the work-stealing task scheduler for bounded background work.\n\n## Coming Soon\n\nThe following features require network and are explicitly separated from the\nlocal-first core:\n\n- Browser-to-daemon portal control\n- Peer or cloud sync flows with documented transport and encryption\n- Self-hosted marketplace and registry configuration\n- Public release artifacts that name exactly which services are enabled\n\n## Why This Architecture Matters\n\nThe local-first constraint forces honest answers about what the editor\nfundamentally is. If the editor cannot function without a network connection, it\nis not a local editor -- it is a terminal into someone else's machine. By\nbuilding the network-dependent features as optional layers (`Air`, `Mist`,\nportal services) on top of a fully local core (`Mountain`, `Cocoon`, `Wind`,\n`Sky`), Land keeps the promise: the editor is yours, on your machine, regardless\nof network state.\n\nThe website should keep that constraint separate from unfinished product\nfeatures. Local-first is the design constraint. Portal and cloud sync surfaces\nshould be labeled as coming soon until the user-facing flow is complete.", "Source/Content/blog/LocalFirstPhilosophy.md", "66f37d00fd593191", { html: 257, metadata: 258 }, '<h1 id="local-first-philosophy">Local-First Philosophy</h1>\n<p>Land’s editor direction is local-first: the core editor should remain useful\nwithout a portal account or cloud sync. That is different from saying every\nplanned sync feature is already shipped.</p>\n<h2 id="what-local-first-means-architecturally">What Local-First Means architecturally</h2>\n<p>“Local-first” is not a marketing term here. It is a design constraint that\nshapes every architectural decision:</p>\n<ul>\n<li><strong>Mountain</strong> owns native file, terminal, process, clipboard, IPC, and\nextension integration paths — all local OS operations, no network dependency.</li>\n<li><strong>Cocoon</strong> runs installed <code>VS Code</code> extensions without rewriting their source\nwhen the APIs they use are implemented.</li>\n<li><strong>Air</strong> contains background service code for updates, downloads,\nauthentication, indexing, health, security, and <code>Vine</code> IPC.</li>\n</ul>\n<p>The editor opens a native window (<code>Tauri</code>), loads the workbench UI (<code>Sky</code> in the\nOS WebView), starts the extension host (<code>Cocoon</code> over <code>Vine</code> gRPC), and is fully\nfunctional. No account, no network, no cloud round-trip required to open a file,\nrun a terminal, or activate extensions.</p>\n<h2 id="the-network-boundary">The Network Boundary</h2>\n<p><code>Mist</code> handles DNS isolation, resolver, and secure service-boundary code. It can\nbe implemented either as a native module within <code>Mountain</code> or as a separate\nsidecar. The point is intentional: network-dependent features are isolated at\nthe service boundary. Core editing never depends on them.</p>\n<p><code>Air</code>’s background services (updates, downloads, auth, indexing, health) are\nprofile-dependent. Whether Air is part of a given build depends on the selected\nprofile and feature flags. The default development configuration makes the\neditor functional without any remote service.</p>\n<h2 id="source-backed-today">Source-Backed Today</h2>\n<p>Here is what works right now without any network dependency:</p>\n<ul>\n<li><code>Mountain</code> provides native-speed file I/O, process management (PTY for\nintegrated terminal), clipboard access, search, and configuration persistence.</li>\n<li><code>Cocoon</code> activates extensions from disk. The <code>vscode</code> API object is built with\nEffect-TS, with privileged calls proxied to <code>Mountain</code> via gRPC.</li>\n<li><code>Wind</code> runs the workbench services in the Tauri WebView, routing UI requests\nthrough typed Tauri commands into Rust handlers.</li>\n<li><code>Sky</code> renders the UI components driven by <code>Wind</code>’s state.</li>\n<li><code>Rest</code> bundles the VS Code platform code from the <code>Dependency/Editor</code>\nsubmodule for <code>Cocoon</code> to consume at runtime.</li>\n<li><code>Output</code> contains the bundled JavaScript artifacts that <code>Cocoon</code> loads.</li>\n<li><code>Echo</code> provides the work-stealing task scheduler for bounded background work.</li>\n</ul>\n<h2 id="coming-soon">Coming Soon</h2>\n<p>The following features require network and are explicitly separated from the\nlocal-first core:</p>\n<ul>\n<li>Browser-to-daemon portal control</li>\n<li>Peer or cloud sync flows with documented transport and encryption</li>\n<li>Self-hosted marketplace and registry configuration</li>\n<li>Public release artifacts that name exactly which services are enabled</li>\n</ul>\n<h2 id="why-this-architecture-matters">Why This Architecture Matters</h2>\n<p>The local-first constraint forces honest answers about what the editor\nfundamentally is. If the editor cannot function without a network connection, it\nis not a local editor — it is a terminal into someone else’s machine. By\nbuilding the network-dependent features as optional layers (<code>Air</code>, <code>Mist</code>,\nportal services) on top of a fully local core (<code>Mountain</code>, <code>Cocoon</code>, <code>Wind</code>,\n<code>Sky</code>), Land keeps the promise: the editor is yours, on your machine, regardless\nof network state.</p>\n<p>The website should keep that constraint separate from unfinished product\nfeatures. Local-first is the design constraint. Portal and cloud sync surfaces\nshould be labeled as coming soon until the user-facing flow is complete.</p>', { headings: 259, localImagePaths: 277, remoteImagePaths: 278, frontmatter: 279, imagePaths: 281 }, [260, 262, 265, 268, 271, 274], { depth: 25, slug: 261, text: 246 }, "local-first-philosophy", { depth: 29, slug: 263, text: 264 }, "what-local-first-means-architecturally", "What Local-First Means architecturally", { depth: 29, slug: 266, text: 267 }, "the-network-boundary", "The Network Boundary", { depth: 29, slug: 269, text: 270 }, "source-backed-today", "Source-Backed Today", { depth: 29, slug: 272, text: 273 }, "coming-soon", "Coming Soon", { depth: 29, slug: 275, text: 276 }, "why-this-architecture-matters", "Why This Architecture Matters", [], [], { title: 246, summary: 247, publishedAt: 248, tags: 280, author: 15, readTime: 252 }, [250, 10, 251], [], "TauriDesktopIntegration", { id: 282, data: 284, body: 291, filePath: 292, digest: 293, rendered: 294 }, { title: 285, summary: 286, publishedAt: 248, tags: 287, author: 15, readTime: 252 }, "Tauri Desktop Integration in Land", "How Mountain uses Tauri for a native editor shell without bundling Chromium.", [288, 289, 290, 10], "Tauri", "Rust", "Desktop", "# Tauri Desktop Integration in Land\n\nCode Editor Land uses `Tauri` through the `Mountain` element. `Tauri` lets the\neditor use the operating system WebView instead of bundling Chromium, while\n`Mountain` keeps native services in `Rust`.\n\nThe source-backed claim is architectural: no bundled Chromium in the `Tauri`\ndesktop path, native `Rust` service code in `Mountain`, and configured targets\nfor multiple operating systems. Public size, memory, and startup comparisons\nshould come from a repeatable benchmark.\n\n## The Electron Problem\n\nVS Code with a medium project uses 500 MB to 1.5 GB of RAM. Three open windows\nmeans three Chromium renderer processes, each carrying a full heap. Every OS\ninteraction crosses a serialized JSON IPC pipe. Where Electron takes 200 ms to\nopen a dialog, Mountain takes 2.\n\nTauri replaces the Electron shell with a native binary that delegates rendering\nto the OS WebView: WKWebView on macOS, WebView2 on Windows, WebKitGTK on Linux.\nThe result is a smaller binary, less memory, and direct FFI access to native\noperations.\n\n## Mountain: The Tauri Application\n\n`Mountain` implements the Tauri application lifecycle. Its startup path is\norganized around build, registration, initialization, and services:\n\n```text\nmain.rs -> Binary::Main -> Build -> Register -> Initialize -> Services\n```\n\n- **Build**: Configures the Tauri application with window settings, custom\n  schemes (`land:`, `vscode-file:`, `vscode-webview:`), and security policies.\n- **Register**: Wires Tauri commands and IPC handlers for filesystem, terminal,\n  clipboard, search, configuration, and workbench events.\n- **Initialize**: Prepares runtime state -- the thread-safe `ApplicationState`\n  struct that serves as the single source of truth.\n- **Services**: Starts the `Vine` gRPC server and spawns `Cocoon` before the\n  Tauri event loop takes over the desktop session.\n\n## ApplicationRunTime and Effects\n\n`Mountain` implements the abstract service traits from `Common` through the\n`ActionEffect` system. Business logic is described as declarative, composable\neffects, executed by a central `ApplicationRunTime`. This replaces the typical\nasync/await sprawl with a structured execution model where error channels are\ntyped and resource cleanup is deterministic.\n\nKey `Mountain` modules:\n\n| Module                | Role                                                                                            |\n| --------------------- | ----------------------------------------------------------------------------------------------- |\n| `ApplicationRunTime`  | Effect execution engine (ApplicationRunTime, Execute, graceful Shutdown)                        |\n| `ApplicationState`    | Thread-safe state machine with DTOs, persistence, recovery, feature state                       |\n| `Environment/*`       | Concrete implementations of Common provider traits (filesystem, documents, terminal, etc.)      |\n| `Track`               | Central request dispatcher routing frontend and sidecar commands into ActionEffects             |\n| `Vine/*`              | gRPC IPC layer: server, client, multiplexer, generated protobuf bindings                        |\n| `ExtensionManagement` | Extension discovery, manifest parsing, VSIX installation                                        |\n| `ProcessManagement`   | Sidecar process lifecycle, Node.js binary resolution (nvm, fnm, asdf, volta, homebrew, shipped) |\n| `Update`              | Application self-updating via Tauri bundled updater and optional Air gRPC delegation            |\n\n## IPC: Two Layers, Two Jobs\n\nThe editor has two independent IPC paths:\n\n**Sky and Wind to Mountain - Tauri IPC**\n\n`Sky`'s UI and `Wind` service layer call `Mountain` through Tauri IPC. The\nimplementation lives in `Source/IPC/TauriIPCServer.rs`, including the\n`mountain_ipc_receive_message` and `mountain_ipc_get_status` entry points. This\npath handles native work: files, terminals, clipboard, search, configuration,\nand workbench events.\n\n**Mountain to Cocoon - Vine gRPC**\n\n`Mountain` and `Cocoon` communicate over a bidirectional gRPC channel described\nby `Vine` proto files. `Mountain` owns the Rust-side services and `Cocoon` owns\nthe TypeScript-side extension-host services. The protocol covers extension-host\nstartup, document notifications, language providers, tree views, command\nexecution, cancellation, and streaming routes.\n\n`Mist` handles DNS isolation and secure service-boundary code. It is not a\nuniversal transport for all editor traffic.\n\n## Platform Targets\n\nThe repository contains Tauri configuration for macOS, Windows, Linux, iOS, and\nAndroid. A configured target is not the same as a published installer or\nverified release artifact. Today:\n\n| Platform | Status                                                               |\n| -------- | -------------------------------------------------------------------- |\n| macOS    | Primary development target, Tauri bundle path active                 |\n| Windows  | Tauri configuration exists, including WebView2 bootstrapper settings |\n| Linux    | Tauri configuration exists, integration needs release validation     |\n| iOS      | Tauri mobile configuration exists                                    |\n| Android  | Tauri mobile configuration exists                                    |\n\n## Build Commands\n\n> **Updated 2026-05-29** - The build system has moved to the\n> `Maintain/Debug/Build.sh` profile runner and Node 24. See the current\n> [Installation guide](https://Editor.Land/Doc/installation) for up-to-date\n> steps.\n\nThe development build:\n\n```bash\n./Maintain/Debug/Build.sh --profile debug-electron-bundled\n```\n\nFor a lighter iteration build:\n\n```bash\n./Maintain/Debug/Build.sh --profile debug-mountain\n```\n\nFor production:\n\n```bash\n./Maintain/Release/Build.sh --profile production-electron-bundled\n```\n\n## NLnet Funding\n\nThis work is funded through the\n[NGI0 Commons Fund](https://nlnet.nl/commonsfund/) (grant No. 101135429),\noperated by PlayForm under the NLnet Foundation.", "Source/Content/blog/TauriDesktopIntegration.md", "aa28e8438d364cf7", { html: 295, metadata: 296 }, '<h1 id="tauri-desktop-integration-in-land">Tauri Desktop Integration in Land</h1>\n<p>Code Editor Land uses <code>Tauri</code> through the <code>Mountain</code> element. <code>Tauri</code> lets the\neditor use the operating system WebView instead of bundling Chromium, while\n<code>Mountain</code> keeps native services in <code>Rust</code>.</p>\n<p>The source-backed claim is architectural: no bundled Chromium in the <code>Tauri</code>\ndesktop path, native <code>Rust</code> service code in <code>Mountain</code>, and configured targets\nfor multiple operating systems. Public size, memory, and startup comparisons\nshould come from a repeatable benchmark.</p>\n<h2 id="the-electron-problem">The Electron Problem</h2>\n<p>VS Code with a medium project uses 500 MB to 1.5 GB of RAM. Three open windows\nmeans three Chromium renderer processes, each carrying a full heap. Every OS\ninteraction crosses a serialized JSON IPC pipe. Where Electron takes 200 ms to\nopen a dialog, Mountain takes 2.</p>\n<p>Tauri replaces the Electron shell with a native binary that delegates rendering\nto the OS WebView: WKWebView on macOS, WebView2 on Windows, WebKitGTK on Linux.\nThe result is a smaller binary, less memory, and direct FFI access to native\noperations.</p>\n<h2 id="mountain-the-tauri-application">Mountain: The Tauri Application</h2>\n<p><code>Mountain</code> implements the Tauri application lifecycle. Its startup path is\norganized around build, registration, initialization, and services:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="text"><code><span class="line"><span>main.rs -> Binary::Main -> Build -> Register -> Initialize -> Services</span></span></code></pre>\n<ul>\n<li><strong>Build</strong>: Configures the Tauri application with window settings, custom\nschemes (<code>land:</code>, <code>vscode-file:</code>, <code>vscode-webview:</code>), and security policies.</li>\n<li><strong>Register</strong>: Wires Tauri commands and IPC handlers for filesystem, terminal,\nclipboard, search, configuration, and workbench events.</li>\n<li><strong>Initialize</strong>: Prepares runtime state — the thread-safe <code>ApplicationState</code>\nstruct that serves as the single source of truth.</li>\n<li><strong>Services</strong>: Starts the <code>Vine</code> gRPC server and spawns <code>Cocoon</code> before the\nTauri event loop takes over the desktop session.</li>\n</ul>\n<h2 id="applicationruntime-and-effects">ApplicationRunTime and Effects</h2>\n<p><code>Mountain</code> implements the abstract service traits from <code>Common</code> through the\n<code>ActionEffect</code> system. Business logic is described as declarative, composable\neffects, executed by a central <code>ApplicationRunTime</code>. This replaces the typical\nasync/await sprawl with a structured execution model where error channels are\ntyped and resource cleanup is deterministic.</p>\n<p>Key <code>Mountain</code> modules:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Module</th><th>Role</th></tr></thead><tbody><tr><td><code>ApplicationRunTime</code></td><td>Effect execution engine (ApplicationRunTime, Execute, graceful Shutdown)</td></tr><tr><td><code>ApplicationState</code></td><td>Thread-safe state machine with DTOs, persistence, recovery, feature state</td></tr><tr><td><code>Environment/*</code></td><td>Concrete implementations of Common provider traits (filesystem, documents, terminal, etc.)</td></tr><tr><td><code>Track</code></td><td>Central request dispatcher routing frontend and sidecar commands into ActionEffects</td></tr><tr><td><code>Vine/*</code></td><td>gRPC IPC layer: server, client, multiplexer, generated protobuf bindings</td></tr><tr><td><code>ExtensionManagement</code></td><td>Extension discovery, manifest parsing, VSIX installation</td></tr><tr><td><code>ProcessManagement</code></td><td>Sidecar process lifecycle, Node.js binary resolution (nvm, fnm, asdf, volta, homebrew, shipped)</td></tr><tr><td><code>Update</code></td><td>Application self-updating via Tauri bundled updater and optional Air gRPC delegation</td></tr></tbody></table>\n<h2 id="ipc-two-layers-two-jobs">IPC: Two Layers, Two Jobs</h2>\n<p>The editor has two independent IPC paths:</p>\n<p><strong>Sky and Wind to Mountain - Tauri IPC</strong></p>\n<p><code>Sky</code>’s UI and <code>Wind</code> service layer call <code>Mountain</code> through Tauri IPC. The\nimplementation lives in <code>Source/IPC/TauriIPCServer.rs</code>, including the\n<code>mountain_ipc_receive_message</code> and <code>mountain_ipc_get_status</code> entry points. This\npath handles native work: files, terminals, clipboard, search, configuration,\nand workbench events.</p>\n<p><strong>Mountain to Cocoon - Vine gRPC</strong></p>\n<p><code>Mountain</code> and <code>Cocoon</code> communicate over a bidirectional gRPC channel described\nby <code>Vine</code> proto files. <code>Mountain</code> owns the Rust-side services and <code>Cocoon</code> owns\nthe TypeScript-side extension-host services. The protocol covers extension-host\nstartup, document notifications, language providers, tree views, command\nexecution, cancellation, and streaming routes.</p>\n<p><code>Mist</code> handles DNS isolation and secure service-boundary code. It is not a\nuniversal transport for all editor traffic.</p>\n<h2 id="platform-targets">Platform Targets</h2>\n<p>The repository contains Tauri configuration for macOS, Windows, Linux, iOS, and\nAndroid. A configured target is not the same as a published installer or\nverified release artifact. Today:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Platform</th><th>Status</th></tr></thead><tbody><tr><td>macOS</td><td>Primary development target, Tauri bundle path active</td></tr><tr><td>Windows</td><td>Tauri configuration exists, including WebView2 bootstrapper settings</td></tr><tr><td>Linux</td><td>Tauri configuration exists, integration needs release validation</td></tr><tr><td>iOS</td><td>Tauri mobile configuration exists</td></tr><tr><td>Android</td><td>Tauri mobile configuration exists</td></tr></tbody></table>\n<h2 id="build-commands">Build Commands</h2>\n<blockquote>\n<p><strong>Updated 2026-05-29</strong> - The build system has moved to the\n<code>Maintain/Debug/Build.sh</code> profile runner and Node 24. See the current\n<a href="https://Editor.Land/Doc/installation">Installation guide</a> for up-to-date\nsteps.</p>\n</blockquote>\n<p>The development build:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">./Maintain/Debug/Build.sh</span><span style="color:#79B8FF"> --profile</span><span style="color:#9ECBFF"> debug-electron-bundled</span></span></code></pre>\n<p>For a lighter iteration build:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">./Maintain/Debug/Build.sh</span><span style="color:#79B8FF"> --profile</span><span style="color:#9ECBFF"> debug-mountain</span></span></code></pre>\n<p>For production:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">./Maintain/Release/Build.sh</span><span style="color:#79B8FF"> --profile</span><span style="color:#9ECBFF"> production-electron-bundled</span></span></code></pre>\n<h2 id="nlnet-funding">NLnet Funding</h2>\n<p>This work is funded through the\n<a href="https://nlnet.nl/commonsfund/">NGI0 Commons Fund</a> (grant No. 101135429),\noperated by PlayForm under the NLnet Foundation.</p>', { headings: 297, localImagePaths: 321, remoteImagePaths: 322, frontmatter: 323, imagePaths: 325 }, [298, 300, 303, 306, 309, 312, 315, 318], { depth: 25, slug: 299, text: 285 }, "tauri-desktop-integration-in-land", { depth: 29, slug: 301, text: 302 }, "the-electron-problem", "The Electron Problem", { depth: 29, slug: 304, text: 305 }, "mountain-the-tauri-application", "Mountain: The Tauri Application", { depth: 29, slug: 307, text: 308 }, "applicationruntime-and-effects", "ApplicationRunTime and Effects", { depth: 29, slug: 310, text: 311 }, "ipc-two-layers-two-jobs", "IPC: Two Layers, Two Jobs", { depth: 29, slug: 313, text: 314 }, "platform-targets", "Platform Targets", { depth: 29, slug: 316, text: 317 }, "build-commands", "Build Commands", { depth: 29, slug: 319, text: 320 }, "nlnet-funding", "NLnet Funding", [], [], { title: 285, summary: 286, publishedAt: 248, tags: 324, author: 15, readTime: 252 }, [288, 289, 290, 10], [], "VSCodeCompatibility", { id: 326, data: 328, body: 334, filePath: 335, digest: 336, rendered: 337 }, { title: 329, summary: 330, publishedAt: 103, tags: 331, author: 15, readTime: 109 }, "VS Code Extension Compatibility in Land", "How Land runs unmodified VS Code extensions, and where coverage still needs validation.", [192, 332, 333], "VS Code", "Compatibility", "# VS Code Extension Compatibility in Land\n\nLand's active compatibility path is `Cocoon`. `Cocoon` loads existing `VS Code`\nextension entry points and provides the `vscode` API object those extensions\nexpect. `Mountain` scans installed extensions, reads manifests, handles local\nVSIX install and uninstall routes, and notifies `Cocoon` when the extension set\nchanges.\n\nThat means Land can make a strong, useful claim: installed `VS Code` extensions\nrun unmodified through `Cocoon` when the APIs they use are implemented.\n\nIt should not claim that every marketplace extension works until a public\nvalidation matrix proves that statement across real extensions and versions.\n\n## How Cocoon Hosts Extensions\n\nThe compatibility architecture has three layers:\n\n### 1. The Extension Host Process\n\n`Cocoon` is a dedicated Node.js sidecar process spawned and managed by\n`Mountain`. It receives initialization data over a gRPC channel (`Vine`\nprotocol) and bootstraps an Effect-TS runtime that mirrors the VS Code Extension\nHost environment:\n\n- `PatchProcess` hardens the environment immediately: patches `process.exit`,\n  handles uncaught exceptions, pipes logs to `Mountain`, and terminates if the\n  parent process exits.\n- `Effect/Bootstrap.ts` coordinates initialization stages: environment\n  detection, configuration loading, gRPC connection, module interceptor setup,\n  extension registry, and health checks.\n- The `AppLayer` (wired via `Service/Mapping.ts`) composes all Effect-TS service\n  layers into a single runnable unit.\n\n### 2. Module Interception\n\nWhen a VS Code extension calls `require('vscode')`, `Cocoon` intercepts the\nmodule request and returns a sandboxed `vscode` API instance specific to that\nextension. This happens for both CJS `require()` and ESM `import` statements.\n\nThe interceptor works at the Node.js module resolution level, before the\nextension code ever sees the module. Each extension gets its own API object\nwired through Effect-TS services, so one extension's API calls do not interfere\nwith another's.\n\n```typescript\n// Services/API/Factory/Service.ts\n// Constructs the vscode API object that extensions receive\n// Wires API calls through the Mountain client service for backend execution\n```\n\n### 3. Service Shims\n\nA comprehensive set of service shims replicates the behavior of the real\n`vscode.workspace`, `vscode.window`, `vscode.commands`, and other VS Code\nextension APIs. Each shim is built on Effect-TS services that route privileged\ncalls to `Mountain` via gRPC.\n\nThe gRPC client in `Cocoon` (`Services/Mountain/gRPC/Client.ts`) provides\nEffect-wrapped methods for window, workspace, command, and secret storage\noperations. The gRPC server (`Services/gRPC/Server/Service.ts`) implements the\nVine protocol with bidirectional streaming for real-time event communication.\n\n## What Is Source-Backed Today\n\n- `Cocoon` hosts existing extension code instead of asking authors to fork or\n  rewrite their extension.\n- `Mountain` contains extension scanning, manifest parsing, local VSIX install\n  and uninstall routes, and extension-state updates.\n- `Output` keeps the `VS Code` extension scanner and offline gallery channels\n  wired for sideloaded extensions.\n- Core surfaces such as commands, workspace, window, terminals, webviews,\n  language providers, diagnostics, output channels, and document events have\n  active source paths.\n\n## In Progress\n\n- Marketplace browsing and install flows beyond local or sideloaded extension\n  sources.\n- Chat, language-model, notebook, tests, and other long-tail `VS Code` APIs.\n- A public extension validation matrix that names extensions, versions,\n  platforms, and the APIs they exercised.\n- `vscode.lm.*`, `vscode.chat.*`, `vscode.notebook.*`, and `vscode.tests.*` are\n  not implemented. Extensions using these APIs activate but the specific\n  features silently no-op.\n\n## Marketplace Integration\n\nLand does not need to connect to the Microsoft Visual Studio Marketplace to run\ninstalled extension code. The current source is oriented around local extension\npaths and sideloaded packages. Open registry and enterprise registry flows\nshould be described as coming soon until the in-editor path is complete.\n\n## Code Generation Pipeline\n\n`Cocoon` includes a code generation pipeline (`Codegen/`) that walks the VS Code\nextension-host source subtree and emits `IExtHost*Upstream` schemas grounded in\nreal upstream definitions. This reuses `Wind`'s extractor and resolver\ninfrastructure. The schemas ensure that the API shims in `Cocoon` stay aligned\nwith the actual VS Code extension-host types, reducing drift.\n\n## Type Conversion\n\nType definitions used by VS Code extensions (`Range`, `Position`, `URI`,\n`Dialog`, `TreeView`, `Webview`, `Task`, `WorkspaceEdit`) are serialized through\npure conversion functions in `TypeConverter/` into plain DTOs suitable for gRPC\ntransport. This avoids serializing rich objects across process boundaries in an\nunstructured way -- every type has a documented conversion path.\n\n## Reporting Compatibility Issues\n\nIf an extension that works in `VS Code` fails in Land, open an issue with the\nextension name, version, platform, activation event, and the API that failed.\nThat keeps compatibility work tied to real behavior instead of broad promises.", "Source/Content/blog/VSCodeCompatibility.md", "58e98b93acd8d72f", { html: 338, metadata: 339 }, `<h1 id="vs-code-extension-compatibility-in-land">VS Code Extension Compatibility in Land</h1>
<p>Land’s active compatibility path is <code>Cocoon</code>. <code>Cocoon</code> loads existing <code>VS Code</code>
extension entry points and provides the <code>vscode</code> API object those extensions
expect. <code>Mountain</code> scans installed extensions, reads manifests, handles local
VSIX install and uninstall routes, and notifies <code>Cocoon</code> when the extension set
changes.</p>
<p>That means Land can make a strong, useful claim: installed <code>VS Code</code> extensions
run unmodified through <code>Cocoon</code> when the APIs they use are implemented.</p>
<p>It should not claim that every marketplace extension works until a public
validation matrix proves that statement across real extensions and versions.</p>
<h2 id="how-cocoon-hosts-extensions">How Cocoon Hosts Extensions</h2>
<p>The compatibility architecture has three layers:</p>
<h3 id="1-the-extension-host-process">1. The Extension Host Process</h3>
<p><code>Cocoon</code> is a dedicated Node.js sidecar process spawned and managed by
<code>Mountain</code>. It receives initialization data over a gRPC channel (<code>Vine</code>
protocol) and bootstraps an Effect-TS runtime that mirrors the VS Code Extension
Host environment:</p>
<ul>
<li><code>PatchProcess</code> hardens the environment immediately: patches <code>process.exit</code>,
handles uncaught exceptions, pipes logs to <code>Mountain</code>, and terminates if the
parent process exits.</li>
<li><code>Effect/Bootstrap.ts</code> coordinates initialization stages: environment
detection, configuration loading, gRPC connection, module interceptor setup,
extension registry, and health checks.</li>
<li>The <code>AppLayer</code> (wired via <code>Service/Mapping.ts</code>) composes all Effect-TS service
layers into a single runnable unit.</li>
</ul>
<h3 id="2-module-interception">2. Module Interception</h3>
<p>When a VS Code extension calls <code>require('vscode')</code>, <code>Cocoon</code> intercepts the
module request and returns a sandboxed <code>vscode</code> API instance specific to that
extension. This happens for both CJS <code>require()</code> and ESM <code>import</code> statements.</p>
<p>The interceptor works at the Node.js module resolution level, before the
extension code ever sees the module. Each extension gets its own API object
wired through Effect-TS services, so one extension’s API calls do not interfere
with another’s.</p>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="typescript"><code><span class="line"><span style="color:#6A737D">// Services/API/Factory/Service.ts</span></span>
<span class="line"><span style="color:#6A737D">// Constructs the vscode API object that extensions receive</span></span>
<span class="line"><span style="color:#6A737D">// Wires API calls through the Mountain client service for backend execution</span></span></code></pre>
<h3 id="3-service-shims">3. Service Shims</h3>
<p>A comprehensive set of service shims replicates the behavior of the real
<code>vscode.workspace</code>, <code>vscode.window</code>, <code>vscode.commands</code>, and other VS Code
extension APIs. Each shim is built on Effect-TS services that route privileged
calls to <code>Mountain</code> via gRPC.</p>
<p>The gRPC client in <code>Cocoon</code> (<code>Services/Mountain/gRPC/Client.ts</code>) provides
Effect-wrapped methods for window, workspace, command, and secret storage
operations. The gRPC server (<code>Services/gRPC/Server/Service.ts</code>) implements the
Vine protocol with bidirectional streaming for real-time event communication.</p>
<h2 id="what-is-source-backed-today">What Is Source-Backed Today</h2>
<ul>
<li><code>Cocoon</code> hosts existing extension code instead of asking authors to fork or
rewrite their extension.</li>
<li><code>Mountain</code> contains extension scanning, manifest parsing, local VSIX install
and uninstall routes, and extension-state updates.</li>
<li><code>Output</code> keeps the <code>VS Code</code> extension scanner and offline gallery channels
wired for sideloaded extensions.</li>
<li>Core surfaces such as commands, workspace, window, terminals, webviews,
language providers, diagnostics, output channels, and document events have
active source paths.</li>
</ul>
<h2 id="in-progress">In Progress</h2>
<ul>
<li>Marketplace browsing and install flows beyond local or sideloaded extension
sources.</li>
<li>Chat, language-model, notebook, tests, and other long-tail <code>VS Code</code> APIs.</li>
<li>A public extension validation matrix that names extensions, versions,
platforms, and the APIs they exercised.</li>
<li><code>vscode.lm.*</code>, <code>vscode.chat.*</code>, <code>vscode.notebook.*</code>, and <code>vscode.tests.*</code> are
not implemented. Extensions using these APIs activate but the specific
features silently no-op.</li>
</ul>
<h2 id="marketplace-integration">Marketplace Integration</h2>
<p>Land does not need to connect to the Microsoft Visual Studio Marketplace to run
installed extension code. The current source is oriented around local extension
paths and sideloaded packages. Open registry and enterprise registry flows
should be described as coming soon until the in-editor path is complete.</p>
<h2 id="code-generation-pipeline">Code Generation Pipeline</h2>
<p><code>Cocoon</code> includes a code generation pipeline (<code>Codegen/</code>) that walks the VS Code
extension-host source subtree and emits <code>IExtHost*Upstream</code> schemas grounded in
real upstream definitions. This reuses <code>Wind</code>’s extractor and resolver
infrastructure. The schemas ensure that the API shims in <code>Cocoon</code> stay aligned
with the actual VS Code extension-host types, reducing drift.</p>
<h2 id="type-conversion">Type Conversion</h2>
<p>Type definitions used by VS Code extensions (<code>Range</code>, <code>Position</code>, <code>URI</code>,
<code>Dialog</code>, <code>TreeView</code>, <code>Webview</code>, <code>Task</code>, <code>WorkspaceEdit</code>) are serialized through
pure conversion functions in <code>TypeConverter/</code> into plain DTOs suitable for gRPC
transport. This avoids serializing rich objects across process boundaries in an
unstructured way — every type has a documented conversion path.</p>
<h2 id="reporting-compatibility-issues">Reporting Compatibility Issues</h2>
<p>If an extension that works in <code>VS Code</code> fails in Land, open an issue with the
extension name, version, platform, activation event, and the API that failed.
That keeps compatibility work tied to real behavior instead of broad promises.</p>`, { headings: 340, localImagePaths: 370, remoteImagePaths: 371, frontmatter: 372, imagePaths: 374 }, [341, 343, 346, 350, 353, 356, 359, 362, 365, 366, 367], { depth: 25, slug: 342, text: 329 }, "vs-code-extension-compatibility-in-land", { depth: 29, slug: 344, text: 345 }, "how-cocoon-hosts-extensions", "How Cocoon Hosts Extensions", { depth: 347, slug: 348, text: 349 }, 3, "1-the-extension-host-process", "1. The Extension Host Process", { depth: 347, slug: 351, text: 352 }, "2-module-interception", "2. Module Interception", { depth: 347, slug: 354, text: 355 }, "3-service-shims", "3. Service Shims", { depth: 29, slug: 357, text: 358 }, "what-is-source-backed-today", "What Is Source-Backed Today", { depth: 29, slug: 360, text: 361 }, "in-progress", "In Progress", { depth: 29, slug: 363, text: 364 }, "marketplace-integration", "Marketplace Integration", { depth: 29, slug: 230, text: 231 }, { depth: 29, slug: 227, text: 228 }, { depth: 29, slug: 368, text: 369 }, "reporting-compatibility-issues", "Reporting Compatibility Issues", [], [], { title: 329, summary: 330, publishedAt: 103, tags: 373, author: 15, readTime: 109 }, [192, 332, 333], [], "doc", ["Map", 377, 378, 407, 408, 71, 452, 489, 490, 545, 546, 578, 579, 625, 626, 428, 651, 687, 688, 723, 724, 757, 758, 801, 802, 836, 837, 871, 872, 906, 907, 938, 939, 968, 969, 1008, 1009, 1035, 1036, 1065, 1066, 1098, 1099, 1128, 1129, 1165, 1166, 1211, 1212, 1258, 1259, 1309, 1310, 1383, 1552, 1582, 1583, 1621, 1622, 1405, 1661, 1692, 1693, 1729, 1730, 1759, 1760, 1415, 1793, 1430, 1820, 1440, 1839, 1878, 1879, 1458, 1916, 1599, 1944, 1997, 1998, 1470, 2033, 1484, 2052, 1495, 2082, 2113, 2114, 2160, 2161, 2194, 2195, 2233, 2234, 2270, 2271, 1513, 2306, 2344, 2345, 2424, 2425, 161, 2462, 2499, 2500, 2530, 2531, 2564, 2565, 2592, 2593, 1522, 2623, 1538, 2645, 2666, 2667, 2706, 2707, 2739, 2740, 2778, 2779, 2811, 2812, 2844, 2845, 2877, 2878, 2908, 2909, 2941, 2942, 2977, 2978], "air", { id: 377, data: 379, body: 384, filePath: 385, digest: 386, rendered: 387 }, { title: 380, section: 381, order: 382, description: 383 }, "Air", "Element", 15, "Rust background services for updates, downloads, integrity checks, authentication, indexing, health, and Vine IPC.", "# Air\n\n`Air` is the `Rust` background-services element for `Editor.Land`. The source\ncontains modules for updates, downloads, authentication, indexing, security,\nhealth, resilience, configuration, logging, metrics, and `Vine` `IPC`.\n\n`Air` should be described as source-backed service plumbing. The public updater,\nrelease signing story, and installer distribution path are still being prepared.\n\n---\n\n## Source Structure 🗺️\n\nConfirmed source areas include:\n\n| Path                                    | Role                                       |\n| --------------------------------------- | ------------------------------------------ |\n| `Source/Binary.rs` and `Source/Binary/` | Daemon entry points and runtime wiring     |\n| `Source/Authentication/`                | Authentication and credential-related work |\n| `Source/Downloader/`                    | Download management                        |\n| `Source/HTTP/`                          | HTTP client code                           |\n| `Source/HealthCheck/`                   | Status and health reporting                |\n| `Source/Indexing/`                      | Workspace metadata work                    |\n| `Source/Resilience/`                    | Recovery and restart coordination          |\n| `Source/Security/`                      | Integrity checks and security helpers      |\n| `Source/Updates/`                       | Update-check and rollout logic             |\n| `Source/Vine/`                          | Vine gRPC client stubs                     |\n\n---\n\n## Responsibilities 🔌\n\n- Checking update metadata where a build profile enables that route.\n- Downloading files or release payloads through `Air`'s downloader and HTTP\n  code.\n- Verifying file integrity through checksum-oriented security helpers.\n- Reporting daemon health and runtime status.\n- Running indexing work outside the active editor surface.\n- Coordinating with `Mountain` through `Air` integration and `Vine` `IPC`.\n\n---\n\n## Coming Soon 🚀\n\n- Public release artifacts with published checksums and signatures.\n- End-to-end staged update flow in a public installer.\n- Release-channel documentation that names which `Air` services are enabled.\n- Platform service setup that is validated per operating system.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Mountain`](https://Editor.Land/Doc/mountain)\n- [Local-First Protocol](https://Editor.Land/Doc/local-first-protocol)\n- [Source Code](https://github.com/CodeEditorLand/Air)", "Source/Content/doc/air.md", "9d35160282c95f5e", { html: 388, metadata: 389 }, '<h1 id="air">Air</h1>\n<p><code>Air</code> is the <code>Rust</code> background-services element for <code>Editor.Land</code>. The source\ncontains modules for updates, downloads, authentication, indexing, security,\nhealth, resilience, configuration, logging, metrics, and <code>Vine</code> <code>IPC</code>.</p>\n<p><code>Air</code> should be described as source-backed service plumbing. The public updater,\nrelease signing story, and installer distribution path are still being prepared.</p>\n<hr>\n<h2 id="source-structure️">Source Structure 🗺️</h2>\n<p>Confirmed source areas include:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Path</th><th>Role</th></tr></thead><tbody><tr><td><code>Source/Binary.rs</code> and <code>Source/Binary/</code></td><td>Daemon entry points and runtime wiring</td></tr><tr><td><code>Source/Authentication/</code></td><td>Authentication and credential-related work</td></tr><tr><td><code>Source/Downloader/</code></td><td>Download management</td></tr><tr><td><code>Source/HTTP/</code></td><td>HTTP client code</td></tr><tr><td><code>Source/HealthCheck/</code></td><td>Status and health reporting</td></tr><tr><td><code>Source/Indexing/</code></td><td>Workspace metadata work</td></tr><tr><td><code>Source/Resilience/</code></td><td>Recovery and restart coordination</td></tr><tr><td><code>Source/Security/</code></td><td>Integrity checks and security helpers</td></tr><tr><td><code>Source/Updates/</code></td><td>Update-check and rollout logic</td></tr><tr><td><code>Source/Vine/</code></td><td>Vine gRPC client stubs</td></tr></tbody></table>\n<hr>\n<h2 id="responsibilities">Responsibilities 🔌</h2>\n<ul>\n<li>Checking update metadata where a build profile enables that route.</li>\n<li>Downloading files or release payloads through <code>Air</code>’s downloader and HTTP\ncode.</li>\n<li>Verifying file integrity through checksum-oriented security helpers.</li>\n<li>Reporting daemon health and runtime status.</li>\n<li>Running indexing work outside the active editor surface.</li>\n<li>Coordinating with <code>Mountain</code> through <code>Air</code> integration and <code>Vine</code> <code>IPC</code>.</li>\n</ul>\n<hr>\n<h2 id="coming-soon">Coming Soon 🚀</h2>\n<ul>\n<li>Public release artifacts with published checksums and signatures.</li>\n<li>End-to-end staged update flow in a public installer.</li>\n<li>Release-channel documentation that names which <code>Air</code> services are enabled.</li>\n<li>Platform service setup that is validated per operating system.</li>\n</ul>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/mountain"><code>Mountain</code></a></li>\n<li><a href="https://Editor.Land/Doc/local-first-protocol">Local-First Protocol</a></li>\n<li><a href="https://github.com/CodeEditorLand/Air">Source Code</a></li>\n</ul>', { headings: 390, localImagePaths: 403, remoteImagePaths: 404, frontmatter: 405, imagePaths: 406 }, [391, 392, 395, 398, 400], { depth: 25, slug: 377, text: 380 }, { depth: 29, slug: 393, text: 394 }, "source-structure️", "Source Structure 🗺️", { depth: 29, slug: 396, text: 397 }, "responsibilities", "Responsibilities 🔌", { depth: 29, slug: 272, text: 399 }, "Coming Soon 🚀", { depth: 29, slug: 401, text: 402 }, "related-documentation", "Related Documentation 📖", [], [], { title: 380, section: 381, order: 382, description: 383 }, [], "api-reference", { id: 407, data: 409, body: 413, filePath: 414, digest: 415, rendered: 416 }, { title: 410, section: 411, order: 16, description: 412 }, "API Reference", "Development", "VS Code extension API coverage in Editor.Land: what works, what no-ops, and what is not yet implemented.", '# API Reference\n\n`Editor.Land` implements the `VS Code` extension `API` through `Cocoon`, the\n`Node.js` extension host. Extensions written against `@types/vscode` compile\nagainst `Cocoon`\'s stubs without modification. Whether they run correctly\ndepends on which `API` surfaces they use.\n\n---\n\n## Coverage Status\n\nThe `API` surface is divided into three categories:\n\n**Implemented** - The `API` is active in the `debug-mountain` profile.\nExtensions using it behave as expected.\n\n**Partial** - The `API` is present and callable but not all methods or\nbehaviours are implemented. Some calls may silently no-op or return empty\nresults.\n\n**Not implemented** - The namespace exists in `Cocoon`\'s type stubs so\nextensions compile, but the runtime calls do nothing. Extensions that depend on\nthese `APIs` activate but their features do not work.\n\n| Namespace                           | Status          | Notes                                                                                                 |\n| ----------------------------------- | --------------- | ----------------------------------------------------------------------------------------------------- |\n| `vscode.commands`                   | Implemented     | Register, execute, command palette                                                                    |\n| `vscode.workspace.fs`               | Implemented     | Routes through Mountain\'s FS layer via Vine                                                           |\n| `vscode.window.createTerminal`      | Implemented     | Routes through Mountain\'s pty layer via Vine                                                          |\n| `vscode.debug`                      | Implemented     | DAP bridge in Mountain, routes via Vine                                                               |\n| `vscode.languages`                  | Implemented     | LSP client via `vscode-languageclient`                                                                |\n| `vscode.workspace.getConfiguration` | Implemented     | Reads workspace and user settings                                                                     |\n| `vscode.window` (core)              | Partial         | showInformationMessage, createWebviewPanel, registerTreeDataProvider work; some view APIs unconfirmed |\n| `vscode.window.createTreeView`      | Partial         | Tree data provider registration works; inline actions and welcome content unconfirmed                 |\n| `vscode.tasks`                      | Partial         | Task definition reading works; full task runner execution unconfirmed                                 |\n| `vscode.extensions`                 | Partial         | getExtension and activate work; some metadata fields unconfirmed                                      |\n| `vscode.lm`                         | Not implemented | Language model / Copilot APIs - no-op                                                                 |\n| `vscode.chat`                       | Not implemented | Chat panel APIs - no-op                                                                               |\n| `vscode.notebook`                   | Not implemented | Notebook document and editor APIs - no-op                                                             |\n| `vscode.tests`                      | Not implemented | Test explorer and runner APIs - no-op                                                                 |\n\n---\n\n## Commands\n\nRegister a command with `vscode.commands.registerCommand`:\n\n```typescript\nconst Disposable = vscode.commands.registerCommand(\n	"MyExtension.SayHello",\n	() => {\n		vscode.window.showInformationMessage("Hello from Land!");\n	},\n);\n```\n\nCommands declared in `package.json` under `contributes.commands` appear in the\ncommand palette. This is one of the more reliably implemented `API` surfaces.\n\n---\n\n## Configuration\n\nExtensions read settings through the `WorkspaceConfiguration` `API`:\n\n```typescript\nconst Config = vscode.workspace.getConfiguration("MyExtension");\nconst FontSize = Config.get<number>("FontSize", 14);\n```\n\nConfiguration keys are declared in `contributes.configuration` in your extension\nmanifest. Land validates configuration values against the `JSON` Schema you\nprovide. The specific user settings file path has not been independently\nconfirmed - see [Configuration](https://Editor.Land/Doc/configuration) for\ndetails.\n\n---\n\n## Keybindings\n\nDeclare keybindings in `contributes.keybindings`:\n\n```json\n{\n	"command": "MyExtension.SayHello",\n	"key": "ctrl+shift+h",\n	"mac": "cmd+shift+h",\n	"when": "editorTextFocus"\n}\n```\n\nThe `when` clause uses the same context key syntax as `VS Code`. Custom context\nkeys set via `vscode.commands.executeCommand(\'setContext\', ...)` are supported.\n\n---\n\n## Tree Views\n\nRegister a tree view provider:\n\n```typescript\nvscode.window.registerTreeDataProvider(\n	"MyExtension.TreeView",\n	MyTreeDataProvider,\n);\n```\n\nDeclare the view container and view in `contributes.viewsContainers` and\n`contributes.views`. Tree data provider registration routes through `Cocoon`\'s\nfiber scheduler. Inline actions and welcome content are not confirmed.\n\n---\n\n## Webview Panels\n\nCreate HTML-based UI panels:\n\n```typescript\nconst Panel = vscode.window.createWebviewPanel(\n	"MyExtension.Preview",\n	"Preview",\n	vscode.ViewColumn.Beside,\n	{ enableScripts: true },\n);\n\nPanel.webview.html = "<html><body><h1>Preview</h1></body></html>";\n```\n\nWebview panels run in a sandboxed context with a Content Security Policy. Use\n`Panel.webview.postMessage()` and `Panel.webview.onDidReceiveMessage` for\nbidirectional communication between the extension and the webview.\n\n---\n\n## Language Server Protocol\n\nLand supports LSP servers through the `vscode-languageclient` package. Point\nyour extension at a language server binary and the LSP client handles\ninitialization, capabilities negotiation, and shutdown. This routes through\n`Cocoon`\'s `vscode.languages` implementation, which is one of the more complete\n`API` surfaces.\n\n---\n\n## Rust API Documentation\n\nGenerated `rustdoc` output is planned for the `Rust` crates listed below. The\nURLs follow the pattern `https://Rust.Documentation.*.Editor.Land` - these may\nnot yet resolve to hosted documentation. Check the\n[source repositories](https://github.com/CodeEditorLand) directly if the links\nare unavailable.\n\n| Crate           | Description                                                                                                                             | Element                                              |\n| --------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |\n| `Mountain`      | Tauri native kernel                                                                                                                     | [Mountain](https://Editor.Land/Doc/mountain)         |\n| `Echo`          | Work-stealing task scheduler                                                                                                            | [Echo](https://Editor.Land/Doc/echo)                 |\n| `Common`        | Shared IPC event type definitions                                                                                                       | [Architecture](https://Editor.Land/Doc/architecture) |\n| `CommonLibrary` | Shared utility functions                                                                                                                | [Architecture](https://Editor.Land/Doc/architecture) |\n| `Air`           | Background update daemon                                                                                                                | [Air](https://Editor.Land/Doc/air)                   |\n| `AirLibrary`    | Air shared library                                                                                                                      | [Air](https://Editor.Land/Doc/air)                   |\n| `Download`      | Binary download logic                                                                                                                   | [Air](https://Editor.Land/Doc/air)                   |\n| `SideCar`       | Pre-built Node.js binaries                                                                                                              | [Architecture](https://Editor.Land/Doc/architecture) |\n| `Maintain`      | Build orchestrator                                                                                                                      | [Contributing](https://Editor.Land/Doc/contributing) |\n| `Grove`         | WASM extension host - WASMtime host, gRPC protocol, API surface, and transport layer implemented; primary build integration in progress | [Grove](https://Editor.Land/Doc/grove)               |\n\n---\n\n## See Also\n\n- [`Cocoon`: Extension Host](https://Editor.Land/Doc/cocoon)\n- [Extension Development](https://Editor.Land/Doc/extension-development)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`VS Code` `API` Documentation](https://code.visualstudio.com/api/references/vscode-api)', "Source/Content/doc/api-reference.md", "8b9289ce3a9b4f72", { html: 417, metadata: 418 }, `<h1 id="api-reference">API Reference</h1>
<p><code>Editor.Land</code> implements the <code>VS Code</code> extension <code>API</code> through <code>Cocoon</code>, the
<code>Node.js</code> extension host. Extensions written against <code>@types/vscode</code> compile
against <code>Cocoon</code>’s stubs without modification. Whether they run correctly
depends on which <code>API</code> surfaces they use.</p>
<hr>
<h2 id="coverage-status">Coverage Status</h2>
<p>The <code>API</code> surface is divided into three categories:</p>
<p><strong>Implemented</strong> - The <code>API</code> is active in the <code>debug-mountain</code> profile.
Extensions using it behave as expected.</p>
<p><strong>Partial</strong> - The <code>API</code> is present and callable but not all methods or
behaviours are implemented. Some calls may silently no-op or return empty
results.</p>
<p><strong>Not implemented</strong> - The namespace exists in <code>Cocoon</code>’s type stubs so
extensions compile, but the runtime calls do nothing. Extensions that depend on
these <code>APIs</code> activate but their features do not work.</p>
















































































<table><thead><tr><th>Namespace</th><th>Status</th><th>Notes</th></tr></thead><tbody><tr><td><code>vscode.commands</code></td><td>Implemented</td><td>Register, execute, command palette</td></tr><tr><td><code>vscode.workspace.fs</code></td><td>Implemented</td><td>Routes through Mountain’s FS layer via Vine</td></tr><tr><td><code>vscode.window.createTerminal</code></td><td>Implemented</td><td>Routes through Mountain’s pty layer via Vine</td></tr><tr><td><code>vscode.debug</code></td><td>Implemented</td><td>DAP bridge in Mountain, routes via Vine</td></tr><tr><td><code>vscode.languages</code></td><td>Implemented</td><td>LSP client via <code>vscode-languageclient</code></td></tr><tr><td><code>vscode.workspace.getConfiguration</code></td><td>Implemented</td><td>Reads workspace and user settings</td></tr><tr><td><code>vscode.window</code> (core)</td><td>Partial</td><td>showInformationMessage, createWebviewPanel, registerTreeDataProvider work; some view APIs unconfirmed</td></tr><tr><td><code>vscode.window.createTreeView</code></td><td>Partial</td><td>Tree data provider registration works; inline actions and welcome content unconfirmed</td></tr><tr><td><code>vscode.tasks</code></td><td>Partial</td><td>Task definition reading works; full task runner execution unconfirmed</td></tr><tr><td><code>vscode.extensions</code></td><td>Partial</td><td>getExtension and activate work; some metadata fields unconfirmed</td></tr><tr><td><code>vscode.lm</code></td><td>Not implemented</td><td>Language model / Copilot APIs - no-op</td></tr><tr><td><code>vscode.chat</code></td><td>Not implemented</td><td>Chat panel APIs - no-op</td></tr><tr><td><code>vscode.notebook</code></td><td>Not implemented</td><td>Notebook document and editor APIs - no-op</td></tr><tr><td><code>vscode.tests</code></td><td>Not implemented</td><td>Test explorer and runner APIs - no-op</td></tr></tbody></table>
<hr>
<h2 id="commands">Commands</h2>
<p>Register a command with <code>vscode.commands.registerCommand</code>:</p>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="typescript"><code><span class="line"><span style="color:#F97583">const</span><span style="color:#79B8FF"> Disposable</span><span style="color:#F97583"> =</span><span style="color:#E1E4E8"> vscode.commands.</span><span style="color:#B392F0">registerCommand</span><span style="color:#E1E4E8">(</span></span>
<span class="line"><span style="color:#9ECBFF">	"MyExtension.SayHello"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#E1E4E8">	() </span><span style="color:#F97583">=></span><span style="color:#E1E4E8"> {</span></span>
<span class="line"><span style="color:#E1E4E8">		vscode.window.</span><span style="color:#B392F0">showInformationMessage</span><span style="color:#E1E4E8">(</span><span style="color:#9ECBFF">"Hello from Land!"</span><span style="color:#E1E4E8">);</span></span>
<span class="line"><span style="color:#E1E4E8">	},</span></span>
<span class="line"><span style="color:#E1E4E8">);</span></span></code></pre>
<p>Commands declared in <code>package.json</code> under <code>contributes.commands</code> appear in the
command palette. This is one of the more reliably implemented <code>API</code> surfaces.</p>
<hr>
<h2 id="configuration">Configuration</h2>
<p>Extensions read settings through the <code>WorkspaceConfiguration</code> <code>API</code>:</p>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="typescript"><code><span class="line"><span style="color:#F97583">const</span><span style="color:#79B8FF"> Config</span><span style="color:#F97583"> =</span><span style="color:#E1E4E8"> vscode.workspace.</span><span style="color:#B392F0">getConfiguration</span><span style="color:#E1E4E8">(</span><span style="color:#9ECBFF">"MyExtension"</span><span style="color:#E1E4E8">);</span></span>
<span class="line"><span style="color:#F97583">const</span><span style="color:#79B8FF"> FontSize</span><span style="color:#F97583"> =</span><span style="color:#E1E4E8"> Config.</span><span style="color:#B392F0">get</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#79B8FF">number</span><span style="color:#E1E4E8">>(</span><span style="color:#9ECBFF">"FontSize"</span><span style="color:#E1E4E8">, </span><span style="color:#79B8FF">14</span><span style="color:#E1E4E8">);</span></span></code></pre>
<p>Configuration keys are declared in <code>contributes.configuration</code> in your extension
manifest. Land validates configuration values against the <code>JSON</code> Schema you
provide. The specific user settings file path has not been independently
confirmed - see <a href="https://Editor.Land/Doc/configuration">Configuration</a> for
details.</p>
<hr>
<h2 id="keybindings">Keybindings</h2>
<p>Declare keybindings in <code>contributes.keybindings</code>:</p>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="json"><code><span class="line"><span style="color:#E1E4E8">{</span></span>
<span class="line"><span style="color:#79B8FF">	"command"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"MyExtension.SayHello"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"key"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"ctrl+shift+h"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"mac"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"cmd+shift+h"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"when"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"editorTextFocus"</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>
<p>The <code>when</code> clause uses the same context key syntax as <code>VS Code</code>. Custom context
keys set via <code>vscode.commands.executeCommand('setContext', ...)</code> are supported.</p>
<hr>
<h2 id="tree-views">Tree Views</h2>
<p>Register a tree view provider:</p>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="typescript"><code><span class="line"><span style="color:#E1E4E8">vscode.window.</span><span style="color:#B392F0">registerTreeDataProvider</span><span style="color:#E1E4E8">(</span></span>
<span class="line"><span style="color:#9ECBFF">	"MyExtension.TreeView"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#E1E4E8">	MyTreeDataProvider,</span></span>
<span class="line"><span style="color:#E1E4E8">);</span></span></code></pre>
<p>Declare the view container and view in <code>contributes.viewsContainers</code> and
<code>contributes.views</code>. Tree data provider registration routes through <code>Cocoon</code>’s
fiber scheduler. Inline actions and welcome content are not confirmed.</p>
<hr>
<h2 id="webview-panels">Webview Panels</h2>
<p>Create HTML-based UI panels:</p>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="typescript"><code><span class="line"><span style="color:#F97583">const</span><span style="color:#79B8FF"> Panel</span><span style="color:#F97583"> =</span><span style="color:#E1E4E8"> vscode.window.</span><span style="color:#B392F0">createWebviewPanel</span><span style="color:#E1E4E8">(</span></span>
<span class="line"><span style="color:#9ECBFF">	"MyExtension.Preview"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#9ECBFF">	"Preview"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#E1E4E8">	vscode.ViewColumn.Beside,</span></span>
<span class="line"><span style="color:#E1E4E8">	{ enableScripts: </span><span style="color:#79B8FF">true</span><span style="color:#E1E4E8"> },</span></span>
<span class="line"><span style="color:#E1E4E8">);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#E1E4E8">Panel.webview.html </span><span style="color:#F97583">=</span><span style="color:#9ECBFF"> "&#x3C;html>&#x3C;body>&#x3C;h1>Preview&#x3C;/h1>&#x3C;/body>&#x3C;/html>"</span><span style="color:#E1E4E8">;</span></span></code></pre>
<p>Webview panels run in a sandboxed context with a Content Security Policy. Use
<code>Panel.webview.postMessage()</code> and <code>Panel.webview.onDidReceiveMessage</code> for
bidirectional communication between the extension and the webview.</p>
<hr>
<h2 id="language-server-protocol">Language Server Protocol</h2>
<p>Land supports LSP servers through the <code>vscode-languageclient</code> package. Point
your extension at a language server binary and the LSP client handles
initialization, capabilities negotiation, and shutdown. This routes through
<code>Cocoon</code>’s <code>vscode.languages</code> implementation, which is one of the more complete
<code>API</code> surfaces.</p>
<hr>
<h2 id="rust-api-documentation">Rust API Documentation</h2>
<p>Generated <code>rustdoc</code> output is planned for the <code>Rust</code> crates listed below. The
URLs follow the pattern <code>https://Rust.Documentation.*.Editor.Land</code> - these may
not yet resolve to hosted documentation. Check the
<a href="https://github.com/CodeEditorLand">source repositories</a> directly if the links
are unavailable.</p>




























































<table><thead><tr><th>Crate</th><th>Description</th><th>Element</th></tr></thead><tbody><tr><td><code>Mountain</code></td><td>Tauri native kernel</td><td><a href="https://Editor.Land/Doc/mountain">Mountain</a></td></tr><tr><td><code>Echo</code></td><td>Work-stealing task scheduler</td><td><a href="https://Editor.Land/Doc/echo">Echo</a></td></tr><tr><td><code>Common</code></td><td>Shared IPC event type definitions</td><td><a href="https://Editor.Land/Doc/architecture">Architecture</a></td></tr><tr><td><code>CommonLibrary</code></td><td>Shared utility functions</td><td><a href="https://Editor.Land/Doc/architecture">Architecture</a></td></tr><tr><td><code>Air</code></td><td>Background update daemon</td><td><a href="https://Editor.Land/Doc/air">Air</a></td></tr><tr><td><code>AirLibrary</code></td><td>Air shared library</td><td><a href="https://Editor.Land/Doc/air">Air</a></td></tr><tr><td><code>Download</code></td><td>Binary download logic</td><td><a href="https://Editor.Land/Doc/air">Air</a></td></tr><tr><td><code>SideCar</code></td><td>Pre-built Node.js binaries</td><td><a href="https://Editor.Land/Doc/architecture">Architecture</a></td></tr><tr><td><code>Maintain</code></td><td>Build orchestrator</td><td><a href="https://Editor.Land/Doc/contributing">Contributing</a></td></tr><tr><td><code>Grove</code></td><td>WASM extension host - WASMtime host, gRPC protocol, API surface, and transport layer implemented; primary build integration in progress</td><td><a href="https://Editor.Land/Doc/grove">Grove</a></td></tr></tbody></table>
<hr>
<h2 id="see-also">See Also</h2>
<ul>
<li><a href="https://Editor.Land/Doc/cocoon"><code>Cocoon</code>: Extension Host</a></li>
<li><a href="https://Editor.Land/Doc/extension-development">Extension Development</a></li>
<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>
<li><a href="https://code.visualstudio.com/api/references/vscode-api"><code>VS Code</code> <code>API</code> Documentation</a></li>
</ul>`, { headings: 419, localImagePaths: 448, remoteImagePaths: 449, frontmatter: 450, imagePaths: 451 }, [420, 421, 424, 427, 430, 433, 436, 439, 442, 445], { depth: 25, slug: 407, text: 410 }, { depth: 29, slug: 422, text: 423 }, "coverage-status", "Coverage Status", { depth: 29, slug: 425, text: 426 }, "commands", "Commands", { depth: 29, slug: 428, text: 429 }, "configuration", "Configuration", { depth: 29, slug: 431, text: 432 }, "keybindings", "Keybindings", { depth: 29, slug: 434, text: 435 }, "tree-views", "Tree Views", { depth: 29, slug: 437, text: 438 }, "webview-panels", "Webview Panels", { depth: 29, slug: 440, text: 441 }, "language-server-protocol", "Language Server Protocol", { depth: 29, slug: 443, text: 444 }, "rust-api-documentation", "Rust API Documentation", { depth: 29, slug: 446, text: 447 }, "see-also", "See Also", [], [], { title: 410, section: 411, order: 16, description: 412 }, [], { id: 71, data: 453, body: 457, filePath: 458, digest: 459, rendered: 460 }, { title: 454, section: 10, order: 455, description: 456 }, "Architecture Overview", 4, "How Editor.Land's elements fit together: Mountain, Cocoon, Sky, Air, and typed IPC.", "# Architecture Overview\n\n`Editor.Land` is split into named elements so the public architecture can be\nchecked against source instead of guessed from a diagram. The primary desktop\npath is `Mountain`, the `Rust` + `Tauri` shell, running `Sky` in the operating\nsystem WebView and `Cocoon` as the `Node.js` extension host.\n\n`Air` contains background service code for downloads, updates, authentication,\nindexing, health, and `Vine` `IPC`. Whether `Air` is part of a given build\ndepends on the selected profile and feature flags.\n\nThe `Mountain` desktop path does not bundle Chromium. `Tauri` uses `WKWebView`\non macOS, `WebView2` on Windows, and `WebKitGTK` on Linux. The repository\ncontains cross-platform configuration, while public installers and full platform\nverification are still release work.\n\nPublic performance numbers are intentionally not listed here until there is a\npublished, repeatable benchmark suite. The source supports the architectural\nclaim: native `Rust` services, typed `IPC`, no bundled Chromium in the `Tauri`\npath, and a separate extension host.\n\n---\n\n## IPC Channels\n\nThe architecture has two main communication paths.\n\n**`Sky` to `Mountain` - `Tauri` `IPC`**\n\n`Sky`'s UI and `Wind` service layer call `Mountain` through `Tauri` `IPC`. The\n`Mountain` implementation lives in `Source/IPC/TauriIPCServer.rs`, including the\n`mountain_ipc_receive_message` and `mountain_ipc_get_status` entry points. This\npath handles native work such as files, terminals, clipboard, search,\nconfiguration, and `workbench` events.\n\n**`Mountain` to `Cocoon` - `Vine` `gRPC`**\n\n`Mountain` and `Cocoon` communicate over a bidirectional `gRPC` protocol\ndescribed by `Vine` proto files. `Mountain` owns the `Rust`-side services and\n`Cocoon` owns the `TypeScript`-side extension-host services. The protocol covers\nextension-host startup, document notifications, language providers, tree views,\ncommand execution, cancellation, and streaming routes where implemented.\n\nThese paths are independent enough that the website should not describe every\nservice call as flowing through one universal bus. `Tauri` `IPC`, `Vine` `gRPC`,\nand local service code each have their own job.\n\n---\n\n## Startup Shape\n\n`Mountain`'s startup path is organized around build, registration,\ninitialization, and services:\n\n```text\nmain.rs -> Binary::Main -> Build -> Register -> Initialize -> Services\n```\n\n`Build` configures the `Tauri` application. `Register` wires commands and `IPC`.\n`Initialize` prepares runtime state. `Services` starts the `Vine` and `Cocoon`\npaths before the `Tauri` event loop owns the desktop session.\n\n---\n\n## Extension Path\n\n`Cocoon` is the active `VS Code` extension-host compatibility path. It loads\nexisting extension entry points, supplies a `vscode` `API` object, and routes\nimplemented `API` calls through `Effect-TS` services and `Mountain`.\n\n`Mountain` scans installed extensions, reads manifests, handles VSIX install and\nuninstall routes, and notifies `Cocoon` with extension changes. `Output` keeps\nthe `VS Code` extension scanner and offline gallery channels wired for\nsideloaded extensions.\n\nThat supports this claim: installed `VS Code` extensions run unmodified through\nthe `Cocoon` path when the `APIs` they use are implemented. The website should\nnot claim marketplace-wide perfection unless it points to a public validation\nmatrix.\n\n---\n\n## Elements By Layer\n\n| Element                                          | Role                                                                             | Source status                           |\n| ------------------------------------------------ | -------------------------------------------------------------------------------- | --------------------------------------- |\n| [**Mountain**](https://Editor.Land/Doc/mountain) | Rust + Tauri desktop shell, native services, IPC broker, Cocoon bridge           | Primary desktop path                    |\n| [**Cocoon**](https://Editor.Land/Doc/cocoon)     | Node.js extension host for unmodified VS Code extension code                     | Primary extension path                  |\n| [**Sky**](https://Editor.Land/Doc/sky)           | Astro workbench routes and WebView bridge                                        | UI source present                       |\n| [**Wind**](https://Editor.Land/Doc/wind)         | Effect-TS workbench service layer                                                | UI service source present               |\n| [**Vine**](https://Editor.Land/Doc/vine)         | Protocol contracts and generated IPC stubs                                       | Active protocol layer                   |\n| [**Air**](https://Editor.Land/Doc/air)           | Background services for updates, downloads, authentication, indexing, and health | Source present, profile-dependent       |\n| [**Echo**](https://Editor.Land/Doc/echo)         | Rust scheduler primitives for bounded background work                            | Source present                          |\n| [**Mist**](https://Editor.Land/Doc/mist)         | DNS isolation, resolver, and secure service-boundary code                        | Source present, integration in progress |\n| [**Grove**](https://Editor.Land/Doc/grove)       | Wasmtime-backed WebAssembly host path                                            | Source present, integration in progress |\n| [**Rest**](https://Editor.Land/Doc/rest)         | OXC-based TypeScript transform work                                              | Source present                          |\n| [**Output**](https://Editor.Land/Doc/output)     | Plugin-routed output and VS Code platform transforms                             | Build pipeline source present           |\n| [**Common**](https://Editor.Land/Doc/common)     | Shared Rust and TypeScript contracts                                             | Shared source present                   |\n| [**SideCar**](https://Editor.Land/Doc/sidecar)   | Host-specific sidecar binary packaging                                           | Source present                          |\n| [**Maintain**](https://Editor.Land/Doc/maintain) | Build and maintenance scripts                                                    | Source present                          |\n| **Worker**                                       | Browser-worker support for web shell concerns                                    | Source present, release scope varies    |\n\n---\n\n## Platform Targets\n\n`Mountain`'s `Tauri` configuration includes macOS, Windows, Linux, iOS, and\nAndroid target settings. The default development configuration is not the same\nthing as published installer coverage, so the website should say \"configured\"\nunless a release artifact is actually available.\n\n| Platform | Source status                                                          |\n| -------- | ---------------------------------------------------------------------- |\n| macOS    | Primary development target and Tauri bundle path                       |\n| Windows  | Tauri configuration exists, including WebView2 bootstrapper settings   |\n| Linux    | Tauri configuration exists, integration still needs release validation |\n| iOS      | Tauri mobile configuration exists                                      |\n| Android  | Tauri mobile configuration exists                                      |\n\n---\n\n## Security Model\n\n`Mountain`'s configuration declares custom schemes such as `land:`,\n`vscode-file:`, and `vscode-webview:`. Extension webviews should be described as\nisolated through the WebView and scheme model, not as a finished universal\nsandbox for all extension behavior.\n\n`Grove` adds a `WebAssembly` host path with Wasmtime and capability-oriented\nmodules. That is real source, but it is not the primary `VS Code` extension path\ntoday. `Cocoon` remains the active compatibility host.\n\n---\n\n## Extension API Notes\n\nImplemented and routed `APIs` include core `workbench` and extension-host\nsurfaces such as commands, workspace, window, terminals, webviews, tree views,\ndiagnostics, language providers, output channels, and document events.\n\nLong-tail areas such as chat, language-model `APIs`, notebooks, tests, and some\ncustom-editor cases should be presented as in progress unless their behavior is\nverified in source and in a runnable scenario.", "Source/Content/doc/architecture.md", "9961adc65384984e", { html: 461, metadata: 462 }, '<h1 id="architecture-overview">Architecture Overview</h1>\n<p><code>Editor.Land</code> is split into named elements so the public architecture can be\nchecked against source instead of guessed from a diagram. The primary desktop\npath is <code>Mountain</code>, the <code>Rust</code> + <code>Tauri</code> shell, running <code>Sky</code> in the operating\nsystem WebView and <code>Cocoon</code> as the <code>Node.js</code> extension host.</p>\n<p><code>Air</code> contains background service code for downloads, updates, authentication,\nindexing, health, and <code>Vine</code> <code>IPC</code>. Whether <code>Air</code> is part of a given build\ndepends on the selected profile and feature flags.</p>\n<p>The <code>Mountain</code> desktop path does not bundle Chromium. <code>Tauri</code> uses <code>WKWebView</code>\non macOS, <code>WebView2</code> on Windows, and <code>WebKitGTK</code> on Linux. The repository\ncontains cross-platform configuration, while public installers and full platform\nverification are still release work.</p>\n<p>Public performance numbers are intentionally not listed here until there is a\npublished, repeatable benchmark suite. The source supports the architectural\nclaim: native <code>Rust</code> services, typed <code>IPC</code>, no bundled Chromium in the <code>Tauri</code>\npath, and a separate extension host.</p>\n<hr>\n<h2 id="ipc-channels">IPC Channels</h2>\n<p>The architecture has two main communication paths.</p>\n<p><strong><code>Sky</code> to <code>Mountain</code> - <code>Tauri</code> <code>IPC</code></strong></p>\n<p><code>Sky</code>’s UI and <code>Wind</code> service layer call <code>Mountain</code> through <code>Tauri</code> <code>IPC</code>. The\n<code>Mountain</code> implementation lives in <code>Source/IPC/TauriIPCServer.rs</code>, including the\n<code>mountain_ipc_receive_message</code> and <code>mountain_ipc_get_status</code> entry points. This\npath handles native work such as files, terminals, clipboard, search,\nconfiguration, and <code>workbench</code> events.</p>\n<p><strong><code>Mountain</code> to <code>Cocoon</code> - <code>Vine</code> <code>gRPC</code></strong></p>\n<p><code>Mountain</code> and <code>Cocoon</code> communicate over a bidirectional <code>gRPC</code> protocol\ndescribed by <code>Vine</code> proto files. <code>Mountain</code> owns the <code>Rust</code>-side services and\n<code>Cocoon</code> owns the <code>TypeScript</code>-side extension-host services. The protocol covers\nextension-host startup, document notifications, language providers, tree views,\ncommand execution, cancellation, and streaming routes where implemented.</p>\n<p>These paths are independent enough that the website should not describe every\nservice call as flowing through one universal bus. <code>Tauri</code> <code>IPC</code>, <code>Vine</code> <code>gRPC</code>,\nand local service code each have their own job.</p>\n<hr>\n<h2 id="startup-shape">Startup Shape</h2>\n<p><code>Mountain</code>’s startup path is organized around build, registration,\ninitialization, and services:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="text"><code><span class="line"><span>main.rs -> Binary::Main -> Build -> Register -> Initialize -> Services</span></span></code></pre>\n<p><code>Build</code> configures the <code>Tauri</code> application. <code>Register</code> wires commands and <code>IPC</code>.\n<code>Initialize</code> prepares runtime state. <code>Services</code> starts the <code>Vine</code> and <code>Cocoon</code>\npaths before the <code>Tauri</code> event loop owns the desktop session.</p>\n<hr>\n<h2 id="extension-path">Extension Path</h2>\n<p><code>Cocoon</code> is the active <code>VS Code</code> extension-host compatibility path. It loads\nexisting extension entry points, supplies a <code>vscode</code> <code>API</code> object, and routes\nimplemented <code>API</code> calls through <code>Effect-TS</code> services and <code>Mountain</code>.</p>\n<p><code>Mountain</code> scans installed extensions, reads manifests, handles VSIX install and\nuninstall routes, and notifies <code>Cocoon</code> with extension changes. <code>Output</code> keeps\nthe <code>VS Code</code> extension scanner and offline gallery channels wired for\nsideloaded extensions.</p>\n<p>That supports this claim: installed <code>VS Code</code> extensions run unmodified through\nthe <code>Cocoon</code> path when the <code>APIs</code> they use are implemented. The website should\nnot claim marketplace-wide perfection unless it points to a public validation\nmatrix.</p>\n<hr>\n<h2 id="elements-by-layer">Elements By Layer</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Element</th><th>Role</th><th>Source status</th></tr></thead><tbody><tr><td><a href="https://Editor.Land/Doc/mountain"><strong>Mountain</strong></a></td><td>Rust + Tauri desktop shell, native services, IPC broker, Cocoon bridge</td><td>Primary desktop path</td></tr><tr><td><a href="https://Editor.Land/Doc/cocoon"><strong>Cocoon</strong></a></td><td>Node.js extension host for unmodified VS Code extension code</td><td>Primary extension path</td></tr><tr><td><a href="https://Editor.Land/Doc/sky"><strong>Sky</strong></a></td><td>Astro workbench routes and WebView bridge</td><td>UI source present</td></tr><tr><td><a href="https://Editor.Land/Doc/wind"><strong>Wind</strong></a></td><td>Effect-TS workbench service layer</td><td>UI service source present</td></tr><tr><td><a href="https://Editor.Land/Doc/vine"><strong>Vine</strong></a></td><td>Protocol contracts and generated IPC stubs</td><td>Active protocol layer</td></tr><tr><td><a href="https://Editor.Land/Doc/air"><strong>Air</strong></a></td><td>Background services for updates, downloads, authentication, indexing, and health</td><td>Source present, profile-dependent</td></tr><tr><td><a href="https://Editor.Land/Doc/echo"><strong>Echo</strong></a></td><td>Rust scheduler primitives for bounded background work</td><td>Source present</td></tr><tr><td><a href="https://Editor.Land/Doc/mist"><strong>Mist</strong></a></td><td>DNS isolation, resolver, and secure service-boundary code</td><td>Source present, integration in progress</td></tr><tr><td><a href="https://Editor.Land/Doc/grove"><strong>Grove</strong></a></td><td>Wasmtime-backed WebAssembly host path</td><td>Source present, integration in progress</td></tr><tr><td><a href="https://Editor.Land/Doc/rest"><strong>Rest</strong></a></td><td>OXC-based TypeScript transform work</td><td>Source present</td></tr><tr><td><a href="https://Editor.Land/Doc/output"><strong>Output</strong></a></td><td>Plugin-routed output and VS Code platform transforms</td><td>Build pipeline source present</td></tr><tr><td><a href="https://Editor.Land/Doc/common"><strong>Common</strong></a></td><td>Shared Rust and TypeScript contracts</td><td>Shared source present</td></tr><tr><td><a href="https://Editor.Land/Doc/sidecar"><strong>SideCar</strong></a></td><td>Host-specific sidecar binary packaging</td><td>Source present</td></tr><tr><td><a href="https://Editor.Land/Doc/maintain"><strong>Maintain</strong></a></td><td>Build and maintenance scripts</td><td>Source present</td></tr><tr><td><strong>Worker</strong></td><td>Browser-worker support for web shell concerns</td><td>Source present, release scope varies</td></tr></tbody></table>\n<hr>\n<h2 id="platform-targets">Platform Targets</h2>\n<p><code>Mountain</code>’s <code>Tauri</code> configuration includes macOS, Windows, Linux, iOS, and\nAndroid target settings. The default development configuration is not the same\nthing as published installer coverage, so the website should say “configured”\nunless a release artifact is actually available.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Platform</th><th>Source status</th></tr></thead><tbody><tr><td>macOS</td><td>Primary development target and Tauri bundle path</td></tr><tr><td>Windows</td><td>Tauri configuration exists, including WebView2 bootstrapper settings</td></tr><tr><td>Linux</td><td>Tauri configuration exists, integration still needs release validation</td></tr><tr><td>iOS</td><td>Tauri mobile configuration exists</td></tr><tr><td>Android</td><td>Tauri mobile configuration exists</td></tr></tbody></table>\n<hr>\n<h2 id="security-model">Security Model</h2>\n<p><code>Mountain</code>’s configuration declares custom schemes such as <code>land:</code>,\n<code>vscode-file:</code>, and <code>vscode-webview:</code>. Extension webviews should be described as\nisolated through the WebView and scheme model, not as a finished universal\nsandbox for all extension behavior.</p>\n<p><code>Grove</code> adds a <code>WebAssembly</code> host path with Wasmtime and capability-oriented\nmodules. That is real source, but it is not the primary <code>VS Code</code> extension path\ntoday. <code>Cocoon</code> remains the active compatibility host.</p>\n<hr>\n<h2 id="extension-api-notes">Extension API Notes</h2>\n<p>Implemented and routed <code>APIs</code> include core <code>workbench</code> and extension-host\nsurfaces such as commands, workspace, window, terminals, webviews, tree views,\ndiagnostics, language providers, output channels, and document events.</p>\n<p>Long-tail areas such as chat, language-model <code>APIs</code>, notebooks, tests, and some\ncustom-editor cases should be presented as in progress unless their behavior is\nverified in source and in a runnable scenario.</p>', { headings: 463, localImagePaths: 485, remoteImagePaths: 486, frontmatter: 487, imagePaths: 488 }, [464, 466, 469, 472, 475, 478, 479, 482], { depth: 25, slug: 465, text: 454 }, "architecture-overview", { depth: 29, slug: 467, text: 468 }, "ipc-channels", "IPC Channels", { depth: 29, slug: 470, text: 471 }, "startup-shape", "Startup Shape", { depth: 29, slug: 473, text: 474 }, "extension-path", "Extension Path", { depth: 29, slug: 476, text: 477 }, "elements-by-layer", "Elements By Layer", { depth: 29, slug: 313, text: 314 }, { depth: 29, slug: 480, text: 481 }, "security-model", "Security Model", { depth: 29, slug: 483, text: 484 }, "extension-api-notes", "Extension API Notes", [], [], { title: 454, section: 10, order: 455, description: 456 }, [], "ci-cd-pipeline", { id: 489, data: 491, body: 496, filePath: 497, digest: 498, rendered: 499 }, { title: 492, section: 493, order: 494, description: 495 }, "CI/CD Pipeline", "Reference", 47, "Documentation of the five GitHub Actions workflow types used across Land elements.", '# CI/CD Pipeline\n\nEvery Land element runs on GitHub Actions. The pipeline consists of five\nstandard workflow types, each handling a specific concern.\n\n---\n\n## Workflow Types\n\n### Rust.yml (Build Pipeline)\n\n**Applies to:** Mountain, Air, Common, Echo, Grove, Mist, Rest, SideCar, Vine,\nMaintain\n\n**Triggers:**\n\n- Push to `Current` branch\n- Pull request targeting `Current`\n- Manual dispatch\n\n**What it does:**\n\n1. Checks out the repository.\n2. Installs Rust toolchain (both `stable` and `nightly` via strategy matrix).\n3. Restores Cargo cache (registry index, cache, git db, and target directories).\n4. Runs `cargo build --release --all-features --manifest-path ./Cargo.toml`.\n\n**Concurrency group:** `Rust-${{ github.workflow }}-${{ github.ref }}` with\n`cancel-in-progress: true` to avoid redundant builds on rapid pushes.\n\n### Node.yml (Pre-Publish Pipeline)\n\n**Applies to:** Wind, Cocoon, Worker\n\n**Triggers:**\n\n- Push to `Current` branch\n- Pull request targeting `Current`\n- Manual dispatch\n\n**What it does:**\n\n1. Checks out the repository.\n2. Sets up pnpm (v9.3.0) with recursive install.\n3. Sets up Node.js 24 with pnpm caching.\n4. Runs `pnpm install`.\n5. Runs `pnpm run prepublishOnly`.\n6. Uploads `./Target` as a build artefact.\n\n**Concurrency group:** `Node-${{ github.workflow }}-${{ github.ref }}`.\n\n### NPM.yml (Publish Pipeline)\n\n**Applies to:** Echo, Wind, Worker\n\n**Triggers:**\n\n- GitHub release creation\n- Manual dispatch\n- Reusable workflow call\n\n**What it does:**\n\n1. Checks out the repository.\n2. Sets up Node.js 24.\n3. Installs latest npm globally.\n4. Runs `npm publish --legacy-peer-deps --ignore-scripts`.\n\nRuns in the `Release` environment with `id-token: write` for provenance\nattestation.\n\n**Concurrency group:** `NPM-${{ github.workflow }}-${{ github.ref }}`.\n\n### GitHub.yml (Issue/PR Management)\n\n**Applies to:** All 15 elements\n\n**Triggers:**\n\n- Issue opened\n- Pull request opened\n\n**What it does:**\n\n1. Uses `pozil/auto-assign-issue@v3.0.0` to auto-assign new issues and PRs to\n   the repository maintainer (`NikolaRHristov`).\n\n**Concurrency group:** `GitHub-${{ github.workflow }}-${{ github.ref }}`.\n\n### Auto.yml (Automated Timestamp Update)\n\n**Applies to:** Echo, Worker\n\n**Triggers:**\n\n- Daily schedule (`0 0 * * *`)\n- Manual dispatch\n- Reusable workflow call\n\n**What it does:**\n\n1. Checks out the repository.\n2. Writes the current date to `.github/Update.md`: `Update: $(date)`.\n3. Commits with git identity "Auto" (`Commit@PlayForm.Cloud`).\n4. Pushes to `Current` branch via `ad-m/github-push-action`.\n\nThis keeps a visible timestamp of the last repository activity even when no\nother commits land.\n\n**Concurrency group:** `Auto-${{ github.workflow }}-${{ github.ref }}`.\n\n### Cloudflare.yml (Workers Deployment)\n\n**Applies to:** Echo\n\n**Triggers:**\n\n- Push to `Current` branch\n- Pull request targeting `Current`\n- Manual dispatch\n\n**What it does:** Deploys Echo to Cloudflare Workers infrastructure.\n\n---\n\n## Shared Patterns\n\n### Concurrency Control\n\nAll workflows use branch-scoped concurrency groups. This means:\n\n- Only one build runs per branch at a time.\n- New commits cancel in-progress builds.\n- PR builds and push builds to the same branch do not race.\n\n### Telemetry Opt-Out\n\nEvery workflow sets 30+ environment variables to disable third-party telemetry\nfrom tools running in the CI environment. This covers Adblock, Astro, Azure,\nDocker, Gatsby, Homebrew, InfluxDB, Next.js, Nuxt, PowerShell, Stripe,\nTerraform, VCPkg, and others.\n\n### Action Versions\n\nThe pipeline uses pinned action versions:\n\n- `actions/checkout@v6.0.2`\n- `actions/setup-node@v6.4.0`\n- `actions/cache@v5.0.5`\n- `actions/upload-artifact@v7.0.1`\n- `actions-rs/toolchain@v1.0.7`\n- `actions-rs/cargo@v1.0.3`\n- `pnpm/action-setup@v6.0.8`\n- `pozil/auto-assign-issue@v3.0.0`\n- `ad-m/github-push-action@v1.1.0`\n\n### Permissions\n\nWorkflow permission scopes vary by type:\n\n- **Rust.yml:** `security-events: write`\n- **Node.yml:** `security-events: write`, `contents: write`,\n  `pull-requests: write`\n- **NPM.yml:** `contents: read`, `id-token: write` (job-level)\n- **GitHub.yml:** `issues: write`, `pull-requests: write`\n- **Auto.yml:** `contents: write`', "Source/Content/doc/ci-cd-pipeline.md", "d1d88506884d9e23", { html: 500, metadata: 501 }, '<h1 id="cicd-pipeline">CI/CD Pipeline</h1>\n<p>Every Land element runs on GitHub Actions. The pipeline consists of five\nstandard workflow types, each handling a specific concern.</p>\n<hr>\n<h2 id="workflow-types">Workflow Types</h2>\n<h3 id="rustyml-build-pipeline">Rust.yml (Build Pipeline)</h3>\n<p><strong>Applies to:</strong> Mountain, Air, Common, Echo, Grove, Mist, Rest, SideCar, Vine,\nMaintain</p>\n<p><strong>Triggers:</strong></p>\n<ul>\n<li>Push to <code>Current</code> branch</li>\n<li>Pull request targeting <code>Current</code></li>\n<li>Manual dispatch</li>\n</ul>\n<p><strong>What it does:</strong></p>\n<ol>\n<li>Checks out the repository.</li>\n<li>Installs Rust toolchain (both <code>stable</code> and <code>nightly</code> via strategy matrix).</li>\n<li>Restores Cargo cache (registry index, cache, git db, and target directories).</li>\n<li>Runs <code>cargo build --release --all-features --manifest-path ./Cargo.toml</code>.</li>\n</ol>\n<p><strong>Concurrency group:</strong> <code>Rust-${{ github.workflow }}-${{ github.ref }}</code> with\n<code>cancel-in-progress: true</code> to avoid redundant builds on rapid pushes.</p>\n<h3 id="nodeyml-pre-publish-pipeline">Node.yml (Pre-Publish Pipeline)</h3>\n<p><strong>Applies to:</strong> Wind, Cocoon, Worker</p>\n<p><strong>Triggers:</strong></p>\n<ul>\n<li>Push to <code>Current</code> branch</li>\n<li>Pull request targeting <code>Current</code></li>\n<li>Manual dispatch</li>\n</ul>\n<p><strong>What it does:</strong></p>\n<ol>\n<li>Checks out the repository.</li>\n<li>Sets up pnpm (v9.3.0) with recursive install.</li>\n<li>Sets up Node.js 24 with pnpm caching.</li>\n<li>Runs <code>pnpm install</code>.</li>\n<li>Runs <code>pnpm run prepublishOnly</code>.</li>\n<li>Uploads <code>./Target</code> as a build artefact.</li>\n</ol>\n<p><strong>Concurrency group:</strong> <code>Node-${{ github.workflow }}-${{ github.ref }}</code>.</p>\n<h3 id="npmyml-publish-pipeline">NPM.yml (Publish Pipeline)</h3>\n<p><strong>Applies to:</strong> Echo, Wind, Worker</p>\n<p><strong>Triggers:</strong></p>\n<ul>\n<li>GitHub release creation</li>\n<li>Manual dispatch</li>\n<li>Reusable workflow call</li>\n</ul>\n<p><strong>What it does:</strong></p>\n<ol>\n<li>Checks out the repository.</li>\n<li>Sets up Node.js 24.</li>\n<li>Installs latest npm globally.</li>\n<li>Runs <code>npm publish --legacy-peer-deps --ignore-scripts</code>.</li>\n</ol>\n<p>Runs in the <code>Release</code> environment with <code>id-token: write</code> for provenance\nattestation.</p>\n<p><strong>Concurrency group:</strong> <code>NPM-${{ github.workflow }}-${{ github.ref }}</code>.</p>\n<h3 id="githubyml-issuepr-management">GitHub.yml (Issue/PR Management)</h3>\n<p><strong>Applies to:</strong> All 15 elements</p>\n<p><strong>Triggers:</strong></p>\n<ul>\n<li>Issue opened</li>\n<li>Pull request opened</li>\n</ul>\n<p><strong>What it does:</strong></p>\n<ol>\n<li>Uses <code>pozil/auto-assign-issue@v3.0.0</code> to auto-assign new issues and PRs to\nthe repository maintainer (<code>NikolaRHristov</code>).</li>\n</ol>\n<p><strong>Concurrency group:</strong> <code>GitHub-${{ github.workflow }}-${{ github.ref }}</code>.</p>\n<h3 id="autoyml-automated-timestamp-update">Auto.yml (Automated Timestamp Update)</h3>\n<p><strong>Applies to:</strong> Echo, Worker</p>\n<p><strong>Triggers:</strong></p>\n<ul>\n<li>Daily schedule (<code>0 0 * * *</code>)</li>\n<li>Manual dispatch</li>\n<li>Reusable workflow call</li>\n</ul>\n<p><strong>What it does:</strong></p>\n<ol>\n<li>Checks out the repository.</li>\n<li>Writes the current date to <code>.github/Update.md</code>: <code>Update: $(date)</code>.</li>\n<li>Commits with git identity “Auto” (<code>Commit@PlayForm.Cloud</code>).</li>\n<li>Pushes to <code>Current</code> branch via <code>ad-m/github-push-action</code>.</li>\n</ol>\n<p>This keeps a visible timestamp of the last repository activity even when no\nother commits land.</p>\n<p><strong>Concurrency group:</strong> <code>Auto-${{ github.workflow }}-${{ github.ref }}</code>.</p>\n<h3 id="cloudflareyml-workers-deployment">Cloudflare.yml (Workers Deployment)</h3>\n<p><strong>Applies to:</strong> Echo</p>\n<p><strong>Triggers:</strong></p>\n<ul>\n<li>Push to <code>Current</code> branch</li>\n<li>Pull request targeting <code>Current</code></li>\n<li>Manual dispatch</li>\n</ul>\n<p><strong>What it does:</strong> Deploys Echo to Cloudflare Workers infrastructure.</p>\n<hr>\n<h2 id="shared-patterns">Shared Patterns</h2>\n<h3 id="concurrency-control">Concurrency Control</h3>\n<p>All workflows use branch-scoped concurrency groups. This means:</p>\n<ul>\n<li>Only one build runs per branch at a time.</li>\n<li>New commits cancel in-progress builds.</li>\n<li>PR builds and push builds to the same branch do not race.</li>\n</ul>\n<h3 id="telemetry-opt-out">Telemetry Opt-Out</h3>\n<p>Every workflow sets 30+ environment variables to disable third-party telemetry\nfrom tools running in the CI environment. This covers Adblock, Astro, Azure,\nDocker, Gatsby, Homebrew, InfluxDB, Next.js, Nuxt, PowerShell, Stripe,\nTerraform, VCPkg, and others.</p>\n<h3 id="action-versions">Action Versions</h3>\n<p>The pipeline uses pinned action versions:</p>\n<ul>\n<li><code>actions/checkout@v6.0.2</code></li>\n<li><code>actions/setup-node@v6.4.0</code></li>\n<li><code>actions/cache@v5.0.5</code></li>\n<li><code>actions/upload-artifact@v7.0.1</code></li>\n<li><code>actions-rs/toolchain@v1.0.7</code></li>\n<li><code>actions-rs/cargo@v1.0.3</code></li>\n<li><code>pnpm/action-setup@v6.0.8</code></li>\n<li><code>pozil/auto-assign-issue@v3.0.0</code></li>\n<li><code>ad-m/github-push-action@v1.1.0</code></li>\n</ul>\n<h3 id="permissions">Permissions</h3>\n<p>Workflow permission scopes vary by type:</p>\n<ul>\n<li><strong>Rust.yml:</strong> <code>security-events: write</code></li>\n<li><strong>Node.yml:</strong> <code>security-events: write</code>, <code>contents: write</code>,\n<code>pull-requests: write</code></li>\n<li><strong>NPM.yml:</strong> <code>contents: read</code>, <code>id-token: write</code> (job-level)</li>\n<li><strong>GitHub.yml:</strong> <code>issues: write</code>, <code>pull-requests: write</code></li>\n<li><strong>Auto.yml:</strong> <code>contents: write</code></li>\n</ul>', { headings: 502, localImagePaths: 541, remoteImagePaths: 542, frontmatter: 543, imagePaths: 544 }, [503, 505, 508, 511, 514, 517, 520, 523, 526, 529, 532, 535, 538], { depth: 25, slug: 504, text: 492 }, "cicd-pipeline", { depth: 29, slug: 506, text: 507 }, "workflow-types", "Workflow Types", { depth: 347, slug: 509, text: 510 }, "rustyml-build-pipeline", "Rust.yml (Build Pipeline)", { depth: 347, slug: 512, text: 513 }, "nodeyml-pre-publish-pipeline", "Node.yml (Pre-Publish Pipeline)", { depth: 347, slug: 515, text: 516 }, "npmyml-publish-pipeline", "NPM.yml (Publish Pipeline)", { depth: 347, slug: 518, text: 519 }, "githubyml-issuepr-management", "GitHub.yml (Issue/PR Management)", { depth: 347, slug: 521, text: 522 }, "autoyml-automated-timestamp-update", "Auto.yml (Automated Timestamp Update)", { depth: 347, slug: 524, text: 525 }, "cloudflareyml-workers-deployment", "Cloudflare.yml (Workers Deployment)", { depth: 29, slug: 527, text: 528 }, "shared-patterns", "Shared Patterns", { depth: 347, slug: 530, text: 531 }, "concurrency-control", "Concurrency Control", { depth: 347, slug: 533, text: 534 }, "telemetry-opt-out", "Telemetry Opt-Out", { depth: 347, slug: 536, text: 537 }, "action-versions", "Action Versions", { depth: 347, slug: 539, text: 540 }, "permissions", "Permissions", [], [], { title: 492, section: 493, order: 494, description: 495 }, [], "cocoon", { id: 545, data: 547, body: 550, filePath: 551, digest: 552, rendered: 553 }, { title: 13, section: 381, order: 548, description: 549 }, 12, "The Node.js extension host that runs unmodified VS Code extension entry points through Effect-TS services and Vine IPC.", "# Cocoon\n\n`Cocoon` is the active `VS Code` extension-host compatibility path for\n`Editor.Land`. It is a `Node.js` process that loads existing extension entry\npoints, provides a `vscode` `API` object, and routes implemented `API` calls\nthrough `Effect-TS` services and `Mountain`.\n\nThe source supports this claim: installed `VS Code` extensions can run\nunmodified through `Cocoon` when the `APIs` they use are implemented.\nMarketplace-wide coverage still needs a public validation matrix.\n\n---\n\n## Source Layout 🗺️\n\nKey `Cocoon` source areas:\n\n- **`Bootstrap/`** - process entry point, service composition, and `gRPC`\n  startup.\n- **`Generated/`** - `TypeScript` stubs generated from `Vine` protocol\n  definitions.\n- **`IPC/`** - typed `IPC` helpers and `Sky` event registry support.\n- **`PatchProcess/`** - targeted adaptation for `VS Code` platform assumptions\n  that do not fit the Land runtime.\n- **`Services/`** - the extension-host service layer.\n- **`TypeConverter/`** - conversion between `Vine` DTOs and `VS Code`-facing\n  types.\n- **`WebviewPanel/`** - `vscode.WebviewPanel` support through `Mountain` and the\n  WebView bridge.\n\n---\n\n## Extension-Host Services 🔌\n\n`Cocoon` contains source paths for:\n\n- Module interception so `require(\"vscode\")` returns Land's `API` shim.\n- Extension loading, manifest handling, activation, and deactivation.\n- `ExtensionContext` construction for extension `activate()` calls.\n- Commands, workspace, configuration, windows, terminals, diagnostics, output,\n  webviews, tree views, and language-provider registration.\n- Health and performance instrumentation for local diagnostics.\n- `gRPC` request handling through `ProcessMountainRequest`,\n  `SendMountainNotification`, and `CancelOperation`.\n\nSome namespaces exist as stubs or partial routes. Chat, language-model,\nnotebook, and tests `APIs` should stay marked WIP until their runtime behavior\nis verified.\n\n---\n\n## Mountain Communication\n\n`Cocoon` talks to `Mountain` through `Vine` `gRPC`. `Mountain` sends\nextension-host requests to `Cocoon`, and `Cocoon` sends events and results back\nto `Mountain`. The wire shape is generated from protocol definitions rather than\nhandwritten `JSON`.\n\nThe route is real source, but public docs should name the route being discussed\ninstead of saying all editor communication flows through `Cocoon`.\n\n---\n\n## Module Interception\n\n`Cocoon`'s module interception path is what lets existing extension code import\nor require `VS Code` `APIs` without being repackaged for Land. The host supplies\nthe Land `vscode` `API` shim and maps implemented calls onto services.\n\nThis is a strong compatibility point, but not a promise that every `API` in\nevery extension has runtime parity yet.\n\n---\n\n## Status 🚀\n\n`Cocoon` should be shown as active for unmodified extension execution and WIP\nfor long-tail `API` coverage. Claims about specific extension categories should\nlink to tests or a validation matrix.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Mountain`](https://Editor.Land/Doc/mountain)\n- [`Vine`](https://Editor.Land/Doc/vine)\n- [`VS Code` Compatibility](https://Editor.Land/Blog/VSCodeCompatibility)", "Source/Content/doc/cocoon.md", "ece93cba1acf5f56", { html: 554, metadata: 555 }, '<h1 id="cocoon">Cocoon</h1>\n<p><code>Cocoon</code> is the active <code>VS Code</code> extension-host compatibility path for\n<code>Editor.Land</code>. It is a <code>Node.js</code> process that loads existing extension entry\npoints, provides a <code>vscode</code> <code>API</code> object, and routes implemented <code>API</code> calls\nthrough <code>Effect-TS</code> services and <code>Mountain</code>.</p>\n<p>The source supports this claim: installed <code>VS Code</code> extensions can run\nunmodified through <code>Cocoon</code> when the <code>APIs</code> they use are implemented.\nMarketplace-wide coverage still needs a public validation matrix.</p>\n<hr>\n<h2 id="source-layout️">Source Layout 🗺️</h2>\n<p>Key <code>Cocoon</code> source areas:</p>\n<ul>\n<li><strong><code>Bootstrap/</code></strong> - process entry point, service composition, and <code>gRPC</code>\nstartup.</li>\n<li><strong><code>Generated/</code></strong> - <code>TypeScript</code> stubs generated from <code>Vine</code> protocol\ndefinitions.</li>\n<li><strong><code>IPC/</code></strong> - typed <code>IPC</code> helpers and <code>Sky</code> event registry support.</li>\n<li><strong><code>PatchProcess/</code></strong> - targeted adaptation for <code>VS Code</code> platform assumptions\nthat do not fit the Land runtime.</li>\n<li><strong><code>Services/</code></strong> - the extension-host service layer.</li>\n<li><strong><code>TypeConverter/</code></strong> - conversion between <code>Vine</code> DTOs and <code>VS Code</code>-facing\ntypes.</li>\n<li><strong><code>WebviewPanel/</code></strong> - <code>vscode.WebviewPanel</code> support through <code>Mountain</code> and the\nWebView bridge.</li>\n</ul>\n<hr>\n<h2 id="extension-host-services">Extension-Host Services 🔌</h2>\n<p><code>Cocoon</code> contains source paths for:</p>\n<ul>\n<li>Module interception so <code>require("vscode")</code> returns Land’s <code>API</code> shim.</li>\n<li>Extension loading, manifest handling, activation, and deactivation.</li>\n<li><code>ExtensionContext</code> construction for extension <code>activate()</code> calls.</li>\n<li>Commands, workspace, configuration, windows, terminals, diagnostics, output,\nwebviews, tree views, and language-provider registration.</li>\n<li>Health and performance instrumentation for local diagnostics.</li>\n<li><code>gRPC</code> request handling through <code>ProcessMountainRequest</code>,\n<code>SendMountainNotification</code>, and <code>CancelOperation</code>.</li>\n</ul>\n<p>Some namespaces exist as stubs or partial routes. Chat, language-model,\nnotebook, and tests <code>APIs</code> should stay marked WIP until their runtime behavior\nis verified.</p>\n<hr>\n<h2 id="mountain-communication">Mountain Communication</h2>\n<p><code>Cocoon</code> talks to <code>Mountain</code> through <code>Vine</code> <code>gRPC</code>. <code>Mountain</code> sends\nextension-host requests to <code>Cocoon</code>, and <code>Cocoon</code> sends events and results back\nto <code>Mountain</code>. The wire shape is generated from protocol definitions rather than\nhandwritten <code>JSON</code>.</p>\n<p>The route is real source, but public docs should name the route being discussed\ninstead of saying all editor communication flows through <code>Cocoon</code>.</p>\n<hr>\n<h2 id="module-interception">Module Interception</h2>\n<p><code>Cocoon</code>’s module interception path is what lets existing extension code import\nor require <code>VS Code</code> <code>APIs</code> without being repackaged for Land. The host supplies\nthe Land <code>vscode</code> <code>API</code> shim and maps implemented calls onto services.</p>\n<p>This is a strong compatibility point, but not a promise that every <code>API</code> in\nevery extension has runtime parity yet.</p>\n<hr>\n<h2 id="status">Status 🚀</h2>\n<p><code>Cocoon</code> should be shown as active for unmodified extension execution and WIP\nfor long-tail <code>API</code> coverage. Claims about specific extension categories should\nlink to tests or a validation matrix.</p>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/mountain"><code>Mountain</code></a></li>\n<li><a href="https://Editor.Land/Doc/vine"><code>Vine</code></a></li>\n<li><a href="https://Editor.Land/Blog/VSCodeCompatibility"><code>VS Code</code> Compatibility</a></li>\n</ul>', { headings: 556, localImagePaths: 574, remoteImagePaths: 575, frontmatter: 576, imagePaths: 577 }, [557, 558, 561, 564, 567, 570, 573], { depth: 25, slug: 545, text: 13 }, { depth: 29, slug: 559, text: 560 }, "source-layout️", "Source Layout 🗺️", { depth: 29, slug: 562, text: 563 }, "extension-host-services", "Extension-Host Services 🔌", { depth: 29, slug: 565, text: 566 }, "mountain-communication", "Mountain Communication", { depth: 29, slug: 568, text: 569 }, "module-interception", "Module Interception", { depth: 29, slug: 571, text: 572 }, "status", "Status 🚀", { depth: 29, slug: 401, text: 402 }, [], [], { title: 13, section: 381, order: 548, description: 549 }, [], "cocoon-vscode-validation", { id: 578, data: 580, body: 585, filePath: 586, digest: 587, rendered: 588 }, { title: 581, section: 582, order: 583, description: 584 }, "Cocoon - VS Code Validation Checklist", "Deep Dive", 216, "VS Code source validation results and compatibility assessment for Cocoon", "# Cocoon - VS Code Validation Checklist\n\nThis document summarizes validation of Cocoon's implementation against the\noriginal VS Code source to ensure extension host compatibility.\n\n## Validation Methodology\n\nCocoon source compared against Microsoft's VS Code GitHub repository, focusing\non extension host architecture, API compatibility, and communication patterns.\n\n## Core Extension Host\n\nAll core features validated against VS Code equivalents:\n\n| Feature                | Cocoon Implementation              | VS Code Equivalent           | Status                          |\n| :--------------------- | :--------------------------------- | :--------------------------- | :------------------------------ |\n| Extension activation   | ExtensionHost.ts - ActivateById()  | activateById()               | Matching activation flow        |\n| Extension lifecycle    | ExtensionHost.ts - DeactivateAll() | deactivateAll()              | Proper cleanup                  |\n| Extension registry     | ExtensionDescriptionRegistry       | ExtensionDescriptionRegistry | Compatible structure            |\n| API factory            | APIFactory.ts                      | ExtHostApiImpl               | Similar API structure           |\n| Context injection      | ExtensionContext creation          | ExtensionContext             | Compatible context              |\n| require() interception | RequireInterceptor.ts              | ExtHostRequireInterceptor    | Similar pattern                 |\n| ESM interception       | ESMInterceptor.ts                  | N/A (ESM not in VS Code)     | Advanced feature beyond VS Code |\n\n## IPC Communication\n\n| Feature               | Cocoon                              | VS Code Equivalent     | Status                   |\n| :-------------------- | :---------------------------------- | :--------------------- | :----------------------- |\n| Protocol definition   | vine_ipc.proto                      | IExtensionHostInitData | gRPC vs custom protocol  |\n| Message passing       | IPC.ts SendRequest/SendNotification | RPCProtocol            | Different but compatible |\n| Connection management | gRPC client management              | IPC channel management | Robust implementation    |\n\n## Service Layer Coverage\n\n| Service   | Cocoon          | VS Code Equivalent | Status             |\n| :-------- | :-------------- | :----------------- | :----------------- |\n| Commands  | Command.ts      | IExtHostCommands   | Matching API       |\n| Documents | Document.ts     | IExtHostDocuments  | Compatible         |\n| Window    | Window.ts       | IExtHostWindow     | Matching methods   |\n| Workspace | WorkSpace.ts    | IExtHostWorkspace  | Compatible         |\n| Debug     | Debug.ts        | IExtHostDebug      | Matching structure |\n| Terminal  | Task.ts         | IExtHostTerminal   | Compatible         |\n| Webview   | WebViewPanel.ts | IExtHostWebview    | Matching API       |\n\n## Effect-TS vs VS Code OOP\n\n| Aspect               | Cocoon             | VS Code               | Compatibility |\n| :------------------- | :----------------- | :-------------------- | :------------ |\n| Dependency injection | Effect-TS Layers   | Service collection    | Bridged       |\n| Error handling       | Effect error types | Exception handling    | Mapped        |\n| Async operations     | Effect pipelines   | Promises/async-await  | Compatible    |\n| Service composition  | Layer composition  | Service instantiation | Similar       |\n\n## API Surface\n\nValidated core APIs:\n\n- **Workspace**: getConfiguration, onDidChangeConfiguration, openTextDocument\n- **Window**: showInformationMessage, createTerminal, showQuickPick\n- **Commands**: registerCommand, executeCommand\n- **Debug**: startDebugging, registerDebugConfigurationProvider\n\nAdvanced APIs implemented:\n\n- Language features (hover, completion, definition) - partially implemented\n- SCM API - basic implementation\n- Tree View API - implemented\n\n## Performance Expectations\n\n| Metric              | VS Code  | Cocoon Expected |\n| :------------------ | :------- | :-------------- |\n| Extension load time | ~1-2s    | ~1-2s           |\n| API call latency    | <100ms   | <100ms          |\n| Memory usage        | Moderate | Comparable      |\n\n## Compatibility Gaps\n\nKnown differences from VS Code:\n\n1. **ESM Support**: Cocoon has ESM interception, VS Code is CJS-only (advantage)\n2. **Effect-TS Architecture**: Different programming paradigm (different, not\n   incompatible)\n3. **gRPC Protocol**: Different communication protocol (more efficient than VS\n   Code custom protocol)\n\n## Related\n\n- [Cocoon overview](/doc/cocoon)\n- [Cocoon deep dive](/doc/deep-dive-cocoon)", "Source/Content/doc/cocoon-vscode-validation.md", "7e497ce71f21c1f4", { html: 589, metadata: 590 }, '<h1 id="cocoon---vs-code-validation-checklist">Cocoon - VS Code Validation Checklist</h1>\n<p>This document summarizes validation of Cocoon’s implementation against the\noriginal VS Code source to ensure extension host compatibility.</p>\n<h2 id="validation-methodology">Validation Methodology</h2>\n<p>Cocoon source compared against Microsoft’s VS Code GitHub repository, focusing\non extension host architecture, API compatibility, and communication patterns.</p>\n<h2 id="core-extension-host">Core Extension Host</h2>\n<p>All core features validated against VS Code equivalents:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th align="left">Feature</th><th align="left">Cocoon Implementation</th><th align="left">VS Code Equivalent</th><th align="left">Status</th></tr></thead><tbody><tr><td align="left">Extension activation</td><td align="left">ExtensionHost.ts - ActivateById()</td><td align="left">activateById()</td><td align="left">Matching activation flow</td></tr><tr><td align="left">Extension lifecycle</td><td align="left">ExtensionHost.ts - DeactivateAll()</td><td align="left">deactivateAll()</td><td align="left">Proper cleanup</td></tr><tr><td align="left">Extension registry</td><td align="left">ExtensionDescriptionRegistry</td><td align="left">ExtensionDescriptionRegistry</td><td align="left">Compatible structure</td></tr><tr><td align="left">API factory</td><td align="left">APIFactory.ts</td><td align="left">ExtHostApiImpl</td><td align="left">Similar API structure</td></tr><tr><td align="left">Context injection</td><td align="left">ExtensionContext creation</td><td align="left">ExtensionContext</td><td align="left">Compatible context</td></tr><tr><td align="left">require() interception</td><td align="left">RequireInterceptor.ts</td><td align="left">ExtHostRequireInterceptor</td><td align="left">Similar pattern</td></tr><tr><td align="left">ESM interception</td><td align="left">ESMInterceptor.ts</td><td align="left">N/A (ESM not in VS Code)</td><td align="left">Advanced feature beyond VS Code</td></tr></tbody></table>\n<h2 id="ipc-communication">IPC Communication</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th align="left">Feature</th><th align="left">Cocoon</th><th align="left">VS Code Equivalent</th><th align="left">Status</th></tr></thead><tbody><tr><td align="left">Protocol definition</td><td align="left">vine_ipc.proto</td><td align="left">IExtensionHostInitData</td><td align="left">gRPC vs custom protocol</td></tr><tr><td align="left">Message passing</td><td align="left">IPC.ts SendRequest/SendNotification</td><td align="left">RPCProtocol</td><td align="left">Different but compatible</td></tr><tr><td align="left">Connection management</td><td align="left">gRPC client management</td><td align="left">IPC channel management</td><td align="left">Robust implementation</td></tr></tbody></table>\n<h2 id="service-layer-coverage">Service Layer Coverage</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th align="left">Service</th><th align="left">Cocoon</th><th align="left">VS Code Equivalent</th><th align="left">Status</th></tr></thead><tbody><tr><td align="left">Commands</td><td align="left">Command.ts</td><td align="left">IExtHostCommands</td><td align="left">Matching API</td></tr><tr><td align="left">Documents</td><td align="left">Document.ts</td><td align="left">IExtHostDocuments</td><td align="left">Compatible</td></tr><tr><td align="left">Window</td><td align="left">Window.ts</td><td align="left">IExtHostWindow</td><td align="left">Matching methods</td></tr><tr><td align="left">Workspace</td><td align="left">WorkSpace.ts</td><td align="left">IExtHostWorkspace</td><td align="left">Compatible</td></tr><tr><td align="left">Debug</td><td align="left">Debug.ts</td><td align="left">IExtHostDebug</td><td align="left">Matching structure</td></tr><tr><td align="left">Terminal</td><td align="left">Task.ts</td><td align="left">IExtHostTerminal</td><td align="left">Compatible</td></tr><tr><td align="left">Webview</td><td align="left">WebViewPanel.ts</td><td align="left">IExtHostWebview</td><td align="left">Matching API</td></tr></tbody></table>\n<h2 id="effect-ts-vs-vs-code-oop">Effect-TS vs VS Code OOP</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th align="left">Aspect</th><th align="left">Cocoon</th><th align="left">VS Code</th><th align="left">Compatibility</th></tr></thead><tbody><tr><td align="left">Dependency injection</td><td align="left">Effect-TS Layers</td><td align="left">Service collection</td><td align="left">Bridged</td></tr><tr><td align="left">Error handling</td><td align="left">Effect error types</td><td align="left">Exception handling</td><td align="left">Mapped</td></tr><tr><td align="left">Async operations</td><td align="left">Effect pipelines</td><td align="left">Promises/async-await</td><td align="left">Compatible</td></tr><tr><td align="left">Service composition</td><td align="left">Layer composition</td><td align="left">Service instantiation</td><td align="left">Similar</td></tr></tbody></table>\n<h2 id="api-surface">API Surface</h2>\n<p>Validated core APIs:</p>\n<ul>\n<li><strong>Workspace</strong>: getConfiguration, onDidChangeConfiguration, openTextDocument</li>\n<li><strong>Window</strong>: showInformationMessage, createTerminal, showQuickPick</li>\n<li><strong>Commands</strong>: registerCommand, executeCommand</li>\n<li><strong>Debug</strong>: startDebugging, registerDebugConfigurationProvider</li>\n</ul>\n<p>Advanced APIs implemented:</p>\n<ul>\n<li>Language features (hover, completion, definition) - partially implemented</li>\n<li>SCM API - basic implementation</li>\n<li>Tree View API - implemented</li>\n</ul>\n<h2 id="performance-expectations">Performance Expectations</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th align="left">Metric</th><th align="left">VS Code</th><th align="left">Cocoon Expected</th></tr></thead><tbody><tr><td align="left">Extension load time</td><td align="left">~1-2s</td><td align="left">~1-2s</td></tr><tr><td align="left">API call latency</td><td align="left">&#x3C;100ms</td><td align="left">&#x3C;100ms</td></tr><tr><td align="left">Memory usage</td><td align="left">Moderate</td><td align="left">Comparable</td></tr></tbody></table>\n<h2 id="compatibility-gaps">Compatibility Gaps</h2>\n<p>Known differences from VS Code:</p>\n<ol>\n<li><strong>ESM Support</strong>: Cocoon has ESM interception, VS Code is CJS-only (advantage)</li>\n<li><strong>Effect-TS Architecture</strong>: Different programming paradigm (different, not\nincompatible)</li>\n<li><strong>gRPC Protocol</strong>: Different communication protocol (more efficient than VS\nCode custom protocol)</li>\n</ol>\n<h2 id="related">Related</h2>\n<ul>\n<li><a href="/doc/cocoon">Cocoon overview</a></li>\n<li><a href="/doc/deep-dive-cocoon">Cocoon deep dive</a></li>\n</ul>', { headings: 591, localImagePaths: 621, remoteImagePaths: 622, frontmatter: 623, imagePaths: 624 }, [592, 594, 597, 600, 603, 606, 609, 612, 615, 618], { depth: 25, slug: 593, text: 581 }, "cocoon---vs-code-validation-checklist", { depth: 29, slug: 595, text: 596 }, "validation-methodology", "Validation Methodology", { depth: 29, slug: 598, text: 599 }, "core-extension-host", "Core Extension Host", { depth: 29, slug: 601, text: 602 }, "ipc-communication", "IPC Communication", { depth: 29, slug: 604, text: 605 }, "service-layer-coverage", "Service Layer Coverage", { depth: 29, slug: 607, text: 608 }, "effect-ts-vs-vs-code-oop", "Effect-TS vs VS Code OOP", { depth: 29, slug: 610, text: 611 }, "api-surface", "API Surface", { depth: 29, slug: 613, text: 614 }, "performance-expectations", "Performance Expectations", { depth: 29, slug: 616, text: 617 }, "compatibility-gaps", "Compatibility Gaps", { depth: 29, slug: 619, text: 620 }, "related", "Related", [], [], { title: 581, section: 582, order: 583, description: 584 }, [], "common", { id: 625, data: 627, body: 631, filePath: 632, digest: 633, rendered: 634 }, { title: 628, section: 381, order: 629, description: 630 }, "Common", 19, "Shared Rust and TypeScript contracts used across Editor.Land elements.", '# Common\n\n`Common` is the shared contract layer for `Editor.Land`. It contains `Rust` and\n`TypeScript` types, traits, configuration helpers, DTOs, testing helpers, and\nservice-boundary definitions that multiple elements depend on.\n\nIt is not a standalone runtime process. It should not be described as having no\nconcrete code at all, because the source includes helpers and dependencies that\nsupport shared behavior. The accurate claim is narrower: `Common` centralizes\ncross-element contracts so elements do not invent their own incompatible shapes.\n\n---\n\n## What Common Provides\n\n- Shared data transfer objects.\n- Trait and provider contracts for native services.\n- Configuration inspection helpers.\n- Testing support used by element-level code.\n- `TypeScript` package contracts for frontend and service code.\n\n---\n\n## Why It Matters\n\nWhen a service boundary changes in `Common`, dependent `Rust` code gets compiler\nfeedback. That keeps cross-element changes visible. It also gives tests a place\nto depend on contracts instead of launching a full editor session for every\nsmall unit.\n\n---\n\n## Status 🚀\n\n`Common` is source-backed and active as a shared library layer. Claims about\n"complete isolation" or "zero implementation" should be avoided unless they are\nqualified to a specific trait or test harness.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Mountain`](https://Editor.Land/Doc/mountain)\n- [Source Code](https://github.com/CodeEditorLand/Common)', "Source/Content/doc/common.md", "af9a0e7482b06c31", { html: 635, metadata: 636 }, '<h1 id="common">Common</h1>\n<p><code>Common</code> is the shared contract layer for <code>Editor.Land</code>. It contains <code>Rust</code> and\n<code>TypeScript</code> types, traits, configuration helpers, DTOs, testing helpers, and\nservice-boundary definitions that multiple elements depend on.</p>\n<p>It is not a standalone runtime process. It should not be described as having no\nconcrete code at all, because the source includes helpers and dependencies that\nsupport shared behavior. The accurate claim is narrower: <code>Common</code> centralizes\ncross-element contracts so elements do not invent their own incompatible shapes.</p>\n<hr>\n<h2 id="what-common-provides">What Common Provides</h2>\n<ul>\n<li>Shared data transfer objects.</li>\n<li>Trait and provider contracts for native services.</li>\n<li>Configuration inspection helpers.</li>\n<li>Testing support used by element-level code.</li>\n<li><code>TypeScript</code> package contracts for frontend and service code.</li>\n</ul>\n<hr>\n<h2 id="why-it-matters">Why It Matters</h2>\n<p>When a service boundary changes in <code>Common</code>, dependent <code>Rust</code> code gets compiler\nfeedback. That keeps cross-element changes visible. It also gives tests a place\nto depend on contracts instead of launching a full editor session for every\nsmall unit.</p>\n<hr>\n<h2 id="status">Status 🚀</h2>\n<p><code>Common</code> is source-backed and active as a shared library layer. Claims about\n“complete isolation” or “zero implementation” should be avoided unless they are\nqualified to a specific trait or test harness.</p>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/mountain"><code>Mountain</code></a></li>\n<li><a href="https://github.com/CodeEditorLand/Common">Source Code</a></li>\n</ul>', { headings: 637, localImagePaths: 647, remoteImagePaths: 648, frontmatter: 649, imagePaths: 650 }, [638, 639, 642, 645, 646], { depth: 25, slug: 625, text: 628 }, { depth: 29, slug: 640, text: 641 }, "what-common-provides", "What Common Provides", { depth: 29, slug: 643, text: 644 }, "why-it-matters", "Why It Matters", { depth: 29, slug: 571, text: 572 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 628, section: 381, order: 629, description: 630 }, [], { id: 428, data: 652, body: 655, filePath: 656, digest: 657, rendered: 658 }, { title: 429, section: 653, order: 109, description: 654 }, "Usage", "Settings, keybindings, themes, and language configuration in Editor.Land.", '# Configuration\n\n`Editor.Land` stores configuration locally in `JSON` files that support comments\n(`//` and `/* */`). The exact file paths depend on the platform and build\nprofile. To locate the active settings file on your system, open the command\npalette (`Cmd+Shift+P` on macOS, `Ctrl+Shift+P` on Windows) and search for\n**Preferences: Open Settings (`JSON`)**.\n\n---\n\n## Settings Precedence\n\nLand merges settings from three levels in order of increasing precedence:\n\n1. **Default** - built-in defaults shipped with Land.\n2. **User** - the user-level settings file (locate via command palette).\n3. **Workspace** - a settings file in the workspace root. Land reads\n   `.vscode/settings.json` for compatibility with existing `VS Code` workspaces.\n\n---\n\n## Setting Keys\n\nLand uses PascalCase for setting keys (`Editor.FontSize`,\n`Workbench.ColorTheme`) rather than `VS Code`\'s `camelCase` (`editor.fontSize`).\nWhether `VS Code`-style `camelCase` keys are automatically mapped to their\nPascalCase equivalents is not confirmed in the current build. Use PascalCase\nkeys to be safe.\n\n### Common Settings\n\n```json\n{\n	"Editor.FontFamily": "\'JetBrains Mono\', monospace",\n	"Editor.FontSize": 14,\n	"Editor.TabSize": 4,\n	"Editor.InsertSpaces": false,\n	"Editor.FormatOnSave": true,\n	"Editor.WordWrap": "on",\n	"Editor.Minimap.Enabled": false,\n	"Files.AutoSave": "afterDelay",\n	"Files.AutoSaveDelay": 1000,\n	"Terminal.Integrated.FontSize": 13,\n	"Workbench.ColorTheme": "Default Dark+",\n	"Workbench.IconTheme": "Default File Icons"\n}\n```\n\n---\n\n## Keybindings\n\nOpen the keybindings file via **Preferences: Open Keyboard Shortcuts (`JSON`)**\nfrom the command palette. Each entry is a `JSON` object with `key`, `command`,\nand an optional `when` clause:\n\n```json\n[\n	{\n		"key": "ctrl+shift+f",\n		"command": "Workbench.Action.FindInFiles"\n	},\n	{\n		"key": "ctrl+b",\n		"command": "Workbench.Action.ToggleSidebarVisibility"\n	},\n	{\n		"key": "ctrl+`",\n		"command": "Workbench.Action.Terminal.ToggleTerminal"\n	}\n]\n```\n\nUse `cmd` instead of `ctrl` on macOS. The `when` clause uses the same context\nkey syntax as `VS Code` (e.g. `"when": "editorTextFocus"`).\n\n---\n\n## Themes\n\nLand supports `VS Code` color themes without modification. Install a theme by\nplacing its `.vsix` file in the extensions directory and installing it via the\ncommand palette (**Extensions: Install from VSIX**). The extensions panel UI for\nbrowsing and installing themes is not yet implemented.\n\nTo create a custom theme, follow the\n[`VS Code` color theme reference](https://code.visualstudio.com/api/references/theme-color)\n\n- the token format is identical. Use the command palette to locate the themes\n  directory on your system, as exact paths vary by platform and build profile.\n\n---\n\n## Language-Specific Settings\n\nLanguage-specific overrides use a language identifier key:\n\n```json\n{\n	"[rust]": {\n		"Editor.TabSize": 4,\n		"Editor.FormatOnSave": true,\n		"Editor.DefaultFormatter": "rust-lang.rust-analyzer"\n	},\n	"[typescript]": {\n		"Editor.TabSize": 4,\n		"Editor.DefaultFormatter": "biomejs.biome"\n	}\n}\n```\n\nThis syntax is the same as `VS Code`\'s language-specific settings.\n\n---\n\n## Current Limitations\n\n- **File paths are not yet publicly documented.** The user data directory,\n  extensions directory, and themes directory paths depend on the build profile\n  and platform. Use the command palette to locate them rather than hardcoding\n  paths.\n- **`JSON` Schema validation** of unrecognised keys (producing warnings) is a\n  planned feature of the `Wind` configuration service and is not confirmed as\n  active in the current build.\n- **Linux** is not yet a supported platform. Configuration on Linux has not been\n  tested.\n\n---\n\n## See Also\n\n- [Getting Started](https://Editor.Land/Doc/getting-started)\n- [Installation](https://Editor.Land/Doc/installation)\n- [`Wind`: Service Layer](https://Editor.Land/Doc/wind)', "Source/Content/doc/configuration.md", "58f27888da125e68", { html: 659, metadata: 660 }, `<h1 id="configuration">Configuration</h1>
<p><code>Editor.Land</code> stores configuration locally in <code>JSON</code> files that support comments
(<code>//</code> and <code>/* */</code>). The exact file paths depend on the platform and build
profile. To locate the active settings file on your system, open the command
palette (<code>Cmd+Shift+P</code> on macOS, <code>Ctrl+Shift+P</code> on Windows) and search for
<strong>Preferences: Open Settings (<code>JSON</code>)</strong>.</p>
<hr>
<h2 id="settings-precedence">Settings Precedence</h2>
<p>Land merges settings from three levels in order of increasing precedence:</p>
<ol>
<li><strong>Default</strong> - built-in defaults shipped with Land.</li>
<li><strong>User</strong> - the user-level settings file (locate via command palette).</li>
<li><strong>Workspace</strong> - a settings file in the workspace root. Land reads
<code>.vscode/settings.json</code> for compatibility with existing <code>VS Code</code> workspaces.</li>
</ol>
<hr>
<h2 id="setting-keys">Setting Keys</h2>
<p>Land uses PascalCase for setting keys (<code>Editor.FontSize</code>,
<code>Workbench.ColorTheme</code>) rather than <code>VS Code</code>’s <code>camelCase</code> (<code>editor.fontSize</code>).
Whether <code>VS Code</code>-style <code>camelCase</code> keys are automatically mapped to their
PascalCase equivalents is not confirmed in the current build. Use PascalCase
keys to be safe.</p>
<h3 id="common-settings">Common Settings</h3>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="json"><code><span class="line"><span style="color:#E1E4E8">{</span></span>
<span class="line"><span style="color:#79B8FF">	"Editor.FontFamily"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"'JetBrains Mono', monospace"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"Editor.FontSize"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">14</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"Editor.TabSize"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">4</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"Editor.InsertSpaces"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">false</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"Editor.FormatOnSave"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">true</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"Editor.WordWrap"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"on"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"Editor.Minimap.Enabled"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">false</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"Files.AutoSave"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"afterDelay"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"Files.AutoSaveDelay"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">1000</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"Terminal.Integrated.FontSize"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">13</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"Workbench.ColorTheme"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"Default Dark+"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">	"Workbench.IconTheme"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"Default File Icons"</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>
<hr>
<h2 id="keybindings">Keybindings</h2>
<p>Open the keybindings file via <strong>Preferences: Open Keyboard Shortcuts (<code>JSON</code>)</strong>
from the command palette. Each entry is a <code>JSON</code> object with <code>key</code>, <code>command</code>,
and an optional <code>when</code> clause:</p>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="json"><code><span class="line"><span style="color:#E1E4E8">[</span></span>
<span class="line"><span style="color:#E1E4E8">	{</span></span>
<span class="line"><span style="color:#79B8FF">		"key"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"ctrl+shift+f"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">		"command"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"Workbench.Action.FindInFiles"</span></span>
<span class="line"><span style="color:#E1E4E8">	},</span></span>
<span class="line"><span style="color:#E1E4E8">	{</span></span>
<span class="line"><span style="color:#79B8FF">		"key"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"ctrl+b"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">		"command"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"Workbench.Action.ToggleSidebarVisibility"</span></span>
<span class="line"><span style="color:#E1E4E8">	},</span></span>
<span class="line"><span style="color:#E1E4E8">	{</span></span>
<span class="line"><span style="color:#79B8FF">		"key"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"ctrl+\`"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">		"command"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"Workbench.Action.Terminal.ToggleTerminal"</span></span>
<span class="line"><span style="color:#E1E4E8">	}</span></span>
<span class="line"><span style="color:#E1E4E8">]</span></span></code></pre>
<p>Use <code>cmd</code> instead of <code>ctrl</code> on macOS. The <code>when</code> clause uses the same context
key syntax as <code>VS Code</code> (e.g. <code>"when": "editorTextFocus"</code>).</p>
<hr>
<h2 id="themes">Themes</h2>
<p>Land supports <code>VS Code</code> color themes without modification. Install a theme by
placing its <code>.vsix</code> file in the extensions directory and installing it via the
command palette (<strong>Extensions: Install from VSIX</strong>). The extensions panel UI for
browsing and installing themes is not yet implemented.</p>
<p>To create a custom theme, follow the
<a href="https://code.visualstudio.com/api/references/theme-color"><code>VS Code</code> color theme reference</a></p>
<ul>
<li>the token format is identical. Use the command palette to locate the themes
directory on your system, as exact paths vary by platform and build profile.</li>
</ul>
<hr>
<h2 id="language-specific-settings">Language-Specific Settings</h2>
<p>Language-specific overrides use a language identifier key:</p>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="json"><code><span class="line"><span style="color:#E1E4E8">{</span></span>
<span class="line"><span style="color:#79B8FF">	"[rust]"</span><span style="color:#E1E4E8">: {</span></span>
<span class="line"><span style="color:#79B8FF">		"Editor.TabSize"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">4</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">		"Editor.FormatOnSave"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">true</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">		"Editor.DefaultFormatter"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"rust-lang.rust-analyzer"</span></span>
<span class="line"><span style="color:#E1E4E8">	},</span></span>
<span class="line"><span style="color:#79B8FF">	"[typescript]"</span><span style="color:#E1E4E8">: {</span></span>
<span class="line"><span style="color:#79B8FF">		"Editor.TabSize"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">4</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">		"Editor.DefaultFormatter"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"biomejs.biome"</span></span>
<span class="line"><span style="color:#E1E4E8">	}</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>
<p>This syntax is the same as <code>VS Code</code>’s language-specific settings.</p>
<hr>
<h2 id="current-limitations">Current Limitations</h2>
<ul>
<li><strong>File paths are not yet publicly documented.</strong> The user data directory,
extensions directory, and themes directory paths depend on the build profile
and platform. Use the command palette to locate them rather than hardcoding
paths.</li>
<li><strong><code>JSON</code> Schema validation</strong> of unrecognised keys (producing warnings) is a
planned feature of the <code>Wind</code> configuration service and is not confirmed as
active in the current build.</li>
<li><strong>Linux</strong> is not yet a supported platform. Configuration on Linux has not been
tested.</li>
</ul>
<hr>
<h2 id="see-also">See Also</h2>
<ul>
<li><a href="https://Editor.Land/Doc/getting-started">Getting Started</a></li>
<li><a href="https://Editor.Land/Doc/installation">Installation</a></li>
<li><a href="https://Editor.Land/Doc/wind"><code>Wind</code>: Service Layer</a></li>
</ul>`, { headings: 661, localImagePaths: 683, remoteImagePaths: 684, frontmatter: 685, imagePaths: 686 }, [662, 663, 666, 669, 672, 673, 676, 679, 682], { depth: 25, slug: 428, text: 429 }, { depth: 29, slug: 664, text: 665 }, "settings-precedence", "Settings Precedence", { depth: 29, slug: 667, text: 668 }, "setting-keys", "Setting Keys", { depth: 347, slug: 670, text: 671 }, "common-settings", "Common Settings", { depth: 29, slug: 431, text: 432 }, { depth: 29, slug: 674, text: 675 }, "themes", "Themes", { depth: 29, slug: 677, text: 678 }, "language-specific-settings", "Language-Specific Settings", { depth: 29, slug: 680, text: 681 }, "current-limitations", "Current Limitations", { depth: 29, slug: 446, text: 447 }, [], [], { title: 429, section: 653, order: 109, description: 654 }, [], "contributing", { id: 687, data: 689, body: 693, filePath: 694, digest: 695, rendered: 696 }, { title: 690, section: 108, order: 691, description: 692 }, "Contributing", 9, "How to contribute code, documentation, or translations to Editor.Land.", '# Contributing to Editor.Land\n\n> **Updated 2026-05-29** - Build steps updated to Node 24 and\n> `Maintain/Debug/Build.sh`. Submodule paths corrected. Canonical source:\n> [Documentation/GitHub/Building.md](https://github.com/CodeEditorLand/Land/tree/Current/Documentation/GitHub/Building.md)\n\n`Editor.Land` is open source and welcomes contributions. The full contributing\nguide is at\n[CONTRIBUTING.md](https://github.com/CodeEditorLand/Land/tree/Current/CONTRIBUTING.md)\non GitHub.\n\n---\n\n## Quick Start\n\n1. **Fork and clone** the repository with submodules:\n\n    ```bash\n    git clone --recurse-submodules https://github.com/YOUR_USERNAME/Land.git\n    cd Land\n    ```\n\n2. **Compile the VS Code Editor submodule** _(mandatory before building Land)_\n\n    Node 24 is required for this step. The version is pinned in\n    `Dependency/Microsoft/Dependency/Editor/.nvmrc`.\n\n    ```bash\n    cd Dependency/Microsoft/Dependency/Editor\n    nvm use 24\n    git fetch --all\n    git reset --hard Parent/main\n    git clean -dfx\n    pnpm install\n    pnpm run compile\n    pnpm run compile-extensions-build\n    ```\n\n3. **Install `Node.js` dependencies:**\n\n    ```bash\n    cd Land # back to repository root\n    pnpm install\n    ```\n\n4. **Start the development build:**\n\n    ```bash\n    ./Maintain/Debug/Build.sh --profile debug-electron-bundled\n    ```\n\n    This compiles `Mountain` and opens the editor window. The first run\n    downloads and compiles `Rust` dependencies from scratch - allow a few\n    minutes on a fresh checkout. Subsequent builds use `Cargo`\'s incremental\n    compilation cache and are significantly faster.\n\n5. **Create a branch** from `Current` and make your changes. The project uses\n   PascalCase naming for files, identifiers, and settings keys throughout the\n   codebase.\n\n6. **Open a pull request** against the `Current` branch with a clear description\n   of what changed and why.\n\n---\n\n## Ways to Contribute\n\n- **Code** - `Rust` or `TypeScript` fixes, features, or performance improvements\n- **Documentation** - Correcting inaccuracies, adding missing context, or\n  improving clarity on any doc page\n- **Testing** - Bug reports with reproductions, test coverage for `Cocoon` or\n  `Mountain`\n- **Design** - UI/UX improvements for the editor or this website\n\n---\n\n## Submodule Workflow\n\nEach Land element (`Mountain`, `Cocoon`, `Sky`, `Wind`, `Vine`, `Air`, `Echo`,\nand others) is a separate Git repository referenced as a submodule. When working\non a specific element:\n\n```bash\ncd Land/Element/Mountain # or whichever element\ngit checkout -b MyFeatureBranch\n# make changes\ngit commit -m "Description"\n```\n\nThen update the submodule reference in the parent Land repo and open pull\nrequests against both the element repo and the Land repo if the change affects\nthe pinned submodule commit.\n\n---\n\n## Code of Conduct\n\nAll participants are expected to be respectful. Read the full\n[Code of Conduct](https://github.com/CodeEditorLand/Land/tree/Current/CODE_OF_CONDUCT.md)\nbefore engaging.\n\n---\n\n## Funding\n\n`Editor.Land` is funded through the\n[NGI0 Commons Fund](https://nlnet.nl/commonsfund/) (grant No. 101135429),\noperated by [PlayForm](https://playform.cloud) (Sofia, Bulgaria) under the\n[NLnet Foundation](https://nlnet.nl).\n\n---\n\n## See Also\n\n- [Getting Started](https://Editor.Land/Doc/getting-started)\n- [Installation](https://Editor.Land/Doc/installation)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)', "Source/Content/doc/contributing.md", "253d2c8125bd630d", { html: 697, metadata: 698 }, '<h1 id="contributing-to-editorland">Contributing to Editor.Land</h1>\n<blockquote>\n<p><strong>Updated 2026-05-29</strong> - Build steps updated to Node 24 and\n<code>Maintain/Debug/Build.sh</code>. Submodule paths corrected. Canonical source:\n<a href="https://github.com/CodeEditorLand/Land/tree/Current/Documentation/GitHub/Building.md">Documentation/GitHub/Building.md</a></p>\n</blockquote>\n<p><code>Editor.Land</code> is open source and welcomes contributions. The full contributing\nguide is at\n<a href="https://github.com/CodeEditorLand/Land/tree/Current/CONTRIBUTING.md">CONTRIBUTING.md</a>\non GitHub.</p>\n<hr>\n<h2 id="quick-start">Quick Start</h2>\n<ol>\n<li>\n<p><strong>Fork and clone</strong> the repository with submodules:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> clone</span><span style="color:#79B8FF"> --recurse-submodules</span><span style="color:#9ECBFF"> https://github.com/YOUR_USERNAME/Land.git</span></span>\n<span class="line"><span style="color:#79B8FF">cd</span><span style="color:#9ECBFF"> Land</span></span></code></pre>\n</li>\n<li>\n<p><strong>Compile the VS Code Editor submodule</strong> <em>(mandatory before building Land)</em></p>\n<p>Node 24 is required for this step. The version is pinned in\n<code>Dependency/Microsoft/Dependency/Editor/.nvmrc</code>.</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#79B8FF">cd</span><span style="color:#9ECBFF"> Dependency/Microsoft/Dependency/Editor</span></span>\n<span class="line"><span style="color:#B392F0">nvm</span><span style="color:#9ECBFF"> use</span><span style="color:#79B8FF"> 24</span></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> fetch</span><span style="color:#79B8FF"> --all</span></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> reset</span><span style="color:#79B8FF"> --hard</span><span style="color:#9ECBFF"> Parent/main</span></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> clean</span><span style="color:#79B8FF"> -dfx</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> install</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> run</span><span style="color:#9ECBFF"> compile</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> run</span><span style="color:#9ECBFF"> compile-extensions-build</span></span></code></pre>\n</li>\n<li>\n<p><strong>Install <code>Node.js</code> dependencies:</strong></p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#79B8FF">cd</span><span style="color:#9ECBFF"> Land</span><span style="color:#6A737D"> # back to repository root</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> install</span></span></code></pre>\n</li>\n<li>\n<p><strong>Start the development build:</strong></p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">./Maintain/Debug/Build.sh</span><span style="color:#79B8FF"> --profile</span><span style="color:#9ECBFF"> debug-electron-bundled</span></span></code></pre>\n<p>This compiles <code>Mountain</code> and opens the editor window. The first run\ndownloads and compiles <code>Rust</code> dependencies from scratch - allow a few\nminutes on a fresh checkout. Subsequent builds use <code>Cargo</code>’s incremental\ncompilation cache and are significantly faster.</p>\n</li>\n<li>\n<p><strong>Create a branch</strong> from <code>Current</code> and make your changes. The project uses\nPascalCase naming for files, identifiers, and settings keys throughout the\ncodebase.</p>\n</li>\n<li>\n<p><strong>Open a pull request</strong> against the <code>Current</code> branch with a clear description\nof what changed and why.</p>\n</li>\n</ol>\n<hr>\n<h2 id="ways-to-contribute">Ways to Contribute</h2>\n<ul>\n<li><strong>Code</strong> - <code>Rust</code> or <code>TypeScript</code> fixes, features, or performance improvements</li>\n<li><strong>Documentation</strong> - Correcting inaccuracies, adding missing context, or\nimproving clarity on any doc page</li>\n<li><strong>Testing</strong> - Bug reports with reproductions, test coverage for <code>Cocoon</code> or\n<code>Mountain</code></li>\n<li><strong>Design</strong> - UI/UX improvements for the editor or this website</li>\n</ul>\n<hr>\n<h2 id="submodule-workflow">Submodule Workflow</h2>\n<p>Each Land element (<code>Mountain</code>, <code>Cocoon</code>, <code>Sky</code>, <code>Wind</code>, <code>Vine</code>, <code>Air</code>, <code>Echo</code>,\nand others) is a separate Git repository referenced as a submodule. When working\non a specific element:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#79B8FF">cd</span><span style="color:#9ECBFF"> Land/Element/Mountain</span><span style="color:#6A737D"> # or whichever element</span></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> checkout</span><span style="color:#79B8FF"> -b</span><span style="color:#9ECBFF"> MyFeatureBranch</span></span>\n<span class="line"><span style="color:#6A737D"># make changes</span></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> commit</span><span style="color:#79B8FF"> -m</span><span style="color:#9ECBFF"> "Description"</span></span></code></pre>\n<p>Then update the submodule reference in the parent Land repo and open pull\nrequests against both the element repo and the Land repo if the change affects\nthe pinned submodule commit.</p>\n<hr>\n<h2 id="code-of-conduct">Code of Conduct</h2>\n<p>All participants are expected to be respectful. Read the full\n<a href="https://github.com/CodeEditorLand/Land/tree/Current/CODE_OF_CONDUCT.md">Code of Conduct</a>\nbefore engaging.</p>\n<hr>\n<h2 id="funding">Funding</h2>\n<p><code>Editor.Land</code> is funded through the\n<a href="https://nlnet.nl/commonsfund/">NGI0 Commons Fund</a> (grant No. 101135429),\noperated by <a href="https://playform.cloud">PlayForm</a> (Sofia, Bulgaria) under the\n<a href="https://nlnet.nl">NLnet Foundation</a>.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/getting-started">Getting Started</a></li>\n<li><a href="https://Editor.Land/Doc/installation">Installation</a></li>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n</ul>', { headings: 699, localImagePaths: 719, remoteImagePaths: 720, frontmatter: 721, imagePaths: 722 }, [700, 703, 706, 709, 712, 715, 718], { depth: 25, slug: 701, text: 702 }, "contributing-to-editorland", "Contributing to Editor.Land", { depth: 29, slug: 704, text: 705 }, "quick-start", "Quick Start", { depth: 29, slug: 707, text: 708 }, "ways-to-contribute", "Ways to Contribute", { depth: 29, slug: 710, text: 711 }, "submodule-workflow", "Submodule Workflow", { depth: 29, slug: 713, text: 714 }, "code-of-conduct", "Code of Conduct", { depth: 29, slug: 716, text: 717 }, "funding", "Funding", { depth: 29, slug: 446, text: 447 }, [], [], { title: 690, section: 108, order: 691, description: 692 }, [], "deep-dive-air", { id: 723, data: 725, body: 729, filePath: 730, digest: 731, rendered: 732 }, { title: 726, section: 582, order: 727, description: 728 }, "Air - Deep Dive", 31, "Technical architecture of the Air background daemon: update management, resilient downloads, authentication, and health monitoring via gRPC.", "# Air - Deep Dive\n\nAir is a persistent background sidecar daemon in the Land project. It runs\nalongside Mountain and offloads resource-intensive operations -- update\nchecking, artifact downloads, cryptographic authentication -- so the editor\nremains responsive.\n\n## Architecture 🚀\n\nAir is a standalone Rust binary built around a central gRPC server on\nport 50053. It receives task delegation from Mountain via the `Vine/Air.proto`\nprotocol and streams progress events back.\n\n```text\nMountain Core -[gRPC]-> Air gRPC Server (port 50053) -[routes]-> Update/Download/Auth/Health modules\n```\n\n## Key Modules 🧩\n\n| Path                     | Responsibility                                           |\n| ------------------------ | -------------------------------------------------------- |\n| `Source/Binary.rs`       | Entry point; bootstraps Tokio runtime                    |\n| `Source/Initialize/`     | Startup sequence: config loading, server binding         |\n| `Source/Vine/`           | gRPC server implementation via tonic                     |\n| `Source/Updates/`        | Update lifecycle: check, download, verify, apply patches |\n| `Source/Downloader/`     | Resilient downloads with pause, resume, and retry        |\n| `Source/Authentication/` | Cryptographic signing and secure token storage           |\n| `Source/HTTP/`           | HTTP client routing through Mist local DNS resolver      |\n| `Source/HealthCheck/`    | Self-monitoring watchdog                                 |\n| `Source/Metrics/`        | Telemetry collection and reporting                       |\n| `Source/Logging/`        | Structured tracing output                                |\n| `Source/CLI/`            | Command-line argument parsing                            |\n| `Source/Resilience/`     | Retry logic and circuit breakers                         |\n| `Source/Security/`       | Signature verification and secure storage                |\n| `Source/Configuration/`  | Runtime config loading and hot-reload                    |\n\n## Data Flow 🔄\n\nMountain delegates an update check to Air over gRPC. Air queries the update\nserver, returns release metadata, and upon Mountain's request begins a resilient\ndownload. Progress events stream back to Mountain via bidirectional gRPC.\n\n**Port allocation:**\n\n- Air: `[::1]:50053`\n- Cocoon: `[::1]:50052`\n\n## Integration Points 🔗\n\n| Connecting Element | Direction     | Mechanism                  | Description                                                         |\n| ------------------ | ------------- | -------------------------- | ------------------------------------------------------------------- |\n| Mountain           | Bidirectional | gRPC over `Vine/Air.proto` | Task delegation and progress streaming                              |\n| Mist               | Inbound       | Local DNS resolver         | Air's HTTP client uses Mist for secure DNS                          |\n| Vine               | Inbound       | Protocol definition        | `Air.proto` defines service contracts; tonic generates server stubs |\n\n## Configuration ⚙️\n\n| Parameter                | Source                          | Description                                    |\n| ------------------------ | ------------------------------- | ---------------------------------------------- |\n| Bind address             | CLI flag / environment          | Default `[::1]:50053`, overridable for testing |\n| Update server URL        | Configuration file              | Base URL for checking and downloading updates  |\n| Download cache directory | Configuration file              | Storage location for partial downloads         |\n| Health check interval    | Configuration file              | Self-monitoring frequency                      |\n| Log level                | `RUST_LOG` environment variable | Tracing filter for structured output           |\n\nAir is spawned automatically by Mountain at startup. Mountain checks for an\nexisting Air process before spawning a new instance to prevent duplicates.\n\n## Related Documentation 📖\n\n- [Architecture overview](https://Editor.Land/Doc/architecture)\n- [Mountain deep dive](https://Editor.Land/Doc/deep-dive-mountain)\n- [Air GitHub repository](https://github.com/CodeEditorLand/Air)", "Source/Content/doc/deep-dive-air.md", "a8eadd4f97653430", { html: 733, metadata: 734 }, '<h1 id="air---deep-dive">Air - Deep Dive</h1>\n<p>Air is a persistent background sidecar daemon in the Land project. It runs\nalongside Mountain and offloads resource-intensive operations — update\nchecking, artifact downloads, cryptographic authentication — so the editor\nremains responsive.</p>\n<h2 id="architecture">Architecture 🚀</h2>\n<p>Air is a standalone Rust binary built around a central gRPC server on\nport 50053. It receives task delegation from Mountain via the <code>Vine/Air.proto</code>\nprotocol and streams progress events back.</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="text"><code><span class="line"><span>Mountain Core -[gRPC]-> Air gRPC Server (port 50053) -[routes]-> Update/Download/Auth/Health modules</span></span></code></pre>\n<h2 id="key-modules">Key Modules 🧩</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Path</th><th>Responsibility</th></tr></thead><tbody><tr><td><code>Source/Binary.rs</code></td><td>Entry point; bootstraps Tokio runtime</td></tr><tr><td><code>Source/Initialize/</code></td><td>Startup sequence: config loading, server binding</td></tr><tr><td><code>Source/Vine/</code></td><td>gRPC server implementation via tonic</td></tr><tr><td><code>Source/Updates/</code></td><td>Update lifecycle: check, download, verify, apply patches</td></tr><tr><td><code>Source/Downloader/</code></td><td>Resilient downloads with pause, resume, and retry</td></tr><tr><td><code>Source/Authentication/</code></td><td>Cryptographic signing and secure token storage</td></tr><tr><td><code>Source/HTTP/</code></td><td>HTTP client routing through Mist local DNS resolver</td></tr><tr><td><code>Source/HealthCheck/</code></td><td>Self-monitoring watchdog</td></tr><tr><td><code>Source/Metrics/</code></td><td>Telemetry collection and reporting</td></tr><tr><td><code>Source/Logging/</code></td><td>Structured tracing output</td></tr><tr><td><code>Source/CLI/</code></td><td>Command-line argument parsing</td></tr><tr><td><code>Source/Resilience/</code></td><td>Retry logic and circuit breakers</td></tr><tr><td><code>Source/Security/</code></td><td>Signature verification and secure storage</td></tr><tr><td><code>Source/Configuration/</code></td><td>Runtime config loading and hot-reload</td></tr></tbody></table>\n<h2 id="data-flow">Data Flow 🔄</h2>\n<p>Mountain delegates an update check to Air over gRPC. Air queries the update\nserver, returns release metadata, and upon Mountain’s request begins a resilient\ndownload. Progress events stream back to Mountain via bidirectional gRPC.</p>\n<p><strong>Port allocation:</strong></p>\n<ul>\n<li>Air: <code>[::1]:50053</code></li>\n<li>Cocoon: <code>[::1]:50052</code></li>\n</ul>\n<h2 id="integration-points">Integration Points 🔗</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Connecting Element</th><th>Direction</th><th>Mechanism</th><th>Description</th></tr></thead><tbody><tr><td>Mountain</td><td>Bidirectional</td><td>gRPC over <code>Vine/Air.proto</code></td><td>Task delegation and progress streaming</td></tr><tr><td>Mist</td><td>Inbound</td><td>Local DNS resolver</td><td>Air’s HTTP client uses Mist for secure DNS</td></tr><tr><td>Vine</td><td>Inbound</td><td>Protocol definition</td><td><code>Air.proto</code> defines service contracts; tonic generates server stubs</td></tr></tbody></table>\n<h2 id="configuration️">Configuration ⚙️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Parameter</th><th>Source</th><th>Description</th></tr></thead><tbody><tr><td>Bind address</td><td>CLI flag / environment</td><td>Default <code>[::1]:50053</code>, overridable for testing</td></tr><tr><td>Update server URL</td><td>Configuration file</td><td>Base URL for checking and downloading updates</td></tr><tr><td>Download cache directory</td><td>Configuration file</td><td>Storage location for partial downloads</td></tr><tr><td>Health check interval</td><td>Configuration file</td><td>Self-monitoring frequency</td></tr><tr><td>Log level</td><td><code>RUST_LOG</code> environment variable</td><td>Tracing filter for structured output</td></tr></tbody></table>\n<p>Air is spawned automatically by Mountain at startup. Mountain checks for an\nexisting Air process before spawning a new instance to prevent duplicates.</p>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture overview</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-mountain">Mountain deep dive</a></li>\n<li><a href="https://github.com/CodeEditorLand/Air">Air GitHub repository</a></li>\n</ul>', { headings: 735, localImagePaths: 753, remoteImagePaths: 754, frontmatter: 755, imagePaths: 756 }, [736, 738, 740, 743, 746, 749, 752], { depth: 25, slug: 737, text: 726 }, "air---deep-dive", { depth: 29, slug: 71, text: 739 }, "Architecture 🚀", { depth: 29, slug: 741, text: 742 }, "key-modules", "Key Modules 🧩", { depth: 29, slug: 744, text: 745 }, "data-flow", "Data Flow 🔄", { depth: 29, slug: 747, text: 748 }, "integration-points", "Integration Points 🔗", { depth: 29, slug: 750, text: 751 }, "configuration️", "Configuration ⚙️", { depth: 29, slug: 401, text: 402 }, [], [], { title: 726, section: 582, order: 727, description: 728 }, [], "deep-dive-cocoon", { id: 757, data: 759, body: 763, filePath: 764, digest: 765, rendered: 766 }, { title: 760, section: 582, order: 761, description: 762 }, "Cocoon - Deep Dive", 32, "Technical architecture of the Cocoon VSCode extension host: Effect-TS service layers, module interception, and gRPC communication with Mountain.", "# Cocoon - Deep Dive\n\nCocoon is the Node.js sidecar that provides VSCode extension host compatibility\nwithin the Land project. It uses Effect-TS for service composition, gRPC for\ncommunication with Mountain, and sophisticated module interception to route\n`vscode` API calls to native implementations.\n\n## Core Architecture 🏗️\n\n| Principle               | Description                                                                          | Key Components               |\n| ----------------------- | ------------------------------------------------------------------------------------ | ---------------------------- |\n| High-Fidelity API Shim  | Comprehensive `IExtHost*` service implementations for VSCode extension compatibility | All `Service/*` modules      |\n| Effect-TS Native        | Entire application built with Effect-TS layer composition                            | `AppLayer`, all services     |\n| Module Interception     | Patches `require()` and `import` to route `vscode` module calls                      | `Core/RequireInterceptor.ts` |\n| gRPC-Powered IPC        | Fast typed communication with Mountain via tonic and Vine protocol                   | `Service/Ipc.ts`             |\n| Process Hardening       | Signal handling, parent monitoring, log piping, uncaught exception boundaries        | `PatchProcess/*`             |\n| Extensible Architecture | Service provider pattern for easy addition of new API implementations                | `AppLayer` composition       |\n\n## Component Breakdown 🧩\n\n### Index.ts (Orchestrator)\n\nMain entry point that builds the complete `AppLayer` by composing all individual\nservice layers, applies process hardening early, manages the gRPC connection to\nMountain, and coordinates graceful shutdown.\n\n### PatchProcess/ (Foundation)\n\nEnsures Cocoon runs as a stable sidecar: captures `SIGTERM`/`SIGINT`, monitors\nthe Mountain parent process for heartbeat, pipes stdout/stderr back to the\nparent, and wraps the application in a top-level error boundary.\n\n### Core/ (Extension Runtime Engine)\n\n- **ExtensionHost.ts** - Activates and manages extension lifecycle.\n- **RequireInterceptor.ts** - Intercepts CommonJS `require()` and ESM `import`\n  for the `vscode` module, giving each extension its own isolated API instance.\n- **ApiFactory.ts** - Constructs the `vscode` API object, wiring service calls\n  to Effect-TS implementations.\n\n### Service/ (VSCode API Implementations)\n\nEach VSCode service is implemented as an Effect-TS layer:\n\n| VSCode Service      | Cocoon Service    | Communication |\n| ------------------- | ----------------- | ------------- |\n| `vscode.commands`   | CommandsProvider  | Vine gRPC     |\n| `vscode.workspace`  | WorkspaceProvider | Vine gRPC     |\n| `vscode.window`     | WindowProvider    | Vine gRPC     |\n| `vscode.extensions` | ExtensionProvider | Vine gRPC     |\n| `vscode.languages`  | LanguageProvider  | Vine gRPC     |\n\n### Service/Ipc.ts (Communication Bridge)\n\nManages bidirectional gRPC with advanced patterns: bidirectional streaming for\nterminal I/O and file watching, request batching, automatic reconnection with\nexponential backoff, and protocol buffer optimization.\n\n## Source Validation Status ✅\n\nCocoon has been validated against Microsoft's VSCode source repositories. Core\nfeatures -- extension activation, lifecycle, API factory, module interception,\nIPC, and service layer communication -- all pass validation. Advanced language\nfeatures and SCM API are marked as partially implemented. ESM support exists as\nan enhancement over VSCode's CJS-only model.\n\nKey API calls flow through a consistent path: extension\n`vscode.window.showInformationMessage()` goes through RequireInterceptor,\nApiFactory, WindowProvider, gRPC serialization, Mountain's Vine server, native\ndialog display, and response flows back along the same chain. Total API call\nlatency is approximately 0.42ms plus network and Mountain processing time.\n\n## Related Documentation 📖\n\n- [Architecture overview](https://Editor.Land/Doc/architecture)\n- [Mountain deep dive](https://Editor.Land/Doc/deep-dive-mountain)\n- [Cocoon GitHub repository](https://github.com/CodeEditorLand/Cocoon)", "Source/Content/doc/deep-dive-cocoon.md", "9f35b25f3a96ffa8", { html: 767, metadata: 768 }, '<h1 id="cocoon---deep-dive">Cocoon - Deep Dive</h1>\n<p>Cocoon is the Node.js sidecar that provides VSCode extension host compatibility\nwithin the Land project. It uses Effect-TS for service composition, gRPC for\ncommunication with Mountain, and sophisticated module interception to route\n<code>vscode</code> API calls to native implementations.</p>\n<h2 id="core-architecture️">Core Architecture 🏗️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Principle</th><th>Description</th><th>Key Components</th></tr></thead><tbody><tr><td>High-Fidelity API Shim</td><td>Comprehensive <code>IExtHost*</code> service implementations for VSCode extension compatibility</td><td>All <code>Service/*</code> modules</td></tr><tr><td>Effect-TS Native</td><td>Entire application built with Effect-TS layer composition</td><td><code>AppLayer</code>, all services</td></tr><tr><td>Module Interception</td><td>Patches <code>require()</code> and <code>import</code> to route <code>vscode</code> module calls</td><td><code>Core/RequireInterceptor.ts</code></td></tr><tr><td>gRPC-Powered IPC</td><td>Fast typed communication with Mountain via tonic and Vine protocol</td><td><code>Service/Ipc.ts</code></td></tr><tr><td>Process Hardening</td><td>Signal handling, parent monitoring, log piping, uncaught exception boundaries</td><td><code>PatchProcess/*</code></td></tr><tr><td>Extensible Architecture</td><td>Service provider pattern for easy addition of new API implementations</td><td><code>AppLayer</code> composition</td></tr></tbody></table>\n<h2 id="component-breakdown">Component Breakdown 🧩</h2>\n<h3 id="indexts-orchestrator">Index.ts (Orchestrator)</h3>\n<p>Main entry point that builds the complete <code>AppLayer</code> by composing all individual\nservice layers, applies process hardening early, manages the gRPC connection to\nMountain, and coordinates graceful shutdown.</p>\n<h3 id="patchprocess-foundation">PatchProcess/ (Foundation)</h3>\n<p>Ensures Cocoon runs as a stable sidecar: captures <code>SIGTERM</code>/<code>SIGINT</code>, monitors\nthe Mountain parent process for heartbeat, pipes stdout/stderr back to the\nparent, and wraps the application in a top-level error boundary.</p>\n<h3 id="core-extension-runtime-engine">Core/ (Extension Runtime Engine)</h3>\n<ul>\n<li><strong>ExtensionHost.ts</strong> - Activates and manages extension lifecycle.</li>\n<li><strong>RequireInterceptor.ts</strong> - Intercepts CommonJS <code>require()</code> and ESM <code>import</code>\nfor the <code>vscode</code> module, giving each extension its own isolated API instance.</li>\n<li><strong>ApiFactory.ts</strong> - Constructs the <code>vscode</code> API object, wiring service calls\nto Effect-TS implementations.</li>\n</ul>\n<h3 id="service-vscode-api-implementations">Service/ (VSCode API Implementations)</h3>\n<p>Each VSCode service is implemented as an Effect-TS layer:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>VSCode Service</th><th>Cocoon Service</th><th>Communication</th></tr></thead><tbody><tr><td><code>vscode.commands</code></td><td>CommandsProvider</td><td>Vine gRPC</td></tr><tr><td><code>vscode.workspace</code></td><td>WorkspaceProvider</td><td>Vine gRPC</td></tr><tr><td><code>vscode.window</code></td><td>WindowProvider</td><td>Vine gRPC</td></tr><tr><td><code>vscode.extensions</code></td><td>ExtensionProvider</td><td>Vine gRPC</td></tr><tr><td><code>vscode.languages</code></td><td>LanguageProvider</td><td>Vine gRPC</td></tr></tbody></table>\n<h3 id="serviceipcts-communication-bridge">Service/Ipc.ts (Communication Bridge)</h3>\n<p>Manages bidirectional gRPC with advanced patterns: bidirectional streaming for\nterminal I/O and file watching, request batching, automatic reconnection with\nexponential backoff, and protocol buffer optimization.</p>\n<h2 id="source-validation-status">Source Validation Status ✅</h2>\n<p>Cocoon has been validated against Microsoft’s VSCode source repositories. Core\nfeatures — extension activation, lifecycle, API factory, module interception,\nIPC, and service layer communication — all pass validation. Advanced language\nfeatures and SCM API are marked as partially implemented. ESM support exists as\nan enhancement over VSCode’s CJS-only model.</p>\n<p>Key API calls flow through a consistent path: extension\n<code>vscode.window.showInformationMessage()</code> goes through RequireInterceptor,\nApiFactory, WindowProvider, gRPC serialization, Mountain’s Vine server, native\ndialog display, and response flows back along the same chain. Total API call\nlatency is approximately 0.42ms plus network and Mountain processing time.</p>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture overview</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-mountain">Mountain deep dive</a></li>\n<li><a href="https://github.com/CodeEditorLand/Cocoon">Cocoon GitHub repository</a></li>\n</ul>', { headings: 769, localImagePaths: 797, remoteImagePaths: 798, frontmatter: 799, imagePaths: 800 }, [770, 772, 775, 778, 781, 784, 787, 790, 793, 796], { depth: 25, slug: 771, text: 760 }, "cocoon---deep-dive", { depth: 29, slug: 773, text: 774 }, "core-architecture️", "Core Architecture 🏗️", { depth: 29, slug: 776, text: 777 }, "component-breakdown", "Component Breakdown 🧩", { depth: 347, slug: 779, text: 780 }, "indexts-orchestrator", "Index.ts (Orchestrator)", { depth: 347, slug: 782, text: 783 }, "patchprocess-foundation", "PatchProcess/ (Foundation)", { depth: 347, slug: 785, text: 786 }, "core-extension-runtime-engine", "Core/ (Extension Runtime Engine)", { depth: 347, slug: 788, text: 789 }, "service-vscode-api-implementations", "Service/ (VSCode API Implementations)", { depth: 347, slug: 791, text: 792 }, "serviceipcts-communication-bridge", "Service/Ipc.ts (Communication Bridge)", { depth: 29, slug: 794, text: 795 }, "source-validation-status", "Source Validation Status ✅", { depth: 29, slug: 401, text: 402 }, [], [], { title: 760, section: 582, order: 761, description: 762 }, [], "deep-dive-common", { id: 801, data: 803, body: 807, filePath: 808, digest: 809, rendered: 810 }, { title: 804, section: 582, order: 805, description: 806 }, "Common - Deep Dive", 35, "Abstract service contracts, ActionEffect system, and cross-language data types", "# Common - Deep Dive\n\nCommon defines the abstract architectural patterns, service contracts, and data\nstructures that enable type-safe, testable service implementations across Rust\nand TypeScript boundaries. It has zero dependencies on Tauri, gRPC, or\napplication logic.\n\n## Architecture Principles 🎯\n\n| Principle                | Description                                                                            |\n| ------------------------ | -------------------------------------------------------------------------------------- |\n| Pure Abstraction         | All capabilities defined as abstract `async trait`s with no concrete implementation    |\n| Declarative Effects      | Operations represented as `ActionEffect` values, separating description from execution |\n| Trait-Based DI           | Dependency injection via `Environment` and `Requires` traits                           |\n| Universal Error Handling | Single `CommonError` enum covering all failure scenarios                               |\n| Contract-First           | DTOs and error types defined first, establishing stable contracts                      |\n\n## ActionEffect System ⚡\n\nAn `ActionEffect<C, E, T>` describes an operation parameterized by:\n\n- **C**: Capability type required for execution\n- **E**: Error type\n- **T**: Successful result type\n\n```rust\npub struct ActionEffect<TCapability, TError, TOutput> {\n    pub Function: Arc<dyn Fn(TCapability) -> Pin<Box<\n        dyn Future<Output = Result<TOutput, TError>> + Send\n    >> + Send + Sync>,\n}\n```\n\nEffects compose via `and_then` (sequential), `zip` (parallel), and `fallback`\n(error recovery). Execution overhead is ~17ns per effect.\n\n## Environment System 🌍\n\nThe `Environment` trait system implements capability-based architecture:\n\n1. Effects declare required capabilities\n2. Concrete environments implement the required traits\n3. `ApplicationRunTime` resolves and provides capabilities at runtime\n4. Effect executes with the provided capability\n\n## Service Contracts (Traits) 📋\n\nCommon defines async traits that VSCode services are lifted into:\n\n| VSCode Service          | Common Trait           |\n| ----------------------- | ---------------------- |\n| `IFileService`          | `FileSystemService`    |\n| `IWorkspaceService`     | `WorkspaceService`     |\n| `IConfigurationService` | `ConfigurationService` |\n| `ICommandService`       | `CommandService`       |\n| `IDocumentService`      | `DocumentProvider`     |\n\nImplementations live in Mountain (Rust) and Wind (Effect-TS).\n\n## Cross-Language Architecture 🌐\n\nDTOs ensure consistent data structures across the project:\n\n- Rust backend serializes to protocol buffers\n- TypeScript frontend deserializes from gRPC\n- Compile-time validation enforces type safety\n\n```\nTraits --shared--> Mountain (Rust impl)\nTraits --shared--> Wind (Effect-TS impl)\nTraits --shared--> Cocoon (Node.js impl)\nDTOs --shared--> All consumers\nErrors --shared--> All consumers\n```\n\n## Related Documentation 📖\n\n- [Common overview](https://Editor.Land/Doc/common)\n- [Mountain implementation details](https://Editor.Land/Doc/deep-dive-mountain)", "Source/Content/doc/deep-dive-common.md", "b9c0270e248644a3", { html: 811, metadata: 812 }, '<h1 id="common---deep-dive">Common - Deep Dive</h1>\n<p>Common defines the abstract architectural patterns, service contracts, and data\nstructures that enable type-safe, testable service implementations across Rust\nand TypeScript boundaries. It has zero dependencies on Tauri, gRPC, or\napplication logic.</p>\n<h2 id="architecture-principles">Architecture Principles 🎯</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Principle</th><th>Description</th></tr></thead><tbody><tr><td>Pure Abstraction</td><td>All capabilities defined as abstract <code>async trait</code>s with no concrete implementation</td></tr><tr><td>Declarative Effects</td><td>Operations represented as <code>ActionEffect</code> values, separating description from execution</td></tr><tr><td>Trait-Based DI</td><td>Dependency injection via <code>Environment</code> and <code>Requires</code> traits</td></tr><tr><td>Universal Error Handling</td><td>Single <code>CommonError</code> enum covering all failure scenarios</td></tr><tr><td>Contract-First</td><td>DTOs and error types defined first, establishing stable contracts</td></tr></tbody></table>\n<h2 id="actioneffect-system">ActionEffect System ⚡</h2>\n<p>An <code>ActionEffect&#x3C;C, E, T></code> describes an operation parameterized by:</p>\n<ul>\n<li><strong>C</strong>: Capability type required for execution</li>\n<li><strong>E</strong>: Error type</li>\n<li><strong>T</strong>: Successful result type</li>\n</ul>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="rust"><code><span class="line"><span style="color:#F97583">pub</span><span style="color:#F97583"> struct</span><span style="color:#B392F0"> ActionEffect</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#B392F0">TCapability</span><span style="color:#E1E4E8">, </span><span style="color:#B392F0">TError</span><span style="color:#E1E4E8">, </span><span style="color:#B392F0">TOutput</span><span style="color:#E1E4E8">> {</span></span>\n<span class="line"><span style="color:#F97583">    pub</span><span style="color:#B392F0"> Function</span><span style="color:#F97583">:</span><span style="color:#B392F0"> Arc</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#F97583">dyn</span><span style="color:#B392F0"> Fn</span><span style="color:#E1E4E8">(</span><span style="color:#B392F0">TCapability</span><span style="color:#E1E4E8">) </span><span style="color:#F97583">-></span><span style="color:#B392F0"> Pin</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#B392F0">Box</span><span style="color:#E1E4E8">&#x3C;</span></span>\n<span class="line"><span style="color:#F97583">        dyn</span><span style="color:#B392F0"> Future</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#B392F0">Output</span><span style="color:#F97583"> =</span><span style="color:#B392F0"> Result</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#B392F0">TOutput</span><span style="color:#E1E4E8">, </span><span style="color:#B392F0">TError</span><span style="color:#E1E4E8">>> </span><span style="color:#F97583">+</span><span style="color:#B392F0"> Send</span></span>\n<span class="line"><span style="color:#E1E4E8">    >> </span><span style="color:#F97583">+</span><span style="color:#B392F0"> Send</span><span style="color:#F97583"> +</span><span style="color:#B392F0"> Sync</span><span style="color:#E1E4E8">>,</span></span>\n<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>\n<p>Effects compose via <code>and_then</code> (sequential), <code>zip</code> (parallel), and <code>fallback</code>\n(error recovery). Execution overhead is ~17ns per effect.</p>\n<h2 id="environment-system">Environment System 🌍</h2>\n<p>The <code>Environment</code> trait system implements capability-based architecture:</p>\n<ol>\n<li>Effects declare required capabilities</li>\n<li>Concrete environments implement the required traits</li>\n<li><code>ApplicationRunTime</code> resolves and provides capabilities at runtime</li>\n<li>Effect executes with the provided capability</li>\n</ol>\n<h2 id="service-contracts-traits">Service Contracts (Traits) 📋</h2>\n<p>Common defines async traits that VSCode services are lifted into:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>VSCode Service</th><th>Common Trait</th></tr></thead><tbody><tr><td><code>IFileService</code></td><td><code>FileSystemService</code></td></tr><tr><td><code>IWorkspaceService</code></td><td><code>WorkspaceService</code></td></tr><tr><td><code>IConfigurationService</code></td><td><code>ConfigurationService</code></td></tr><tr><td><code>ICommandService</code></td><td><code>CommandService</code></td></tr><tr><td><code>IDocumentService</code></td><td><code>DocumentProvider</code></td></tr></tbody></table>\n<p>Implementations live in Mountain (Rust) and Wind (Effect-TS).</p>\n<h2 id="cross-language-architecture">Cross-Language Architecture 🌐</h2>\n<p>DTOs ensure consistent data structures across the project:</p>\n<ul>\n<li>Rust backend serializes to protocol buffers</li>\n<li>TypeScript frontend deserializes from gRPC</li>\n<li>Compile-time validation enforces type safety</li>\n</ul>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>Traits --shared--> Mountain (Rust impl)</span></span>\n<span class="line"><span>Traits --shared--> Wind (Effect-TS impl)</span></span>\n<span class="line"><span>Traits --shared--> Cocoon (Node.js impl)</span></span>\n<span class="line"><span>DTOs --shared--> All consumers</span></span>\n<span class="line"><span>Errors --shared--> All consumers</span></span></code></pre>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/common">Common overview</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-mountain">Mountain implementation details</a></li>\n</ul>', { headings: 813, localImagePaths: 832, remoteImagePaths: 833, frontmatter: 834, imagePaths: 835 }, [814, 816, 819, 822, 825, 828, 831], { depth: 25, slug: 815, text: 804 }, "common---deep-dive", { depth: 29, slug: 817, text: 818 }, "architecture-principles", "Architecture Principles 🎯", { depth: 29, slug: 820, text: 821 }, "actioneffect-system", "ActionEffect System ⚡", { depth: 29, slug: 823, text: 824 }, "environment-system", "Environment System 🌍", { depth: 29, slug: 826, text: 827 }, "service-contracts-traits", "Service Contracts (Traits) 📋", { depth: 29, slug: 829, text: 830 }, "cross-language-architecture", "Cross-Language Architecture 🌐", { depth: 29, slug: 401, text: 402 }, [], [], { title: 804, section: 582, order: 805, description: 806 }, [], "deep-dive-echo", { id: 836, data: 838, body: 842, filePath: 843, digest: 844, rendered: 845 }, { title: 839, section: 582, order: 840, description: 841 }, "Echo - Deep Dive", 36, "Work-stealing scheduler architecture, priority queues, and Mountain integration", "# Echo - Deep Dive\n\nEcho is a work-stealing scheduler providing high-performance task execution for\nMountain's ApplicationRunTime and other components requiring async task\nmanagement.\n\n## Architecture 🏗️\n\nBuilt on `crossbeam-deque` for lock-free work-stealing queues. Supports three\npriority levels (High, Normal, Low) for latency-sensitive operations.\n\n### Core Components\n\n| Module             | Role                                                                        |\n| ------------------ | --------------------------------------------------------------------------- |\n| `Task`             | Unit of work with priority, generic future support, execution context       |\n| `Queue`            | Lock-free work-stealing queue using `crossbeam_deque::Worker` and `Stealer` |\n| `Scheduler`        | Master coordinator with fluent builder API and worker pool                  |\n| `SchedulerBuilder` | Configures worker count and scheduler parameters                            |\n\n### Design Principles\n\n- **Work-stealing**: Idle workers steal from busy workers' queues for load\n  balancing\n- **Lock-free**: All queue operations use atomic instructions\n- **Structured concurrency**: Supervised worker pool with graceful\n  startup/shutdown\n- **Priority-aware**: High/Normal/Low queues interleaved by worker threads\n- **Failure resilient**: Task failure does not crash the worker pool\n\n## Performance ⚡\n\n| Metric                     | Value                        |\n| -------------------------- | ---------------------------- |\n| Task submission latency    | ~0.05ms                      |\n| Queue operations           | ~0.02ms (lock-free push/pop) |\n| Work-stealing transfer     | ~0.1ms                       |\n| Total task latency         | ~0.18ms + execution time     |\n| Single-threaded throughput | 1M tasks/second              |\n| Memory per task            | < 64 bytes                   |\n| Context switch cost        | ~100 nanoseconds             |\n\nScales linearly with available CPU cores.\n\n## Integration with Mountain 🔗\n\nEcho is integrated as Mountain's primary scheduler:\n\n```\nMountain ApplicationRunTime\n  -> submits ActionEffects as Tasks to Echo\n  -> Echo distributes to worker threads\n  -> Workers resolve capabilities and execute\n  -> Results returned to ApplicationRunTime\n```\n\nEffect execution overhead when integrated: < 1 microsecond.\n\n## Related Documentation 📖\n\n- [Echo overview](https://Editor.Land/Doc/echo)\n- [Mountain ApplicationRunTime](https://Editor.Land/Doc/deep-dive-mountain)\n- [Common ActionEffect system](https://Editor.Land/Doc/deep-dive-common)", "Source/Content/doc/deep-dive-echo.md", "309145c1b3288417", { html: 846, metadata: 847 }, '<h1 id="echo---deep-dive">Echo - Deep Dive</h1>\n<p>Echo is a work-stealing scheduler providing high-performance task execution for\nMountain’s ApplicationRunTime and other components requiring async task\nmanagement.</p>\n<h2 id="architecture️">Architecture 🏗️</h2>\n<p>Built on <code>crossbeam-deque</code> for lock-free work-stealing queues. Supports three\npriority levels (High, Normal, Low) for latency-sensitive operations.</p>\n<h3 id="core-components">Core Components</h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Module</th><th>Role</th></tr></thead><tbody><tr><td><code>Task</code></td><td>Unit of work with priority, generic future support, execution context</td></tr><tr><td><code>Queue</code></td><td>Lock-free work-stealing queue using <code>crossbeam_deque::Worker</code> and <code>Stealer</code></td></tr><tr><td><code>Scheduler</code></td><td>Master coordinator with fluent builder API and worker pool</td></tr><tr><td><code>SchedulerBuilder</code></td><td>Configures worker count and scheduler parameters</td></tr></tbody></table>\n<h3 id="design-principles">Design Principles</h3>\n<ul>\n<li><strong>Work-stealing</strong>: Idle workers steal from busy workers’ queues for load\nbalancing</li>\n<li><strong>Lock-free</strong>: All queue operations use atomic instructions</li>\n<li><strong>Structured concurrency</strong>: Supervised worker pool with graceful\nstartup/shutdown</li>\n<li><strong>Priority-aware</strong>: High/Normal/Low queues interleaved by worker threads</li>\n<li><strong>Failure resilient</strong>: Task failure does not crash the worker pool</li>\n</ul>\n<h2 id="performance">Performance ⚡</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Metric</th><th>Value</th></tr></thead><tbody><tr><td>Task submission latency</td><td>~0.05ms</td></tr><tr><td>Queue operations</td><td>~0.02ms (lock-free push/pop)</td></tr><tr><td>Work-stealing transfer</td><td>~0.1ms</td></tr><tr><td>Total task latency</td><td>~0.18ms + execution time</td></tr><tr><td>Single-threaded throughput</td><td>1M tasks/second</td></tr><tr><td>Memory per task</td><td>&#x3C; 64 bytes</td></tr><tr><td>Context switch cost</td><td>~100 nanoseconds</td></tr></tbody></table>\n<p>Scales linearly with available CPU cores.</p>\n<h2 id="integration-with-mountain">Integration with Mountain 🔗</h2>\n<p>Echo is integrated as Mountain’s primary scheduler:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>Mountain ApplicationRunTime</span></span>\n<span class="line"><span>  -> submits ActionEffects as Tasks to Echo</span></span>\n<span class="line"><span>  -> Echo distributes to worker threads</span></span>\n<span class="line"><span>  -> Workers resolve capabilities and execute</span></span>\n<span class="line"><span>  -> Results returned to ApplicationRunTime</span></span></code></pre>\n<p>Effect execution overhead when integrated: &#x3C; 1 microsecond.</p>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/echo">Echo overview</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-mountain">Mountain ApplicationRunTime</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-common">Common ActionEffect system</a></li>\n</ul>', { headings: 848, localImagePaths: 867, remoteImagePaths: 868, frontmatter: 869, imagePaths: 870 }, [849, 851, 854, 857, 860, 863, 866], { depth: 25, slug: 850, text: 839 }, "echo---deep-dive", { depth: 29, slug: 852, text: 853 }, "architecture️", "Architecture 🏗️", { depth: 347, slug: 855, text: 856 }, "core-components", "Core Components", { depth: 347, slug: 858, text: 859 }, "design-principles", "Design Principles", { depth: 29, slug: 861, text: 862 }, "performance", "Performance ⚡", { depth: 29, slug: 864, text: 865 }, "integration-with-mountain", "Integration with Mountain 🔗", { depth: 29, slug: 401, text: 402 }, [], [], { title: 839, section: 582, order: 840, description: 841 }, [], "deep-dive-grove", { id: 871, data: 873, body: 877, filePath: 878, digest: 879, rendered: 880 }, { title: 874, section: 582, order: 875, description: 876 }, "Grove - Deep Dive", 37, "Rust/WASM extension host, WebAssembly runtime, and sandboxed extension execution", "# Grove - Deep Dive\n\nGrove is a native, sandboxed Rust/WASM extension host that runs VS Code\nextensions compiled to WebAssembly, complementing the Node.js-based Cocoon host.\n\n## Architecture 🏗️\n\nFive-layer design: extension host controller, WASM runtime, VS Code API bridge,\ntransport layer, and shared utilities.\n\n### Layer Breakdown\n\n| Layer                     | Key Modules                                                                                  |\n| ------------------------- | -------------------------------------------------------------------------------------------- |\n| Extension Host Controller | `ExtensionHost.rs`, `ExtensionManager.rs`, `Activation.rs`, `Lifecycle.rs`, `APIBridge.rs`   |\n| WASM Runtime              | `Runtime.rs` (WASMtime engine), `ModuleLoader.rs`, `MemoryManager.rs`, `HostBridge.rs`       |\n| Transport                 | `gRPCTransport.rs`, `IPCTransport.rs`, `WASMTransport.rs` with pluggable `Strategy.rs` trait |\n| API                       | `vscode.rs`, `types.rs` matching VS Code TypeScript types                                    |\n| Protocol/Services         | `SpineConnection.rs`, host services, shared utilities                                        |\n\n## Data Flow 🔄\n\n1. Mountain sends `ActivateExtension` via Grove.proto\n2. Grove loads extension manifest and compiles WASM module\n3. WASM runtime instantiates the module with host functions\n4. Extension calls host functions (e.g. `vscode.workspace.readFile`)\n5. HostBridge dispatches, Grove forwards service call to Mountain via Transport\n6. Mountain returns result, passed back through WASM to extension\n\n## WASM Runtime ⚡\n\n- **Engine**: WASMtime with per-extension memory limits\n- **Module loading**: Compilation and caching of WASM files\n- **Memory management**: Linear memory allocation and bounds enforcement\n- **Host bridge**: Registered functions available to extension WASM code\n\n## Transport Options 🚚\n\nSelectable via Cargo features:\n\n| Feature | Description                            |\n| ------- | -------------------------------------- |\n| `grpc`  | gRPC via Mountain (default)            |\n| `wasm`  | WASMtime runtime (default)             |\n| `ipc`   | Unix/Windows IPC transport (Unix only) |\n| `all`   | All features enabled                   |\n\n## Configuration ⚙️\n\n| Option          | Default          | Description                                       |\n| --------------- | ---------------- | ------------------------------------------------- |\n| Standalone mode | off              | Run without Mountain for testing (`--standalone`) |\n| Extension path  | unset            | Load specific extension directly (`--extension`)  |\n| Memory limit    | platform default | Per-extension WASM memory ceiling                 |\n| WASM target     | `wasm32-wasi`    | Extensions must target this ABI                   |\n\n## Integration 🔗\n\n| Element  | Direction     | Mechanism            | Description                                  |\n| :------- | :------------ | :------------------- | :------------------------------------------- |\n| Mountain | Bidirectional | gRPC via Grove.proto | Extension activation and API call forwarding |\n| Vine     | Inbound       | Protocol definition  | Grove.proto extends Vine service definitions |\n| Cocoon   | Sibling       | Shared API surface   | Same VS Code API surface for portability     |\n\n## Related\n\n- [Grove overview](/doc/grove)\n- [Cocoon Node.js extension host](/doc/deep-dive-cocoon)\n- [Vine protocol reference](/doc/deep-dive-vine)", "Source/Content/doc/deep-dive-grove.md", "44383484edac16a5", { html: 881, metadata: 882 }, '<h1 id="grove---deep-dive">Grove - Deep Dive</h1>\n<p>Grove is a native, sandboxed Rust/WASM extension host that runs VS Code\nextensions compiled to WebAssembly, complementing the Node.js-based Cocoon host.</p>\n<h2 id="architecture️">Architecture 🏗️</h2>\n<p>Five-layer design: extension host controller, WASM runtime, VS Code API bridge,\ntransport layer, and shared utilities.</p>\n<h3 id="layer-breakdown">Layer Breakdown</h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Layer</th><th>Key Modules</th></tr></thead><tbody><tr><td>Extension Host Controller</td><td><code>ExtensionHost.rs</code>, <code>ExtensionManager.rs</code>, <code>Activation.rs</code>, <code>Lifecycle.rs</code>, <code>APIBridge.rs</code></td></tr><tr><td>WASM Runtime</td><td><code>Runtime.rs</code> (WASMtime engine), <code>ModuleLoader.rs</code>, <code>MemoryManager.rs</code>, <code>HostBridge.rs</code></td></tr><tr><td>Transport</td><td><code>gRPCTransport.rs</code>, <code>IPCTransport.rs</code>, <code>WASMTransport.rs</code> with pluggable <code>Strategy.rs</code> trait</td></tr><tr><td>API</td><td><code>vscode.rs</code>, <code>types.rs</code> matching VS Code TypeScript types</td></tr><tr><td>Protocol/Services</td><td><code>SpineConnection.rs</code>, host services, shared utilities</td></tr></tbody></table>\n<h2 id="data-flow">Data Flow 🔄</h2>\n<ol>\n<li>Mountain sends <code>ActivateExtension</code> via Grove.proto</li>\n<li>Grove loads extension manifest and compiles WASM module</li>\n<li>WASM runtime instantiates the module with host functions</li>\n<li>Extension calls host functions (e.g. <code>vscode.workspace.readFile</code>)</li>\n<li>HostBridge dispatches, Grove forwards service call to Mountain via Transport</li>\n<li>Mountain returns result, passed back through WASM to extension</li>\n</ol>\n<h2 id="wasm-runtime">WASM Runtime ⚡</h2>\n<ul>\n<li><strong>Engine</strong>: WASMtime with per-extension memory limits</li>\n<li><strong>Module loading</strong>: Compilation and caching of WASM files</li>\n<li><strong>Memory management</strong>: Linear memory allocation and bounds enforcement</li>\n<li><strong>Host bridge</strong>: Registered functions available to extension WASM code</li>\n</ul>\n<h2 id="transport-options">Transport Options 🚚</h2>\n<p>Selectable via Cargo features:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Feature</th><th>Description</th></tr></thead><tbody><tr><td><code>grpc</code></td><td>gRPC via Mountain (default)</td></tr><tr><td><code>wasm</code></td><td>WASMtime runtime (default)</td></tr><tr><td><code>ipc</code></td><td>Unix/Windows IPC transport (Unix only)</td></tr><tr><td><code>all</code></td><td>All features enabled</td></tr></tbody></table>\n<h2 id="configuration️">Configuration ⚙️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Option</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td>Standalone mode</td><td>off</td><td>Run without Mountain for testing (<code>--standalone</code>)</td></tr><tr><td>Extension path</td><td>unset</td><td>Load specific extension directly (<code>--extension</code>)</td></tr><tr><td>Memory limit</td><td>platform default</td><td>Per-extension WASM memory ceiling</td></tr><tr><td>WASM target</td><td><code>wasm32-wasi</code></td><td>Extensions must target this ABI</td></tr></tbody></table>\n<h2 id="integration">Integration 🔗</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th align="left">Element</th><th align="left">Direction</th><th align="left">Mechanism</th><th align="left">Description</th></tr></thead><tbody><tr><td align="left">Mountain</td><td align="left">Bidirectional</td><td align="left">gRPC via Grove.proto</td><td align="left">Extension activation and API call forwarding</td></tr><tr><td align="left">Vine</td><td align="left">Inbound</td><td align="left">Protocol definition</td><td align="left">Grove.proto extends Vine service definitions</td></tr><tr><td align="left">Cocoon</td><td align="left">Sibling</td><td align="left">Shared API surface</td><td align="left">Same VS Code API surface for portability</td></tr></tbody></table>\n<h2 id="related">Related</h2>\n<ul>\n<li><a href="/doc/grove">Grove overview</a></li>\n<li><a href="/doc/deep-dive-cocoon">Cocoon Node.js extension host</a></li>\n<li><a href="/doc/deep-dive-vine">Vine protocol reference</a></li>\n</ul>', { headings: 883, localImagePaths: 902, remoteImagePaths: 903, frontmatter: 904, imagePaths: 905 }, [884, 886, 887, 890, 891, 894, 897, 898, 901], { depth: 25, slug: 885, text: 874 }, "grove---deep-dive", { depth: 29, slug: 852, text: 853 }, { depth: 347, slug: 888, text: 889 }, "layer-breakdown", "Layer Breakdown", { depth: 29, slug: 744, text: 745 }, { depth: 29, slug: 892, text: 893 }, "wasm-runtime", "WASM Runtime ⚡", { depth: 29, slug: 895, text: 896 }, "transport-options", "Transport Options 🚚", { depth: 29, slug: 750, text: 751 }, { depth: 29, slug: 899, text: 900 }, "integration", "Integration 🔗", { depth: 29, slug: 619, text: 620 }, [], [], { title: 874, section: 582, order: 875, description: 876 }, [], "deep-dive-maintain", { id: 906, data: 908, body: 912, filePath: 913, digest: 914, rendered: 915 }, { title: 909, section: 582, order: 910, description: 911 }, "Maintain - Deep Dive", 44, "Build system, Rhai scripting engine, and type-safe configuration editing", "# Maintain - Deep Dive\n\nMaintain is the build system and CI/CD toolkit for the Land project. It\norchestrates builds across all elements, embeds a Rhai scripting engine for\nflexible automation, and performs type-safe TOML/JSON5 configuration editing.\n\n## Architecture 🚀\n\nMaintain is a Rust binary and library. The CLI layer delegates to the Rhai\nengine or direct build functions. Configuration editing uses `toml_edit` and\n`json5` for structured, non-lossy modifications.\n\n### Modules\n\n## || Path | ||\n\n| | `Source/Library.rs` - Entry point, wires CLI, logging, error handling | |\n`Source/Build/CLI.rs` - `clap`-based CLI: debug, release, profile, dev | |\n`Source/Build/Definition.rs` - Build group definitions with ordering | |\n`Source/Build/Fn.rs` - Core build function dispatch: clean, compile, link,\npost-process | | `Source/Build/Process.rs` - Child process spawning with\nstdout/stderr capture | | `Source/Build/TomlEdit.rs` - Non-lossy TOML editing\nfor Cargo.toml | | `Source/Build/JsonEdit.rs` - JSON5-aware editing for\npackage.json | | `Source/Build/Pascalize.rs` - PascalCase naming utilities | |\n`Source/Build/GetTauriTargetTriple.rs` - Platform detection | |\n`Source/Build/EnvironmentResolver.rs` - Environment variable resolution | |\n`Source/Build/Rhai/ConfigLoader.rs` - Rhai build script loading | |\n`Source/Build/Rhai/ScriptRunner.rs` - Script execution with Land API |\n\n## Data Flow 🔄\n\nDeveloper runs a shell script (e.g. `Release.sh`) which calls Maintain CLI. The\nCLI:\n\n1. Loads the Rhai build configuration script\n2. Reads current Cargo.toml/package.json versions\n3. Iterates build group elements, spawning `cargo build` subprocesses\n4. Updates Cargo.toml if version bump needed\n5. Reports build summary with timing\n\n## Build Script Entry Points 📜\n\n|| Script | Purpose | || ----------------- | -----------------------------------\n| | `Debug.sh` | Full debug build of all elements | | `Dev-Mountain.sh` |\nHot-reload development mode | | `Release.sh` | Optimized release build | |\n`Profile.sh` | Release build with profiling | | `Debug/All.sh` | Debug all\nincluding frontend | | `Debug/Wind.sh` | Debug Wind TypeScript services only |\n\n## Integration Points 🔗\n\nMaintain builds all Rust elements (Mountain, Air, Echo, Rest, SideCar) as\nsubprocess targets, and triggers TypeScript builds (Output, Wind, Sky) via\npnpm/Turborepo.\n\n## Configuration ⚙️\n\n|| CLI Flag | Description | || ------------------- |\n---------------------------------------- | | `debug` / `release` | Build mode\nand optimization level | | `--target` | Rust target triple for cross-compilation\n| | `--element` | Build specific element only | | `--script` | Override default\nRhai build script | | `--verbose` | Detailed subprocess logging |\n\n## Related Documentation 📖\n\n- [Architecture overview](https://Editor.Land/Doc/architecture)\n- [Maintain GitHub repository](https://github.com/CodeEditorLand/Maintain)", "Source/Content/doc/deep-dive-maintain.md", "60618ba9ec4f159c", { html: 916, metadata: 917 }, '<h1 id="maintain---deep-dive">Maintain - Deep Dive</h1>\n<p>Maintain is the build system and CI/CD toolkit for the Land project. It\norchestrates builds across all elements, embeds a Rhai scripting engine for\nflexible automation, and performs type-safe TOML/JSON5 configuration editing.</p>\n<h2 id="architecture">Architecture 🚀</h2>\n<p>Maintain is a Rust binary and library. The CLI layer delegates to the Rhai\nengine or direct build functions. Configuration editing uses <code>toml_edit</code> and\n<code>json5</code> for structured, non-lossy modifications.</p>\n<h3 id="modules">Modules</h3>\n<h2 id="-path--">|| Path | ||</h2>\n<p>| | <code>Source/Library.rs</code> - Entry point, wires CLI, logging, error handling | |\n<code>Source/Build/CLI.rs</code> - <code>clap</code>-based CLI: debug, release, profile, dev | |\n<code>Source/Build/Definition.rs</code> - Build group definitions with ordering | |\n<code>Source/Build/Fn.rs</code> - Core build function dispatch: clean, compile, link,\npost-process | | <code>Source/Build/Process.rs</code> - Child process spawning with\nstdout/stderr capture | | <code>Source/Build/TomlEdit.rs</code> - Non-lossy TOML editing\nfor Cargo.toml | | <code>Source/Build/JsonEdit.rs</code> - JSON5-aware editing for\npackage.json | | <code>Source/Build/Pascalize.rs</code> - PascalCase naming utilities | |\n<code>Source/Build/GetTauriTargetTriple.rs</code> - Platform detection | |\n<code>Source/Build/EnvironmentResolver.rs</code> - Environment variable resolution | |\n<code>Source/Build/Rhai/ConfigLoader.rs</code> - Rhai build script loading | |\n<code>Source/Build/Rhai/ScriptRunner.rs</code> - Script execution with Land API |</p>\n<h2 id="data-flow">Data Flow 🔄</h2>\n<p>Developer runs a shell script (e.g. <code>Release.sh</code>) which calls Maintain CLI. The\nCLI:</p>\n<ol>\n<li>Loads the Rhai build configuration script</li>\n<li>Reads current Cargo.toml/package.json versions</li>\n<li>Iterates build group elements, spawning <code>cargo build</code> subprocesses</li>\n<li>Updates Cargo.toml if version bump needed</li>\n<li>Reports build summary with timing</li>\n</ol>\n<h2 id="build-script-entry-points">Build Script Entry Points 📜</h2>\n<p>|| Script | Purpose | || ----------------- | -----------------------------------\n| | <code>Debug.sh</code> | Full debug build of all elements | | <code>Dev-Mountain.sh</code> |\nHot-reload development mode | | <code>Release.sh</code> | Optimized release build | |\n<code>Profile.sh</code> | Release build with profiling | | <code>Debug/All.sh</code> | Debug all\nincluding frontend | | <code>Debug/Wind.sh</code> | Debug Wind TypeScript services only |</p>\n<h2 id="integration-points">Integration Points 🔗</h2>\n<p>Maintain builds all Rust elements (Mountain, Air, Echo, Rest, SideCar) as\nsubprocess targets, and triggers TypeScript builds (Output, Wind, Sky) via\npnpm/Turborepo.</p>\n<h2 id="configuration️">Configuration ⚙️</h2>\n<p>|| CLI Flag | Description | || ------------------- |\n---------------------------------------- | | <code>debug</code> / <code>release</code> | Build mode\nand optimization level | | <code>--target</code> | Rust target triple for cross-compilation\n| | <code>--element</code> | Build specific element only | | <code>--script</code> | Override default\nRhai build script | | <code>--verbose</code> | Detailed subprocess logging |</p>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture overview</a></li>\n<li><a href="https://github.com/CodeEditorLand/Maintain">Maintain GitHub repository</a></li>\n</ul>', { headings: 918, localImagePaths: 934, remoteImagePaths: 935, frontmatter: 936, imagePaths: 937 }, [919, 921, 922, 924, 927, 928, 931, 932, 933], { depth: 25, slug: 920, text: 909 }, "maintain---deep-dive", { depth: 29, slug: 71, text: 739 }, { depth: 347, slug: 923, text: 193 }, "modules", { depth: 29, slug: 925, text: 926 }, "-path--", "|| Path | ||", { depth: 29, slug: 744, text: 745 }, { depth: 29, slug: 929, text: 930 }, "build-script-entry-points", "Build Script Entry Points 📜", { depth: 29, slug: 747, text: 748 }, { depth: 29, slug: 750, text: 751 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 909, section: 582, order: 910, description: 911 }, [], "deep-dive-mist", { id: 938, data: 940, body: 944, filePath: 945, digest: 946, rendered: 947 }, { title: 941, section: 582, order: 942, description: 943 }, "Mist - Deep Dive", 38, "Local DNS server, land.playform.cloud zone authority, and DNSSEC enforcement", "# Mist - Deep Dive\n\nMist operates a local authoritative DNS server for the `land.playform.cloud`\nzone, ensuring all private network communication stays on loopback and\npreventing sidecars from reaching unauthorized external hosts.\n\n## Architecture 🚀\n\nBuilt on Hickory DNS. Two zones: an authoritative `land.playform.cloud` zone and\na restricted forward allowlist.\n\n### Modules\n\n|                                                                                    | Path |     |     |\n| ---------------------------------------------------------------------------------- | ---- | --- | --- |\n| `Source/lib.rs` - Public API: `start(port)`, `dns_port()`, `LandDnsResolver`       |\n| `Source/server.rs` - Hickory UDP + TCP listeners, catalog wiring                   |      |\n| `Source/zone.rs` - `land.playform.cloud` zone: SOA, A records, wildcard resolution |      |\n| `Source/resolver.rs` - DNS client pointed at the local server for consumer use     |\n| `Source/forward_security.rs` - Forward allowlist enforcement                       |\n\n## Configuration ⚙️\n\n| Parameter          | Value                                                          |\n| ------------------ | -------------------------------------------------------------- |\n| Preferred port     | `5380` (fallback via portpicker)                               |\n| Bind address       | `127.0.0.1` (loopback only)                                    |\n| Authoritative zone | `land.playform.cloud` - all subdomains resolve to `127.0.0.1`  |\n| Forward allowlist  | `update.land.playform.cloud` only domain resolvable externally |\n| DNSSEC             | ECDSA P-256 zone signing                                       |\n| Transport          | UDP + TCP                                                      |\n\n## Startup Sequence 🏁\n\n1. Mountain calls `Mist::start(5380)` during initialization\n2. Mist binds to port; portpicker selects alternative if unavailable\n3. Bound port stored in Mountain's `DnsPort` managed Tauri state\n4. Mountain passes port to Air, SideCar, and Cocoon for DNS client configuration\n\n## Resolution Flow 🔍\n\nFor `api.land.playform.cloud`: query resolves to `127.0.0.1` (authoritative,\nwith RRSIG). For external domains not in allowlist: query returns `REFUSED`.\n\n## Integration Points 🔗\n\n| Element  | Direction | Mechanism                                                             |\n| -------- | --------- | --------------------------------------------------------------------- |\n| Mountain | Consumer  | `Mist::start()` Rust API, stores port in DnsPort state                |\n| Air      | Consumer  | `LandDnsResolver` for HTTP client DNS override                        |\n| SideCar  | Consumer  | Environment variable passed to Node.js processes                      |\n| Cocoon   | Consumer  | Resolves `cocoon.land.playform.cloud` and gRPC addresses through Mist |\n\n## Related Documentation 📖\n\n- [Architecture overview](https://Land.PlayForm.Cloud/Doc/architecture)\n- [Mist GitHub repository](https://github.com/CodeEditorLand/Mist)\n- [Air HTTP client DNS configuration](https://Land.PlayForm.Cloud/Doc/deep-dive-air)", "Source/Content/doc/deep-dive-mist.md", "d07e32157021af1e", { html: 948, metadata: 949 }, '<h1 id="mist---deep-dive">Mist - Deep Dive</h1>\n<p>Mist operates a local authoritative DNS server for the <code>land.playform.cloud</code>\nzone, ensuring all private network communication stays on loopback and\npreventing sidecars from reaching unauthorized external hosts.</p>\n<h2 id="architecture">Architecture 🚀</h2>\n<p>Built on Hickory DNS. Two zones: an authoritative <code>land.playform.cloud</code> zone and\na restricted forward allowlist.</p>\n<h3 id="modules">Modules</h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th></th><th>Path</th><th></th><th></th></tr></thead><tbody><tr><td><code>Source/lib.rs</code> - Public API: <code>start(port)</code>, <code>dns_port()</code>, <code>LandDnsResolver</code></td><td></td><td></td><td></td></tr><tr><td><code>Source/server.rs</code> - Hickory UDP + TCP listeners, catalog wiring</td><td></td><td></td><td></td></tr><tr><td><code>Source/zone.rs</code> - <code>land.playform.cloud</code> zone: SOA, A records, wildcard resolution</td><td></td><td></td><td></td></tr><tr><td><code>Source/resolver.rs</code> - DNS client pointed at the local server for consumer use</td><td></td><td></td><td></td></tr><tr><td><code>Source/forward_security.rs</code> - Forward allowlist enforcement</td><td></td><td></td><td></td></tr></tbody></table>\n<h2 id="configuration️">Configuration ⚙️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Preferred port</td><td><code>5380</code> (fallback via portpicker)</td></tr><tr><td>Bind address</td><td><code>127.0.0.1</code> (loopback only)</td></tr><tr><td>Authoritative zone</td><td><code>land.playform.cloud</code> - all subdomains resolve to <code>127.0.0.1</code></td></tr><tr><td>Forward allowlist</td><td><code>update.land.playform.cloud</code> only domain resolvable externally</td></tr><tr><td>DNSSEC</td><td>ECDSA P-256 zone signing</td></tr><tr><td>Transport</td><td>UDP + TCP</td></tr></tbody></table>\n<h2 id="startup-sequence">Startup Sequence 🏁</h2>\n<ol>\n<li>Mountain calls <code>Mist::start(5380)</code> during initialization</li>\n<li>Mist binds to port; portpicker selects alternative if unavailable</li>\n<li>Bound port stored in Mountain’s <code>DnsPort</code> managed Tauri state</li>\n<li>Mountain passes port to Air, SideCar, and Cocoon for DNS client configuration</li>\n</ol>\n<h2 id="resolution-flow">Resolution Flow 🔍</h2>\n<p>For <code>api.land.playform.cloud</code>: query resolves to <code>127.0.0.1</code> (authoritative,\nwith RRSIG). For external domains not in allowlist: query returns <code>REFUSED</code>.</p>\n<h2 id="integration-points">Integration Points 🔗</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Element</th><th>Direction</th><th>Mechanism</th></tr></thead><tbody><tr><td>Mountain</td><td>Consumer</td><td><code>Mist::start()</code> Rust API, stores port in DnsPort state</td></tr><tr><td>Air</td><td>Consumer</td><td><code>LandDnsResolver</code> for HTTP client DNS override</td></tr><tr><td>SideCar</td><td>Consumer</td><td>Environment variable passed to Node.js processes</td></tr><tr><td>Cocoon</td><td>Consumer</td><td>Resolves <code>cocoon.land.playform.cloud</code> and gRPC addresses through Mist</td></tr></tbody></table>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Land.PlayForm.Cloud/Doc/architecture">Architecture overview</a></li>\n<li><a href="https://github.com/CodeEditorLand/Mist">Mist GitHub repository</a></li>\n<li><a href="https://Land.PlayForm.Cloud/Doc/deep-dive-air">Air HTTP client DNS configuration</a></li>\n</ul>', { headings: 950, localImagePaths: 964, remoteImagePaths: 965, frontmatter: 966, imagePaths: 967 }, [951, 953, 954, 955, 956, 959, 962, 963], { depth: 25, slug: 952, text: 941 }, "mist---deep-dive", { depth: 29, slug: 71, text: 739 }, { depth: 347, slug: 923, text: 193 }, { depth: 29, slug: 750, text: 751 }, { depth: 29, slug: 957, text: 958 }, "startup-sequence", "Startup Sequence 🏁", { depth: 29, slug: 960, text: 961 }, "resolution-flow", "Resolution Flow 🔍", { depth: 29, slug: 747, text: 748 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 941, section: 582, order: 942, description: 943 }, [], "deep-dive-mountain", { id: 968, data: 970, body: 974, filePath: 975, digest: 976, rendered: 977 }, { title: 971, section: 582, order: 972, description: 973 }, "Mountain - Deep Dive", 30, "Technical architecture of the Mountain Rust core: native service implementations, the ApplicationRunTime engine, state management, and gRPC communication.", '# Mountain - Deep Dive\n\nMountain is the core Rust binary in the Land project. It provides native\nimplementations of VSCode-compatible services, manages the application lifecycle\nvia Tauri, and orchestrates all sidecar processes (Cocoon, Air) through gRPC.\n\n## Core Architecture\n\nMountain implements VSCode service `trait`s defined in the `Common` crate and\nexecutes them through the `ApplicationRunTime`, which bridges declarative effect\ndescriptions to concrete native capabilities.\n\n| Principle                     | Description                                                                         | Key Components                         |\n| :---------------------------- | :---------------------------------------------------------------------------------- | :------------------------------------- |\n| Native Service Implementation | Concrete implementations of all abstract service traits for native-speed operations | `Environment/*` providers              |\n| Effect System Execution       | Executes `ActionEffect`s through `ApplicationRunTime`                               | `RunTime/*`, `Track/EffectCreation.rs` |\n| Centralized State Management  | Thread-safe `ApplicationState` managed by Tauri                                     | `ApplicationState/*`                   |\n| gRPC-Powered IPC              | `tonic`-based gRPC for communication with Cocoon                                    | `Vine/*`                               |\n| Tauri Integration             | Cross-platform windowing, system integration, secure communication                  | `Binary.rs`, Tauri plugins             |\n| Sidecar Orchestration         | Launch, manage, and health-monitor the Cocoon process                               | `ProcessManagement/*`                  |\n\n## ApplicationRunTime Engine\n\n`MountainRunTime` resolves capabilities from `MountainEnvironment` and submits\neffects to an `Echo` scheduler. The execution flow:\n\n1. Wind UI sends a Tauri command request.\n2. Track dispatcher forwards the effect to `ApplicationRunTime`.\n3. RunTime resolves the required capability from the environment.\n4. The scheduler executes the effect against native OS operations.\n5. Results flow back through Track to the UI via Tauri events.\n\n```rust\npub struct MountainRunTime {\n    environment: Arc<MountainEnvironment>,\n    scheduler: Arc<Echo::Scheduler::Scheduler>,\n}\n```\n\n## State Management\n\n`ApplicationState` holds documents, extensions, UI layout, and preferences\nbehind `RwLock` guards for concurrent access. Tauri events broadcast state\nchanges to all listeners.\n\n## VSCode Service Implementations\n\nMountain provides concrete implementations for three major service areas:\n\n- **File System** - Async file operations via `tokio::fs`, with URI-to-path\n  conversion and optional LRU caching.\n- **Window Management** - Terminal creation via `portable-pty`, native\n  notifications via Tauri.\n- **Workspace** - Workspace folder management, document opening, and\n  configuration handling.\n\n## Naming Convention\n\nMountain (and the broader Land project) uses PascalCase for all Rust elements --\nstructs, enums, traits, functions, methods, modules, fields, local variables,\nand file names. This intentional departure from Rust conventions ensures:\n\n- Direct mapping between Rust DTOs, Protocol Buffer messages, and TypeScript\n  interfaces.\n- No `#[serde(rename = "...")]` boilerplate.\n- Consistent naming across Rust (Mountain/Common), TypeScript (Wind, Cocoon),\n  and Protocol Buffers (Vine).\n\nModules signal this deviation with\n`#![allow(non_snake_case, non_camel_case_types)]` at the top. Exceptions exist\nfor external crate types, standard library items, lifetime parameters, and\nbuilt-in attributes/macros.\n\n## Sidecar Orchestration\n\n`ProcessManager` launches the Cocoon Node.js process, configures its environment\nvariables (including `VINE_PORT`), and runs a periodic health-monitoring loop on\na Tokio task. Unhealthy sidecars are restarted automatically.\n\n## Process Structure\n\n```text\nSource/\n+-- ApplicationState/      Central state with RwLock guards\n+-- Environment/           MountainEnvironment and service providers\n+-- RunTime/               ApplicationRunTime engine\n+-- IPC/                   Tauri IPC server and handlers\n+-- ProcessManagement/     Cocoon process lifecycle\n+-- Track/                 Effect dispatch and routing\n+-- Binary.rs              Entry point\n```', "Source/Content/doc/deep-dive-mountain.md", "6902e6b1f0d5b6e8", { html: 978, metadata: 979 }, '<h1 id="mountain---deep-dive">Mountain - Deep Dive</h1>\n<p>Mountain is the core Rust binary in the Land project. It provides native\nimplementations of VSCode-compatible services, manages the application lifecycle\nvia Tauri, and orchestrates all sidecar processes (Cocoon, Air) through gRPC.</p>\n<h2 id="core-architecture">Core Architecture</h2>\n<p>Mountain implements VSCode service <code>trait</code>s defined in the <code>Common</code> crate and\nexecutes them through the <code>ApplicationRunTime</code>, which bridges declarative effect\ndescriptions to concrete native capabilities.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th align="left">Principle</th><th align="left">Description</th><th align="left">Key Components</th></tr></thead><tbody><tr><td align="left">Native Service Implementation</td><td align="left">Concrete implementations of all abstract service traits for native-speed operations</td><td align="left"><code>Environment/*</code> providers</td></tr><tr><td align="left">Effect System Execution</td><td align="left">Executes <code>ActionEffect</code>s through <code>ApplicationRunTime</code></td><td align="left"><code>RunTime/*</code>, <code>Track/EffectCreation.rs</code></td></tr><tr><td align="left">Centralized State Management</td><td align="left">Thread-safe <code>ApplicationState</code> managed by Tauri</td><td align="left"><code>ApplicationState/*</code></td></tr><tr><td align="left">gRPC-Powered IPC</td><td align="left"><code>tonic</code>-based gRPC for communication with Cocoon</td><td align="left"><code>Vine/*</code></td></tr><tr><td align="left">Tauri Integration</td><td align="left">Cross-platform windowing, system integration, secure communication</td><td align="left"><code>Binary.rs</code>, Tauri plugins</td></tr><tr><td align="left">Sidecar Orchestration</td><td align="left">Launch, manage, and health-monitor the Cocoon process</td><td align="left"><code>ProcessManagement/*</code></td></tr></tbody></table>\n<h2 id="applicationruntime-engine">ApplicationRunTime Engine</h2>\n<p><code>MountainRunTime</code> resolves capabilities from <code>MountainEnvironment</code> and submits\neffects to an <code>Echo</code> scheduler. The execution flow:</p>\n<ol>\n<li>Wind UI sends a Tauri command request.</li>\n<li>Track dispatcher forwards the effect to <code>ApplicationRunTime</code>.</li>\n<li>RunTime resolves the required capability from the environment.</li>\n<li>The scheduler executes the effect against native OS operations.</li>\n<li>Results flow back through Track to the UI via Tauri events.</li>\n</ol>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="rust"><code><span class="line"><span style="color:#F97583">pub</span><span style="color:#F97583"> struct</span><span style="color:#B392F0"> MountainRunTime</span><span style="color:#E1E4E8"> {</span></span>\n<span class="line"><span style="color:#E1E4E8">    environment</span><span style="color:#F97583">:</span><span style="color:#B392F0"> Arc</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#B392F0">MountainEnvironment</span><span style="color:#E1E4E8">>,</span></span>\n<span class="line"><span style="color:#E1E4E8">    scheduler</span><span style="color:#F97583">:</span><span style="color:#B392F0"> Arc</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#B392F0">Echo</span><span style="color:#F97583">::</span><span style="color:#B392F0">Scheduler</span><span style="color:#F97583">::</span><span style="color:#B392F0">Scheduler</span><span style="color:#E1E4E8">>,</span></span>\n<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>\n<h2 id="state-management">State Management</h2>\n<p><code>ApplicationState</code> holds documents, extensions, UI layout, and preferences\nbehind <code>RwLock</code> guards for concurrent access. Tauri events broadcast state\nchanges to all listeners.</p>\n<h2 id="vscode-service-implementations">VSCode Service Implementations</h2>\n<p>Mountain provides concrete implementations for three major service areas:</p>\n<ul>\n<li><strong>File System</strong> - Async file operations via <code>tokio::fs</code>, with URI-to-path\nconversion and optional LRU caching.</li>\n<li><strong>Window Management</strong> - Terminal creation via <code>portable-pty</code>, native\nnotifications via Tauri.</li>\n<li><strong>Workspace</strong> - Workspace folder management, document opening, and\nconfiguration handling.</li>\n</ul>\n<h2 id="naming-convention">Naming Convention</h2>\n<p>Mountain (and the broader Land project) uses PascalCase for all Rust elements —\nstructs, enums, traits, functions, methods, modules, fields, local variables,\nand file names. This intentional departure from Rust conventions ensures:</p>\n<ul>\n<li>Direct mapping between Rust DTOs, Protocol Buffer messages, and TypeScript\ninterfaces.</li>\n<li>No <code>#[serde(rename = "...")]</code> boilerplate.</li>\n<li>Consistent naming across Rust (Mountain/Common), TypeScript (Wind, Cocoon),\nand Protocol Buffers (Vine).</li>\n</ul>\n<p>Modules signal this deviation with\n<code>#![allow(non_snake_case, non_camel_case_types)]</code> at the top. Exceptions exist\nfor external crate types, standard library items, lifetime parameters, and\nbuilt-in attributes/macros.</p>\n<h2 id="sidecar-orchestration">Sidecar Orchestration</h2>\n<p><code>ProcessManager</code> launches the Cocoon Node.js process, configures its environment\nvariables (including <code>VINE_PORT</code>), and runs a periodic health-monitoring loop on\na Tokio task. Unhealthy sidecars are restarted automatically.</p>\n<h2 id="process-structure">Process Structure</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="text"><code><span class="line"><span>Source/</span></span>\n<span class="line"><span>+-- ApplicationState/      Central state with RwLock guards</span></span>\n<span class="line"><span>+-- Environment/           MountainEnvironment and service providers</span></span>\n<span class="line"><span>+-- RunTime/               ApplicationRunTime engine</span></span>\n<span class="line"><span>+-- IPC/                   Tauri IPC server and handlers</span></span>\n<span class="line"><span>+-- ProcessManagement/     Cocoon process lifecycle</span></span>\n<span class="line"><span>+-- Track/                 Effect dispatch and routing</span></span>\n<span class="line"><span>+-- Binary.rs              Entry point</span></span></code></pre>', { headings: 980, localImagePaths: 1004, remoteImagePaths: 1005, frontmatter: 1006, imagePaths: 1007 }, [981, 983, 986, 989, 992, 995, 998, 1001], { depth: 25, slug: 982, text: 971 }, "mountain---deep-dive", { depth: 29, slug: 984, text: 985 }, "core-architecture", "Core Architecture", { depth: 29, slug: 987, text: 988 }, "applicationruntime-engine", "ApplicationRunTime Engine", { depth: 29, slug: 990, text: 991 }, "state-management", "State Management", { depth: 29, slug: 993, text: 994 }, "vscode-service-implementations", "VSCode Service Implementations", { depth: 29, slug: 996, text: 997 }, "naming-convention", "Naming Convention", { depth: 29, slug: 999, text: 1e3 }, "sidecar-orchestration", "Sidecar Orchestration", { depth: 29, slug: 1002, text: 1003 }, "process-structure", "Process Structure", [], [], { title: 971, section: 582, order: 972, description: 973 }, [], "deep-dive-output", { id: 1008, data: 1010, body: 1014, filePath: 1015, digest: 1016, rendered: 1017 }, { title: 1011, section: 582, order: 1012, description: 1013 }, "Output - Deep Dive", 41, "Build artifact management, esbuild pipeline, and Rest compiler integration", "# Output - Deep Dive\n\nOutput is a JavaScript/TypeScript build package that wraps esbuild. It compiles\nVSCode TypeScript source and CodeEditorLand editor code into JavaScript bundles\nconsumed by Sky, Wind, and Cocoon.\n\n## Architecture 🏗️\n\nTwo compiler modes: default esbuild pipeline and optional Rest (OXC-based)\npipeline, selectable via environment variable.\n\n### Modules\n\n| Path                                                                               |\n| ---------------------------------------------------------------------------------- |\n| `Source/prepublishOnly.sh` - Main build entry script                               |\n| `Source/Run.sh` - Development watch script                                         |\n| `Source/ESBuild/Output.ts` - esbuild programmatic configuration                    |\n| `Source/ESBuild/RestPlugin.ts` - esbuild plugin delegating to Rest binary          |\n| `Source/ESBuild/Microsoft/` - VSCode platform bundle entry points                  |\n| `Source/ESBuild/CodeEditorLand/` - Editor customization bundles                    |\n| `Source/ESBuild/Exclude/` - Module exclusion rules                                 |\n| `Configuration/ESBuild/Microsoft/VSCode.js` - esbuild config for VSCode dependency |\n| `Configuration/ESBuild/CodeEditorLand/Editor.js` - esbuild config for editor       |\n\n## Build Pipeline 🔧\n\nDefault path (esbuild):\n\n1. esbuild reads TypeScript source from `Dependency/Microsoft/VSCode/`\n2. esbuild resolves, bundles, tree-shakes\n3. Output written to `Target/Microsoft/VSCode/`\n\nRest path (`Compiler=Rest`):\n\n1. Rest binary compiles TypeScript to `Target/Rest/Microsoft/VSCode/`\n2. `RestPlugin.ts` intercepts `.ts` files, invokes Rest subprocess\n3. esbuild reads Rest output, applies bundling and merging\n4. Final artifacts land in `Target/Microsoft/VSCode/`\n\n## Integration 🔗\n\n| Element | Direction | Mechanism                                 |\n| ------- | --------- | ----------------------------------------- |\n| Rest    | Consumer  | Process invocation via RestPlugin         |\n| Sky     | Provider  | `@codeeditorland/output` npm package      |\n| Wind    | Provider  | `@codeeditorland/output` npm package      |\n| Cocoon  | Provider  | File path reference from Target directory |\n\n## Configuration ⚙️\n\n| Variable           | Default      | Description                       |\n| ------------------ | ------------ | --------------------------------- |\n| `Compiler`         | `esbuild`    | Set to `Rest` for OXC compilation |\n| `REST_BINARY_PATH` | auto-detect  | Override Rest binary location     |\n| `REST_VERBOSE`     | `false`      | Enable verbose Rest logging       |\n| `NODE_ENV`         | `production` | `development` enables source maps |\n\nRest mode runs 2-3x faster on TypeScript-heavy codebases but requires the Rest\nbinary in the build environment.\n\n## Related Documentation 📖\n\n- [Output overview](https://Editor.Land/Doc/output)\n- [Rest compiler reference](https://Editor.Land/Doc/deep-dive-rest)\n- [Sky UI layer](https://Editor.Land/Doc/deep-dive-sky)\n- [Wind service layer](https://Editor.Land/Doc/deep-dive-wind)", "Source/Content/doc/deep-dive-output.md", "8b1a5e7fafbeb413", { html: 1018, metadata: 1019 }, '<h1 id="output---deep-dive">Output - Deep Dive</h1>\n<p>Output is a JavaScript/TypeScript build package that wraps esbuild. It compiles\nVSCode TypeScript source and CodeEditorLand editor code into JavaScript bundles\nconsumed by Sky, Wind, and Cocoon.</p>\n<h2 id="architecture️">Architecture 🏗️</h2>\n<p>Two compiler modes: default esbuild pipeline and optional Rest (OXC-based)\npipeline, selectable via environment variable.</p>\n<h3 id="modules">Modules</h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Path</th></tr></thead><tbody><tr><td><code>Source/prepublishOnly.sh</code> - Main build entry script</td></tr><tr><td><code>Source/Run.sh</code> - Development watch script</td></tr><tr><td><code>Source/ESBuild/Output.ts</code> - esbuild programmatic configuration</td></tr><tr><td><code>Source/ESBuild/RestPlugin.ts</code> - esbuild plugin delegating to Rest binary</td></tr><tr><td><code>Source/ESBuild/Microsoft/</code> - VSCode platform bundle entry points</td></tr><tr><td><code>Source/ESBuild/CodeEditorLand/</code> - Editor customization bundles</td></tr><tr><td><code>Source/ESBuild/Exclude/</code> - Module exclusion rules</td></tr><tr><td><code>Configuration/ESBuild/Microsoft/VSCode.js</code> - esbuild config for VSCode dependency</td></tr><tr><td><code>Configuration/ESBuild/CodeEditorLand/Editor.js</code> - esbuild config for editor</td></tr></tbody></table>\n<h2 id="build-pipeline">Build Pipeline 🔧</h2>\n<p>Default path (esbuild):</p>\n<ol>\n<li>esbuild reads TypeScript source from <code>Dependency/Microsoft/VSCode/</code></li>\n<li>esbuild resolves, bundles, tree-shakes</li>\n<li>Output written to <code>Target/Microsoft/VSCode/</code></li>\n</ol>\n<p>Rest path (<code>Compiler=Rest</code>):</p>\n<ol>\n<li>Rest binary compiles TypeScript to <code>Target/Rest/Microsoft/VSCode/</code></li>\n<li><code>RestPlugin.ts</code> intercepts <code>.ts</code> files, invokes Rest subprocess</li>\n<li>esbuild reads Rest output, applies bundling and merging</li>\n<li>Final artifacts land in <code>Target/Microsoft/VSCode/</code></li>\n</ol>\n<h2 id="integration">Integration 🔗</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Element</th><th>Direction</th><th>Mechanism</th></tr></thead><tbody><tr><td>Rest</td><td>Consumer</td><td>Process invocation via RestPlugin</td></tr><tr><td>Sky</td><td>Provider</td><td><code>@codeeditorland/output</code> npm package</td></tr><tr><td>Wind</td><td>Provider</td><td><code>@codeeditorland/output</code> npm package</td></tr><tr><td>Cocoon</td><td>Provider</td><td>File path reference from Target directory</td></tr></tbody></table>\n<h2 id="configuration️">Configuration ⚙️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Variable</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>Compiler</code></td><td><code>esbuild</code></td><td>Set to <code>Rest</code> for OXC compilation</td></tr><tr><td><code>REST_BINARY_PATH</code></td><td>auto-detect</td><td>Override Rest binary location</td></tr><tr><td><code>REST_VERBOSE</code></td><td><code>false</code></td><td>Enable verbose Rest logging</td></tr><tr><td><code>NODE_ENV</code></td><td><code>production</code></td><td><code>development</code> enables source maps</td></tr></tbody></table>\n<p>Rest mode runs 2-3x faster on TypeScript-heavy codebases but requires the Rest\nbinary in the build environment.</p>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/output">Output overview</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-rest">Rest compiler reference</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-sky">Sky UI layer</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-wind">Wind service layer</a></li>\n</ul>', { headings: 1020, localImagePaths: 1031, remoteImagePaths: 1032, frontmatter: 1033, imagePaths: 1034 }, [1021, 1023, 1024, 1025, 1028, 1029, 1030], { depth: 25, slug: 1022, text: 1011 }, "output---deep-dive", { depth: 29, slug: 852, text: 853 }, { depth: 347, slug: 923, text: 193 }, { depth: 29, slug: 1026, text: 1027 }, "build-pipeline", "Build Pipeline 🔧", { depth: 29, slug: 899, text: 900 }, { depth: 29, slug: 750, text: 751 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 1011, section: 582, order: 1012, description: 1013 }, [], "deep-dive-rest", { id: 1035, data: 1037, body: 1041, filePath: 1042, digest: 1043, rendered: 1044 }, { title: 1038, section: 582, order: 1039, description: 1040 }, "Rest - Deep Dive", 40, "OXC-based TypeScript compiler with parallel execution and VSCode compatibility", "# Rest - Deep Dive\n\nRest is a Rust binary that uses the OXC toolchain to compile TypeScript 2-3x\nfaster than esbuild while producing output compatible with VSCode's build\nprocess.\n\n## Architecture 🏗️\n\nCompilation pipeline: source files pass through OXC parser, transformer, and\ncode generator in sequence. Optional parallel mode processes multiple files\nconcurrently.\n\n### Modules\n\n| Path                                                                                     |\n| ---------------------------------------------------------------------------------------- |\n| `Source/Library.rs` - Binary entry point, CLI parsing, compiler dispatch                 |\n| `Source/Fn/OXC/Compiler.rs` - Main orchestration: parser, transformer, codegen           |\n| `Source/Fn/OXC/Parser.rs` - Wraps `oxc_parser` with error normalization                  |\n| `Source/Fn/OXC/Transformer.rs` - TypeScript-to-JavaScript AST transformations            |\n| `Source/Fn/OXC/Codegen.rs` - JavaScript generation from transformed AST                  |\n| `Source/Fn/Build.rs` - Directory-based compilation preserving structure                  |\n| `Source/Fn/Bundle/` - Bundling utilities                                                 |\n| `Source/Fn/NLS/` - Natural Language Support string extraction                            |\n| `Source/Fn/SWC/` - SWC integration shims (alternative transformer path)                  |\n| `Source/Fn/Worker/` - Worker thread support for parallel compilation                     |\n| `Source/Struct/CompilerConfig.rs` - Configuration: decorators, class fields, source maps |\n\n## Compilation Flow 🔄\n\n```\nCLI / RestPlugin -> Compiler\n  -> Parser (OXC Parser) -> AST + diagnostics\n  -> Transformer -> Transformed AST\n  -> Codegen -> JavaScript text\n  -> Write .js file to output directory\n```\n\nTransformer applies:\n\n- `emitDecoratorMetadata` (VSCode compatibility)\n- `useDefineForClassFields=false` (matches VSCode gulp build)\n- TypeScript type stripping\n\nWhen `--Parallel` is specified, file compilation fans out across Tokio worker\nthreads.\n\n## VSCode Compatibility Settings ⚙️\n\n| Option             | Value            | Description                              |\n| ------------------ | ---------------- | ---------------------------------------- |\n| Decorator metadata | `true`           | Required for VSCode dependency injection |\n| Class fields mode  | `false`          | Matches VSCode's gulp build behavior     |\n| Source maps        | development only | Inline maps for debug builds             |\n\n## Configuration ⚙️\n\n| CLI Flag     | Default  | Description                         |\n| ------------ | -------- | ----------------------------------- |\n| `--Input`    | required | Directory or file to compile        |\n| `--Output`   | required | Destination for compiled JavaScript |\n| `--Parallel` | off      | Multi-core parallel compilation     |\n\n## Integration 🔗\n\n| Element | Direction | Mechanism                                     |\n| ------- | --------- | --------------------------------------------- |\n| Output  | Consumer  | Process invocation / esbuild plugin           |\n| Cocoon  | Indirect  | Loads JS from `Target/Microsoft/VSCode/`      |\n| Sky     | Indirect  | Loads VSCode UI components via Output package |\n\n## Related Documentation 📖\n\n- [Rest overview](https://Editor.Land/Doc/rest)\n- [Output build orchestration](https://Editor.Land/Doc/deep-dive-output)", "Source/Content/doc/deep-dive-rest.md", "c85df350d930de2b", { html: 1045, metadata: 1046 }, '<h1 id="rest---deep-dive">Rest - Deep Dive</h1>\n<p>Rest is a Rust binary that uses the OXC toolchain to compile TypeScript 2-3x\nfaster than esbuild while producing output compatible with VSCode’s build\nprocess.</p>\n<h2 id="architecture️">Architecture 🏗️</h2>\n<p>Compilation pipeline: source files pass through OXC parser, transformer, and\ncode generator in sequence. Optional parallel mode processes multiple files\nconcurrently.</p>\n<h3 id="modules">Modules</h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Path</th></tr></thead><tbody><tr><td><code>Source/Library.rs</code> - Binary entry point, CLI parsing, compiler dispatch</td></tr><tr><td><code>Source/Fn/OXC/Compiler.rs</code> - Main orchestration: parser, transformer, codegen</td></tr><tr><td><code>Source/Fn/OXC/Parser.rs</code> - Wraps <code>oxc_parser</code> with error normalization</td></tr><tr><td><code>Source/Fn/OXC/Transformer.rs</code> - TypeScript-to-JavaScript AST transformations</td></tr><tr><td><code>Source/Fn/OXC/Codegen.rs</code> - JavaScript generation from transformed AST</td></tr><tr><td><code>Source/Fn/Build.rs</code> - Directory-based compilation preserving structure</td></tr><tr><td><code>Source/Fn/Bundle/</code> - Bundling utilities</td></tr><tr><td><code>Source/Fn/NLS/</code> - Natural Language Support string extraction</td></tr><tr><td><code>Source/Fn/SWC/</code> - SWC integration shims (alternative transformer path)</td></tr><tr><td><code>Source/Fn/Worker/</code> - Worker thread support for parallel compilation</td></tr><tr><td><code>Source/Struct/CompilerConfig.rs</code> - Configuration: decorators, class fields, source maps</td></tr></tbody></table>\n<h2 id="compilation-flow">Compilation Flow 🔄</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>CLI / RestPlugin -> Compiler</span></span>\n<span class="line"><span>  -> Parser (OXC Parser) -> AST + diagnostics</span></span>\n<span class="line"><span>  -> Transformer -> Transformed AST</span></span>\n<span class="line"><span>  -> Codegen -> JavaScript text</span></span>\n<span class="line"><span>  -> Write .js file to output directory</span></span></code></pre>\n<p>Transformer applies:</p>\n<ul>\n<li><code>emitDecoratorMetadata</code> (VSCode compatibility)</li>\n<li><code>useDefineForClassFields=false</code> (matches VSCode gulp build)</li>\n<li>TypeScript type stripping</li>\n</ul>\n<p>When <code>--Parallel</code> is specified, file compilation fans out across Tokio worker\nthreads.</p>\n<h2 id="vscode-compatibility-settings️">VSCode Compatibility Settings ⚙️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Option</th><th>Value</th><th>Description</th></tr></thead><tbody><tr><td>Decorator metadata</td><td><code>true</code></td><td>Required for VSCode dependency injection</td></tr><tr><td>Class fields mode</td><td><code>false</code></td><td>Matches VSCode’s gulp build behavior</td></tr><tr><td>Source maps</td><td>development only</td><td>Inline maps for debug builds</td></tr></tbody></table>\n<h2 id="configuration️">Configuration ⚙️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>CLI Flag</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>--Input</code></td><td>required</td><td>Directory or file to compile</td></tr><tr><td><code>--Output</code></td><td>required</td><td>Destination for compiled JavaScript</td></tr><tr><td><code>--Parallel</code></td><td>off</td><td>Multi-core parallel compilation</td></tr></tbody></table>\n<h2 id="integration">Integration 🔗</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Element</th><th>Direction</th><th>Mechanism</th></tr></thead><tbody><tr><td>Output</td><td>Consumer</td><td>Process invocation / esbuild plugin</td></tr><tr><td>Cocoon</td><td>Indirect</td><td>Loads JS from <code>Target/Microsoft/VSCode/</code></td></tr><tr><td>Sky</td><td>Indirect</td><td>Loads VSCode UI components via Output package</td></tr></tbody></table>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/rest">Rest overview</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-output">Output build orchestration</a></li>\n</ul>', { headings: 1047, localImagePaths: 1061, remoteImagePaths: 1062, frontmatter: 1063, imagePaths: 1064 }, [1048, 1050, 1051, 1052, 1055, 1058, 1059, 1060], { depth: 25, slug: 1049, text: 1038 }, "rest---deep-dive", { depth: 29, slug: 852, text: 853 }, { depth: 347, slug: 923, text: 193 }, { depth: 29, slug: 1053, text: 1054 }, "compilation-flow", "Compilation Flow 🔄", { depth: 29, slug: 1056, text: 1057 }, "vscode-compatibility-settings️", "VSCode Compatibility Settings ⚙️", { depth: 29, slug: 750, text: 751 }, { depth: 29, slug: 899, text: 900 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 1038, section: 582, order: 1039, description: 1040 }, [], "deep-dive-sidecar", { id: 1065, data: 1067, body: 1071, filePath: 1072, digest: 1073, rendered: 1074 }, { title: 1068, section: 582, order: 1069, description: 1070 }, "SideCar - Deep Dive", 43, "Binary distribution layer for vendored runtimes and target-triple management", "# SideCar - Deep Dive\n\nSideCar manages pre-compiled, platform-specific runtime binaries (primarily\nNode.js) so the Land editor can bundle vendored runtimes without requiring users\nto install them separately.\n\n## Architecture 🏗️\n\nTwo binaries: a download tool that fetches official distributions and organizes\nthem by target triple, and a spawn helper used by Mountain to launch sidecars\nfrom the vendored store.\n\n### Modules\n\n| Path                                                                                                         |\n| ------------------------------------------------------------------------------------------------------------ |\n| `Source/Download.rs` - Download binary: fetches distributions, resolves versions, organizes by target triple |\n| `Source/Spawn.rs` - Spawn helper: launches a sidecar binary from the vendored store                          |\n| `Source/Library.rs` - Shared utilities: version resolution, path helpers, cache types                        |\n| `Cache.json` - Tracks downloaded versions per target triple                                                  |\n| `.gitattributes` - Auto-updated by download tool for Git LFS tracking                                        |\n\n## Directory Structure 📂\n\n```\n[target-triple]/\n  [SIDECAR_NAME]/\n    [version]/\n      bin/\n        node\n```\n\nExample: `aarch64-apple-darwin/NODE/22/bin/node`\n\n## Download Flow ⬇️\n\n1. Developer runs `./Target/release/Download`\n2. Tool reads `Cache.json` for existing versions\n3. Fetches version manifest from nodejs.org\n4. Downloads binary archive for latest patch version\n5. Extracts to target-triple directory\n6. Updates `Cache.json` and `.gitattributes` for Git LFS\n\n## Build-Time Selection 🎯\n\nMountain's `build.rs` reads the SideCar directory, selects the binary matching\nthe current Tauri target triple, and copies it into the Tauri sidecar resource\npath for bundling.\n\n## Configuration ⚙️\n\n| Parameter        | Convention                                                 |\n| ---------------- | ---------------------------------------------------------- |\n| Directory layout | `[target-triple]/[NAME]/[version]/bin/`                    |\n| Target triples   | All Tauri-supported platform identifiers                   |\n| Node.js version  | `24` (default, controlled by `--node-version`)             |\n| Cache file       | `Cache.json` mapping triple/name/major to resolved version |\n| Git LFS          | Auto-updated `.gitattributes` for all binary files         |\n\nThe SideCar directory is not committed in its populated form. Run Download\nduring initial setup or CI release pipeline.\n\n## Integration 🔗\n\n| Element  | Direction          | Mechanism                                     |\n| -------- | ------------------ | --------------------------------------------- |\n| Mountain | Consumer           | `build.rs` file copy by target triple         |\n| Tauri    | Consumer           | Sidecar resource bundling in installer        |\n| Cocoon   | Runtime dependency | Mountain spawns Cocoon using vendored Node.js |\n| Air      | Potential consumer | Same target-triple convention                 |\n\n## Related Documentation 📖\n\n- [SideCar overview](https://Editor.Land/Doc/sidecar)", "Source/Content/doc/deep-dive-sidecar.md", "5e1c030f8b29f826", { html: 1075, metadata: 1076 }, '<h1 id="sidecar---deep-dive">SideCar - Deep Dive</h1>\n<p>SideCar manages pre-compiled, platform-specific runtime binaries (primarily\nNode.js) so the Land editor can bundle vendored runtimes without requiring users\nto install them separately.</p>\n<h2 id="architecture️">Architecture 🏗️</h2>\n<p>Two binaries: a download tool that fetches official distributions and organizes\nthem by target triple, and a spawn helper used by Mountain to launch sidecars\nfrom the vendored store.</p>\n<h3 id="modules">Modules</h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Path</th></tr></thead><tbody><tr><td><code>Source/Download.rs</code> - Download binary: fetches distributions, resolves versions, organizes by target triple</td></tr><tr><td><code>Source/Spawn.rs</code> - Spawn helper: launches a sidecar binary from the vendored store</td></tr><tr><td><code>Source/Library.rs</code> - Shared utilities: version resolution, path helpers, cache types</td></tr><tr><td><code>Cache.json</code> - Tracks downloaded versions per target triple</td></tr><tr><td><code>.gitattributes</code> - Auto-updated by download tool for Git LFS tracking</td></tr></tbody></table>\n<h2 id="directory-structure">Directory Structure 📂</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>[target-triple]/</span></span>\n<span class="line"><span>  [SIDECAR_NAME]/</span></span>\n<span class="line"><span>    [version]/</span></span>\n<span class="line"><span>      bin/</span></span>\n<span class="line"><span>        node</span></span></code></pre>\n<p>Example: <code>aarch64-apple-darwin/NODE/22/bin/node</code></p>\n<h2 id="download-flow️">Download Flow ⬇️</h2>\n<ol>\n<li>Developer runs <code>./Target/release/Download</code></li>\n<li>Tool reads <code>Cache.json</code> for existing versions</li>\n<li>Fetches version manifest from nodejs.org</li>\n<li>Downloads binary archive for latest patch version</li>\n<li>Extracts to target-triple directory</li>\n<li>Updates <code>Cache.json</code> and <code>.gitattributes</code> for Git LFS</li>\n</ol>\n<h2 id="build-time-selection">Build-Time Selection 🎯</h2>\n<p>Mountain’s <code>build.rs</code> reads the SideCar directory, selects the binary matching\nthe current Tauri target triple, and copies it into the Tauri sidecar resource\npath for bundling.</p>\n<h2 id="configuration️">Configuration ⚙️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Parameter</th><th>Convention</th></tr></thead><tbody><tr><td>Directory layout</td><td><code>[target-triple]/[NAME]/[version]/bin/</code></td></tr><tr><td>Target triples</td><td>All Tauri-supported platform identifiers</td></tr><tr><td>Node.js version</td><td><code>24</code> (default, controlled by <code>--node-version</code>)</td></tr><tr><td>Cache file</td><td><code>Cache.json</code> mapping triple/name/major to resolved version</td></tr><tr><td>Git LFS</td><td>Auto-updated <code>.gitattributes</code> for all binary files</td></tr></tbody></table>\n<p>The SideCar directory is not committed in its populated form. Run Download\nduring initial setup or CI release pipeline.</p>\n<h2 id="integration">Integration 🔗</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Element</th><th>Direction</th><th>Mechanism</th></tr></thead><tbody><tr><td>Mountain</td><td>Consumer</td><td><code>build.rs</code> file copy by target triple</td></tr><tr><td>Tauri</td><td>Consumer</td><td>Sidecar resource bundling in installer</td></tr><tr><td>Cocoon</td><td>Runtime dependency</td><td>Mountain spawns Cocoon using vendored Node.js</td></tr><tr><td>Air</td><td>Potential consumer</td><td>Same target-triple convention</td></tr></tbody></table>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/sidecar">SideCar overview</a></li>\n</ul>', { headings: 1077, localImagePaths: 1094, remoteImagePaths: 1095, frontmatter: 1096, imagePaths: 1097 }, [1078, 1080, 1081, 1082, 1085, 1088, 1091, 1092, 1093], { depth: 25, slug: 1079, text: 1068 }, "sidecar---deep-dive", { depth: 29, slug: 852, text: 853 }, { depth: 347, slug: 923, text: 193 }, { depth: 29, slug: 1083, text: 1084 }, "directory-structure", "Directory Structure 📂", { depth: 29, slug: 1086, text: 1087 }, "download-flow️", "Download Flow ⬇️", { depth: 29, slug: 1089, text: 1090 }, "build-time-selection", "Build-Time Selection 🎯", { depth: 29, slug: 750, text: 751 }, { depth: 29, slug: 899, text: 900 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 1068, section: 582, order: 1069, description: 1070 }, [], "deep-dive-sky", { id: 1098, data: 1100, body: 1104, filePath: 1105, digest: 1106, rendered: 1107 }, { title: 1101, section: 582, order: 1102, description: 1103 }, "Sky - Deep Dive", 33, "Technical architecture of the Sky UI component layer: Astro-based page routes, workbench variants, and integration with Wind and Mountain through Tauri.", "# Sky - Deep Dive\n\nSky renders the complete editor interface inside the Tauri webview using Astro.\nIt consumes state and services from the Wind service layer and communicates with\nthe Mountain Rust backend through Tauri IPC.\n\n## Architecture 🏗️\n\nSky is organized into three tiers:\n\n```text\nPages (Tauri webview entry points)\n  |\n  +-- Workbench Components (VSCode-compatible editor layout)\n        |\n        +-- Workbench Implementations (Browser/Mountain/Electron variants)\n        +-- Function utilities (Debug, Shared, Meta, Markup)\n```\n\n## Key Modules 📁\n\n| Path                                             | Description                                                              |\n| ------------------------------------------------ | ------------------------------------------------------------------------ |\n| `Source/pages/index.astro`                       | Default entry point; selects workbench variant via environment variables |\n| `Source/pages/Mountain.astro`                    | A2 workbench page - recommended production entry point                   |\n| `Source/pages/Browser.astro`                     | A1 browser-only workbench                                                |\n| `Source/pages/BrowserProxy.astro`                | A1 workbench with service proxy                                          |\n| `Source/pages/Electron.astro`                    | A3 workbench with Electron polyfills                                     |\n| `Source/pages/Isolation.astro`                   | Isolated mode for extension sandboxing                                   |\n| `Source/Workbench/Mountain.astro`                | A2 workbench - loads VSCode UI with Mountain providers                   |\n| `Source/Workbench/Browser.astro`                 | A1 pure browser workbench                                                |\n| `Source/Workbench/BrowserProxy/Bootstrap.ts`     | Effect-TS runtime and services initialization                            |\n| `Source/Workbench/BrowserProxy/ServicesProxy.ts` | Service proxy implementation                                             |\n| `Source/Workbench/Electron/Polyfills.ts`         | Electron compatibility shims                                             |\n| `Source/Workbench/NLS.astro`                     | Natural language support                                                 |\n| `Source/Function/Debug.ts`                       | Build-time debug utilities                                               |\n| `Source/Function/Shared.ts`                      | Shared runtime utilities                                                 |\n| `astro.config.ts`                                | Astro build config, Vite aliases, output to `Target/`                    |\n\n## Startup Sequence 🚀\n\n1. Tauri loads the webview pointing at Sky's built output.\n2. The page route reads environment variables and selects a workbench variant.\n3. Wind's `Preload.ts` shims `window.vscode` globals before VSCode code runs.\n4. Wind bootstraps the Effect-TS service layer and establishes Tauri IPC.\n5. Sky components subscribe to Wind services for live state updates.\n6. Sky listens for Tauri events from Mountain (`sky://terminal/data`,\n   `sky://scm/update-group`, `sky://configuration/changed`).\n\n## Data Flow 🔄\n\nUser interaction triggers a Sky component, which calls a Wind service method.\nWind invokes a Tauri command, Mountain's Rust handler processes it and returns.\nThe result resolves through Wind back to Sky, which re-renders.\n\n## Workbench Variants 🖥️\n\n| Variant         | Description                          | Status      |\n| --------------- | ------------------------------------ | ----------- |\n| A1 Browser      | Pure browser workbench               | Available   |\n| A1 BrowserProxy | Browser workbench with service proxy | Available   |\n| A2 Mountain     | VSCode UI with Mountain providers    | Recommended |\n| A3 Electron     | Workbench with Electron polyfills    | Available   |\n| Isolation       | Extension sandboxing mode            | Available   |\n\nWhen no variant flag is set, `index.astro` loads `Workbench/Default.astro`. The\nrecommended deployment sets `Mountain=true`.\n\n## Integration Points 🔗\n\n| Connecting Element | Direction     | Mechanism          | Description                                         |\n| ------------------ | ------------- | ------------------ | --------------------------------------------------- |\n| Wind               | Inbound       | Direct import      | Consumes Wind Effect-TS services for business logic |\n| Mountain           | Bidirectional | Tauri IPC + Events | Commands via Tauri invoke; updates as Tauri events  |\n| Output             | Inbound       | Static bundle      | VSCode core UI from `@codeeditorland/output`        |\n| Worker             | Inbound       | Web Worker API     | Background processing from `@codeeditorland/worker` |\n\n## Related Documentation 📖\n\n- [Sky overview](https://Editor.Land/Doc/sky)\n- [Architecture overview](https://Editor.Land/Doc/architecture)\n- [Mountain Rust backend](https://Editor.Land/Doc/deep-dive-mountain)", "Source/Content/doc/deep-dive-sky.md", "9620d904a65ab656", { html: 1108, metadata: 1109 }, '<h1 id="sky---deep-dive">Sky - Deep Dive</h1>\n<p>Sky renders the complete editor interface inside the Tauri webview using Astro.\nIt consumes state and services from the Wind service layer and communicates with\nthe Mountain Rust backend through Tauri IPC.</p>\n<h2 id="architecture️">Architecture 🏗️</h2>\n<p>Sky is organized into three tiers:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="text"><code><span class="line"><span>Pages (Tauri webview entry points)</span></span>\n<span class="line"><span>  |</span></span>\n<span class="line"><span>  +-- Workbench Components (VSCode-compatible editor layout)</span></span>\n<span class="line"><span>        |</span></span>\n<span class="line"><span>        +-- Workbench Implementations (Browser/Mountain/Electron variants)</span></span>\n<span class="line"><span>        +-- Function utilities (Debug, Shared, Meta, Markup)</span></span></code></pre>\n<h2 id="key-modules">Key Modules 📁</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Path</th><th>Description</th></tr></thead><tbody><tr><td><code>Source/pages/index.astro</code></td><td>Default entry point; selects workbench variant via environment variables</td></tr><tr><td><code>Source/pages/Mountain.astro</code></td><td>A2 workbench page - recommended production entry point</td></tr><tr><td><code>Source/pages/Browser.astro</code></td><td>A1 browser-only workbench</td></tr><tr><td><code>Source/pages/BrowserProxy.astro</code></td><td>A1 workbench with service proxy</td></tr><tr><td><code>Source/pages/Electron.astro</code></td><td>A3 workbench with Electron polyfills</td></tr><tr><td><code>Source/pages/Isolation.astro</code></td><td>Isolated mode for extension sandboxing</td></tr><tr><td><code>Source/Workbench/Mountain.astro</code></td><td>A2 workbench - loads VSCode UI with Mountain providers</td></tr><tr><td><code>Source/Workbench/Browser.astro</code></td><td>A1 pure browser workbench</td></tr><tr><td><code>Source/Workbench/BrowserProxy/Bootstrap.ts</code></td><td>Effect-TS runtime and services initialization</td></tr><tr><td><code>Source/Workbench/BrowserProxy/ServicesProxy.ts</code></td><td>Service proxy implementation</td></tr><tr><td><code>Source/Workbench/Electron/Polyfills.ts</code></td><td>Electron compatibility shims</td></tr><tr><td><code>Source/Workbench/NLS.astro</code></td><td>Natural language support</td></tr><tr><td><code>Source/Function/Debug.ts</code></td><td>Build-time debug utilities</td></tr><tr><td><code>Source/Function/Shared.ts</code></td><td>Shared runtime utilities</td></tr><tr><td><code>astro.config.ts</code></td><td>Astro build config, Vite aliases, output to <code>Target/</code></td></tr></tbody></table>\n<h2 id="startup-sequence">Startup Sequence 🚀</h2>\n<ol>\n<li>Tauri loads the webview pointing at Sky’s built output.</li>\n<li>The page route reads environment variables and selects a workbench variant.</li>\n<li>Wind’s <code>Preload.ts</code> shims <code>window.vscode</code> globals before VSCode code runs.</li>\n<li>Wind bootstraps the Effect-TS service layer and establishes Tauri IPC.</li>\n<li>Sky components subscribe to Wind services for live state updates.</li>\n<li>Sky listens for Tauri events from Mountain (<code>sky://terminal/data</code>,\n<code>sky://scm/update-group</code>, <code>sky://configuration/changed</code>).</li>\n</ol>\n<h2 id="data-flow">Data Flow 🔄</h2>\n<p>User interaction triggers a Sky component, which calls a Wind service method.\nWind invokes a Tauri command, Mountain’s Rust handler processes it and returns.\nThe result resolves through Wind back to Sky, which re-renders.</p>\n<h2 id="workbench-variants️">Workbench Variants 🖥️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Variant</th><th>Description</th><th>Status</th></tr></thead><tbody><tr><td>A1 Browser</td><td>Pure browser workbench</td><td>Available</td></tr><tr><td>A1 BrowserProxy</td><td>Browser workbench with service proxy</td><td>Available</td></tr><tr><td>A2 Mountain</td><td>VSCode UI with Mountain providers</td><td>Recommended</td></tr><tr><td>A3 Electron</td><td>Workbench with Electron polyfills</td><td>Available</td></tr><tr><td>Isolation</td><td>Extension sandboxing mode</td><td>Available</td></tr></tbody></table>\n<p>When no variant flag is set, <code>index.astro</code> loads <code>Workbench/Default.astro</code>. The\nrecommended deployment sets <code>Mountain=true</code>.</p>\n<h2 id="integration-points">Integration Points 🔗</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Connecting Element</th><th>Direction</th><th>Mechanism</th><th>Description</th></tr></thead><tbody><tr><td>Wind</td><td>Inbound</td><td>Direct import</td><td>Consumes Wind Effect-TS services for business logic</td></tr><tr><td>Mountain</td><td>Bidirectional</td><td>Tauri IPC + Events</td><td>Commands via Tauri invoke; updates as Tauri events</td></tr><tr><td>Output</td><td>Inbound</td><td>Static bundle</td><td>VSCode core UI from <code>@codeeditorland/output</code></td></tr><tr><td>Worker</td><td>Inbound</td><td>Web Worker API</td><td>Background processing from <code>@codeeditorland/worker</code></td></tr></tbody></table>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/sky">Sky overview</a></li>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture overview</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-mountain">Mountain Rust backend</a></li>\n</ul>', { headings: 1110, localImagePaths: 1124, remoteImagePaths: 1125, frontmatter: 1126, imagePaths: 1127 }, [1111, 1113, 1114, 1116, 1118, 1119, 1122, 1123], { depth: 25, slug: 1112, text: 1101 }, "sky---deep-dive", { depth: 29, slug: 852, text: 853 }, { depth: 29, slug: 741, text: 1115 }, "Key Modules 📁", { depth: 29, slug: 957, text: 1117 }, "Startup Sequence 🚀", { depth: 29, slug: 744, text: 745 }, { depth: 29, slug: 1120, text: 1121 }, "workbench-variants️", "Workbench Variants 🖥️", { depth: 29, slug: 747, text: 748 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 1101, section: 582, order: 1102, description: 1103 }, [], "deep-dive-vine", { id: 1128, data: 1130, body: 1134, filePath: 1135, digest: 1136, rendered: 1137 }, { title: 1131, section: 582, order: 1132, description: 1133 }, "Vine - Deep Dive", 39, "gRPC protocol definitions, message types, and inter-process communication contracts", "# Vine - Deep Dive\n\nVine is the contract-first gRPC protocol layer defining strongly-typed\ninter-process communication contracts between Mountain, Cocoon, Grove, and Air.\n\n## Architecture 🏗️\n\nThe proto files are the source of truth. Generated Rust code from tonic/prost is\nused by Mountain for the server implementation and by Cocoon/Grove for client\nstubs.\n\n### Protocol Files\n\n| Proto File  | Purpose                                                       |\n| ----------- | ------------------------------------------------------------- |\n| Vine.proto  | Core protocol: Mountain to Cocoon commands, events, handshake |\n| Spine.proto | Extension host coordination: action/response pattern          |\n| Grove.proto | Grove-specific: WASM host function calls, extension lifecycle |\n| Air.proto   | Air daemon: updates, downloads, health checks                 |\n\n### Modules\n\n| Path                                                     |\n| -------------------------------------------------------- | ----------------------------------------------- |\n| Source/lib.rs - Library root, re-exports generated types |\n| Source/Message/                                          | Structured message types shared across services |\n| Source/Service/                                          | gRPC service trait implementations              |\n| Source/Client/                                           | Protocol client helpers for consumer crates     |\n\n## Communication Patterns 📡\n\n| Pattern                 | Use Case                                                |\n| ----------------------- | ------------------------------------------------------- |\n| Unary RPC               | Commands and queries                                    |\n| Server streaming        | Mountain streams terminal output, diagnostics to Cocoon |\n| Client streaming        | Cocoon sends batched registration at startup            |\n| Bidirectional streaming | Spine protocol real-time extension host coordination    |\n\n## Ports and Transport 🌐\n\n| Service     | Port     | Transport                                         |\n| ----------- | -------- | ------------------------------------------------- |\n| Vine/Cocoon | 50052    | TCP loopback                                      |\n| Air daemon  | 50053    | TCP loopback                                      |\n| TLS         | Disabled | Loopback only, Mist DNS provides network boundary |\n\n## Client Implementations 📱\n\n| Element    | Implementation                                  |\n| ---------- | ----------------------------------------------- |\n| Cocoon     | @grpc/grpc-js Node.js client                    |\n| Grove      | tonic Rust client                               |\n| Air daemon | tonic gRPC server (Mountain connects as client) |\n\n## Request Flow Example 📋\n\n```\nWind UI -> Mountain Tauri invoke (executeCommand)\n  -> Mountain serializes to CommandRequest protobuf\n  -> gRPC unary call to Cocoon\n  -> Cocoon returns CommandResponse protobuf\n  -> Mountain deserializes, sends Tauri event with result\n```\n\nProto files compiled at build time by prost-build in Mountain build script.\n\n## Related Documentation 📖\n\n- [Vine overview](https://Editor.Land/Doc/vine)\n- [Cocoon gRPC client](https://Editor.Land/Doc/deep-dive-cocoon)\n- [Mountain gRPC server](https://Editor.Land/Doc/deep-dive-mountain)", "Source/Content/doc/deep-dive-vine.md", "77b6aa1d5d41046a", { html: 1138, metadata: 1139 }, '<h1 id="vine---deep-dive">Vine - Deep Dive</h1>\n<p>Vine is the contract-first gRPC protocol layer defining strongly-typed\ninter-process communication contracts between Mountain, Cocoon, Grove, and Air.</p>\n<h2 id="architecture️">Architecture 🏗️</h2>\n<p>The proto files are the source of truth. Generated Rust code from tonic/prost is\nused by Mountain for the server implementation and by Cocoon/Grove for client\nstubs.</p>\n<h3 id="protocol-files">Protocol Files</h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Proto File</th><th>Purpose</th></tr></thead><tbody><tr><td>Vine.proto</td><td>Core protocol: Mountain to Cocoon commands, events, handshake</td></tr><tr><td>Spine.proto</td><td>Extension host coordination: action/response pattern</td></tr><tr><td>Grove.proto</td><td>Grove-specific: WASM host function calls, extension lifecycle</td></tr><tr><td>Air.proto</td><td>Air daemon: updates, downloads, health checks</td></tr></tbody></table>\n<h3 id="modules">Modules</h3>\n<p>| Path                                                     |\n| -------------------------------------------------------- | ----------------------------------------------- |\n| Source/lib.rs - Library root, re-exports generated types |\n| Source/Message/                                          | Structured message types shared across services |\n| Source/Service/                                          | gRPC service trait implementations              |\n| Source/Client/                                           | Protocol client helpers for consumer crates     |</p>\n<h2 id="communication-patterns">Communication Patterns 📡</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Pattern</th><th>Use Case</th></tr></thead><tbody><tr><td>Unary RPC</td><td>Commands and queries</td></tr><tr><td>Server streaming</td><td>Mountain streams terminal output, diagnostics to Cocoon</td></tr><tr><td>Client streaming</td><td>Cocoon sends batched registration at startup</td></tr><tr><td>Bidirectional streaming</td><td>Spine protocol real-time extension host coordination</td></tr></tbody></table>\n<h2 id="ports-and-transport">Ports and Transport 🌐</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Service</th><th>Port</th><th>Transport</th></tr></thead><tbody><tr><td>Vine/Cocoon</td><td>50052</td><td>TCP loopback</td></tr><tr><td>Air daemon</td><td>50053</td><td>TCP loopback</td></tr><tr><td>TLS</td><td>Disabled</td><td>Loopback only, Mist DNS provides network boundary</td></tr></tbody></table>\n<h2 id="client-implementations">Client Implementations 📱</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Element</th><th>Implementation</th></tr></thead><tbody><tr><td>Cocoon</td><td>@grpc/grpc-js Node.js client</td></tr><tr><td>Grove</td><td>tonic Rust client</td></tr><tr><td>Air daemon</td><td>tonic gRPC server (Mountain connects as client)</td></tr></tbody></table>\n<h2 id="request-flow-example">Request Flow Example 📋</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>Wind UI -> Mountain Tauri invoke (executeCommand)</span></span>\n<span class="line"><span>  -> Mountain serializes to CommandRequest protobuf</span></span>\n<span class="line"><span>  -> gRPC unary call to Cocoon</span></span>\n<span class="line"><span>  -> Cocoon returns CommandResponse protobuf</span></span>\n<span class="line"><span>  -> Mountain deserializes, sends Tauri event with result</span></span></code></pre>\n<p>Proto files compiled at build time by prost-build in Mountain build script.</p>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/vine">Vine overview</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-cocoon">Cocoon gRPC client</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-mountain">Mountain gRPC server</a></li>\n</ul>', { headings: 1140, localImagePaths: 1161, remoteImagePaths: 1162, frontmatter: 1163, imagePaths: 1164 }, [1141, 1143, 1144, 1147, 1148, 1151, 1154, 1157, 1160], { depth: 25, slug: 1142, text: 1131 }, "vine---deep-dive", { depth: 29, slug: 852, text: 853 }, { depth: 347, slug: 1145, text: 1146 }, "protocol-files", "Protocol Files", { depth: 347, slug: 923, text: 193 }, { depth: 29, slug: 1149, text: 1150 }, "communication-patterns", "Communication Patterns 📡", { depth: 29, slug: 1152, text: 1153 }, "ports-and-transport", "Ports and Transport 🌐", { depth: 29, slug: 1155, text: 1156 }, "client-implementations", "Client Implementations 📱", { depth: 29, slug: 1158, text: 1159 }, "request-flow-example", "Request Flow Example 📋", { depth: 29, slug: 401, text: 402 }, [], [], { title: 1131, section: 582, order: 1132, description: 1133 }, [], "deep-dive-wind", { id: 1165, data: 1167, body: 1171, filePath: 1172, digest: 1173, rendered: 1174 }, { title: 1168, section: 582, order: 1169, description: 1170 }, "Wind - Deep Dive", 34, "Technical architecture of the Wind frontend service layer: Effect-TS native services, VSCode environment emulation, and Tauri integration via anti-corruption patterns.", "# Wind - Deep Dive\n\nWind is the frontend service layer in the Land project. It provides VSCode API\ncompatibility through Effect-TS native implementations and a Tauri\nanti-corruption layer, enabling Sky's VSCode-based UI components to communicate\nwith the Mountain Rust backend.\n\n## Core Architecture 🏗️\n\n| Principle                       | Description                                                                        | Key Components                           |\n| ------------------------------- | ---------------------------------------------------------------------------------- | ---------------------------------------- |\n| High-Fidelity Emulation         | VSCode renderer environment emulation so Sky reuses VSCode UI with minimal changes | `Preload.ts`, `Platform/VSCode/*`        |\n| Effect-TS Native                | Entire service layer built with Effect-TS layer composition                        | All `Effect`-based modules               |\n| Anti-Corruption Layer           | Clean abstraction over Tauri APIs, isolating platform specifics                    | `Integration/Tauri/Wrap/*`, `Preload.ts` |\n| Declarative Service Composition | Effect-TS patterns for dependency injection and composable service construction    | `Application/*`, `AppLayer`              |\n| Performance Optimization        | Efficient bundling and API shimming                                                | `Configuration/ESBuild/*`                |\n| Security Hardening              | CSP and secure API boundaries for the Tauri webview                                | `Preload.ts` security patterns           |\n\n## Component Breakdown 🧩\n\n### Preload.ts (Environmental Foundation)\n\nExecutes in the Tauri webview before Sky loads. Reconstructs the `window.vscode`\nglobal object with:\n\n- **ipcRenderer** - Maps to Tauri's event system (`TauriEmit`, `TauriListen`,\n  `TauriInvoke`).\n- **process** - Provides an `ISandboxNodeProcess`-like object with `platform`,\n  `arch`, `env`, and `cwd()`.\n- **Context resolution** - Reads `ISandboxConfiguration` from meta tags injected\n  by Mountain.\n- **Security** - CSP compliance and secure API boundary enforcement.\n\n### Integration/Tauri/ (Anti-Corruption Layer)\n\n- **Effect Factories (`Wrap/*`)** - Converts promise-based Tauri APIs to Effect\n  operations.\n- **Type Converters (`Convert/*`)** - Pure functions for VSCode-to-Tauri type\n  conversion.\n- **Complex Resolvers (`Resolve/*`)** - Multi-step operations with error\n  handling and fallbacks.\n- **Domain-Specific Errors (`Error/*`)** - Precise error classes for\n  `Effect.catchTag` handling.\n\n### Effect/Produce/ (Meta-Factories)\n\n- `FromAsync` - Transforms async functions into Effect-producing functions with\n  proper error typing.\n- `OptionalFromAsync` - Variant for functions returning `null`/`undefined`,\n  wrapping results in `Option<T>`.\n\n### Application/ (Core Frontend Services)\n\n- **Tag-Based Dependency Injection** - Uses `Context.Tag` for clean service\n  management.\n- **Orchestration Patterns** - Multi-step Effects defining core business logic.\n- **Layer Composition** - Builds complete services from individual components.\n- **Error Transformation** - Converts integration-level errors to\n  application-level types.\n\n## VSCode Environment Emulation 🖥️\n\nWind ensures API compatibility through:\n\n1. **Interface Matching** - TypeScript definitions match all VSCode API\n   interfaces.\n2. **Behavior Preservation** - Sophisticated shimming and event mapping.\n3. **Error Handling Compatibility** - Error types match VSCode expectations.\n4. **Asynchronous Semantics** - Proper sequencing and error propagation.\n\n## Service Call Latency ⏱️\n\n| Layer                | Time    |\n| -------------------- | ------- |\n| VSCode API Shim      | ~0.02ms |\n| Effect Orchestration | ~0.05ms |\n| Tauri Integration    | ~0.03ms |\n| **Total overhead**   | ~0.10ms |\n\nNative execution time is variable and OS-dependent. Full-stack calls include\nMountain backend processing and gRPC round-trips.\n\n## Security Architecture 🔒\n\nWind protects the Tauri webview through CSP enforcement, API boundary security,\ninput validation at all boundaries, and error containment that prevents\ninformation leakage.\n\n## Platform Integration 🔗\n\n```text\nSky UI -[calls]-> Wind App Services -[orchestrates]-> Tauri Integration -[invokes]-> Mountain Track\n  |                                                                             |\n  +--[listens via Tauri events]---+                                             |\n                                   +--> Environment Providers --> ApplicationState\n```\n\nWind bridges the Sky UI component layer and the Mountain Rust backend,\ntranslating VSCode-style API calls into typed Tauri commands and returning\nresults as Effect resolutions.\n\n## Related Documentation 📖\n\n- [Wind overview](https://Editor.Land/Doc/wind)\n- [Architecture overview](https://Editor.Land/Doc/architecture)\n- [Sky UI layer](https://Editor.Land/Doc/deep-dive-sky)\n- [Mountain Rust backend](https://Editor.Land/Doc/deep-dive-mountain)", "Source/Content/doc/deep-dive-wind.md", "1223419f3a91da28", { html: 1175, metadata: 1176 }, '<h1 id="wind---deep-dive">Wind - Deep Dive</h1>\n<p>Wind is the frontend service layer in the Land project. It provides VSCode API\ncompatibility through Effect-TS native implementations and a Tauri\nanti-corruption layer, enabling Sky’s VSCode-based UI components to communicate\nwith the Mountain Rust backend.</p>\n<h2 id="core-architecture️">Core Architecture 🏗️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Principle</th><th>Description</th><th>Key Components</th></tr></thead><tbody><tr><td>High-Fidelity Emulation</td><td>VSCode renderer environment emulation so Sky reuses VSCode UI with minimal changes</td><td><code>Preload.ts</code>, <code>Platform/VSCode/*</code></td></tr><tr><td>Effect-TS Native</td><td>Entire service layer built with Effect-TS layer composition</td><td>All <code>Effect</code>-based modules</td></tr><tr><td>Anti-Corruption Layer</td><td>Clean abstraction over Tauri APIs, isolating platform specifics</td><td><code>Integration/Tauri/Wrap/*</code>, <code>Preload.ts</code></td></tr><tr><td>Declarative Service Composition</td><td>Effect-TS patterns for dependency injection and composable service construction</td><td><code>Application/*</code>, <code>AppLayer</code></td></tr><tr><td>Performance Optimization</td><td>Efficient bundling and API shimming</td><td><code>Configuration/ESBuild/*</code></td></tr><tr><td>Security Hardening</td><td>CSP and secure API boundaries for the Tauri webview</td><td><code>Preload.ts</code> security patterns</td></tr></tbody></table>\n<h2 id="component-breakdown">Component Breakdown 🧩</h2>\n<h3 id="preloadts-environmental-foundation">Preload.ts (Environmental Foundation)</h3>\n<p>Executes in the Tauri webview before Sky loads. Reconstructs the <code>window.vscode</code>\nglobal object with:</p>\n<ul>\n<li><strong>ipcRenderer</strong> - Maps to Tauri’s event system (<code>TauriEmit</code>, <code>TauriListen</code>,\n<code>TauriInvoke</code>).</li>\n<li><strong>process</strong> - Provides an <code>ISandboxNodeProcess</code>-like object with <code>platform</code>,\n<code>arch</code>, <code>env</code>, and <code>cwd()</code>.</li>\n<li><strong>Context resolution</strong> - Reads <code>ISandboxConfiguration</code> from meta tags injected\nby Mountain.</li>\n<li><strong>Security</strong> - CSP compliance and secure API boundary enforcement.</li>\n</ul>\n<h3 id="integrationtauri-anti-corruption-layer">Integration/Tauri/ (Anti-Corruption Layer)</h3>\n<ul>\n<li><strong>Effect Factories (<code>Wrap/*</code>)</strong> - Converts promise-based Tauri APIs to Effect\noperations.</li>\n<li><strong>Type Converters (<code>Convert/*</code>)</strong> - Pure functions for VSCode-to-Tauri type\nconversion.</li>\n<li><strong>Complex Resolvers (<code>Resolve/*</code>)</strong> - Multi-step operations with error\nhandling and fallbacks.</li>\n<li><strong>Domain-Specific Errors (<code>Error/*</code>)</strong> - Precise error classes for\n<code>Effect.catchTag</code> handling.</li>\n</ul>\n<h3 id="effectproduce-meta-factories">Effect/Produce/ (Meta-Factories)</h3>\n<ul>\n<li><code>FromAsync</code> - Transforms async functions into Effect-producing functions with\nproper error typing.</li>\n<li><code>OptionalFromAsync</code> - Variant for functions returning <code>null</code>/<code>undefined</code>,\nwrapping results in <code>Option&#x3C;T></code>.</li>\n</ul>\n<h3 id="application-core-frontend-services">Application/ (Core Frontend Services)</h3>\n<ul>\n<li><strong>Tag-Based Dependency Injection</strong> - Uses <code>Context.Tag</code> for clean service\nmanagement.</li>\n<li><strong>Orchestration Patterns</strong> - Multi-step Effects defining core business logic.</li>\n<li><strong>Layer Composition</strong> - Builds complete services from individual components.</li>\n<li><strong>Error Transformation</strong> - Converts integration-level errors to\napplication-level types.</li>\n</ul>\n<h2 id="vscode-environment-emulation️">VSCode Environment Emulation 🖥️</h2>\n<p>Wind ensures API compatibility through:</p>\n<ol>\n<li><strong>Interface Matching</strong> - TypeScript definitions match all VSCode API\ninterfaces.</li>\n<li><strong>Behavior Preservation</strong> - Sophisticated shimming and event mapping.</li>\n<li><strong>Error Handling Compatibility</strong> - Error types match VSCode expectations.</li>\n<li><strong>Asynchronous Semantics</strong> - Proper sequencing and error propagation.</li>\n</ol>\n<h2 id="service-call-latency️">Service Call Latency ⏱️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Layer</th><th>Time</th></tr></thead><tbody><tr><td>VSCode API Shim</td><td>~0.02ms</td></tr><tr><td>Effect Orchestration</td><td>~0.05ms</td></tr><tr><td>Tauri Integration</td><td>~0.03ms</td></tr><tr><td><strong>Total overhead</strong></td><td>~0.10ms</td></tr></tbody></table>\n<p>Native execution time is variable and OS-dependent. Full-stack calls include\nMountain backend processing and gRPC round-trips.</p>\n<h2 id="security-architecture">Security Architecture 🔒</h2>\n<p>Wind protects the Tauri webview through CSP enforcement, API boundary security,\ninput validation at all boundaries, and error containment that prevents\ninformation leakage.</p>\n<h2 id="platform-integration">Platform Integration 🔗</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="text"><code><span class="line"><span>Sky UI -[calls]-> Wind App Services -[orchestrates]-> Tauri Integration -[invokes]-> Mountain Track</span></span>\n<span class="line"><span>  |                                                                             |</span></span>\n<span class="line"><span>  +--[listens via Tauri events]---+                                             |</span></span>\n<span class="line"><span>                                   +--> Environment Providers --> ApplicationState</span></span></code></pre>\n<p>Wind bridges the Sky UI component layer and the Mountain Rust backend,\ntranslating VSCode-style API calls into typed Tauri commands and returning\nresults as Effect resolutions.</p>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/wind">Wind overview</a></li>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture overview</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-sky">Sky UI layer</a></li>\n<li><a href="https://Editor.Land/Doc/deep-dive-mountain">Mountain Rust backend</a></li>\n</ul>', { headings: 1177, localImagePaths: 1207, remoteImagePaths: 1208, frontmatter: 1209, imagePaths: 1210 }, [1178, 1180, 1181, 1182, 1185, 1188, 1191, 1194, 1197, 1200, 1203, 1206], { depth: 25, slug: 1179, text: 1168 }, "wind---deep-dive", { depth: 29, slug: 773, text: 774 }, { depth: 29, slug: 776, text: 777 }, { depth: 347, slug: 1183, text: 1184 }, "preloadts-environmental-foundation", "Preload.ts (Environmental Foundation)", { depth: 347, slug: 1186, text: 1187 }, "integrationtauri-anti-corruption-layer", "Integration/Tauri/ (Anti-Corruption Layer)", { depth: 347, slug: 1189, text: 1190 }, "effectproduce-meta-factories", "Effect/Produce/ (Meta-Factories)", { depth: 347, slug: 1192, text: 1193 }, "application-core-frontend-services", "Application/ (Core Frontend Services)", { depth: 29, slug: 1195, text: 1196 }, "vscode-environment-emulation️", "VSCode Environment Emulation 🖥️", { depth: 29, slug: 1198, text: 1199 }, "service-call-latency️", "Service Call Latency ⏱️", { depth: 29, slug: 1201, text: 1202 }, "security-architecture", "Security Architecture 🔒", { depth: 29, slug: 1204, text: 1205 }, "platform-integration", "Platform Integration 🔗", { depth: 29, slug: 401, text: 402 }, [], [], { title: 1168, section: 582, order: 1169, description: 1170 }, [], "deep-dive-worker", { id: 1211, data: 1213, body: 1217, filePath: 1218, digest: 1219, rendered: 1220 }, { title: 1214, section: 582, order: 1215, description: 1216 }, "Worker - Deep Dive", 42, "Service Worker for caching, dynamic CSS loading, and offline web application support", "# Worker - Deep Dive\n\nWorker provides Service Worker functionality including intelligent caching,\ndynamic CSS loading, and offline capabilities for web applications in the Land\nproject.\n\n## Architecture 🏗️\n\nThree-phase design: Service Worker bootstrap, multi-tier caching, and dynamic\nCSS loading system.\n\n### Core Files\n\n| File               | Purpose                                                                 |\n| ------------------ | ----------------------------------------------------------------------- |\n| Source/Worker.js   | Main Service Worker implementation with fetch event handling            |\n| Source/Load.js     | Client-side CSS loading coordination                                    |\n| Source/Register.js | Service Worker registration, update detection, and lifecycle management |\n\n## Caching Strategy 💾\n\nTwo-tier caching with distinct strategies:\n\n### CACHE_CORE (Network-First)\n\n- **Target**: Application shell files (Application/, Register.js, Load.js)\n- **Strategy**: Network fetch first, fallback to cache on failure\n- **Rationale**: Application changes require latest versions\n\n### CACHE_ASSET (Cache-First)\n\n- **Target**: Static resources (Static/Application/\\)\n- **Strategy**: Serve cached immediately, validate freshness in background\n- **Optimization**: LRU eviction for cache size management\n\nPre-caching at install phase, runtime caching at fetch events.\n\n## Dynamic CSS Loading 🎨\n\nTwo-phase system prevents infinite interception loops:\n\n**Phase 1**: Service Worker intercepts CSS import requests, responds with a\ngenerated JavaScript module that calls `window._LOAD_CSS_WORKER()`.\n\n**Phase 2**: Client creates a `<link>` with `?Skip=Intercept` parameter. Service\nWorker detects the parameter, bypasses JS generation, serves actual CSS.\n\nThe `?Skip=Intercept` parameter creates a state transition that guarantees the\ninterception terminates.\n\n### Performance\n\n| Metric           | Worker         | Without Worker | Improvement         |\n| ---------------- | -------------- | -------------- | ------------------- |\n| CSS Loading Time | ~15-65ms       | ~50-200ms      | ~70% faster         |\n| Cache Hit Ratio  | >95%           | N/A            | Significant         |\n| Bandwidth Usage  | ~60% reduction | Baseline       | Significant savings |\n\n## Version Management 📌\n\n- Content hashing detects meaningful changes beyond file size\n- New Service Workers enter waiting state until clients are ready\n- Register.js orchestrates controlled reload cycles\n- Previous version caches retained as rollback fallback\n\n## Progressive Enhancement 📈\n\n1. Basic functionality without Service Worker\n2. Enhanced performance with caching\n3. Advanced features with dynamic asset transformation\n4. Offline capabilities with update management\n\n## Security 🔒\n\n- Generated JavaScript modules validated for CSP compliance\n- Cryptographic signature verification for critical assets\n- Strict CORS enforcement for intercepted requests\n- No external telemetry, fully local operation\n\n## Related Documentation 📖\n\n- [Worker overview](https://Editor.Land/Doc/worker)", "Source/Content/doc/deep-dive-worker.md", "bd6dce53ccd67cd8", { html: 1221, metadata: 1222 }, '<h1 id="worker---deep-dive">Worker - Deep Dive</h1>\n<p>Worker provides Service Worker functionality including intelligent caching,\ndynamic CSS loading, and offline capabilities for web applications in the Land\nproject.</p>\n<h2 id="architecture️">Architecture 🏗️</h2>\n<p>Three-phase design: Service Worker bootstrap, multi-tier caching, and dynamic\nCSS loading system.</p>\n<h3 id="core-files">Core Files</h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>File</th><th>Purpose</th></tr></thead><tbody><tr><td>Source/Worker.js</td><td>Main Service Worker implementation with fetch event handling</td></tr><tr><td>Source/Load.js</td><td>Client-side CSS loading coordination</td></tr><tr><td>Source/Register.js</td><td>Service Worker registration, update detection, and lifecycle management</td></tr></tbody></table>\n<h2 id="caching-strategy">Caching Strategy 💾</h2>\n<p>Two-tier caching with distinct strategies:</p>\n<h3 id="cache_core-network-first">CACHE_CORE (Network-First)</h3>\n<ul>\n<li><strong>Target</strong>: Application shell files (Application/, Register.js, Load.js)</li>\n<li><strong>Strategy</strong>: Network fetch first, fallback to cache on failure</li>\n<li><strong>Rationale</strong>: Application changes require latest versions</li>\n</ul>\n<h3 id="cache_asset-cache-first">CACHE_ASSET (Cache-First)</h3>\n<ul>\n<li><strong>Target</strong>: Static resources (Static/Application/)</li>\n<li><strong>Strategy</strong>: Serve cached immediately, validate freshness in background</li>\n<li><strong>Optimization</strong>: LRU eviction for cache size management</li>\n</ul>\n<p>Pre-caching at install phase, runtime caching at fetch events.</p>\n<h2 id="dynamic-css-loading">Dynamic CSS Loading 🎨</h2>\n<p>Two-phase system prevents infinite interception loops:</p>\n<p><strong>Phase 1</strong>: Service Worker intercepts CSS import requests, responds with a\ngenerated JavaScript module that calls <code>window._LOAD_CSS_WORKER()</code>.</p>\n<p><strong>Phase 2</strong>: Client creates a <code>&#x3C;link></code> with <code>?Skip=Intercept</code> parameter. Service\nWorker detects the parameter, bypasses JS generation, serves actual CSS.</p>\n<p>The <code>?Skip=Intercept</code> parameter creates a state transition that guarantees the\ninterception terminates.</p>\n<h3 id="performance">Performance</h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Metric</th><th>Worker</th><th>Without Worker</th><th>Improvement</th></tr></thead><tbody><tr><td>CSS Loading Time</td><td>~15-65ms</td><td>~50-200ms</td><td>~70% faster</td></tr><tr><td>Cache Hit Ratio</td><td>>95%</td><td>N/A</td><td>Significant</td></tr><tr><td>Bandwidth Usage</td><td>~60% reduction</td><td>Baseline</td><td>Significant savings</td></tr></tbody></table>\n<h2 id="version-management">Version Management 📌</h2>\n<ul>\n<li>Content hashing detects meaningful changes beyond file size</li>\n<li>New Service Workers enter waiting state until clients are ready</li>\n<li>Register.js orchestrates controlled reload cycles</li>\n<li>Previous version caches retained as rollback fallback</li>\n</ul>\n<h2 id="progressive-enhancement">Progressive Enhancement 📈</h2>\n<ol>\n<li>Basic functionality without Service Worker</li>\n<li>Enhanced performance with caching</li>\n<li>Advanced features with dynamic asset transformation</li>\n<li>Offline capabilities with update management</li>\n</ol>\n<h2 id="security">Security 🔒</h2>\n<ul>\n<li>Generated JavaScript modules validated for CSP compliance</li>\n<li>Cryptographic signature verification for critical assets</li>\n<li>Strict CORS enforcement for intercepted requests</li>\n<li>No external telemetry, fully local operation</li>\n</ul>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/worker">Worker overview</a></li>\n</ul>', { headings: 1223, localImagePaths: 1254, remoteImagePaths: 1255, frontmatter: 1256, imagePaths: 1257 }, [1224, 1226, 1227, 1230, 1233, 1236, 1239, 1242, 1244, 1247, 1250, 1253], { depth: 25, slug: 1225, text: 1214 }, "worker---deep-dive", { depth: 29, slug: 852, text: 853 }, { depth: 347, slug: 1228, text: 1229 }, "core-files", "Core Files", { depth: 29, slug: 1231, text: 1232 }, "caching-strategy", "Caching Strategy 💾", { depth: 347, slug: 1234, text: 1235 }, "cache_core-network-first", "CACHE_CORE (Network-First)", { depth: 347, slug: 1237, text: 1238 }, "cache_asset-cache-first", "CACHE_ASSET (Cache-First)", { depth: 29, slug: 1240, text: 1241 }, "dynamic-css-loading", "Dynamic CSS Loading 🎨", { depth: 347, slug: 861, text: 1243 }, "Performance", { depth: 29, slug: 1245, text: 1246 }, "version-management", "Version Management 📌", { depth: 29, slug: 1248, text: 1249 }, "progressive-enhancement", "Progressive Enhancement 📈", { depth: 29, slug: 1251, text: 1252 }, "security", "Security 🔒", { depth: 29, slug: 401, text: 402 }, [], [], { title: 1214, section: 582, order: 1215, description: 1216 }, [], "dependency-management", { id: 1258, data: 1260, body: 1264, filePath: 1265, digest: 1266, rendered: 1267 }, { title: 1261, section: 493, order: 1262, description: 1263 }, "Dependency Management", 48, "Dependabot configuration, ecosystem coverage, and dependency update strategy across Land elements.", '# Dependency Management\n\nEach Land element configures its own Dependabot. This page documents the shared\npatterns and element-specific variations.\n\n---\n\n## Dependabot Configuration\n\nAll configurated elements use Dependabot v2 with beta ecosystems enabled:\n\n```yaml\nversion: 2\nenable-beta-ecosystems: true\n```\n\nThis single flag allows Dependabot to pick up newer or experimental\npackage-ecosystem values beyond the stable set (npm, cargo, docker,\ngithub-actions).\n\n---\n\n## Ecosystem Coverage\n\n### github-actions (All 13 elements with dependabot.yml)\n\nEvery element monitors GitHub Action versions in its `.github/workflows/`\ndirectory for updates. This keeps CI tooling current without manual tracking.\n\n```yaml\nupdates:\n    - package-ecosystem: "github-actions"\n      directory: "/"\n      schedule:\n          interval: "daily"\n```\n\n### cargo (Rust elements)\n\nApplies to: Mountain, Air, Common, Echo, Grove, Mist, Rest, SideCar, Vine,\nMaintain\n\n```yaml\n- package-ecosystem: "cargo"\n  directory: "/"\n  schedule:\n      interval: "daily"\n  versioning-strategy: lockfile-only\n```\n\nThe `lockfile-only` strategy means Dependabot updates `Cargo.lock` without\ntouching `Cargo.toml` version pins. This ensures:\n\n- Version constraints in `Cargo.toml` remain stable and explicit.\n- Lockfile picks up compatible new releases automatically.\n- No breaking semver bumps slip through the lockfile gate.\n\n### npm (TypeScript/JavaScript elements)\n\nApplies to: Wind, Cocoon, Output, Sky, Worker\n\n```yaml\n- package-ecosystem: "npm"\n  directory: "/"\n  schedule:\n      interval: "daily"\n  versioning-strategy: increase\n  ignore:\n      - dependency-name: "tailwindcss"\n        versions:\n            - "^4.0.0"\n```\n\nThe `increase` strategy means Dependabot bumps the version ranges in\n`package.json` itself, not just the lockfile. This gives each element the latest\ncompatible ranges on every dependency roll.\n\nTailwind 4.x is explicitly ignored across all JS elements due to the breaking\nchanges in the v4 rewrite (CSS-first configuration, different plugin API).\n\n---\n\n## Update Strategy Summary\n\n| Element  | Ecosystems            | Versioning Strategy |\n| -------- | --------------------- | ------------------- |\n| Mountain | github-actions, cargo | lockfile-only       |\n| Air      | github-actions, cargo | lockfile-only       |\n| Common   | github-actions, cargo | lockfile-only       |\n| Echo     | github-actions, cargo | lockfile-only       |\n| Grove    | github-actions, cargo | lockfile-only       |\n| Mist     | github-actions, cargo | lockfile-only       |\n| Rest     | github-actions, cargo | lockfile-only       |\n| SideCar  | github-actions, cargo | lockfile-only       |\n| Vine     | github-actions, cargo | lockfile-only       |\n| Maintain | github-actions, cargo | lockfile-only       |\n| Wind     | github-actions, npm   | increase            |\n| Cocoon   | github-actions, npm   | increase            |\n| Output   | github-actions, npm   | increase            |\n| Sky      | github-actions, npm   | increase            |\n| Worker   | github-actions, npm   | increase            |\n\n---\n\n## Dependency Philosophy\n\n### Rust: Lockfile-Only Stability\n\nRust elements keep their `Cargo.toml` pins stable. Dependabot rolls the\nlockfile, bringing in bugfix and minor releases within the existing constraint.\nMajor version bumps (e.g., Hickory-Server 0.24 to 0.26 in Mist, OXC 0.127 in\nRest) require manual review because they may change public APIs or require code\nmigrations.\n\n### JavaScript: Aggressive Range Updates\n\nJavaScript elements let Dependabot bump package.json ranges directly. This keeps\nthe dependency surface fresh but requires CI to catch incompatibilities. The\nNode.yml matrix (Node 24) and pre-publish checks serve as the safety net.\nTailwind 4.x is the single explicit exclude, as its rewrite broke the SCSS-based\nconfiguration surface the UI layer depends on.\n\n### GitHub Actions: Always Current\n\nCI action versions are the one dependency type that always bumps in package.json\nmanifests across all elements. Using the latest stable action version ensures\nbugfixes and security patches land automatically.\n\n---\n\n## Elements Without Dependabot\n\nVine and Mist do not have a `.github/dependabot.yml` file in their repositories\n(despite being Rust elements). Their dependencies are managed through manual\nreview and the umbrella-level Dependabot if applicable.\n\nNote: The search found dependabot.yml in 13 element directories. The two\nelements without one are likely Vine and Mist, which had minimal or placeholder\nrepositories for extended periods.', "Source/Content/doc/dependency-management.md", "b1a6bd2e9bb64c32", { html: 1268, metadata: 1269 }, '<h1 id="dependency-management">Dependency Management</h1>\n<p>Each Land element configures its own Dependabot. This page documents the shared\npatterns and element-specific variations.</p>\n<hr>\n<h2 id="dependabot-configuration">Dependabot Configuration</h2>\n<p>All configurated elements use Dependabot v2 with beta ecosystems enabled:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="yaml"><code><span class="line"><span style="color:#85E89D">version</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">2</span></span>\n<span class="line"><span style="color:#85E89D">enable-beta-ecosystems</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">true</span></span></code></pre>\n<p>This single flag allows Dependabot to pick up newer or experimental\npackage-ecosystem values beyond the stable set (npm, cargo, docker,\ngithub-actions).</p>\n<hr>\n<h2 id="ecosystem-coverage">Ecosystem Coverage</h2>\n<h3 id="github-actions-all-13-elements-with-dependabotyml">github-actions (All 13 elements with dependabot.yml)</h3>\n<p>Every element monitors GitHub Action versions in its <code>.github/workflows/</code>\ndirectory for updates. This keeps CI tooling current without manual tracking.</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="yaml"><code><span class="line"><span style="color:#85E89D">updates</span><span style="color:#E1E4E8">:</span></span>\n<span class="line"><span style="color:#E1E4E8">    - </span><span style="color:#85E89D">package-ecosystem</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"github-actions"</span></span>\n<span class="line"><span style="color:#85E89D">      directory</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"/"</span></span>\n<span class="line"><span style="color:#85E89D">      schedule</span><span style="color:#E1E4E8">:</span></span>\n<span class="line"><span style="color:#85E89D">          interval</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"daily"</span></span></code></pre>\n<h3 id="cargo-rust-elements">cargo (Rust elements)</h3>\n<p>Applies to: Mountain, Air, Common, Echo, Grove, Mist, Rest, SideCar, Vine,\nMaintain</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="yaml"><code><span class="line"><span style="color:#E1E4E8">- </span><span style="color:#85E89D">package-ecosystem</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"cargo"</span></span>\n<span class="line"><span style="color:#85E89D">  directory</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"/"</span></span>\n<span class="line"><span style="color:#85E89D">  schedule</span><span style="color:#E1E4E8">:</span></span>\n<span class="line"><span style="color:#85E89D">      interval</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"daily"</span></span>\n<span class="line"><span style="color:#85E89D">  versioning-strategy</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">lockfile-only</span></span></code></pre>\n<p>The <code>lockfile-only</code> strategy means Dependabot updates <code>Cargo.lock</code> without\ntouching <code>Cargo.toml</code> version pins. This ensures:</p>\n<ul>\n<li>Version constraints in <code>Cargo.toml</code> remain stable and explicit.</li>\n<li>Lockfile picks up compatible new releases automatically.</li>\n<li>No breaking semver bumps slip through the lockfile gate.</li>\n</ul>\n<h3 id="npm-typescriptjavascript-elements">npm (TypeScript/JavaScript elements)</h3>\n<p>Applies to: Wind, Cocoon, Output, Sky, Worker</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="yaml"><code><span class="line"><span style="color:#E1E4E8">- </span><span style="color:#85E89D">package-ecosystem</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"npm"</span></span>\n<span class="line"><span style="color:#85E89D">  directory</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"/"</span></span>\n<span class="line"><span style="color:#85E89D">  schedule</span><span style="color:#E1E4E8">:</span></span>\n<span class="line"><span style="color:#85E89D">      interval</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"daily"</span></span>\n<span class="line"><span style="color:#85E89D">  versioning-strategy</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">increase</span></span>\n<span class="line"><span style="color:#85E89D">  ignore</span><span style="color:#E1E4E8">:</span></span>\n<span class="line"><span style="color:#E1E4E8">      - </span><span style="color:#85E89D">dependency-name</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"tailwindcss"</span></span>\n<span class="line"><span style="color:#85E89D">        versions</span><span style="color:#E1E4E8">:</span></span>\n<span class="line"><span style="color:#E1E4E8">            - </span><span style="color:#9ECBFF">"^4.0.0"</span></span></code></pre>\n<p>The <code>increase</code> strategy means Dependabot bumps the version ranges in\n<code>package.json</code> itself, not just the lockfile. This gives each element the latest\ncompatible ranges on every dependency roll.</p>\n<p>Tailwind 4.x is explicitly ignored across all JS elements due to the breaking\nchanges in the v4 rewrite (CSS-first configuration, different plugin API).</p>\n<hr>\n<h2 id="update-strategy-summary">Update Strategy Summary</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Element</th><th>Ecosystems</th><th>Versioning Strategy</th></tr></thead><tbody><tr><td>Mountain</td><td>github-actions, cargo</td><td>lockfile-only</td></tr><tr><td>Air</td><td>github-actions, cargo</td><td>lockfile-only</td></tr><tr><td>Common</td><td>github-actions, cargo</td><td>lockfile-only</td></tr><tr><td>Echo</td><td>github-actions, cargo</td><td>lockfile-only</td></tr><tr><td>Grove</td><td>github-actions, cargo</td><td>lockfile-only</td></tr><tr><td>Mist</td><td>github-actions, cargo</td><td>lockfile-only</td></tr><tr><td>Rest</td><td>github-actions, cargo</td><td>lockfile-only</td></tr><tr><td>SideCar</td><td>github-actions, cargo</td><td>lockfile-only</td></tr><tr><td>Vine</td><td>github-actions, cargo</td><td>lockfile-only</td></tr><tr><td>Maintain</td><td>github-actions, cargo</td><td>lockfile-only</td></tr><tr><td>Wind</td><td>github-actions, npm</td><td>increase</td></tr><tr><td>Cocoon</td><td>github-actions, npm</td><td>increase</td></tr><tr><td>Output</td><td>github-actions, npm</td><td>increase</td></tr><tr><td>Sky</td><td>github-actions, npm</td><td>increase</td></tr><tr><td>Worker</td><td>github-actions, npm</td><td>increase</td></tr></tbody></table>\n<hr>\n<h2 id="dependency-philosophy">Dependency Philosophy</h2>\n<h3 id="rust-lockfile-only-stability">Rust: Lockfile-Only Stability</h3>\n<p>Rust elements keep their <code>Cargo.toml</code> pins stable. Dependabot rolls the\nlockfile, bringing in bugfix and minor releases within the existing constraint.\nMajor version bumps (e.g., Hickory-Server 0.24 to 0.26 in Mist, OXC 0.127 in\nRest) require manual review because they may change public APIs or require code\nmigrations.</p>\n<h3 id="javascript-aggressive-range-updates">JavaScript: Aggressive Range Updates</h3>\n<p>JavaScript elements let Dependabot bump package.json ranges directly. This keeps\nthe dependency surface fresh but requires CI to catch incompatibilities. The\nNode.yml matrix (Node 24) and pre-publish checks serve as the safety net.\nTailwind 4.x is the single explicit exclude, as its rewrite broke the SCSS-based\nconfiguration surface the UI layer depends on.</p>\n<h3 id="github-actions-always-current">GitHub Actions: Always Current</h3>\n<p>CI action versions are the one dependency type that always bumps in package.json\nmanifests across all elements. Using the latest stable action version ensures\nbugfixes and security patches land automatically.</p>\n<hr>\n<h2 id="elements-without-dependabot">Elements Without Dependabot</h2>\n<p>Vine and Mist do not have a <code>.github/dependabot.yml</code> file in their repositories\n(despite being Rust elements). Their dependencies are managed through manual\nreview and the umbrella-level Dependabot if applicable.</p>\n<p>Note: The search found dependabot.yml in 13 element directories. The two\nelements without one are likely Vine and Mist, which had minimal or placeholder\nrepositories for extended periods.</p>', { headings: 1270, localImagePaths: 1305, remoteImagePaths: 1306, frontmatter: 1307, imagePaths: 1308 }, [1271, 1272, 1275, 1278, 1281, 1284, 1287, 1290, 1293, 1296, 1299, 1302], { depth: 25, slug: 1258, text: 1261 }, { depth: 29, slug: 1273, text: 1274 }, "dependabot-configuration", "Dependabot Configuration", { depth: 29, slug: 1276, text: 1277 }, "ecosystem-coverage", "Ecosystem Coverage", { depth: 347, slug: 1279, text: 1280 }, "github-actions-all-13-elements-with-dependabotyml", "github-actions (All 13 elements with dependabot.yml)", { depth: 347, slug: 1282, text: 1283 }, "cargo-rust-elements", "cargo (Rust elements)", { depth: 347, slug: 1285, text: 1286 }, "npm-typescriptjavascript-elements", "npm (TypeScript/JavaScript elements)", { depth: 29, slug: 1288, text: 1289 }, "update-strategy-summary", "Update Strategy Summary", { depth: 29, slug: 1291, text: 1292 }, "dependency-philosophy", "Dependency Philosophy", { depth: 347, slug: 1294, text: 1295 }, "rust-lockfile-only-stability", "Rust: Lockfile-Only Stability", { depth: 347, slug: 1297, text: 1298 }, "javascript-aggressive-range-updates", "JavaScript: Aggressive Range Updates", { depth: 347, slug: 1300, text: 1301 }, "github-actions-always-current", "GitHub Actions: Always Current", { depth: 29, slug: 1303, text: 1304 }, "elements-without-dependabot", "Elements Without Dependabot", [], [], { title: 1261, section: 493, order: 1262, description: 1263 }, [], "development-history", { id: 1309, data: 1311, body: 1315, filePath: 1316, digest: 1317, rendered: 1318 }, { title: 1312, section: 493, order: 1313, description: 1314 }, "Development History", 45, "Consolidated changelog across all 15 Land elements, organized by element with dates and scope.", "# Development History\n\nConsolidated version history across all 15 Land elements. Each element maintains\nits own changelog; this page aggregates them for reference.\n\n---\n\n## Element (Umbrella)\n\nThe umbrella repository for the Land submodule fleet at `Land/Element/`.\n\n### v2.2 - Bundled-Electron Profile: Correctness Pass (April 2026)\n\n- Output and Sky submodules synced as a pair alongside CHANGELOG rewrites.\n\n### v2.1 - Full Workbench Lift (April 2026)\n\n- CHANGELOG revisions for Common, Air, Wind, Cocoon, Output, and Sky.\n- Submodule pin updates for each element following their individual rewrites.\n\n### v0.0 - Project Inception\n\n- Initial empty umbrella scaffold. Substance lives in per-element changelogs.\n\n---\n\n## Air\n\nBackground daemon sidecar for updates, downloads, cryptographic signing, and\nindexing.\n\n### v2.1 - Full Workbench Lift (April 2026)\n\n- Tag-filtered DevLog system in `Source/DevLog.rs` (~295 lines), threaded\n  through 44 files.\n- PascalCase identifier sweep across `Binary/Monitor`, `Binary/Shutdown`, and\n  all source files.\n- Obsolete TODOs cleared (12 changes); consistent formatting across 14 files.\n\n### v2.0 - Editor Launch (Q1 2026)\n\n- 73 Rust modules landed: DNS resolver (~160 lines with 367 lines of tests),\n  updater, downloader, indexer, authentication, HTTP, gRPC service (281 lines in\n  `AirVinegRPCService.rs`), health monitoring, graceful shutdown, config\n  hot-reload, observability trio.\n- TODO closure: 35 subsystems implemented in a single commit (529 insertions).\n- Field-naming standardisation: 192 field renames.\n- `Cargo.toml` gained 12 new dependencies.\n\n### v1.2 - Full-Stack Integration (2025-2026)\n\n- Initial architecture planning and placeholder maintenance.\n\n---\n\n## Cocoon\n\nExtension-host sidecar running VS Code extensions through an Effect-TS-driven\n`vscode` API shim.\n\n### v2.2 - Bundled-Electron Profile: Correctness Pass (April 2026)\n\n- Parent-PID watchdog polling `process.kill(ppid, 0)` every 2s.\n- `-32004` benign-404 classification in RPC-error and catch paths.\n- `FileWatcher.Register` benign classifier for absent paths.\n- `BuildOpenTextDocument` decodes Mountain's `Vec<u8>` body to UTF-8 with full\n  `TextDocument` API.\n\n### v2.1 - Full Workbench Lift, Effect-TS Namespace Split (April 2026)\n\n- `GRPCServerService` split into 7 focused handlers under `Services/Handler/`.\n- `vscode` API split into 10 namespaces with Effect-TS heuristic Proxy for gap\n  detection.\n- Workspace events, document content handler, extension storage, secrets, and\n  window messaging.\n- Bidirectional language-provider sync with Mountain.\n- Platform abstraction layer under `Source/Platform/`.\n\n### v2.0 - Editor Launch (Q1 2026: Effect-TS Rewrite)\n\n- 7 Effect-TS service layers (~2,325 lines): Bootstrap, Extension, Health,\n  MountainClient, RPCServer, Telemetry, index.\n- `MountainGRPCClient` with typed wrappers for 70+ RPCs.\n- 13 service interfaces, `ModuleInterceptor` security sandbox,\n  `EchoActionClient`.\n- Effect 3.19.13 to 3.21.0, `@effect/platform` 0.94.0 to 0.96.0.\n\n### v1.3 - Dependency Maintenance (2025)\n\n- Effect 3.18.1 to 3.19.14, `@effect/platform` 0.92.1 to 0.94.1.\n\n### v1.2 - Full-Stack Integration (2025)\n\n- `Source/IPC/` with `Generated.ts` and `ProtoConverter` pair. 177-file\n  structural reorganisation.\n\n### v1.1 - Architecture Buildout (2025)\n\n- ~400 commits, ~33k lines: core services, Tauri integration, TypeConverter,\n  Mountain handshake.\n\n### v0.0 - Project Inception\n\n- Initial scaffold with Effect 3.16.10.\n\n---\n\n## Common\n\nAbstract core Rust crate with trait definitions, `ActionEffect` system, and\nshared DTOs.\n\n### v2.1 - Full Workbench Lift (April 2026)\n\n- PascalCase enforcement across 34 Rust files (514 insertions, 705 deletions).\n- `EvaluatableExpression` + `InlineValues` provider types.\n- 19 `LanguageFeature` provider methods standardised.\n- Transport-layer refactor: `CircuitBreaker.rs`, `IPC.rs`, `gRPC.rs`.\n- 36 new TypeScript declaration files for Workers API surface.\n\n### v1.3 - Dependency Maintenance (2025)\n\n- `@cloudflare/workers-types` rolled forward. No source changes.\n\n### v1.2 - Full-Stack Integration (2025)\n\n- `@cloudflare/workers-types` 4.20240721 to 4.20240925. Doc styling refreshed\n  (98 CSS lines).\n\n### v1.1 - Architecture Buildout (2025)\n\n- Cloudflare Workers library with crypto-operations focus. 8 generated\n  TypeScript declaration files.\n\n### v1.0 - Integration Phase (2025)\n\n- Maintenance-heavy cycle, `@cloudflare/workers-types` rolls.\n\n### v0.2 - Architecture Solidification (2024)\n\n- License expansion (42 to 131 lines). 172 insertions across 18 TypeScript\n  declaration files.\n\n### v0.0 - Project Inception (2024)\n\n- Initial crypto-operation surface, Cloudflare Workers types, TypeDoc pipeline.\n\n---\n\n## Echo\n\nHigh-performance work-stealing task scheduler (Rust crate consumed by Mountain).\n\n### v2.1 - Full Workbench Lift (April 2026)\n\n- Comprehensive CHANGELOG rebuilt from commit record.\n- Crate-level rustdoc rewritten benefit-first. README expansion with\n  getting-started guide.\n- `build.rs` cleanup for warning-free `cargo build`.\n\n### v2.0 - Editor Launch (Q1 2026)\n\n- Workbench fixes stabilized Echo's call sites against new bundled-electron boot\n  order.\n\n### v1.3 - Dependency Maintenance (Q4 2025)\n\n- Dependency rolls with no source changes.\n\n### v1.2 - Full-Stack Integration (Q3 2025)\n\n- `tsconfig` updated for declaration generation. Example formatting fixed.\n\n### v1.1 - Architecture Buildout (June 2025)\n\n- Scheduler/Queue/Task module split with real Rust source.\n- PascalCase module conversion. Deep Dive document, README rewrite, performance\n  roadmap.\n\n### v1.0 - Integration Phase (May 2025)\n\n- License transitions (CC0 to Land Public License v1.0). URL normalisation.\n\n### v0.0 - Project Inception (July 2024)\n\n- Cloudflare Workers scaffold, GitHub Actions workflows, Dependabot config.\n\n---\n\n## Grove\n\nRust/WASM extension-host sidecar for sandboxed WebAssembly extensions.\n\n### v2.1 - Full Workbench Lift (April 2026)\n\n- Language-provider registration system with forwarding to Mountain via\n  transport.\n- Custom DevLog tag-filtered logging replaces `tracing`. PascalCase rename.\n- `Grove.proto` field names migrated to PascalCase. Transport-layer migration.\n\n### v2.0 - Editor Launch (Q1 2026)\n\n- Full scaffold: Rust/WASM host, VS Code API facade, gRPC transport, integration\n  tests.\n- Wasmtime 20 integration, host function execution, Spine action client.\n- Common Transport Adapter to bridge through Common's vocabulary.\n\n### v0.0 - Project Inception (April 2025)\n\n- Placeholder repository. 14-commit buildout on 2026-02-08 was first real work.\n\n---\n\n## Maintain\n\nBuild/CI/GritQL surface for compiling Mountain, sidecars, and bundled VS Code\ntree.\n\n### v2.1 - Full Workbench Lift (April 2026)\n\n- Multi-profile build support for Mountain, Electron, and compiler variants.\n- Wildcard re-exports removed; explicit module paths enforced.\n- Consistent Rust formatting for Rhai config tests.\n\n### v2.0 - Editor Launch (Q1 2026)\n\n- Guard disarm mechanism in `Maintain/Build` for transactional config rewrites.\n- Binary entry point restructured with dedicated `main.rs`.\n\n### v1.3 - Dependency Maintenance (Q4 2025)\n\n- Continuous Dependabot rolls.\n\n### v1.2 - Full-Stack Integration (Q3 2025)\n\n- Node.js sidecar version bundling support. Compiled executables refreshed.\n\n### v1.1 - Architecture Buildout (May 2025)\n\n- Structured build system replacing ad-hoc shell scripts. Binary build\n  configurability.\n\n### v0.0 - Project Inception (2024)\n\n- Catch-all build/CI repository. 200+ commits of `squash!` consolidation and\n  Dependabot rolls.\n\n---\n\n## Mist\n\nDNS isolation element - Hickory-DNS authoritative server resolving\n`*.land.playform.cloud` to loopback.\n\n### v2.1 - Full Workbench Lift (April 2026)\n\n- Hickory-server upgraded to 0.26 API.\n- Library renamed `mist` to `Mist` for PascalCase compliance. Integration-test\n  imports refactored.\n\n### v2.0 - Editor Launch (Q1 2026: DNS Isolation Introduced, 2026-02-27)\n\n- Complete DNS server: `Server.rs` (UDP/TCP loopback listeners), `Zone.rs`\n  (authoritative zone), `Resolver.rs` (HTTP-client integration with IP\n  validation), `ForwardSecurity.rs` (forward allowlist).\n- Integration: Mountain starts server, Air uses it for secure HTTP, SideCar\n  spawns Node with DNS override, Cocoon resolves `land.playform.cloud`.\n\n### v0.0 - Project Inception (April 2025)\n\n- Placeholder for nine months before DNS implementation landed.\n\n---\n\n## Mountain\n\nNative Rust Tauri backend with gRPC server, workbench service runtime, and all\nIPC routing.\n\n### v2.2 - Bundled-Electron Profile: Correctness Pass (April 2026)\n\n- Dropped Cocoon-facing circuit-breaker cascade from 17,134 rejections to zero.\n- JSON-RPC code `-32004` for benign 404s with `LooksLike404` predicate.\n- Three-shape `GetURLFromURIComponentsDTO`. Deferred-no-op file watching.\n- 64 KB per-terminal ring buffer. `sky:replay-events` IPC handler.\n- Canonical-path comparison for `/Volumes/...` workspace mounts.\n- `tree:getChildren` timeout 5000ms to 1500ms.\n\n### v2.1 - Full Workbench Lift (April 2026)\n\n- 24-module split of `WindServiceHandlers`, 15-submodule split of\n  `CocoonService`.\n- `dev_log!` macro across 170 files with 38 granular tags and `Trace=short`\n  mode.\n- 18 CocoonService handlers, 14 language-feature RPC methods.\n- PostHog plugin, OTLP proxy, NodeResolver (8-step ladder), VsixInstaller.\n- Navigation history, label resolution, text model registry, DualTrack env\n  knobs.\n- macOS window dragging, pre-boot zombie sweep, atomic shutdown guard.\n- Vine protocol PascalCase, DTO serialization fixes, Cocoon health monitor\n  improvements.\n\n### v2.0 - Editor Launch (Q1 2026)\n\n- Modular startup: Tray, Shutdown, Service, Register, Initialize.\n- 20+ Tauri commands, TreeView foundation, Hover feature, Debug state.\n- 351 Rust files, 70+ gRPC RPCs, 24 IPC domain modules.\n- Thread safety: `static mut` to `RwLock` across 3828 lines.\n\n### v1.3 - Dependency Maintenance (2025)\n\n- `gen/schemas/macOS-schema.json` for tray/menu bar/dock. CI action updates.\n\n### v1.2 - Full-Stack Integration (2025)\n\n- Error handling standardised. Sidecar naming consolidated. Windows installer\n  naming standardised.\n\n### v1.1 - Architecture Buildout (2025)\n\n- 185 commits: `Command/`, `Environment/` (14 providers), `ProcessManagement/`,\n  `RunTime/`, `Track/`, `Vine/server/`.\n- Auto-generated `vine_ipc.rs` (1,714 lines from `Vine.proto`).\n\n### v1.0 - Integration Phase (2025)\n\n- `ApplicationState/` reorganisation with DTOs and feature-organized state.\n- `.github/workflows/Auto.yml` for automated update/push CI.\n\n### v0.0 - Project Inception\n\n- Relocated from `Editor/editor/src-tauri/` to monorepo root. Tauri build system\n  integrated. CI landed.\n\n---\n\n## Output\n\nBundled JS artifact tree - compiled VS Code platform code loaded by Cocoon and\nSky at runtime.\n\n### v2.2 - Bundled-Electron Profile: Correctness Pass (April 2026)\n\n- `ExposeWorkbenchAccessor` transform for service-accessor capture.\n- Service-IIFE wrapping for singleton registration survival through esbuild\n  tree-shake.\n- `__CEL_SERVICES__.invokeFunction(IFooService)` documented pattern.\n- JSDoc nested `/* */` fix for esbuild compatibility.\n\n### v2.1 - Full Workbench Lift, Polyfill Hardening (April 2026)\n\n- `Source/Polyfill/` (~5,200 lines): 6 Electron-to-Tauri shims (Process,\n  FileSystem, FileProtocol, ChildProcess, IPCRenderer, NativeModule).\n- `TauriMainProcessService` IPC adapter, `DevLog` tag-filtered logging, `Trace`\n  tracer surface.\n- ESBuild framework rebuilt around dedicated config modules with 12\n  exclusion-pattern modules.\n\n### v2.0 - Editor Launch (Q1 2026)\n\n- Migrated from prebuilt VS Code `out/` to source compilation. 4,287 JS files /\n  169 MB per build.\n\n### v1.1 - Architecture Buildout (2025)\n\n- 35,000+ JS files, 14,000 source maps per build.\n\n---\n\n## Rest\n\nRust JS bundler using OXC (migrated from SWC in March 2026) for VS Code source\ncompilation.\n\n### v2.1 - Full Workbench Lift (April 2026)\n\n- OXC 0.127 API migration. Binary entry point switched to `Main.rs`.\n- PascalCase rename. Unused imports cleared. Shell scripts POSIX-compliant.\n\n### v2.0 - Editor Launch (OXC Migration, March 2026)\n\n- SWC replaced with OXC compiler ecosystem. Compile subcommand. Phase 3 advanced\n  build features.\n- Static class-property transformation, comprehensive test suite.\n\n### v1.0 - Integration Phase (Q1 2025)\n\n- 152 commits, mostly Dependabot rolls.\n\n### v0.0 - Project Inception (September 2024)\n\n- First commit. Started as standalone `BinaryRest` before Land monorepo\n  absorption.\n\n---\n\n## SideCar\n\nNode.js sidecar runtime for platform-specific Node binary download and spawn\nwith DNS isolation.\n\n### v2.1 - Full Workbench Lift (April 2026)\n\n- CHANGELOG and README expansion. Crate-level rustdoc rewritten.\n- `LAND_DNS_SERVER` renamed to `Resolve`. `tauri-plugin-shell` updated to 2.3.\n- Mist import-path casing corrected.\n\n### v2.0 - Editor Launch (Q1 2026: SideCar Born, 2026-03-24)\n\n- Extracted from Mountain: `Download.rs` and `SideCar` binary with DNS override\n  for Mist isolation.\n- Cargo manifest with tokio, reqwest, tar, flate2, zip for platform binary\n  unpacking.\n\n### v0.0 - Project Inception (March 2026)\n\n- Node.js download/spawn logic previously lived inline in Mountain.\n\n---\n\n## Sky\n\nAstro-driven UI component layer hosting the workbench inside Tauri webview.\n\n### v2.2 - Bundled-Electron Profile: Correctness Pass (April 2026)\n\n- `sky:replay-events` invoke at end of `InstallSkyBridge` for event-drain during\n  Cocoon activation.\n- Channel-event bridge translating `<channel>.<event>` subscriptions to Tauri\n  event listeners.\n- Promise-protocol short-circuit for workbench wake-cycle cleanup.\n- Search-result mapper updated to current `ITextSearchMatch` shape.\n\n### v2.1 - Full Workbench Lift, Telemetry Bridges, Bundle Profiles (April 2026)\n\n- OTELBridge (~153 lines) and PostHogBridge (~159 lines).\n- `enableWorkspaceTrust: false` across 5 workbench profiles. Astro 6.1.1 to\n  6.1.7.\n- 327 lines of obsolete bootstrap code removed.\n\n### v2.0 - Editor Launch (Q1 2026)\n\n- Astro 5.16.6 to 6.1.1. TypeScript 6.0.2. Vite 8.0.3.\n- Deleted 80 HTML files from ephemeral `Target/` (414 deletions).\n\n### v1.3 - Dependency Maintenance (2025)\n\n- Astro 5.14.1 to 5.16.6. Vite 7.1.6 to 7.3.0.\n\n### v1.2 - Full-Stack Integration (2025)\n\n- `jsconfig.json` for JS module surface. `sky-ipc-router` and `sky-host-bridge`\n  added.\n\n### v1.1 - Architecture Buildout (2025)\n\n- Tauri workbench bootstrap: `WorkBench.ts` (~200 lines), keyboard layouts (37\n  files). Vite 6.3.5 to 7.0.0.\n\n### v0.0 - Project Inception (2024)\n\n- Astro 4.x setup, SWUP page transitions, Tailwind CSS.\n\n---\n\n## Vine\n\ngRPC protocol definition contract spoken between Mountain and Cocoon/Air\nsidecars.\n\n### v2.1 - Full Workbench Lift (April 2026)\n\n- CHANGELOG and comprehensive architecture documentation. See Also section\n  added.\n\n### v2.0 - Editor Launch (Q1 2026)\n\n- Protocol stable through editor-launch window. Proto-doc edits alongside\n  Mountain's gRPC handler split.\n\n### v0.0 - Project Inception (April 2025)\n\n- Placeholder repository. `Vine.proto` lived in Mountain throughout.\n\n---\n\n## Wind\n\nUI service layer - Effect-TS re-implementation of VS Code workbench services\nbridging renderer to Mountain through Tauri IPC.\n\n### v2.1 - Full Workbench Lift, Tauri IPC Bridge (April 2026)\n\n- 5 streaming `listen()` subscriptions: vfs/fileChange, configuration/changed,\n  terminal/data, lifecycle/willShutdown, lifecycle/phaseChanged.\n- `TauriMainProcessService` with 24 routed channels and 14 stub channels.\n- MessageChannel for extension-host IPC. `ResolveConfiguration` for\n  `INativeWindowConfiguration`.\n- `DevLog` (~72 lines) with short-mode formatting.\n- 1,076 lines of legacy polyfills deleted.\n\n### v2.0 - Editor Launch (Q1 2026)\n\n- `Preload.ts` (99 lines) as Tauri/Electron bridge. 30+ VS Code type mirror\n  files. TypeScript 5.9.3 to 6.0.2.\n\n### v1.3 - Dependency Maintenance (2025)\n\n- Effect 3.18.0 to 3.19.14. Module count stable.\n\n### v1.2 - Full-Stack Integration (2025)\n\n- 100+ application service modules with Define/Implement/Problem pattern. 300+\n  file infrastructure commit.\n- Effect-TS 3.17.x to 3.18.x.\n\n### v1.1 - Architecture Buildout (2025)\n\n- Effect-TS adoption. `Source/Effect/` and `Source/Application/` directories.\n  SolidJS evaluation concluded.\n\n### v0.0 - Project Inception (2024)\n\n- SolidJS-era bones: Context, Element, Library, Function, Stylesheet, Variable.\n\n---\n\n## Worker\n\nService-worker/web-worker layer for renderer background processing and shim\ncaching.\n\n### v2.1 - Full Workbench Lift (April 2026)\n\n- Service-worker registration wrapped in IIFE with early return. ESBuild config\n  with source maps.\n- Obsolete precache paths removed. Build scripts switched to POSIX-compliant\n  `sh`.\n\n### v2.0 - Editor Launch (Q1 2026)\n\n- Service-worker refresh loop prevention with version tracking. Production\n  minification with development-readable source maps.\n\n### v1.0 - Integration Phase (May 2025)\n\n- Initial web-worker implementation. Service-worker security and version\n  tracking. ESBuild configuration consolidated.\n\n### v0.0 - Project Inception (April 2025)\n\n- 67-commit setup push. No worker implementation yet.", "Source/Content/doc/development-history.md", "f1140f5ea5d51357", { html: 1319, metadata: 1320 }, '<h1 id="development-history">Development History</h1>\n<p>Consolidated version history across all 15 Land elements. Each element maintains\nits own changelog; this page aggregates them for reference.</p>\n<hr>\n<h2 id="element-umbrella">Element (Umbrella)</h2>\n<p>The umbrella repository for the Land submodule fleet at <code>Land/Element/</code>.</p>\n<h3 id="v22---bundled-electron-profile-correctness-pass-april-2026">v2.2 - Bundled-Electron Profile: Correctness Pass (April 2026)</h3>\n<ul>\n<li>Output and Sky submodules synced as a pair alongside CHANGELOG rewrites.</li>\n</ul>\n<h3 id="v21---full-workbench-lift-april-2026">v2.1 - Full Workbench Lift (April 2026)</h3>\n<ul>\n<li>CHANGELOG revisions for Common, Air, Wind, Cocoon, Output, and Sky.</li>\n<li>Submodule pin updates for each element following their individual rewrites.</li>\n</ul>\n<h3 id="v00---project-inception">v0.0 - Project Inception</h3>\n<ul>\n<li>Initial empty umbrella scaffold. Substance lives in per-element changelogs.</li>\n</ul>\n<hr>\n<h2 id="air">Air</h2>\n<p>Background daemon sidecar for updates, downloads, cryptographic signing, and\nindexing.</p>\n<h3 id="v21---full-workbench-lift-april-2026-1">v2.1 - Full Workbench Lift (April 2026)</h3>\n<ul>\n<li>Tag-filtered DevLog system in <code>Source/DevLog.rs</code> (~295 lines), threaded\nthrough 44 files.</li>\n<li>PascalCase identifier sweep across <code>Binary/Monitor</code>, <code>Binary/Shutdown</code>, and\nall source files.</li>\n<li>Obsolete TODOs cleared (12 changes); consistent formatting across 14 files.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026">v2.0 - Editor Launch (Q1 2026)</h3>\n<ul>\n<li>73 Rust modules landed: DNS resolver (~160 lines with 367 lines of tests),\nupdater, downloader, indexer, authentication, HTTP, gRPC service (281 lines in\n<code>AirVinegRPCService.rs</code>), health monitoring, graceful shutdown, config\nhot-reload, observability trio.</li>\n<li>TODO closure: 35 subsystems implemented in a single commit (529 insertions).</li>\n<li>Field-naming standardisation: 192 field renames.</li>\n<li><code>Cargo.toml</code> gained 12 new dependencies.</li>\n</ul>\n<h3 id="v12---full-stack-integration-2025-2026">v1.2 - Full-Stack Integration (2025-2026)</h3>\n<ul>\n<li>Initial architecture planning and placeholder maintenance.</li>\n</ul>\n<hr>\n<h2 id="cocoon">Cocoon</h2>\n<p>Extension-host sidecar running VS Code extensions through an Effect-TS-driven\n<code>vscode</code> API shim.</p>\n<h3 id="v22---bundled-electron-profile-correctness-pass-april-2026-1">v2.2 - Bundled-Electron Profile: Correctness Pass (April 2026)</h3>\n<ul>\n<li>Parent-PID watchdog polling <code>process.kill(ppid, 0)</code> every 2s.</li>\n<li><code>-32004</code> benign-404 classification in RPC-error and catch paths.</li>\n<li><code>FileWatcher.Register</code> benign classifier for absent paths.</li>\n<li><code>BuildOpenTextDocument</code> decodes Mountain’s <code>Vec&#x3C;u8></code> body to UTF-8 with full\n<code>TextDocument</code> API.</li>\n</ul>\n<h3 id="v21---full-workbench-lift-effect-ts-namespace-split-april-2026">v2.1 - Full Workbench Lift, Effect-TS Namespace Split (April 2026)</h3>\n<ul>\n<li><code>GRPCServerService</code> split into 7 focused handlers under <code>Services/Handler/</code>.</li>\n<li><code>vscode</code> API split into 10 namespaces with Effect-TS heuristic Proxy for gap\ndetection.</li>\n<li>Workspace events, document content handler, extension storage, secrets, and\nwindow messaging.</li>\n<li>Bidirectional language-provider sync with Mountain.</li>\n<li>Platform abstraction layer under <code>Source/Platform/</code>.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026-effect-ts-rewrite">v2.0 - Editor Launch (Q1 2026: Effect-TS Rewrite)</h3>\n<ul>\n<li>7 Effect-TS service layers (~2,325 lines): Bootstrap, Extension, Health,\nMountainClient, RPCServer, Telemetry, index.</li>\n<li><code>MountainGRPCClient</code> with typed wrappers for 70+ RPCs.</li>\n<li>13 service interfaces, <code>ModuleInterceptor</code> security sandbox,\n<code>EchoActionClient</code>.</li>\n<li>Effect 3.19.13 to 3.21.0, <code>@effect/platform</code> 0.94.0 to 0.96.0.</li>\n</ul>\n<h3 id="v13---dependency-maintenance-2025">v1.3 - Dependency Maintenance (2025)</h3>\n<ul>\n<li>Effect 3.18.1 to 3.19.14, <code>@effect/platform</code> 0.92.1 to 0.94.1.</li>\n</ul>\n<h3 id="v12---full-stack-integration-2025">v1.2 - Full-Stack Integration (2025)</h3>\n<ul>\n<li><code>Source/IPC/</code> with <code>Generated.ts</code> and <code>ProtoConverter</code> pair. 177-file\nstructural reorganisation.</li>\n</ul>\n<h3 id="v11---architecture-buildout-2025">v1.1 - Architecture Buildout (2025)</h3>\n<ul>\n<li>~400 commits, ~33k lines: core services, Tauri integration, TypeConverter,\nMountain handshake.</li>\n</ul>\n<h3 id="v00---project-inception-1">v0.0 - Project Inception</h3>\n<ul>\n<li>Initial scaffold with Effect 3.16.10.</li>\n</ul>\n<hr>\n<h2 id="common">Common</h2>\n<p>Abstract core Rust crate with trait definitions, <code>ActionEffect</code> system, and\nshared DTOs.</p>\n<h3 id="v21---full-workbench-lift-april-2026-2">v2.1 - Full Workbench Lift (April 2026)</h3>\n<ul>\n<li>PascalCase enforcement across 34 Rust files (514 insertions, 705 deletions).</li>\n<li><code>EvaluatableExpression</code> + <code>InlineValues</code> provider types.</li>\n<li>19 <code>LanguageFeature</code> provider methods standardised.</li>\n<li>Transport-layer refactor: <code>CircuitBreaker.rs</code>, <code>IPC.rs</code>, <code>gRPC.rs</code>.</li>\n<li>36 new TypeScript declaration files for Workers API surface.</li>\n</ul>\n<h3 id="v13---dependency-maintenance-2025-1">v1.3 - Dependency Maintenance (2025)</h3>\n<ul>\n<li><code>@cloudflare/workers-types</code> rolled forward. No source changes.</li>\n</ul>\n<h3 id="v12---full-stack-integration-2025-1">v1.2 - Full-Stack Integration (2025)</h3>\n<ul>\n<li><code>@cloudflare/workers-types</code> 4.20240721 to 4.20240925. Doc styling refreshed\n(98 CSS lines).</li>\n</ul>\n<h3 id="v11---architecture-buildout-2025-1">v1.1 - Architecture Buildout (2025)</h3>\n<ul>\n<li>Cloudflare Workers library with crypto-operations focus. 8 generated\nTypeScript declaration files.</li>\n</ul>\n<h3 id="v10---integration-phase-2025">v1.0 - Integration Phase (2025)</h3>\n<ul>\n<li>Maintenance-heavy cycle, <code>@cloudflare/workers-types</code> rolls.</li>\n</ul>\n<h3 id="v02---architecture-solidification-2024">v0.2 - Architecture Solidification (2024)</h3>\n<ul>\n<li>License expansion (42 to 131 lines). 172 insertions across 18 TypeScript\ndeclaration files.</li>\n</ul>\n<h3 id="v00---project-inception-2024">v0.0 - Project Inception (2024)</h3>\n<ul>\n<li>Initial crypto-operation surface, Cloudflare Workers types, TypeDoc pipeline.</li>\n</ul>\n<hr>\n<h2 id="echo">Echo</h2>\n<p>High-performance work-stealing task scheduler (Rust crate consumed by Mountain).</p>\n<h3 id="v21---full-workbench-lift-april-2026-3">v2.1 - Full Workbench Lift (April 2026)</h3>\n<ul>\n<li>Comprehensive CHANGELOG rebuilt from commit record.</li>\n<li>Crate-level rustdoc rewritten benefit-first. README expansion with\ngetting-started guide.</li>\n<li><code>build.rs</code> cleanup for warning-free <code>cargo build</code>.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026-1">v2.0 - Editor Launch (Q1 2026)</h3>\n<ul>\n<li>Workbench fixes stabilized Echo’s call sites against new bundled-electron boot\norder.</li>\n</ul>\n<h3 id="v13---dependency-maintenance-q4-2025">v1.3 - Dependency Maintenance (Q4 2025)</h3>\n<ul>\n<li>Dependency rolls with no source changes.</li>\n</ul>\n<h3 id="v12---full-stack-integration-q3-2025">v1.2 - Full-Stack Integration (Q3 2025)</h3>\n<ul>\n<li><code>tsconfig</code> updated for declaration generation. Example formatting fixed.</li>\n</ul>\n<h3 id="v11---architecture-buildout-june-2025">v1.1 - Architecture Buildout (June 2025)</h3>\n<ul>\n<li>Scheduler/Queue/Task module split with real Rust source.</li>\n<li>PascalCase module conversion. Deep Dive document, README rewrite, performance\nroadmap.</li>\n</ul>\n<h3 id="v10---integration-phase-may-2025">v1.0 - Integration Phase (May 2025)</h3>\n<ul>\n<li>License transitions (CC0 to Land Public License v1.0). URL normalisation.</li>\n</ul>\n<h3 id="v00---project-inception-july-2024">v0.0 - Project Inception (July 2024)</h3>\n<ul>\n<li>Cloudflare Workers scaffold, GitHub Actions workflows, Dependabot config.</li>\n</ul>\n<hr>\n<h2 id="grove">Grove</h2>\n<p>Rust/WASM extension-host sidecar for sandboxed WebAssembly extensions.</p>\n<h3 id="v21---full-workbench-lift-april-2026-4">v2.1 - Full Workbench Lift (April 2026)</h3>\n<ul>\n<li>Language-provider registration system with forwarding to Mountain via\ntransport.</li>\n<li>Custom DevLog tag-filtered logging replaces <code>tracing</code>. PascalCase rename.</li>\n<li><code>Grove.proto</code> field names migrated to PascalCase. Transport-layer migration.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026-2">v2.0 - Editor Launch (Q1 2026)</h3>\n<ul>\n<li>Full scaffold: Rust/WASM host, VS Code API facade, gRPC transport, integration\ntests.</li>\n<li>Wasmtime 20 integration, host function execution, Spine action client.</li>\n<li>Common Transport Adapter to bridge through Common’s vocabulary.</li>\n</ul>\n<h3 id="v00---project-inception-april-2025">v0.0 - Project Inception (April 2025)</h3>\n<ul>\n<li>Placeholder repository. 14-commit buildout on 2026-02-08 was first real work.</li>\n</ul>\n<hr>\n<h2 id="maintain">Maintain</h2>\n<p>Build/CI/GritQL surface for compiling Mountain, sidecars, and bundled VS Code\ntree.</p>\n<h3 id="v21---full-workbench-lift-april-2026-5">v2.1 - Full Workbench Lift (April 2026)</h3>\n<ul>\n<li>Multi-profile build support for Mountain, Electron, and compiler variants.</li>\n<li>Wildcard re-exports removed; explicit module paths enforced.</li>\n<li>Consistent Rust formatting for Rhai config tests.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026-3">v2.0 - Editor Launch (Q1 2026)</h3>\n<ul>\n<li>Guard disarm mechanism in <code>Maintain/Build</code> for transactional config rewrites.</li>\n<li>Binary entry point restructured with dedicated <code>main.rs</code>.</li>\n</ul>\n<h3 id="v13---dependency-maintenance-q4-2025-1">v1.3 - Dependency Maintenance (Q4 2025)</h3>\n<ul>\n<li>Continuous Dependabot rolls.</li>\n</ul>\n<h3 id="v12---full-stack-integration-q3-2025-1">v1.2 - Full-Stack Integration (Q3 2025)</h3>\n<ul>\n<li>Node.js sidecar version bundling support. Compiled executables refreshed.</li>\n</ul>\n<h3 id="v11---architecture-buildout-may-2025">v1.1 - Architecture Buildout (May 2025)</h3>\n<ul>\n<li>Structured build system replacing ad-hoc shell scripts. Binary build\nconfigurability.</li>\n</ul>\n<h3 id="v00---project-inception-2024-1">v0.0 - Project Inception (2024)</h3>\n<ul>\n<li>Catch-all build/CI repository. 200+ commits of <code>squash!</code> consolidation and\nDependabot rolls.</li>\n</ul>\n<hr>\n<h2 id="mist">Mist</h2>\n<p>DNS isolation element - Hickory-DNS authoritative server resolving\n<code>*.land.playform.cloud</code> to loopback.</p>\n<h3 id="v21---full-workbench-lift-april-2026-6">v2.1 - Full Workbench Lift (April 2026)</h3>\n<ul>\n<li>Hickory-server upgraded to 0.26 API.</li>\n<li>Library renamed <code>mist</code> to <code>Mist</code> for PascalCase compliance. Integration-test\nimports refactored.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026-dns-isolation-introduced-2026-02-27">v2.0 - Editor Launch (Q1 2026: DNS Isolation Introduced, 2026-02-27)</h3>\n<ul>\n<li>Complete DNS server: <code>Server.rs</code> (UDP/TCP loopback listeners), <code>Zone.rs</code>\n(authoritative zone), <code>Resolver.rs</code> (HTTP-client integration with IP\nvalidation), <code>ForwardSecurity.rs</code> (forward allowlist).</li>\n<li>Integration: Mountain starts server, Air uses it for secure HTTP, SideCar\nspawns Node with DNS override, Cocoon resolves <code>land.playform.cloud</code>.</li>\n</ul>\n<h3 id="v00---project-inception-april-2025-1">v0.0 - Project Inception (April 2025)</h3>\n<ul>\n<li>Placeholder for nine months before DNS implementation landed.</li>\n</ul>\n<hr>\n<h2 id="mountain">Mountain</h2>\n<p>Native Rust Tauri backend with gRPC server, workbench service runtime, and all\nIPC routing.</p>\n<h3 id="v22---bundled-electron-profile-correctness-pass-april-2026-2">v2.2 - Bundled-Electron Profile: Correctness Pass (April 2026)</h3>\n<ul>\n<li>Dropped Cocoon-facing circuit-breaker cascade from 17,134 rejections to zero.</li>\n<li>JSON-RPC code <code>-32004</code> for benign 404s with <code>LooksLike404</code> predicate.</li>\n<li>Three-shape <code>GetURLFromURIComponentsDTO</code>. Deferred-no-op file watching.</li>\n<li>64 KB per-terminal ring buffer. <code>sky:replay-events</code> IPC handler.</li>\n<li>Canonical-path comparison for <code>/Volumes/...</code> workspace mounts.</li>\n<li><code>tree:getChildren</code> timeout 5000ms to 1500ms.</li>\n</ul>\n<h3 id="v21---full-workbench-lift-april-2026-7">v2.1 - Full Workbench Lift (April 2026)</h3>\n<ul>\n<li>24-module split of <code>WindServiceHandlers</code>, 15-submodule split of\n<code>CocoonService</code>.</li>\n<li><code>dev_log!</code> macro across 170 files with 38 granular tags and <code>Trace=short</code>\nmode.</li>\n<li>18 CocoonService handlers, 14 language-feature RPC methods.</li>\n<li>PostHog plugin, OTLP proxy, NodeResolver (8-step ladder), VsixInstaller.</li>\n<li>Navigation history, label resolution, text model registry, DualTrack env\nknobs.</li>\n<li>macOS window dragging, pre-boot zombie sweep, atomic shutdown guard.</li>\n<li>Vine protocol PascalCase, DTO serialization fixes, Cocoon health monitor\nimprovements.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026-4">v2.0 - Editor Launch (Q1 2026)</h3>\n<ul>\n<li>Modular startup: Tray, Shutdown, Service, Register, Initialize.</li>\n<li>20+ Tauri commands, TreeView foundation, Hover feature, Debug state.</li>\n<li>351 Rust files, 70+ gRPC RPCs, 24 IPC domain modules.</li>\n<li>Thread safety: <code>static mut</code> to <code>RwLock</code> across 3828 lines.</li>\n</ul>\n<h3 id="v13---dependency-maintenance-2025-2">v1.3 - Dependency Maintenance (2025)</h3>\n<ul>\n<li><code>gen/schemas/macOS-schema.json</code> for tray/menu bar/dock. CI action updates.</li>\n</ul>\n<h3 id="v12---full-stack-integration-2025-2">v1.2 - Full-Stack Integration (2025)</h3>\n<ul>\n<li>Error handling standardised. Sidecar naming consolidated. Windows installer\nnaming standardised.</li>\n</ul>\n<h3 id="v11---architecture-buildout-2025-2">v1.1 - Architecture Buildout (2025)</h3>\n<ul>\n<li>185 commits: <code>Command/</code>, <code>Environment/</code> (14 providers), <code>ProcessManagement/</code>,\n<code>RunTime/</code>, <code>Track/</code>, <code>Vine/server/</code>.</li>\n<li>Auto-generated <code>vine_ipc.rs</code> (1,714 lines from <code>Vine.proto</code>).</li>\n</ul>\n<h3 id="v10---integration-phase-2025-1">v1.0 - Integration Phase (2025)</h3>\n<ul>\n<li><code>ApplicationState/</code> reorganisation with DTOs and feature-organized state.</li>\n<li><code>.github/workflows/Auto.yml</code> for automated update/push CI.</li>\n</ul>\n<h3 id="v00---project-inception-2">v0.0 - Project Inception</h3>\n<ul>\n<li>Relocated from <code>Editor/editor/src-tauri/</code> to monorepo root. Tauri build system\nintegrated. CI landed.</li>\n</ul>\n<hr>\n<h2 id="output">Output</h2>\n<p>Bundled JS artifact tree - compiled VS Code platform code loaded by Cocoon and\nSky at runtime.</p>\n<h3 id="v22---bundled-electron-profile-correctness-pass-april-2026-3">v2.2 - Bundled-Electron Profile: Correctness Pass (April 2026)</h3>\n<ul>\n<li><code>ExposeWorkbenchAccessor</code> transform for service-accessor capture.</li>\n<li>Service-IIFE wrapping for singleton registration survival through esbuild\ntree-shake.</li>\n<li><code>__CEL_SERVICES__.invokeFunction(IFooService)</code> documented pattern.</li>\n<li>JSDoc nested <code>/* */</code> fix for esbuild compatibility.</li>\n</ul>\n<h3 id="v21---full-workbench-lift-polyfill-hardening-april-2026">v2.1 - Full Workbench Lift, Polyfill Hardening (April 2026)</h3>\n<ul>\n<li><code>Source/Polyfill/</code> (~5,200 lines): 6 Electron-to-Tauri shims (Process,\nFileSystem, FileProtocol, ChildProcess, IPCRenderer, NativeModule).</li>\n<li><code>TauriMainProcessService</code> IPC adapter, <code>DevLog</code> tag-filtered logging, <code>Trace</code>\ntracer surface.</li>\n<li>ESBuild framework rebuilt around dedicated config modules with 12\nexclusion-pattern modules.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026-5">v2.0 - Editor Launch (Q1 2026)</h3>\n<ul>\n<li>Migrated from prebuilt VS Code <code>out/</code> to source compilation. 4,287 JS files /\n169 MB per build.</li>\n</ul>\n<h3 id="v11---architecture-buildout-2025-3">v1.1 - Architecture Buildout (2025)</h3>\n<ul>\n<li>35,000+ JS files, 14,000 source maps per build.</li>\n</ul>\n<hr>\n<h2 id="rest">Rest</h2>\n<p>Rust JS bundler using OXC (migrated from SWC in March 2026) for VS Code source\ncompilation.</p>\n<h3 id="v21---full-workbench-lift-april-2026-8">v2.1 - Full Workbench Lift (April 2026)</h3>\n<ul>\n<li>OXC 0.127 API migration. Binary entry point switched to <code>Main.rs</code>.</li>\n<li>PascalCase rename. Unused imports cleared. Shell scripts POSIX-compliant.</li>\n</ul>\n<h3 id="v20---editor-launch-oxc-migration-march-2026">v2.0 - Editor Launch (OXC Migration, March 2026)</h3>\n<ul>\n<li>SWC replaced with OXC compiler ecosystem. Compile subcommand. Phase 3 advanced\nbuild features.</li>\n<li>Static class-property transformation, comprehensive test suite.</li>\n</ul>\n<h3 id="v10---integration-phase-q1-2025">v1.0 - Integration Phase (Q1 2025)</h3>\n<ul>\n<li>152 commits, mostly Dependabot rolls.</li>\n</ul>\n<h3 id="v00---project-inception-september-2024">v0.0 - Project Inception (September 2024)</h3>\n<ul>\n<li>First commit. Started as standalone <code>BinaryRest</code> before Land monorepo\nabsorption.</li>\n</ul>\n<hr>\n<h2 id="sidecar">SideCar</h2>\n<p>Node.js sidecar runtime for platform-specific Node binary download and spawn\nwith DNS isolation.</p>\n<h3 id="v21---full-workbench-lift-april-2026-9">v2.1 - Full Workbench Lift (April 2026)</h3>\n<ul>\n<li>CHANGELOG and README expansion. Crate-level rustdoc rewritten.</li>\n<li><code>LAND_DNS_SERVER</code> renamed to <code>Resolve</code>. <code>tauri-plugin-shell</code> updated to 2.3.</li>\n<li>Mist import-path casing corrected.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026-sidecar-born-2026-03-24">v2.0 - Editor Launch (Q1 2026: SideCar Born, 2026-03-24)</h3>\n<ul>\n<li>Extracted from Mountain: <code>Download.rs</code> and <code>SideCar</code> binary with DNS override\nfor Mist isolation.</li>\n<li>Cargo manifest with tokio, reqwest, tar, flate2, zip for platform binary\nunpacking.</li>\n</ul>\n<h3 id="v00---project-inception-march-2026">v0.0 - Project Inception (March 2026)</h3>\n<ul>\n<li>Node.js download/spawn logic previously lived inline in Mountain.</li>\n</ul>\n<hr>\n<h2 id="sky">Sky</h2>\n<p>Astro-driven UI component layer hosting the workbench inside Tauri webview.</p>\n<h3 id="v22---bundled-electron-profile-correctness-pass-april-2026-4">v2.2 - Bundled-Electron Profile: Correctness Pass (April 2026)</h3>\n<ul>\n<li><code>sky:replay-events</code> invoke at end of <code>InstallSkyBridge</code> for event-drain during\nCocoon activation.</li>\n<li>Channel-event bridge translating <code>&#x3C;channel>.&#x3C;event></code> subscriptions to Tauri\nevent listeners.</li>\n<li>Promise-protocol short-circuit for workbench wake-cycle cleanup.</li>\n<li>Search-result mapper updated to current <code>ITextSearchMatch</code> shape.</li>\n</ul>\n<h3 id="v21---full-workbench-lift-telemetry-bridges-bundle-profiles-april-2026">v2.1 - Full Workbench Lift, Telemetry Bridges, Bundle Profiles (April 2026)</h3>\n<ul>\n<li>OTELBridge (~153 lines) and PostHogBridge (~159 lines).</li>\n<li><code>enableWorkspaceTrust: false</code> across 5 workbench profiles. Astro 6.1.1 to\n6.1.7.</li>\n<li>327 lines of obsolete bootstrap code removed.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026-6">v2.0 - Editor Launch (Q1 2026)</h3>\n<ul>\n<li>Astro 5.16.6 to 6.1.1. TypeScript 6.0.2. Vite 8.0.3.</li>\n<li>Deleted 80 HTML files from ephemeral <code>Target/</code> (414 deletions).</li>\n</ul>\n<h3 id="v13---dependency-maintenance-2025-3">v1.3 - Dependency Maintenance (2025)</h3>\n<ul>\n<li>Astro 5.14.1 to 5.16.6. Vite 7.1.6 to 7.3.0.</li>\n</ul>\n<h3 id="v12---full-stack-integration-2025-3">v1.2 - Full-Stack Integration (2025)</h3>\n<ul>\n<li><code>jsconfig.json</code> for JS module surface. <code>sky-ipc-router</code> and <code>sky-host-bridge</code>\nadded.</li>\n</ul>\n<h3 id="v11---architecture-buildout-2025-4">v1.1 - Architecture Buildout (2025)</h3>\n<ul>\n<li>Tauri workbench bootstrap: <code>WorkBench.ts</code> (~200 lines), keyboard layouts (37\nfiles). Vite 6.3.5 to 7.0.0.</li>\n</ul>\n<h3 id="v00---project-inception-2024-2">v0.0 - Project Inception (2024)</h3>\n<ul>\n<li>Astro 4.x setup, SWUP page transitions, Tailwind CSS.</li>\n</ul>\n<hr>\n<h2 id="vine">Vine</h2>\n<p>gRPC protocol definition contract spoken between Mountain and Cocoon/Air\nsidecars.</p>\n<h3 id="v21---full-workbench-lift-april-2026-10">v2.1 - Full Workbench Lift (April 2026)</h3>\n<ul>\n<li>CHANGELOG and comprehensive architecture documentation. See Also section\nadded.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026-7">v2.0 - Editor Launch (Q1 2026)</h3>\n<ul>\n<li>Protocol stable through editor-launch window. Proto-doc edits alongside\nMountain’s gRPC handler split.</li>\n</ul>\n<h3 id="v00---project-inception-april-2025-2">v0.0 - Project Inception (April 2025)</h3>\n<ul>\n<li>Placeholder repository. <code>Vine.proto</code> lived in Mountain throughout.</li>\n</ul>\n<hr>\n<h2 id="wind">Wind</h2>\n<p>UI service layer - Effect-TS re-implementation of VS Code workbench services\nbridging renderer to Mountain through Tauri IPC.</p>\n<h3 id="v21---full-workbench-lift-tauri-ipc-bridge-april-2026">v2.1 - Full Workbench Lift, Tauri IPC Bridge (April 2026)</h3>\n<ul>\n<li>5 streaming <code>listen()</code> subscriptions: vfs/fileChange, configuration/changed,\nterminal/data, lifecycle/willShutdown, lifecycle/phaseChanged.</li>\n<li><code>TauriMainProcessService</code> with 24 routed channels and 14 stub channels.</li>\n<li>MessageChannel for extension-host IPC. <code>ResolveConfiguration</code> for\n<code>INativeWindowConfiguration</code>.</li>\n<li><code>DevLog</code> (~72 lines) with short-mode formatting.</li>\n<li>1,076 lines of legacy polyfills deleted.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026-8">v2.0 - Editor Launch (Q1 2026)</h3>\n<ul>\n<li><code>Preload.ts</code> (99 lines) as Tauri/Electron bridge. 30+ VS Code type mirror\nfiles. TypeScript 5.9.3 to 6.0.2.</li>\n</ul>\n<h3 id="v13---dependency-maintenance-2025-4">v1.3 - Dependency Maintenance (2025)</h3>\n<ul>\n<li>Effect 3.18.0 to 3.19.14. Module count stable.</li>\n</ul>\n<h3 id="v12---full-stack-integration-2025-4">v1.2 - Full-Stack Integration (2025)</h3>\n<ul>\n<li>100+ application service modules with Define/Implement/Problem pattern. 300+\nfile infrastructure commit.</li>\n<li>Effect-TS 3.17.x to 3.18.x.</li>\n</ul>\n<h3 id="v11---architecture-buildout-2025-5">v1.1 - Architecture Buildout (2025)</h3>\n<ul>\n<li>Effect-TS adoption. <code>Source/Effect/</code> and <code>Source/Application/</code> directories.\nSolidJS evaluation concluded.</li>\n</ul>\n<h3 id="v00---project-inception-2024-3">v0.0 - Project Inception (2024)</h3>\n<ul>\n<li>SolidJS-era bones: Context, Element, Library, Function, Stylesheet, Variable.</li>\n</ul>\n<hr>\n<h2 id="worker">Worker</h2>\n<p>Service-worker/web-worker layer for renderer background processing and shim\ncaching.</p>\n<h3 id="v21---full-workbench-lift-april-2026-11">v2.1 - Full Workbench Lift (April 2026)</h3>\n<ul>\n<li>Service-worker registration wrapped in IIFE with early return. ESBuild config\nwith source maps.</li>\n<li>Obsolete precache paths removed. Build scripts switched to POSIX-compliant\n<code>sh</code>.</li>\n</ul>\n<h3 id="v20---editor-launch-q1-2026-9">v2.0 - Editor Launch (Q1 2026)</h3>\n<ul>\n<li>Service-worker refresh loop prevention with version tracking. Production\nminification with development-readable source maps.</li>\n</ul>\n<h3 id="v10---integration-phase-may-2025-1">v1.0 - Integration Phase (May 2025)</h3>\n<ul>\n<li>Initial web-worker implementation. Service-worker security and version\ntracking. ESBuild configuration consolidated.</li>\n</ul>\n<h3 id="v00---project-inception-april-2025-3">v0.0 - Project Inception (April 2025)</h3>\n<ul>\n<li>67-commit setup push. No worker implementation yet.</li>\n</ul>', { headings: 1321, localImagePaths: 1548, remoteImagePaths: 1549, frontmatter: 1550, imagePaths: 1551 }, [1322, 1323, 1326, 1329, 1332, 1335, 1336, 1338, 1341, 1344, 1345, 1347, 1350, 1353, 1356, 1359, 1362, 1364, 1365, 1367, 1369, 1371, 1373, 1376, 1379, 1382, 1385, 1387, 1389, 1392, 1395, 1398, 1401, 1404, 1407, 1409, 1411, 1414, 1416, 1418, 1420, 1422, 1424, 1427, 1429, 1432, 1434, 1437, 1439, 1441, 1443, 1445, 1447, 1449, 1451, 1453, 1455, 1457, 1460, 1462, 1465, 1467, 1469, 1472, 1474, 1477, 1480, 1483, 1486, 1488, 1491, 1494, 1497, 1499, 1502, 1504, 1506, 1508, 1510, 1512, 1515, 1517, 1519, 1521, 1524, 1527, 1529, 1531, 1533, 1535, 1537, 1540, 1542, 1544, 1546], { depth: 25, slug: 1309, text: 1312 }, { depth: 29, slug: 1324, text: 1325 }, "element-umbrella", "Element (Umbrella)", { depth: 347, slug: 1327, text: 1328 }, "v22---bundled-electron-profile-correctness-pass-april-2026", "v2.2 - Bundled-Electron Profile: Correctness Pass (April 2026)", { depth: 347, slug: 1330, text: 1331 }, "v21---full-workbench-lift-april-2026", "v2.1 - Full Workbench Lift (April 2026)", { depth: 347, slug: 1333, text: 1334 }, "v00---project-inception", "v0.0 - Project Inception", { depth: 29, slug: 377, text: 380 }, { depth: 347, slug: 1337, text: 1331 }, "v21---full-workbench-lift-april-2026-1", { depth: 347, slug: 1339, text: 1340 }, "v20---editor-launch-q1-2026", "v2.0 - Editor Launch (Q1 2026)", { depth: 347, slug: 1342, text: 1343 }, "v12---full-stack-integration-2025-2026", "v1.2 - Full-Stack Integration (2025-2026)", { depth: 29, slug: 545, text: 13 }, { depth: 347, slug: 1346, text: 1328 }, "v22---bundled-electron-profile-correctness-pass-april-2026-1", { depth: 347, slug: 1348, text: 1349 }, "v21---full-workbench-lift-effect-ts-namespace-split-april-2026", "v2.1 - Full Workbench Lift, Effect-TS Namespace Split (April 2026)", { depth: 347, slug: 1351, text: 1352 }, "v20---editor-launch-q1-2026-effect-ts-rewrite", "v2.0 - Editor Launch (Q1 2026: Effect-TS Rewrite)", { depth: 347, slug: 1354, text: 1355 }, "v13---dependency-maintenance-2025", "v1.3 - Dependency Maintenance (2025)", { depth: 347, slug: 1357, text: 1358 }, "v12---full-stack-integration-2025", "v1.2 - Full-Stack Integration (2025)", { depth: 347, slug: 1360, text: 1361 }, "v11---architecture-buildout-2025", "v1.1 - Architecture Buildout (2025)", { depth: 347, slug: 1363, text: 1334 }, "v00---project-inception-1", { depth: 29, slug: 625, text: 628 }, { depth: 347, slug: 1366, text: 1331 }, "v21---full-workbench-lift-april-2026-2", { depth: 347, slug: 1368, text: 1355 }, "v13---dependency-maintenance-2025-1", { depth: 347, slug: 1370, text: 1358 }, "v12---full-stack-integration-2025-1", { depth: 347, slug: 1372, text: 1361 }, "v11---architecture-buildout-2025-1", { depth: 347, slug: 1374, text: 1375 }, "v10---integration-phase-2025", "v1.0 - Integration Phase (2025)", { depth: 347, slug: 1377, text: 1378 }, "v02---architecture-solidification-2024", "v0.2 - Architecture Solidification (2024)", { depth: 347, slug: 1380, text: 1381 }, "v00---project-inception-2024", "v0.0 - Project Inception (2024)", { depth: 29, slug: 1383, text: 1384 }, "echo", "Echo", { depth: 347, slug: 1386, text: 1331 }, "v21---full-workbench-lift-april-2026-3", { depth: 347, slug: 1388, text: 1340 }, "v20---editor-launch-q1-2026-1", { depth: 347, slug: 1390, text: 1391 }, "v13---dependency-maintenance-q4-2025", "v1.3 - Dependency Maintenance (Q4 2025)", { depth: 347, slug: 1393, text: 1394 }, "v12---full-stack-integration-q3-2025", "v1.2 - Full-Stack Integration (Q3 2025)", { depth: 347, slug: 1396, text: 1397 }, "v11---architecture-buildout-june-2025", "v1.1 - Architecture Buildout (June 2025)", { depth: 347, slug: 1399, text: 1400 }, "v10---integration-phase-may-2025", "v1.0 - Integration Phase (May 2025)", { depth: 347, slug: 1402, text: 1403 }, "v00---project-inception-july-2024", "v0.0 - Project Inception (July 2024)", { depth: 29, slug: 1405, text: 1406 }, "grove", "Grove", { depth: 347, slug: 1408, text: 1331 }, "v21---full-workbench-lift-april-2026-4", { depth: 347, slug: 1410, text: 1340 }, "v20---editor-launch-q1-2026-2", { depth: 347, slug: 1412, text: 1413 }, "v00---project-inception-april-2025", "v0.0 - Project Inception (April 2025)", { depth: 29, slug: 1415, text: 58 }, "maintain", { depth: 347, slug: 1417, text: 1331 }, "v21---full-workbench-lift-april-2026-5", { depth: 347, slug: 1419, text: 1340 }, "v20---editor-launch-q1-2026-3", { depth: 347, slug: 1421, text: 1391 }, "v13---dependency-maintenance-q4-2025-1", { depth: 347, slug: 1423, text: 1394 }, "v12---full-stack-integration-q3-2025-1", { depth: 347, slug: 1425, text: 1426 }, "v11---architecture-buildout-may-2025", "v1.1 - Architecture Buildout (May 2025)", { depth: 347, slug: 1428, text: 1381 }, "v00---project-inception-2024-1", { depth: 29, slug: 1430, text: 1431 }, "mist", "Mist", { depth: 347, slug: 1433, text: 1331 }, "v21---full-workbench-lift-april-2026-6", { depth: 347, slug: 1435, text: 1436 }, "v20---editor-launch-q1-2026-dns-isolation-introduced-2026-02-27", "v2.0 - Editor Launch (Q1 2026: DNS Isolation Introduced, 2026-02-27)", { depth: 347, slug: 1438, text: 1413 }, "v00---project-inception-april-2025-1", { depth: 29, slug: 1440, text: 12 }, "mountain", { depth: 347, slug: 1442, text: 1328 }, "v22---bundled-electron-profile-correctness-pass-april-2026-2", { depth: 347, slug: 1444, text: 1331 }, "v21---full-workbench-lift-april-2026-7", { depth: 347, slug: 1446, text: 1340 }, "v20---editor-launch-q1-2026-4", { depth: 347, slug: 1448, text: 1355 }, "v13---dependency-maintenance-2025-2", { depth: 347, slug: 1450, text: 1358 }, "v12---full-stack-integration-2025-2", { depth: 347, slug: 1452, text: 1361 }, "v11---architecture-buildout-2025-2", { depth: 347, slug: 1454, text: 1375 }, "v10---integration-phase-2025-1", { depth: 347, slug: 1456, text: 1334 }, "v00---project-inception-2", { depth: 29, slug: 1458, text: 1459 }, "output", "Output", { depth: 347, slug: 1461, text: 1328 }, "v22---bundled-electron-profile-correctness-pass-april-2026-3", { depth: 347, slug: 1463, text: 1464 }, "v21---full-workbench-lift-polyfill-hardening-april-2026", "v2.1 - Full Workbench Lift, Polyfill Hardening (April 2026)", { depth: 347, slug: 1466, text: 1340 }, "v20---editor-launch-q1-2026-5", { depth: 347, slug: 1468, text: 1361 }, "v11---architecture-buildout-2025-3", { depth: 29, slug: 1470, text: 1471 }, "rest", "Rest", { depth: 347, slug: 1473, text: 1331 }, "v21---full-workbench-lift-april-2026-8", { depth: 347, slug: 1475, text: 1476 }, "v20---editor-launch-oxc-migration-march-2026", "v2.0 - Editor Launch (OXC Migration, March 2026)", { depth: 347, slug: 1478, text: 1479 }, "v10---integration-phase-q1-2025", "v1.0 - Integration Phase (Q1 2025)", { depth: 347, slug: 1481, text: 1482 }, "v00---project-inception-september-2024", "v0.0 - Project Inception (September 2024)", { depth: 29, slug: 1484, text: 1485 }, "sidecar", "SideCar", { depth: 347, slug: 1487, text: 1331 }, "v21---full-workbench-lift-april-2026-9", { depth: 347, slug: 1489, text: 1490 }, "v20---editor-launch-q1-2026-sidecar-born-2026-03-24", "v2.0 - Editor Launch (Q1 2026: SideCar Born, 2026-03-24)", { depth: 347, slug: 1492, text: 1493 }, "v00---project-inception-march-2026", "v0.0 - Project Inception (March 2026)", { depth: 29, slug: 1495, text: 1496 }, "sky", "Sky", { depth: 347, slug: 1498, text: 1328 }, "v22---bundled-electron-profile-correctness-pass-april-2026-4", { depth: 347, slug: 1500, text: 1501 }, "v21---full-workbench-lift-telemetry-bridges-bundle-profiles-april-2026", "v2.1 - Full Workbench Lift, Telemetry Bridges, Bundle Profiles (April 2026)", { depth: 347, slug: 1503, text: 1340 }, "v20---editor-launch-q1-2026-6", { depth: 347, slug: 1505, text: 1355 }, "v13---dependency-maintenance-2025-3", { depth: 347, slug: 1507, text: 1358 }, "v12---full-stack-integration-2025-3", { depth: 347, slug: 1509, text: 1361 }, "v11---architecture-buildout-2025-4", { depth: 347, slug: 1511, text: 1381 }, "v00---project-inception-2024-2", { depth: 29, slug: 1513, text: 1514 }, "vine", "Vine", { depth: 347, slug: 1516, text: 1331 }, "v21---full-workbench-lift-april-2026-10", { depth: 347, slug: 1518, text: 1340 }, "v20---editor-launch-q1-2026-7", { depth: 347, slug: 1520, text: 1413 }, "v00---project-inception-april-2025-2", { depth: 29, slug: 1522, text: 1523 }, "wind", "Wind", { depth: 347, slug: 1525, text: 1526 }, "v21---full-workbench-lift-tauri-ipc-bridge-april-2026", "v2.1 - Full Workbench Lift, Tauri IPC Bridge (April 2026)", { depth: 347, slug: 1528, text: 1340 }, "v20---editor-launch-q1-2026-8", { depth: 347, slug: 1530, text: 1355 }, "v13---dependency-maintenance-2025-4", { depth: 347, slug: 1532, text: 1358 }, "v12---full-stack-integration-2025-4", { depth: 347, slug: 1534, text: 1361 }, "v11---architecture-buildout-2025-5", { depth: 347, slug: 1536, text: 1381 }, "v00---project-inception-2024-3", { depth: 29, slug: 1538, text: 1539 }, "worker", "Worker", { depth: 347, slug: 1541, text: 1331 }, "v21---full-workbench-lift-april-2026-11", { depth: 347, slug: 1543, text: 1340 }, "v20---editor-launch-q1-2026-9", { depth: 347, slug: 1545, text: 1400 }, "v10---integration-phase-may-2025-1", { depth: 347, slug: 1547, text: 1413 }, "v00---project-inception-april-2025-3", [], [], { title: 1312, section: 493, order: 1313, description: 1314 }, [], { id: 1383, data: 1553, body: 1556, filePath: 1557, digest: 1558, rendered: 1559 }, { title: 1384, section: 381, order: 1554, description: 1555 }, 16, "The work-stealing task scheduler embedded inside Mountain's Rust binary.", "# Echo\n\n`Echo` is a work-stealing task scheduler written in `Rust`. It is not a separate\nprocess - it is embedded directly inside `Mountain`'s binary. When `Mountain`\nneeds to dispatch parallel work (file indexing, search, background jobs), it\nsubmits tasks to `Echo`'s worker pool rather than spawning child processes.\n\n`Echo` is built on [crossbeam-deque](https://github.com/crossbeam-rs/crossbeam)\nfor lock-free work stealing and [tokio](https://tokio.rs) for async I/O.\n\n---\n\n## What Work Stealing Means\n\nA work-stealing scheduler maintains a queue of pending tasks per worker thread.\nWhen a thread finishes its own queue, it steals tasks from the back of a busy\nthread's queue rather than waiting idle. This keeps all available CPU cores busy\nwithout a central dispatcher becoming a bottleneck.\n\nThe practical effect is that a batch of independent tasks - reading 200 files,\nrunning ripgrep over a large workspace, computing symbol indexes - completes\nfaster than sequential dispatch and uses available cores without requiring the\ncaller to manage thread lifetimes manually.\n\n---\n\n## How Echo Relates to Mountain\n\n`VS Code` dispatches background work (file indexing, symbol scanning, search)\nthrough its shared process and extension host, both of which are `Node.js`.\nHeavy batches compete with extension event handlers on the same event loop.\n\nBecause `Echo` runs inside `Mountain`'s `Rust` binary, background work\ndispatched through `Echo` runs on native threads outside the `Node.js` event\nloop entirely. `Cocoon`'s fiber scheduler and `Echo`'s worker pool are\nindependent - a saturated `Echo` pool does not delay `Cocoon`'s extension\nfibers, and a slow extension activation does not delay `Echo`'s background\ntasks.\n\n---\n\n## Source Structure 🗺️\n\n`Echo`'s source tree (confirmed in the\n[repository](https://github.com/CodeEditorLand/Echo)):\n\n| Path                | Role                                                         |\n| ------------------- | ------------------------------------------------------------ |\n| `Source/Library.rs` | Crate root - re-exports the public API                       |\n| `Source/Queue/`     | Lock-free deque implementation (work-stealing queues)        |\n| `Source/Scheduler/` | Scheduler core - thread pool management and task dispatch    |\n| `Source/Task/`      | Task wrapper types, supervision scopes, and panic boundaries |\n\n---\n\n## Current Status 🚀\n\n`Echo` is active inside `Mountain`'s binary on both macOS and Windows. The\nworker pool infrastructure runs in the `debug-mountain` build profile and is\npart of the standard release build.\n\n**Confirmed working:**\n\n- `Echo`'s worker pool initialises and runs inside `Mountain` on macOS (Apple\n  Silicon and Intel) and Windows 10/11.\n- File system operations dispatched through `Mountain` route through `Echo`'s\n  async runtime.\n- In-process file search runs through `Mountain`'s `grep-regex` +\n  `grep-searcher` integration - ripgrep-compatible search without spawning a\n  child process - dispatched through the `Echo` task layer.\n- Task supervision with panic-boundary scopes: a panicking task is caught at the\n  scope boundary and reported without taking down the `Mountain` process.\n- Graceful shutdown: `Mountain` signals `Echo`'s pool to drain before exit; no\n  task outlives its scope.\n\n**In Progress:**\n\n- Workspace-wide symbol indexing dispatched through `Echo`'s work-stealing pool\n  (infrastructure present; index computation routines being connected).\n- Build pipeline jobs submitted to `Echo` for parallel execution.\n\n---\n\n## Supervision and Shutdown\n\n`Echo` tasks run inside supervised scopes. Each task has a parent scope; if a\ntask panics, the panic is caught at the scope boundary and reported without\ntaking down the `Mountain` process. When the editor closes, `Mountain` signals\n`Echo`'s pool to drain - no task outlives its scope, which prevents orphaned\nthreads from holding file handles or sockets after shutdown.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Mountain`: Native Kernel](https://Editor.Land/Doc/mountain)\n- [Source Code](https://github.com/CodeEditorLand/Echo)", "Source/Content/doc/echo.md", "d04a3a2b8a43527a", { html: 1560, metadata: 1561 }, '<h1 id="echo">Echo</h1>\n<p><code>Echo</code> is a work-stealing task scheduler written in <code>Rust</code>. It is not a separate\nprocess - it is embedded directly inside <code>Mountain</code>’s binary. When <code>Mountain</code>\nneeds to dispatch parallel work (file indexing, search, background jobs), it\nsubmits tasks to <code>Echo</code>’s worker pool rather than spawning child processes.</p>\n<p><code>Echo</code> is built on <a href="https://github.com/crossbeam-rs/crossbeam">crossbeam-deque</a>\nfor lock-free work stealing and <a href="https://tokio.rs">tokio</a> for async I/O.</p>\n<hr>\n<h2 id="what-work-stealing-means">What Work Stealing Means</h2>\n<p>A work-stealing scheduler maintains a queue of pending tasks per worker thread.\nWhen a thread finishes its own queue, it steals tasks from the back of a busy\nthread’s queue rather than waiting idle. This keeps all available CPU cores busy\nwithout a central dispatcher becoming a bottleneck.</p>\n<p>The practical effect is that a batch of independent tasks - reading 200 files,\nrunning ripgrep over a large workspace, computing symbol indexes - completes\nfaster than sequential dispatch and uses available cores without requiring the\ncaller to manage thread lifetimes manually.</p>\n<hr>\n<h2 id="how-echo-relates-to-mountain">How Echo Relates to Mountain</h2>\n<p><code>VS Code</code> dispatches background work (file indexing, symbol scanning, search)\nthrough its shared process and extension host, both of which are <code>Node.js</code>.\nHeavy batches compete with extension event handlers on the same event loop.</p>\n<p>Because <code>Echo</code> runs inside <code>Mountain</code>’s <code>Rust</code> binary, background work\ndispatched through <code>Echo</code> runs on native threads outside the <code>Node.js</code> event\nloop entirely. <code>Cocoon</code>’s fiber scheduler and <code>Echo</code>’s worker pool are\nindependent - a saturated <code>Echo</code> pool does not delay <code>Cocoon</code>’s extension\nfibers, and a slow extension activation does not delay <code>Echo</code>’s background\ntasks.</p>\n<hr>\n<h2 id="source-structure️">Source Structure 🗺️</h2>\n<p><code>Echo</code>’s source tree (confirmed in the\n<a href="https://github.com/CodeEditorLand/Echo">repository</a>):</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Path</th><th>Role</th></tr></thead><tbody><tr><td><code>Source/Library.rs</code></td><td>Crate root - re-exports the public API</td></tr><tr><td><code>Source/Queue/</code></td><td>Lock-free deque implementation (work-stealing queues)</td></tr><tr><td><code>Source/Scheduler/</code></td><td>Scheduler core - thread pool management and task dispatch</td></tr><tr><td><code>Source/Task/</code></td><td>Task wrapper types, supervision scopes, and panic boundaries</td></tr></tbody></table>\n<hr>\n<h2 id="current-status">Current Status 🚀</h2>\n<p><code>Echo</code> is active inside <code>Mountain</code>’s binary on both macOS and Windows. The\nworker pool infrastructure runs in the <code>debug-mountain</code> build profile and is\npart of the standard release build.</p>\n<p><strong>Confirmed working:</strong></p>\n<ul>\n<li><code>Echo</code>’s worker pool initialises and runs inside <code>Mountain</code> on macOS (Apple\nSilicon and Intel) and Windows 10/11.</li>\n<li>File system operations dispatched through <code>Mountain</code> route through <code>Echo</code>’s\nasync runtime.</li>\n<li>In-process file search runs through <code>Mountain</code>’s <code>grep-regex</code> +\n<code>grep-searcher</code> integration - ripgrep-compatible search without spawning a\nchild process - dispatched through the <code>Echo</code> task layer.</li>\n<li>Task supervision with panic-boundary scopes: a panicking task is caught at the\nscope boundary and reported without taking down the <code>Mountain</code> process.</li>\n<li>Graceful shutdown: <code>Mountain</code> signals <code>Echo</code>’s pool to drain before exit; no\ntask outlives its scope.</li>\n</ul>\n<p><strong>In Progress:</strong></p>\n<ul>\n<li>Workspace-wide symbol indexing dispatched through <code>Echo</code>’s work-stealing pool\n(infrastructure present; index computation routines being connected).</li>\n<li>Build pipeline jobs submitted to <code>Echo</code> for parallel execution.</li>\n</ul>\n<hr>\n<h2 id="supervision-and-shutdown">Supervision and Shutdown</h2>\n<p><code>Echo</code> tasks run inside supervised scopes. Each task has a parent scope; if a\ntask panics, the panic is caught at the scope boundary and reported without\ntaking down the <code>Mountain</code> process. When the editor closes, <code>Mountain</code> signals\n<code>Echo</code>’s pool to drain - no task outlives its scope, which prevents orphaned\nthreads from holding file handles or sockets after shutdown.</p>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/mountain"><code>Mountain</code>: Native Kernel</a></li>\n<li><a href="https://github.com/CodeEditorLand/Echo">Source Code</a></li>\n</ul>', { headings: 1562, localImagePaths: 1578, remoteImagePaths: 1579, frontmatter: 1580, imagePaths: 1581 }, [1563, 1564, 1567, 1570, 1571, 1574, 1577], { depth: 25, slug: 1383, text: 1384 }, { depth: 29, slug: 1565, text: 1566 }, "what-work-stealing-means", "What Work Stealing Means", { depth: 29, slug: 1568, text: 1569 }, "how-echo-relates-to-mountain", "How Echo Relates to Mountain", { depth: 29, slug: 393, text: 394 }, { depth: 29, slug: 1572, text: 1573 }, "current-status", "Current Status 🚀", { depth: 29, slug: 1575, text: 1576 }, "supervision-and-shutdown", "Supervision and Shutdown", { depth: 29, slug: 401, text: 402 }, [], [], { title: 1384, section: 381, order: 1554, description: 1555 }, [], "extension-development", { id: 1582, data: 1584, body: 1587, filePath: 1588, digest: 1589, rendered: 1590 }, { title: 1585, section: 411, order: 59, description: 1586 }, "Extension Development", "Build and test extensions for Editor.Land using the VS Code extension toolchain.", "# Extension Development\n\n`Editor.Land` runs `VS Code` extensions through `Cocoon`, the `Node.js`\nextension host. If you have built a `VS Code` extension before, the scaffolding,\nstructure, `TypeScript` compilation, and packaging steps are identical. This\nguide covers the workflow from scaffolding to distribution, with clear notes on\nwhat is confirmed working in Land today versus what is planned.\n\n---\n\n## Scaffold a New Extension\n\nUse the Yeoman generator to create the project structure:\n\n```bash\nnpm install -g yo generator-code\nyo code\n```\n\nSelect **New Extension (`TypeScript`)** and fill in the prompts. The generator\ncreates a directory with `package.json`, `tsconfig.json`, and a `src/` folder\ncontaining an `Extension.ts` entry point. This step is entirely independent of\nLand - it uses the standard `VS Code` extension scaffolding toolchain.\n\n---\n\n## Project Structure\n\n```\nMyExtension/\n  package.json          # Manifest: commands, configuration, keybindings\n  tsconfig.json         # TypeScript configuration\n  src/\n    Extension.ts        # activate() and deactivate() entry points\n  test/\n    Extension.test.ts   # Integration tests\n```\n\nThe `main` field in `package.json` points to the compiled JavaScript output\n(typically `out/Extension.js`). `Cocoon` reads `package.json` at activation time\nto discover contributed commands, configuration, and activation events.\n\n---\n\n## Loading an Extension in Development\n\nTo load your extension into a running Land instance from source:\n\n```bash\ncargo tauri dev -- --extensionDevelopmentPath=/path/to/MyExtension\n```\n\nThe `--extensionDevelopmentPath` flag instructs `Cocoon` to load the extension\nat the specified path in addition to any normally discovered extensions. Whether\nthis flag is fully wired through `Tauri`'s CLI argument passing to `Cocoon` has\nnot been independently confirmed. If the flag does not take effect, place the\nextension directory in the location `Cocoon` discovers extensions from and\nrestart the editor.\n\nFor `TypeScript` changes to take effect, run `tsc --watch` in a separate\nterminal alongside `cargo tauri dev`.\n\n---\n\n## Testing\n\n**Unit tests** that do not require the editor runtime can use any test runner\ndirectly:\n\n```bash\npnpx vitest\n# or\nnode --test\n```\n\nThis is the recommended approach for testing pure extension logic (parsers,\nformatters, data transformations).\n\n**Integration tests** that require the editor runtime use\n`@vscode/test-electron` in the `VS Code` ecosystem. Using this package with Land\nrequires configuring it to point at Land's binary rather than `VS Code`'s\n`Electron` binary - this configuration is not yet documented for Land\nspecifically. Until it is, use unit tests for logic and manual testing in the\ndevelopment build for editor integration.\n\n---\n\n## Package\n\nBundle your extension into a `.vsix` file for distribution:\n\n```bash\nnpm install -g @vscode/vsce\nvsce package\n```\n\nThis produces `MyExtension-0.0.1.vsix`. The `.vsix` format is the same as VS\nCode's. An extension packaged this way can be installed in Land manually via the\ncommand palette (**Extensions: Install from VSIX**), subject to that command\nbeing available in the current build.\n\n---\n\n## Distribution and Publishing\n\nThere is no marketplace or extension registry integration in Land today. The\nextensions panel UI and Open VSX Registry browsing are not yet implemented.\n\nDistribution options today:\n\n- **Share the `.vsix` file directly** - users install it via the command palette\n  or by placing it in the extensions directory.\n- **Publish to Open VSX** - the Open VSX Registry\n  ([open-vsx.org](https://open-vsx.org)) is the planned default registry for\n  Land. Publishing there now means your extension is available when marketplace\n  integration is implemented. The publish workflow:\n    ```bash\n    npx ovsx publish MyExtension-0.0.1.vsix -p YOUR_TOKEN\n    ```\n    Publishing to Open VSX does not make the extension available to Land users\n    through any in-editor browser today - there is no in-editor extension\n    browser in the current build.\n\n---\n\n## API Coverage\n\nThe following `API` surfaces are implemented in `Cocoon` and work reliably for\nextension use today:\n\n- `vscode.commands` - register, execute, command palette\n- `vscode.workspace.fs` - file read/write via `Mountain`'s FS layer\n- `vscode.window.createTerminal` - pty via `Mountain`\n- `vscode.debug` - DAP bridge via `Mountain`\n- `vscode.languages` - LSP client via `vscode-languageclient`\n- `vscode.workspace.getConfiguration` - settings read\n\nThe following are **not implemented** - extensions using them compile but their\nfeatures silently no-op at runtime:\n\n- `vscode.lm.*` - language model / Copilot\n- `vscode.chat.*` - chat panel\n- `vscode.notebook.*` - notebook documents and editors\n- `vscode.tests.*` - test explorer and runner\n\nSee [`API` Reference](https://Editor.Land/Doc/api-reference) for the full\ncoverage table.\n\n---\n\n## See Also\n\n- [`API` Reference](https://Editor.Land/Doc/api-reference)\n- [`Cocoon`: Extension Host](https://Editor.Land/Doc/cocoon)\n- [Contributing](https://Editor.Land/Doc/contributing)", "Source/Content/doc/extension-development.md", "80afcaadc41d1685", { html: 1591, metadata: 1592 }, '<h1 id="extension-development">Extension Development</h1>\n<p><code>Editor.Land</code> runs <code>VS Code</code> extensions through <code>Cocoon</code>, the <code>Node.js</code>\nextension host. If you have built a <code>VS Code</code> extension before, the scaffolding,\nstructure, <code>TypeScript</code> compilation, and packaging steps are identical. This\nguide covers the workflow from scaffolding to distribution, with clear notes on\nwhat is confirmed working in Land today versus what is planned.</p>\n<hr>\n<h2 id="scaffold-a-new-extension">Scaffold a New Extension</h2>\n<p>Use the Yeoman generator to create the project structure:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">npm</span><span style="color:#9ECBFF"> install</span><span style="color:#79B8FF"> -g</span><span style="color:#9ECBFF"> yo</span><span style="color:#9ECBFF"> generator-code</span></span>\n<span class="line"><span style="color:#B392F0">yo</span><span style="color:#9ECBFF"> code</span></span></code></pre>\n<p>Select <strong>New Extension (<code>TypeScript</code>)</strong> and fill in the prompts. The generator\ncreates a directory with <code>package.json</code>, <code>tsconfig.json</code>, and a <code>src/</code> folder\ncontaining an <code>Extension.ts</code> entry point. This step is entirely independent of\nLand - it uses the standard <code>VS Code</code> extension scaffolding toolchain.</p>\n<hr>\n<h2 id="project-structure">Project Structure</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>MyExtension/</span></span>\n<span class="line"><span>  package.json          # Manifest: commands, configuration, keybindings</span></span>\n<span class="line"><span>  tsconfig.json         # TypeScript configuration</span></span>\n<span class="line"><span>  src/</span></span>\n<span class="line"><span>    Extension.ts        # activate() and deactivate() entry points</span></span>\n<span class="line"><span>  test/</span></span>\n<span class="line"><span>    Extension.test.ts   # Integration tests</span></span></code></pre>\n<p>The <code>main</code> field in <code>package.json</code> points to the compiled JavaScript output\n(typically <code>out/Extension.js</code>). <code>Cocoon</code> reads <code>package.json</code> at activation time\nto discover contributed commands, configuration, and activation events.</p>\n<hr>\n<h2 id="loading-an-extension-in-development">Loading an Extension in Development</h2>\n<p>To load your extension into a running Land instance from source:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">cargo</span><span style="color:#9ECBFF"> tauri</span><span style="color:#9ECBFF"> dev</span><span style="color:#79B8FF"> --</span><span style="color:#79B8FF"> --extensionDevelopmentPath=/path/to/MyExtension</span></span></code></pre>\n<p>The <code>--extensionDevelopmentPath</code> flag instructs <code>Cocoon</code> to load the extension\nat the specified path in addition to any normally discovered extensions. Whether\nthis flag is fully wired through <code>Tauri</code>’s CLI argument passing to <code>Cocoon</code> has\nnot been independently confirmed. If the flag does not take effect, place the\nextension directory in the location <code>Cocoon</code> discovers extensions from and\nrestart the editor.</p>\n<p>For <code>TypeScript</code> changes to take effect, run <code>tsc --watch</code> in a separate\nterminal alongside <code>cargo tauri dev</code>.</p>\n<hr>\n<h2 id="testing">Testing</h2>\n<p><strong>Unit tests</strong> that do not require the editor runtime can use any test runner\ndirectly:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">pnpx</span><span style="color:#9ECBFF"> vitest</span></span>\n<span class="line"><span style="color:#6A737D"># or</span></span>\n<span class="line"><span style="color:#B392F0">node</span><span style="color:#79B8FF"> --test</span></span></code></pre>\n<p>This is the recommended approach for testing pure extension logic (parsers,\nformatters, data transformations).</p>\n<p><strong>Integration tests</strong> that require the editor runtime use\n<code>@vscode/test-electron</code> in the <code>VS Code</code> ecosystem. Using this package with Land\nrequires configuring it to point at Land’s binary rather than <code>VS Code</code>’s\n<code>Electron</code> binary - this configuration is not yet documented for Land\nspecifically. Until it is, use unit tests for logic and manual testing in the\ndevelopment build for editor integration.</p>\n<hr>\n<h2 id="package">Package</h2>\n<p>Bundle your extension into a <code>.vsix</code> file for distribution:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">npm</span><span style="color:#9ECBFF"> install</span><span style="color:#79B8FF"> -g</span><span style="color:#9ECBFF"> @vscode/vsce</span></span>\n<span class="line"><span style="color:#B392F0">vsce</span><span style="color:#9ECBFF"> package</span></span></code></pre>\n<p>This produces <code>MyExtension-0.0.1.vsix</code>. The <code>.vsix</code> format is the same as VS\nCode’s. An extension packaged this way can be installed in Land manually via the\ncommand palette (<strong>Extensions: Install from VSIX</strong>), subject to that command\nbeing available in the current build.</p>\n<hr>\n<h2 id="distribution-and-publishing">Distribution and Publishing</h2>\n<p>There is no marketplace or extension registry integration in Land today. The\nextensions panel UI and Open VSX Registry browsing are not yet implemented.</p>\n<p>Distribution options today:</p>\n<ul>\n<li><strong>Share the <code>.vsix</code> file directly</strong> - users install it via the command palette\nor by placing it in the extensions directory.</li>\n<li><strong>Publish to Open VSX</strong> - the Open VSX Registry\n(<a href="https://open-vsx.org">open-vsx.org</a>) is the planned default registry for\nLand. Publishing there now means your extension is available when marketplace\nintegration is implemented. The publish workflow:\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">npx</span><span style="color:#9ECBFF"> ovsx</span><span style="color:#9ECBFF"> publish</span><span style="color:#9ECBFF"> MyExtension-0.0.1.vsix</span><span style="color:#79B8FF"> -p</span><span style="color:#9ECBFF"> YOUR_TOKEN</span></span></code></pre>\nPublishing to Open VSX does not make the extension available to Land users\nthrough any in-editor browser today - there is no in-editor extension\nbrowser in the current build.</li>\n</ul>\n<hr>\n<h2 id="api-coverage">API Coverage</h2>\n<p>The following <code>API</code> surfaces are implemented in <code>Cocoon</code> and work reliably for\nextension use today:</p>\n<ul>\n<li><code>vscode.commands</code> - register, execute, command palette</li>\n<li><code>vscode.workspace.fs</code> - file read/write via <code>Mountain</code>’s FS layer</li>\n<li><code>vscode.window.createTerminal</code> - pty via <code>Mountain</code></li>\n<li><code>vscode.debug</code> - DAP bridge via <code>Mountain</code></li>\n<li><code>vscode.languages</code> - LSP client via <code>vscode-languageclient</code></li>\n<li><code>vscode.workspace.getConfiguration</code> - settings read</li>\n</ul>\n<p>The following are <strong>not implemented</strong> - extensions using them compile but their\nfeatures silently no-op at runtime:</p>\n<ul>\n<li><code>vscode.lm.*</code> - language model / Copilot</li>\n<li><code>vscode.chat.*</code> - chat panel</li>\n<li><code>vscode.notebook.*</code> - notebook documents and editors</li>\n<li><code>vscode.tests.*</code> - test explorer and runner</li>\n</ul>\n<p>See <a href="https://Editor.Land/Doc/api-reference"><code>API</code> Reference</a> for the full\ncoverage table.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/api-reference"><code>API</code> Reference</a></li>\n<li><a href="https://Editor.Land/Doc/cocoon"><code>Cocoon</code>: Extension Host</a></li>\n<li><a href="https://Editor.Land/Doc/contributing">Contributing</a></li>\n</ul>', { headings: 1593, localImagePaths: 1617, remoteImagePaths: 1618, frontmatter: 1619, imagePaths: 1620 }, [1594, 1595, 1598, 1601, 1604, 1607, 1610, 1613, 1616], { depth: 25, slug: 1582, text: 1585 }, { depth: 29, slug: 1596, text: 1597 }, "scaffold-a-new-extension", "Scaffold a New Extension", { depth: 29, slug: 1599, text: 1600 }, "project-structure", "Project Structure", { depth: 29, slug: 1602, text: 1603 }, "loading-an-extension-in-development", "Loading an Extension in Development", { depth: 29, slug: 1605, text: 1606 }, "testing", "Testing", { depth: 29, slug: 1608, text: 1609 }, "package", "Package", { depth: 29, slug: 1611, text: 1612 }, "distribution-and-publishing", "Distribution and Publishing", { depth: 29, slug: 1614, text: 1615 }, "api-coverage", "API Coverage", { depth: 29, slug: 446, text: 447 }, [], [], { title: 1585, section: 411, order: 59, description: 1586 }, [], "getting-started", { id: 1621, data: 1623, body: 1627, filePath: 1628, digest: 1629, rendered: 1630 }, { title: 1624, section: 1625, order: 25, description: 1626 }, "Getting Started", "Start", "How to build and run Editor.Land from source on macOS or Windows.", "# Getting Started\n\n> **Updated 2026-05-29** - Build process updated to Node 24 and the\n> `Maintain/Debug/Build.sh` profile system. Canonical source:\n> [Documentation/GitHub/Building.md](https://github.com/CodeEditorLand/Land/tree/Current/Documentation/GitHub/Building.md)\n\n`Editor.Land` is in active development and is currently source-only. There are\nno pre-built installers or package manager releases yet. The supported way to\nrun the editor today is to build from source on **macOS or Windows**.\n\n---\n\n## What Works Today\n\nOn Apple Silicon or Intel macOS (13.0 Ventura or later) and on Windows 10/11, a\nsuccessful build produces a working editor that:\n\n- Opens a native window with the `workbench` UI (`WKWebView` on macOS,\n  `WebView2` on Windows).\n- Loads `VS Code` extensions from disk and activates them through `Cocoon`, the\n  `Node.js` extension host.\n- Provides file system, terminal, and debug adapter access through `Mountain`,\n  the `Rust` kernel.\n- Runs the full `gRPC`-based `IPC` stack between `Mountain` and `Cocoon` via\n  `Vine`.\n\nThe following are **in progress**: a Homebrew tap, a winget package, an apt\nrepository, and an automated update mechanism.\n\n---\n\n## Build Requirements\n\nAll of the following must be installed before building:\n\n- **`Rust`** nightly (install via [rustup](https://rustup.rs), then\n  `rustup default nightly`)\n- **`Node.js`** 24 - required to compile the VS Code Editor submodule (version\n  pinned in `Dependency/Microsoft/Dependency/Editor/.nvmrc`)\n- **`nvm`** - recommended for switching Node versions\n  ([nvm-sh/nvm](https://github.com/nvm-sh/nvm))\n- **`pnpm`** 9 or later (`npm install -g pnpm`)\n- **macOS:** Xcode Command Line Tools (`xcode-select --install`), macOS 13.0\n  (Ventura) or later\n- **Windows:** `WebView2` Runtime (included with Windows 11; available\n  separately for Windows 10), Visual Studio Build Tools with the C++ workload\n\n---\n\n## Building from Source\n\nThe build is a **two-step linear flow.** Do not pull submodules recursively -\neach submodule is managed independently on its own branch.\n\n### Step 1 - Compile the VS Code Editor submodule _(mandatory)_\n\n`Cocoon` (the extension host) and `Output` (the platform bundle) both depend on\nthe compiled output of the VS Code Editor submodule. This step must complete\nbefore Step 2 will succeed.\n\n```bash\ncd Dependency/Microsoft/Dependency/Editor\n\n# Node 24 is required here - reads .nvmrc automatically\nnvm use 24\n\ngit fetch --all\ngit reset --hard Parent/main\ngit clean -dfx\n\npnpm install\npnpm run compile\npnpm run compile-extensions-build\n```\n\n### Step 2 - Build the Land application\n\n```bash\ncd Land # back to repository root\n./Maintain/Debug/Build.sh --profile debug-electron-bundled\n```\n\nThe first build takes several minutes because it compiles `Mountain`'s `Rust`\ndependencies from scratch. Subsequent builds are significantly faster due to\n`Cargo`'s incremental compilation.\n\nFor a quick iteration build without the full bundling pass:\n\n```bash\n./Maintain/Debug/Build.sh --profile debug-mountain\n```\n\nSee\n[BuildPipeline.md](https://github.com/CodeEditorLand/Land/tree/Current/Documentation/GitHub/BuildPipeline.md)\nfor the full profile matrix.\n\n---\n\n## Extension Loading\n\n`Cocoon` discovers extensions from the filesystem at startup. The exact\nextension discovery path in the current build has not been independently\nconfirmed to match `VS Code`'s `~/.vscode/extensions` directory exactly. If an\nextension you expect to appear does not load, check the `Cocoon` output channel\nin the editor's `Output` panel for activation errors.\n\n---\n\n## Known Limitations\n\n- **Linux not yet supported.** Linux (`WebKitGTK`) is planned and in progress.\n- **No marketplace integration.** Extensions must be installed manually as\n  `.vsix` files or sourced from disk. Marketplace `API` access is not yet\n  implemented.\n- **`API` gaps.** `vscode.lm.*`, `vscode.chat.*`, `vscode.notebook.*`, and\n  `vscode.tests.*` are not implemented. Extensions using these `APIs` activate\n  but the specific features silently no-op.\n\n---\n\n## See Also\n\n- [Installation](https://Editor.Land/Doc/installation)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Cocoon`: Extension Host](https://Editor.Land/Doc/cocoon)\n- [`Mountain`: Native Kernel](https://Editor.Land/Doc/mountain)", "Source/Content/doc/getting-started.md", "0909445155128117", { html: 1631, metadata: 1632 }, '<h1 id="getting-started">Getting Started</h1>\n<blockquote>\n<p><strong>Updated 2026-05-29</strong> - Build process updated to Node 24 and the\n<code>Maintain/Debug/Build.sh</code> profile system. Canonical source:\n<a href="https://github.com/CodeEditorLand/Land/tree/Current/Documentation/GitHub/Building.md">Documentation/GitHub/Building.md</a></p>\n</blockquote>\n<p><code>Editor.Land</code> is in active development and is currently source-only. There are\nno pre-built installers or package manager releases yet. The supported way to\nrun the editor today is to build from source on <strong>macOS or Windows</strong>.</p>\n<hr>\n<h2 id="what-works-today">What Works Today</h2>\n<p>On Apple Silicon or Intel macOS (13.0 Ventura or later) and on Windows 10/11, a\nsuccessful build produces a working editor that:</p>\n<ul>\n<li>Opens a native window with the <code>workbench</code> UI (<code>WKWebView</code> on macOS,\n<code>WebView2</code> on Windows).</li>\n<li>Loads <code>VS Code</code> extensions from disk and activates them through <code>Cocoon</code>, the\n<code>Node.js</code> extension host.</li>\n<li>Provides file system, terminal, and debug adapter access through <code>Mountain</code>,\nthe <code>Rust</code> kernel.</li>\n<li>Runs the full <code>gRPC</code>-based <code>IPC</code> stack between <code>Mountain</code> and <code>Cocoon</code> via\n<code>Vine</code>.</li>\n</ul>\n<p>The following are <strong>in progress</strong>: a Homebrew tap, a winget package, an apt\nrepository, and an automated update mechanism.</p>\n<hr>\n<h2 id="build-requirements">Build Requirements</h2>\n<p>All of the following must be installed before building:</p>\n<ul>\n<li><strong><code>Rust</code></strong> nightly (install via <a href="https://rustup.rs">rustup</a>, then\n<code>rustup default nightly</code>)</li>\n<li><strong><code>Node.js</code></strong> 24 - required to compile the VS Code Editor submodule (version\npinned in <code>Dependency/Microsoft/Dependency/Editor/.nvmrc</code>)</li>\n<li><strong><code>nvm</code></strong> - recommended for switching Node versions\n(<a href="https://github.com/nvm-sh/nvm">nvm-sh/nvm</a>)</li>\n<li><strong><code>pnpm</code></strong> 9 or later (<code>npm install -g pnpm</code>)</li>\n<li><strong>macOS:</strong> Xcode Command Line Tools (<code>xcode-select --install</code>), macOS 13.0\n(Ventura) or later</li>\n<li><strong>Windows:</strong> <code>WebView2</code> Runtime (included with Windows 11; available\nseparately for Windows 10), Visual Studio Build Tools with the C++ workload</li>\n</ul>\n<hr>\n<h2 id="building-from-source">Building from Source</h2>\n<p>The build is a <strong>two-step linear flow.</strong> Do not pull submodules recursively -\neach submodule is managed independently on its own branch.</p>\n<h3 id="step-1---compile-the-vs-code-editor-submodule-mandatory">Step 1 - Compile the VS Code Editor submodule <em>(mandatory)</em></h3>\n<p><code>Cocoon</code> (the extension host) and <code>Output</code> (the platform bundle) both depend on\nthe compiled output of the VS Code Editor submodule. This step must complete\nbefore Step 2 will succeed.</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#79B8FF">cd</span><span style="color:#9ECBFF"> Dependency/Microsoft/Dependency/Editor</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#6A737D"># Node 24 is required here - reads .nvmrc automatically</span></span>\n<span class="line"><span style="color:#B392F0">nvm</span><span style="color:#9ECBFF"> use</span><span style="color:#79B8FF"> 24</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> fetch</span><span style="color:#79B8FF"> --all</span></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> reset</span><span style="color:#79B8FF"> --hard</span><span style="color:#9ECBFF"> Parent/main</span></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> clean</span><span style="color:#79B8FF"> -dfx</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> install</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> run</span><span style="color:#9ECBFF"> compile</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> run</span><span style="color:#9ECBFF"> compile-extensions-build</span></span></code></pre>\n<h3 id="step-2---build-the-land-application">Step 2 - Build the Land application</h3>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#79B8FF">cd</span><span style="color:#9ECBFF"> Land</span><span style="color:#6A737D"> # back to repository root</span></span>\n<span class="line"><span style="color:#B392F0">./Maintain/Debug/Build.sh</span><span style="color:#79B8FF"> --profile</span><span style="color:#9ECBFF"> debug-electron-bundled</span></span></code></pre>\n<p>The first build takes several minutes because it compiles <code>Mountain</code>’s <code>Rust</code>\ndependencies from scratch. Subsequent builds are significantly faster due to\n<code>Cargo</code>’s incremental compilation.</p>\n<p>For a quick iteration build without the full bundling pass:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">./Maintain/Debug/Build.sh</span><span style="color:#79B8FF"> --profile</span><span style="color:#9ECBFF"> debug-mountain</span></span></code></pre>\n<p>See\n<a href="https://github.com/CodeEditorLand/Land/tree/Current/Documentation/GitHub/BuildPipeline.md">BuildPipeline.md</a>\nfor the full profile matrix.</p>\n<hr>\n<h2 id="extension-loading">Extension Loading</h2>\n<p><code>Cocoon</code> discovers extensions from the filesystem at startup. The exact\nextension discovery path in the current build has not been independently\nconfirmed to match <code>VS Code</code>’s <code>~/.vscode/extensions</code> directory exactly. If an\nextension you expect to appear does not load, check the <code>Cocoon</code> output channel\nin the editor’s <code>Output</code> panel for activation errors.</p>\n<hr>\n<h2 id="known-limitations">Known Limitations</h2>\n<ul>\n<li><strong>Linux not yet supported.</strong> Linux (<code>WebKitGTK</code>) is planned and in progress.</li>\n<li><strong>No marketplace integration.</strong> Extensions must be installed manually as\n<code>.vsix</code> files or sourced from disk. Marketplace <code>API</code> access is not yet\nimplemented.</li>\n<li><strong><code>API</code> gaps.</strong> <code>vscode.lm.*</code>, <code>vscode.chat.*</code>, <code>vscode.notebook.*</code>, and\n<code>vscode.tests.*</code> are not implemented. Extensions using these <code>APIs</code> activate\nbut the specific features silently no-op.</li>\n</ul>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/installation">Installation</a></li>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/cocoon"><code>Cocoon</code>: Extension Host</a></li>\n<li><a href="https://Editor.Land/Doc/mountain"><code>Mountain</code>: Native Kernel</a></li>\n</ul>', { headings: 1633, localImagePaths: 1657, remoteImagePaths: 1658, frontmatter: 1659, imagePaths: 1660 }, [1634, 1635, 1638, 1641, 1644, 1647, 1650, 1653, 1656], { depth: 25, slug: 1621, text: 1624 }, { depth: 29, slug: 1636, text: 1637 }, "what-works-today", "What Works Today", { depth: 29, slug: 1639, text: 1640 }, "build-requirements", "Build Requirements", { depth: 29, slug: 1642, text: 1643 }, "building-from-source", "Building from Source", { depth: 347, slug: 1645, text: 1646 }, "step-1---compile-the-vs-code-editor-submodule-mandatory", "Step 1 - Compile the VS Code Editor submodule (mandatory)", { depth: 347, slug: 1648, text: 1649 }, "step-2---build-the-land-application", "Step 2 - Build the Land application", { depth: 29, slug: 1651, text: 1652 }, "extension-loading", "Extension Loading", { depth: 29, slug: 1654, text: 1655 }, "known-limitations", "Known Limitations", { depth: 29, slug: 446, text: 447 }, [], [], { title: 1624, section: 1625, order: 25, description: 1626 }, [], { id: 1405, data: 1662, body: 1665, filePath: 1666, digest: 1667, rendered: 1668 }, { title: 1406, section: 381, order: 1663, description: 1664 }, 18, "The Wasmtime extension-host path - capability-based security for Editor.Land extensions.", "# Grove\n\n`Grove` is the `WebAssembly` extension-host path for `Editor.Land`. Where\n`Cocoon` runs `VS Code` extensions in a `Node.js` process, `Grove` contains a\nWasmtime-backed path for extensions compiled to `WebAssembly` with\ncapability-oriented boundaries.\n\n`Grove` is WIP in the primary editor flow. The `gRPC` protocol definitions\n(`Proto/`), Wasmtime host (`Source/Host/`), `API` surface (`Source/API/`),\ntransport layer (`Source/Transport/`), service registrations\n(`Source/Services/`), and `WASM` runtime integration (`Source/WASM/`) are\npresent in source. Full integration with the primary `debug-mountain` build is\nstill in progress.\n\n---\n\n## The Problem Grove Solves\n\n`VS Code` extensions run with broad `Node.js` capabilities in a shared process.\n`Grove` is the path for extensions that can trade raw Node compatibility for a\nruntime boundary with explicit capabilities.\n\n---\n\n## How Grove Addresses It\n\n`Grove` uses Wasmtime as its `WebAssembly` runtime. The host can grant specific\nresources to a module instead of giving ambient access to the whole system. That\nmodel is useful for a future extension marketplace where permissions can be\nshown to users before code runs.\n\nThis is real source, but it is not the current compatibility story for existing\n`VS Code` extensions. `Cocoon` remains the active unmodified-extension path.\n\n---\n\n## Source Structure 🗺️\n\nConfirmed present in the `Grove` source tree:\n\n| Path                | Role                                                  |\n| ------------------- | ----------------------------------------------------- |\n| `Source/API/`       | Public API surface exposed to hosted extensions       |\n| `Source/Binary/`    | Binary entry point                                    |\n| `Source/Common/`    | Shared types and utilities                            |\n| `Source/Host/`      | Wasmtime host implementation                          |\n| `Source/Protocol/`  | Protocol definitions for host-guest communication     |\n| `Source/Services/`  | Service registrations inside the sandbox              |\n| `Source/Transport/` | Transport layer for IPC between Grove and Mountain    |\n| `Source/WASM/`      | WASM runtime integration                              |\n| `Proto/`            | gRPC `.proto` definitions for Grove-Mountain protocol |\n| `Tests/`            | Integration tests                                     |\n| `build.rs`          | Cargo build script                                    |\n\n---\n\n## What Grove Enables\n\nWhen `Grove` is fully integrated, an extension marketplace with stronger\nsecurity guarantees becomes possible. Users should be able to see what resources\nan extension can access, and `Grove` can enforce that through Wasmtime and\nexplicit capability grants.\n\n---\n\n## Status 🚀\n\n`Grove` should be shown as `WIP` in product copy, not as a shipped replacement\nfor `Cocoon`. It is source-backed work in progress.\n\n---\n\n## Key Technologies\n\n`Rust`, Wasmtime, `WebAssembly`, `gRPC`, Capability-Based Security.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Cocoon`: Extension Host](https://Editor.Land/Doc/cocoon)\n- [Source Code](https://github.com/CodeEditorLand/Grove)", "Source/Content/doc/grove.md", "4552784b6602e302", { html: 1669, metadata: 1670 }, '<h1 id="grove">Grove</h1>\n<p><code>Grove</code> is the <code>WebAssembly</code> extension-host path for <code>Editor.Land</code>. Where\n<code>Cocoon</code> runs <code>VS Code</code> extensions in a <code>Node.js</code> process, <code>Grove</code> contains a\nWasmtime-backed path for extensions compiled to <code>WebAssembly</code> with\ncapability-oriented boundaries.</p>\n<p><code>Grove</code> is WIP in the primary editor flow. The <code>gRPC</code> protocol definitions\n(<code>Proto/</code>), Wasmtime host (<code>Source/Host/</code>), <code>API</code> surface (<code>Source/API/</code>),\ntransport layer (<code>Source/Transport/</code>), service registrations\n(<code>Source/Services/</code>), and <code>WASM</code> runtime integration (<code>Source/WASM/</code>) are\npresent in source. Full integration with the primary <code>debug-mountain</code> build is\nstill in progress.</p>\n<hr>\n<h2 id="the-problem-grove-solves">The Problem Grove Solves</h2>\n<p><code>VS Code</code> extensions run with broad <code>Node.js</code> capabilities in a shared process.\n<code>Grove</code> is the path for extensions that can trade raw Node compatibility for a\nruntime boundary with explicit capabilities.</p>\n<hr>\n<h2 id="how-grove-addresses-it">How Grove Addresses It</h2>\n<p><code>Grove</code> uses Wasmtime as its <code>WebAssembly</code> runtime. The host can grant specific\nresources to a module instead of giving ambient access to the whole system. That\nmodel is useful for a future extension marketplace where permissions can be\nshown to users before code runs.</p>\n<p>This is real source, but it is not the current compatibility story for existing\n<code>VS Code</code> extensions. <code>Cocoon</code> remains the active unmodified-extension path.</p>\n<hr>\n<h2 id="source-structure️">Source Structure 🗺️</h2>\n<p>Confirmed present in the <code>Grove</code> source tree:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Path</th><th>Role</th></tr></thead><tbody><tr><td><code>Source/API/</code></td><td>Public API surface exposed to hosted extensions</td></tr><tr><td><code>Source/Binary/</code></td><td>Binary entry point</td></tr><tr><td><code>Source/Common/</code></td><td>Shared types and utilities</td></tr><tr><td><code>Source/Host/</code></td><td>Wasmtime host implementation</td></tr><tr><td><code>Source/Protocol/</code></td><td>Protocol definitions for host-guest communication</td></tr><tr><td><code>Source/Services/</code></td><td>Service registrations inside the sandbox</td></tr><tr><td><code>Source/Transport/</code></td><td>Transport layer for IPC between Grove and Mountain</td></tr><tr><td><code>Source/WASM/</code></td><td>WASM runtime integration</td></tr><tr><td><code>Proto/</code></td><td>gRPC <code>.proto</code> definitions for Grove-Mountain protocol</td></tr><tr><td><code>Tests/</code></td><td>Integration tests</td></tr><tr><td><code>build.rs</code></td><td>Cargo build script</td></tr></tbody></table>\n<hr>\n<h2 id="what-grove-enables">What Grove Enables</h2>\n<p>When <code>Grove</code> is fully integrated, an extension marketplace with stronger\nsecurity guarantees becomes possible. Users should be able to see what resources\nan extension can access, and <code>Grove</code> can enforce that through Wasmtime and\nexplicit capability grants.</p>\n<hr>\n<h2 id="status">Status 🚀</h2>\n<p><code>Grove</code> should be shown as <code>WIP</code> in product copy, not as a shipped replacement\nfor <code>Cocoon</code>. It is source-backed work in progress.</p>\n<hr>\n<h2 id="key-technologies">Key Technologies</h2>\n<p><code>Rust</code>, Wasmtime, <code>WebAssembly</code>, <code>gRPC</code>, Capability-Based Security.</p>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/cocoon"><code>Cocoon</code>: Extension Host</a></li>\n<li><a href="https://github.com/CodeEditorLand/Grove">Source Code</a></li>\n</ul>', { headings: 1671, localImagePaths: 1688, remoteImagePaths: 1689, frontmatter: 1690, imagePaths: 1691 }, [1672, 1673, 1676, 1679, 1680, 1683, 1684, 1687], { depth: 25, slug: 1405, text: 1406 }, { depth: 29, slug: 1674, text: 1675 }, "the-problem-grove-solves", "The Problem Grove Solves", { depth: 29, slug: 1677, text: 1678 }, "how-grove-addresses-it", "How Grove Addresses It", { depth: 29, slug: 393, text: 394 }, { depth: 29, slug: 1681, text: 1682 }, "what-grove-enables", "What Grove Enables", { depth: 29, slug: 571, text: 572 }, { depth: 29, slug: 1685, text: 1686 }, "key-technologies", "Key Technologies", { depth: 29, slug: 401, text: 402 }, [], [], { title: 1406, section: 381, order: 1663, description: 1664 }, [], "installation", { id: 1692, data: 1694, body: 1697, filePath: 1698, digest: 1699, rendered: 1700 }, { title: 1695, section: 1625, order: 29, description: 1696 }, "Installation", "Build requirements and source installation steps for macOS and Windows.", "# Installation\n\n> **Updated 2026-05-29** - Requirements updated to Rust nightly and Node 24.\n> Build commands updated to the `Maintain/Debug/Build.sh` profile system.\n> Canonical source:\n> [Documentation/GitHub/Building.md](https://github.com/CodeEditorLand/Land/tree/Current/Documentation/GitHub/Building.md)\n\n`Editor.Land` is currently source-only. There are no pre-built binaries,\nHomebrew tap, winget package, or apt repository yet. The instructions below\ncover building from source on **macOS and Windows**, both of which are supported\ntoday.\n\n---\n\n## Supported Platforms\n\n| Platform                | Status      | Notes                                      |\n| ----------------------- | ----------- | ------------------------------------------ |\n| **macOS 13+ (aarch64)** | Supported   | Apple Silicon - primary development target |\n| **macOS 13+ (x86_64)**  | Supported   | Intel Mac - tested                         |\n| **Windows 10 / 11**     | Supported   | WebView2 rendering active                  |\n| **Linux**               | In progress | WebKitGTK integration in development       |\n\n---\n\n## Build Requirements\n\n### macOS\n\n| Dependency | Minimum Version | Install                                                   |\n| ---------- | --------------- | --------------------------------------------------------- |\n| Rust       | nightly         | [rustup.rs](https://rustup.rs) → `rustup default nightly` |\n| Node.js    | 24              | [nodejs.org](https://nodejs.org) or `nvm install 24`      |\n| nvm        | latest          | [nvm-sh/nvm](https://github.com/nvm-sh/nvm)               |\n| pnpm       | 9               | `npm install -g pnpm`                                     |\n| Xcode CLI  | latest          | `xcode-select --install`                                  |\n| macOS      | 13.0 (Ventura)  | -                                                         |\n\n### Windows\n\n| Dependency                | Minimum Version | Install                                                                                                             |\n| ------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------- |\n| Rust                      | nightly         | [rustup.rs](https://rustup.rs) → `rustup default nightly`                                                           |\n| Node.js                   | 24              | [nodejs.org](https://nodejs.org)                                                                                    |\n| pnpm                      | 9               | `npm install -g pnpm`                                                                                               |\n| Visual Studio Build Tools | 2019+           | C++ workload required                                                                                               |\n| WebView2 Runtime          | latest          | Included with Windows 11; [download for Windows 10](https://developer.microsoft.com/en-us/microsoft-edge/webview2/) |\n\n---\n\n## Steps\n\nThe build is a **two-step linear flow.** Do not pull submodules recursively -\neach submodule is managed independently on its own branch.\n\n**1. Clone the repository**\n\n```bash\ngit clone --recurse-submodules https://github.com/CodeEditorLand/Land.git\ncd Land\n```\n\nThe `--recurse-submodules` flag is required. The Land repository uses Git\nsubmodules to reference each element (`Mountain`, `Cocoon`, `Sky`, `Wind`,\n`Vine`, and others) as a pinned commit. Cloning without this flag produces an\nincomplete source tree.\n\n**2. Compile the VS Code Editor submodule** _(mandatory)_\n\n`Cocoon` and `Output` both depend on this compiled output. Node 24 is required\nhere - the exact version is pinned in\n`Dependency/Microsoft/Dependency/Editor/.nvmrc`.\n\n```bash\ncd Dependency/Microsoft/Dependency/Editor\n\n# Switch to Node 24 (reads .nvmrc automatically)\nnvm use 24\n\ngit fetch --all\ngit reset --hard Parent/main\ngit clean -dfx\n\npnpm install\npnpm run compile\npnpm run compile-extensions-build\n```\n\n**3. Install `Node.js` dependencies**\n\n```bash\ncd Land # back to repository root\npnpm install\n```\n\nThis installs the `TypeScript` dependencies for `Cocoon`, `Sky`, `Wind`, and the\nbuild toolchain. It does not build the `Rust` components.\n\n**4. Run the development build**\n\n```bash\n./Maintain/Debug/Build.sh --profile debug-electron-bundled\n```\n\nThis compiles `Mountain` and its dependencies, then opens the editor window. The\nfirst run compiles all `Rust` dependencies from scratch - a few minutes on a\nfresh checkout. Subsequent runs rely on `Cargo`'s incremental compilation cache.\n\nFor a lighter iteration build that skips the full Vite/Astro bundling pass:\n\n```bash\n./Maintain/Debug/Build.sh --profile debug-mountain\n```\n\n---\n\n## Release Build\n\n```bash\n./Maintain/Release/Build.sh --profile production-electron-bundled\n```\n\n---\n\n## Updating\n\nTo update to the latest commits and rebuild:\n\n```bash\ngit pull --recurse-submodules\ncd Dependency/Microsoft/Dependency/Editor\nnvm use 24\ngit fetch --all\ngit reset --hard Parent/main\ngit clean -dfx\npnpm install\npnpm run compile\npnpm run compile-extensions-build\ncd Land\npnpm install\n./Maintain/Debug/Build.sh --profile debug-electron-bundled\n```\n\nThe `Air` module - active in the compiled binary today - manages daemon-level\nlifecycle and is the foundation for surfacing update notifications in the UI.\n\n---\n\n## See Also\n\n- [Getting Started](https://Editor.Land/Doc/getting-started)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Air`: Update Daemon](https://Editor.Land/Doc/air)\n- [Contributing](https://Editor.Land/Doc/contributing)", "Source/Content/doc/installation.md", "04decf8042b8baeb", { html: 1701, metadata: 1702 }, '<h1 id="installation">Installation</h1>\n<blockquote>\n<p><strong>Updated 2026-05-29</strong> - Requirements updated to Rust nightly and Node 24.\nBuild commands updated to the <code>Maintain/Debug/Build.sh</code> profile system.\nCanonical source:\n<a href="https://github.com/CodeEditorLand/Land/tree/Current/Documentation/GitHub/Building.md">Documentation/GitHub/Building.md</a></p>\n</blockquote>\n<p><code>Editor.Land</code> is currently source-only. There are no pre-built binaries,\nHomebrew tap, winget package, or apt repository yet. The instructions below\ncover building from source on <strong>macOS and Windows</strong>, both of which are supported\ntoday.</p>\n<hr>\n<h2 id="supported-platforms">Supported Platforms</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Platform</th><th>Status</th><th>Notes</th></tr></thead><tbody><tr><td><strong>macOS 13+ (aarch64)</strong></td><td>Supported</td><td>Apple Silicon - primary development target</td></tr><tr><td><strong>macOS 13+ (x86_64)</strong></td><td>Supported</td><td>Intel Mac - tested</td></tr><tr><td><strong>Windows 10 / 11</strong></td><td>Supported</td><td>WebView2 rendering active</td></tr><tr><td><strong>Linux</strong></td><td>In progress</td><td>WebKitGTK integration in development</td></tr></tbody></table>\n<hr>\n<h2 id="build-requirements">Build Requirements</h2>\n<h3 id="macos">macOS</h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Dependency</th><th>Minimum Version</th><th>Install</th></tr></thead><tbody><tr><td>Rust</td><td>nightly</td><td><a href="https://rustup.rs">rustup.rs</a> → <code>rustup default nightly</code></td></tr><tr><td>Node.js</td><td>24</td><td><a href="https://nodejs.org">nodejs.org</a> or <code>nvm install 24</code></td></tr><tr><td>nvm</td><td>latest</td><td><a href="https://github.com/nvm-sh/nvm">nvm-sh/nvm</a></td></tr><tr><td>pnpm</td><td>9</td><td><code>npm install -g pnpm</code></td></tr><tr><td>Xcode CLI</td><td>latest</td><td><code>xcode-select --install</code></td></tr><tr><td>macOS</td><td>13.0 (Ventura)</td><td>-</td></tr></tbody></table>\n<h3 id="windows">Windows</h3>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Dependency</th><th>Minimum Version</th><th>Install</th></tr></thead><tbody><tr><td>Rust</td><td>nightly</td><td><a href="https://rustup.rs">rustup.rs</a> → <code>rustup default nightly</code></td></tr><tr><td>Node.js</td><td>24</td><td><a href="https://nodejs.org">nodejs.org</a></td></tr><tr><td>pnpm</td><td>9</td><td><code>npm install -g pnpm</code></td></tr><tr><td>Visual Studio Build Tools</td><td>2019+</td><td>C++ workload required</td></tr><tr><td>WebView2 Runtime</td><td>latest</td><td>Included with Windows 11; <a href="https://developer.microsoft.com/en-us/microsoft-edge/webview2/">download for Windows 10</a></td></tr></tbody></table>\n<hr>\n<h2 id="steps">Steps</h2>\n<p>The build is a <strong>two-step linear flow.</strong> Do not pull submodules recursively -\neach submodule is managed independently on its own branch.</p>\n<p><strong>1. Clone the repository</strong></p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> clone</span><span style="color:#79B8FF"> --recurse-submodules</span><span style="color:#9ECBFF"> https://github.com/CodeEditorLand/Land.git</span></span>\n<span class="line"><span style="color:#79B8FF">cd</span><span style="color:#9ECBFF"> Land</span></span></code></pre>\n<p>The <code>--recurse-submodules</code> flag is required. The Land repository uses Git\nsubmodules to reference each element (<code>Mountain</code>, <code>Cocoon</code>, <code>Sky</code>, <code>Wind</code>,\n<code>Vine</code>, and others) as a pinned commit. Cloning without this flag produces an\nincomplete source tree.</p>\n<p><strong>2. Compile the VS Code Editor submodule</strong> <em>(mandatory)</em></p>\n<p><code>Cocoon</code> and <code>Output</code> both depend on this compiled output. Node 24 is required\nhere - the exact version is pinned in\n<code>Dependency/Microsoft/Dependency/Editor/.nvmrc</code>.</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#79B8FF">cd</span><span style="color:#9ECBFF"> Dependency/Microsoft/Dependency/Editor</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#6A737D"># Switch to Node 24 (reads .nvmrc automatically)</span></span>\n<span class="line"><span style="color:#B392F0">nvm</span><span style="color:#9ECBFF"> use</span><span style="color:#79B8FF"> 24</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> fetch</span><span style="color:#79B8FF"> --all</span></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> reset</span><span style="color:#79B8FF"> --hard</span><span style="color:#9ECBFF"> Parent/main</span></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> clean</span><span style="color:#79B8FF"> -dfx</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> install</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> run</span><span style="color:#9ECBFF"> compile</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> run</span><span style="color:#9ECBFF"> compile-extensions-build</span></span></code></pre>\n<p><strong>3. Install <code>Node.js</code> dependencies</strong></p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#79B8FF">cd</span><span style="color:#9ECBFF"> Land</span><span style="color:#6A737D"> # back to repository root</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> install</span></span></code></pre>\n<p>This installs the <code>TypeScript</code> dependencies for <code>Cocoon</code>, <code>Sky</code>, <code>Wind</code>, and the\nbuild toolchain. It does not build the <code>Rust</code> components.</p>\n<p><strong>4. Run the development build</strong></p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">./Maintain/Debug/Build.sh</span><span style="color:#79B8FF"> --profile</span><span style="color:#9ECBFF"> debug-electron-bundled</span></span></code></pre>\n<p>This compiles <code>Mountain</code> and its dependencies, then opens the editor window. The\nfirst run compiles all <code>Rust</code> dependencies from scratch - a few minutes on a\nfresh checkout. Subsequent runs rely on <code>Cargo</code>’s incremental compilation cache.</p>\n<p>For a lighter iteration build that skips the full Vite/Astro bundling pass:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">./Maintain/Debug/Build.sh</span><span style="color:#79B8FF"> --profile</span><span style="color:#9ECBFF"> debug-mountain</span></span></code></pre>\n<hr>\n<h2 id="release-build">Release Build</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">./Maintain/Release/Build.sh</span><span style="color:#79B8FF"> --profile</span><span style="color:#9ECBFF"> production-electron-bundled</span></span></code></pre>\n<hr>\n<h2 id="updating">Updating</h2>\n<p>To update to the latest commits and rebuild:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> pull</span><span style="color:#79B8FF"> --recurse-submodules</span></span>\n<span class="line"><span style="color:#79B8FF">cd</span><span style="color:#9ECBFF"> Dependency/Microsoft/Dependency/Editor</span></span>\n<span class="line"><span style="color:#B392F0">nvm</span><span style="color:#9ECBFF"> use</span><span style="color:#79B8FF"> 24</span></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> fetch</span><span style="color:#79B8FF"> --all</span></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> reset</span><span style="color:#79B8FF"> --hard</span><span style="color:#9ECBFF"> Parent/main</span></span>\n<span class="line"><span style="color:#B392F0">git</span><span style="color:#9ECBFF"> clean</span><span style="color:#79B8FF"> -dfx</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> install</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> run</span><span style="color:#9ECBFF"> compile</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> run</span><span style="color:#9ECBFF"> compile-extensions-build</span></span>\n<span class="line"><span style="color:#79B8FF">cd</span><span style="color:#9ECBFF"> Land</span></span>\n<span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> install</span></span>\n<span class="line"><span style="color:#B392F0">./Maintain/Debug/Build.sh</span><span style="color:#79B8FF"> --profile</span><span style="color:#9ECBFF"> debug-electron-bundled</span></span></code></pre>\n<p>The <code>Air</code> module - active in the compiled binary today - manages daemon-level\nlifecycle and is the foundation for surfacing update notifications in the UI.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/getting-started">Getting Started</a></li>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/air"><code>Air</code>: Update Daemon</a></li>\n<li><a href="https://Editor.Land/Doc/contributing">Contributing</a></li>\n</ul>', { headings: 1703, localImagePaths: 1725, remoteImagePaths: 1726, frontmatter: 1727, imagePaths: 1728 }, [1704, 1705, 1708, 1709, 1712, 1715, 1718, 1721, 1724], { depth: 25, slug: 1692, text: 1695 }, { depth: 29, slug: 1706, text: 1707 }, "supported-platforms", "Supported Platforms", { depth: 29, slug: 1639, text: 1640 }, { depth: 347, slug: 1710, text: 1711 }, "macos", "macOS", { depth: 347, slug: 1713, text: 1714 }, "windows", "Windows", { depth: 29, slug: 1716, text: 1717 }, "steps", "Steps", { depth: 29, slug: 1719, text: 1720 }, "release-build", "Release Build", { depth: 29, slug: 1722, text: 1723 }, "updating", "Updating", { depth: 29, slug: 446, text: 447 }, [], [], { title: 1695, section: 1625, order: 29, description: 1696 }, [], "introduction", { id: 1729, data: 1731, body: 1735, filePath: 1736, digest: 1737, rendered: 1738 }, { title: 1732, section: 1625, order: 1733, description: 1734 }, "Introduction", 0, "What Editor.Land is, how it is built, and what is source-backed today.", "# Introduction to Editor.Land\n\n`Editor.Land` is an open-source code editor stack built around `Rust`, `Tauri`,\n`Effect-TS`, and a `VS Code`-compatible extension host. `Mountain` is the\n`Rust` + `Tauri` desktop shell. `Sky` and `Wind` provide the `workbench` UI and\nservice layer. `Cocoon` runs unmodified `VS Code` extension entry points through\na `vscode` `API` shim where the `API` surface is implemented.\n\nThe project is source-build first today. Public installers, release signing, and\na full extension validation matrix are still in progress.\n\n---\n\n## Current Status\n\nThe source supports these claims:\n\n- `Mountain` provides the `Tauri` desktop shell, native services, `IPC`, and\n  `Cocoon` bridge.\n- `Cocoon` runs existing `VS Code` extension entry points without rewriting\n  their source when the `APIs` they use are implemented.\n- `Mountain` scans installed extensions, reads manifests, handles local VSIX\n  install and uninstall routes, and notifies `Cocoon` when extension state\n  changes.\n- `Vine` provides `.proto` contracts and generated `IPC` stubs for routes such\n  as `Mountain` to `Cocoon` and `Air` to `Mountain`.\n- `Air` contains background service code for updates, downloads, integrity,\n  authentication, indexing, health, and `Vine` `IPC`.\n- `Grove` contains a Wasmtime-backed `WebAssembly` host path, but that path is\n  WIP for the primary editor flow.\n\nPerformance numbers are intentionally left out until a repeatable public\nbenchmark suite is published.\n\n---\n\n## In Progress\n\n- Public macOS, Windows, and Linux installer validation.\n- Release signing and verification artifacts.\n- Marketplace browsing and install flows beyond local or sideloaded extension\n  sources.\n- Chat, language-model, notebook, tests, and other long-tail `VS Code` `APIs`.\n- `Grove` integration into the primary build.\n\n---\n\n## Elements\n\n`Editor.Land` is composed of named elements. Each element should be described by\nsource status and integration status, not by broad product promises.\n\n| Element      | Role                                                                           |\n| ------------ | ------------------------------------------------------------------------------ |\n| **Mountain** | Rust + Tauri desktop shell and native service host                             |\n| **Cocoon**   | Node.js extension host for unmodified VS Code extension code                   |\n| **Sky**      | Astro workbench routes and WebView bridge                                      |\n| **Wind**     | Effect-TS workbench service layer                                              |\n| **Vine**     | Protocol contracts and generated IPC stubs                                     |\n| **Air**      | Background services for update, download, authentication, indexing, and health |\n| **Echo**     | Rust scheduler primitives                                                      |\n| **Mist**     | Local DNS and service-boundary work                                            |\n| **Grove**    | WIP Wasmtime extension-host path                                               |\n| **Rest**     | OXC-based TypeScript transform work                                            |\n| **Output**   | Plugin-routed output and VS Code platform transforms                           |\n| **Common**   | Shared Rust and TypeScript contracts                                           |\n| **SideCar**  | Host-specific sidecar binary packaging                                         |\n| **Maintain** | Build and maintenance scripts                                                  |\n| **Worker**   | Browser-worker support where the web shell needs it                            |\n\n---\n\n## Build Profiles\n\n- **`debug`** - browser-oriented UI development without the full `Mountain`\n  desktop shell.\n- **`debug-mountain`** - primary `Tauri` desktop development profile with\n  `Mountain` and `Cocoon`.\n- **`debug-electron`** - legacy compatibility path for comparison work.\n\n---\n\n## See Also\n\n- [Getting Started](https://Editor.Land/Doc/getting-started)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Mountain`](https://Editor.Land/Doc/mountain)\n- [`Cocoon`](https://Editor.Land/Doc/cocoon)", "Source/Content/doc/introduction.md", "c2b4691513cda96e", { html: 1739, metadata: 1740 }, '<h1 id="introduction-to-editorland">Introduction to Editor.Land</h1>\n<p><code>Editor.Land</code> is an open-source code editor stack built around <code>Rust</code>, <code>Tauri</code>,\n<code>Effect-TS</code>, and a <code>VS Code</code>-compatible extension host. <code>Mountain</code> is the\n<code>Rust</code> + <code>Tauri</code> desktop shell. <code>Sky</code> and <code>Wind</code> provide the <code>workbench</code> UI and\nservice layer. <code>Cocoon</code> runs unmodified <code>VS Code</code> extension entry points through\na <code>vscode</code> <code>API</code> shim where the <code>API</code> surface is implemented.</p>\n<p>The project is source-build first today. Public installers, release signing, and\na full extension validation matrix are still in progress.</p>\n<hr>\n<h2 id="current-status">Current Status</h2>\n<p>The source supports these claims:</p>\n<ul>\n<li><code>Mountain</code> provides the <code>Tauri</code> desktop shell, native services, <code>IPC</code>, and\n<code>Cocoon</code> bridge.</li>\n<li><code>Cocoon</code> runs existing <code>VS Code</code> extension entry points without rewriting\ntheir source when the <code>APIs</code> they use are implemented.</li>\n<li><code>Mountain</code> scans installed extensions, reads manifests, handles local VSIX\ninstall and uninstall routes, and notifies <code>Cocoon</code> when extension state\nchanges.</li>\n<li><code>Vine</code> provides <code>.proto</code> contracts and generated <code>IPC</code> stubs for routes such\nas <code>Mountain</code> to <code>Cocoon</code> and <code>Air</code> to <code>Mountain</code>.</li>\n<li><code>Air</code> contains background service code for updates, downloads, integrity,\nauthentication, indexing, health, and <code>Vine</code> <code>IPC</code>.</li>\n<li><code>Grove</code> contains a Wasmtime-backed <code>WebAssembly</code> host path, but that path is\nWIP for the primary editor flow.</li>\n</ul>\n<p>Performance numbers are intentionally left out until a repeatable public\nbenchmark suite is published.</p>\n<hr>\n<h2 id="in-progress">In Progress</h2>\n<ul>\n<li>Public macOS, Windows, and Linux installer validation.</li>\n<li>Release signing and verification artifacts.</li>\n<li>Marketplace browsing and install flows beyond local or sideloaded extension\nsources.</li>\n<li>Chat, language-model, notebook, tests, and other long-tail <code>VS Code</code> <code>APIs</code>.</li>\n<li><code>Grove</code> integration into the primary build.</li>\n</ul>\n<hr>\n<h2 id="elements">Elements</h2>\n<p><code>Editor.Land</code> is composed of named elements. Each element should be described by\nsource status and integration status, not by broad product promises.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Element</th><th>Role</th></tr></thead><tbody><tr><td><strong>Mountain</strong></td><td>Rust + Tauri desktop shell and native service host</td></tr><tr><td><strong>Cocoon</strong></td><td>Node.js extension host for unmodified VS Code extension code</td></tr><tr><td><strong>Sky</strong></td><td>Astro workbench routes and WebView bridge</td></tr><tr><td><strong>Wind</strong></td><td>Effect-TS workbench service layer</td></tr><tr><td><strong>Vine</strong></td><td>Protocol contracts and generated IPC stubs</td></tr><tr><td><strong>Air</strong></td><td>Background services for update, download, authentication, indexing, and health</td></tr><tr><td><strong>Echo</strong></td><td>Rust scheduler primitives</td></tr><tr><td><strong>Mist</strong></td><td>Local DNS and service-boundary work</td></tr><tr><td><strong>Grove</strong></td><td>WIP Wasmtime extension-host path</td></tr><tr><td><strong>Rest</strong></td><td>OXC-based TypeScript transform work</td></tr><tr><td><strong>Output</strong></td><td>Plugin-routed output and VS Code platform transforms</td></tr><tr><td><strong>Common</strong></td><td>Shared Rust and TypeScript contracts</td></tr><tr><td><strong>SideCar</strong></td><td>Host-specific sidecar binary packaging</td></tr><tr><td><strong>Maintain</strong></td><td>Build and maintenance scripts</td></tr><tr><td><strong>Worker</strong></td><td>Browser-worker support where the web shell needs it</td></tr></tbody></table>\n<hr>\n<h2 id="build-profiles">Build Profiles</h2>\n<ul>\n<li><strong><code>debug</code></strong> - browser-oriented UI development without the full <code>Mountain</code>\ndesktop shell.</li>\n<li><strong><code>debug-mountain</code></strong> - primary <code>Tauri</code> desktop development profile with\n<code>Mountain</code> and <code>Cocoon</code>.</li>\n<li><strong><code>debug-electron</code></strong> - legacy compatibility path for comparison work.</li>\n</ul>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/getting-started">Getting Started</a></li>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/mountain"><code>Mountain</code></a></li>\n<li><a href="https://Editor.Land/Doc/cocoon"><code>Cocoon</code></a></li>\n</ul>', { headings: 1741, localImagePaths: 1755, remoteImagePaths: 1756, frontmatter: 1757, imagePaths: 1758 }, [1742, 1745, 1747, 1748, 1751, 1754], { depth: 25, slug: 1743, text: 1744 }, "introduction-to-editorland", "Introduction to Editor.Land", { depth: 29, slug: 1572, text: 1746 }, "Current Status", { depth: 29, slug: 360, text: 361 }, { depth: 29, slug: 1749, text: 1750 }, "elements", "Elements", { depth: 29, slug: 1752, text: 1753 }, "build-profiles", "Build Profiles", { depth: 29, slug: 446, text: 447 }, [], [], { title: 1732, section: 1625, order: 1733, description: 1734 }, [], "local-first-protocol", { id: 1759, data: 1761, body: 1764, filePath: 1765, digest: 1766, rendered: 1767 }, { title: 1762, section: 10, order: 252, description: 1763 }, "Local-First Protocol", "Air Daemon protocol design: discovery, sync, and conflict resolution.", '# Local-First Protocol\n\n> **Note: The peer-to-peer sync protocol described here is the design\n> specification for the `Air` Daemon. `Air` exists as an active `Rust`\n> background daemon, but the full sync protocol - including `mDNS` discovery,\n> mutual authentication, and content-addressed delta transfer - is under active\n> development. Specific constants, port numbers, and file paths in this document\n> reflect the intended design, not a confirmed working implementation. This page\n> will be updated as features ship.**\n\nThe `Air` Daemon provides optional peer-to-peer synchronisation between Land\ninstances. This document describes the design of the protocol used for\ndiscovery, data transfer, and conflict resolution. All synchronisation is\nlocal-network-first: no data passes through external servers.\n\n---\n\n## Design Principles\n\nLand\'s sync layer is built on three commitments:\n\n- **Local-first** - the editor is always fully functional without network\n  access. Sync is additive, never blocking.\n- **No central relay** - peers discover and communicate with each other directly\n  on the local network. There is no Land-operated server in the data path.\n- **Encrypted by default** - all peer communication is encrypted at the frame\n  level using established cryptographic primitives. No certificate authority or\n  TLS infrastructure is required.\n\n---\n\n## Discovery\n\n`Air` is designed to broadcast an `mDNS` service record on the local network.\nEach record will contain an instance ID (a UUID generated on first launch), a\nlisten port, and a protocol version identifier. When a peer is discovered, the\ninitiating daemon opens a connection and performs a mutual authentication\nhandshake using pre-shared keys exchanged out-of-band (such as a QR code or\nmanual key entry).\n\n---\n\n## Authentication\n\n`Air` is designed to use `NaCl` (libsodium) for all cryptographic operations.\nThe intended scheme is:\n\n1. Each instance generates a Curve25519 key pair on first launch.\n2. Pairing exchanges public keys out-of-band.\n3. Every frame is encrypted using `crypto_box` (X25519 + XSalsa20 + Poly1305).\n\nThis eliminates the need for a certificate authority, a PKI, or TLS negotiation.\nTrust is established directly between two instances via key exchange.\n\n---\n\n## Sync Protocol\n\nOnce authenticated, the design calls for peers to exchange file state using a\ncontent-addressed protocol:\n\n1. **Manifest exchange** - each peer sends a manifest listing file paths and\n   their content hashes (BLAKE3).\n2. **Delta request** - the receiver identifies files whose hashes differ and\n   requests only those.\n3. **Content transfer** - changed files are sent as compressed byte frames.\n4. **Acknowledgment** - the receiver confirms receipt and updates its manifest.\n\nOnly changed files are transferred. This design avoids re-sending content that\nboth peers already have.\n\n---\n\n## Conflict Resolution\n\nWhen both peers have modified the same file since the last sync, the intended\nbehaviour is:\n\n- **Default: last-write-wins** - the file with the more recent modification\n  timestamp is accepted. The overwritten version is preserved in a conflicts\n  directory within the workspace.\n- **Manual merge** - the user is notified and can open a three-way merge view to\n  resolve conflicts.\n\nConflict resolution strategy is intended to be configurable per workspace.\n\n---\n\n## Offline-First Guarantees\n\n`Air` is designed never to block the editor. If no peers are available, the\neditor operates normally. Sync is intended to resume automatically when\nconnectivity is restored. There is no "offline mode" toggle - the editor is\nalways fully functional regardless of network state.\n\n---\n\n## See Also\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Air`](https://Editor.Land/Doc/air)\n- [Configuration](https://Editor.Land/Doc/configuration)', "Source/Content/doc/local-first-protocol.md", "1fb58dc1e292324e", { html: 1768, metadata: 1769 }, '<h1 id="local-first-protocol">Local-First Protocol</h1>\n<blockquote>\n<p><strong>Note: The peer-to-peer sync protocol described here is the design\nspecification for the <code>Air</code> Daemon. <code>Air</code> exists as an active <code>Rust</code>\nbackground daemon, but the full sync protocol - including <code>mDNS</code> discovery,\nmutual authentication, and content-addressed delta transfer - is under active\ndevelopment. Specific constants, port numbers, and file paths in this document\nreflect the intended design, not a confirmed working implementation. This page\nwill be updated as features ship.</strong></p>\n</blockquote>\n<p>The <code>Air</code> Daemon provides optional peer-to-peer synchronisation between Land\ninstances. This document describes the design of the protocol used for\ndiscovery, data transfer, and conflict resolution. All synchronisation is\nlocal-network-first: no data passes through external servers.</p>\n<hr>\n<h2 id="design-principles">Design Principles</h2>\n<p>Land’s sync layer is built on three commitments:</p>\n<ul>\n<li><strong>Local-first</strong> - the editor is always fully functional without network\naccess. Sync is additive, never blocking.</li>\n<li><strong>No central relay</strong> - peers discover and communicate with each other directly\non the local network. There is no Land-operated server in the data path.</li>\n<li><strong>Encrypted by default</strong> - all peer communication is encrypted at the frame\nlevel using established cryptographic primitives. No certificate authority or\nTLS infrastructure is required.</li>\n</ul>\n<hr>\n<h2 id="discovery">Discovery</h2>\n<p><code>Air</code> is designed to broadcast an <code>mDNS</code> service record on the local network.\nEach record will contain an instance ID (a UUID generated on first launch), a\nlisten port, and a protocol version identifier. When a peer is discovered, the\ninitiating daemon opens a connection and performs a mutual authentication\nhandshake using pre-shared keys exchanged out-of-band (such as a QR code or\nmanual key entry).</p>\n<hr>\n<h2 id="authentication">Authentication</h2>\n<p><code>Air</code> is designed to use <code>NaCl</code> (libsodium) for all cryptographic operations.\nThe intended scheme is:</p>\n<ol>\n<li>Each instance generates a Curve25519 key pair on first launch.</li>\n<li>Pairing exchanges public keys out-of-band.</li>\n<li>Every frame is encrypted using <code>crypto_box</code> (X25519 + XSalsa20 + Poly1305).</li>\n</ol>\n<p>This eliminates the need for a certificate authority, a PKI, or TLS negotiation.\nTrust is established directly between two instances via key exchange.</p>\n<hr>\n<h2 id="sync-protocol">Sync Protocol</h2>\n<p>Once authenticated, the design calls for peers to exchange file state using a\ncontent-addressed protocol:</p>\n<ol>\n<li><strong>Manifest exchange</strong> - each peer sends a manifest listing file paths and\ntheir content hashes (BLAKE3).</li>\n<li><strong>Delta request</strong> - the receiver identifies files whose hashes differ and\nrequests only those.</li>\n<li><strong>Content transfer</strong> - changed files are sent as compressed byte frames.</li>\n<li><strong>Acknowledgment</strong> - the receiver confirms receipt and updates its manifest.</li>\n</ol>\n<p>Only changed files are transferred. This design avoids re-sending content that\nboth peers already have.</p>\n<hr>\n<h2 id="conflict-resolution">Conflict Resolution</h2>\n<p>When both peers have modified the same file since the last sync, the intended\nbehaviour is:</p>\n<ul>\n<li><strong>Default: last-write-wins</strong> - the file with the more recent modification\ntimestamp is accepted. The overwritten version is preserved in a conflicts\ndirectory within the workspace.</li>\n<li><strong>Manual merge</strong> - the user is notified and can open a three-way merge view to\nresolve conflicts.</li>\n</ul>\n<p>Conflict resolution strategy is intended to be configurable per workspace.</p>\n<hr>\n<h2 id="offline-first-guarantees">Offline-First Guarantees</h2>\n<p><code>Air</code> is designed never to block the editor. If no peers are available, the\neditor operates normally. Sync is intended to resume automatically when\nconnectivity is restored. There is no “offline mode” toggle - the editor is\nalways fully functional regardless of network state.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/air"><code>Air</code></a></li>\n<li><a href="https://Editor.Land/Doc/configuration">Configuration</a></li>\n</ul>', { headings: 1770, localImagePaths: 1789, remoteImagePaths: 1790, frontmatter: 1791, imagePaths: 1792 }, [1771, 1772, 1773, 1776, 1779, 1782, 1785, 1788], { depth: 25, slug: 1759, text: 1762 }, { depth: 29, slug: 858, text: 859 }, { depth: 29, slug: 1774, text: 1775 }, "discovery", "Discovery", { depth: 29, slug: 1777, text: 1778 }, "authentication", "Authentication", { depth: 29, slug: 1780, text: 1781 }, "sync-protocol", "Sync Protocol", { depth: 29, slug: 1783, text: 1784 }, "conflict-resolution", "Conflict Resolution", { depth: 29, slug: 1786, text: 1787 }, "offline-first-guarantees", "Offline-First Guarantees", { depth: 29, slug: 446, text: 447 }, [], [], { title: 1762, section: 10, order: 252, description: 1763 }, [], { id: 1415, data: 1794, body: 1797, filePath: 1798, digest: 1799, rendered: 1800 }, { title: 58, section: 381, order: 1795, description: 1796 }, 25, "The build orchestrator with Rhai scripting and deterministic output.", "# Maintain\n\n`Maintain` is the build orchestrator for `Editor.Land`. It coordinates `Rest`,\n`Output`, and other build-time elements into a single reproducible pipeline.\nBuild logic is written in Rhai scripts rather than shell scripts, and\nconfigurations are validated at compile time.\n\n---\n\n## The Problem\n\nBuild pipelines that depend on environment variables, shell conditionals, and\nplatform-specific scripts become difficult to debug. A build passes on one\nmachine and fails on another because of a missing `$PATH` entry or a difference\nin how `sed` handles newlines across platforms. `VS Code`'s build system spans\nGulp tasks, shell scripts, and `Node.js` scripts scattered across dozens of\nfiles. Reproducing a failure requires replicating the exact environment.\n\n---\n\n## How Maintain Eliminates It\n\n`Maintain` replaces shell scripts with Rhai, an embedded scripting language\ndesigned for `Rust` applications. Rhai scripts are sandboxed and cross-platform\nby design. They cannot access the file system, network, or environment variables\nunless explicitly granted permission through `Maintain`'s capability system.\n\nBuild configurations are `Rust` structs validated at compile time. A typo in a\nconfiguration key fails the build before any artifact is produced, not after\ntwenty minutes of compilation.\n\n`Maintain` orchestrates the pipeline: `Rest` compiles `TypeScript`, `Output`\nstores artifacts. Each step receives typed inputs and produces typed outputs. If\na step fails, the error message identifies the exact input that caused the\nfailure.\n\n---\n\n## What You Experience\n\nYou run a single command and the build either succeeds or fails with a clear,\nactionable error. Build scripts are readable: Rhai's syntax is close to `Rust`,\nso anyone who can read the application code can read the build logic.\n\n`Editor.Land` currently builds and runs on **macOS 13+** and **Windows 10/11**.\nLinux (`WebKitGTK`) build support is part of the roadmap but not yet active.\n\n---\n\n## Key Technologies\n\n`Rust`, Rhai Scripting, Compile-Time Configuration Validation, Deterministic\nOrchestration, Capability-Based Sandboxing.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Output`](https://Editor.Land/Doc/output)\n- [Source Code](https://github.com/CodeEditorLand/Maintain)", "Source/Content/doc/maintain.md", "5f6392261f6bb87e", { html: 1801, metadata: 1802 }, '<h1 id="maintain">Maintain</h1>\n<p><code>Maintain</code> is the build orchestrator for <code>Editor.Land</code>. It coordinates <code>Rest</code>,\n<code>Output</code>, and other build-time elements into a single reproducible pipeline.\nBuild logic is written in Rhai scripts rather than shell scripts, and\nconfigurations are validated at compile time.</p>\n<hr>\n<h2 id="the-problem">The Problem</h2>\n<p>Build pipelines that depend on environment variables, shell conditionals, and\nplatform-specific scripts become difficult to debug. A build passes on one\nmachine and fails on another because of a missing <code>$PATH</code> entry or a difference\nin how <code>sed</code> handles newlines across platforms. <code>VS Code</code>’s build system spans\nGulp tasks, shell scripts, and <code>Node.js</code> scripts scattered across dozens of\nfiles. Reproducing a failure requires replicating the exact environment.</p>\n<hr>\n<h2 id="how-maintain-eliminates-it">How Maintain Eliminates It</h2>\n<p><code>Maintain</code> replaces shell scripts with Rhai, an embedded scripting language\ndesigned for <code>Rust</code> applications. Rhai scripts are sandboxed and cross-platform\nby design. They cannot access the file system, network, or environment variables\nunless explicitly granted permission through <code>Maintain</code>’s capability system.</p>\n<p>Build configurations are <code>Rust</code> structs validated at compile time. A typo in a\nconfiguration key fails the build before any artifact is produced, not after\ntwenty minutes of compilation.</p>\n<p><code>Maintain</code> orchestrates the pipeline: <code>Rest</code> compiles <code>TypeScript</code>, <code>Output</code>\nstores artifacts. Each step receives typed inputs and produces typed outputs. If\na step fails, the error message identifies the exact input that caused the\nfailure.</p>\n<hr>\n<h2 id="what-you-experience">What You Experience</h2>\n<p>You run a single command and the build either succeeds or fails with a clear,\nactionable error. Build scripts are readable: Rhai’s syntax is close to <code>Rust</code>,\nso anyone who can read the application code can read the build logic.</p>\n<p><code>Editor.Land</code> currently builds and runs on <strong>macOS 13+</strong> and <strong>Windows 10/11</strong>.\nLinux (<code>WebKitGTK</code>) build support is part of the roadmap but not yet active.</p>\n<hr>\n<h2 id="key-technologies">Key Technologies</h2>\n<p><code>Rust</code>, Rhai Scripting, Compile-Time Configuration Validation, Deterministic\nOrchestration, Capability-Based Sandboxing.</p>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/output"><code>Output</code></a></li>\n<li><a href="https://github.com/CodeEditorLand/Maintain">Source Code</a></li>\n</ul>', { headings: 1803, localImagePaths: 1816, remoteImagePaths: 1817, frontmatter: 1818, imagePaths: 1819 }, [1804, 1805, 1808, 1811, 1814, 1815], { depth: 25, slug: 1415, text: 58 }, { depth: 29, slug: 1806, text: 1807 }, "the-problem", "The Problem", { depth: 29, slug: 1809, text: 1810 }, "how-maintain-eliminates-it", "How Maintain Eliminates It", { depth: 29, slug: 1812, text: 1813 }, "what-you-experience", "What You Experience", { depth: 29, slug: 1685, text: 1686 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 58, section: 381, order: 1795, description: 1796 }, [], { id: 1430, data: 1821, body: 1824, filePath: 1825, digest: 1826, rendered: 1827 }, { title: 1431, section: 381, order: 1822, description: 1823 }, 20, "Local DNS, resolver, WebSocket, zone, and forward-security code for Land.PlayForm.Cloud service boundaries.", "# Mist\n\n`Mist` is the local service-boundary element for `Land.PlayForm.Cloud`. The\nsource contains DNS server, resolver, zone, WebSocket, and forward-security\ncode. `Mountain` has a native `Mist` feature path, but not every internal editor\nroute flows through `Mist` yet.\n\n---\n\n## Source Structure 🗺️\n\nConfirmed source areas include:\n\n| Path                        | Role                                     |\n| --------------------------- | ---------------------------------------- |\n| `Source/Server.rs`          | DNS server                               |\n| `Source/Resolver.rs`        | Zone lookup and response construction    |\n| `Source/Zone.rs`            | `*.land.playform.cloud` zone definitions |\n| `Source/WebSocket.rs`       | WebSocket service code                   |\n| `Source/ForwardSecurity.rs` | Forward-security helpers                 |\n| `tests/`                    | Integration tests                        |\n\n---\n\n## Status 🚀\n\n`Mist` is source-backed and WIP for full integration. Public copy should\ndescribe the local DNS and service-boundary direction without promising that all\neditor traffic already flows through `Mist`.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Land.PlayForm.Cloud/Doc/architecture)\n- [`Air`](https://Land.PlayForm.Cloud/Doc/air)\n- [Source Code](https://github.com/CodeEditorLand/Mist)", "Source/Content/doc/mist.md", "b61138c93ddfabab", { html: 1828, metadata: 1829 }, '<h1 id="mist">Mist</h1>\n<p><code>Mist</code> is the local service-boundary element for <code>Land.PlayForm.Cloud</code>. The\nsource contains DNS server, resolver, zone, WebSocket, and forward-security\ncode. <code>Mountain</code> has a native <code>Mist</code> feature path, but not every internal editor\nroute flows through <code>Mist</code> yet.</p>\n<hr>\n<h2 id="source-structure️">Source Structure 🗺️</h2>\n<p>Confirmed source areas include:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Path</th><th>Role</th></tr></thead><tbody><tr><td><code>Source/Server.rs</code></td><td>DNS server</td></tr><tr><td><code>Source/Resolver.rs</code></td><td>Zone lookup and response construction</td></tr><tr><td><code>Source/Zone.rs</code></td><td><code>*.land.playform.cloud</code> zone definitions</td></tr><tr><td><code>Source/WebSocket.rs</code></td><td>WebSocket service code</td></tr><tr><td><code>Source/ForwardSecurity.rs</code></td><td>Forward-security helpers</td></tr><tr><td><code>tests/</code></td><td>Integration tests</td></tr></tbody></table>\n<hr>\n<h2 id="status">Status 🚀</h2>\n<p><code>Mist</code> is source-backed and WIP for full integration. Public copy should\ndescribe the local DNS and service-boundary direction without promising that all\neditor traffic already flows through <code>Mist</code>.</p>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Land.PlayForm.Cloud/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Land.PlayForm.Cloud/Doc/air"><code>Air</code></a></li>\n<li><a href="https://github.com/CodeEditorLand/Mist">Source Code</a></li>\n</ul>', { headings: 1830, localImagePaths: 1835, remoteImagePaths: 1836, frontmatter: 1837, imagePaths: 1838 }, [1831, 1832, 1833, 1834], { depth: 25, slug: 1430, text: 1431 }, { depth: 29, slug: 393, text: 394 }, { depth: 29, slug: 571, text: 572 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 1431, section: 381, order: 1822, description: 1823 }, [], { id: 1440, data: 1840, body: 1843, filePath: 1844, digest: 1845, rendered: 1846 }, { title: 12, section: 381, order: 1841, description: 1842 }, 11, "The Rust + Tauri native shell for Editor.Land, with native services, IPC, and the Cocoon extension-host bridge.", "# Mountain\n\n`Mountain` is the `Rust` + `Tauri` shell for `Editor.Land`. It owns the desktop\nwindow, native services, `Tauri` `IPC`, `Vine` `gRPC`, and the bridge to\n`Cocoon`. `Sky` renders in the operating system WebView, while extension-host\nwork runs in `Cocoon`.\n\nThe source supports a clear architectural claim: `Mountain` gives Land a native\ndesktop path without bundling Chromium. It should not be described with public\nspeed or memory numbers until those numbers are published with a repeatable\nbenchmark.\n\n---\n\n## Platform Configuration ⚙️\n\n| Platform | WebView        | Source status                                               |\n| -------- | -------------- | ----------------------------------------------------------- |\n| macOS    | WKWebView      | Primary development and bundle path                         |\n| Windows  | WebView2       | Tauri configuration exists, including bootstrapper settings |\n| Linux    | WebKitGTK      | Tauri configuration exists, release validation in progress  |\n| iOS      | WKWebView      | Tauri mobile configuration exists                           |\n| Android  | System WebView | Tauri mobile configuration exists                           |\n\n`tauri.conf.json` and production `Tauri` config files define the desktop bundle\nsettings. A configured target is not the same as a published installer, so\npublic release pages should keep installer claims separate from source support.\n\n---\n\n## Startup Pipeline 🚀\n\n`Mountain`'s binary is organized around build, registration, initialization, and\nservices:\n\n```text\nmain.rs -> Binary::Main -> Build -> Register -> Initialize -> Services\n```\n\n`Build` configures the `Tauri` application. `Register` wires commands, `IPC`,\nand service entry points. `Initialize` prepares runtime state. `Services` starts\nthe `Vine` and `Cocoon` path before handing control to the `Tauri` event loop.\n\n---\n\n## Source Areas\n\n- **`ApplicationState`** - shared state for workspace, extensions, features,\n  diagnostics, tree views, webviews, and lifecycle state.\n- **`Environment`** - provider interfaces for files, processes, terminals,\n  search, configuration, language features, debug, and related native concerns.\n- **`IPC`** - `Tauri` IPC server and secure message-channel code, including\n  `mountain_ipc_receive_message` and `mountain_ipc_get_status`.\n- **`RPC`** - `Vine` `gRPC` services for `Cocoon` and extension-host\n  interaction.\n- **`ProcessManagement`** - sidecar process lifecycle for `Cocoon` and other\n  helper processes.\n- **`ExtensionManagement`** - installed extension scanning, manifests, VSIX\n  install and uninstall routes, and extension state.\n- **`Binary`** - `Tauri` app setup, service startup, local TLS certificate\n  helpers, command registration, and runtime entry points.\n- **`Telemetry`** - optional tracing and telemetry paths behind feature gates.\n\n---\n\n## Native Responsibilities 🔌\n\n`Mountain` owns the native work that should not live inside a browser runtime:\n\n- File reads, writes, search, and workspace watchers through `Rust` services.\n- Terminal creation and process handling through native process and pty code.\n- Clipboard access through a cross-platform native dependency.\n- Keychain and secret-provider paths where enabled.\n- Debug adapter, source control, diagnostic, tree-view, webview, and language\n  feature provider state.\n- `Tauri` `IPC` between the WebView `workbench` and `Rust`.\n- `Vine` `gRPC` between `Mountain` and `Cocoon`.\n- Extension scanning and manifest delivery into `Cocoon`.\n\nThese are source-backed responsibilities. Exact latency, memory, and startup\nclaims should point to a public benchmark before they appear on the website.\n\n---\n\n## Extension Integration\n\n`Mountain` does not rewrite extension source. It scans installed extensions,\nreads `package.json` manifests, handles local VSIX install and uninstall paths,\nupdates extension state, and sends extension changes to `Cocoon`. `Cocoon` then\nruns the extension entry points and handles the `vscode` `API` shim.\n\nThe accurate public claim is: installed `VS Code` extensions run unmodified\nthrough the `Cocoon` path when their `API` usage is implemented. A claim that\nevery marketplace extension works needs a validation matrix.\n\n---\n\n## Feature Flags ⚙️\n\n`Mountain` uses `Cargo` feature flags for capability gates and experimental\ntiers. The default feature set includes paths such as `ExtensionHostCocoon`,\n`MistNative`, and `AirIntegration`. Other tier flags control file-system,\nsearch, watcher, clipboard, extension-scan, configuration, shared-memory, HTTP\nproxy, and diagnostic experiments.\n\nFeature flags are useful public documentation because they show which behavior\nis compiled into a build. They should not be used as shorthand for behavior that\nhas not been exercised by the selected profile.\n\n---\n\n## Build Profiles\n\n- **`debug`** - browser-oriented UI development without the full `Mountain`\n  desktop shell.\n- **`debug-mountain`** - the primary `Tauri` desktop development profile with\n  `Mountain` and `Cocoon`.\n- **`debug-electron`** - compatibility path for the legacy `Electron`\n  `workbench`.\n\nRelease profiles may enable different pieces. Public docs should name the\nprofile when making behavior claims.\n\n---\n\n## Planned Improvements\n\n- Public benchmark suite for startup, memory, file operations, and extension\n  activation.\n- Broader installer validation across macOS, Windows, and Linux.\n- Continued expansion of extension `API` coverage through `Cocoon` and\n  `Mountain`.\n- Further separation of background services where sidecars give a clearer\n  lifecycle boundary.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Cocoon`](https://Editor.Land/Doc/cocoon)\n- [`Vine`](https://Editor.Land/Doc/vine)\n- [`Air`](https://Editor.Land/Doc/air)", "Source/Content/doc/mountain.md", "0d3de6d4f758f2fd", { html: 1847, metadata: 1848 }, '<h1 id="mountain">Mountain</h1>\n<p><code>Mountain</code> is the <code>Rust</code> + <code>Tauri</code> shell for <code>Editor.Land</code>. It owns the desktop\nwindow, native services, <code>Tauri</code> <code>IPC</code>, <code>Vine</code> <code>gRPC</code>, and the bridge to\n<code>Cocoon</code>. <code>Sky</code> renders in the operating system WebView, while extension-host\nwork runs in <code>Cocoon</code>.</p>\n<p>The source supports a clear architectural claim: <code>Mountain</code> gives Land a native\ndesktop path without bundling Chromium. It should not be described with public\nspeed or memory numbers until those numbers are published with a repeatable\nbenchmark.</p>\n<hr>\n<h2 id="platform-configuration️">Platform Configuration ⚙️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Platform</th><th>WebView</th><th>Source status</th></tr></thead><tbody><tr><td>macOS</td><td>WKWebView</td><td>Primary development and bundle path</td></tr><tr><td>Windows</td><td>WebView2</td><td>Tauri configuration exists, including bootstrapper settings</td></tr><tr><td>Linux</td><td>WebKitGTK</td><td>Tauri configuration exists, release validation in progress</td></tr><tr><td>iOS</td><td>WKWebView</td><td>Tauri mobile configuration exists</td></tr><tr><td>Android</td><td>System WebView</td><td>Tauri mobile configuration exists</td></tr></tbody></table>\n<p><code>tauri.conf.json</code> and production <code>Tauri</code> config files define the desktop bundle\nsettings. A configured target is not the same as a published installer, so\npublic release pages should keep installer claims separate from source support.</p>\n<hr>\n<h2 id="startup-pipeline">Startup Pipeline 🚀</h2>\n<p><code>Mountain</code>’s binary is organized around build, registration, initialization, and\nservices:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="text"><code><span class="line"><span>main.rs -> Binary::Main -> Build -> Register -> Initialize -> Services</span></span></code></pre>\n<p><code>Build</code> configures the <code>Tauri</code> application. <code>Register</code> wires commands, <code>IPC</code>,\nand service entry points. <code>Initialize</code> prepares runtime state. <code>Services</code> starts\nthe <code>Vine</code> and <code>Cocoon</code> path before handing control to the <code>Tauri</code> event loop.</p>\n<hr>\n<h2 id="source-areas">Source Areas</h2>\n<ul>\n<li><strong><code>ApplicationState</code></strong> - shared state for workspace, extensions, features,\ndiagnostics, tree views, webviews, and lifecycle state.</li>\n<li><strong><code>Environment</code></strong> - provider interfaces for files, processes, terminals,\nsearch, configuration, language features, debug, and related native concerns.</li>\n<li><strong><code>IPC</code></strong> - <code>Tauri</code> IPC server and secure message-channel code, including\n<code>mountain_ipc_receive_message</code> and <code>mountain_ipc_get_status</code>.</li>\n<li><strong><code>RPC</code></strong> - <code>Vine</code> <code>gRPC</code> services for <code>Cocoon</code> and extension-host\ninteraction.</li>\n<li><strong><code>ProcessManagement</code></strong> - sidecar process lifecycle for <code>Cocoon</code> and other\nhelper processes.</li>\n<li><strong><code>ExtensionManagement</code></strong> - installed extension scanning, manifests, VSIX\ninstall and uninstall routes, and extension state.</li>\n<li><strong><code>Binary</code></strong> - <code>Tauri</code> app setup, service startup, local TLS certificate\nhelpers, command registration, and runtime entry points.</li>\n<li><strong><code>Telemetry</code></strong> - optional tracing and telemetry paths behind feature gates.</li>\n</ul>\n<hr>\n<h2 id="native-responsibilities">Native Responsibilities 🔌</h2>\n<p><code>Mountain</code> owns the native work that should not live inside a browser runtime:</p>\n<ul>\n<li>File reads, writes, search, and workspace watchers through <code>Rust</code> services.</li>\n<li>Terminal creation and process handling through native process and pty code.</li>\n<li>Clipboard access through a cross-platform native dependency.</li>\n<li>Keychain and secret-provider paths where enabled.</li>\n<li>Debug adapter, source control, diagnostic, tree-view, webview, and language\nfeature provider state.</li>\n<li><code>Tauri</code> <code>IPC</code> between the WebView <code>workbench</code> and <code>Rust</code>.</li>\n<li><code>Vine</code> <code>gRPC</code> between <code>Mountain</code> and <code>Cocoon</code>.</li>\n<li>Extension scanning and manifest delivery into <code>Cocoon</code>.</li>\n</ul>\n<p>These are source-backed responsibilities. Exact latency, memory, and startup\nclaims should point to a public benchmark before they appear on the website.</p>\n<hr>\n<h2 id="extension-integration">Extension Integration</h2>\n<p><code>Mountain</code> does not rewrite extension source. It scans installed extensions,\nreads <code>package.json</code> manifests, handles local VSIX install and uninstall paths,\nupdates extension state, and sends extension changes to <code>Cocoon</code>. <code>Cocoon</code> then\nruns the extension entry points and handles the <code>vscode</code> <code>API</code> shim.</p>\n<p>The accurate public claim is: installed <code>VS Code</code> extensions run unmodified\nthrough the <code>Cocoon</code> path when their <code>API</code> usage is implemented. A claim that\nevery marketplace extension works needs a validation matrix.</p>\n<hr>\n<h2 id="feature-flags️">Feature Flags ⚙️</h2>\n<p><code>Mountain</code> uses <code>Cargo</code> feature flags for capability gates and experimental\ntiers. The default feature set includes paths such as <code>ExtensionHostCocoon</code>,\n<code>MistNative</code>, and <code>AirIntegration</code>. Other tier flags control file-system,\nsearch, watcher, clipboard, extension-scan, configuration, shared-memory, HTTP\nproxy, and diagnostic experiments.</p>\n<p>Feature flags are useful public documentation because they show which behavior\nis compiled into a build. They should not be used as shorthand for behavior that\nhas not been exercised by the selected profile.</p>\n<hr>\n<h2 id="build-profiles">Build Profiles</h2>\n<ul>\n<li><strong><code>debug</code></strong> - browser-oriented UI development without the full <code>Mountain</code>\ndesktop shell.</li>\n<li><strong><code>debug-mountain</code></strong> - the primary <code>Tauri</code> desktop development profile with\n<code>Mountain</code> and <code>Cocoon</code>.</li>\n<li><strong><code>debug-electron</code></strong> - compatibility path for the legacy <code>Electron</code>\n<code>workbench</code>.</li>\n</ul>\n<p>Release profiles may enable different pieces. Public docs should name the\nprofile when making behavior claims.</p>\n<hr>\n<h2 id="planned-improvements">Planned Improvements</h2>\n<ul>\n<li>Public benchmark suite for startup, memory, file operations, and extension\nactivation.</li>\n<li>Broader installer validation across macOS, Windows, and Linux.</li>\n<li>Continued expansion of extension <code>API</code> coverage through <code>Cocoon</code> and\n<code>Mountain</code>.</li>\n<li>Further separation of background services where sidecars give a clearer\nlifecycle boundary.</li>\n</ul>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/cocoon"><code>Cocoon</code></a></li>\n<li><a href="https://Editor.Land/Doc/vine"><code>Vine</code></a></li>\n<li><a href="https://Editor.Land/Doc/air"><code>Air</code></a></li>\n</ul>', { headings: 1849, localImagePaths: 1874, remoteImagePaths: 1875, frontmatter: 1876, imagePaths: 1877 }, [1850, 1851, 1854, 1857, 1860, 1863, 1866, 1869, 1870, 1873], { depth: 25, slug: 1440, text: 12 }, { depth: 29, slug: 1852, text: 1853 }, "platform-configuration️", "Platform Configuration ⚙️", { depth: 29, slug: 1855, text: 1856 }, "startup-pipeline", "Startup Pipeline 🚀", { depth: 29, slug: 1858, text: 1859 }, "source-areas", "Source Areas", { depth: 29, slug: 1861, text: 1862 }, "native-responsibilities", "Native Responsibilities 🔌", { depth: 29, slug: 1864, text: 1865 }, "extension-integration", "Extension Integration", { depth: 29, slug: 1867, text: 1868 }, "feature-flags️", "Feature Flags ⚙️", { depth: 29, slug: 1752, text: 1753 }, { depth: 29, slug: 1871, text: 1872 }, "planned-improvements", "Planned Improvements", { depth: 29, slug: 401, text: 402 }, [], [], { title: 12, section: 381, order: 1841, description: 1842 }, [], "mountain-naming-conventions", { id: 1878, data: 1880, body: 1884, filePath: 1885, digest: 1886, rendered: 1887 }, { title: 1881, section: 582, order: 1882, description: 1883 }, "Mountain - Naming Conventions", 217, "PascalCase naming conventions for cross-language interoperability across the Land ecosystem", "# Mountain - Naming Conventions\n\nMountain adopts PascalCase as the primary naming convention for nearly all Rust\nelements, diverging from Rust traditional snake_case. This intentional choice\nserves critical ecosystem requirements.\n\n## Rationale\n\nThe Land ecosystem consists of multiple language-specific components that must\ninteroperate seamlessly:\n\n| Component | Language           | Naming Convention |\n| :-------- | :----------------- | :---------------- |\n| Mountain  | Rust               | PascalCase        |\n| Cocoon    | Node.js/TypeScript | PascalCase        |\n| Wind      | TypeScript/React   | PascalCase        |\n| Common    | Rust (shared)      | PascalCase        |\n| Vine gRPC | Protocol Buffers   | PascalCase        |\n\nUsing PascalCase across all components eliminates cognitive overhead during\ncross-language development and ensures type safety in gRPC interfaces.\n\nEach module that follows this convention includes:\n\n```rust\n#![allow(non_snake_case, non_camel_case_types)]\n```\n\n## DTO Alignment\n\nData Transfer Objects in Rust map directly to Protocol Buffer messages,\nTypeScript interfaces, and JSON schemas without field name mapping:\n\n```rust\n// Rust DTO\npub struct WorkSpaceFolderStateDTO {\n    pub URI: url::Url,\n    pub Name: String,\n    pub Index: usize,\n}\n```\n\nDirect correspondence with protobuf and TypeScript eliminates serde rename\nattributes and custom protobuf code generation filters.\n\n## Complete Naming Patterns\n\n| Element Type    | Convention | Example                        |\n| :-------------- | :--------- | :----------------------------- |\n| Structs         | PascalCase | WorkSpaceFolderStateDTO        |\n| Enums           | PascalCase | CommandHandler                 |\n| Traits          | PascalCase | ConfigurationProvider          |\n| Functions       | PascalCase | GetConfigurationValue          |\n| Methods         | PascalCase | CreateWorkSpace                |\n| Modules         | PascalCase | ApplicationState               |\n| Constants       | PascalCase | MAX_CONNECTIONS                |\n| Static Items    | PascalCase | SIDECAR_CLIENTS                |\n| Type Aliases    | PascalCase | CocoonClient                   |\n| Generics        | PascalCase | TCapabilityProvider (T prefix) |\n| Lifetimes       | lowercase  | 'a, 'result (standard Rust)    |\n| Local Variables | PascalCase | WorkSpaceIdentifier            |\n| Parameters      | PascalCase | ApplicationHandle              |\n| Fields          | PascalCase | ActiveDocuments                |\n| File Names      | PascalCase | ApplicationState.rs            |\n\n## Allowed Exceptions\n\n1. **External crate types**: tokio, tauri, serde_json retain their original\n   conventions\n2. **Standard library types**: std types and methods use Rust conventions\n3. **Lifetime parameters**: Standard Rust convention (lowercase with prefix)\n4. **Attributes and macros**: Built-in attributes like derive, async_trait\n   retain Rust conventions\n5. **Raw identifiers**: r#async and system-level constants may use lowercase\n\n## Migration Guidelines\n\nNew code must follow PascalCase. Checklist:\n\n1. Add allow attribute at module top\n2. Use PascalCase for struct/enum/trait names\n3. Use PascalCase for all functions and methods\n4. Use PascalCase for all fields and variables\n5. Align DTO fields with Protocol Buffer message fields\n6. Use PascalCase for generic type parameters with T prefix\n7. Use PascalCase for file names\n\n## Implementation Status\n\nAll core modules follow PascalCase:\n\n- ApplicationState - complete\n- Environment - complete (24 providers)\n- RunTime - complete\n- Track - complete\n- Vine - complete\n- IPC - complete\n- WorkSpace - complete\n- All DTOs - complete (13 DTO types)\n\n## Related\n\n- [Mountain overview](/doc/mountain)\n- [Mountain deep dive](/doc/deep-dive-mountain)\n- [Common service contracts](/doc/deep-dive-common)", "Source/Content/doc/mountain-naming-conventions.md", "ebf77838e1c7875f", { html: 1888, metadata: 1889 }, '<h1 id="mountain---naming-conventions">Mountain - Naming Conventions</h1>\n<p>Mountain adopts PascalCase as the primary naming convention for nearly all Rust\nelements, diverging from Rust traditional snake_case. This intentional choice\nserves critical ecosystem requirements.</p>\n<h2 id="rationale">Rationale</h2>\n<p>The Land ecosystem consists of multiple language-specific components that must\ninteroperate seamlessly:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th align="left">Component</th><th align="left">Language</th><th align="left">Naming Convention</th></tr></thead><tbody><tr><td align="left">Mountain</td><td align="left">Rust</td><td align="left">PascalCase</td></tr><tr><td align="left">Cocoon</td><td align="left">Node.js/TypeScript</td><td align="left">PascalCase</td></tr><tr><td align="left">Wind</td><td align="left">TypeScript/React</td><td align="left">PascalCase</td></tr><tr><td align="left">Common</td><td align="left">Rust (shared)</td><td align="left">PascalCase</td></tr><tr><td align="left">Vine gRPC</td><td align="left">Protocol Buffers</td><td align="left">PascalCase</td></tr></tbody></table>\n<p>Using PascalCase across all components eliminates cognitive overhead during\ncross-language development and ensures type safety in gRPC interfaces.</p>\n<p>Each module that follows this convention includes:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="rust"><code><span class="line"><span style="color:#E1E4E8">#![allow(non_snake_case, non_camel_case_types)]</span></span></code></pre>\n<h2 id="dto-alignment">DTO Alignment</h2>\n<p>Data Transfer Objects in Rust map directly to Protocol Buffer messages,\nTypeScript interfaces, and JSON schemas without field name mapping:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="rust"><code><span class="line"><span style="color:#6A737D">// Rust DTO</span></span>\n<span class="line"><span style="color:#F97583">pub</span><span style="color:#F97583"> struct</span><span style="color:#B392F0"> WorkSpaceFolderStateDTO</span><span style="color:#E1E4E8"> {</span></span>\n<span class="line"><span style="color:#F97583">    pub</span><span style="color:#79B8FF"> URI</span><span style="color:#F97583">:</span><span style="color:#B392F0"> url</span><span style="color:#F97583">::</span><span style="color:#B392F0">Url</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#F97583">    pub</span><span style="color:#B392F0"> Name</span><span style="color:#F97583">:</span><span style="color:#B392F0"> String</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#F97583">    pub</span><span style="color:#B392F0"> Index</span><span style="color:#F97583">:</span><span style="color:#B392F0"> usize</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>\n<p>Direct correspondence with protobuf and TypeScript eliminates serde rename\nattributes and custom protobuf code generation filters.</p>\n<h2 id="complete-naming-patterns">Complete Naming Patterns</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th align="left">Element Type</th><th align="left">Convention</th><th align="left">Example</th></tr></thead><tbody><tr><td align="left">Structs</td><td align="left">PascalCase</td><td align="left">WorkSpaceFolderStateDTO</td></tr><tr><td align="left">Enums</td><td align="left">PascalCase</td><td align="left">CommandHandler</td></tr><tr><td align="left">Traits</td><td align="left">PascalCase</td><td align="left">ConfigurationProvider</td></tr><tr><td align="left">Functions</td><td align="left">PascalCase</td><td align="left">GetConfigurationValue</td></tr><tr><td align="left">Methods</td><td align="left">PascalCase</td><td align="left">CreateWorkSpace</td></tr><tr><td align="left">Modules</td><td align="left">PascalCase</td><td align="left">ApplicationState</td></tr><tr><td align="left">Constants</td><td align="left">PascalCase</td><td align="left">MAX_CONNECTIONS</td></tr><tr><td align="left">Static Items</td><td align="left">PascalCase</td><td align="left">SIDECAR_CLIENTS</td></tr><tr><td align="left">Type Aliases</td><td align="left">PascalCase</td><td align="left">CocoonClient</td></tr><tr><td align="left">Generics</td><td align="left">PascalCase</td><td align="left">TCapabilityProvider (T prefix)</td></tr><tr><td align="left">Lifetimes</td><td align="left">lowercase</td><td align="left">’a, ‘result (standard Rust)</td></tr><tr><td align="left">Local Variables</td><td align="left">PascalCase</td><td align="left">WorkSpaceIdentifier</td></tr><tr><td align="left">Parameters</td><td align="left">PascalCase</td><td align="left">ApplicationHandle</td></tr><tr><td align="left">Fields</td><td align="left">PascalCase</td><td align="left">ActiveDocuments</td></tr><tr><td align="left">File Names</td><td align="left">PascalCase</td><td align="left">ApplicationState.rs</td></tr></tbody></table>\n<h2 id="allowed-exceptions">Allowed Exceptions</h2>\n<ol>\n<li><strong>External crate types</strong>: tokio, tauri, serde_json retain their original\nconventions</li>\n<li><strong>Standard library types</strong>: std types and methods use Rust conventions</li>\n<li><strong>Lifetime parameters</strong>: Standard Rust convention (lowercase with prefix)</li>\n<li><strong>Attributes and macros</strong>: Built-in attributes like derive, async_trait\nretain Rust conventions</li>\n<li><strong>Raw identifiers</strong>: r#async and system-level constants may use lowercase</li>\n</ol>\n<h2 id="migration-guidelines">Migration Guidelines</h2>\n<p>New code must follow PascalCase. Checklist:</p>\n<ol>\n<li>Add allow attribute at module top</li>\n<li>Use PascalCase for struct/enum/trait names</li>\n<li>Use PascalCase for all functions and methods</li>\n<li>Use PascalCase for all fields and variables</li>\n<li>Align DTO fields with Protocol Buffer message fields</li>\n<li>Use PascalCase for generic type parameters with T prefix</li>\n<li>Use PascalCase for file names</li>\n</ol>\n<h2 id="implementation-status">Implementation Status</h2>\n<p>All core modules follow PascalCase:</p>\n<ul>\n<li>ApplicationState - complete</li>\n<li>Environment - complete (24 providers)</li>\n<li>RunTime - complete</li>\n<li>Track - complete</li>\n<li>Vine - complete</li>\n<li>IPC - complete</li>\n<li>WorkSpace - complete</li>\n<li>All DTOs - complete (13 DTO types)</li>\n</ul>\n<h2 id="related">Related</h2>\n<ul>\n<li><a href="/doc/mountain">Mountain overview</a></li>\n<li><a href="/doc/deep-dive-mountain">Mountain deep dive</a></li>\n<li><a href="/doc/deep-dive-common">Common service contracts</a></li>\n</ul>', { headings: 1890, localImagePaths: 1912, remoteImagePaths: 1913, frontmatter: 1914, imagePaths: 1915 }, [1891, 1893, 1896, 1899, 1902, 1905, 1908, 1911], { depth: 25, slug: 1892, text: 1881 }, "mountain---naming-conventions", { depth: 29, slug: 1894, text: 1895 }, "rationale", "Rationale", { depth: 29, slug: 1897, text: 1898 }, "dto-alignment", "DTO Alignment", { depth: 29, slug: 1900, text: 1901 }, "complete-naming-patterns", "Complete Naming Patterns", { depth: 29, slug: 1903, text: 1904 }, "allowed-exceptions", "Allowed Exceptions", { depth: 29, slug: 1906, text: 1907 }, "migration-guidelines", "Migration Guidelines", { depth: 29, slug: 1909, text: 1910 }, "implementation-status", "Implementation Status", { depth: 29, slug: 619, text: 620 }, [], [], { title: 1881, section: 582, order: 1882, description: 1883 }, [], { id: 1458, data: 1917, body: 1920, filePath: 1921, digest: 1922, rendered: 1923 }, { title: 1459, section: 381, order: 1918, description: 1919 }, 22, "The VS Code build transformation pipeline that adapts VS Code's compiled output for Editor.Land's native stack.", "# Output\n\n`Output` is the `VS Code` build transformation pipeline for `Editor.Land`. It\ntakes the upstream `VS Code` compiled tree (populated into\n`Target/Microsoft/VSCode/` by the build step) and applies a set of `TypeScript`\ntransform plugins that adapt it for Land's native stack - replacing `Electron`\n`IPC` with the `Mountain` `gRPC` channel, injecting WebView polyfills, rewriting\nworker URLs, and patching service registrations.\n\n`Output` is a `TypeScript`/`Node.js` package. Its source is in the\n[`Output` repository](https://github.com/CodeEditorLand/Output).\n\n---\n\n## Why a Transformation Pipeline\n\n`VS Code`'s compiled output is written for `Electron`: it expects `ipcRenderer`,\nshared process communication, `Electron`-specific node integration, and a\nbrowser environment supplied by Chromium. `Editor.Land` replaces all of that\nwith `Mountain`'s `Rust` kernel, `WebView2` (Windows) or `WKWebView` (macOS),\nand the `Vine` `gRPC` protocol.\n\nRather than maintaining a permanent fork of `VS Code`'s `TypeScript` source,\n`Output` applies targeted transforms to the compiled output. The upstream source\nremains untouched; the adaptation is fully described by the plugin list and can\nbe updated as `VS Code` evolves.\n\n---\n\n## The Transform Plugins\n\n`Output`'s `ApplyPipeline.ts` runs 21 active transform plugins against the\n`VS Code` compiled tree. Each plugin has a `Match` filter and rewrites only the\nspecific files it targets:\n\n| Plugin                         | What it does                                                                                                                                                 |\n| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `ReplaceElectronIPCService`    | Replaces `vs/platform/ipc/electron-browser/mainProcessService.js` with `TauriMainProcessService.js`, routing all VS Code IPC through Mountain's gRPC channel |\n| `InjectWebViewPolyfills`       | Injects polyfills required by WKWebView / WebView2 that Chromium provides natively in Electron                                                               |\n| `InjectNameShim`               | Injects the `name` global shim expected by the VS Code workbench bootstrap                                                                                   |\n| `InjectWorkerBootstrapShim`    | Injects the worker bootstrap shim for the WebView worker environment                                                                                         |\n| `RewriteNestedWorkerBootstrap` | Rewrites nested worker bootstrap paths for the WebView runtime                                                                                               |\n| `RewritePerfBaselineWorker`    | Rewrites the performance baseline worker URL for the WebView context                                                                                         |\n| `RewriteNodeModulesPath`       | Rewrites `node_modules` path references to the correct resolved location                                                                                     |\n| `RewriteWorkerURLs`            | Rewrites worker `new URL(...)` constructor paths to WebView-compatible URLs                                                                                  |\n| `RewriteWorkbenchBaseURL`      | Rewrites the workbench static base URL to match Land's serving path                                                                                          |\n| `RewriteStaticBlockSelfRef`    | Rewrites static block self-references that do not survive the WebView load context                                                                           |\n| `HoistFunctionDeclarations`    | Hoists function declarations that VS Code relies on being in scope before module evaluation                                                                  |\n| `ReplaceSharedProcess`         | Replaces the Electron shared process channel with the Mountain equivalent                                                                                    |\n| `ExtensionScannerIPC`          | Patches extension scanner IPC to route through Mountain                                                                                                      |\n| `StripDanglingSourceMap`       | Removes dangling `//# sourceMappingURL` references that point to files not present in the output tree                                                        |\n| `CatchOutputFolderRejection`   | Catches unhandled promise rejections from output folder operations                                                                                           |\n| `StripWebviewIframeSandbox`    | Removes Electron-specific `sandbox` attributes from WebView iframes                                                                                          |\n| `ExposeWorkbenchAccessor`      | Exposes the workbench instance accessor for Mountain's integration surface                                                                                   |\n| `InstrumentVscodeGit`          | Instruments the VS Code Git extension to use Mountain's file system layer                                                                                    |\n| `DisableUnusedServices`        | Disables Electron-specific services that have no equivalent in the WebView stack                                                                             |\n| `ReplaceSearchService`         | Replaces VS Code's Electron search service with Mountain's ripgrep-based implementation                                                                      |\n| `PatchLocalTerminalBackend`    | Patches the local terminal backend to route through Mountain's pty layer                                                                                     |\n\n---\n\n## Dual-Consumer Architecture\n\n`Output`'s transformed `Target/Microsoft/VSCode/` tree is consumed by `Sky` in\ntwo different ways:\n\n- **`Sky`'s `/Static/Application/` copy** - static files served directly by\n  `Mountain`'s HTTP layer to the WebView at runtime. These files use dynamic\n  imports and runtime CSS loading.\n- **`Sky`'s `Vite` bundler walk** - `Vite` follows the module graph from\n  `Output`'s Target before `astro:build:done` fires. This path requires static\n  imports so `Rollup` can emit hashed chunks; CSS is extracted by `Vite`'s\n  native pipeline.\n\nBecause `Vite` walks `Output`'s Target before `Sky`'s build hooks run, `Output`\nmust ship a pre-transformed tree. Transform plugins that convert static imports\nto dynamic forms (`StaticToDynamicImport`, `StripCSSImport`) are intentionally\nexcluded from `Output`'s pipeline and run only in `Sky`'s `astro:build:done`\nhook for the static path. The two output trees diverge here by design.\n\n---\n\n## Source Structure 🗺️\n\n| Path                      | Role                                                              |\n| ------------------------- | ----------------------------------------------------------------- |\n| `Source/ApplyPipeline.ts` | Pipeline runner - composes and executes the transform plugin list |\n| `Source/ESBuild.ts`       | esbuild integration                                               |\n| `Source/ESBuild/`         | esbuild configuration sub-modules                                 |\n| `Source/Plugin/`          | Transform plugin definitions (one file per plugin)                |\n| `Source/Polyfill/`        | WebView polyfill sources                                          |\n| `Source/Service/`         | Service-layer utilities                                           |\n| `Source/tsconfig/`        | TypeScript configuration variants                                 |\n\n---\n\n## Key Technologies\n\n`TypeScript`, `Node.js`, `esbuild`, Plugin-Routed AST Transforms, WebView\nPolyfills, `Tauri`/`Mountain` `IPC` Replacement.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Rest`](https://Editor.Land/Doc/rest)\n- [`Mountain`: Native Kernel](https://Editor.Land/Doc/mountain)\n- [`Sky`: WebView UI](https://Editor.Land/Doc/sky)\n- [Source Code](https://github.com/CodeEditorLand/Output)", "Source/Content/doc/output.md", "03de0c645f4820ff", { html: 1924, metadata: 1925 }, '<h1 id="output">Output</h1>\n<p><code>Output</code> is the <code>VS Code</code> build transformation pipeline for <code>Editor.Land</code>. It\ntakes the upstream <code>VS Code</code> compiled tree (populated into\n<code>Target/Microsoft/VSCode/</code> by the build step) and applies a set of <code>TypeScript</code>\ntransform plugins that adapt it for Land’s native stack - replacing <code>Electron</code>\n<code>IPC</code> with the <code>Mountain</code> <code>gRPC</code> channel, injecting WebView polyfills, rewriting\nworker URLs, and patching service registrations.</p>\n<p><code>Output</code> is a <code>TypeScript</code>/<code>Node.js</code> package. Its source is in the\n<a href="https://github.com/CodeEditorLand/Output"><code>Output</code> repository</a>.</p>\n<hr>\n<h2 id="why-a-transformation-pipeline">Why a Transformation Pipeline</h2>\n<p><code>VS Code</code>’s compiled output is written for <code>Electron</code>: it expects <code>ipcRenderer</code>,\nshared process communication, <code>Electron</code>-specific node integration, and a\nbrowser environment supplied by Chromium. <code>Editor.Land</code> replaces all of that\nwith <code>Mountain</code>’s <code>Rust</code> kernel, <code>WebView2</code> (Windows) or <code>WKWebView</code> (macOS),\nand the <code>Vine</code> <code>gRPC</code> protocol.</p>\n<p>Rather than maintaining a permanent fork of <code>VS Code</code>’s <code>TypeScript</code> source,\n<code>Output</code> applies targeted transforms to the compiled output. The upstream source\nremains untouched; the adaptation is fully described by the plugin list and can\nbe updated as <code>VS Code</code> evolves.</p>\n<hr>\n<h2 id="the-transform-plugins">The Transform Plugins</h2>\n<p><code>Output</code>’s <code>ApplyPipeline.ts</code> runs 21 active transform plugins against the\n<code>VS Code</code> compiled tree. Each plugin has a <code>Match</code> filter and rewrites only the\nspecific files it targets:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Plugin</th><th>What it does</th></tr></thead><tbody><tr><td><code>ReplaceElectronIPCService</code></td><td>Replaces <code>vs/platform/ipc/electron-browser/mainProcessService.js</code> with <code>TauriMainProcessService.js</code>, routing all VS Code IPC through Mountain’s gRPC channel</td></tr><tr><td><code>InjectWebViewPolyfills</code></td><td>Injects polyfills required by WKWebView / WebView2 that Chromium provides natively in Electron</td></tr><tr><td><code>InjectNameShim</code></td><td>Injects the <code>name</code> global shim expected by the VS Code workbench bootstrap</td></tr><tr><td><code>InjectWorkerBootstrapShim</code></td><td>Injects the worker bootstrap shim for the WebView worker environment</td></tr><tr><td><code>RewriteNestedWorkerBootstrap</code></td><td>Rewrites nested worker bootstrap paths for the WebView runtime</td></tr><tr><td><code>RewritePerfBaselineWorker</code></td><td>Rewrites the performance baseline worker URL for the WebView context</td></tr><tr><td><code>RewriteNodeModulesPath</code></td><td>Rewrites <code>node_modules</code> path references to the correct resolved location</td></tr><tr><td><code>RewriteWorkerURLs</code></td><td>Rewrites worker <code>new URL(...)</code> constructor paths to WebView-compatible URLs</td></tr><tr><td><code>RewriteWorkbenchBaseURL</code></td><td>Rewrites the workbench static base URL to match Land’s serving path</td></tr><tr><td><code>RewriteStaticBlockSelfRef</code></td><td>Rewrites static block self-references that do not survive the WebView load context</td></tr><tr><td><code>HoistFunctionDeclarations</code></td><td>Hoists function declarations that VS Code relies on being in scope before module evaluation</td></tr><tr><td><code>ReplaceSharedProcess</code></td><td>Replaces the Electron shared process channel with the Mountain equivalent</td></tr><tr><td><code>ExtensionScannerIPC</code></td><td>Patches extension scanner IPC to route through Mountain</td></tr><tr><td><code>StripDanglingSourceMap</code></td><td>Removes dangling <code>//# sourceMappingURL</code> references that point to files not present in the output tree</td></tr><tr><td><code>CatchOutputFolderRejection</code></td><td>Catches unhandled promise rejections from output folder operations</td></tr><tr><td><code>StripWebviewIframeSandbox</code></td><td>Removes Electron-specific <code>sandbox</code> attributes from WebView iframes</td></tr><tr><td><code>ExposeWorkbenchAccessor</code></td><td>Exposes the workbench instance accessor for Mountain’s integration surface</td></tr><tr><td><code>InstrumentVscodeGit</code></td><td>Instruments the VS Code Git extension to use Mountain’s file system layer</td></tr><tr><td><code>DisableUnusedServices</code></td><td>Disables Electron-specific services that have no equivalent in the WebView stack</td></tr><tr><td><code>ReplaceSearchService</code></td><td>Replaces VS Code’s Electron search service with Mountain’s ripgrep-based implementation</td></tr><tr><td><code>PatchLocalTerminalBackend</code></td><td>Patches the local terminal backend to route through Mountain’s pty layer</td></tr></tbody></table>\n<hr>\n<h2 id="dual-consumer-architecture">Dual-Consumer Architecture</h2>\n<p><code>Output</code>’s transformed <code>Target/Microsoft/VSCode/</code> tree is consumed by <code>Sky</code> in\ntwo different ways:</p>\n<ul>\n<li><strong><code>Sky</code>’s <code>/Static/Application/</code> copy</strong> - static files served directly by\n<code>Mountain</code>’s HTTP layer to the WebView at runtime. These files use dynamic\nimports and runtime CSS loading.</li>\n<li><strong><code>Sky</code>’s <code>Vite</code> bundler walk</strong> - <code>Vite</code> follows the module graph from\n<code>Output</code>’s Target before <code>astro:build:done</code> fires. This path requires static\nimports so <code>Rollup</code> can emit hashed chunks; CSS is extracted by <code>Vite</code>’s\nnative pipeline.</li>\n</ul>\n<p>Because <code>Vite</code> walks <code>Output</code>’s Target before <code>Sky</code>’s build hooks run, <code>Output</code>\nmust ship a pre-transformed tree. Transform plugins that convert static imports\nto dynamic forms (<code>StaticToDynamicImport</code>, <code>StripCSSImport</code>) are intentionally\nexcluded from <code>Output</code>’s pipeline and run only in <code>Sky</code>’s <code>astro:build:done</code>\nhook for the static path. The two output trees diverge here by design.</p>\n<hr>\n<h2 id="source-structure️">Source Structure 🗺️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Path</th><th>Role</th></tr></thead><tbody><tr><td><code>Source/ApplyPipeline.ts</code></td><td>Pipeline runner - composes and executes the transform plugin list</td></tr><tr><td><code>Source/ESBuild.ts</code></td><td>esbuild integration</td></tr><tr><td><code>Source/ESBuild/</code></td><td>esbuild configuration sub-modules</td></tr><tr><td><code>Source/Plugin/</code></td><td>Transform plugin definitions (one file per plugin)</td></tr><tr><td><code>Source/Polyfill/</code></td><td>WebView polyfill sources</td></tr><tr><td><code>Source/Service/</code></td><td>Service-layer utilities</td></tr><tr><td><code>Source/tsconfig/</code></td><td>TypeScript configuration variants</td></tr></tbody></table>\n<hr>\n<h2 id="key-technologies">Key Technologies</h2>\n<p><code>TypeScript</code>, <code>Node.js</code>, <code>esbuild</code>, Plugin-Routed AST Transforms, WebView\nPolyfills, <code>Tauri</code>/<code>Mountain</code> <code>IPC</code> Replacement.</p>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/rest"><code>Rest</code></a></li>\n<li><a href="https://Editor.Land/Doc/mountain"><code>Mountain</code>: Native Kernel</a></li>\n<li><a href="https://Editor.Land/Doc/sky"><code>Sky</code>: WebView UI</a></li>\n<li><a href="https://github.com/CodeEditorLand/Output">Source Code</a></li>\n</ul>', { headings: 1926, localImagePaths: 1940, remoteImagePaths: 1941, frontmatter: 1942, imagePaths: 1943 }, [1927, 1928, 1931, 1934, 1937, 1938, 1939], { depth: 25, slug: 1458, text: 1459 }, { depth: 29, slug: 1929, text: 1930 }, "why-a-transformation-pipeline", "Why a Transformation Pipeline", { depth: 29, slug: 1932, text: 1933 }, "the-transform-plugins", "The Transform Plugins", { depth: 29, slug: 1935, text: 1936 }, "dual-consumer-architecture", "Dual-Consumer Architecture", { depth: 29, slug: 393, text: 394 }, { depth: 29, slug: 1685, text: 1686 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 1459, section: 381, order: 1918, description: 1919 }, [], { id: 1599, data: 1945, body: 1948, filePath: 1949, digest: 1950, rendered: 1951 }, { title: 1600, section: 493, order: 1946, description: 1947 }, 46, "Overview of the Element directory organization, funding configuration, dependabot setup, and CI/CD workflow patterns.", '# Project Structure\n\nThe Land monorepo uses Git submodules to compose 15 independent Elements under\n`Land/Element/`. Each Element is its own repository with its own versioning,\nchangelog, and CI pipeline.\n\n---\n\n## Element Directory Layout\n\n```\nLand/Element/\n    .gitmodules          # 15 submodule definitions\n    CHANGELOG.md         # Umbrella-level changelog (cross-cutting syncs)\n    .github/\n        Update.md        # Automated daily timestamp (Auto.yml)\n        FUNDING.yml      # Open Collective funding configuration\n        dependabot.yml   # Root-level Dependabot (not used; per-element configs apply)\n        workflows/       # Shared workflow templates (if any)\n    Air/                 # Background daemon sidecar\n    Cocoon/              # Extension-host Node.js sidecar\n    Common/              # Abstract core Rust crate\n    Echo/                # Work-stealing task scheduler\n    Grove/               # Rust/WASM extension-host sidecar\n    Maintain/            # Build/CI surface\n    Mist/                # DNS isolation server\n    Mountain/            # Native Rust Tauri backend\n    Output/              # Bundled JS artifact tree\n    Rest/                # JS bundler (OXC)\n    SideCar/             # Node.js sidecar runtime\n    Sky/                 # Astro UI component layer\n    Vine/                # gRPC protocol definitions\n    Wind/                # Effect-TS UI service layer\n    Worker/              # Service-worker layer\n```\n\n### Per-Element Structure\n\nEach Element repository contains:\n\n```\nElementName/\n    .github/\n        Update.md        # Last-commit timestamp (daily automation)\n        FUNDING.yml      # Open Collective: code-editor-land\n        dependabot.yml   # Per-ecosystem dependency updates\n        workflows/       # CI workflow definitions\n    CHANGELOG.md         # Version history (Keep a Changelog format)\n    Cargo.toml           # (Rust elements) package manifest\n    package.json         # (Node/TS elements) package manifest\n    README.md            # Element documentation\n```\n\n### Elements Without Dedicated .github/ Directory\n\nSome smaller or placeholder elements (Grove, Vine, Mist) did not have a\n`.github/` directory with `Update.md` at the time of documentation. Grove has\nworkflows and dependabot under `.github/` but its changelog lives in\n`CHANGELOG.md` at the root.\n\n---\n\n## Funding Configuration\n\nAll elements with a `.github/FUNDING.yml` file point to the same Open\nCollective:\n\n```yaml\nopen_collective: code-editor-land\n```\n\nElements with FUNDING.yml (8 of 15):\n\n- Element (umbrella)\n- Echo\n- Mountain\n- Rest\n- SideCar\n- Sky\n- Wind\n- Maintain\n\nElements without a dedicated FUNDING.yml (7):\n\n- Air, Cocoon, Common, Grove, Mist, Output, Vine, Worker\n\nAll funding converges on the\n[Open Collective for Code Editor Land](https://opencollective.com/code-editor-land).\n\n---\n\n## Dependabot Configuration\n\nAll 13 elements with a `.github/dependabot.yml` share the same base pattern:\n\n```yaml\nversion: 2\nenable-beta-ecosystems: true\nupdates:\n    - package-ecosystem: "github-actions"\n      directory: "/"\n      schedule:\n          interval: "daily"\n```\n\nRust elements (Mountain, Air, Common, Echo, Grove, Mist, Maintain, Rest,\nSideCar, Vine) add:\n\n```yaml\n- package-ecosystem: "cargo"\n  directory: "/"\n  schedule:\n      interval: "daily"\n  versioning-strategy: lockfile-only\n```\n\nTypeScript/JS elements (Wind, Cocoon, Output, Sky, Worker) add:\n\n```yaml\n- package-ecosystem: "npm"\n  directory: "/"\n  schedule:\n      interval: "daily"\n  versioning-strategy: increase\n  ignore:\n      - dependency-name: "tailwindcss"\n        versions:\n            - "^4.0.0"\n```\n\nKey policy: all dependencies update daily. Cargo uses lockfile-only to preserve\nthe source tree; npm uses `increase` to bump package.json. Tailwind 4.x is\nexplicitly ignored across JS elements.\n\n---\n\n## CI/CD Workflow Overview\n\nFive standard workflow types appear across elements:\n\n### Rust.yml\n\nPresent in: Mountain, Air, Common, Echo, Grove, Mist, Rest, SideCar, Vine,\nMaintain\n\nRuns on push/PR to `Current` branch. Builds with both `stable` and `nightly`\ntoolchains using `cargo build --release --all-features`. Caches Cargo registry\nand target directories.\n\n### Node.yml\n\nPresent in: Wind, Cocoon, Worker\n\nRuns on push/PR to `Current`. Pre-publishes with pnpm across a Node version\nmatrix (18, 19, 20). Uploads build artefacts.\n\n### NPM.yml\n\nPresent in: Echo, Wind, Worker\n\nPublishes packages on release creation. Runs against Node 24 with\n`npm publish --legacy-peer-deps --ignore-scripts`.\n\n### GitHub.yml\n\nPresent in all elements with CI configuration.\n\nAuto-assigns new issues and PRs to the repository maintainer using\n`pozil/auto-assign-issue`.\n\n### Auto.yml\n\nPresent in: Echo, Worker\n\nScheduled daily (`cron: 0 0 * * *`). Updates `.github/Update.md` with current\ntimestamp and commits the change. This is how the single-line Update.md files\nare maintained.\n\n### Cloudflare.yml\n\nPresent in: Echo\n\nCloudflare Workers deployment pipeline.\n\n---\n\n## Default Environment Variables\n\nAll CI workflows set the same 30+ telemetry-opt-out environment variables to\nprevent third-party analytics from cloud build hosts (Adblock, Astro, Azure,\nDocker, Gatsby, Homebrew, InfluxDB, Next.js, Nuxt, PowerShell, Stripe,\nTerraform, VCPkg, and others).', "Source/Content/doc/project-structure.md", "cc808ce2c7d41058", { html: 1952, metadata: 1953 }, '<h1 id="project-structure">Project Structure</h1>\n<p>The Land monorepo uses Git submodules to compose 15 independent Elements under\n<code>Land/Element/</code>. Each Element is its own repository with its own versioning,\nchangelog, and CI pipeline.</p>\n<hr>\n<h2 id="element-directory-layout">Element Directory Layout</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>Land/Element/</span></span>\n<span class="line"><span>    .gitmodules          # 15 submodule definitions</span></span>\n<span class="line"><span>    CHANGELOG.md         # Umbrella-level changelog (cross-cutting syncs)</span></span>\n<span class="line"><span>    .github/</span></span>\n<span class="line"><span>        Update.md        # Automated daily timestamp (Auto.yml)</span></span>\n<span class="line"><span>        FUNDING.yml      # Open Collective funding configuration</span></span>\n<span class="line"><span>        dependabot.yml   # Root-level Dependabot (not used; per-element configs apply)</span></span>\n<span class="line"><span>        workflows/       # Shared workflow templates (if any)</span></span>\n<span class="line"><span>    Air/                 # Background daemon sidecar</span></span>\n<span class="line"><span>    Cocoon/              # Extension-host Node.js sidecar</span></span>\n<span class="line"><span>    Common/              # Abstract core Rust crate</span></span>\n<span class="line"><span>    Echo/                # Work-stealing task scheduler</span></span>\n<span class="line"><span>    Grove/               # Rust/WASM extension-host sidecar</span></span>\n<span class="line"><span>    Maintain/            # Build/CI surface</span></span>\n<span class="line"><span>    Mist/                # DNS isolation server</span></span>\n<span class="line"><span>    Mountain/            # Native Rust Tauri backend</span></span>\n<span class="line"><span>    Output/              # Bundled JS artifact tree</span></span>\n<span class="line"><span>    Rest/                # JS bundler (OXC)</span></span>\n<span class="line"><span>    SideCar/             # Node.js sidecar runtime</span></span>\n<span class="line"><span>    Sky/                 # Astro UI component layer</span></span>\n<span class="line"><span>    Vine/                # gRPC protocol definitions</span></span>\n<span class="line"><span>    Wind/                # Effect-TS UI service layer</span></span>\n<span class="line"><span>    Worker/              # Service-worker layer</span></span></code></pre>\n<h3 id="per-element-structure">Per-Element Structure</h3>\n<p>Each Element repository contains:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>ElementName/</span></span>\n<span class="line"><span>    .github/</span></span>\n<span class="line"><span>        Update.md        # Last-commit timestamp (daily automation)</span></span>\n<span class="line"><span>        FUNDING.yml      # Open Collective: code-editor-land</span></span>\n<span class="line"><span>        dependabot.yml   # Per-ecosystem dependency updates</span></span>\n<span class="line"><span>        workflows/       # CI workflow definitions</span></span>\n<span class="line"><span>    CHANGELOG.md         # Version history (Keep a Changelog format)</span></span>\n<span class="line"><span>    Cargo.toml           # (Rust elements) package manifest</span></span>\n<span class="line"><span>    package.json         # (Node/TS elements) package manifest</span></span>\n<span class="line"><span>    README.md            # Element documentation</span></span></code></pre>\n<h3 id="elements-without-dedicated-github-directory">Elements Without Dedicated .github/ Directory</h3>\n<p>Some smaller or placeholder elements (Grove, Vine, Mist) did not have a\n<code>.github/</code> directory with <code>Update.md</code> at the time of documentation. Grove has\nworkflows and dependabot under <code>.github/</code> but its changelog lives in\n<code>CHANGELOG.md</code> at the root.</p>\n<hr>\n<h2 id="funding-configuration">Funding Configuration</h2>\n<p>All elements with a <code>.github/FUNDING.yml</code> file point to the same Open\nCollective:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="yaml"><code><span class="line"><span style="color:#85E89D">open_collective</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">code-editor-land</span></span></code></pre>\n<p>Elements with FUNDING.yml (8 of 15):</p>\n<ul>\n<li>Element (umbrella)</li>\n<li>Echo</li>\n<li>Mountain</li>\n<li>Rest</li>\n<li>SideCar</li>\n<li>Sky</li>\n<li>Wind</li>\n<li>Maintain</li>\n</ul>\n<p>Elements without a dedicated FUNDING.yml (7):</p>\n<ul>\n<li>Air, Cocoon, Common, Grove, Mist, Output, Vine, Worker</li>\n</ul>\n<p>All funding converges on the\n<a href="https://opencollective.com/code-editor-land">Open Collective for Code Editor Land</a>.</p>\n<hr>\n<h2 id="dependabot-configuration">Dependabot Configuration</h2>\n<p>All 13 elements with a <code>.github/dependabot.yml</code> share the same base pattern:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="yaml"><code><span class="line"><span style="color:#85E89D">version</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">2</span></span>\n<span class="line"><span style="color:#85E89D">enable-beta-ecosystems</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">true</span></span>\n<span class="line"><span style="color:#85E89D">updates</span><span style="color:#E1E4E8">:</span></span>\n<span class="line"><span style="color:#E1E4E8">    - </span><span style="color:#85E89D">package-ecosystem</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"github-actions"</span></span>\n<span class="line"><span style="color:#85E89D">      directory</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"/"</span></span>\n<span class="line"><span style="color:#85E89D">      schedule</span><span style="color:#E1E4E8">:</span></span>\n<span class="line"><span style="color:#85E89D">          interval</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"daily"</span></span></code></pre>\n<p>Rust elements (Mountain, Air, Common, Echo, Grove, Mist, Maintain, Rest,\nSideCar, Vine) add:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="yaml"><code><span class="line"><span style="color:#E1E4E8">- </span><span style="color:#85E89D">package-ecosystem</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"cargo"</span></span>\n<span class="line"><span style="color:#85E89D">  directory</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"/"</span></span>\n<span class="line"><span style="color:#85E89D">  schedule</span><span style="color:#E1E4E8">:</span></span>\n<span class="line"><span style="color:#85E89D">      interval</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"daily"</span></span>\n<span class="line"><span style="color:#85E89D">  versioning-strategy</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">lockfile-only</span></span></code></pre>\n<p>TypeScript/JS elements (Wind, Cocoon, Output, Sky, Worker) add:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="yaml"><code><span class="line"><span style="color:#E1E4E8">- </span><span style="color:#85E89D">package-ecosystem</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"npm"</span></span>\n<span class="line"><span style="color:#85E89D">  directory</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"/"</span></span>\n<span class="line"><span style="color:#85E89D">  schedule</span><span style="color:#E1E4E8">:</span></span>\n<span class="line"><span style="color:#85E89D">      interval</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"daily"</span></span>\n<span class="line"><span style="color:#85E89D">  versioning-strategy</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">increase</span></span>\n<span class="line"><span style="color:#85E89D">  ignore</span><span style="color:#E1E4E8">:</span></span>\n<span class="line"><span style="color:#E1E4E8">      - </span><span style="color:#85E89D">dependency-name</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"tailwindcss"</span></span>\n<span class="line"><span style="color:#85E89D">        versions</span><span style="color:#E1E4E8">:</span></span>\n<span class="line"><span style="color:#E1E4E8">            - </span><span style="color:#9ECBFF">"^4.0.0"</span></span></code></pre>\n<p>Key policy: all dependencies update daily. Cargo uses lockfile-only to preserve\nthe source tree; npm uses <code>increase</code> to bump package.json. Tailwind 4.x is\nexplicitly ignored across JS elements.</p>\n<hr>\n<h2 id="cicd-workflow-overview">CI/CD Workflow Overview</h2>\n<p>Five standard workflow types appear across elements:</p>\n<h3 id="rustyml">Rust.yml</h3>\n<p>Present in: Mountain, Air, Common, Echo, Grove, Mist, Rest, SideCar, Vine,\nMaintain</p>\n<p>Runs on push/PR to <code>Current</code> branch. Builds with both <code>stable</code> and <code>nightly</code>\ntoolchains using <code>cargo build --release --all-features</code>. Caches Cargo registry\nand target directories.</p>\n<h3 id="nodeyml">Node.yml</h3>\n<p>Present in: Wind, Cocoon, Worker</p>\n<p>Runs on push/PR to <code>Current</code>. Pre-publishes with pnpm across a Node version\nmatrix (18, 19, 20). Uploads build artefacts.</p>\n<h3 id="npmyml">NPM.yml</h3>\n<p>Present in: Echo, Wind, Worker</p>\n<p>Publishes packages on release creation. Runs against Node 24 with\n<code>npm publish --legacy-peer-deps --ignore-scripts</code>.</p>\n<h3 id="githubyml">GitHub.yml</h3>\n<p>Present in all elements with CI configuration.</p>\n<p>Auto-assigns new issues and PRs to the repository maintainer using\n<code>pozil/auto-assign-issue</code>.</p>\n<h3 id="autoyml">Auto.yml</h3>\n<p>Present in: Echo, Worker</p>\n<p>Scheduled daily (<code>cron: 0 0 * * *</code>). Updates <code>.github/Update.md</code> with current\ntimestamp and commits the change. This is how the single-line Update.md files\nare maintained.</p>\n<h3 id="cloudflareyml">Cloudflare.yml</h3>\n<p>Present in: Echo</p>\n<p>Cloudflare Workers deployment pipeline.</p>\n<hr>\n<h2 id="default-environment-variables">Default Environment Variables</h2>\n<p>All CI workflows set the same 30+ telemetry-opt-out environment variables to\nprevent third-party analytics from cloud build hosts (Adblock, Astro, Azure,\nDocker, Gatsby, Homebrew, InfluxDB, Next.js, Nuxt, PowerShell, Stripe,\nTerraform, VCPkg, and others).</p>', { headings: 1954, localImagePaths: 1993, remoteImagePaths: 1994, frontmatter: 1995, imagePaths: 1996 }, [1955, 1956, 1959, 1962, 1965, 1968, 1969, 1972, 1975, 1978, 1981, 1984, 1987, 1990], { depth: 25, slug: 1599, text: 1600 }, { depth: 29, slug: 1957, text: 1958 }, "element-directory-layout", "Element Directory Layout", { depth: 347, slug: 1960, text: 1961 }, "per-element-structure", "Per-Element Structure", { depth: 347, slug: 1963, text: 1964 }, "elements-without-dedicated-github-directory", "Elements Without Dedicated .github/ Directory", { depth: 29, slug: 1966, text: 1967 }, "funding-configuration", "Funding Configuration", { depth: 29, slug: 1273, text: 1274 }, { depth: 29, slug: 1970, text: 1971 }, "cicd-workflow-overview", "CI/CD Workflow Overview", { depth: 347, slug: 1973, text: 1974 }, "rustyml", "Rust.yml", { depth: 347, slug: 1976, text: 1977 }, "nodeyml", "Node.yml", { depth: 347, slug: 1979, text: 1980 }, "npmyml", "NPM.yml", { depth: 347, slug: 1982, text: 1983 }, "githubyml", "GitHub.yml", { depth: 347, slug: 1985, text: 1986 }, "autoyml", "Auto.yml", { depth: 347, slug: 1988, text: 1989 }, "cloudflareyml", "Cloudflare.yml", { depth: 29, slug: 1991, text: 1992 }, "default-environment-variables", "Default Environment Variables", [], [], { title: 1600, section: 493, order: 1946, description: 1947 }, [], "quickstart", { id: 1997, data: 1999, body: 2002, filePath: 2003, digest: 2004, rendered: 2005 }, { title: 2e3, section: 1625, order: 347, description: 2001 }, "Quickstart", "What you can do after a successful source build on macOS or Windows.", '# Quickstart\n\nThis guide describes what is available after a successful `cargo tauri dev`\nbuild. It covers macOS and Windows, both of which produce a working editor from\nsource today.\n\n---\n\n## After the Editor Opens\n\nA successful build opens a native window with the `workbench` UI - `WKWebView`\non macOS, `WebView2` on Windows. You should see:\n\n- The editor surface (Monaco) in the main area.\n- A status bar at the bottom of the window.\n- An activity bar on the left with sidebar icons.\n- A command palette accessible via `Cmd+Shift+P` (macOS) or `Ctrl+Shift+P`\n  (Windows).\n\nIf the window opens but is blank or shows an error, the most common cause is a\nmissing or failed submodule. Run `git submodule update --init --recursive` from\nthe Land root and rebuild.\n\n---\n\n## Opening a Project\n\n**File > Open Folder** or `Cmd+O` / `Ctrl+O` opens a directory as a workspace.\nThis routes through `Mountain`\'s file system layer via `Tauri` `IPC`.\n\nIf you have an existing `.vscode/` directory with workspace settings, Land reads\nit. See [Configuration](https://Editor.Land/Doc/configuration) for details on\nsupported configuration keys.\n\n---\n\n## Extensions\n\n`Cocoon` activates extensions at startup. The following is confirmed working:\n\n- Extensions present on disk activate through `Cocoon`\'s fiber scheduler.\n- Extensions using standard `vscode.*` file system, terminal, language server,\n  and debug adapter `APIs` work for those `APIs` implemented in `Cocoon`.\n- A `.vsix` file can be installed manually. Open the command palette\n  (`Cmd+Shift+P` / `Ctrl+Shift+P`) and run **Extensions: Install from VSIX**.\n\nThe following is **in progress**:\n\n- An Extensions panel with browsing and search UI.\n- Marketplace or Open VSX Registry integration.\n\nIf an extension activates but its features do not respond, check the\n**`Output`** panel (command palette → **View: Toggle `Output`**) and select the\nextension\'s output channel. Activation errors appear there.\n\n---\n\n## Settings\n\nLand has a fully implemented configuration system backed by `Mountain`\'s\n`ConfigurationState` - a thread-safe, merged store covering both global and\nworkspace scopes. It supports dotted-path key access (e.g. `Editor.FontSize`,\n`Workbench.ColorTheme`) with nested object writes handled by\n`MergedConfigurationStateDTO`. Memento storage for crash recovery is also active\nfor both global and workspace contexts.\n\nOpen the command palette and search for **Preferences: Open Settings** to locate\nand edit the active settings file. The settings format is `JSON`, compatible\nwith `VS Code`\'s `settings.json` structure.\n\n---\n\n## Terminal\n\nThe integrated terminal is backed by `Mountain`\'s pty layer. Open it with\n`` Cmd+` `` (macOS) or `` Ctrl+` `` (Windows). This routes a spawn call through\n`Vine` `gRPC` to `Mountain`, which creates a native pty. The terminal is one of\nthe most reliable features in the current build, mapping directly to verified\n`Vine` service definitions.\n\n---\n\n## Tasks\n\nLand reads task definitions from `.vscode/tasks.json`. Use **Tasks: Run Task**\nfrom the command palette to run configured tasks. The standard\n`.vscode/tasks.json` format is supported:\n\n```json\n{\n	"version": "2.0.0",\n	"tasks": [\n		{\n			"label": "Build",\n			"type": "shell",\n			"command": "cargo build",\n			"group": "build"\n		}\n	]\n}\n```\n\n---\n\n## Extension API Notes\n\nThe vast majority of `vscode.*` `APIs` covering file system, terminal, language\nserver protocol, diagnostics, status bar, tree views, custom editors, and\nwebview panels are routed and active. The following `APIs` have stub\nimplementations - they allow extensions that declare them to activate without\ncrashing, but the underlying features are not yet wired to a backend:\n\n- `vscode.lm.*` - language model / Copilot\n- `vscode.chat.*` - chat panel\n- `vscode.notebook.*` - notebook UI\n- `vscode.tests.*` - test explorer\n\nSee [`Cocoon`](https://Editor.Land/Doc/cocoon) for the full `API` coverage\ntable.\n\n---\n\n## See Also\n\n- [Getting Started](https://Editor.Land/Doc/getting-started)\n- [Installation](https://Editor.Land/Doc/installation)\n- [Configuration](https://Editor.Land/Doc/configuration)\n- [`Cocoon`: Extension Host](https://Editor.Land/Doc/cocoon)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)', "Source/Content/doc/quickstart.md", "85d2b9fe45ed98ac", { html: 2006, metadata: 2007 }, '<h1 id="quickstart">Quickstart</h1>\n<p>This guide describes what is available after a successful <code>cargo tauri dev</code>\nbuild. It covers macOS and Windows, both of which produce a working editor from\nsource today.</p>\n<hr>\n<h2 id="after-the-editor-opens">After the Editor Opens</h2>\n<p>A successful build opens a native window with the <code>workbench</code> UI - <code>WKWebView</code>\non macOS, <code>WebView2</code> on Windows. You should see:</p>\n<ul>\n<li>The editor surface (Monaco) in the main area.</li>\n<li>A status bar at the bottom of the window.</li>\n<li>An activity bar on the left with sidebar icons.</li>\n<li>A command palette accessible via <code>Cmd+Shift+P</code> (macOS) or <code>Ctrl+Shift+P</code>\n(Windows).</li>\n</ul>\n<p>If the window opens but is blank or shows an error, the most common cause is a\nmissing or failed submodule. Run <code>git submodule update --init --recursive</code> from\nthe Land root and rebuild.</p>\n<hr>\n<h2 id="opening-a-project">Opening a Project</h2>\n<p><strong>File > Open Folder</strong> or <code>Cmd+O</code> / <code>Ctrl+O</code> opens a directory as a workspace.\nThis routes through <code>Mountain</code>’s file system layer via <code>Tauri</code> <code>IPC</code>.</p>\n<p>If you have an existing <code>.vscode/</code> directory with workspace settings, Land reads\nit. See <a href="https://Editor.Land/Doc/configuration">Configuration</a> for details on\nsupported configuration keys.</p>\n<hr>\n<h2 id="extensions">Extensions</h2>\n<p><code>Cocoon</code> activates extensions at startup. The following is confirmed working:</p>\n<ul>\n<li>Extensions present on disk activate through <code>Cocoon</code>’s fiber scheduler.</li>\n<li>Extensions using standard <code>vscode.*</code> file system, terminal, language server,\nand debug adapter <code>APIs</code> work for those <code>APIs</code> implemented in <code>Cocoon</code>.</li>\n<li>A <code>.vsix</code> file can be installed manually. Open the command palette\n(<code>Cmd+Shift+P</code> / <code>Ctrl+Shift+P</code>) and run <strong>Extensions: Install from VSIX</strong>.</li>\n</ul>\n<p>The following is <strong>in progress</strong>:</p>\n<ul>\n<li>An Extensions panel with browsing and search UI.</li>\n<li>Marketplace or Open VSX Registry integration.</li>\n</ul>\n<p>If an extension activates but its features do not respond, check the\n<strong><code>Output</code></strong> panel (command palette → <strong>View: Toggle <code>Output</code></strong>) and select the\nextension’s output channel. Activation errors appear there.</p>\n<hr>\n<h2 id="settings">Settings</h2>\n<p>Land has a fully implemented configuration system backed by <code>Mountain</code>’s\n<code>ConfigurationState</code> - a thread-safe, merged store covering both global and\nworkspace scopes. It supports dotted-path key access (e.g. <code>Editor.FontSize</code>,\n<code>Workbench.ColorTheme</code>) with nested object writes handled by\n<code>MergedConfigurationStateDTO</code>. Memento storage for crash recovery is also active\nfor both global and workspace contexts.</p>\n<p>Open the command palette and search for <strong>Preferences: Open Settings</strong> to locate\nand edit the active settings file. The settings format is <code>JSON</code>, compatible\nwith <code>VS Code</code>’s <code>settings.json</code> structure.</p>\n<hr>\n<h2 id="terminal">Terminal</h2>\n<p>The integrated terminal is backed by <code>Mountain</code>’s pty layer. Open it with\n<code>Cmd+`</code> (macOS) or <code>Ctrl+`</code> (Windows). This routes a spawn call through\n<code>Vine</code> <code>gRPC</code> to <code>Mountain</code>, which creates a native pty. The terminal is one of\nthe most reliable features in the current build, mapping directly to verified\n<code>Vine</code> service definitions.</p>\n<hr>\n<h2 id="tasks">Tasks</h2>\n<p>Land reads task definitions from <code>.vscode/tasks.json</code>. Use <strong>Tasks: Run Task</strong>\nfrom the command palette to run configured tasks. The standard\n<code>.vscode/tasks.json</code> format is supported:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="json"><code><span class="line"><span style="color:#E1E4E8">{</span></span>\n<span class="line"><span style="color:#79B8FF">	"version"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"2.0.0"</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#79B8FF">	"tasks"</span><span style="color:#E1E4E8">: [</span></span>\n<span class="line"><span style="color:#E1E4E8">		{</span></span>\n<span class="line"><span style="color:#79B8FF">			"label"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"Build"</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#79B8FF">			"type"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"shell"</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#79B8FF">			"command"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"cargo build"</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#79B8FF">			"group"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"build"</span></span>\n<span class="line"><span style="color:#E1E4E8">		}</span></span>\n<span class="line"><span style="color:#E1E4E8">	]</span></span>\n<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>\n<hr>\n<h2 id="extension-api-notes">Extension API Notes</h2>\n<p>The vast majority of <code>vscode.*</code> <code>APIs</code> covering file system, terminal, language\nserver protocol, diagnostics, status bar, tree views, custom editors, and\nwebview panels are routed and active. The following <code>APIs</code> have stub\nimplementations - they allow extensions that declare them to activate without\ncrashing, but the underlying features are not yet wired to a backend:</p>\n<ul>\n<li><code>vscode.lm.*</code> - language model / Copilot</li>\n<li><code>vscode.chat.*</code> - chat panel</li>\n<li><code>vscode.notebook.*</code> - notebook UI</li>\n<li><code>vscode.tests.*</code> - test explorer</li>\n</ul>\n<p>See <a href="https://Editor.Land/Doc/cocoon"><code>Cocoon</code></a> for the full <code>API</code> coverage\ntable.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/getting-started">Getting Started</a></li>\n<li><a href="https://Editor.Land/Doc/installation">Installation</a></li>\n<li><a href="https://Editor.Land/Doc/configuration">Configuration</a></li>\n<li><a href="https://Editor.Land/Doc/cocoon"><code>Cocoon</code>: Extension Host</a></li>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n</ul>', { headings: 2008, localImagePaths: 2029, remoteImagePaths: 2030, frontmatter: 2031, imagePaths: 2032 }, [2009, 2010, 2013, 2016, 2018, 2021, 2024, 2027, 2028], { depth: 25, slug: 1997, text: 2e3 }, { depth: 29, slug: 2011, text: 2012 }, "after-the-editor-opens", "After the Editor Opens", { depth: 29, slug: 2014, text: 2015 }, "opening-a-project", "Opening a Project", { depth: 29, slug: 2017, text: 192 }, "extensions", { depth: 29, slug: 2019, text: 2020 }, "settings", "Settings", { depth: 29, slug: 2022, text: 2023 }, "terminal", "Terminal", { depth: 29, slug: 2025, text: 2026 }, "tasks", "Tasks", { depth: 29, slug: 483, text: 484 }, { depth: 29, slug: 446, text: 447 }, [], [], { title: 2e3, section: 1625, order: 347, description: 2001 }, [], { id: 1470, data: 2034, body: 2037, filePath: 2038, digest: 2039, rendered: 2040 }, { title: 1471, section: 381, order: 2035, description: 2036 }, 21, "OXC-based TypeScript transform work for Editor.Land's output pipeline.", "# Rest\n\n`Rest` is the `Rust` element for `OXC`-based `TypeScript` transform work in\n`Editor.Land`'s build pipeline. It is part of the path that reduces reliance on\nNode-hosted `TypeScript` compilation over time.\n\nThe source-backed claim is that `Rest` contains `OXC` integration and transform\nwork. Public speed claims should wait for a benchmark that runs inside Land's\nactual pipeline.\n\n---\n\n## Source Structure 🗺️\n\nConfirmed source areas include:\n\n| Path                | Role                       |\n| ------------------- | -------------------------- |\n| `Source/Binary.rs`  | Binary entry point         |\n| `Source/Main.rs`    | Main function              |\n| `Source/Library.rs` | Crate root                 |\n| `Source/Fn/`        | Function modules           |\n| `Source/Struct/`    | Data structure definitions |\n\n---\n\n## Status 🚀\n\n`Rest` is WIP build-tooling work. It should be labeled `WIP` anywhere the\nwebsite talks about replacing or outperforming other compilers. `Output`\ncompatibility, source maps, and measured pipeline speed need published\nvalidation before they become product claims.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Output`](https://Editor.Land/Doc/output)\n- [Source Code](https://github.com/CodeEditorLand/Rest)", "Source/Content/doc/rest.md", "e3626c3c6f892406", { html: 2041, metadata: 2042 }, '<h1 id="rest">Rest</h1>\n<p><code>Rest</code> is the <code>Rust</code> element for <code>OXC</code>-based <code>TypeScript</code> transform work in\n<code>Editor.Land</code>’s build pipeline. It is part of the path that reduces reliance on\nNode-hosted <code>TypeScript</code> compilation over time.</p>\n<p>The source-backed claim is that <code>Rest</code> contains <code>OXC</code> integration and transform\nwork. Public speed claims should wait for a benchmark that runs inside Land’s\nactual pipeline.</p>\n<hr>\n<h2 id="source-structure️">Source Structure 🗺️</h2>\n<p>Confirmed source areas include:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Path</th><th>Role</th></tr></thead><tbody><tr><td><code>Source/Binary.rs</code></td><td>Binary entry point</td></tr><tr><td><code>Source/Main.rs</code></td><td>Main function</td></tr><tr><td><code>Source/Library.rs</code></td><td>Crate root</td></tr><tr><td><code>Source/Fn/</code></td><td>Function modules</td></tr><tr><td><code>Source/Struct/</code></td><td>Data structure definitions</td></tr></tbody></table>\n<hr>\n<h2 id="status">Status 🚀</h2>\n<p><code>Rest</code> is WIP build-tooling work. It should be labeled <code>WIP</code> anywhere the\nwebsite talks about replacing or outperforming other compilers. <code>Output</code>\ncompatibility, source maps, and measured pipeline speed need published\nvalidation before they become product claims.</p>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/output"><code>Output</code></a></li>\n<li><a href="https://github.com/CodeEditorLand/Rest">Source Code</a></li>\n</ul>', { headings: 2043, localImagePaths: 2048, remoteImagePaths: 2049, frontmatter: 2050, imagePaths: 2051 }, [2044, 2045, 2046, 2047], { depth: 25, slug: 1470, text: 1471 }, { depth: 29, slug: 393, text: 394 }, { depth: 29, slug: 571, text: 572 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 1471, section: 381, order: 2035, description: 2036 }, [], { id: 1484, data: 2053, body: 2056, filePath: 2057, digest: 2058, rendered: 2059 }, { title: 1485, section: 381, order: 2054, description: 2055 }, 23, "Cross-platform Node.js binary distribution compiled per target triple.", "# SideCar\n\n`SideCar` packages the correct `Node.js` binary for each target platform at\ncompile time - no runtime detection, no fallback chains. The binary that ships\nis the binary that runs.\n\n---\n\n## The Problem\n\n`VS Code` ships a single `Node.js` binary and detects the platform at runtime. A\nchain of fallback logic handles architecture mismatches, Rosetta translation on\nApple Silicon, musl versus glibc on Linux, and missing shared libraries on\nminimal containers.\n\nWhen the detection works, it works silently. When it fails, the error messages\nare cryptic: a segfault on ARM, a missing `.so` on Alpine, a silent hang.\nDebugging requires reverse-engineering the detection chain to find which\nfallback was selected and why it broke.\n\n---\n\n## How SideCar Eliminates It\n\n`SideCar` resolves the platform question at compile time, not runtime. The build\nmatrix defines supported target triples explicitly. For each triple, `SideCar`\ndownloads the corresponding `Node.js` binary from the official release, verifies\nits checksum, and makes it available as the canonical binary for that target.\nThe runtime code contains no platform detection logic.\n\n---\n\n## Supported Target Triples\n\nAll four target triple directories are confirmed present in the\n[`SideCar` repository](https://github.com/CodeEditorLand/SideCar):\n\n| Triple                      | Platform             |\n| --------------------------- | -------------------- |\n| `aarch64-apple-darwin`      | Apple Silicon macOS  |\n| `x86_64-apple-darwin`       | Intel macOS          |\n| `aarch64-unknown-linux-gnu` | ARM64 Linux (glibc)  |\n| `x86_64-unknown-linux-gnu`  | x86-64 Linux (glibc) |\n| `x86_64-pc-windows-msvc`    | Windows 10/11 (MSVC) |\n\n---\n\n## Source Structure 🗺️\n\n| Path                 | Role                                                          |\n| -------------------- | ------------------------------------------------------------- |\n| `Source/Download.rs` | Node.js binary download, checksum verification (~25 KB)       |\n| `Source/Spawn.rs`    | Binary spawn and process management                           |\n| `Source/Library.rs`  | Crate root re-exports                                         |\n| `Source/main.rs`     | Entry point                                                   |\n| `Source/Source/`     | Sub-modules                                                   |\n| `Cache.json`         | Cached binary metadata per target triple                      |\n| `build.rs`           | Cargo build script - target triple resolution at compile time |\n| `Resource/`          | Bundled resources                                             |\n\n---\n\n## What SideCar Enables\n\nThe editor starts with the correct `Node.js` binary already in place for the\ntarget platform - no detection step, no fallback chain. If the build succeeded\nfor your target triple, the binary is already there. This removes an entire\ncategory of platform-specific runtime failures that exist in `Electron`-based\neditors.\n\n---\n\n## In Progress\n\n- `x86_64-unknown-linux-musl` (Alpine/musl Linux) triple is not yet in the build\n  matrix.\n- Full integration test coverage across all five current triples.\n\n---\n\n## Key Technologies\n\n`Rust`, Target Triple Resolution, Compile-Time Binary Selection, Checksum\nVerification, Cross-Platform Distribution.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Cocoon`](https://Editor.Land/Doc/cocoon)\n- [Source Code](https://github.com/CodeEditorLand/SideCar)", "Source/Content/doc/sidecar.md", "0ed41afb1d794ab8", { html: 2060, metadata: 2061 }, '<h1 id="sidecar">SideCar</h1>\n<p><code>SideCar</code> packages the correct <code>Node.js</code> binary for each target platform at\ncompile time - no runtime detection, no fallback chains. The binary that ships\nis the binary that runs.</p>\n<hr>\n<h2 id="the-problem">The Problem</h2>\n<p><code>VS Code</code> ships a single <code>Node.js</code> binary and detects the platform at runtime. A\nchain of fallback logic handles architecture mismatches, Rosetta translation on\nApple Silicon, musl versus glibc on Linux, and missing shared libraries on\nminimal containers.</p>\n<p>When the detection works, it works silently. When it fails, the error messages\nare cryptic: a segfault on ARM, a missing <code>.so</code> on Alpine, a silent hang.\nDebugging requires reverse-engineering the detection chain to find which\nfallback was selected and why it broke.</p>\n<hr>\n<h2 id="how-sidecar-eliminates-it">How SideCar Eliminates It</h2>\n<p><code>SideCar</code> resolves the platform question at compile time, not runtime. The build\nmatrix defines supported target triples explicitly. For each triple, <code>SideCar</code>\ndownloads the corresponding <code>Node.js</code> binary from the official release, verifies\nits checksum, and makes it available as the canonical binary for that target.\nThe runtime code contains no platform detection logic.</p>\n<hr>\n<h2 id="supported-target-triples">Supported Target Triples</h2>\n<p>All four target triple directories are confirmed present in the\n<a href="https://github.com/CodeEditorLand/SideCar"><code>SideCar</code> repository</a>:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Triple</th><th>Platform</th></tr></thead><tbody><tr><td><code>aarch64-apple-darwin</code></td><td>Apple Silicon macOS</td></tr><tr><td><code>x86_64-apple-darwin</code></td><td>Intel macOS</td></tr><tr><td><code>aarch64-unknown-linux-gnu</code></td><td>ARM64 Linux (glibc)</td></tr><tr><td><code>x86_64-unknown-linux-gnu</code></td><td>x86-64 Linux (glibc)</td></tr><tr><td><code>x86_64-pc-windows-msvc</code></td><td>Windows 10/11 (MSVC)</td></tr></tbody></table>\n<hr>\n<h2 id="source-structure️">Source Structure 🗺️</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Path</th><th>Role</th></tr></thead><tbody><tr><td><code>Source/Download.rs</code></td><td>Node.js binary download, checksum verification (~25 KB)</td></tr><tr><td><code>Source/Spawn.rs</code></td><td>Binary spawn and process management</td></tr><tr><td><code>Source/Library.rs</code></td><td>Crate root re-exports</td></tr><tr><td><code>Source/main.rs</code></td><td>Entry point</td></tr><tr><td><code>Source/Source/</code></td><td>Sub-modules</td></tr><tr><td><code>Cache.json</code></td><td>Cached binary metadata per target triple</td></tr><tr><td><code>build.rs</code></td><td>Cargo build script - target triple resolution at compile time</td></tr><tr><td><code>Resource/</code></td><td>Bundled resources</td></tr></tbody></table>\n<hr>\n<h2 id="what-sidecar-enables">What SideCar Enables</h2>\n<p>The editor starts with the correct <code>Node.js</code> binary already in place for the\ntarget platform - no detection step, no fallback chain. If the build succeeded\nfor your target triple, the binary is already there. This removes an entire\ncategory of platform-specific runtime failures that exist in <code>Electron</code>-based\neditors.</p>\n<hr>\n<h2 id="in-progress">In Progress</h2>\n<ul>\n<li><code>x86_64-unknown-linux-musl</code> (Alpine/musl Linux) triple is not yet in the build\nmatrix.</li>\n<li>Full integration test coverage across all five current triples.</li>\n</ul>\n<hr>\n<h2 id="key-technologies">Key Technologies</h2>\n<p><code>Rust</code>, Target Triple Resolution, Compile-Time Binary Selection, Checksum\nVerification, Cross-Platform Distribution.</p>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/cocoon"><code>Cocoon</code></a></li>\n<li><a href="https://github.com/CodeEditorLand/SideCar">Source Code</a></li>\n</ul>', { headings: 2062, localImagePaths: 2078, remoteImagePaths: 2079, frontmatter: 2080, imagePaths: 2081 }, [2063, 2064, 2065, 2068, 2071, 2072, 2075, 2076, 2077], { depth: 25, slug: 1484, text: 1485 }, { depth: 29, slug: 1806, text: 1807 }, { depth: 29, slug: 2066, text: 2067 }, "how-sidecar-eliminates-it", "How SideCar Eliminates It", { depth: 29, slug: 2069, text: 2070 }, "supported-target-triples", "Supported Target Triples", { depth: 29, slug: 393, text: 394 }, { depth: 29, slug: 2073, text: 2074 }, "what-sidecar-enables", "What SideCar Enables", { depth: 29, slug: 360, text: 361 }, { depth: 29, slug: 1685, text: 1686 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 1485, section: 381, order: 2054, description: 2055 }, [], { id: 1495, data: 2083, body: 2086, filePath: 2087, digest: 2088, rendered: 2089 }, { title: 1496, section: 381, order: 2084, description: 2085 }, 14, "The Astro workbench UI that renders the editor interface inside WKWebView (macOS) or WebView2 (Windows).", "# Sky\n\n`Sky` is the `workbench` UI for `Editor.Land`. It is built with\n[`Astro`](https://astro.build) and runs inside the OS WebView when the editor is\nlaunched in native mode: `WKWebView` on macOS, `WebView2` on Windows. `Sky`\nconsumes typed service interfaces from `Wind` (the `Effect-TS` service layer)\nfor all state that crosses the UI boundary, including open files, panel layout,\nextension contributions, and language server results.\n\n`Sky` communicates with `Mountain` (the `Rust` kernel) exclusively through\n`Tauri`'s typed `IPC`. It does not call the file system, spawn terminals, or\ntalk to the Debug Adapter Protocol directly. Every native operation is a typed\n`IPC` call that `Mountain` fulfils. This strict boundary means the same `Sky`\ncomponent tree can run in a browser during development (with `Wind` stubs) and\nin the real desktop application without any code change.\n\n---\n\n## Source Layout 🗺️\n\n`Sky`'s source tree is compact and purpose-built:\n\n```\nSource/\n├── Workbench/\n│   ├── Mountain.astro       ← primary desktop entry-point (Tauri / native)\n│   ├── Browser.astro        ← browser dev-mode entry-point\n│   ├── BrowserTest.astro    ← browser test harness\n│   ├── Default.astro        ← shared workbench shell (layout, panels)\n│   ├── NLS.astro            ← National Language Support injection\n│   ├── TelemetryBridge.astro← telemetry bridge to Mountain\n│   ├── BrowserProxy/        ← dev proxy for browser debug profile\n│   ├── Bundled/             ← bundled workbench module output\n│   └── Electron/            ← Electron compatibility profile\n├── Function/            ← pure functional utilities\n├── pages/               ← Astro page routing\n└── env.d.ts             ← Astro environment type declarations\n```\n\n`Mountain.astro` is the file that matters most for the native desktop\nexperience. It bootstraps the `Tauri` WebView context, loads `Wind`'s Layer\nstack, and hands control to `Default.astro` which renders the `workbench` shell.\nAll `Wind` service subscriptions, `IPC` channel initialization, and the\n`Sky`Event listener registry are wired in `Mountain.astro` before the first\nframe paints.\n\n`NLS.astro` injects the National Language Support data bundle into the\n`workbench` so that `VS Code`'s UI string catalog is available to extensions and\nplatform modules that call `vscode.l10n.*`.\n\n`TelemetryBridge.astro` connects `Sky`'s in-page telemetry events to\n`Mountain`'s `PostHog` reporter (behind the `Telemetry` feature flag).\n\n`Bundled/` holds the output of the `Rest`/`Output` build pipeline - the bundled\nVS Code `workbench` module graph that `Sky` loads inside the WebView.\n\n---\n\n## Sky and Wind\n\n`Sky` and `Wind` are two distinct layers that work together. `Wind` defines the\n`Effect-TS` service interfaces: the typed contracts for `workbench` state. `Sky`\nconsumes those interfaces to render the actual UI components. The separation\nmeans UI components do not hold raw state. They subscribe to `Wind` services and\nre-render when those services emit changes.\n\nThis is the same model `VS Code` uses internally (a service container with\ninjected dependencies), implemented with `Effect-TS`'s typed Layer system rather\nthan `VS Code`'s untyped `createDecorator` approach. The practical result is\nthat every `Sky` component is testable without a running desktop window.\n\n---\n\n## The SkyEvent Registry\n\n`Mountain` and `Sky` coordinate through a typed event URI registry called\n`SkyEvent`. The registry is maintained in two places that must stay in sync:\n`Source/IPC/SkyEvent.ts` in `Wind` (the `TypeScript` side) and\n`Source/IPC/SkyEvent.rs` in `Mountain` (the `Rust` side). When `Mountain` emits\nan event it uses the `Rust` enum variant; when `Wind` subscribes it uses the\n`TypeScript` constant. Drift between the two sides would cause listeners to\nnever fire, so both files are updated together whenever a new event is added.\n\nThe registry currently defines 95 event URIs across the following domains:\n\n- **Editor**: `sky://editor/applyEdits`, `sky://editor/openDocument`,\n  `sky://editor/saveAll`\n- **Terminal**: `sky://terminal/create`, `sky://terminal/data`,\n  `sky://terminal/resize`, `sky://terminal/processId`, `sky://terminal/show`,\n  `sky://terminal/hide`, `sky://terminal/closed`, `sky://terminal/opened`,\n  `sky://terminal/exit`\n- **Tree view**: `sky://tree-view/create`, `sky://tree-view/refresh`,\n  `sky://tree-view/node-expanded`, `sky://tree-view/selection-changed`,\n  `sky://tree-view/reveal`, `sky://tree-view/restore-state`,\n  `sky://tree-view/set-badge`, `sky://tree-view/set-message`,\n  `sky://tree-view/set-title`, `sky://tree-view/dispose`\n- **Status bar**: `sky://statusbar/create`, `sky://statusbar/set-entry`,\n  `sky://statusbar/set-message`, `sky://statusbar/update`,\n  `sky://statusbar/dispose`, `sky://statusbar/dispose-entry`,\n  `sky://statusbar/dispose-message`\n- **`Output`**: `sky://output/create`, `sky://output/append`,\n  `sky://output/replace`, `sky://output/clear`, `sky://output/show`,\n  `sky://output/reveal`, `sky://output/dispose`\n- **Webview**: `sky://webview/create`, `sky://webview/created`,\n  `sky://webview/set-html`, `sky://webview/post-message`,\n  `sky://webview/message`, `sky://webview/options-changed`,\n  `sky://webview/dispose`, `sky://webview/disposed`, `sky://webview/revealed`\n- **Notifications**: `sky://notification/show`,\n  `sky://notification/progress-begin`, `sky://notification/progress-update`,\n  `sky://notification/progress-end`\n- **SCM**: `sky://scm/register`, `sky://scm/provider/added`,\n  `sky://scm/provider/changed`, `sky://scm/provider/removed`,\n  `sky://scm/group/changed`, `sky://scm/updateGroup`\n- **Debug**: `sky://debug/register`, `sky://debug/start`, `sky://debug/stop`\n- **Test**: `sky://test/registered`, `sky://test/run-started`,\n  `sky://test/run-status-changed`\n- **Documents**, **Diagnostics**, **Configuration**, **Language**,\n  **Lifecycle**, **Progress**, **Task**, **Theme**, **UI**, **VFS**,\n  **`Wind`ow**, **Workspace**: each with dedicated typed channels.\n\nAll URI strings follow a canonical kebab-case pattern (`sky://domain/action`).\nOlder camelCase variants have been migrated to the canonical form so `Sky` only\never sees a single consistent shape per event.\n\n---\n\n## Build Profile Behaviour ⚙️\n\n`Sky`'s runtime environment depends on which build profile is active:\n\n- **`debug`** - `Sky` runs as a plain web app in the browser. `Browser.astro` is\n  the entry point; no `Tauri` WebView, no `Mountain` process. `Astro`'s dev\n  server with HMR is active so component changes reflect without a full page\n  reload. File system, terminal, and debug `APIs` are unavailable. Used for\n  rapid UI iteration.\n- **`debug-mountain`** - `Sky` runs inside `WKWebView` (macOS) or `WebView2`\n  (Windows) via `Mountain.astro`. `Mountain` is running; all `Tauri` `IPC` calls\n  resolve through the `Rust` kernel. This is the primary development target for\n  both platforms and the only profile where the full editor experience is\n  available.\n- **`debug-electron`** - `Workbench/Electron/` provides compatibility shims for\n  running `Sky` inside `Electron`'s renderer. This profile exists for\n  compatibility testing and is not actively maintained.\n\n---\n\n## Current Status 🚀\n\n`Sky` is the active UI layer for the `debug-mountain` profile on macOS and\nWindows. The `workbench` builds, installs, and runs correctly on both platforms.\n\n**Verified working in `debug-mountain`:**\n\n- The editor window opens and renders the full `workbench` UI via `WKWebView`\n  (macOS) or `WebView2` (Windows).\n- The `VS Code`-compatible editor surface (Monaco or equivalent) is active.\n- Panel layout, command palette, and status bar render through `Sky` components\n  driven by `Wind` service subscriptions.\n- Terminal panels open, receive data on `sky://terminal/data`, and resize\n  correctly via `sky://terminal/resize`.\n- Tree-view panels populate via `$provideTreeChildren` through `Cocoon` and\n  refresh via `sky://tree-view/refresh`.\n- `Output` channels create, append, and clear correctly.\n- Status bar entries create and update without flicker.\n- Webview panels open and receive HTML via `sky://webview/set-html`.\n- `Wind` service subscriptions drive UI state updates without full-page reloads.\n- NLS data bundle loads correctly, making `VS Code` UI strings and extension\n  localisation available via `vscode.l10n.*`.\n- TelemetryBridge connects in-page events to `Mountain`'s reporter.\n\n**Optimisation roadmap:**\n\n- The `workbench` module graph currently consists of 3,385 dynamically imported\n  modules loaded sequentially at cold boot. The application is fully functional\n  today. The `Rest`/`Output` pipeline is being extended to bundle this graph\n  into a single pre-linked module, which is projected to cut cold-boot time by\n  ~550 ms. This is a well-scoped, in-progress optimisation - not a blocker.\n- Notebook UI, chat panel, and language model UI panels are on the roadmap,\n  consistent with the planned `vscode.notebook.*`, `vscode.chat.*`, and\n  `vscode.lm.*` `API` work in `Cocoon`.\n\n---\n\n## VS Code UI Compatibility\n\n`Sky` reimplements the `VS Code` `workbench` UI rather than forking it. The goal\nis behavioural compatibility: panels, tabs, the command palette, the status bar,\nand the activity bar behave as a `VS Code` user expects, without carrying\n`Electron`'s renderer process or `VS Code`'s original webpack bundle. The\nsurfaces that are complete work correctly today. Incomplete surfaces are absent\nrather than silently broken.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Wind`: Service Layer](https://Editor.Land/Doc/wind)\n- [`Mountain`: Native Kernel](https://Editor.Land/Doc/mountain)\n- [Source Code](https://github.com/CodeEditorLand/Sky)", "Source/Content/doc/sky.md", "cc2b36f2c06e8b4a", { html: 2090, metadata: 2091 }, '<h1 id="sky">Sky</h1>\n<p><code>Sky</code> is the <code>workbench</code> UI for <code>Editor.Land</code>. It is built with\n<a href="https://astro.build"><code>Astro</code></a> and runs inside the OS WebView when the editor is\nlaunched in native mode: <code>WKWebView</code> on macOS, <code>WebView2</code> on Windows. <code>Sky</code>\nconsumes typed service interfaces from <code>Wind</code> (the <code>Effect-TS</code> service layer)\nfor all state that crosses the UI boundary, including open files, panel layout,\nextension contributions, and language server results.</p>\n<p><code>Sky</code> communicates with <code>Mountain</code> (the <code>Rust</code> kernel) exclusively through\n<code>Tauri</code>’s typed <code>IPC</code>. It does not call the file system, spawn terminals, or\ntalk to the Debug Adapter Protocol directly. Every native operation is a typed\n<code>IPC</code> call that <code>Mountain</code> fulfils. This strict boundary means the same <code>Sky</code>\ncomponent tree can run in a browser during development (with <code>Wind</code> stubs) and\nin the real desktop application without any code change.</p>\n<hr>\n<h2 id="source-layout️">Source Layout 🗺️</h2>\n<p><code>Sky</code>’s source tree is compact and purpose-built:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>Source/</span></span>\n<span class="line"><span>├── Workbench/</span></span>\n<span class="line"><span>│   ├── Mountain.astro       ← primary desktop entry-point (Tauri / native)</span></span>\n<span class="line"><span>│   ├── Browser.astro        ← browser dev-mode entry-point</span></span>\n<span class="line"><span>│   ├── BrowserTest.astro    ← browser test harness</span></span>\n<span class="line"><span>│   ├── Default.astro        ← shared workbench shell (layout, panels)</span></span>\n<span class="line"><span>│   ├── NLS.astro            ← National Language Support injection</span></span>\n<span class="line"><span>│   ├── TelemetryBridge.astro← telemetry bridge to Mountain</span></span>\n<span class="line"><span>│   ├── BrowserProxy/        ← dev proxy for browser debug profile</span></span>\n<span class="line"><span>│   ├── Bundled/             ← bundled workbench module output</span></span>\n<span class="line"><span>│   └── Electron/            ← Electron compatibility profile</span></span>\n<span class="line"><span>├── Function/            ← pure functional utilities</span></span>\n<span class="line"><span>├── pages/               ← Astro page routing</span></span>\n<span class="line"><span>└── env.d.ts             ← Astro environment type declarations</span></span></code></pre>\n<p><code>Mountain.astro</code> is the file that matters most for the native desktop\nexperience. It bootstraps the <code>Tauri</code> WebView context, loads <code>Wind</code>’s Layer\nstack, and hands control to <code>Default.astro</code> which renders the <code>workbench</code> shell.\nAll <code>Wind</code> service subscriptions, <code>IPC</code> channel initialization, and the\n<code>Sky</code>Event listener registry are wired in <code>Mountain.astro</code> before the first\nframe paints.</p>\n<p><code>NLS.astro</code> injects the National Language Support data bundle into the\n<code>workbench</code> so that <code>VS Code</code>’s UI string catalog is available to extensions and\nplatform modules that call <code>vscode.l10n.*</code>.</p>\n<p><code>TelemetryBridge.astro</code> connects <code>Sky</code>’s in-page telemetry events to\n<code>Mountain</code>’s <code>PostHog</code> reporter (behind the <code>Telemetry</code> feature flag).</p>\n<p><code>Bundled/</code> holds the output of the <code>Rest</code>/<code>Output</code> build pipeline - the bundled\nVS Code <code>workbench</code> module graph that <code>Sky</code> loads inside the WebView.</p>\n<hr>\n<h2 id="sky-and-wind">Sky and Wind</h2>\n<p><code>Sky</code> and <code>Wind</code> are two distinct layers that work together. <code>Wind</code> defines the\n<code>Effect-TS</code> service interfaces: the typed contracts for <code>workbench</code> state. <code>Sky</code>\nconsumes those interfaces to render the actual UI components. The separation\nmeans UI components do not hold raw state. They subscribe to <code>Wind</code> services and\nre-render when those services emit changes.</p>\n<p>This is the same model <code>VS Code</code> uses internally (a service container with\ninjected dependencies), implemented with <code>Effect-TS</code>’s typed Layer system rather\nthan <code>VS Code</code>’s untyped <code>createDecorator</code> approach. The practical result is\nthat every <code>Sky</code> component is testable without a running desktop window.</p>\n<hr>\n<h2 id="the-skyevent-registry">The SkyEvent Registry</h2>\n<p><code>Mountain</code> and <code>Sky</code> coordinate through a typed event URI registry called\n<code>SkyEvent</code>. The registry is maintained in two places that must stay in sync:\n<code>Source/IPC/SkyEvent.ts</code> in <code>Wind</code> (the <code>TypeScript</code> side) and\n<code>Source/IPC/SkyEvent.rs</code> in <code>Mountain</code> (the <code>Rust</code> side). When <code>Mountain</code> emits\nan event it uses the <code>Rust</code> enum variant; when <code>Wind</code> subscribes it uses the\n<code>TypeScript</code> constant. Drift between the two sides would cause listeners to\nnever fire, so both files are updated together whenever a new event is added.</p>\n<p>The registry currently defines 95 event URIs across the following domains:</p>\n<ul>\n<li><strong>Editor</strong>: <code>sky://editor/applyEdits</code>, <code>sky://editor/openDocument</code>,\n<code>sky://editor/saveAll</code></li>\n<li><strong>Terminal</strong>: <code>sky://terminal/create</code>, <code>sky://terminal/data</code>,\n<code>sky://terminal/resize</code>, <code>sky://terminal/processId</code>, <code>sky://terminal/show</code>,\n<code>sky://terminal/hide</code>, <code>sky://terminal/closed</code>, <code>sky://terminal/opened</code>,\n<code>sky://terminal/exit</code></li>\n<li><strong>Tree view</strong>: <code>sky://tree-view/create</code>, <code>sky://tree-view/refresh</code>,\n<code>sky://tree-view/node-expanded</code>, <code>sky://tree-view/selection-changed</code>,\n<code>sky://tree-view/reveal</code>, <code>sky://tree-view/restore-state</code>,\n<code>sky://tree-view/set-badge</code>, <code>sky://tree-view/set-message</code>,\n<code>sky://tree-view/set-title</code>, <code>sky://tree-view/dispose</code></li>\n<li><strong>Status bar</strong>: <code>sky://statusbar/create</code>, <code>sky://statusbar/set-entry</code>,\n<code>sky://statusbar/set-message</code>, <code>sky://statusbar/update</code>,\n<code>sky://statusbar/dispose</code>, <code>sky://statusbar/dispose-entry</code>,\n<code>sky://statusbar/dispose-message</code></li>\n<li><strong><code>Output</code></strong>: <code>sky://output/create</code>, <code>sky://output/append</code>,\n<code>sky://output/replace</code>, <code>sky://output/clear</code>, <code>sky://output/show</code>,\n<code>sky://output/reveal</code>, <code>sky://output/dispose</code></li>\n<li><strong>Webview</strong>: <code>sky://webview/create</code>, <code>sky://webview/created</code>,\n<code>sky://webview/set-html</code>, <code>sky://webview/post-message</code>,\n<code>sky://webview/message</code>, <code>sky://webview/options-changed</code>,\n<code>sky://webview/dispose</code>, <code>sky://webview/disposed</code>, <code>sky://webview/revealed</code></li>\n<li><strong>Notifications</strong>: <code>sky://notification/show</code>,\n<code>sky://notification/progress-begin</code>, <code>sky://notification/progress-update</code>,\n<code>sky://notification/progress-end</code></li>\n<li><strong>SCM</strong>: <code>sky://scm/register</code>, <code>sky://scm/provider/added</code>,\n<code>sky://scm/provider/changed</code>, <code>sky://scm/provider/removed</code>,\n<code>sky://scm/group/changed</code>, <code>sky://scm/updateGroup</code></li>\n<li><strong>Debug</strong>: <code>sky://debug/register</code>, <code>sky://debug/start</code>, <code>sky://debug/stop</code></li>\n<li><strong>Test</strong>: <code>sky://test/registered</code>, <code>sky://test/run-started</code>,\n<code>sky://test/run-status-changed</code></li>\n<li><strong>Documents</strong>, <strong>Diagnostics</strong>, <strong>Configuration</strong>, <strong>Language</strong>,\n<strong>Lifecycle</strong>, <strong>Progress</strong>, <strong>Task</strong>, <strong>Theme</strong>, <strong>UI</strong>, <strong>VFS</strong>,\n<strong><code>Wind</code>ow</strong>, <strong>Workspace</strong>: each with dedicated typed channels.</li>\n</ul>\n<p>All URI strings follow a canonical kebab-case pattern (<code>sky://domain/action</code>).\nOlder camelCase variants have been migrated to the canonical form so <code>Sky</code> only\never sees a single consistent shape per event.</p>\n<hr>\n<h2 id="build-profile-behaviour️">Build Profile Behaviour ⚙️</h2>\n<p><code>Sky</code>’s runtime environment depends on which build profile is active:</p>\n<ul>\n<li><strong><code>debug</code></strong> - <code>Sky</code> runs as a plain web app in the browser. <code>Browser.astro</code> is\nthe entry point; no <code>Tauri</code> WebView, no <code>Mountain</code> process. <code>Astro</code>’s dev\nserver with HMR is active so component changes reflect without a full page\nreload. File system, terminal, and debug <code>APIs</code> are unavailable. Used for\nrapid UI iteration.</li>\n<li><strong><code>debug-mountain</code></strong> - <code>Sky</code> runs inside <code>WKWebView</code> (macOS) or <code>WebView2</code>\n(Windows) via <code>Mountain.astro</code>. <code>Mountain</code> is running; all <code>Tauri</code> <code>IPC</code> calls\nresolve through the <code>Rust</code> kernel. This is the primary development target for\nboth platforms and the only profile where the full editor experience is\navailable.</li>\n<li><strong><code>debug-electron</code></strong> - <code>Workbench/Electron/</code> provides compatibility shims for\nrunning <code>Sky</code> inside <code>Electron</code>’s renderer. This profile exists for\ncompatibility testing and is not actively maintained.</li>\n</ul>\n<hr>\n<h2 id="current-status">Current Status 🚀</h2>\n<p><code>Sky</code> is the active UI layer for the <code>debug-mountain</code> profile on macOS and\nWindows. The <code>workbench</code> builds, installs, and runs correctly on both platforms.</p>\n<p><strong>Verified working in <code>debug-mountain</code>:</strong></p>\n<ul>\n<li>The editor window opens and renders the full <code>workbench</code> UI via <code>WKWebView</code>\n(macOS) or <code>WebView2</code> (Windows).</li>\n<li>The <code>VS Code</code>-compatible editor surface (Monaco or equivalent) is active.</li>\n<li>Panel layout, command palette, and status bar render through <code>Sky</code> components\ndriven by <code>Wind</code> service subscriptions.</li>\n<li>Terminal panels open, receive data on <code>sky://terminal/data</code>, and resize\ncorrectly via <code>sky://terminal/resize</code>.</li>\n<li>Tree-view panels populate via <code>$provideTreeChildren</code> through <code>Cocoon</code> and\nrefresh via <code>sky://tree-view/refresh</code>.</li>\n<li><code>Output</code> channels create, append, and clear correctly.</li>\n<li>Status bar entries create and update without flicker.</li>\n<li>Webview panels open and receive HTML via <code>sky://webview/set-html</code>.</li>\n<li><code>Wind</code> service subscriptions drive UI state updates without full-page reloads.</li>\n<li>NLS data bundle loads correctly, making <code>VS Code</code> UI strings and extension\nlocalisation available via <code>vscode.l10n.*</code>.</li>\n<li>TelemetryBridge connects in-page events to <code>Mountain</code>’s reporter.</li>\n</ul>\n<p><strong>Optimisation roadmap:</strong></p>\n<ul>\n<li>The <code>workbench</code> module graph currently consists of 3,385 dynamically imported\nmodules loaded sequentially at cold boot. The application is fully functional\ntoday. The <code>Rest</code>/<code>Output</code> pipeline is being extended to bundle this graph\ninto a single pre-linked module, which is projected to cut cold-boot time by\n~550 ms. This is a well-scoped, in-progress optimisation - not a blocker.</li>\n<li>Notebook UI, chat panel, and language model UI panels are on the roadmap,\nconsistent with the planned <code>vscode.notebook.*</code>, <code>vscode.chat.*</code>, and\n<code>vscode.lm.*</code> <code>API</code> work in <code>Cocoon</code>.</li>\n</ul>\n<hr>\n<h2 id="vs-code-ui-compatibility">VS Code UI Compatibility</h2>\n<p><code>Sky</code> reimplements the <code>VS Code</code> <code>workbench</code> UI rather than forking it. The goal\nis behavioural compatibility: panels, tabs, the command palette, the status bar,\nand the activity bar behave as a <code>VS Code</code> user expects, without carrying\n<code>Electron</code>’s renderer process or <code>VS Code</code>’s original webpack bundle. The\nsurfaces that are complete work correctly today. Incomplete surfaces are absent\nrather than silently broken.</p>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/wind"><code>Wind</code>: Service Layer</a></li>\n<li><a href="https://Editor.Land/Doc/mountain"><code>Mountain</code>: Native Kernel</a></li>\n<li><a href="https://github.com/CodeEditorLand/Sky">Source Code</a></li>\n</ul>', { headings: 2092, localImagePaths: 2109, remoteImagePaths: 2110, frontmatter: 2111, imagePaths: 2112 }, [2093, 2094, 2095, 2098, 2101, 2104, 2105, 2108], { depth: 25, slug: 1495, text: 1496 }, { depth: 29, slug: 559, text: 560 }, { depth: 29, slug: 2096, text: 2097 }, "sky-and-wind", "Sky and Wind", { depth: 29, slug: 2099, text: 2100 }, "the-skyevent-registry", "The SkyEvent Registry", { depth: 29, slug: 2102, text: 2103 }, "build-profile-behaviour️", "Build Profile Behaviour ⚙️", { depth: 29, slug: 1572, text: 1573 }, { depth: 29, slug: 2106, text: 2107 }, "vs-code-ui-compatibility", "VS Code UI Compatibility", { depth: 29, slug: 401, text: 402 }, [], [], { title: 1496, section: 381, order: 2084, description: 2085 }, [], "telemetry/effect-otel", { id: 2113, data: 2115, body: 2119, filePath: 2120, digest: 2121, rendered: 2122 }, { title: 2116, section: 2117, order: 1822, description: 2118 }, "Effect-TS and OpenTelemetry Integration", "Telemetry", "How Wind and Cocoon use Effect-TS with @effect/opentelemetry for dual-sink span export.", '# Effect-TS and OpenTelemetry Integration\n\nWind and Cocoon are written in Effect-TS. Effect ships first-class OpenTelemetry\nsupport through `@effect/opentelemetry`. The Land integration lets Effect own\nspan creation and attribute propagation, then export to two sinks: a\n`BatchSpanProcessor` to OTLP collector, and a custom `SpanProcessor` that\nre-emits each span as a PostHog event.\n\n## Package Surface\n\n```\npnpm add @effect/opentelemetry \\\n         @opentelemetry/api \\\n         @opentelemetry/sdk-trace-base \\\n         @opentelemetry/sdk-trace-node \\\n         @opentelemetry/sdk-trace-web \\\n         @opentelemetry/exporter-trace-otlp-http \\\n         @opentelemetry/resources \\\n         @opentelemetry/semantic-conventions\n```\n\n`sdk-trace-node` for Cocoon (Node), `sdk-trace-web` for Sky (browser),\n`sdk-trace-base` for Wind (composes either).\n\n## Wiring\n\n### Cocoon (Node, Effect-TS)\n\n```ts\n// Element/Cocoon/Source/Telemetry/OTel.ts\nimport { NodeSdk } from "@effect/opentelemetry";\nimport { OTLPTraceExporter } from "@opentelemetry/exporter-trace-otlp-http";\nimport { Resource } from "@opentelemetry/resources";\nimport { BatchSpanProcessor } from "@opentelemetry/sdk-trace-base";\nimport { Layer } from "effect";\n\nexport default Layer.unwrapEffect(\n	NodeSdk.layer(() => ({\n		resource: {\n			serviceName: "land-editor-cocoon",\n			attributes: { "land.tier": "cocoon" },\n		},\n		spanProcessor: new BatchSpanProcessor(\n			new OTLPTraceExporter({\n				url: `${process.env["OTLPEndpoint"] ?? "http://127.0.0.1:4318"}/v1/traces`,\n			}),\n			{ maxExportBatchSize: 64, scheduledDelayMillis: 1500 },\n		),\n	})),\n);\n```\n\nCompose into the application layer:\n\n```ts\n// Element/Cocoon/Source/Effect/AppLayer.ts\nimport { Layer } from "effect";\n\nimport OTelLayer from "../Telemetry/OTel.js";\nimport PostHogLayer from "../Telemetry/PostHogLayer.js";\n\nconst Telemetry =\n	process.env["Capture"] === "false"\n		? Layer.empty\n		: Layer.mergeAll(OTelLayer, PostHogLayer);\n\nexport const AppLayer = Layer.mergeAll(Telemetry);\n```\n\n`Capture=false` collapses Telemetry to `Layer.empty`. Effect drops the service\nreferences at build time when paired with bundler `define` substitution.\n\n## Spans\n\nUse Effect\'s `Effect.withSpan`:\n\n```ts\nimport { Effect } from "effect";\n\nconst HandleRequest = (Method: string, Parameters: unknown) =>\n	Effect.gen(function* () {\n		const Result = yield* doWork(Method, Parameters);\n		return Result;\n	}).pipe(\n		Effect.withSpan("land:cocoon:handler", {\n			attributes: { "land.method": Method },\n		}),\n	);\n```\n\nThe span\'s `trace_id` / `span_id` flow into PostHog through the custom\nprocessor. The span name follows the `land:<element>:<action>` convention.\n\n## Custom Processor -- PostHog Dual-Emit\n\n```ts\n// Element/Cocoon/Source/Telemetry/PostHogProcessor.ts\nimport type {\n	ReadableSpan,\n	SpanProcessor,\n} from "@opentelemetry/sdk-trace-base";\n\nimport { CaptureEvent } from "./PostHogBridge.js";\n\nexport default class PostHogSpanProcessor implements SpanProcessor {\n	onStart(): void {}\n\n	onEnd(Span: ReadableSpan): void {\n		const Properties: Record<string, unknown> = {\n			$trace_id: Span.spanContext().traceId,\n			$span_id: Span.spanContext().spanId,\n			$parent_span_id: Span.parentSpanContext?.spanId,\n			duration_ms: Span.duration[0] * 1e3 + Span.duration[1] / 1e6,\n			status_code: Span.status.code,\n		};\n		for (const [Key, Value] of Object.entries(Span.attributes)) {\n			Properties[Key] = Value;\n		}\n		CaptureEvent(Span.name, Properties);\n	}\n\n	shutdown(): Promise<void> {\n		return Promise.resolve();\n	}\n	forceFlush(): Promise<void> {\n		return Promise.resolve();\n	}\n}\n```\n\nRegister it alongside the OTLP `BatchSpanProcessor` so every span fans out into\nboth sinks.\n\n## Sky (Browser)\n\n```ts\n// Element/Sky/Source/Function/Telemetry/Bridge.ts\nimport { OTLPTraceExporter } from "@opentelemetry/exporter-trace-otlp-http";\nimport {\n	BatchSpanProcessor,\n	WebTracerProvider,\n} from "@opentelemetry/sdk-trace-web";\n\nif (import.meta.env.DEV && import.meta.env["Capture"] !== "false") {\n	const Provider = new WebTracerProvider({\n		resource: {\n			serviceName: "land-editor-sky",\n			attributes: { "land.tier": "sky" },\n		},\n	});\n	Provider.addSpanProcessor(\n		new BatchSpanProcessor(\n			new OTLPTraceExporter({\n				url: `${import.meta.env["OTLPEndpoint"] ?? "http://127.0.0.1:4318"}/v1/traces`,\n			}),\n		),\n	);\n	Provider.register();\n}\n```\n\nTree-shakeable: `import.meta.env.DEV` is replaced at compile time by Vite/Astro\nwith the literal `false` for production builds.\n\n## Mountain (Rust) Bridge\n\nMountain emits spans through the `tracing` crate. The bridge is\n`tracing-opentelemetry`, converting every `tracing::span!` into an OTLP span.\n`otel_span!` macro spans flow into Jaeger; the PostHog bridge in\n`Binary/Build/PostHogPlugin/CaptureHandler` receives the same\n`(name, duration, attributes)` triplet.\n\n## Cross-Tier Trace Propagation\n\nTauri events between Mountain and Sky carry the `traceparent` header (W3C\nformat) on every IPC envelope. Wind extracts it from\n`event.payload._traceparent`, opens a child span with `Effect.linkSpans`, and\nforwards downstream. This lets Jaeger render a single trace that begins in Sky,\nhops Mountain (Rust), fans into Cocoon (Node), and rejoins on the response.\n\nMountain attaches the header in `IPC/Sky/EmitToWebview.rs`; Cocoon extracts it\nin `Effect/RPCServer.ts`.\n\n## When Not to Use Effect Spans\n\nFor single-callsite numeric events (`land:cocoon:stub:active`,\n`land:wind:command:invoke`) the PostHog bridge alone is sufficient. Reserve\nspans for operations with causal children: handler -> downstream gRPC, extension\nactivation -> workspaceContains glob walk, build phase -> sub-build.\n\n---\n\n## See Also\n\n- [Telemetry Overview](https://Editor.Land/Doc/telemetry/overview)\n- [Sidecar Telemetry](https://Editor.Land/Doc/telemetry/sidecars)', "Source/Content/doc/telemetry/effect-otel.md", "1089015bb59249db", { html: 2123, metadata: 2124 }, '<h1 id="effect-ts-and-opentelemetry-integration">Effect-TS and OpenTelemetry Integration</h1>\n<p>Wind and Cocoon are written in Effect-TS. Effect ships first-class OpenTelemetry\nsupport through <code>@effect/opentelemetry</code>. The Land integration lets Effect own\nspan creation and attribute propagation, then export to two sinks: a\n<code>BatchSpanProcessor</code> to OTLP collector, and a custom <code>SpanProcessor</code> that\nre-emits each span as a PostHog event.</p>\n<h2 id="package-surface">Package Surface</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>pnpm add @effect/opentelemetry \\</span></span>\n<span class="line"><span>         @opentelemetry/api \\</span></span>\n<span class="line"><span>         @opentelemetry/sdk-trace-base \\</span></span>\n<span class="line"><span>         @opentelemetry/sdk-trace-node \\</span></span>\n<span class="line"><span>         @opentelemetry/sdk-trace-web \\</span></span>\n<span class="line"><span>         @opentelemetry/exporter-trace-otlp-http \\</span></span>\n<span class="line"><span>         @opentelemetry/resources \\</span></span>\n<span class="line"><span>         @opentelemetry/semantic-conventions</span></span></code></pre>\n<p><code>sdk-trace-node</code> for Cocoon (Node), <code>sdk-trace-web</code> for Sky (browser),\n<code>sdk-trace-base</code> for Wind (composes either).</p>\n<h2 id="wiring">Wiring</h2>\n<h3 id="cocoon-node-effect-ts">Cocoon (Node, Effect-TS)</h3>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="ts"><code><span class="line"><span style="color:#6A737D">// Element/Cocoon/Source/Telemetry/OTel.ts</span></span>\n<span class="line"><span style="color:#F97583">import</span><span style="color:#E1E4E8"> { NodeSdk } </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "@effect/opentelemetry"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"><span style="color:#F97583">import</span><span style="color:#E1E4E8"> { OTLPTraceExporter } </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "@opentelemetry/exporter-trace-otlp-http"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"><span style="color:#F97583">import</span><span style="color:#E1E4E8"> { Resource } </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "@opentelemetry/resources"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"><span style="color:#F97583">import</span><span style="color:#E1E4E8"> { BatchSpanProcessor } </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "@opentelemetry/sdk-trace-base"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"><span style="color:#F97583">import</span><span style="color:#E1E4E8"> { Layer } </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "effect"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#F97583">export</span><span style="color:#F97583"> default</span><span style="color:#E1E4E8"> Layer.</span><span style="color:#B392F0">unwrapEffect</span><span style="color:#E1E4E8">(</span></span>\n<span class="line"><span style="color:#E1E4E8">	NodeSdk.</span><span style="color:#B392F0">layer</span><span style="color:#E1E4E8">(() </span><span style="color:#F97583">=></span><span style="color:#E1E4E8"> ({</span></span>\n<span class="line"><span style="color:#E1E4E8">		resource: {</span></span>\n<span class="line"><span style="color:#E1E4E8">			serviceName: </span><span style="color:#9ECBFF">"land-editor-cocoon"</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#E1E4E8">			attributes: { </span><span style="color:#9ECBFF">"land.tier"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"cocoon"</span><span style="color:#E1E4E8"> },</span></span>\n<span class="line"><span style="color:#E1E4E8">		},</span></span>\n<span class="line"><span style="color:#E1E4E8">		spanProcessor: </span><span style="color:#F97583">new</span><span style="color:#B392F0"> BatchSpanProcessor</span><span style="color:#E1E4E8">(</span></span>\n<span class="line"><span style="color:#F97583">			new</span><span style="color:#B392F0"> OTLPTraceExporter</span><span style="color:#E1E4E8">({</span></span>\n<span class="line"><span style="color:#E1E4E8">				url: </span><span style="color:#9ECBFF">`${</span><span style="color:#E1E4E8">process</span><span style="color:#9ECBFF">.</span><span style="color:#E1E4E8">env</span><span style="color:#9ECBFF">[</span><span style="color:#9ECBFF">"OTLPEndpoint"</span><span style="color:#9ECBFF">] </span><span style="color:#F97583">??</span><span style="color:#9ECBFF"> "http://127.0.0.1:4318"}/v1/traces`</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#E1E4E8">			}),</span></span>\n<span class="line"><span style="color:#E1E4E8">			{ maxExportBatchSize: </span><span style="color:#79B8FF">64</span><span style="color:#E1E4E8">, scheduledDelayMillis: </span><span style="color:#79B8FF">1500</span><span style="color:#E1E4E8"> },</span></span>\n<span class="line"><span style="color:#E1E4E8">		),</span></span>\n<span class="line"><span style="color:#E1E4E8">	})),</span></span>\n<span class="line"><span style="color:#E1E4E8">);</span></span></code></pre>\n<p>Compose into the application layer:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="ts"><code><span class="line"><span style="color:#6A737D">// Element/Cocoon/Source/Effect/AppLayer.ts</span></span>\n<span class="line"><span style="color:#F97583">import</span><span style="color:#E1E4E8"> { Layer } </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "effect"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#F97583">import</span><span style="color:#E1E4E8"> OTelLayer </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "../Telemetry/OTel.js"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"><span style="color:#F97583">import</span><span style="color:#E1E4E8"> PostHogLayer </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "../Telemetry/PostHogLayer.js"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#F97583">const</span><span style="color:#79B8FF"> Telemetry</span><span style="color:#F97583"> =</span></span>\n<span class="line"><span style="color:#E1E4E8">	process.env[</span><span style="color:#9ECBFF">"Capture"</span><span style="color:#E1E4E8">] </span><span style="color:#F97583">===</span><span style="color:#9ECBFF"> "false"</span></span>\n<span class="line"><span style="color:#F97583">		?</span><span style="color:#E1E4E8"> Layer.empty</span></span>\n<span class="line"><span style="color:#F97583">		:</span><span style="color:#E1E4E8"> Layer.</span><span style="color:#B392F0">mergeAll</span><span style="color:#E1E4E8">(OTelLayer, PostHogLayer);</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#F97583">export</span><span style="color:#F97583"> const</span><span style="color:#79B8FF"> AppLayer</span><span style="color:#F97583"> =</span><span style="color:#E1E4E8"> Layer.</span><span style="color:#B392F0">mergeAll</span><span style="color:#E1E4E8">(Telemetry);</span></span></code></pre>\n<p><code>Capture=false</code> collapses Telemetry to <code>Layer.empty</code>. Effect drops the service\nreferences at build time when paired with bundler <code>define</code> substitution.</p>\n<h2 id="spans">Spans</h2>\n<p>Use Effect’s <code>Effect.withSpan</code>:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="ts"><code><span class="line"><span style="color:#F97583">import</span><span style="color:#E1E4E8"> { Effect } </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "effect"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#F97583">const</span><span style="color:#B392F0"> HandleRequest</span><span style="color:#F97583"> =</span><span style="color:#E1E4E8"> (</span><span style="color:#FFAB70">Method</span><span style="color:#F97583">:</span><span style="color:#79B8FF"> string</span><span style="color:#E1E4E8">, </span><span style="color:#FFAB70">Parameters</span><span style="color:#F97583">:</span><span style="color:#79B8FF"> unknown</span><span style="color:#E1E4E8">) </span><span style="color:#F97583">=></span></span>\n<span class="line"><span style="color:#E1E4E8">	Effect.</span><span style="color:#B392F0">gen</span><span style="color:#E1E4E8">(</span><span style="color:#F97583">function*</span><span style="color:#E1E4E8"> () {</span></span>\n<span class="line"><span style="color:#F97583">		const</span><span style="color:#79B8FF"> Result</span><span style="color:#F97583"> =</span><span style="color:#F97583"> yield*</span><span style="color:#B392F0"> doWork</span><span style="color:#E1E4E8">(Method, Parameters);</span></span>\n<span class="line"><span style="color:#F97583">		return</span><span style="color:#E1E4E8"> Result;</span></span>\n<span class="line"><span style="color:#E1E4E8">	}).</span><span style="color:#B392F0">pipe</span><span style="color:#E1E4E8">(</span></span>\n<span class="line"><span style="color:#E1E4E8">		Effect.</span><span style="color:#B392F0">withSpan</span><span style="color:#E1E4E8">(</span><span style="color:#9ECBFF">"land:cocoon:handler"</span><span style="color:#E1E4E8">, {</span></span>\n<span class="line"><span style="color:#E1E4E8">			attributes: { </span><span style="color:#9ECBFF">"land.method"</span><span style="color:#E1E4E8">: Method },</span></span>\n<span class="line"><span style="color:#E1E4E8">		}),</span></span>\n<span class="line"><span style="color:#E1E4E8">	);</span></span></code></pre>\n<p>The span’s <code>trace_id</code> / <code>span_id</code> flow into PostHog through the custom\nprocessor. The span name follows the <code>land:&#x3C;element>:&#x3C;action></code> convention.</p>\n<h2 id="custom-processor--posthog-dual-emit">Custom Processor — PostHog Dual-Emit</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="ts"><code><span class="line"><span style="color:#6A737D">// Element/Cocoon/Source/Telemetry/PostHogProcessor.ts</span></span>\n<span class="line"><span style="color:#F97583">import</span><span style="color:#F97583"> type</span><span style="color:#E1E4E8"> {</span></span>\n<span class="line"><span style="color:#E1E4E8">	ReadableSpan,</span></span>\n<span class="line"><span style="color:#E1E4E8">	SpanProcessor,</span></span>\n<span class="line"><span style="color:#E1E4E8">} </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "@opentelemetry/sdk-trace-base"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#F97583">import</span><span style="color:#E1E4E8"> { CaptureEvent } </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "./PostHogBridge.js"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#F97583">export</span><span style="color:#F97583"> default</span><span style="color:#F97583"> class</span><span style="color:#B392F0"> PostHogSpanProcessor</span><span style="color:#F97583"> implements</span><span style="color:#B392F0"> SpanProcessor</span><span style="color:#E1E4E8"> {</span></span>\n<span class="line"><span style="color:#B392F0">	onStart</span><span style="color:#E1E4E8">()</span><span style="color:#F97583">:</span><span style="color:#79B8FF"> void</span><span style="color:#E1E4E8"> {}</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#B392F0">	onEnd</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">Span</span><span style="color:#F97583">:</span><span style="color:#B392F0"> ReadableSpan</span><span style="color:#E1E4E8">)</span><span style="color:#F97583">:</span><span style="color:#79B8FF"> void</span><span style="color:#E1E4E8"> {</span></span>\n<span class="line"><span style="color:#F97583">		const</span><span style="color:#79B8FF"> Properties</span><span style="color:#F97583">:</span><span style="color:#B392F0"> Record</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#79B8FF">string</span><span style="color:#E1E4E8">, </span><span style="color:#79B8FF">unknown</span><span style="color:#E1E4E8">> </span><span style="color:#F97583">=</span><span style="color:#E1E4E8"> {</span></span>\n<span class="line"><span style="color:#E1E4E8">			$trace_id: Span.</span><span style="color:#B392F0">spanContext</span><span style="color:#E1E4E8">().traceId,</span></span>\n<span class="line"><span style="color:#E1E4E8">			$span_id: Span.</span><span style="color:#B392F0">spanContext</span><span style="color:#E1E4E8">().spanId,</span></span>\n<span class="line"><span style="color:#E1E4E8">			$parent_span_id: Span.parentSpanContext?.spanId,</span></span>\n<span class="line"><span style="color:#E1E4E8">			duration_ms: Span.duration[</span><span style="color:#79B8FF">0</span><span style="color:#E1E4E8">] </span><span style="color:#F97583">*</span><span style="color:#79B8FF"> 1e3</span><span style="color:#F97583"> +</span><span style="color:#E1E4E8"> Span.duration[</span><span style="color:#79B8FF">1</span><span style="color:#E1E4E8">] </span><span style="color:#F97583">/</span><span style="color:#79B8FF"> 1e6</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#E1E4E8">			status_code: Span.status.code,</span></span>\n<span class="line"><span style="color:#E1E4E8">		};</span></span>\n<span class="line"><span style="color:#F97583">		for</span><span style="color:#E1E4E8"> (</span><span style="color:#F97583">const</span><span style="color:#E1E4E8"> [</span><span style="color:#79B8FF">Key</span><span style="color:#E1E4E8">, </span><span style="color:#79B8FF">Value</span><span style="color:#E1E4E8">] </span><span style="color:#F97583">of</span><span style="color:#E1E4E8"> Object.</span><span style="color:#B392F0">entries</span><span style="color:#E1E4E8">(Span.attributes)) {</span></span>\n<span class="line"><span style="color:#E1E4E8">			Properties[Key] </span><span style="color:#F97583">=</span><span style="color:#E1E4E8"> Value;</span></span>\n<span class="line"><span style="color:#E1E4E8">		}</span></span>\n<span class="line"><span style="color:#B392F0">		CaptureEvent</span><span style="color:#E1E4E8">(Span.name, Properties);</span></span>\n<span class="line"><span style="color:#E1E4E8">	}</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#B392F0">	shutdown</span><span style="color:#E1E4E8">()</span><span style="color:#F97583">:</span><span style="color:#B392F0"> Promise</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#79B8FF">void</span><span style="color:#E1E4E8">> {</span></span>\n<span class="line"><span style="color:#F97583">		return</span><span style="color:#79B8FF"> Promise</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">resolve</span><span style="color:#E1E4E8">();</span></span>\n<span class="line"><span style="color:#E1E4E8">	}</span></span>\n<span class="line"><span style="color:#B392F0">	forceFlush</span><span style="color:#E1E4E8">()</span><span style="color:#F97583">:</span><span style="color:#B392F0"> Promise</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#79B8FF">void</span><span style="color:#E1E4E8">> {</span></span>\n<span class="line"><span style="color:#F97583">		return</span><span style="color:#79B8FF"> Promise</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">resolve</span><span style="color:#E1E4E8">();</span></span>\n<span class="line"><span style="color:#E1E4E8">	}</span></span>\n<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>\n<p>Register it alongside the OTLP <code>BatchSpanProcessor</code> so every span fans out into\nboth sinks.</p>\n<h2 id="sky-browser">Sky (Browser)</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="ts"><code><span class="line"><span style="color:#6A737D">// Element/Sky/Source/Function/Telemetry/Bridge.ts</span></span>\n<span class="line"><span style="color:#F97583">import</span><span style="color:#E1E4E8"> { OTLPTraceExporter } </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "@opentelemetry/exporter-trace-otlp-http"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"><span style="color:#F97583">import</span><span style="color:#E1E4E8"> {</span></span>\n<span class="line"><span style="color:#E1E4E8">	BatchSpanProcessor,</span></span>\n<span class="line"><span style="color:#E1E4E8">	WebTracerProvider,</span></span>\n<span class="line"><span style="color:#E1E4E8">} </span><span style="color:#F97583">from</span><span style="color:#9ECBFF"> "@opentelemetry/sdk-trace-web"</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#F97583">if</span><span style="color:#E1E4E8"> (</span><span style="color:#F97583">import</span><span style="color:#E1E4E8">.</span><span style="color:#79B8FF">meta</span><span style="color:#E1E4E8">.env.</span><span style="color:#79B8FF">DEV</span><span style="color:#F97583"> &#x26;&#x26;</span><span style="color:#F97583"> import</span><span style="color:#E1E4E8">.</span><span style="color:#79B8FF">meta</span><span style="color:#E1E4E8">.env[</span><span style="color:#9ECBFF">"Capture"</span><span style="color:#E1E4E8">] </span><span style="color:#F97583">!==</span><span style="color:#9ECBFF"> "false"</span><span style="color:#E1E4E8">) {</span></span>\n<span class="line"><span style="color:#F97583">	const</span><span style="color:#79B8FF"> Provider</span><span style="color:#F97583"> =</span><span style="color:#F97583"> new</span><span style="color:#B392F0"> WebTracerProvider</span><span style="color:#E1E4E8">({</span></span>\n<span class="line"><span style="color:#E1E4E8">		resource: {</span></span>\n<span class="line"><span style="color:#E1E4E8">			serviceName: </span><span style="color:#9ECBFF">"land-editor-sky"</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#E1E4E8">			attributes: { </span><span style="color:#9ECBFF">"land.tier"</span><span style="color:#E1E4E8">: </span><span style="color:#9ECBFF">"sky"</span><span style="color:#E1E4E8"> },</span></span>\n<span class="line"><span style="color:#E1E4E8">		},</span></span>\n<span class="line"><span style="color:#E1E4E8">	});</span></span>\n<span class="line"><span style="color:#E1E4E8">	Provider.</span><span style="color:#B392F0">addSpanProcessor</span><span style="color:#E1E4E8">(</span></span>\n<span class="line"><span style="color:#F97583">		new</span><span style="color:#B392F0"> BatchSpanProcessor</span><span style="color:#E1E4E8">(</span></span>\n<span class="line"><span style="color:#F97583">			new</span><span style="color:#B392F0"> OTLPTraceExporter</span><span style="color:#E1E4E8">({</span></span>\n<span class="line"><span style="color:#E1E4E8">				url: </span><span style="color:#9ECBFF">`${</span><span style="color:#F97583">import</span><span style="color:#9ECBFF">.</span><span style="color:#79B8FF">meta</span><span style="color:#9ECBFF">.</span><span style="color:#E1E4E8">env</span><span style="color:#9ECBFF">[</span><span style="color:#9ECBFF">"OTLPEndpoint"</span><span style="color:#9ECBFF">] </span><span style="color:#F97583">??</span><span style="color:#9ECBFF"> "http://127.0.0.1:4318"}/v1/traces`</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#E1E4E8">			}),</span></span>\n<span class="line"><span style="color:#E1E4E8">		),</span></span>\n<span class="line"><span style="color:#E1E4E8">	);</span></span>\n<span class="line"><span style="color:#E1E4E8">	Provider.</span><span style="color:#B392F0">register</span><span style="color:#E1E4E8">();</span></span>\n<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>\n<p>Tree-shakeable: <code>import.meta.env.DEV</code> is replaced at compile time by Vite/Astro\nwith the literal <code>false</code> for production builds.</p>\n<h2 id="mountain-rust-bridge">Mountain (Rust) Bridge</h2>\n<p>Mountain emits spans through the <code>tracing</code> crate. The bridge is\n<code>tracing-opentelemetry</code>, converting every <code>tracing::span!</code> into an OTLP span.\n<code>otel_span!</code> macro spans flow into Jaeger; the PostHog bridge in\n<code>Binary/Build/PostHogPlugin/CaptureHandler</code> receives the same\n<code>(name, duration, attributes)</code> triplet.</p>\n<h2 id="cross-tier-trace-propagation">Cross-Tier Trace Propagation</h2>\n<p>Tauri events between Mountain and Sky carry the <code>traceparent</code> header (W3C\nformat) on every IPC envelope. Wind extracts it from\n<code>event.payload._traceparent</code>, opens a child span with <code>Effect.linkSpans</code>, and\nforwards downstream. This lets Jaeger render a single trace that begins in Sky,\nhops Mountain (Rust), fans into Cocoon (Node), and rejoins on the response.</p>\n<p>Mountain attaches the header in <code>IPC/Sky/EmitToWebview.rs</code>; Cocoon extracts it\nin <code>Effect/RPCServer.ts</code>.</p>\n<h2 id="when-not-to-use-effect-spans">When Not to Use Effect Spans</h2>\n<p>For single-callsite numeric events (<code>land:cocoon:stub:active</code>,\n<code>land:wind:command:invoke</code>) the PostHog bridge alone is sufficient. Reserve\nspans for operations with causal children: handler -> downstream gRPC, extension\nactivation -> workspaceContains glob walk, build phase -> sub-build.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/telemetry/overview">Telemetry Overview</a></li>\n<li><a href="https://Editor.Land/Doc/telemetry/sidecars">Sidecar Telemetry</a></li>\n</ul>', { headings: 2125, localImagePaths: 2156, remoteImagePaths: 2157, frontmatter: 2158, imagePaths: 2159 }, [2126, 2128, 2131, 2134, 2137, 2140, 2143, 2146, 2149, 2152, 2155], { depth: 25, slug: 2127, text: 2116 }, "effect-ts-and-opentelemetry-integration", { depth: 29, slug: 2129, text: 2130 }, "package-surface", "Package Surface", { depth: 29, slug: 2132, text: 2133 }, "wiring", "Wiring", { depth: 347, slug: 2135, text: 2136 }, "cocoon-node-effect-ts", "Cocoon (Node, Effect-TS)", { depth: 29, slug: 2138, text: 2139 }, "spans", "Spans", { depth: 29, slug: 2141, text: 2142 }, "custom-processor--posthog-dual-emit", "Custom Processor — PostHog Dual-Emit", { depth: 29, slug: 2144, text: 2145 }, "sky-browser", "Sky (Browser)", { depth: 29, slug: 2147, text: 2148 }, "mountain-rust-bridge", "Mountain (Rust) Bridge", { depth: 29, slug: 2150, text: 2151 }, "cross-tier-trace-propagation", "Cross-Tier Trace Propagation", { depth: 29, slug: 2153, text: 2154 }, "when-not-to-use-effect-spans", "When Not to Use Effect Spans", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2116, section: 2117, order: 1822, description: 2118 }, [], "telemetry/overview", { id: 2160, data: 2162, body: 2165, filePath: 2166, digest: 2167, rendered: 2168 }, { title: 2163, section: 2117, order: 629, description: 2164 }, "Telemetry Overview", "Two parallel telemetry pipes (PostHog and OTLP) sharing one trace_id across all Land layers.", "# Telemetry Overview\n\nLand emits telemetry through two parallel pipes that share one `trace_id`:\n\n| Pipe        | Sink                       | Strength                            | Cost                           |\n| ----------- | -------------------------- | ----------------------------------- | ------------------------------ |\n| **PostHog** | `https://eu.i.posthog.com` | Aggregations, dashboards, retention | One event per millisecond cap  |\n| **OTLP**    | `127.0.0.1:4318` (Jaeger)  | Per-request waterfall, parent/child | Local-only, requires collector |\n\nJoining the pipes: every PostHog event carries `$trace_id` and `$span_id`\nproperties; every Jaeger span carries `posthog.event` / `posthog.distinct_id`\nattributes. A HogQL JOIN against Jaeger's trace export -- or a manual\nclick-through from a PostHog event into the Jaeger UI -- reconstructs the full\ncausal chain.\n\n## Layout\n\n| Layer    | File / Module                                          | Pipe(s)        |\n| -------- | ------------------------------------------------------ | -------------- |\n| Build.sh | `Maintain/Script/PostHogCapture.sh`                    | PostHog + OTLP |\n| Mountain | `Element/Mountain/Source/Binary/Build/PostHogPlugin/*` | PostHog        |\n| Mountain | `Element/Mountain/Source/IPC/DevLog/EmitOTLPSpan.rs`   | OTLP           |\n| Mountain | `Element/Mountain/Source/Telemetry/*`                  | OTLP (tracing) |\n| Cocoon   | `Element/Cocoon/Source/Telemetry/PostHogBridge.ts`     | PostHog        |\n| Cocoon   | `Element/Cocoon/Source/Telemetry/OTLPBridge.ts`        | OTLP           |\n| Wind     | `Element/Wind/Source/Telemetry/Bridge.ts`              | both           |\n| Sky      | `Element/Sky/Source/Function/Telemetry/Bridge.ts`      | both           |\n| Output   | `Element/Output/Source/Telemetry/*`                    | both           |\n| Sidecars | re-export Mountain's macros via the `tracing` crate    | OTLP           |\n\n## Event-Name Convention\n\nEvery Land-emitted event uses the prefix `land:<element>:<action>`:\n\n- `land:build:start`, `land:build:phase:complete`, `land:build:complete`,\n  `land:build:error`\n- `land:mountain:session:start`, `land:mountain:ipc:invoke`,\n  `land:mountain:handler:complete`, `land:mountain:error`\n- `land:cocoon:session:start`, `land:cocoon:entry:load`,\n  `land:cocoon:entry:loaded`, `land:cocoon:handler:complete`,\n  `land:cocoon:stub:active`, `land:cocoon:error`\n- `land:wind:layer:ready`, `land:wind:command:invoke`\n- `land:sky:build:complete`, `land:sky:resource:error`,\n  `land:sky:throttle-dropped`\n- `land:output:build:complete`\n- `land:session:start`, `land:session:end` -- Sky-emitted aggregates\n- `land:boot:timing` -- workbench boot percentile telemetry\n- `land:ipc:marks`, `land:vscode:marks`, `land:extension-host:marks`,\n  `land:all:marks` -- performance mark batches\n\nPostHog's `$exception` autocapture stays as-is.\n\n## Configuration - `.env.Land.PostHog`\n\nSingle source of truth for both pipes. Read at boot by every layer.\n\n| Var            | Default                    | Effect                                       |\n| -------------- | -------------------------- | -------------------------------------------- |\n| `Authorize`    | `phc_...`                  | PostHog project key                          |\n| `Beam`         | `https://eu.i.posthog.com` | PostHog endpoint                             |\n| `Report`       | `true`                     | Master toggle for the PostHog pipe           |\n| `OTLPEndpoint` | `http://127.0.0.1:4318`    | OTLP collector                               |\n| `OTLPEnabled`  | `true`                     | Master toggle for the OTLP pipe              |\n| `Capture`      | `true`                     | Master telemetry kill - short-circuits both  |\n| `Trace`        | `all`                      | RUST_LOG-like span filter                    |\n| `Record`       | `0`                        | Mirror every payload to a per-session NDJSON |\n| `Brand`        | _(empty)_                  | `distinct_id` seed                           |\n| `Throttle`     | `5`                        | posthog-js client-side rate-limit            |\n| `Buffer`       | `3000`                     | Cocoon batch window (ms)                     |\n| `Batch`        | `20`                       | Cocoon batch size                            |\n| `Cap`          | `7`                        | Sky `$exception`/10 s                        |\n| `Replay`       | `false`                    | posthog-js session recording                 |\n\n## Production = Zero Bytes\n\nEvery layer compiles the telemetry stack out of release builds:\n\n- **Rust** (Mountain, Air, Echo, Rest, Grove, Mist) -- capture paths gate on\n  `cfg!(debug_assertions)`. LLVM dead-codes them in `--release`.\n- **TypeScript** -- bridges import lazily through\n  `if (process.env.NODE_ENV !== \"production\") { ... }` so esbuild's `define`\n  collapses the conditional and tree-shakes the SDK.\n- **Astro** -- bundle config checks `if (import.meta.env.DEV)` drops the\n  posthog-js + OTLP SDK paths from `astro build`.\n\nPlus runtime kill switches: `Capture=false` short-circuits all pipes regardless\nof build mode; `Report=false` / `OTLPEnabled=false` flip a single pipe.\n\n## Bring-up Sequence\n\n```\n# Once: bring up the local Jaeger collector.\nsh Land/Container/Up.sh\n\n# Every dev session: env vars feed every layer.\nexport Trace=all Record=1 Capture=true\nsh Land/Maintain/Debug/Build.sh --profile debug-electron-bundled\n\n# Run the binary; Mountain initialises both pipes from .env.Land.PostHog.\nexport Trace=all\n./Element/Mountain/Target/debug/<binary>\n\n# Inspect:\n#   PostHog dashboards: https://eu.posthog.com/project/151871\n#   Jaeger UI:          http://127.0.0.1:16686\n```\n\n---\n\n## See Also\n\n- [Effect-TS + OpenTelemetry Integration](https://Editor.Land/Doc/telemetry/effect-otel)\n- [Sidecar Telemetry](https://Editor.Land/Doc/telemetry/sidecars)\n- [Tree-Shaking Telemetry](https://Editor.Land/Doc/telemetry/tree-shaking)", "Source/Content/doc/telemetry/overview.md", "d06876141d2e96bf", { html: 2169, metadata: 2170 }, '<h1 id="telemetry-overview">Telemetry Overview</h1>\n<p>Land emits telemetry through two parallel pipes that share one <code>trace_id</code>:</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Pipe</th><th>Sink</th><th>Strength</th><th>Cost</th></tr></thead><tbody><tr><td><strong>PostHog</strong></td><td><code>https://eu.i.posthog.com</code></td><td>Aggregations, dashboards, retention</td><td>One event per millisecond cap</td></tr><tr><td><strong>OTLP</strong></td><td><code>127.0.0.1:4318</code> (Jaeger)</td><td>Per-request waterfall, parent/child</td><td>Local-only, requires collector</td></tr></tbody></table>\n<p>Joining the pipes: every PostHog event carries <code>$trace_id</code> and <code>$span_id</code>\nproperties; every Jaeger span carries <code>posthog.event</code> / <code>posthog.distinct_id</code>\nattributes. A HogQL JOIN against Jaeger’s trace export — or a manual\nclick-through from a PostHog event into the Jaeger UI — reconstructs the full\ncausal chain.</p>\n<h2 id="layout">Layout</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Layer</th><th>File / Module</th><th>Pipe(s)</th></tr></thead><tbody><tr><td>Build.sh</td><td><code>Maintain/Script/PostHogCapture.sh</code></td><td>PostHog + OTLP</td></tr><tr><td>Mountain</td><td><code>Element/Mountain/Source/Binary/Build/PostHogPlugin/*</code></td><td>PostHog</td></tr><tr><td>Mountain</td><td><code>Element/Mountain/Source/IPC/DevLog/EmitOTLPSpan.rs</code></td><td>OTLP</td></tr><tr><td>Mountain</td><td><code>Element/Mountain/Source/Telemetry/*</code></td><td>OTLP (tracing)</td></tr><tr><td>Cocoon</td><td><code>Element/Cocoon/Source/Telemetry/PostHogBridge.ts</code></td><td>PostHog</td></tr><tr><td>Cocoon</td><td><code>Element/Cocoon/Source/Telemetry/OTLPBridge.ts</code></td><td>OTLP</td></tr><tr><td>Wind</td><td><code>Element/Wind/Source/Telemetry/Bridge.ts</code></td><td>both</td></tr><tr><td>Sky</td><td><code>Element/Sky/Source/Function/Telemetry/Bridge.ts</code></td><td>both</td></tr><tr><td>Output</td><td><code>Element/Output/Source/Telemetry/*</code></td><td>both</td></tr><tr><td>Sidecars</td><td>re-export Mountain’s macros via the <code>tracing</code> crate</td><td>OTLP</td></tr></tbody></table>\n<h2 id="event-name-convention">Event-Name Convention</h2>\n<p>Every Land-emitted event uses the prefix <code>land:&#x3C;element>:&#x3C;action></code>:</p>\n<ul>\n<li><code>land:build:start</code>, <code>land:build:phase:complete</code>, <code>land:build:complete</code>,\n<code>land:build:error</code></li>\n<li><code>land:mountain:session:start</code>, <code>land:mountain:ipc:invoke</code>,\n<code>land:mountain:handler:complete</code>, <code>land:mountain:error</code></li>\n<li><code>land:cocoon:session:start</code>, <code>land:cocoon:entry:load</code>,\n<code>land:cocoon:entry:loaded</code>, <code>land:cocoon:handler:complete</code>,\n<code>land:cocoon:stub:active</code>, <code>land:cocoon:error</code></li>\n<li><code>land:wind:layer:ready</code>, <code>land:wind:command:invoke</code></li>\n<li><code>land:sky:build:complete</code>, <code>land:sky:resource:error</code>,\n<code>land:sky:throttle-dropped</code></li>\n<li><code>land:output:build:complete</code></li>\n<li><code>land:session:start</code>, <code>land:session:end</code> — Sky-emitted aggregates</li>\n<li><code>land:boot:timing</code> — workbench boot percentile telemetry</li>\n<li><code>land:ipc:marks</code>, <code>land:vscode:marks</code>, <code>land:extension-host:marks</code>,\n<code>land:all:marks</code> — performance mark batches</li>\n</ul>\n<p>PostHog’s <code>$exception</code> autocapture stays as-is.</p>\n<h2 id="configuration---envlandposthog">Configuration - <code>.env.Land.PostHog</code></h2>\n<p>Single source of truth for both pipes. Read at boot by every layer.</p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Var</th><th>Default</th><th>Effect</th></tr></thead><tbody><tr><td><code>Authorize</code></td><td><code>phc_...</code></td><td>PostHog project key</td></tr><tr><td><code>Beam</code></td><td><code>https://eu.i.posthog.com</code></td><td>PostHog endpoint</td></tr><tr><td><code>Report</code></td><td><code>true</code></td><td>Master toggle for the PostHog pipe</td></tr><tr><td><code>OTLPEndpoint</code></td><td><code>http://127.0.0.1:4318</code></td><td>OTLP collector</td></tr><tr><td><code>OTLPEnabled</code></td><td><code>true</code></td><td>Master toggle for the OTLP pipe</td></tr><tr><td><code>Capture</code></td><td><code>true</code></td><td>Master telemetry kill - short-circuits both</td></tr><tr><td><code>Trace</code></td><td><code>all</code></td><td>RUST_LOG-like span filter</td></tr><tr><td><code>Record</code></td><td><code>0</code></td><td>Mirror every payload to a per-session NDJSON</td></tr><tr><td><code>Brand</code></td><td><em>(empty)</em></td><td><code>distinct_id</code> seed</td></tr><tr><td><code>Throttle</code></td><td><code>5</code></td><td>posthog-js client-side rate-limit</td></tr><tr><td><code>Buffer</code></td><td><code>3000</code></td><td>Cocoon batch window (ms)</td></tr><tr><td><code>Batch</code></td><td><code>20</code></td><td>Cocoon batch size</td></tr><tr><td><code>Cap</code></td><td><code>7</code></td><td>Sky <code>$exception</code>/10 s</td></tr><tr><td><code>Replay</code></td><td><code>false</code></td><td>posthog-js session recording</td></tr></tbody></table>\n<h2 id="production--zero-bytes">Production = Zero Bytes</h2>\n<p>Every layer compiles the telemetry stack out of release builds:</p>\n<ul>\n<li><strong>Rust</strong> (Mountain, Air, Echo, Rest, Grove, Mist) — capture paths gate on\n<code>cfg!(debug_assertions)</code>. LLVM dead-codes them in <code>--release</code>.</li>\n<li><strong>TypeScript</strong> — bridges import lazily through\n<code>if (process.env.NODE_ENV !== "production") { ... }</code> so esbuild’s <code>define</code>\ncollapses the conditional and tree-shakes the SDK.</li>\n<li><strong>Astro</strong> — bundle config checks <code>if (import.meta.env.DEV)</code> drops the\nposthog-js + OTLP SDK paths from <code>astro build</code>.</li>\n</ul>\n<p>Plus runtime kill switches: <code>Capture=false</code> short-circuits all pipes regardless\nof build mode; <code>Report=false</code> / <code>OTLPEnabled=false</code> flip a single pipe.</p>\n<h2 id="bring-up-sequence">Bring-up Sequence</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span># Once: bring up the local Jaeger collector.</span></span>\n<span class="line"><span>sh Land/Container/Up.sh</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span># Every dev session: env vars feed every layer.</span></span>\n<span class="line"><span>export Trace=all Record=1 Capture=true</span></span>\n<span class="line"><span>sh Land/Maintain/Debug/Build.sh --profile debug-electron-bundled</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span># Run the binary; Mountain initialises both pipes from .env.Land.PostHog.</span></span>\n<span class="line"><span>export Trace=all</span></span>\n<span class="line"><span>./Element/Mountain/Target/debug/&#x3C;binary></span></span>\n<span class="line"><span></span></span>\n<span class="line"><span># Inspect:</span></span>\n<span class="line"><span>#   PostHog dashboards: https://eu.posthog.com/project/151871</span></span>\n<span class="line"><span>#   Jaeger UI:          http://127.0.0.1:16686</span></span></code></pre>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/telemetry/effect-otel">Effect-TS + OpenTelemetry Integration</a></li>\n<li><a href="https://Editor.Land/Doc/telemetry/sidecars">Sidecar Telemetry</a></li>\n<li><a href="https://Editor.Land/Doc/telemetry/tree-shaking">Tree-Shaking Telemetry</a></li>\n</ul>', { headings: 2171, localImagePaths: 2190, remoteImagePaths: 2191, frontmatter: 2192, imagePaths: 2193 }, [2172, 2174, 2177, 2180, 2183, 2186, 2189], { depth: 25, slug: 2173, text: 2163 }, "telemetry-overview", { depth: 29, slug: 2175, text: 2176 }, "layout", "Layout", { depth: 29, slug: 2178, text: 2179 }, "event-name-convention", "Event-Name Convention", { depth: 29, slug: 2181, text: 2182 }, "configuration---envlandposthog", "Configuration - .env.Land.PostHog", { depth: 29, slug: 2184, text: 2185 }, "production--zero-bytes", "Production = Zero Bytes", { depth: 29, slug: 2187, text: 2188 }, "bring-up-sequence", "Bring-up Sequence", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2163, section: 2117, order: 629, description: 2164 }, [], "telemetry/sidecars", { id: 2194, data: 2196, body: 2199, filePath: 2200, digest: 2201, rendered: 2202 }, { title: 2197, section: 2117, order: 2035, description: 2198 }, "Sidecar Telemetry", "How Air, Echo, Rest, Grove, Mist, SideCar, and Worker emit telemetry through the shared land:<tier>:* namespace.", "# Sidecar Telemetry\n\nEvery Rust sidecar and the Worker service worker emit through the same\n`land:<tier>:*` namespace and stamp `$tier` so dashboards can pivot without\nper-Element changes.\n\n## Architecture\n\n| Type    | Path                                        | Wiring                                                                                                 |\n| ------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------ |\n| Library | `Element/Common/Source/Telemetry/`          | shared. Provides `Capture*::Fn`, `EmitOTLPSpan::Fn`, `Initialize::Fn(Tier)`                            |\n| Binary  | `Element/Air/Source/Binary.rs`              | calls `CommonLibrary::Telemetry::Initialize::Fn(Tier::Air)` in `Main`                                  |\n| Binary  | `Element/Rest/Source/Main.rs`               | calls `Initialize::Fn(Tier::Rest)`                                                                     |\n| Binary  | `Element/Grove/Source/main.rs`              | calls `Initialize::Fn(Tier::Grove)` after `Cli::parse`                                                 |\n| Binary  | `Element/SideCar/Source/main.rs`            | sync `main` -- spawns short-lived `tokio::Runtime` to call `Initialize::Fn(Tier::SideCar)`             |\n| Library | `Element/Echo/Source/Library.rs`            | linked into Mountain -- emits via `CommonLibrary::Telemetry::CaptureEvent::Fn`, inherits Mountain tier |\n| Library | `Element/Mist/Source/lib.rs`                | linked into Mountain and Air -- same                                                                   |\n| Worker  | `Element/Worker/Source/Telemetry/Bridge.ts` | TS service worker; auto-loaded from `Worker.ts` install/activate hooks                                 |\n\n## Tier Identification\n\n`Element/Common/Source/Telemetry/Tier.rs`:\n\n```rust\npub enum Tier {\n    Mountain, Air, Echo, Rest, Grove, Mist, SideCar, Common,\n}\n```\n\nThe Tier is registered once via `Initialize::Fn(Tier::X)` and read by every\n`Capture*::Fn` to stamp `$tier` / `$component` automatically.\n\n## Mountain -- Dual-Init Contract\n\nMountain runs both its compile-baked plugin (`Binary/Build/PostHogPlugin/*` for\ntree-shake guarantee) and `Common::Telemetry::Initialize::Fn(Tier::Mountain)` so\nlibrary code linked into Mountain (Echo, Mist) emits through the same client and\nis tagged `$tier=mountain`.\n\n## Worker\n\nThe Worker bridge uses build-baked `import.meta.env` values and the SW-native\n`fetch` API -- no `posthog-js`, no Node `https`. Loaded lazily inside the\n`__DEV__` branch so `astro build` with `NODE_ENV=production` drops it entirely.\n\n## What Library Crates Do\n\nEcho / Mist / Common are linked into Mountain (and Air for Mist). They never run\nas standalone processes, so they never call `Initialize::Fn`. They emit events\nusing `CommonLibrary::Telemetry::CaptureEvent::Fn(name, props)` and the\nresulting events are tagged with the parent process's tier. Use the `name` to\ndisambiguate:\n\n```rust\n// Inside Echo, running in Mountain:\nCommonLibrary::Telemetry::CaptureEvent::Fn(\n    \"land:echo:queue:saturated\",\n    Some(vec![(\"queue_depth\", \"1024\")]),\n);\n// Event arrives in PostHog with $tier=mountain, name=land:echo:*.\n```\n\n## Cross-Tier Joining\n\nEvery PostHog event carries `$trace_id` (where the Tier ran an OTLP span).\nCocoon's bridge sets it via `Event::SetTraceIdentifier` from\n`OTLPBridge.TraceIdentifier()`. Common's `EmitOTLPSpan::Fn` uses one\n`OTLP_TRACE_ID` per process so all spans from one Air / Rest / Grove boot roll\ninto one Jaeger trace.\n\n## Profile-Specific Tier Coverage\n\nSome build profiles intentionally skip whole tiers. The Tier Coverage dashboard\nshows zero counts for those -- that is correct behaviour, not a bug:\n\n| Profile                                       | Mountain | Cocoon | Sky | Air | Notes                                   |\n| --------------------------------------------- | -------- | ------ | --- | --- | --------------------------------------- |\n| `debug` / `debug-mountain` / `debug-electron` | Yes      | Yes    | Yes | Yes | Full stack                              |\n| `debug-cocoon-headless`                       | Yes      | Yes    | No  | Yes | `Render=false` skips Wind preload + Sky |\n| `debug-mountain-only`                         | Yes      | No     | Yes | Yes | `Spawn=false` skips Cocoon              |\n| `debug-kernel`                                | Yes      | No     | No  | Yes | `Spawn=false; Render=false`             |\n| `debug-electron-minimal`                      | Yes      | Yes    | Yes | Yes | `Skip=true` skips built-in extensions   |\n\nMountain's `Binary/Main/Entry.rs` always initialises both\n`PostHogPlugin::Initialize` and\n`CommonLibrary::Telemetry::Initialize::Fn(Tier::Mountain)` regardless of\nprofile.\n\n## Adding a New Sidecar\n\n1. Add `Common = { workspace = true }` to `Element/<New>/Cargo.toml`.\n2. Add a variant to `Tier::Tier` enum.\n3. Call `CommonLibrary::Telemetry::Initialize::Fn(Tier::<New>).await` from the\n   binary's main / async entry.\n4. (Optional) Add a tile to the Tier Coverage dashboard pointing at\n   `event LIKE 'land:<new>:%'`.\n\nEverything else -- env propagation from Mountain, OTLP fan-in, `$tier` tagging,\ndashboard rollups, prod tree-shake -- comes for free.\n\n---\n\n## See Also\n\n- [Telemetry Overview](https://Editor.Land/Doc/telemetry/overview)\n- [SideCar Element](https://Editor.Land/Doc/sidecar)", "Source/Content/doc/telemetry/sidecars.md", "15eb99e28a57b50f", { html: 2203, metadata: 2204 }, `<h1 id="sidecar-telemetry">Sidecar Telemetry</h1>
<p>Every Rust sidecar and the Worker service worker emit through the same
<code>land:&#x3C;tier>:*</code> namespace and stamp <code>$tier</code> so dashboards can pivot without
per-Element changes.</p>
<h2 id="architecture">Architecture</h2>


















































<table><thead><tr><th>Type</th><th>Path</th><th>Wiring</th></tr></thead><tbody><tr><td>Library</td><td><code>Element/Common/Source/Telemetry/</code></td><td>shared. Provides <code>Capture*::Fn</code>, <code>EmitOTLPSpan::Fn</code>, <code>Initialize::Fn(Tier)</code></td></tr><tr><td>Binary</td><td><code>Element/Air/Source/Binary.rs</code></td><td>calls <code>CommonLibrary::Telemetry::Initialize::Fn(Tier::Air)</code> in <code>Main</code></td></tr><tr><td>Binary</td><td><code>Element/Rest/Source/Main.rs</code></td><td>calls <code>Initialize::Fn(Tier::Rest)</code></td></tr><tr><td>Binary</td><td><code>Element/Grove/Source/main.rs</code></td><td>calls <code>Initialize::Fn(Tier::Grove)</code> after <code>Cli::parse</code></td></tr><tr><td>Binary</td><td><code>Element/SideCar/Source/main.rs</code></td><td>sync <code>main</code> — spawns short-lived <code>tokio::Runtime</code> to call <code>Initialize::Fn(Tier::SideCar)</code></td></tr><tr><td>Library</td><td><code>Element/Echo/Source/Library.rs</code></td><td>linked into Mountain — emits via <code>CommonLibrary::Telemetry::CaptureEvent::Fn</code>, inherits Mountain tier</td></tr><tr><td>Library</td><td><code>Element/Mist/Source/lib.rs</code></td><td>linked into Mountain and Air — same</td></tr><tr><td>Worker</td><td><code>Element/Worker/Source/Telemetry/Bridge.ts</code></td><td>TS service worker; auto-loaded from <code>Worker.ts</code> install/activate hooks</td></tr></tbody></table>
<h2 id="tier-identification">Tier Identification</h2>
<p><code>Element/Common/Source/Telemetry/Tier.rs</code>:</p>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="rust"><code><span class="line"><span style="color:#F97583">pub</span><span style="color:#F97583"> enum</span><span style="color:#B392F0"> Tier</span><span style="color:#E1E4E8"> {</span></span>
<span class="line"><span style="color:#B392F0">    Mountain</span><span style="color:#E1E4E8">, </span><span style="color:#B392F0">Air</span><span style="color:#E1E4E8">, </span><span style="color:#B392F0">Echo</span><span style="color:#E1E4E8">, </span><span style="color:#B392F0">Rest</span><span style="color:#E1E4E8">, </span><span style="color:#B392F0">Grove</span><span style="color:#E1E4E8">, </span><span style="color:#B392F0">Mist</span><span style="color:#E1E4E8">, </span><span style="color:#B392F0">SideCar</span><span style="color:#E1E4E8">, </span><span style="color:#B392F0">Common</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>
<p>The Tier is registered once via <code>Initialize::Fn(Tier::X)</code> and read by every
<code>Capture*::Fn</code> to stamp <code>$tier</code> / <code>$component</code> automatically.</p>
<h2 id="mountain--dual-init-contract">Mountain — Dual-Init Contract</h2>
<p>Mountain runs both its compile-baked plugin (<code>Binary/Build/PostHogPlugin/*</code> for
tree-shake guarantee) and <code>Common::Telemetry::Initialize::Fn(Tier::Mountain)</code> so
library code linked into Mountain (Echo, Mist) emits through the same client and
is tagged <code>$tier=mountain</code>.</p>
<h2 id="worker">Worker</h2>
<p>The Worker bridge uses build-baked <code>import.meta.env</code> values and the SW-native
<code>fetch</code> API — no <code>posthog-js</code>, no Node <code>https</code>. Loaded lazily inside the
<code>__DEV__</code> branch so <code>astro build</code> with <code>NODE_ENV=production</code> drops it entirely.</p>
<h2 id="what-library-crates-do">What Library Crates Do</h2>
<p>Echo / Mist / Common are linked into Mountain (and Air for Mist). They never run
as standalone processes, so they never call <code>Initialize::Fn</code>. They emit events
using <code>CommonLibrary::Telemetry::CaptureEvent::Fn(name, props)</code> and the
resulting events are tagged with the parent process’s tier. Use the <code>name</code> to
disambiguate:</p>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="rust"><code><span class="line"><span style="color:#6A737D">// Inside Echo, running in Mountain:</span></span>
<span class="line"><span style="color:#B392F0">CommonLibrary</span><span style="color:#F97583">::</span><span style="color:#B392F0">Telemetry</span><span style="color:#F97583">::</span><span style="color:#B392F0">CaptureEvent</span><span style="color:#F97583">::</span><span style="color:#B392F0">Fn</span><span style="color:#E1E4E8">(</span></span>
<span class="line"><span style="color:#9ECBFF">    "land:echo:queue:saturated"</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#B392F0">    Some</span><span style="color:#E1E4E8">(</span><span style="color:#B392F0">vec!</span><span style="color:#E1E4E8">[(</span><span style="color:#9ECBFF">"queue_depth"</span><span style="color:#E1E4E8">, </span><span style="color:#9ECBFF">"1024"</span><span style="color:#E1E4E8">)]),</span></span>
<span class="line"><span style="color:#E1E4E8">);</span></span>
<span class="line"><span style="color:#6A737D">// Event arrives in PostHog with $tier=mountain, name=land:echo:*.</span></span></code></pre>
<h2 id="cross-tier-joining">Cross-Tier Joining</h2>
<p>Every PostHog event carries <code>$trace_id</code> (where the Tier ran an OTLP span).
Cocoon’s bridge sets it via <code>Event::SetTraceIdentifier</code> from
<code>OTLPBridge.TraceIdentifier()</code>. Common’s <code>EmitOTLPSpan::Fn</code> uses one
<code>OTLP_TRACE_ID</code> per process so all spans from one Air / Rest / Grove boot roll
into one Jaeger trace.</p>
<h2 id="profile-specific-tier-coverage">Profile-Specific Tier Coverage</h2>
<p>Some build profiles intentionally skip whole tiers. The Tier Coverage dashboard
shows zero counts for those — that is correct behaviour, not a bug:</p>





















































<table><thead><tr><th>Profile</th><th>Mountain</th><th>Cocoon</th><th>Sky</th><th>Air</th><th>Notes</th></tr></thead><tbody><tr><td><code>debug</code> / <code>debug-mountain</code> / <code>debug-electron</code></td><td>Yes</td><td>Yes</td><td>Yes</td><td>Yes</td><td>Full stack</td></tr><tr><td><code>debug-cocoon-headless</code></td><td>Yes</td><td>Yes</td><td>No</td><td>Yes</td><td><code>Render=false</code> skips Wind preload + Sky</td></tr><tr><td><code>debug-mountain-only</code></td><td>Yes</td><td>No</td><td>Yes</td><td>Yes</td><td><code>Spawn=false</code> skips Cocoon</td></tr><tr><td><code>debug-kernel</code></td><td>Yes</td><td>No</td><td>No</td><td>Yes</td><td><code>Spawn=false; Render=false</code></td></tr><tr><td><code>debug-electron-minimal</code></td><td>Yes</td><td>Yes</td><td>Yes</td><td>Yes</td><td><code>Skip=true</code> skips built-in extensions</td></tr></tbody></table>
<p>Mountain’s <code>Binary/Main/Entry.rs</code> always initialises both
<code>PostHogPlugin::Initialize</code> and
<code>CommonLibrary::Telemetry::Initialize::Fn(Tier::Mountain)</code> regardless of
profile.</p>
<h2 id="adding-a-new-sidecar">Adding a New Sidecar</h2>
<ol>
<li>Add <code>Common = { workspace = true }</code> to <code>Element/&#x3C;New>/Cargo.toml</code>.</li>
<li>Add a variant to <code>Tier::Tier</code> enum.</li>
<li>Call <code>CommonLibrary::Telemetry::Initialize::Fn(Tier::&#x3C;New>).await</code> from the
binary’s main / async entry.</li>
<li>(Optional) Add a tile to the Tier Coverage dashboard pointing at
<code>event LIKE 'land:&#x3C;new>:%'</code>.</li>
</ol>
<p>Everything else — env propagation from Mountain, OTLP fan-in, <code>$tier</code> tagging,
dashboard rollups, prod tree-shake — comes for free.</p>
<hr>
<h2 id="see-also">See Also</h2>
<ul>
<li><a href="https://Editor.Land/Doc/telemetry/overview">Telemetry Overview</a></li>
<li><a href="https://Editor.Land/Doc/sidecar">SideCar Element</a></li>
</ul>`, { headings: 2205, localImagePaths: 2229, remoteImagePaths: 2230, frontmatter: 2231, imagePaths: 2232 }, [2206, 2208, 2209, 2212, 2215, 2216, 2219, 2222, 2225, 2228], { depth: 25, slug: 2207, text: 2197 }, "sidecar-telemetry", { depth: 29, slug: 71, text: 10 }, { depth: 29, slug: 2210, text: 2211 }, "tier-identification", "Tier Identification", { depth: 29, slug: 2213, text: 2214 }, "mountain--dual-init-contract", "Mountain — Dual-Init Contract", { depth: 29, slug: 1538, text: 1539 }, { depth: 29, slug: 2217, text: 2218 }, "what-library-crates-do", "What Library Crates Do", { depth: 29, slug: 2220, text: 2221 }, "cross-tier-joining", "Cross-Tier Joining", { depth: 29, slug: 2223, text: 2224 }, "profile-specific-tier-coverage", "Profile-Specific Tier Coverage", { depth: 29, slug: 2226, text: 2227 }, "adding-a-new-sidecar", "Adding a New Sidecar", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2197, section: 2117, order: 2035, description: 2198 }, [], "telemetry/tree-shaking", { id: 2233, data: 2235, body: 2238, filePath: 2239, digest: 2240, rendered: 2241 }, { title: 2236, section: 2117, order: 1918, description: 2237 }, "Tree-Shaking Telemetry", "Production zero contract: release builds contain no telemetry SDK code, no PostHog endpoint, no OTLP URL.", '# Tree-Shaking Telemetry\n\nThe contract: a `cargo build --release` of any Rust element, and an\n`astro build` of Sky / a `pnpm run --filter Output build` of Output, must\ncontain zero bytes of telemetry SDK code, no PostHog endpoint string, no OTLP\ncollector URL, no `posthog-js` import.\n\n## The Three Gates\n\nEvery capture site is gated by three layers stacked from outside in:\n\n1. **Build mode gate** -- compile-time. Drops the whole stack from release /\n   production bundles.\n2. **Master kill switch (`Capture`)** -- runtime. Drops the runtime call\n   regardless of build mode. Used for one-off airgapped runs.\n3. **Per-pipe toggle (`Report`, `OTLPEnabled`)** -- runtime, per-pipe. Lets\n   PostHog and Jaeger be flipped independently.\n\nProduction builds rely on (1) so (2) and (3) never even run.\n\n## Rust -- `cfg!(debug_assertions)`\n\n```rust\npub fn Fn(EventName: &str, Properties: Option<Vec<(&str, &str)>>) {\n    if !cfg!(debug_assertions) { return; }\n    if std::env::var("Capture").as_deref() == Ok("false") { return; }\n    // ... call PostHog ...\n}\n```\n\n`cfg!(debug_assertions)` is `false` in `--release`. LLVM constant-folds the\n`if`, drops the dead branch, and the `posthog-rs` calls underneath become\nunreachable. Linker GC then strips the symbol entirely.\n\nVerify:\n\n```sh\ncargo build -p Mountain --release\nstrings Element/Mountain/Target/release/Mountain | grep -c "posthog\\|/capture/\\|i.posthog.com"\n# 0\n```\n\n## TypeScript -- `process.env.NODE_ENV` / `import.meta.env.DEV`\n\nesbuild + Vite substitute these literally at build time when configured with\n`define`:\n\n```ts\n// esbuild.config.ts\n{\n    define: {\n        "process.env.NODE_ENV": JSON.stringify(\n            process.env["NODE_ENV"] ?? "development",\n        ),\n    },\n}\n```\n\nThen in code:\n\n```ts\nlet Bridge: typeof import("./PostHogBridge.js") | undefined;\nif (process.env.NODE_ENV !== "production") {\n	Bridge = await import("./PostHogBridge.js");\n}\nexport const CaptureEvent = (\n	Name: string,\n	Properties: Record<string, unknown>,\n): void => {\n	Bridge?.CaptureEvent(Name, Properties);\n};\n```\n\nIn a production build esbuild rewrites `process.env.NODE_ENV` to `"production"`,\nconstant-folds to `if (false)`, drops the `await import()`, and the chunk graph\nnever sees `PostHogBridge.js`.\n\nVerify:\n\n```sh\nNODE_ENV=production pnpm run --filter Cocoon build\ngrep -rn "i.posthog.com\\|/batch/" Element/Cocoon/Target/\n# 0 matches\n```\n\n## Astro -- `import.meta.env.DEV`\n\nVite replaces `import.meta.env.DEV` with `false` for `astro build`. Wrap every\nSky bridge import in:\n\n```ts\nif (import.meta.env.DEV) {\n	const { default: InitTelemetry } = await import("./Telemetry/Bridge.js");\n	InitTelemetry();\n}\n```\n\nVerify:\n\n```sh\npnpm run --filter Sky build\ngrep -rn "posthog\\|otlp\\|jaeger" Element/Sky/Target/Static/Application/\n# 0 matches\n```\n\n## What About the PostHog API Key?\n\nThe key is built into release builds only if a release path imports the bridge.\nThe gate above means a clean release never sees the key. For the few\nrelease-mode events we deliberately keep (e.g. license activations), use a\nseparate "anonymous-only" key in `.env.Land.PostHog.Production`, gated through a\ndifferent module that does not import the dev bridge.\n\n## Smoke Test\n\n```sh\nsh Maintain/Test/TelemetryTreeShake.sh\n```\n\nBuilds Mountain in `--release`, Cocoon / Sky / Output in `NODE_ENV=production`,\nruns `strings` / `grep` / `du -sh` against the output, fails CI if any forbidden\nsymbol is found.\n\n---\n\n## See Also\n\n- [Telemetry Overview](https://Editor.Land/Doc/telemetry/overview)\n- [Effect-TS and OpenTelemetry Integration](https://Editor.Land/Doc/telemetry/effect-otel)', "Source/Content/doc/telemetry/tree-shaking.md", "a6174347b1aa9949", { html: 2242, metadata: 2243 }, '<h1 id="tree-shaking-telemetry">Tree-Shaking Telemetry</h1>\n<p>The contract: a <code>cargo build --release</code> of any Rust element, and an\n<code>astro build</code> of Sky / a <code>pnpm run --filter Output build</code> of Output, must\ncontain zero bytes of telemetry SDK code, no PostHog endpoint string, no OTLP\ncollector URL, no <code>posthog-js</code> import.</p>\n<h2 id="the-three-gates">The Three Gates</h2>\n<p>Every capture site is gated by three layers stacked from outside in:</p>\n<ol>\n<li><strong>Build mode gate</strong> — compile-time. Drops the whole stack from release /\nproduction bundles.</li>\n<li><strong>Master kill switch (<code>Capture</code>)</strong> — runtime. Drops the runtime call\nregardless of build mode. Used for one-off airgapped runs.</li>\n<li><strong>Per-pipe toggle (<code>Report</code>, <code>OTLPEnabled</code>)</strong> — runtime, per-pipe. Lets\nPostHog and Jaeger be flipped independently.</li>\n</ol>\n<p>Production builds rely on (1) so (2) and (3) never even run.</p>\n<h2 id="rust--cfgdebug_assertions">Rust — <code>cfg!(debug_assertions)</code></h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="rust"><code><span class="line"><span style="color:#F97583">pub</span><span style="color:#F97583"> fn</span><span style="color:#B392F0"> Fn</span><span style="color:#E1E4E8">(</span><span style="color:#B392F0">EventName</span><span style="color:#F97583">:</span><span style="color:#F97583"> &#x26;</span><span style="color:#B392F0">str</span><span style="color:#E1E4E8">, </span><span style="color:#B392F0">Properties</span><span style="color:#F97583">:</span><span style="color:#B392F0"> Option</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#B392F0">Vec</span><span style="color:#E1E4E8">&#x3C;(</span><span style="color:#F97583">&#x26;</span><span style="color:#B392F0">str</span><span style="color:#E1E4E8">, </span><span style="color:#F97583">&#x26;</span><span style="color:#B392F0">str</span><span style="color:#E1E4E8">)>>) {</span></span>\n<span class="line"><span style="color:#F97583">    if</span><span style="color:#F97583"> !</span><span style="color:#B392F0">cfg!</span><span style="color:#E1E4E8">(debug_assertions) { </span><span style="color:#F97583">return</span><span style="color:#E1E4E8">; }</span></span>\n<span class="line"><span style="color:#F97583">    if</span><span style="color:#B392F0"> std</span><span style="color:#F97583">::</span><span style="color:#B392F0">env</span><span style="color:#F97583">::</span><span style="color:#B392F0">var</span><span style="color:#E1E4E8">(</span><span style="color:#9ECBFF">"Capture"</span><span style="color:#E1E4E8">)</span><span style="color:#F97583">.</span><span style="color:#B392F0">as_deref</span><span style="color:#E1E4E8">() </span><span style="color:#F97583">==</span><span style="color:#B392F0"> Ok</span><span style="color:#E1E4E8">(</span><span style="color:#9ECBFF">"false"</span><span style="color:#E1E4E8">) { </span><span style="color:#F97583">return</span><span style="color:#E1E4E8">; }</span></span>\n<span class="line"><span style="color:#6A737D">    // ... call PostHog ...</span></span>\n<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>\n<p><code>cfg!(debug_assertions)</code> is <code>false</code> in <code>--release</code>. LLVM constant-folds the\n<code>if</code>, drops the dead branch, and the <code>posthog-rs</code> calls underneath become\nunreachable. Linker GC then strips the symbol entirely.</p>\n<p>Verify:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="sh"><code><span class="line"><span style="color:#B392F0">cargo</span><span style="color:#9ECBFF"> build</span><span style="color:#79B8FF"> -p</span><span style="color:#9ECBFF"> Mountain</span><span style="color:#79B8FF"> --release</span></span>\n<span class="line"><span style="color:#B392F0">strings</span><span style="color:#9ECBFF"> Element/Mountain/Target/release/Mountain</span><span style="color:#F97583"> |</span><span style="color:#B392F0"> grep</span><span style="color:#79B8FF"> -c</span><span style="color:#9ECBFF"> "posthog\\|/capture/\\|i.posthog.com"</span></span>\n<span class="line"><span style="color:#6A737D"># 0</span></span></code></pre>\n<h2 id="typescript--processenvnode_env--importmetaenvdev">TypeScript — <code>process.env.NODE_ENV</code> / <code>import.meta.env.DEV</code></h2>\n<p>esbuild + Vite substitute these literally at build time when configured with\n<code>define</code>:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="ts"><code><span class="line"><span style="color:#6A737D">// esbuild.config.ts</span></span>\n<span class="line"><span style="color:#E1E4E8">{</span></span>\n<span class="line"><span style="color:#B392F0">    define</span><span style="color:#E1E4E8">: {</span></span>\n<span class="line"><span style="color:#9ECBFF">        "process.env.NODE_ENV"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">JSON</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">stringify</span><span style="color:#E1E4E8">(</span></span>\n<span class="line"><span style="color:#E1E4E8">            process.env[</span><span style="color:#9ECBFF">"NODE_ENV"</span><span style="color:#E1E4E8">] </span><span style="color:#F97583">??</span><span style="color:#9ECBFF"> "development"</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#E1E4E8">        ),</span></span>\n<span class="line"><span style="color:#E1E4E8">    },</span></span>\n<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>\n<p>Then in code:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="ts"><code><span class="line"><span style="color:#F97583">let</span><span style="color:#E1E4E8"> Bridge</span><span style="color:#F97583">:</span><span style="color:#F97583"> typeof</span><span style="color:#F97583"> import</span><span style="color:#E1E4E8">(</span><span style="color:#9ECBFF">"./PostHogBridge.js"</span><span style="color:#E1E4E8">) </span><span style="color:#F97583">|</span><span style="color:#79B8FF"> undefined</span><span style="color:#E1E4E8">;</span></span>\n<span class="line"><span style="color:#F97583">if</span><span style="color:#E1E4E8"> (process.env.</span><span style="color:#79B8FF">NODE_ENV</span><span style="color:#F97583"> !==</span><span style="color:#9ECBFF"> "production"</span><span style="color:#E1E4E8">) {</span></span>\n<span class="line"><span style="color:#E1E4E8">	Bridge </span><span style="color:#F97583">=</span><span style="color:#F97583"> await</span><span style="color:#F97583"> import</span><span style="color:#E1E4E8">(</span><span style="color:#9ECBFF">"./PostHogBridge.js"</span><span style="color:#E1E4E8">);</span></span>\n<span class="line"><span style="color:#E1E4E8">}</span></span>\n<span class="line"><span style="color:#F97583">export</span><span style="color:#F97583"> const</span><span style="color:#B392F0"> CaptureEvent</span><span style="color:#F97583"> =</span><span style="color:#E1E4E8"> (</span></span>\n<span class="line"><span style="color:#FFAB70">	Name</span><span style="color:#F97583">:</span><span style="color:#79B8FF"> string</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#FFAB70">	Properties</span><span style="color:#F97583">:</span><span style="color:#B392F0"> Record</span><span style="color:#E1E4E8">&#x3C;</span><span style="color:#79B8FF">string</span><span style="color:#E1E4E8">, </span><span style="color:#79B8FF">unknown</span><span style="color:#E1E4E8">>,</span></span>\n<span class="line"><span style="color:#E1E4E8">)</span><span style="color:#F97583">:</span><span style="color:#79B8FF"> void</span><span style="color:#F97583"> =></span><span style="color:#E1E4E8"> {</span></span>\n<span class="line"><span style="color:#E1E4E8">	Bridge?.</span><span style="color:#B392F0">CaptureEvent</span><span style="color:#E1E4E8">(Name, Properties);</span></span>\n<span class="line"><span style="color:#E1E4E8">};</span></span></code></pre>\n<p>In a production build esbuild rewrites <code>process.env.NODE_ENV</code> to <code>"production"</code>,\nconstant-folds to <code>if (false)</code>, drops the <code>await import()</code>, and the chunk graph\nnever sees <code>PostHogBridge.js</code>.</p>\n<p>Verify:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="sh"><code><span class="line"><span style="color:#E1E4E8">NODE_ENV</span><span style="color:#F97583">=</span><span style="color:#9ECBFF">production</span><span style="color:#B392F0"> pnpm</span><span style="color:#9ECBFF"> run</span><span style="color:#79B8FF"> --filter</span><span style="color:#9ECBFF"> Cocoon</span><span style="color:#9ECBFF"> build</span></span>\n<span class="line"><span style="color:#B392F0">grep</span><span style="color:#79B8FF"> -rn</span><span style="color:#9ECBFF"> "i.posthog.com\\|/batch/"</span><span style="color:#9ECBFF"> Element/Cocoon/Target/</span></span>\n<span class="line"><span style="color:#6A737D"># 0 matches</span></span></code></pre>\n<h2 id="astro--importmetaenvdev">Astro — <code>import.meta.env.DEV</code></h2>\n<p>Vite replaces <code>import.meta.env.DEV</code> with <code>false</code> for <code>astro build</code>. Wrap every\nSky bridge import in:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="ts"><code><span class="line"><span style="color:#F97583">if</span><span style="color:#E1E4E8"> (</span><span style="color:#F97583">import</span><span style="color:#E1E4E8">.</span><span style="color:#79B8FF">meta</span><span style="color:#E1E4E8">.env.</span><span style="color:#79B8FF">DEV</span><span style="color:#E1E4E8">) {</span></span>\n<span class="line"><span style="color:#F97583">	const</span><span style="color:#E1E4E8"> { </span><span style="color:#FFAB70">default</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">InitTelemetry</span><span style="color:#E1E4E8"> } </span><span style="color:#F97583">=</span><span style="color:#F97583"> await</span><span style="color:#F97583"> import</span><span style="color:#E1E4E8">(</span><span style="color:#9ECBFF">"./Telemetry/Bridge.js"</span><span style="color:#E1E4E8">);</span></span>\n<span class="line"><span style="color:#B392F0">	InitTelemetry</span><span style="color:#E1E4E8">();</span></span>\n<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>\n<p>Verify:</p>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="sh"><code><span class="line"><span style="color:#B392F0">pnpm</span><span style="color:#9ECBFF"> run</span><span style="color:#79B8FF"> --filter</span><span style="color:#9ECBFF"> Sky</span><span style="color:#9ECBFF"> build</span></span>\n<span class="line"><span style="color:#B392F0">grep</span><span style="color:#79B8FF"> -rn</span><span style="color:#9ECBFF"> "posthog\\|otlp\\|jaeger"</span><span style="color:#9ECBFF"> Element/Sky/Target/Static/Application/</span></span>\n<span class="line"><span style="color:#6A737D"># 0 matches</span></span></code></pre>\n<h2 id="what-about-the-posthog-api-key">What About the PostHog API Key?</h2>\n<p>The key is built into release builds only if a release path imports the bridge.\nThe gate above means a clean release never sees the key. For the few\nrelease-mode events we deliberately keep (e.g. license activations), use a\nseparate “anonymous-only” key in <code>.env.Land.PostHog.Production</code>, gated through a\ndifferent module that does not import the dev bridge.</p>\n<h2 id="smoke-test">Smoke Test</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="sh"><code><span class="line"><span style="color:#B392F0">sh</span><span style="color:#9ECBFF"> Maintain/Test/TelemetryTreeShake.sh</span></span></code></pre>\n<p>Builds Mountain in <code>--release</code>, Cocoon / Sky / Output in <code>NODE_ENV=production</code>,\nruns <code>strings</code> / <code>grep</code> / <code>du -sh</code> against the output, fails CI if any forbidden\nsymbol is found.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/telemetry/overview">Telemetry Overview</a></li>\n<li><a href="https://Editor.Land/Doc/telemetry/effect-otel">Effect-TS and OpenTelemetry Integration</a></li>\n</ul>', { headings: 2244, localImagePaths: 2266, remoteImagePaths: 2267, frontmatter: 2268, imagePaths: 2269 }, [2245, 2247, 2250, 2253, 2256, 2259, 2262, 2265], { depth: 25, slug: 2246, text: 2236 }, "tree-shaking-telemetry", { depth: 29, slug: 2248, text: 2249 }, "the-three-gates", "The Three Gates", { depth: 29, slug: 2251, text: 2252 }, "rust--cfgdebug_assertions", "Rust — cfg!(debug_assertions)", { depth: 29, slug: 2254, text: 2255 }, "typescript--processenvnode_env--importmetaenvdev", "TypeScript — process.env.NODE_ENV / import.meta.env.DEV", { depth: 29, slug: 2257, text: 2258 }, "astro--importmetaenvdev", "Astro — import.meta.env.DEV", { depth: 29, slug: 2260, text: 2261 }, "what-about-the-posthog-api-key", "What About the PostHog API Key?", { depth: 29, slug: 2263, text: 2264 }, "smoke-test", "Smoke Test", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2236, section: 2117, order: 1918, description: 2237 }, [], "troubleshooting", { id: 2270, data: 2272, body: 2276, filePath: 2277, digest: 2278, rendered: 2279 }, { title: 2273, section: 2274, order: 194, description: 2275 }, "Troubleshooting", "Support", "Common issues and their solutions when running Editor.Land on macOS or Windows.", '# Troubleshooting\n\nThis page covers the most common issues encountered when running `Editor.Land`\non **macOS 13+** and **Windows 10/11**, along with diagnosis steps and fixes.\n\n---\n\n## Blank Screen on Launch\n\n**Symptoms:** The window opens but shows a white or black screen with no UI.\n\n**Diagnosis on macOS:**\n\nLaunch Land from the terminal via `cargo tauri dev` and watch the console output\nfor errors. WebKit or renderer errors will appear there. If you are running a\npre-built binary, open **Console.app**, filter by the Land process name, and\nlook for errors at or around the launch timestamp.\n\n**Diagnosis on Windows:**\n\nLaunch via `cargo tauri dev` in a terminal window. `WebView2` errors are written\nto standard output. If you are running a pre-built binary, open **Event\nViewer**, navigate to **Windows Logs → Application**, and filter for the Land\nprocess name.\n\n**Fixes:**\n\n- **macOS:** Confirm you are running **macOS 13.0 or later**. Older WebKit\n  versions do not support all CSS features Land\'s UI requires.\n- **Windows:** Confirm the **`WebView2` Runtime** is installed. Download it from\n  [microsoft.com/edge/webview2](https://developer.microsoft.com/en-us/microsoft-edge/webview2/)\n  if missing. The Evergreen Bootstrapper is the simplest install option.\n- If the console shows GPU-related errors, try quitting other GPU-intensive\n  applications and relaunching.\n- If you built from source, ensure the build completed without errors:\n    ```bash\n    cargo tauri build\n    ```\n    A partially-failed build can produce a binary that launches but renders\n    nothing.\n\n---\n\n## Extension Not Loading\n\n**Symptoms:** An extension appears to be installed but does not activate, or\ncommands it contributes do not appear in the command palette.\n\n**Diagnosis:**\n\n1. Check the `Output` panel. Select **Extension Host** from the channel dropdown\n   to see activation errors and log output from `Cocoon`.\n2. Look for activation event mismatches - if the extension declares\n   `activationEvents` that Land does not fire, it will never activate.\n\n**Fixes:**\n\n- Check the `engines.vscode` field in the extension\'s `package.json`. If it\n  requires a `VS Code` version whose `API` surface is not yet implemented in\n  `Cocoon`, the extension may silently fail. See\n  [`API` Reference](https://Editor.Land/Doc/api-reference) for the current\n  coverage table.\n- If the extension uses an `API` namespace listed as **Not Implemented** in the\n  `API` Reference (`vscode.lm`, `vscode.chat`, `vscode.notebook`,\n  `vscode.tests`), those calls will no-op at runtime.\n- Try reinstalling the extension: remove it, restart Land, then install again.\n\n---\n\n## High CPU Usage\n\n**Symptoms:** Land consumes excessive CPU even when idle.\n\n**Diagnosis:**\n\nOpen **Activity Monitor** on macOS or **Task Manager** on Windows, filter by\nprocess name, and identify which process is consuming CPU - the main Land\nprocess, a language server child process, or `Node.js` (the `Cocoon` extension\nhost).\n\n**Fixes:**\n\n- **Language server runaway:** Some language servers index the entire file\n  system on first open. Add large directories to `Files.Exclude` in your\n  settings:\n    ```json\n    {\n    	"Files.Exclude": {\n    		"**/node_modules": true,\n    		"**/target": true,\n    		"**/.git": true\n    	}\n    }\n    ```\n- **Extension host:** Disable extensions one by one via the command palette to\n  identify the offending extension. Then file an issue with the extension\n  author.\n- **File watcher overload:** Reduce the number of watched files by adding paths\n  to `Files.WatcherExclude` in your settings.\n\n---\n\n## Permission Errors\n\n**Symptoms:** Land cannot access files, or shows "operation not permitted" /\n"access denied" errors when opening a project.\n\n### macOS\n\nmacOS requires explicit user consent for file access outside standard\ndirectories, enforced by the TCC (Transparency, Consent, and Control) system\nregardless of file ownership.\n\n1. Open **System Settings → Privacy & Security → Files and Folders**.\n2. Find Land in the list and grant access to the directories it needs.\n3. If Land does not appear, try opening a folder from within Land using **File →\n   Open Folder** so macOS can prompt for consent.\n4. If permissions appear stuck, you can reset the TCC entry. The bundle\n   identifier follows the pattern `com.codeeditorland.*` - check the exact\n   identifier in the built `.app` bundle\'s `Info.plist` before running any\n   `tccutil reset` command.\n\n### Windows\n\n- Ensure the directory you are opening is not under a path blocked by **Windows\n  Defender Controlled Folder Access**. If it is, add Land as an allowed\n  application in **Windows Security → Virus & threat protection → Ransomware\n  protection**.\n- Verify NTFS permissions on the target directory. Right-click the folder,\n  select **Properties → Security**, and confirm your user account has Read and\n  Write permissions.\n- If running a dev build, ensure you launched the terminal as your normal user\n  (not as Administrator), as some paths behave differently under elevated\n  contexts.\n\n---\n\n## Getting Further Help\n\nIf none of the above resolves your issue:\n\n1. Collect diagnostic information: your OS version (macOS version or Windows\n   version and build number), Land build date or commit SHA, and the exact steps\n   to reproduce.\n2. If you built from source, run `cargo tauri dev` and copy any error output\n   from the terminal.\n3. Open an issue at\n   [CodeEditorLand/Land](https://github.com/CodeEditorLand/Land/issues) with the\n   collected information. Clear reproduction steps are the single most useful\n   thing you can include.\n\n---\n\n## See Also\n\n- [Installation](https://Editor.Land/Doc/installation)\n- [Getting Started](https://Editor.Land/Doc/getting-started)\n- [Configuration](https://Editor.Land/Doc/configuration)\n- [`API` Reference](https://Editor.Land/Doc/api-reference)', "Source/Content/doc/troubleshooting.md", "797c17bc6b6c9611", { html: 2280, metadata: 2281 }, '<h1 id="troubleshooting">Troubleshooting</h1>\n<p>This page covers the most common issues encountered when running <code>Editor.Land</code>\non <strong>macOS 13+</strong> and <strong>Windows 10/11</strong>, along with diagnosis steps and fixes.</p>\n<hr>\n<h2 id="blank-screen-on-launch">Blank Screen on Launch</h2>\n<p><strong>Symptoms:</strong> The window opens but shows a white or black screen with no UI.</p>\n<p><strong>Diagnosis on macOS:</strong></p>\n<p>Launch Land from the terminal via <code>cargo tauri dev</code> and watch the console output\nfor errors. WebKit or renderer errors will appear there. If you are running a\npre-built binary, open <strong>Console.app</strong>, filter by the Land process name, and\nlook for errors at or around the launch timestamp.</p>\n<p><strong>Diagnosis on Windows:</strong></p>\n<p>Launch via <code>cargo tauri dev</code> in a terminal window. <code>WebView2</code> errors are written\nto standard output. If you are running a pre-built binary, open <strong>Event\nViewer</strong>, navigate to <strong>Windows Logs → Application</strong>, and filter for the Land\nprocess name.</p>\n<p><strong>Fixes:</strong></p>\n<ul>\n<li><strong>macOS:</strong> Confirm you are running <strong>macOS 13.0 or later</strong>. Older WebKit\nversions do not support all CSS features Land’s UI requires.</li>\n<li><strong>Windows:</strong> Confirm the <strong><code>WebView2</code> Runtime</strong> is installed. Download it from\n<a href="https://developer.microsoft.com/en-us/microsoft-edge/webview2/">microsoft.com/edge/webview2</a>\nif missing. The Evergreen Bootstrapper is the simplest install option.</li>\n<li>If the console shows GPU-related errors, try quitting other GPU-intensive\napplications and relaunching.</li>\n<li>If you built from source, ensure the build completed without errors:\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="bash"><code><span class="line"><span style="color:#B392F0">cargo</span><span style="color:#9ECBFF"> tauri</span><span style="color:#9ECBFF"> build</span></span></code></pre>\nA partially-failed build can produce a binary that launches but renders\nnothing.</li>\n</ul>\n<hr>\n<h2 id="extension-not-loading">Extension Not Loading</h2>\n<p><strong>Symptoms:</strong> An extension appears to be installed but does not activate, or\ncommands it contributes do not appear in the command palette.</p>\n<p><strong>Diagnosis:</strong></p>\n<ol>\n<li>Check the <code>Output</code> panel. Select <strong>Extension Host</strong> from the channel dropdown\nto see activation errors and log output from <code>Cocoon</code>.</li>\n<li>Look for activation event mismatches - if the extension declares\n<code>activationEvents</code> that Land does not fire, it will never activate.</li>\n</ol>\n<p><strong>Fixes:</strong></p>\n<ul>\n<li>Check the <code>engines.vscode</code> field in the extension’s <code>package.json</code>. If it\nrequires a <code>VS Code</code> version whose <code>API</code> surface is not yet implemented in\n<code>Cocoon</code>, the extension may silently fail. See\n<a href="https://Editor.Land/Doc/api-reference"><code>API</code> Reference</a> for the current\ncoverage table.</li>\n<li>If the extension uses an <code>API</code> namespace listed as <strong>Not Implemented</strong> in the\n<code>API</code> Reference (<code>vscode.lm</code>, <code>vscode.chat</code>, <code>vscode.notebook</code>,\n<code>vscode.tests</code>), those calls will no-op at runtime.</li>\n<li>Try reinstalling the extension: remove it, restart Land, then install again.</li>\n</ul>\n<hr>\n<h2 id="high-cpu-usage">High CPU Usage</h2>\n<p><strong>Symptoms:</strong> Land consumes excessive CPU even when idle.</p>\n<p><strong>Diagnosis:</strong></p>\n<p>Open <strong>Activity Monitor</strong> on macOS or <strong>Task Manager</strong> on Windows, filter by\nprocess name, and identify which process is consuming CPU - the main Land\nprocess, a language server child process, or <code>Node.js</code> (the <code>Cocoon</code> extension\nhost).</p>\n<p><strong>Fixes:</strong></p>\n<ul>\n<li><strong>Language server runaway:</strong> Some language servers index the entire file\nsystem on first open. Add large directories to <code>Files.Exclude</code> in your\nsettings:\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="json"><code><span class="line"><span style="color:#E1E4E8">{</span></span>\n<span class="line"><span style="color:#79B8FF">	"Files.Exclude"</span><span style="color:#E1E4E8">: {</span></span>\n<span class="line"><span style="color:#79B8FF">		"**/node_modules"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">true</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#79B8FF">		"**/target"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">true</span><span style="color:#E1E4E8">,</span></span>\n<span class="line"><span style="color:#79B8FF">		"**/.git"</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">true</span></span>\n<span class="line"><span style="color:#E1E4E8">	}</span></span>\n<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>\n</li>\n<li><strong>Extension host:</strong> Disable extensions one by one via the command palette to\nidentify the offending extension. Then file an issue with the extension\nauthor.</li>\n<li><strong>File watcher overload:</strong> Reduce the number of watched files by adding paths\nto <code>Files.WatcherExclude</code> in your settings.</li>\n</ul>\n<hr>\n<h2 id="permission-errors">Permission Errors</h2>\n<p><strong>Symptoms:</strong> Land cannot access files, or shows “operation not permitted” /\n“access denied” errors when opening a project.</p>\n<h3 id="macos">macOS</h3>\n<p>macOS requires explicit user consent for file access outside standard\ndirectories, enforced by the TCC (Transparency, Consent, and Control) system\nregardless of file ownership.</p>\n<ol>\n<li>Open <strong>System Settings → Privacy &#x26; Security → Files and Folders</strong>.</li>\n<li>Find Land in the list and grant access to the directories it needs.</li>\n<li>If Land does not appear, try opening a folder from within Land using <strong>File →\nOpen Folder</strong> so macOS can prompt for consent.</li>\n<li>If permissions appear stuck, you can reset the TCC entry. The bundle\nidentifier follows the pattern <code>com.codeeditorland.*</code> - check the exact\nidentifier in the built <code>.app</code> bundle’s <code>Info.plist</code> before running any\n<code>tccutil reset</code> command.</li>\n</ol>\n<h3 id="windows">Windows</h3>\n<ul>\n<li>Ensure the directory you are opening is not under a path blocked by <strong>Windows\nDefender Controlled Folder Access</strong>. If it is, add Land as an allowed\napplication in <strong>Windows Security → Virus &#x26; threat protection → Ransomware\nprotection</strong>.</li>\n<li>Verify NTFS permissions on the target directory. Right-click the folder,\nselect <strong>Properties → Security</strong>, and confirm your user account has Read and\nWrite permissions.</li>\n<li>If running a dev build, ensure you launched the terminal as your normal user\n(not as Administrator), as some paths behave differently under elevated\ncontexts.</li>\n</ul>\n<hr>\n<h2 id="getting-further-help">Getting Further Help</h2>\n<p>If none of the above resolves your issue:</p>\n<ol>\n<li>Collect diagnostic information: your OS version (macOS version or Windows\nversion and build number), Land build date or commit SHA, and the exact steps\nto reproduce.</li>\n<li>If you built from source, run <code>cargo tauri dev</code> and copy any error output\nfrom the terminal.</li>\n<li>Open an issue at\n<a href="https://github.com/CodeEditorLand/Land/issues">CodeEditorLand/Land</a> with the\ncollected information. Clear reproduction steps are the single most useful\nthing you can include.</li>\n</ol>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/installation">Installation</a></li>\n<li><a href="https://Editor.Land/Doc/getting-started">Getting Started</a></li>\n<li><a href="https://Editor.Land/Doc/configuration">Configuration</a></li>\n<li><a href="https://Editor.Land/Doc/api-reference"><code>API</code> Reference</a></li>\n</ul>', { headings: 2282, localImagePaths: 2302, remoteImagePaths: 2303, frontmatter: 2304, imagePaths: 2305 }, [2283, 2284, 2287, 2290, 2293, 2296, 2297, 2298, 2301], { depth: 25, slug: 2270, text: 2273 }, { depth: 29, slug: 2285, text: 2286 }, "blank-screen-on-launch", "Blank Screen on Launch", { depth: 29, slug: 2288, text: 2289 }, "extension-not-loading", "Extension Not Loading", { depth: 29, slug: 2291, text: 2292 }, "high-cpu-usage", "High CPU Usage", { depth: 29, slug: 2294, text: 2295 }, "permission-errors", "Permission Errors", { depth: 347, slug: 1710, text: 1711 }, { depth: 347, slug: 1713, text: 1714 }, { depth: 29, slug: 2299, text: 2300 }, "getting-further-help", "Getting Further Help", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2273, section: 2274, order: 194, description: 2275 }, [], { id: 1513, data: 2307, body: 2310, filePath: 2311, digest: 2312, rendered: 2313 }, { title: 1514, section: 381, order: 2308, description: 2309 }, 17, "The protobuf schema and bidirectional gRPC transport layer connecting Mountain and Cocoon.", "# Vine\n\n`Vine` is the wire protocol for `Editor.Land` - a set of Protocol Buffers\nschemas (`.proto` files) that define the bidirectional `gRPC` service interfaces\nused between `Mountain` (the `Rust` kernel) and `Cocoon` (the `Node.js`\nextension host). All inter-process communication that crosses the\n`Mountain`↔`Cocoon` boundary goes through `Vine`.\n\n**Where the implementation lives today:** The working proto definitions and\n`gRPC` server code currently reside inside `Mountain`'s own `Source/Vine/`\ndirectory, and the `Cocoon` `gRPC` client lives at\n`Services/MountainGRPCClient.ts`. The standalone\n[`Vine` repository](https://github.com/CodeEditorLand/Vine) is the future home\nfor these schemas as a published, independently versioned package - it is\nactively being populated as the protocol stabilises.\n\n---\n\n## Why a Typed Protocol\n\n`Electron`'s `IPC` is untyped. A call to\n`ipcRenderer.send('open-file', { path })` has no enforced contract on either\nside. Renaming a field, changing an argument type, or removing a handler\nproduces a silent runtime failure, not a build error.\n\n`Vine` defines every inter-process call as a `.proto` service method with typed\nrequest and response messages. The generated `Rust` stubs (via\n[tonic](https://github.com/hyperium/tonic)) and `TypeScript` stubs are the only\nway `Mountain` and `Cocoon` communicate. If a message field is renamed or\nremoved, the `Rust` build and the `TypeScript` build each fail at their own\ncompile step. There is no single cross-language compile - the two languages\nvalidate independently - but neither side can silently drift from the schema.\n\n---\n\n## Protocol Schema Family\n\n`Vine` is the umbrella name for a family of three `.proto` files:\n\n| Schema        | Purpose                                                                     |\n| ------------- | --------------------------------------------------------------------------- |\n| `Vine.proto`  | Core Mountain↔Cocoon communication (file system, pty, DAP, lifecycle)       |\n| `Spine.proto` | Extension host coordination - action/response pattern for command execution |\n| `Grove.proto` | Grove-specific extensions for the planned Rust/WASM extension host          |\n\n---\n\n## What the Schema Covers\n\nService interfaces defined for both directions of the `Mountain`↔`Cocoon`\ncommunication:\n\n**`Cocoon` → `Mountain` (`MountainService`):**\n\n- **File system operations** - read, write, stat, watch, and delete calls that\n  back the `vscode.workspace.fs.*` `API`.\n- **Terminal (pty) management** - spawn, resize, write, and close calls that\n  back `vscode.window.createTerminal`.\n- **DAP bridge** - the Debug Adapter Protocol proxy calls that back\n  `vscode.debug.startDebugging`.\n- **Process lifecycle** - extension host registration, heartbeat, and shutdown\n  coordination.\n\n**`Mountain` → `Cocoon` (`CocoonService`):**\n\n- **Extension host lifecycle** - `InitializeExtensionHost`, `$deltaExtensions`,\n  `$activateByEvent`, `$startExtensionHost`.\n- **Language feature invocations** - `$provideHover`, `$provideCompletionItems`,\n  `$provideDefinition`, `$provideReferences`, and related methods.\n- **Document content** - `$acceptModelChanged` delivers text change deltas from\n  `Mountain` to `Cocoon`'s document content cache.\n- **Notifications and requests** - `SendMountainNotification`,\n  `ProcessMountainRequest`, `CancelOperation`.\n\n`Vine` is versioned through the protobuf schema itself. Adding a new optional\nfield is backwards-compatible; removing or renaming a field requires updating\nboth the `Mountain` implementation and the `Cocoon` stubs in the same change.\n\n---\n\n## Transport\n\nBoth `Mountain` and `Cocoon` run `gRPC` servers; both also act as `gRPC`\nclients:\n\n- **`Mountain` server** - `Cocoon` dials `Mountain` at startup for file system,\n  pty, DAP, and lifecycle calls.\n- **`Cocoon` server** - `Mountain` dials `Cocoon` to invoke extension host\n  methods and deliver workspace notifications.\n\nBoth sockets are strictly local - both processes run on the same machine, so no\nexternal network traffic is involved. Each socket is secured with a TLS\ncertificate generated at startup using `rcgen` + `p256`, ensuring that even\nlocal inter-process traffic is authenticated and encrypted.\n\n`Vine` is designed for transport agnosticism: TCP and `IPC` are the current\nbackends; `WASM` host functions are the planned backend for the `Grove`\nextension host.\n\n---\n\n## Relationship to Mist\n\n[`architecture.md`](https://Editor.Land/Doc/architecture) lists `Mist` as a\nseparate WebSocket communication layer. `Mist` handles `Sky` (the OS WebView\nUI)↔`Mountain` communication over WebSockets, which is a different transport\nfrom `Vine`'s `gRPC`. `Vine` is specifically the `Mountain`↔`Cocoon` layer. The\ntwo protocols are not interchangeable and serve different parts of the system.\n\n---\n\n## Development Status\n\n| Feature                                            | Status         |\n| -------------------------------------------------- | -------------- |\n| `Spine.proto` - extension host coordination        | ✅ Specified   |\n| `Vine.proto` - Mountain↔Cocoon gRPC (in Mountain)  | ✅ Active      |\n| Standalone Vine package (published `.proto` files) | 🔄 In progress |\n| `Grove.proto` - WASM extension host protocol       | 🔄 In progress |\n| Health monitoring / heartbeat                      | 🔄 In progress |\n| Transport agnosticism (WASM host functions)        | 📋 Planned     |\n\n---\n\n## What Is Next\n\n- The schema does not yet cover the `vscode.lm.*`, `vscode.chat.*`,\n  `vscode.notebook.*`, or `vscode.tests.*` namespaces. Those `APIs` are not yet\n  implemented in `Cocoon`, so no `Vine` service definitions exist for them.\n- The `vscode.tasks.*` task resolver is partially implemented; the corresponding\n  `Vine` service methods are being completed.\n- The standalone `Vine` package will be published once the schema stabilises, so\n  downstream `Rust` crates and `TypeScript` clients can depend on it directly.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Mountain`: Native Kernel](https://Editor.Land/Doc/mountain)\n- [`Cocoon`: Extension Host](https://Editor.Land/Doc/cocoon)\n- [Source Code](https://github.com/CodeEditorLand/Vine)", "Source/Content/doc/vine.md", "6acb07a4266238bf", { html: 2314, metadata: 2315 }, `<h1 id="vine">Vine</h1>
<p><code>Vine</code> is the wire protocol for <code>Editor.Land</code> - a set of Protocol Buffers
schemas (<code>.proto</code> files) that define the bidirectional <code>gRPC</code> service interfaces
used between <code>Mountain</code> (the <code>Rust</code> kernel) and <code>Cocoon</code> (the <code>Node.js</code>
extension host). All inter-process communication that crosses the
<code>Mountain</code>↔<code>Cocoon</code> boundary goes through <code>Vine</code>.</p>
<p><strong>Where the implementation lives today:</strong> The working proto definitions and
<code>gRPC</code> server code currently reside inside <code>Mountain</code>’s own <code>Source/Vine/</code>
directory, and the <code>Cocoon</code> <code>gRPC</code> client lives at
<code>Services/MountainGRPCClient.ts</code>. The standalone
<a href="https://github.com/CodeEditorLand/Vine"><code>Vine</code> repository</a> is the future home
for these schemas as a published, independently versioned package - it is
actively being populated as the protocol stabilises.</p>
<hr>
<h2 id="why-a-typed-protocol">Why a Typed Protocol</h2>
<p><code>Electron</code>’s <code>IPC</code> is untyped. A call to
<code>ipcRenderer.send('open-file', { path })</code> has no enforced contract on either
side. Renaming a field, changing an argument type, or removing a handler
produces a silent runtime failure, not a build error.</p>
<p><code>Vine</code> defines every inter-process call as a <code>.proto</code> service method with typed
request and response messages. The generated <code>Rust</code> stubs (via
<a href="https://github.com/hyperium/tonic">tonic</a>) and <code>TypeScript</code> stubs are the only
way <code>Mountain</code> and <code>Cocoon</code> communicate. If a message field is renamed or
removed, the <code>Rust</code> build and the <code>TypeScript</code> build each fail at their own
compile step. There is no single cross-language compile - the two languages
validate independently - but neither side can silently drift from the schema.</p>
<hr>
<h2 id="protocol-schema-family">Protocol Schema Family</h2>
<p><code>Vine</code> is the umbrella name for a family of three <code>.proto</code> files:</p>





















<table><thead><tr><th>Schema</th><th>Purpose</th></tr></thead><tbody><tr><td><code>Vine.proto</code></td><td>Core Mountain↔Cocoon communication (file system, pty, DAP, lifecycle)</td></tr><tr><td><code>Spine.proto</code></td><td>Extension host coordination - action/response pattern for command execution</td></tr><tr><td><code>Grove.proto</code></td><td>Grove-specific extensions for the planned Rust/WASM extension host</td></tr></tbody></table>
<hr>
<h2 id="what-the-schema-covers">What the Schema Covers</h2>
<p>Service interfaces defined for both directions of the <code>Mountain</code>↔<code>Cocoon</code>
communication:</p>
<p><strong><code>Cocoon</code> → <code>Mountain</code> (<code>MountainService</code>):</strong></p>
<ul>
<li><strong>File system operations</strong> - read, write, stat, watch, and delete calls that
back the <code>vscode.workspace.fs.*</code> <code>API</code>.</li>
<li><strong>Terminal (pty) management</strong> - spawn, resize, write, and close calls that
back <code>vscode.window.createTerminal</code>.</li>
<li><strong>DAP bridge</strong> - the Debug Adapter Protocol proxy calls that back
<code>vscode.debug.startDebugging</code>.</li>
<li><strong>Process lifecycle</strong> - extension host registration, heartbeat, and shutdown
coordination.</li>
</ul>
<p><strong><code>Mountain</code> → <code>Cocoon</code> (<code>CocoonService</code>):</strong></p>
<ul>
<li><strong>Extension host lifecycle</strong> - <code>InitializeExtensionHost</code>, <code>$deltaExtensions</code>,
<code>$activateByEvent</code>, <code>$startExtensionHost</code>.</li>
<li><strong>Language feature invocations</strong> - <code>$provideHover</code>, <code>$provideCompletionItems</code>,
<code>$provideDefinition</code>, <code>$provideReferences</code>, and related methods.</li>
<li><strong>Document content</strong> - <code>$acceptModelChanged</code> delivers text change deltas from
<code>Mountain</code> to <code>Cocoon</code>’s document content cache.</li>
<li><strong>Notifications and requests</strong> - <code>SendMountainNotification</code>,
<code>ProcessMountainRequest</code>, <code>CancelOperation</code>.</li>
</ul>
<p><code>Vine</code> is versioned through the protobuf schema itself. Adding a new optional
field is backwards-compatible; removing or renaming a field requires updating
both the <code>Mountain</code> implementation and the <code>Cocoon</code> stubs in the same change.</p>
<hr>
<h2 id="transport">Transport</h2>
<p>Both <code>Mountain</code> and <code>Cocoon</code> run <code>gRPC</code> servers; both also act as <code>gRPC</code>
clients:</p>
<ul>
<li><strong><code>Mountain</code> server</strong> - <code>Cocoon</code> dials <code>Mountain</code> at startup for file system,
pty, DAP, and lifecycle calls.</li>
<li><strong><code>Cocoon</code> server</strong> - <code>Mountain</code> dials <code>Cocoon</code> to invoke extension host
methods and deliver workspace notifications.</li>
</ul>
<p>Both sockets are strictly local - both processes run on the same machine, so no
external network traffic is involved. Each socket is secured with a TLS
certificate generated at startup using <code>rcgen</code> + <code>p256</code>, ensuring that even
local inter-process traffic is authenticated and encrypted.</p>
<p><code>Vine</code> is designed for transport agnosticism: TCP and <code>IPC</code> are the current
backends; <code>WASM</code> host functions are the planned backend for the <code>Grove</code>
extension host.</p>
<hr>
<h2 id="relationship-to-mist">Relationship to Mist</h2>
<p><a href="https://Editor.Land/Doc/architecture"><code>architecture.md</code></a> lists <code>Mist</code> as a
separate WebSocket communication layer. <code>Mist</code> handles <code>Sky</code> (the OS WebView
UI)↔<code>Mountain</code> communication over WebSockets, which is a different transport
from <code>Vine</code>’s <code>gRPC</code>. <code>Vine</code> is specifically the <code>Mountain</code>↔<code>Cocoon</code> layer. The
two protocols are not interchangeable and serve different parts of the system.</p>
<hr>
<h2 id="development-status">Development Status</h2>

































<table><thead><tr><th>Feature</th><th>Status</th></tr></thead><tbody><tr><td><code>Spine.proto</code> - extension host coordination</td><td>✅ Specified</td></tr><tr><td><code>Vine.proto</code> - Mountain↔Cocoon gRPC (in Mountain)</td><td>✅ Active</td></tr><tr><td>Standalone Vine package (published <code>.proto</code> files)</td><td>🔄 In progress</td></tr><tr><td><code>Grove.proto</code> - WASM extension host protocol</td><td>🔄 In progress</td></tr><tr><td>Health monitoring / heartbeat</td><td>🔄 In progress</td></tr><tr><td>Transport agnosticism (WASM host functions)</td><td>📋 Planned</td></tr></tbody></table>
<hr>
<h2 id="what-is-next">What Is Next</h2>
<ul>
<li>The schema does not yet cover the <code>vscode.lm.*</code>, <code>vscode.chat.*</code>,
<code>vscode.notebook.*</code>, or <code>vscode.tests.*</code> namespaces. Those <code>APIs</code> are not yet
implemented in <code>Cocoon</code>, so no <code>Vine</code> service definitions exist for them.</li>
<li>The <code>vscode.tasks.*</code> task resolver is partially implemented; the corresponding
<code>Vine</code> service methods are being completed.</li>
<li>The standalone <code>Vine</code> package will be published once the schema stabilises, so
downstream <code>Rust</code> crates and <code>TypeScript</code> clients can depend on it directly.</li>
</ul>
<hr>
<h2 id="related-documentation">Related Documentation 📖</h2>
<ul>
<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>
<li><a href="https://Editor.Land/Doc/mountain"><code>Mountain</code>: Native Kernel</a></li>
<li><a href="https://Editor.Land/Doc/cocoon"><code>Cocoon</code>: Extension Host</a></li>
<li><a href="https://github.com/CodeEditorLand/Vine">Source Code</a></li>
</ul>`, { headings: 2316, localImagePaths: 2340, remoteImagePaths: 2341, frontmatter: 2342, imagePaths: 2343 }, [2317, 2318, 2321, 2324, 2327, 2330, 2333, 2336, 2339], { depth: 25, slug: 1513, text: 1514 }, { depth: 29, slug: 2319, text: 2320 }, "why-a-typed-protocol", "Why a Typed Protocol", { depth: 29, slug: 2322, text: 2323 }, "protocol-schema-family", "Protocol Schema Family", { depth: 29, slug: 2325, text: 2326 }, "what-the-schema-covers", "What the Schema Covers", { depth: 29, slug: 2328, text: 2329 }, "transport", "Transport", { depth: 29, slug: 2331, text: 2332 }, "relationship-to-mist", "Relationship to Mist", { depth: 29, slug: 2334, text: 2335 }, "development-status", "Development Status", { depth: 29, slug: 2337, text: 2338 }, "what-is-next", "What Is Next", { depth: 29, slug: 401, text: 402 }, [], [], { title: 1514, section: 381, order: 2308, description: 2309 }, [], "vscode-api-coverage", { id: 2344, data: 2346, body: 2349, filePath: 2350, digest: 2351, rendered: 2352 }, { title: 2347, section: 493, order: 16, description: 2348 }, "VS Code API Coverage", "Map of vscode.* API surfaces across Sky, Cocoon, and Mountain with implementation status.", "# VS Code API Coverage\n\nAuthoritative map of every top-level `vscode.*` API surface and its\nimplementation split across **Sky** (workbench renderer), **Cocoon**\n(extension-host Node sidecar), and **Mountain** (Rust backend).\n\n---\n\n## Dual-Track Strategy\n\n- **Track A - Stock-Node**: Cocoon loads stock VS Code `extHost*.ts` sources\n  unchanged. The workbench's `mainThread*.ts` runs inside Sky. The\n  ExtHostContext/MainContext RPC glue is provided by Cocoon's shim alongside\n  stock extHost code. Maximum compatibility from day one for any API the stock\n  implementation handles in-process.\n\n- **Track B - Rust-native**: Mountain owns the backend. Cocoon's `vscode` shim\n  sends gRPC to Mountain which does the work natively (filesystem, process,\n  terminal, search, git). Faster and safer than bouncing through Node for\n  I/O-heavy APIs.\n\nThe two tracks are not mutually exclusive. Most APIs live on Track A by default,\nand we promote individual operations to Track B when a measured benefit exists.\nThe Cocoon `<Namespace>Route.ts` tier router decides per-call.\n\n---\n\n## Legend\n\n| Track | Meaning                                                       |\n| ----- | ------------------------------------------------------------- |\n| **A** | Stock-Node - lift unchanged `extHost*.ts` into Cocoon         |\n| **B** | Rust-native - Mountain owns backend, gRPC from Cocoon         |\n| **C** | Cocoon-bespoke - hand-rolled TS in Land (last resort)         |\n| **S** | Sky-direct - call via `__CEL_SERVICES__.*` workbench accessor |\n\nStatus symbols:\n\n- **Working** - end-to-end (activation + feature render path confirmed)\n- **Partial** - RPC wired but UI render gap, or missing sub-method\n- **Stubbed** - registration accepted, no effect\n- **Not attempted**\n- **Pure lift** - stateless, no RPC, already landed via `StockLift.ts`\n\n---\n\n## Commands\n\n| Operation                   | Primary | Status  |\n| --------------------------- | ------- | ------- |\n| `registerCommand`           | A+S     | Working |\n| `registerTextEditorCommand` | A       | Working |\n| `executeCommand`            | A+S     | Working |\n| `getCommands`               | A       | Working |\n\n## Window - Editors\n\n| Operation                     | Primary | Status  |\n| ----------------------------- | ------- | ------- |\n| `activeTextEditor`            | A       | Working |\n| `visibleTextEditors`          | A       | Working |\n| `showTextDocument`            | A       | Working |\n| `onDidChangeActiveTextEditor` | A       | Working |\n\n## Window - Surfaces\n\n| Operation                                                            | Primary    | Status  |\n| -------------------------------------------------------------------- | ---------- | ------- |\n| `createStatusBarItem`                                                | S (native) | Working |\n| `setStatusBarMessage`                                                | S          | Working |\n| `createTreeView`                                                     | A+S        | Working |\n| `registerTreeDataProvider`                                           | A+S        | Working |\n| `createWebviewPanel`                                                 | A          | Stubbed |\n| `registerWebviewViewProvider`                                        | A          | Stubbed |\n| `registerCustomEditorProvider`                                       | A          | Stubbed |\n| `createTerminal`                                                     | B          | Partial |\n| `onDidOpen/CloseTerminal`                                            | B          | Partial |\n| `createOutputChannel`                                                | S          | Working |\n| `showInformationMessage` / `showWarningMessage` / `showErrorMessage` | A          | Working |\n| `showQuickPick` / `createQuickPick`                                  | A          | Partial |\n| `showInputBox`                                                       | A          | Partial |\n| `showOpenDialog` / `showSaveDialog`                                  | B          | Working |\n| `withProgress`                                                       | A+S        | Partial |\n| `registerFileDecorationProvider`                                     | A          | Partial |\n| `registerUriHandler`                                                 | A          | Partial |\n| `onDidChangeWindowState`                                             | A          | Working |\n| `showNotebookDocument`                                               | A          | Stubbed |\n\n## Workspace\n\n| Operation                                                                                                 | Primary | Status  |\n| --------------------------------------------------------------------------------------------------------- | ------- | ------- |\n| `workspaceFolders`                                                                                        | A       | Working |\n| `onDidChangeWorkspaceFolders`                                                                             | A       | Working |\n| `getWorkspaceFolder`                                                                                      | A+B     | Working |\n| `textDocuments`                                                                                           | A       | Working |\n| `openTextDocument`                                                                                        | A+B     | Working |\n| `onDidOpen/Close/SaveTextDocument`                                                                        | A       | Working |\n| `onWillSaveTextDocument`                                                                                  | A       | Partial |\n| `applyEdit`                                                                                               | A+S     | Partial |\n| `save` / `saveAs`                                                                                         | A       | Gap     |\n| `fs.readFile` / `writeFile` / `stat` / `readDirectory` / `createDirectory` / `delete` / `rename` / `copy` | **B**   | Working |\n| `fs.isWritableFileSystem`                                                                                 | A       | Working |\n| `registerFileSystemProvider`                                                                              | A       | Partial |\n| `createFileSystemWatcher`                                                                                 | B       | Partial |\n| `findFiles`                                                                                               | **B**   | Working |\n| `findTextInFiles`                                                                                         | **B**   | Working |\n| `registerTextDocumentContentProvider`                                                                     | A       | Partial |\n| `getConfiguration` / `onDidChangeConfiguration`                                                           | A+B     | Working |\n| `registerTaskProvider`                                                                                    | A       | Partial |\n\n## Languages\n\n| Operation                                | Primary | Status  |\n| ---------------------------------------- | ------- | ------- |\n| `registerCompletionItemProvider`         | A       | Partial |\n| `registerHoverProvider`                  | A       | Partial |\n| `registerDefinitionProvider`             | A       | Partial |\n| `registerReferenceProvider`              | A       | Partial |\n| `registerDocumentSymbolProvider`         | A       | Partial |\n| `registerCodeActionsProvider`            | A       | Partial |\n| `registerCodeLensProvider`               | A       | Partial |\n| `registerDocumentFormattingEditProvider` | A       | Partial |\n| `registerRenameProvider`                 | A       | Partial |\n| `registerInlayHintsProvider`             | A       | Partial |\n| `registerFoldingRangeProvider`           | A       | Partial |\n| `registerSemanticTokensProvider`         | A       | Partial |\n| `registerSignatureHelpProvider`          | A       | Partial |\n| `setTextDocumentLanguage`                | A+S     | Working |\n| `setLanguageConfiguration`               | A+S     | Working |\n| `createDiagnosticCollection`             | A+S     | Working |\n| `match`                                  | Pure    | Working |\n\n## Debug\n\n| Operation                                         | Primary | Status  |\n| ------------------------------------------------- | ------- | ------- |\n| `registerDebugConfigurationProvider`              | A       | Partial |\n| `registerDebugAdapterDescriptorFactory`           | A       | Partial |\n| `registerDebugAdapterTrackerFactory`              | A       | Partial |\n| `startDebugging`                                  | A+B     | Stubbed |\n| `activeDebugSession` / `onDidStart/ChangeSession` | A       | Stubbed |\n| `addBreakpoints` / `removeBreakpoints`            | A       | Stubbed |\n| `activeStackItem`                                 | A       | Gap     |\n\n## Tasks\n\n| Operation                                            | Primary | Status  |\n| ---------------------------------------------------- | ------- | ------- |\n| `registerTaskProvider`                               | A       | Partial |\n| `fetchTasks`                                         | A       | Stubbed |\n| `executeTask`                                        | A+B     | Stubbed |\n| `taskExecutions` / `onDidStartTask` / `onDidEndTask` | A       | Stubbed |\n\n## SCM\n\n| Operation                     | Primary | Status  |\n| ----------------------------- | ------- | ------- |\n| `createSourceControl`         | A+S     | Partial |\n| `inputBox.value` read/write   | A       | Partial |\n| `$gitExec` (built-in git ext) | **B**   | Stubbed |\n\n## Environment\n\n| Operation                                                                                                  | Primary | Status  |\n| ---------------------------------------------------------------------------------------------------------- | ------- | ------- |\n| `appName` / `appRoot` / `uriScheme` / `language` / `shell` / `machineId` / `sessionId` / `isNewAppInstall` | A       | Working |\n| `clipboard.readText` / `writeText`                                                                         | B       | Working |\n| `openExternal`                                                                                             | B       | Working |\n| `asExternalUri`                                                                                            | A       | Gap     |\n| `remoteName` / `remoteAuthority`                                                                           | A       | Working |\n\n## Extensions\n\n| Operation          | Primary | Status  |\n| ------------------ | ------- | ------- |\n| `getExtension(id)` | A       | Working |\n| `all`              | A       | Working |\n| `onDidChange`      | A       | Working |\n\n## Authentication\n\n| Operation                        | Primary | Status  |\n| -------------------------------- | ------- | ------- |\n| `registerAuthenticationProvider` | A       | Partial |\n| `getSession`                     | A       | Partial |\n| `getAccounts`                    | A       | Partial |\n\n## Notebooks\n\n| Operation                                   | Primary | Status  |\n| ------------------------------------------- | ------- | ------- |\n| `createNotebookController`                  | A       | Stubbed |\n| `registerNotebookCellStatusBarItemProvider` | A       | Stubbed |\n| `registerNotebookSerializer`                | A       | Partial |\n\n## Tests\n\n| Operation                 | Primary | Status  |\n| ------------------------- | ------- | ------- |\n| `createTestController`    | A       | Partial |\n| Run profile / run request | A       | Stubbed |\n\n## Chat / LM\n\n| Operation                           | Primary | Status  |\n| ----------------------------------- | ------- | ------- |\n| `createChatParticipant`             | A       | Stubbed |\n| `registerLanguageModelChatProvider` | A       | Gap     |\n| `selectChatModels`                  | A       | Stubbed |\n\n## Localization\n\n| Operation         | Primary | Status  |\n| ----------------- | ------- | ------- |\n| `t(message, ...)` | Pure    | Working |\n| `bundle` / `uri`  | A       | Working |\n\n## Comments\n\n| Operation                 | Primary | Status  |\n| ------------------------- | ------- | ------- |\n| `createCommentController` | A       | Stubbed |\n\n---\n\n## Domain-Grouped Primary Assignment\n\n### Rust-native primary (Track B wins)\n\nHigh I/O, process, or syscall-bound operations where Node adds latency:\n\n- File system -- `workspace.fs.*`, scanner reads. Mountain owns `File::*` via\n  tokio.\n- Find files / text search -- `workspace.findFiles` / `findTextInFiles`.\n  Mountain's `globset` walk.\n- Terminal PTY -- Mountain-owned via `portable-pty`.\n- Git subprocess -- Mountain spawns `git` as a child process.\n- File watch -- `createFileSystemWatcher`. Mountain `notify` crate.\n- Native dialogs / clipboard / openExternal -- Tauri covers these.\n- Process spawn for debug adapters / task exec -- upcoming via `tokio::process`.\n\n### Node-stock primary (Track A wins)\n\nState-heavy or coordination-heavy APIs where stock VS Code handles nuance:\n\n- Command registry, language features, editor/document/selection, configuration,\n  messages/dialogs/quick-input, progress, secrets/storage/memento,\n  authentication, debug/tasks (stock core + Mountain process spawn), webviews,\n  notebooks, tree views.\n\n### Hybrid (per-operation split)\n\n- SCM -- stock for provider registration, Mountain-native for `$gitExec`.\n- Terminal shell integration -- shell events via extHost, PTY via Mountain.\n- FileSystemProvider -- Cocoon forwards reads via gRPC to Mountain for disk ops.\n- Status bar -- stock manages entry lifecycle, Sky renders via\n  `__CEL_SERVICES__.Statusbar`.\n\n---\n\n## Highest-Priority Gaps\n\n- `vscode.debug.*` end-to-end (startDebugging, sessions, breakpoints) -- blocks\n  every language debugger.\n- `vscode.tasks.executeTask` -- blocks Jake / Gulp / Grunt / npm task providers.\n- `vscode.window.createWebviewPanel` -- blocks GitLens graph panel, Copilot\n  chat, markdown-preview internals.\n- `vscode.scm` viewlet route into `ISCMService` -- extensions register but UI\n  stays empty.\n- Tree-view renderer wiring -- **landed**: extension-registered tree views\n  render via `__CEL_SERVICES__.TreeViewByViewId(id).dataProvider`.\n- `localGit` channel -- **landed**.\n\n---\n\n## See Also\n\n- [Extension Development](https://Editor.Land/Doc/extension-development)\n- [Cocoon: Extension Host](https://Editor.Land/Doc/cocoon)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)", "Source/Content/doc/vscode-api-coverage.md", "c24674d5f5f917e5", { html: 2353, metadata: 2354 }, '<h1 id="vs-code-api-coverage">VS Code API Coverage</h1>\n<p>Authoritative map of every top-level <code>vscode.*</code> API surface and its\nimplementation split across <strong>Sky</strong> (workbench renderer), <strong>Cocoon</strong>\n(extension-host Node sidecar), and <strong>Mountain</strong> (Rust backend).</p>\n<hr>\n<h2 id="dual-track-strategy">Dual-Track Strategy</h2>\n<ul>\n<li>\n<p><strong>Track A - Stock-Node</strong>: Cocoon loads stock VS Code <code>extHost*.ts</code> sources\nunchanged. The workbench’s <code>mainThread*.ts</code> runs inside Sky. The\nExtHostContext/MainContext RPC glue is provided by Cocoon’s shim alongside\nstock extHost code. Maximum compatibility from day one for any API the stock\nimplementation handles in-process.</p>\n</li>\n<li>\n<p><strong>Track B - Rust-native</strong>: Mountain owns the backend. Cocoon’s <code>vscode</code> shim\nsends gRPC to Mountain which does the work natively (filesystem, process,\nterminal, search, git). Faster and safer than bouncing through Node for\nI/O-heavy APIs.</p>\n</li>\n</ul>\n<p>The two tracks are not mutually exclusive. Most APIs live on Track A by default,\nand we promote individual operations to Track B when a measured benefit exists.\nThe Cocoon <code>&#x3C;Namespace>Route.ts</code> tier router decides per-call.</p>\n<hr>\n<h2 id="legend">Legend</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Track</th><th>Meaning</th></tr></thead><tbody><tr><td><strong>A</strong></td><td>Stock-Node - lift unchanged <code>extHost*.ts</code> into Cocoon</td></tr><tr><td><strong>B</strong></td><td>Rust-native - Mountain owns backend, gRPC from Cocoon</td></tr><tr><td><strong>C</strong></td><td>Cocoon-bespoke - hand-rolled TS in Land (last resort)</td></tr><tr><td><strong>S</strong></td><td>Sky-direct - call via <code>__CEL_SERVICES__.*</code> workbench accessor</td></tr></tbody></table>\n<p>Status symbols:</p>\n<ul>\n<li><strong>Working</strong> - end-to-end (activation + feature render path confirmed)</li>\n<li><strong>Partial</strong> - RPC wired but UI render gap, or missing sub-method</li>\n<li><strong>Stubbed</strong> - registration accepted, no effect</li>\n<li><strong>Not attempted</strong></li>\n<li><strong>Pure lift</strong> - stateless, no RPC, already landed via <code>StockLift.ts</code></li>\n</ul>\n<hr>\n<h2 id="commands">Commands</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>registerCommand</code></td><td>A+S</td><td>Working</td></tr><tr><td><code>registerTextEditorCommand</code></td><td>A</td><td>Working</td></tr><tr><td><code>executeCommand</code></td><td>A+S</td><td>Working</td></tr><tr><td><code>getCommands</code></td><td>A</td><td>Working</td></tr></tbody></table>\n<h2 id="window---editors">Window - Editors</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>activeTextEditor</code></td><td>A</td><td>Working</td></tr><tr><td><code>visibleTextEditors</code></td><td>A</td><td>Working</td></tr><tr><td><code>showTextDocument</code></td><td>A</td><td>Working</td></tr><tr><td><code>onDidChangeActiveTextEditor</code></td><td>A</td><td>Working</td></tr></tbody></table>\n<h2 id="window---surfaces">Window - Surfaces</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>createStatusBarItem</code></td><td>S (native)</td><td>Working</td></tr><tr><td><code>setStatusBarMessage</code></td><td>S</td><td>Working</td></tr><tr><td><code>createTreeView</code></td><td>A+S</td><td>Working</td></tr><tr><td><code>registerTreeDataProvider</code></td><td>A+S</td><td>Working</td></tr><tr><td><code>createWebviewPanel</code></td><td>A</td><td>Stubbed</td></tr><tr><td><code>registerWebviewViewProvider</code></td><td>A</td><td>Stubbed</td></tr><tr><td><code>registerCustomEditorProvider</code></td><td>A</td><td>Stubbed</td></tr><tr><td><code>createTerminal</code></td><td>B</td><td>Partial</td></tr><tr><td><code>onDidOpen/CloseTerminal</code></td><td>B</td><td>Partial</td></tr><tr><td><code>createOutputChannel</code></td><td>S</td><td>Working</td></tr><tr><td><code>showInformationMessage</code> / <code>showWarningMessage</code> / <code>showErrorMessage</code></td><td>A</td><td>Working</td></tr><tr><td><code>showQuickPick</code> / <code>createQuickPick</code></td><td>A</td><td>Partial</td></tr><tr><td><code>showInputBox</code></td><td>A</td><td>Partial</td></tr><tr><td><code>showOpenDialog</code> / <code>showSaveDialog</code></td><td>B</td><td>Working</td></tr><tr><td><code>withProgress</code></td><td>A+S</td><td>Partial</td></tr><tr><td><code>registerFileDecorationProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerUriHandler</code></td><td>A</td><td>Partial</td></tr><tr><td><code>onDidChangeWindowState</code></td><td>A</td><td>Working</td></tr><tr><td><code>showNotebookDocument</code></td><td>A</td><td>Stubbed</td></tr></tbody></table>\n<h2 id="workspace">Workspace</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>workspaceFolders</code></td><td>A</td><td>Working</td></tr><tr><td><code>onDidChangeWorkspaceFolders</code></td><td>A</td><td>Working</td></tr><tr><td><code>getWorkspaceFolder</code></td><td>A+B</td><td>Working</td></tr><tr><td><code>textDocuments</code></td><td>A</td><td>Working</td></tr><tr><td><code>openTextDocument</code></td><td>A+B</td><td>Working</td></tr><tr><td><code>onDidOpen/Close/SaveTextDocument</code></td><td>A</td><td>Working</td></tr><tr><td><code>onWillSaveTextDocument</code></td><td>A</td><td>Partial</td></tr><tr><td><code>applyEdit</code></td><td>A+S</td><td>Partial</td></tr><tr><td><code>save</code> / <code>saveAs</code></td><td>A</td><td>Gap</td></tr><tr><td><code>fs.readFile</code> / <code>writeFile</code> / <code>stat</code> / <code>readDirectory</code> / <code>createDirectory</code> / <code>delete</code> / <code>rename</code> / <code>copy</code></td><td><strong>B</strong></td><td>Working</td></tr><tr><td><code>fs.isWritableFileSystem</code></td><td>A</td><td>Working</td></tr><tr><td><code>registerFileSystemProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>createFileSystemWatcher</code></td><td>B</td><td>Partial</td></tr><tr><td><code>findFiles</code></td><td><strong>B</strong></td><td>Working</td></tr><tr><td><code>findTextInFiles</code></td><td><strong>B</strong></td><td>Working</td></tr><tr><td><code>registerTextDocumentContentProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>getConfiguration</code> / <code>onDidChangeConfiguration</code></td><td>A+B</td><td>Working</td></tr><tr><td><code>registerTaskProvider</code></td><td>A</td><td>Partial</td></tr></tbody></table>\n<h2 id="languages">Languages</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>registerCompletionItemProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerHoverProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerDefinitionProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerReferenceProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerDocumentSymbolProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerCodeActionsProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerCodeLensProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerDocumentFormattingEditProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerRenameProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerInlayHintsProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerFoldingRangeProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerSemanticTokensProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerSignatureHelpProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>setTextDocumentLanguage</code></td><td>A+S</td><td>Working</td></tr><tr><td><code>setLanguageConfiguration</code></td><td>A+S</td><td>Working</td></tr><tr><td><code>createDiagnosticCollection</code></td><td>A+S</td><td>Working</td></tr><tr><td><code>match</code></td><td>Pure</td><td>Working</td></tr></tbody></table>\n<h2 id="debug">Debug</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>registerDebugConfigurationProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerDebugAdapterDescriptorFactory</code></td><td>A</td><td>Partial</td></tr><tr><td><code>registerDebugAdapterTrackerFactory</code></td><td>A</td><td>Partial</td></tr><tr><td><code>startDebugging</code></td><td>A+B</td><td>Stubbed</td></tr><tr><td><code>activeDebugSession</code> / <code>onDidStart/ChangeSession</code></td><td>A</td><td>Stubbed</td></tr><tr><td><code>addBreakpoints</code> / <code>removeBreakpoints</code></td><td>A</td><td>Stubbed</td></tr><tr><td><code>activeStackItem</code></td><td>A</td><td>Gap</td></tr></tbody></table>\n<h2 id="tasks">Tasks</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>registerTaskProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>fetchTasks</code></td><td>A</td><td>Stubbed</td></tr><tr><td><code>executeTask</code></td><td>A+B</td><td>Stubbed</td></tr><tr><td><code>taskExecutions</code> / <code>onDidStartTask</code> / <code>onDidEndTask</code></td><td>A</td><td>Stubbed</td></tr></tbody></table>\n<h2 id="scm">SCM</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>createSourceControl</code></td><td>A+S</td><td>Partial</td></tr><tr><td><code>inputBox.value</code> read/write</td><td>A</td><td>Partial</td></tr><tr><td><code>$gitExec</code> (built-in git ext)</td><td><strong>B</strong></td><td>Stubbed</td></tr></tbody></table>\n<h2 id="environment">Environment</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>appName</code> / <code>appRoot</code> / <code>uriScheme</code> / <code>language</code> / <code>shell</code> / <code>machineId</code> / <code>sessionId</code> / <code>isNewAppInstall</code></td><td>A</td><td>Working</td></tr><tr><td><code>clipboard.readText</code> / <code>writeText</code></td><td>B</td><td>Working</td></tr><tr><td><code>openExternal</code></td><td>B</td><td>Working</td></tr><tr><td><code>asExternalUri</code></td><td>A</td><td>Gap</td></tr><tr><td><code>remoteName</code> / <code>remoteAuthority</code></td><td>A</td><td>Working</td></tr></tbody></table>\n<h2 id="extensions">Extensions</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>getExtension(id)</code></td><td>A</td><td>Working</td></tr><tr><td><code>all</code></td><td>A</td><td>Working</td></tr><tr><td><code>onDidChange</code></td><td>A</td><td>Working</td></tr></tbody></table>\n<h2 id="authentication">Authentication</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>registerAuthenticationProvider</code></td><td>A</td><td>Partial</td></tr><tr><td><code>getSession</code></td><td>A</td><td>Partial</td></tr><tr><td><code>getAccounts</code></td><td>A</td><td>Partial</td></tr></tbody></table>\n<h2 id="notebooks">Notebooks</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>createNotebookController</code></td><td>A</td><td>Stubbed</td></tr><tr><td><code>registerNotebookCellStatusBarItemProvider</code></td><td>A</td><td>Stubbed</td></tr><tr><td><code>registerNotebookSerializer</code></td><td>A</td><td>Partial</td></tr></tbody></table>\n<h2 id="tests">Tests</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>createTestController</code></td><td>A</td><td>Partial</td></tr><tr><td>Run profile / run request</td><td>A</td><td>Stubbed</td></tr></tbody></table>\n<h2 id="chat--lm">Chat / LM</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>createChatParticipant</code></td><td>A</td><td>Stubbed</td></tr><tr><td><code>registerLanguageModelChatProvider</code></td><td>A</td><td>Gap</td></tr><tr><td><code>selectChatModels</code></td><td>A</td><td>Stubbed</td></tr></tbody></table>\n<h2 id="localization">Localization</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>t(message, ...)</code></td><td>Pure</td><td>Working</td></tr><tr><td><code>bundle</code> / <code>uri</code></td><td>A</td><td>Working</td></tr></tbody></table>\n<h2 id="comments">Comments</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Operation</th><th>Primary</th><th>Status</th></tr></thead><tbody><tr><td><code>createCommentController</code></td><td>A</td><td>Stubbed</td></tr></tbody></table>\n<hr>\n<h2 id="domain-grouped-primary-assignment">Domain-Grouped Primary Assignment</h2>\n<h3 id="rust-native-primary-track-b-wins">Rust-native primary (Track B wins)</h3>\n<p>High I/O, process, or syscall-bound operations where Node adds latency:</p>\n<ul>\n<li>File system — <code>workspace.fs.*</code>, scanner reads. Mountain owns <code>File::*</code> via\ntokio.</li>\n<li>Find files / text search — <code>workspace.findFiles</code> / <code>findTextInFiles</code>.\nMountain’s <code>globset</code> walk.</li>\n<li>Terminal PTY — Mountain-owned via <code>portable-pty</code>.</li>\n<li>Git subprocess — Mountain spawns <code>git</code> as a child process.</li>\n<li>File watch — <code>createFileSystemWatcher</code>. Mountain <code>notify</code> crate.</li>\n<li>Native dialogs / clipboard / openExternal — Tauri covers these.</li>\n<li>Process spawn for debug adapters / task exec — upcoming via <code>tokio::process</code>.</li>\n</ul>\n<h3 id="node-stock-primary-track-a-wins">Node-stock primary (Track A wins)</h3>\n<p>State-heavy or coordination-heavy APIs where stock VS Code handles nuance:</p>\n<ul>\n<li>Command registry, language features, editor/document/selection, configuration,\nmessages/dialogs/quick-input, progress, secrets/storage/memento,\nauthentication, debug/tasks (stock core + Mountain process spawn), webviews,\nnotebooks, tree views.</li>\n</ul>\n<h3 id="hybrid-per-operation-split">Hybrid (per-operation split)</h3>\n<ul>\n<li>SCM — stock for provider registration, Mountain-native for <code>$gitExec</code>.</li>\n<li>Terminal shell integration — shell events via extHost, PTY via Mountain.</li>\n<li>FileSystemProvider — Cocoon forwards reads via gRPC to Mountain for disk ops.</li>\n<li>Status bar — stock manages entry lifecycle, Sky renders via\n<code>__CEL_SERVICES__.Statusbar</code>.</li>\n</ul>\n<hr>\n<h2 id="highest-priority-gaps">Highest-Priority Gaps</h2>\n<ul>\n<li><code>vscode.debug.*</code> end-to-end (startDebugging, sessions, breakpoints) — blocks\nevery language debugger.</li>\n<li><code>vscode.tasks.executeTask</code> — blocks Jake / Gulp / Grunt / npm task providers.</li>\n<li><code>vscode.window.createWebviewPanel</code> — blocks GitLens graph panel, Copilot\nchat, markdown-preview internals.</li>\n<li><code>vscode.scm</code> viewlet route into <code>ISCMService</code> — extensions register but UI\nstays empty.</li>\n<li>Tree-view renderer wiring — <strong>landed</strong>: extension-registered tree views\nrender via <code>__CEL_SERVICES__.TreeViewByViewId(id).dataProvider</code>.</li>\n<li><code>localGit</code> channel — <strong>landed</strong>.</li>\n</ul>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/extension-development">Extension Development</a></li>\n<li><a href="https://Editor.Land/Doc/cocoon">Cocoon: Extension Host</a></li>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n</ul>', { headings: 2355, localImagePaths: 2420, remoteImagePaths: 2421, frontmatter: 2422, imagePaths: 2423 }, [2356, 2358, 2361, 2364, 2365, 2368, 2371, 2374, 2377, 2380, 2381, 2384, 2387, 2388, 2389, 2392, 2395, 2398, 2401, 2404, 2407, 2410, 2413, 2416, 2419], { depth: 25, slug: 2357, text: 2347 }, "vs-code-api-coverage", { depth: 29, slug: 2359, text: 2360 }, "dual-track-strategy", "Dual-Track Strategy", { depth: 29, slug: 2362, text: 2363 }, "legend", "Legend", { depth: 29, slug: 425, text: 426 }, { depth: 29, slug: 2366, text: 2367 }, "window---editors", "Window - Editors", { depth: 29, slug: 2369, text: 2370 }, "window---surfaces", "Window - Surfaces", { depth: 29, slug: 2372, text: 2373 }, "workspace", "Workspace", { depth: 29, slug: 2375, text: 2376 }, "languages", "Languages", { depth: 29, slug: 2378, text: 2379 }, "debug", "Debug", { depth: 29, slug: 2025, text: 2026 }, { depth: 29, slug: 2382, text: 2383 }, "scm", "SCM", { depth: 29, slug: 2385, text: 2386 }, "environment", "Environment", { depth: 29, slug: 2017, text: 192 }, { depth: 29, slug: 1777, text: 1778 }, { depth: 29, slug: 2390, text: 2391 }, "notebooks", "Notebooks", { depth: 29, slug: 2393, text: 2394 }, "tests", "Tests", { depth: 29, slug: 2396, text: 2397 }, "chat--lm", "Chat / LM", { depth: 29, slug: 2399, text: 2400 }, "localization", "Localization", { depth: 29, slug: 2402, text: 2403 }, "comments", "Comments", { depth: 29, slug: 2405, text: 2406 }, "domain-grouped-primary-assignment", "Domain-Grouped Primary Assignment", { depth: 347, slug: 2408, text: 2409 }, "rust-native-primary-track-b-wins", "Rust-native primary (Track B wins)", { depth: 347, slug: 2411, text: 2412 }, "node-stock-primary-track-a-wins", "Node-stock primary (Track A wins)", { depth: 347, slug: 2414, text: 2415 }, "hybrid-per-operation-split", "Hybrid (per-operation split)", { depth: 29, slug: 2417, text: 2418 }, "highest-priority-gaps", "Highest-Priority Gaps", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2347, section: 493, order: 16, description: 2348 }, [], "why-cc0", { id: 2424, data: 2426, body: 2430, filePath: 2431, digest: 2432, rendered: 2433 }, { title: 2427, section: 2428, order: 972, description: 2429 }, "Why CC0", "License", "Public domain dedication: fork it, ship it, sell it, no restrictions.", "# Why CC0\n\n`Editor.Land` is released under CC0 1.0 Universal, a public domain dedication.\nThis is the most permissive option available. There are no conditions on use,\nmodification, distribution, or commercialization. No attribution is required. No\nlicense notice must be preserved. No patent clauses apply. The code belongs to\neveryone.\n\n---\n\n## What CC0 Means in Practice\n\nCC0 is not a license in the traditional sense. It is a waiver. The author\nirrevocably surrenders all copyright and related rights to the fullest extent\npermitted by law. In jurisdictions where full waiver is not legally possible,\nCC0 includes a fallback permissive license that grants all rights\nunconditionally.\n\nFor users, this means:\n\n- You can fork Land and sell it under any name.\n- You can embed Land's code in proprietary products.\n- You can modify Land without publishing your changes.\n- You can use Land in government, military, healthcare, or any regulated\n  environment without legal review of license compatibility.\n- You do not need to include a copyright notice, license file, or attribution of\n  any kind.\n\n---\n\n## Why Not MIT\n\nMIT is permissive, but it still requires attribution. Every binary that includes\nMIT-licensed code must ship a copy of the license text and the original\ncopyright notice. For a project with hundreds of dependencies, this creates a\ncascade of `NOTICES` files and legal compliance work. CC0 eliminates that burden\nentirely.\n\n---\n\n## Why Not Apache 2.0\n\nApache 2.0 includes an explicit patent grant, which sounds protective but\ncreates complexity. The patent clause interacts with contributor agreements, and\nthe license's termination provisions can trigger in ways that surprise\ndownstream users. CC0's simplicity avoids these entanglements. There are no\npatent clauses to analyze, no contributor license agreements to negotiate, and\nno termination conditions to track.\n\n---\n\n## License Compatibility\n\nCC0 is compatible with every license in existence. MIT, Apache, GPL, LGPL, MPL,\nBSD, proprietary, and everything else. CC0 code can be included in any project\nwithout creating a license conflict. This is particularly valuable for Land's\nelements, which are designed to be reusable. Any developer can extract a crate\nor package from Land and use it in their own project without checking a\ncompatibility matrix.\n\n---\n\n## How NLnet Makes This Possible\n\nReleasing a large project under CC0 is financially possible because Land is\nfunded by the NLnet NGI0 Commons Fund, part of the European Commission's Next\nGeneration Internet initiative. NLnet's mission is to fund software that belongs\nto the public. CC0 is the natural expression of that mission. The funding model\nreplaces the need for restrictive licensing as a revenue protection mechanism.\n\n---\n\n## Where CC0 Applies\n\nCC0 covers the entire Land project: all 16 elements, the website, the\ndocumentation, the build tooling, and the CI/CD infrastructure. Every repository\nunder the CodeEditorLand GitHub organization carries the same dedication. There\nare no dual-licensed components, no \"community edition\" restrictions, and no\nfeatures gated behind a proprietary license.\n\n---\n\n## The Philosophical Position\n\nSoftware tools should not restrict the people who use them. A code editor is\ninfrastructure. It is a means to an end, not an end in itself. Placing\ninfrastructure in the public domain ensures that it remains available to\neveryone, permanently, without conditions. That is the purpose of CC0, and it is\nthe reason Land uses it.\n\n---\n\n## See Also\n\n- [Contributing](https://Editor.Land/Doc/contributing)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)", "Source/Content/doc/why-cc0.md", "fe914a83ab35cf37", { html: 2434, metadata: 2435 }, '<h1 id="why-cc0">Why CC0</h1>\n<p><code>Editor.Land</code> is released under CC0 1.0 Universal, a public domain dedication.\nThis is the most permissive option available. There are no conditions on use,\nmodification, distribution, or commercialization. No attribution is required. No\nlicense notice must be preserved. No patent clauses apply. The code belongs to\neveryone.</p>\n<hr>\n<h2 id="what-cc0-means-in-practice">What CC0 Means in Practice</h2>\n<p>CC0 is not a license in the traditional sense. It is a waiver. The author\nirrevocably surrenders all copyright and related rights to the fullest extent\npermitted by law. In jurisdictions where full waiver is not legally possible,\nCC0 includes a fallback permissive license that grants all rights\nunconditionally.</p>\n<p>For users, this means:</p>\n<ul>\n<li>You can fork Land and sell it under any name.</li>\n<li>You can embed Land’s code in proprietary products.</li>\n<li>You can modify Land without publishing your changes.</li>\n<li>You can use Land in government, military, healthcare, or any regulated\nenvironment without legal review of license compatibility.</li>\n<li>You do not need to include a copyright notice, license file, or attribution of\nany kind.</li>\n</ul>\n<hr>\n<h2 id="why-not-mit">Why Not MIT</h2>\n<p>MIT is permissive, but it still requires attribution. Every binary that includes\nMIT-licensed code must ship a copy of the license text and the original\ncopyright notice. For a project with hundreds of dependencies, this creates a\ncascade of <code>NOTICES</code> files and legal compliance work. CC0 eliminates that burden\nentirely.</p>\n<hr>\n<h2 id="why-not-apache-20">Why Not Apache 2.0</h2>\n<p>Apache 2.0 includes an explicit patent grant, which sounds protective but\ncreates complexity. The patent clause interacts with contributor agreements, and\nthe license’s termination provisions can trigger in ways that surprise\ndownstream users. CC0’s simplicity avoids these entanglements. There are no\npatent clauses to analyze, no contributor license agreements to negotiate, and\nno termination conditions to track.</p>\n<hr>\n<h2 id="license-compatibility">License Compatibility</h2>\n<p>CC0 is compatible with every license in existence. MIT, Apache, GPL, LGPL, MPL,\nBSD, proprietary, and everything else. CC0 code can be included in any project\nwithout creating a license conflict. This is particularly valuable for Land’s\nelements, which are designed to be reusable. Any developer can extract a crate\nor package from Land and use it in their own project without checking a\ncompatibility matrix.</p>\n<hr>\n<h2 id="how-nlnet-makes-this-possible">How NLnet Makes This Possible</h2>\n<p>Releasing a large project under CC0 is financially possible because Land is\nfunded by the NLnet NGI0 Commons Fund, part of the European Commission’s Next\nGeneration Internet initiative. NLnet’s mission is to fund software that belongs\nto the public. CC0 is the natural expression of that mission. The funding model\nreplaces the need for restrictive licensing as a revenue protection mechanism.</p>\n<hr>\n<h2 id="where-cc0-applies">Where CC0 Applies</h2>\n<p>CC0 covers the entire Land project: all 16 elements, the website, the\ndocumentation, the build tooling, and the CI/CD infrastructure. Every repository\nunder the CodeEditorLand GitHub organization carries the same dedication. There\nare no dual-licensed components, no “community edition” restrictions, and no\nfeatures gated behind a proprietary license.</p>\n<hr>\n<h2 id="the-philosophical-position">The Philosophical Position</h2>\n<p>Software tools should not restrict the people who use them. A code editor is\ninfrastructure. It is a means to an end, not an end in itself. Placing\ninfrastructure in the public domain ensures that it remains available to\neveryone, permanently, without conditions. That is the purpose of CC0, and it is\nthe reason Land uses it.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/contributing">Contributing</a></li>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n</ul>', { headings: 2436, localImagePaths: 2458, remoteImagePaths: 2459, frontmatter: 2460, imagePaths: 2461 }, [2437, 2438, 2441, 2444, 2447, 2450, 2451, 2454, 2457], { depth: 25, slug: 2424, text: 2427 }, { depth: 29, slug: 2439, text: 2440 }, "what-cc0-means-in-practice", "What CC0 Means in Practice", { depth: 29, slug: 2442, text: 2443 }, "why-not-mit", "Why Not MIT", { depth: 29, slug: 2445, text: 2446 }, "why-not-apache-20", "Why Not Apache 2.0", { depth: 29, slug: 2448, text: 2449 }, "license-compatibility", "License Compatibility", { depth: 29, slug: 132, text: 133 }, { depth: 29, slug: 2452, text: 2453 }, "where-cc0-applies", "Where CC0 Applies", { depth: 29, slug: 2455, text: 2456 }, "the-philosophical-position", "The Philosophical Position", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2427, section: 2428, order: 972, description: 2429 }, [], { id: 161, data: 2463, body: 2468, filePath: 2469, digest: 2470, rendered: 2471 }, { title: 2464, section: 2465, order: 2466, description: 2467 }, "Why Effect-TS", "Technology", 28, "Typed errors, structured concurrency, and dependency injection for TypeScript.", "# Why Effect-TS\n\n`TypeScript`'s type system is powerful, but its concurrency model is not.\nPromises lose error types. Async functions cannot be interrupted. Dependency\ninjection requires external frameworks. `Effect-TS` solves all three problems\nwithin the type system itself, and that is why Land uses it for its `TypeScript`\nextension host and `workbench` layers.\n\n---\n\n## Typed Error Channels\n\nA standard `TypeScript` `Promise<string>` tells you the success type but says\nnothing about what can go wrong. The error is `unknown`. Every caller must cast,\nguess, or ignore it. `Effect-TS` encodes errors into the type signature:\n`Effect<string, ParseError | NetworkError, never>`. The compiler tracks which\nerrors are possible, which have been handled, and which still need attention. If\na new error variant is added, every call site that does not handle it becomes a\ncompile error.\n\n---\n\n## Fiber-Based Structured Concurrency\n\nJavaScript's `async/await` runs tasks on a single event loop with no built-in\nmechanism for cancellation, timeouts, or supervision. `Effect-TS` introduces\nfibers: lightweight, schedulable units of work that support interruption,\nracing, forking, and joining. A parent fiber can spawn children, and if the\nparent is interrupted, all children are interrupted automatically. This is\nstructured concurrency. It prevents resource leaks, orphaned tasks, and\nunhandled background failures.\n\n---\n\n## Interruption and Timeouts\n\nIn standard `TypeScript`, aborting an in-flight operation requires manual\n`AbortController` wiring at every level of the call stack. `Effect-TS` makes\ninterruption a first-class concept. Any effect can be wrapped with a timeout,\nand the runtime handles cancellation cleanly. When a user closes a tab or\ncancels a search, the associated fibers terminate immediately without leaving\ndangling resources.\n\n---\n\n## Layer-Based Dependency Injection\n\n`Effect-TS` provides a Layer system for dependency injection that is checked at\ncompile time. Each service declares its dependencies as part of its type\nsignature. Layers compose into a dependency graph, and the compiler verifies\nthat every dependency is satisfied before the program runs. There are no runtime\nsurprises from missing bindings, no service locator patterns, and no\nregistration order bugs.\n\n---\n\n## Where Effect-TS Appears in Land\n\nTwo elements rely on `Effect-TS` as their concurrency and composition backbone:\n\n- **`Cocoon`** is the `VS Code` extension host. It runs third-party extensions\n  in isolated fibers, manages their lifecycles, and coordinates communication\n  with the native backend. `Effect-TS` gives `Cocoon` typed error handling for\n  extension activation failures, structured concurrency for parallel extension\n  loading, and clean shutdown when extensions misbehave.\n\n- **`Wind`** is the `workbench` layer. It manages editor state, view containers,\n  panel layouts, and command dispatch. `Effect-TS` provides `Wind` with a\n  composable service architecture where each UI subsystem declares its\n  dependencies and the runtime wires them together.\n\n---\n\n## Why Not Raw Promises\n\nPromises work well for simple request-response flows. They break down when you\nneed cancellation, supervision, typed errors, retries with backoff, or resource\nfinalization. An extension host must handle all of these. Building that\ninfrastructure on raw promises would mean reimplementing what `Effect-TS`\nalready provides - tested, documented, and type-safe.\n\n---\n\n## See Also\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Cocoon`: Extension Host](https://Editor.Land/Doc/cocoon)\n- [Why `Rust`](https://Editor.Land/Doc/why-rust)", "Source/Content/doc/why-effect-ts.md", "2ef90e0a0d5a25f5", { html: 2472, metadata: 2473 }, '<h1 id="why-effect-ts">Why Effect-TS</h1>\n<p><code>TypeScript</code>’s type system is powerful, but its concurrency model is not.\nPromises lose error types. Async functions cannot be interrupted. Dependency\ninjection requires external frameworks. <code>Effect-TS</code> solves all three problems\nwithin the type system itself, and that is why Land uses it for its <code>TypeScript</code>\nextension host and <code>workbench</code> layers.</p>\n<hr>\n<h2 id="typed-error-channels">Typed Error Channels</h2>\n<p>A standard <code>TypeScript</code> <code>Promise&#x3C;string></code> tells you the success type but says\nnothing about what can go wrong. The error is <code>unknown</code>. Every caller must cast,\nguess, or ignore it. <code>Effect-TS</code> encodes errors into the type signature:\n<code>Effect&#x3C;string, ParseError | NetworkError, never></code>. The compiler tracks which\nerrors are possible, which have been handled, and which still need attention. If\na new error variant is added, every call site that does not handle it becomes a\ncompile error.</p>\n<hr>\n<h2 id="fiber-based-structured-concurrency">Fiber-Based Structured Concurrency</h2>\n<p>JavaScript’s <code>async/await</code> runs tasks on a single event loop with no built-in\nmechanism for cancellation, timeouts, or supervision. <code>Effect-TS</code> introduces\nfibers: lightweight, schedulable units of work that support interruption,\nracing, forking, and joining. A parent fiber can spawn children, and if the\nparent is interrupted, all children are interrupted automatically. This is\nstructured concurrency. It prevents resource leaks, orphaned tasks, and\nunhandled background failures.</p>\n<hr>\n<h2 id="interruption-and-timeouts">Interruption and Timeouts</h2>\n<p>In standard <code>TypeScript</code>, aborting an in-flight operation requires manual\n<code>AbortController</code> wiring at every level of the call stack. <code>Effect-TS</code> makes\ninterruption a first-class concept. Any effect can be wrapped with a timeout,\nand the runtime handles cancellation cleanly. When a user closes a tab or\ncancels a search, the associated fibers terminate immediately without leaving\ndangling resources.</p>\n<hr>\n<h2 id="layer-based-dependency-injection">Layer-Based Dependency Injection</h2>\n<p><code>Effect-TS</code> provides a Layer system for dependency injection that is checked at\ncompile time. Each service declares its dependencies as part of its type\nsignature. Layers compose into a dependency graph, and the compiler verifies\nthat every dependency is satisfied before the program runs. There are no runtime\nsurprises from missing bindings, no service locator patterns, and no\nregistration order bugs.</p>\n<hr>\n<h2 id="where-effect-ts-appears-in-land">Where Effect-TS Appears in Land</h2>\n<p>Two elements rely on <code>Effect-TS</code> as their concurrency and composition backbone:</p>\n<ul>\n<li>\n<p><strong><code>Cocoon</code></strong> is the <code>VS Code</code> extension host. It runs third-party extensions\nin isolated fibers, manages their lifecycles, and coordinates communication\nwith the native backend. <code>Effect-TS</code> gives <code>Cocoon</code> typed error handling for\nextension activation failures, structured concurrency for parallel extension\nloading, and clean shutdown when extensions misbehave.</p>\n</li>\n<li>\n<p><strong><code>Wind</code></strong> is the <code>workbench</code> layer. It manages editor state, view containers,\npanel layouts, and command dispatch. <code>Effect-TS</code> provides <code>Wind</code> with a\ncomposable service architecture where each UI subsystem declares its\ndependencies and the runtime wires them together.</p>\n</li>\n</ul>\n<hr>\n<h2 id="why-not-raw-promises">Why Not Raw Promises</h2>\n<p>Promises work well for simple request-response flows. They break down when you\nneed cancellation, supervision, typed errors, retries with backoff, or resource\nfinalization. An extension host must handle all of these. Building that\ninfrastructure on raw promises would mean reimplementing what <code>Effect-TS</code>\nalready provides - tested, documented, and type-safe.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/cocoon"><code>Cocoon</code>: Extension Host</a></li>\n<li><a href="https://Editor.Land/Doc/why-rust">Why <code>Rust</code></a></li>\n</ul>', { headings: 2474, localImagePaths: 2495, remoteImagePaths: 2496, frontmatter: 2497, imagePaths: 2498 }, [2475, 2476, 2479, 2482, 2485, 2488, 2491, 2494], { depth: 25, slug: 161, text: 2464 }, { depth: 29, slug: 2477, text: 2478 }, "typed-error-channels", "Typed Error Channels", { depth: 29, slug: 2480, text: 2481 }, "fiber-based-structured-concurrency", "Fiber-Based Structured Concurrency", { depth: 29, slug: 2483, text: 2484 }, "interruption-and-timeouts", "Interruption and Timeouts", { depth: 29, slug: 2486, text: 2487 }, "layer-based-dependency-injection", "Layer-Based Dependency Injection", { depth: 29, slug: 2489, text: 2490 }, "where-effect-ts-appears-in-land", "Where Effect-TS Appears in Land", { depth: 29, slug: 2492, text: 2493 }, "why-not-raw-promises", "Why Not Raw Promises", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2464, section: 2465, order: 2466, description: 2467 }, [], "why-grpc", { id: 2499, data: 2501, body: 2505, filePath: 2506, digest: 2507, rendered: 2508 }, { title: 2502, section: 2465, order: 2503, description: 2504 }, "Why gRPC", 29, "Typed contracts at the wire: one schema for Rust and TypeScript callers.", "# Why gRPC\n\nLand uses Protocol Buffers and `gRPC` because the editor is split across\ncooperating elements. `Mountain`, `Cocoon`, `Air`, `Grove`, and other services\nneed shared message contracts that can be generated into `Rust` and\n`TypeScript`.\n\nThe important claim is not a latency slogan. The important claim is that a route\nstarts from a `.proto` contract, generated code, and a named service boundary.\n\n---\n\n## Protocol Buffer Schema\n\nEvery `Vine` message is defined in a `.proto` file. The schema specifies field\nnames, types, and service methods. Generated `Rust` and `TypeScript` code keeps\nboth sides aligned when implemented routes change.\n\n---\n\n## Generated Stubs\n\n`Rust` uses tonic and prost. `TypeScript` uses generated stubs for the `Cocoon`\nand frontend side. This avoids hand-written `JSON` contracts for critical `IPC`\npaths.\n\n---\n\n## Streaming And Cancellation\n\n`gRPC` gives Land unary requests, notifications, cancellation, and streaming\nroutes under one schema model. The current source includes bidirectional\n`Mountain` and `Cocoon` channels, plus `Air` and `Grove` protocol definitions.\n\n---\n\n## Where gRPC Appears\n\n`Vine` owns the protocol layer. `Mountain` exposes and consumes `gRPC` services.\n`Cocoon` runs a `gRPC` server and client for extension-host work. `Air` and\n`Grove` also define service contracts. Not every internal call goes through\n`gRPC`, so public copy should name the route instead of saying all communication\nuses one channel.\n\n---\n\n## See Also\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [Why `Rust`](https://Editor.Land/Doc/why-rust)\n- [Why `Tauri`](https://Editor.Land/Doc/why-tauri)\n- [`API` Reference](https://Editor.Land/Doc/api-reference)", "Source/Content/doc/why-grpc.md", "87111458ed50e2a7", { html: 2509, metadata: 2510 }, '<h1 id="why-grpc">Why gRPC</h1>\n<p>Land uses Protocol Buffers and <code>gRPC</code> because the editor is split across\ncooperating elements. <code>Mountain</code>, <code>Cocoon</code>, <code>Air</code>, <code>Grove</code>, and other services\nneed shared message contracts that can be generated into <code>Rust</code> and\n<code>TypeScript</code>.</p>\n<p>The important claim is not a latency slogan. The important claim is that a route\nstarts from a <code>.proto</code> contract, generated code, and a named service boundary.</p>\n<hr>\n<h2 id="protocol-buffer-schema">Protocol Buffer Schema</h2>\n<p>Every <code>Vine</code> message is defined in a <code>.proto</code> file. The schema specifies field\nnames, types, and service methods. Generated <code>Rust</code> and <code>TypeScript</code> code keeps\nboth sides aligned when implemented routes change.</p>\n<hr>\n<h2 id="generated-stubs">Generated Stubs</h2>\n<p><code>Rust</code> uses tonic and prost. <code>TypeScript</code> uses generated stubs for the <code>Cocoon</code>\nand frontend side. This avoids hand-written <code>JSON</code> contracts for critical <code>IPC</code>\npaths.</p>\n<hr>\n<h2 id="streaming-and-cancellation">Streaming And Cancellation</h2>\n<p><code>gRPC</code> gives Land unary requests, notifications, cancellation, and streaming\nroutes under one schema model. The current source includes bidirectional\n<code>Mountain</code> and <code>Cocoon</code> channels, plus <code>Air</code> and <code>Grove</code> protocol definitions.</p>\n<hr>\n<h2 id="where-grpc-appears">Where gRPC Appears</h2>\n<p><code>Vine</code> owns the protocol layer. <code>Mountain</code> exposes and consumes <code>gRPC</code> services.\n<code>Cocoon</code> runs a <code>gRPC</code> server and client for extension-host work. <code>Air</code> and\n<code>Grove</code> also define service contracts. Not every internal call goes through\n<code>gRPC</code>, so public copy should name the route instead of saying all communication\nuses one channel.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/why-rust">Why <code>Rust</code></a></li>\n<li><a href="https://Editor.Land/Doc/why-tauri">Why <code>Tauri</code></a></li>\n<li><a href="https://Editor.Land/Doc/api-reference"><code>API</code> Reference</a></li>\n</ul>', { headings: 2511, localImagePaths: 2526, remoteImagePaths: 2527, frontmatter: 2528, imagePaths: 2529 }, [2512, 2513, 2516, 2519, 2522, 2525], { depth: 25, slug: 2499, text: 2502 }, { depth: 29, slug: 2514, text: 2515 }, "protocol-buffer-schema", "Protocol Buffer Schema", { depth: 29, slug: 2517, text: 2518 }, "generated-stubs", "Generated Stubs", { depth: 29, slug: 2520, text: 2521 }, "streaming-and-cancellation", "Streaming And Cancellation", { depth: 29, slug: 2523, text: 2524 }, "where-grpc-appears", "Where gRPC Appears", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2502, section: 2465, order: 2503, description: 2504 }, [], "why-rust", { id: 2530, data: 2532, body: 2536, filePath: 2537, digest: 2538, rendered: 2539 }, { title: 2533, section: 2465, order: 2534, description: 2535 }, "Why Rust", 26, "Memory safety without garbage collection, native speed, and fearless concurrency.", "# Why Rust\n\nLand is a code editor. It needs native services for files, terminals, search,\nprocesses, and long-running background work. `Rust` gives those services memory\nsafety, strong concurrency tools, and predictable resource ownership without a\ngarbage collector.\n\n---\n\n## Memory Safety Without a Garbage Collector\n\nC and C++ editors have historically shipped with entire classes of\nvulnerabilities: buffer overflows, use-after-free, double-free, and dangling\npointers. Garbage-collected languages like Go or Java eliminate those bugs, but\nthey introduce unpredictable pauses. A GC pause of even 10 milliseconds is\nvisible to a user who is typing. `Rust` eliminates both problems. The ownership\nsystem enforces memory safety at compile time. There is no garbage collector to\npause the editor, and there are no dangling pointers to crash it.\n\n---\n\n## Fearless Concurrency\n\nA modern editor runs dozens of tasks in parallel: language servers, file\nwatchers, syntax highlighting, search indexing, terminal I/O, and extension\nhosts. In C++, coordinating these tasks requires manual lock management, and a\nsingle mistake produces a data race that may surface weeks later in production.\n`Rust`'s ownership model prevents data races at compile time. If the code\ncompiles, the concurrency is sound. This is what the `Rust` community calls\n\"fearless concurrency,\" and it is not a slogan. It is a compiler guarantee.\n\n---\n\n## Performance Without Compromise\n\n`Rust` compiles to native machine code with no runtime overhead. There is no\ninterpreter, no JIT warmup, and no virtual machine. The generated code is\ncompetitive with hand-tuned C, and in many benchmarks it matches or exceeds it\ndue to aliasing guarantees that allow more aggressive optimization.\n\n---\n\n## The Cargo Ecosystem\n\n`Rust`'s package manager, `Cargo`, provides deterministic builds, integrated\ntesting, documentation generation, and access to over 150,000 crates. Land uses\n`Cargo` workspaces to manage its `Rust` elements as a single coordinated build.\nDependencies are pinned through `Cargo` manifests and the workspace lockfile.\nReproducibility should be described per release profile once public artifacts\nare published.\n\n---\n\n## Where Rust Appears in Land\n\n`Rust` powers the majority of Land's backend:\n\n- **`Mountain`** handles window management, file system access, process\n  lifecycle, terminals, `IPC`, and `Cocoon` startup through `Tauri`.\n- **`Air`** contains background services for updates, downloads, integrity\n  checks, authentication, indexing, health, and network calls.\n- **`Echo`** provides work-stealing task execution for CPU-bound operations.\n- **`Common`** defines abstract traits and data transfer objects shared across\n  elements.\n- **`Vine`** implements `gRPC` protocol definitions for inter-process\n  communication.\n- **`Rest`** contains `OXC`-based `TypeScript` transform work for the output\n  pipeline.\n- **`SideCar`** downloads and organizes `Node.js` binaries per target triple at\n  build time, enabling `Cocoon` to run with the correct `Node.js` version on\n  macOS, Windows, and Linux without runtime detection.\n- **`Grove`** contains a Wasmtime-backed `WebAssembly` host path with `gRPC`\n  protocol, `API` surface, and transport code. Integration with the primary\n  build is in progress.\n- **`Mist`** provides local DNS, resolver, WebSocket, zone, and forward-security\n  code for local service-boundary work.\n\nEvery element that touches the operating system, processes bytes, or coordinates\nconcurrent native work uses `Rust` where the repository has a `Rust` element for\nthat role. The language is a structural decision, not decoration.\n\n---\n\n## See Also\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [Why `Tauri`](https://Editor.Land/Doc/why-tauri)\n- [Why `gRPC`](https://Editor.Land/Doc/why-grpc)\n- [Why `WebAssembly`](https://Editor.Land/Doc/why-wasm)", "Source/Content/doc/why-rust.md", "a33d0009e50f6518", { html: 2540, metadata: 2541 }, '<h1 id="why-rust">Why Rust</h1>\n<p>Land is a code editor. It needs native services for files, terminals, search,\nprocesses, and long-running background work. <code>Rust</code> gives those services memory\nsafety, strong concurrency tools, and predictable resource ownership without a\ngarbage collector.</p>\n<hr>\n<h2 id="memory-safety-without-a-garbage-collector">Memory Safety Without a Garbage Collector</h2>\n<p>C and C++ editors have historically shipped with entire classes of\nvulnerabilities: buffer overflows, use-after-free, double-free, and dangling\npointers. Garbage-collected languages like Go or Java eliminate those bugs, but\nthey introduce unpredictable pauses. A GC pause of even 10 milliseconds is\nvisible to a user who is typing. <code>Rust</code> eliminates both problems. The ownership\nsystem enforces memory safety at compile time. There is no garbage collector to\npause the editor, and there are no dangling pointers to crash it.</p>\n<hr>\n<h2 id="fearless-concurrency">Fearless Concurrency</h2>\n<p>A modern editor runs dozens of tasks in parallel: language servers, file\nwatchers, syntax highlighting, search indexing, terminal I/O, and extension\nhosts. In C++, coordinating these tasks requires manual lock management, and a\nsingle mistake produces a data race that may surface weeks later in production.\n<code>Rust</code>’s ownership model prevents data races at compile time. If the code\ncompiles, the concurrency is sound. This is what the <code>Rust</code> community calls\n“fearless concurrency,” and it is not a slogan. It is a compiler guarantee.</p>\n<hr>\n<h2 id="performance-without-compromise">Performance Without Compromise</h2>\n<p><code>Rust</code> compiles to native machine code with no runtime overhead. There is no\ninterpreter, no JIT warmup, and no virtual machine. The generated code is\ncompetitive with hand-tuned C, and in many benchmarks it matches or exceeds it\ndue to aliasing guarantees that allow more aggressive optimization.</p>\n<hr>\n<h2 id="the-cargo-ecosystem">The Cargo Ecosystem</h2>\n<p><code>Rust</code>’s package manager, <code>Cargo</code>, provides deterministic builds, integrated\ntesting, documentation generation, and access to over 150,000 crates. Land uses\n<code>Cargo</code> workspaces to manage its <code>Rust</code> elements as a single coordinated build.\nDependencies are pinned through <code>Cargo</code> manifests and the workspace lockfile.\nReproducibility should be described per release profile once public artifacts\nare published.</p>\n<hr>\n<h2 id="where-rust-appears-in-land">Where Rust Appears in Land</h2>\n<p><code>Rust</code> powers the majority of Land’s backend:</p>\n<ul>\n<li><strong><code>Mountain</code></strong> handles window management, file system access, process\nlifecycle, terminals, <code>IPC</code>, and <code>Cocoon</code> startup through <code>Tauri</code>.</li>\n<li><strong><code>Air</code></strong> contains background services for updates, downloads, integrity\nchecks, authentication, indexing, health, and network calls.</li>\n<li><strong><code>Echo</code></strong> provides work-stealing task execution for CPU-bound operations.</li>\n<li><strong><code>Common</code></strong> defines abstract traits and data transfer objects shared across\nelements.</li>\n<li><strong><code>Vine</code></strong> implements <code>gRPC</code> protocol definitions for inter-process\ncommunication.</li>\n<li><strong><code>Rest</code></strong> contains <code>OXC</code>-based <code>TypeScript</code> transform work for the output\npipeline.</li>\n<li><strong><code>SideCar</code></strong> downloads and organizes <code>Node.js</code> binaries per target triple at\nbuild time, enabling <code>Cocoon</code> to run with the correct <code>Node.js</code> version on\nmacOS, Windows, and Linux without runtime detection.</li>\n<li><strong><code>Grove</code></strong> contains a Wasmtime-backed <code>WebAssembly</code> host path with <code>gRPC</code>\nprotocol, <code>API</code> surface, and transport code. Integration with the primary\nbuild is in progress.</li>\n<li><strong><code>Mist</code></strong> provides local DNS, resolver, WebSocket, zone, and forward-security\ncode for local service-boundary work.</li>\n</ul>\n<p>Every element that touches the operating system, processes bytes, or coordinates\nconcurrent native work uses <code>Rust</code> where the repository has a <code>Rust</code> element for\nthat role. The language is a structural decision, not decoration.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/why-tauri">Why <code>Tauri</code></a></li>\n<li><a href="https://Editor.Land/Doc/why-grpc">Why <code>gRPC</code></a></li>\n<li><a href="https://Editor.Land/Doc/why-wasm">Why <code>WebAssembly</code></a></li>\n</ul>', { headings: 2542, localImagePaths: 2560, remoteImagePaths: 2561, frontmatter: 2562, imagePaths: 2563 }, [2543, 2544, 2547, 2550, 2553, 2556, 2559], { depth: 25, slug: 2530, text: 2533 }, { depth: 29, slug: 2545, text: 2546 }, "memory-safety-without-a-garbage-collector", "Memory Safety Without a Garbage Collector", { depth: 29, slug: 2548, text: 2549 }, "fearless-concurrency", "Fearless Concurrency", { depth: 29, slug: 2551, text: 2552 }, "performance-without-compromise", "Performance Without Compromise", { depth: 29, slug: 2554, text: 2555 }, "the-cargo-ecosystem", "The Cargo Ecosystem", { depth: 29, slug: 2557, text: 2558 }, "where-rust-appears-in-land", "Where Rust Appears in Land", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2533, section: 2465, order: 2534, description: 2535 }, [], "why-tauri", { id: 2564, data: 2566, body: 2570, filePath: 2571, digest: 2572, rendered: 2573 }, { title: 2567, section: 2465, order: 2568, description: 2569 }, "Why Tauri", 27, "The operating system WebView instead of a bundled Chromium runtime.", "# Why Tauri\n\n`Tauri` is the reason `Mountain` can be a native desktop shell without shipping\na private Chromium runtime. `Sky` still renders web UI, but the WebView comes\nfrom the operating system: `WKWebView` on macOS, `WebView2` on Windows, and\n`WebKitGTK` on Linux.\n\nThat is the source-backed claim. Exact RAM, binary-size, and startup numbers\nbelong in a published benchmark, not in a static documentation page.\n\n---\n\n## No Bundled Chromium\n\n`Electron` applications include Chromium and `Node.js` as part of the app\nruntime. `Tauri` uses the platform WebView and lets the native side live in\n`Rust`. For Land, that means `Mountain` owns native desktop concerns while `Sky`\nremains a web UI and `Cocoon` remains the `Node.js` extension-host compatibility\npath.\n\n---\n\n## Rust Backend\n\n`Mountain` handles native work such as file access, process management, terminal\nsupport, clipboard access, `IPC`, and `Cocoon` process lifecycle in `Rust`. The\nfrontend communicates with `Mountain` through `Tauri` `IPC`, and extension-host\ncommunication uses `Vine` `gRPC` where that route is implemented.\n\n---\n\n## Platform Support\n\nThe repository contains `Tauri` configuration for macOS, Windows, Linux, iOS,\nand Android. Public pages should describe macOS as the primary development path\nand other platforms as configured or in validation unless a release artifact\nexists for that platform.\n\n---\n\n## See Also\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [Why `Rust`](https://Editor.Land/Doc/why-rust)\n- [Why `gRPC`](https://Editor.Land/Doc/why-grpc)", "Source/Content/doc/why-tauri.md", "1af262af3b736168", { html: 2574, metadata: 2575 }, '<h1 id="why-tauri">Why Tauri</h1>\n<p><code>Tauri</code> is the reason <code>Mountain</code> can be a native desktop shell without shipping\na private Chromium runtime. <code>Sky</code> still renders web UI, but the WebView comes\nfrom the operating system: <code>WKWebView</code> on macOS, <code>WebView2</code> on Windows, and\n<code>WebKitGTK</code> on Linux.</p>\n<p>That is the source-backed claim. Exact RAM, binary-size, and startup numbers\nbelong in a published benchmark, not in a static documentation page.</p>\n<hr>\n<h2 id="no-bundled-chromium">No Bundled Chromium</h2>\n<p><code>Electron</code> applications include Chromium and <code>Node.js</code> as part of the app\nruntime. <code>Tauri</code> uses the platform WebView and lets the native side live in\n<code>Rust</code>. For Land, that means <code>Mountain</code> owns native desktop concerns while <code>Sky</code>\nremains a web UI and <code>Cocoon</code> remains the <code>Node.js</code> extension-host compatibility\npath.</p>\n<hr>\n<h2 id="rust-backend">Rust Backend</h2>\n<p><code>Mountain</code> handles native work such as file access, process management, terminal\nsupport, clipboard access, <code>IPC</code>, and <code>Cocoon</code> process lifecycle in <code>Rust</code>. The\nfrontend communicates with <code>Mountain</code> through <code>Tauri</code> <code>IPC</code>, and extension-host\ncommunication uses <code>Vine</code> <code>gRPC</code> where that route is implemented.</p>\n<hr>\n<h2 id="platform-support">Platform Support</h2>\n<p>The repository contains <code>Tauri</code> configuration for macOS, Windows, Linux, iOS,\nand Android. Public pages should describe macOS as the primary development path\nand other platforms as configured or in validation unless a release artifact\nexists for that platform.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/why-rust">Why <code>Rust</code></a></li>\n<li><a href="https://Editor.Land/Doc/why-grpc">Why <code>gRPC</code></a></li>\n</ul>', { headings: 2576, localImagePaths: 2588, remoteImagePaths: 2589, frontmatter: 2590, imagePaths: 2591 }, [2577, 2578, 2581, 2584, 2587], { depth: 25, slug: 2564, text: 2567 }, { depth: 29, slug: 2579, text: 2580 }, "no-bundled-chromium", "No Bundled Chromium", { depth: 29, slug: 2582, text: 2583 }, "rust-backend", "Rust Backend", { depth: 29, slug: 2585, text: 2586 }, "platform-support", "Platform Support", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2567, section: 2465, order: 2568, description: 2569 }, [], "why-wasm", { id: 2592, data: 2594, body: 2597, filePath: 2598, digest: 2599, rendered: 2600 }, { title: 2595, section: 2465, order: 727, description: 2596 }, "Why WebAssembly", "Capability-based extension isolation through a runtime boundary.", "# Why WebAssembly\n\nExtensions are the most sensitive third-party code a code editor runs. They can\ntouch files, spawn tools, open network connections, and react to workspace\nstate. `Grove` is Land's WIP path for extensions that can run inside a\n`WebAssembly` runtime instead of broad `Node.js` access.\n\nThe [`Grove`](https://Editor.Land/Doc/grove) source contains a Wasmtime host,\n`gRPC` protocol definitions, `API` surface, transport layer, and `WASM` runtime\nintegration. The primary `VS Code` extension compatibility path remains\n`Cocoon`.\n\n---\n\n## Runtime-Enforced Isolation\n\n`WebAssembly` modules run in linear memory allocated by the host. A module\ncannot read the host heap or another module's memory through normal execution.\nThat runtime boundary gives `Grove` a stronger starting point than policy-only\nchecks.\n\n---\n\n## Capability-Based Security\n\nWasmtime supports explicit resource grants. A host can decide which directories,\nenvironment values, network endpoints, or `IPC` channels a module receives. If a\ncapability is not granted, the module should not see that resource.\n\nEnd-to-end capability enforcement is still a WIP product path. Public copy\nshould describe `Grove` as a source-backed integration effort until release\nprofiles and tests prove the full behavior.\n\n---\n\n## Resource Controls\n\n`Grove` is the right place for memory ceilings, CPU budgets, and interruption.\nWasmtime supports mechanisms that make those controls possible, but budget\nclaims should stay labeled `WIP` until they are verified in the active build.\n\n---\n\n## Grove And Cocoon Together\n\n`Cocoon` runs unmodified `VS Code` extension code today. `Grove` is for future\nextensions that prioritize isolation and explicit capabilities. The two hosts\nserve different needs and should not be described as interchangeable.\n\n---\n\n## See Also\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Grove`](https://Editor.Land/Doc/grove)\n- [Why `Rust`](https://Editor.Land/Doc/why-rust)\n- [`Cocoon`: Extension Host](https://Editor.Land/Doc/cocoon)", "Source/Content/doc/why-wasm.md", "f849351a88cf1c7b", { html: 2601, metadata: 2602 }, '<h1 id="why-webassembly">Why WebAssembly</h1>\n<p>Extensions are the most sensitive third-party code a code editor runs. They can\ntouch files, spawn tools, open network connections, and react to workspace\nstate. <code>Grove</code> is Land’s WIP path for extensions that can run inside a\n<code>WebAssembly</code> runtime instead of broad <code>Node.js</code> access.</p>\n<p>The <a href="https://Editor.Land/Doc/grove"><code>Grove</code></a> source contains a Wasmtime host,\n<code>gRPC</code> protocol definitions, <code>API</code> surface, transport layer, and <code>WASM</code> runtime\nintegration. The primary <code>VS Code</code> extension compatibility path remains\n<code>Cocoon</code>.</p>\n<hr>\n<h2 id="runtime-enforced-isolation">Runtime-Enforced Isolation</h2>\n<p><code>WebAssembly</code> modules run in linear memory allocated by the host. A module\ncannot read the host heap or another module’s memory through normal execution.\nThat runtime boundary gives <code>Grove</code> a stronger starting point than policy-only\nchecks.</p>\n<hr>\n<h2 id="capability-based-security">Capability-Based Security</h2>\n<p>Wasmtime supports explicit resource grants. A host can decide which directories,\nenvironment values, network endpoints, or <code>IPC</code> channels a module receives. If a\ncapability is not granted, the module should not see that resource.</p>\n<p>End-to-end capability enforcement is still a WIP product path. Public copy\nshould describe <code>Grove</code> as a source-backed integration effort until release\nprofiles and tests prove the full behavior.</p>\n<hr>\n<h2 id="resource-controls">Resource Controls</h2>\n<p><code>Grove</code> is the right place for memory ceilings, CPU budgets, and interruption.\nWasmtime supports mechanisms that make those controls possible, but budget\nclaims should stay labeled <code>WIP</code> until they are verified in the active build.</p>\n<hr>\n<h2 id="grove-and-cocoon-together">Grove And Cocoon Together</h2>\n<p><code>Cocoon</code> runs unmodified <code>VS Code</code> extension code today. <code>Grove</code> is for future\nextensions that prioritize isolation and explicit capabilities. The two hosts\nserve different needs and should not be described as interchangeable.</p>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/grove"><code>Grove</code></a></li>\n<li><a href="https://Editor.Land/Doc/why-rust">Why <code>Rust</code></a></li>\n<li><a href="https://Editor.Land/Doc/cocoon"><code>Cocoon</code>: Extension Host</a></li>\n</ul>', { headings: 2603, localImagePaths: 2619, remoteImagePaths: 2620, frontmatter: 2621, imagePaths: 2622 }, [2604, 2606, 2609, 2612, 2615, 2618], { depth: 25, slug: 2605, text: 2595 }, "why-webassembly", { depth: 29, slug: 2607, text: 2608 }, "runtime-enforced-isolation", "Runtime-Enforced Isolation", { depth: 29, slug: 2610, text: 2611 }, "capability-based-security", "Capability-Based Security", { depth: 29, slug: 2613, text: 2614 }, "resource-controls", "Resource Controls", { depth: 29, slug: 2616, text: 2617 }, "grove-and-cocoon-together", "Grove And Cocoon Together", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2595, section: 2465, order: 727, description: 2596 }, [], { id: 1522, data: 2624, body: 2627, filePath: 2628, digest: 2629, rendered: 2630 }, { title: 1523, section: 381, order: 2625, description: 2626 }, 13, "The Effect-TS service layer that provides typed workbench interfaces to Sky.", "# Wind\n\n`Wind` is the `TypeScript` and `Effect-TS` `workbench` service layer. It sits\nbetween `Sky` and `Mountain` so UI components can depend on typed services\ninstead of raw `Tauri` calls.\n\n---\n\n## Source Layout 🗺️\n\n`Wind` source areas include:\n\n- **`Bootstrap/`** - process startup and layer composition.\n- **`Configuration/`** - configuration reading, watching, and typed access.\n- **`Effect/`** - `Effect-TS` utilities shared across `Wind` layers.\n- **`FileSystem/`** - virtual file-system service abstractions.\n- **`IPC/`** - typed `Tauri` `IPC` helpers and the `SkyEvent` registry.\n- **`Service/`** - `workbench` service implementations and `Tauri` wrappers.\n- **`Workbench/`** - panels, layout, tabs, activity bar, and related services.\n- **`Preload.ts`** - WebView preload support for the typed bridge.\n\n---\n\n## Status 🚀\n\n`Wind` is active source for the `Mountain` `workbench` path. Core service\nsurfaces are present, and more coverage is added as `Mountain` and `Cocoon`\nexpose additional routes. Claims about platform parity or complete `workbench`\ncoverage should name the route being verified.\n\n---\n\n## Relationship To Sky\n\n`Sky` renders UI components. `Wind` provides the services those components\ndepend on. A component can use a `Wind` layer for state or native work without\nembedding `Tauri` `IPC` details directly into the component.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Sky`](https://Editor.Land/Doc/sky)\n- [`Mountain`](https://Editor.Land/Doc/mountain)\n- [Source Code](https://github.com/CodeEditorLand/Wind)", "Source/Content/doc/wind.md", "29c6054a40cd5de5", { html: 2631, metadata: 2632 }, '<h1 id="wind">Wind</h1>\n<p><code>Wind</code> is the <code>TypeScript</code> and <code>Effect-TS</code> <code>workbench</code> service layer. It sits\nbetween <code>Sky</code> and <code>Mountain</code> so UI components can depend on typed services\ninstead of raw <code>Tauri</code> calls.</p>\n<hr>\n<h2 id="source-layout️">Source Layout 🗺️</h2>\n<p><code>Wind</code> source areas include:</p>\n<ul>\n<li><strong><code>Bootstrap/</code></strong> - process startup and layer composition.</li>\n<li><strong><code>Configuration/</code></strong> - configuration reading, watching, and typed access.</li>\n<li><strong><code>Effect/</code></strong> - <code>Effect-TS</code> utilities shared across <code>Wind</code> layers.</li>\n<li><strong><code>FileSystem/</code></strong> - virtual file-system service abstractions.</li>\n<li><strong><code>IPC/</code></strong> - typed <code>Tauri</code> <code>IPC</code> helpers and the <code>SkyEvent</code> registry.</li>\n<li><strong><code>Service/</code></strong> - <code>workbench</code> service implementations and <code>Tauri</code> wrappers.</li>\n<li><strong><code>Workbench/</code></strong> - panels, layout, tabs, activity bar, and related services.</li>\n<li><strong><code>Preload.ts</code></strong> - WebView preload support for the typed bridge.</li>\n</ul>\n<hr>\n<h2 id="status">Status 🚀</h2>\n<p><code>Wind</code> is active source for the <code>Mountain</code> <code>workbench</code> path. Core service\nsurfaces are present, and more coverage is added as <code>Mountain</code> and <code>Cocoon</code>\nexpose additional routes. Claims about platform parity or complete <code>workbench</code>\ncoverage should name the route being verified.</p>\n<hr>\n<h2 id="relationship-to-sky">Relationship To Sky</h2>\n<p><code>Sky</code> renders UI components. <code>Wind</code> provides the services those components\ndepend on. A component can use a <code>Wind</code> layer for state or native work without\nembedding <code>Tauri</code> <code>IPC</code> details directly into the component.</p>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/sky"><code>Sky</code></a></li>\n<li><a href="https://Editor.Land/Doc/mountain"><code>Mountain</code></a></li>\n<li><a href="https://github.com/CodeEditorLand/Wind">Source Code</a></li>\n</ul>', { headings: 2633, localImagePaths: 2641, remoteImagePaths: 2642, frontmatter: 2643, imagePaths: 2644 }, [2634, 2635, 2636, 2637, 2640], { depth: 25, slug: 1522, text: 1523 }, { depth: 29, slug: 559, text: 560 }, { depth: 29, slug: 571, text: 572 }, { depth: 29, slug: 2638, text: 2639 }, "relationship-to-sky", "Relationship To Sky", { depth: 29, slug: 401, text: 402 }, [], [], { title: 1523, section: 381, order: 2625, description: 2626 }, [], { id: 1538, data: 2646, body: 2649, filePath: 2650, digest: 2651, rendered: 2652 }, { title: 1539, section: 381, order: 2647, description: 2648 }, 24, "Browser-worker support for cached assets and CSS import handling where the web shell needs it.", "# Worker\n\n`Worker` is the browser-worker layer for `Editor.Land` surfaces that need cached\nassets, versioned application files, or dynamic CSS import handling. It belongs\nto the web shell and WebView side of the system.\n\n`Worker` should be shown as source-backed, with release scope depending on the\nactive build profile.\n\n---\n\n## Source Areas 🗺️\n\n- `Source/Worker.ts`\n- `Source/Worker/`\n- `Source/Configuration/`\n- `Source/Run.sh`\n- `Source/prepublishOnly.sh`\n\n---\n\n## Status 🚀\n\n`Worker` is useful infrastructure, but public copy should avoid claiming that it\nintercepts all editor requests or guarantees offline behavior for every asset.\nUse `WIP` where the active release profile has not been verified.\n\n---\n\n## Related Documentation 📖\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [`Output`](https://Editor.Land/Doc/output)\n- [`Mountain`](https://Editor.Land/Doc/mountain)\n- [Source Code](https://github.com/CodeEditorLand/Worker)", "Source/Content/doc/worker.md", "c513d4fc6a6ab313", { html: 2653, metadata: 2654 }, '<h1 id="worker">Worker</h1>\n<p><code>Worker</code> is the browser-worker layer for <code>Editor.Land</code> surfaces that need cached\nassets, versioned application files, or dynamic CSS import handling. It belongs\nto the web shell and WebView side of the system.</p>\n<p><code>Worker</code> should be shown as source-backed, with release scope depending on the\nactive build profile.</p>\n<hr>\n<h2 id="source-areas️">Source Areas 🗺️</h2>\n<ul>\n<li><code>Source/Worker.ts</code></li>\n<li><code>Source/Worker/</code></li>\n<li><code>Source/Configuration/</code></li>\n<li><code>Source/Run.sh</code></li>\n<li><code>Source/prepublishOnly.sh</code></li>\n</ul>\n<hr>\n<h2 id="status">Status 🚀</h2>\n<p><code>Worker</code> is useful infrastructure, but public copy should avoid claiming that it\nintercepts all editor requests or guarantees offline behavior for every asset.\nUse <code>WIP</code> where the active release profile has not been verified.</p>\n<hr>\n<h2 id="related-documentation">Related Documentation 📖</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n<li><a href="https://Editor.Land/Doc/output"><code>Output</code></a></li>\n<li><a href="https://Editor.Land/Doc/mountain"><code>Mountain</code></a></li>\n<li><a href="https://github.com/CodeEditorLand/Worker">Source Code</a></li>\n</ul>', { headings: 2655, localImagePaths: 2662, remoteImagePaths: 2663, frontmatter: 2664, imagePaths: 2665 }, [2656, 2657, 2660, 2661], { depth: 25, slug: 1538, text: 1539 }, { depth: 29, slug: 2658, text: 2659 }, "source-areas️", "Source Areas 🗺️", { depth: 29, slug: 571, text: 572 }, { depth: 29, slug: 401, text: 402 }, [], [], { title: 1539, section: 381, order: 2647, description: 2648 }, [], "workflow-command-palette", { id: 2666, data: 2668, body: 2672, filePath: 2673, digest: 2674, rendered: 2675 }, { title: 2669, section: 2670, order: 1841, description: 2671 }, "Executing a Command from the Command Palette", "Workflow", "Unified command system dispatch: Mountain routes execution to native Rust handlers or proxied Cocoon commands.", "# Executing a Command from the Command Palette\n\nIllustrates how Mountain's command registry dispatches execution to either a\nnative Rust handler or a proxied command in Cocoon.\n\n## Data Flow\n\n```\nUser presses Ctrl+Shift+P\n  -> workbench.action.showCommands dispatched\n  -> QuickInputService.quickAccess.show()\n  -> CommandsQuickAccessProvider fetches all commands\n  -> TauriInvoke('mountain://command/get-all')\n  -> Mountain: CommandProvider.GetAllCommands()\n  -> Returns list of command IDs (native + proxied)\n  -> Quick Pick UI populates with command list\n  -> User types and selects \"Format Document\"\n  -> ICommandService.executeCommand('editor.action.formatDocument')\n\nIf NATIVE:\n  -> TauriInvoke('mountain://command/execute')\n  -> Mountain: CommandProvider.ExecuteCommand()\n  -> Lookup handler in CommandRegistry\n  -> Found CommandHandler::Native\n  -> Invoke native Rust function pointer\n  -> Execute command logic\n  -> Return result\n\nIf EXTENSION-PROVIDED:\n  -> TauriInvoke('mountain://command/execute')\n  -> Mountain: CommandProvider.ExecuteCommand()\n  -> Lookup handler in CommandRegistry\n  -> Found CommandHandler::Proxied\n  -> IpcProvider makes $executeContributedCommand gRPC to Cocoon\n  -> Cocoon looks up command in its registry\n  -> Execute extension's JavaScript function\n  -> Result returned to Mountain, forwarded to Wind\n```\n\n## Phase 1: Opening the Command Palette (Wind/Sky)\n\n1. User presses `Ctrl+Shift+P`. `workbench.action.showCommands` is dispatched.\n2. `QuickInputService` opens the Quick Pick UI configured for commands.\n3. `CommandsQuickAccessProvider` needs the list of all commands. Since the\n   registry is in Mountain, it executes\n   `TauriInvoke('mountain://command/get-all')`.\n\n## Phase 2: Fetching Command List (Mountain)\n\n4. The Tauri command is dispatched to `track`, which creates the\n   `Common::command::GetAllCommands` Effect.\n5. `CommandProvider.GetAllCommands()` acquires a lock on\n   `AppState.CommandRegistry` and returns the keys of the HashMap containing all\n   registered native AND proxied command IDs.\n\n## Phase 3: Displaying and Selecting (Wind/Sky)\n\n6. The command list populates the Quick Pick UI.\n7. User selects a command, triggering\n   `ICommandService.executeCommand(commandId)`.\n\n## Phase 4A: Native Command (Wind -> Mountain)\n\n8. `CommandService` in Wind forwards the request:\n   `TauriInvoke('mountain://command/execute', { commandId, args })`.\n9. Mountain's `CommandExecutor::ExecuteCommand` looks up the handler in\n   `CommandRegistry`.\n10. Found as `CommandHandler::Native` -- invokes the corresponding Rust function\n    pointer.\n\n## Phase 4B: Extension Command (Wind -> Mountain -> Cocoon)\n\n10. Mountain looks up the handler and finds\n    `CommandHandler::Proxied { SidecarIdentifier: \"cocoon-main\", ... }`.\n11. `IpcProvider` makes a `$executeContributedCommand` gRPC request to Cocoon.\n12. Cocoon's `CommandsProvider` looks up the command in its registry and\n    executes the extension's JavaScript function.\n13. Result is serialized and returned through Mountain back to Wind.\n\n## Key Source Files\n\n- `Mountain/Source/Environment/CommandProvider.rs` -- command registry\n- `Mountain/Source/Track/TrackLogic.rs` -- track dispatcher\n- `Wind/Source/Application/QuickInput/Definition.ts` -- quick input service\n\n---\n\n## See Also\n\n- [Application Startup and Handshake](https://Editor.Land/Doc/workflow-startup)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)", "Source/Content/doc/workflow-command-palette.md", "197a390dc04272e5", { html: 2676, metadata: 2677 }, `<h1 id="executing-a-command-from-the-command-palette">Executing a Command from the Command Palette</h1>
<p>Illustrates how Mountain’s command registry dispatches execution to either a
native Rust handler or a proxied command in Cocoon.</p>
<h2 id="data-flow">Data Flow</h2>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>User presses Ctrl+Shift+P</span></span>
<span class="line"><span>  -> workbench.action.showCommands dispatched</span></span>
<span class="line"><span>  -> QuickInputService.quickAccess.show()</span></span>
<span class="line"><span>  -> CommandsQuickAccessProvider fetches all commands</span></span>
<span class="line"><span>  -> TauriInvoke('mountain://command/get-all')</span></span>
<span class="line"><span>  -> Mountain: CommandProvider.GetAllCommands()</span></span>
<span class="line"><span>  -> Returns list of command IDs (native + proxied)</span></span>
<span class="line"><span>  -> Quick Pick UI populates with command list</span></span>
<span class="line"><span>  -> User types and selects "Format Document"</span></span>
<span class="line"><span>  -> ICommandService.executeCommand('editor.action.formatDocument')</span></span>
<span class="line"><span></span></span>
<span class="line"><span>If NATIVE:</span></span>
<span class="line"><span>  -> TauriInvoke('mountain://command/execute')</span></span>
<span class="line"><span>  -> Mountain: CommandProvider.ExecuteCommand()</span></span>
<span class="line"><span>  -> Lookup handler in CommandRegistry</span></span>
<span class="line"><span>  -> Found CommandHandler::Native</span></span>
<span class="line"><span>  -> Invoke native Rust function pointer</span></span>
<span class="line"><span>  -> Execute command logic</span></span>
<span class="line"><span>  -> Return result</span></span>
<span class="line"><span></span></span>
<span class="line"><span>If EXTENSION-PROVIDED:</span></span>
<span class="line"><span>  -> TauriInvoke('mountain://command/execute')</span></span>
<span class="line"><span>  -> Mountain: CommandProvider.ExecuteCommand()</span></span>
<span class="line"><span>  -> Lookup handler in CommandRegistry</span></span>
<span class="line"><span>  -> Found CommandHandler::Proxied</span></span>
<span class="line"><span>  -> IpcProvider makes $executeContributedCommand gRPC to Cocoon</span></span>
<span class="line"><span>  -> Cocoon looks up command in its registry</span></span>
<span class="line"><span>  -> Execute extension's JavaScript function</span></span>
<span class="line"><span>  -> Result returned to Mountain, forwarded to Wind</span></span></code></pre>
<h2 id="phase-1-opening-the-command-palette-windsky">Phase 1: Opening the Command Palette (Wind/Sky)</h2>
<ol>
<li>User presses <code>Ctrl+Shift+P</code>. <code>workbench.action.showCommands</code> is dispatched.</li>
<li><code>QuickInputService</code> opens the Quick Pick UI configured for commands.</li>
<li><code>CommandsQuickAccessProvider</code> needs the list of all commands. Since the
registry is in Mountain, it executes
<code>TauriInvoke('mountain://command/get-all')</code>.</li>
</ol>
<h2 id="phase-2-fetching-command-list-mountain">Phase 2: Fetching Command List (Mountain)</h2>
<ol start="4">
<li>The Tauri command is dispatched to <code>track</code>, which creates the
<code>Common::command::GetAllCommands</code> Effect.</li>
<li><code>CommandProvider.GetAllCommands()</code> acquires a lock on
<code>AppState.CommandRegistry</code> and returns the keys of the HashMap containing all
registered native AND proxied command IDs.</li>
</ol>
<h2 id="phase-3-displaying-and-selecting-windsky">Phase 3: Displaying and Selecting (Wind/Sky)</h2>
<ol start="6">
<li>The command list populates the Quick Pick UI.</li>
<li>User selects a command, triggering
<code>ICommandService.executeCommand(commandId)</code>.</li>
</ol>
<h2 id="phase-4a-native-command-wind---mountain">Phase 4A: Native Command (Wind -> Mountain)</h2>
<ol start="8">
<li><code>CommandService</code> in Wind forwards the request:
<code>TauriInvoke('mountain://command/execute', { commandId, args })</code>.</li>
<li>Mountain’s <code>CommandExecutor::ExecuteCommand</code> looks up the handler in
<code>CommandRegistry</code>.</li>
<li>Found as <code>CommandHandler::Native</code> — invokes the corresponding Rust function
pointer.</li>
</ol>
<h2 id="phase-4b-extension-command-wind---mountain---cocoon">Phase 4B: Extension Command (Wind -> Mountain -> Cocoon)</h2>
<ol start="10">
<li>Mountain looks up the handler and finds
<code>CommandHandler::Proxied { SidecarIdentifier: "cocoon-main", ... }</code>.</li>
<li><code>IpcProvider</code> makes a <code>$executeContributedCommand</code> gRPC request to Cocoon.</li>
<li>Cocoon’s <code>CommandsProvider</code> looks up the command in its registry and
executes the extension’s JavaScript function.</li>
<li>Result is serialized and returned through Mountain back to Wind.</li>
</ol>
<h2 id="key-source-files">Key Source Files</h2>
<ul>
<li><code>Mountain/Source/Environment/CommandProvider.rs</code> — command registry</li>
<li><code>Mountain/Source/Track/TrackLogic.rs</code> — track dispatcher</li>
<li><code>Wind/Source/Application/QuickInput/Definition.ts</code> — quick input service</li>
</ul>
<hr>
<h2 id="see-also">See Also</h2>
<ul>
<li><a href="https://Editor.Land/Doc/workflow-startup">Application Startup and Handshake</a></li>
<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>
</ul>`, { headings: 2678, localImagePaths: 2702, remoteImagePaths: 2703, frontmatter: 2704, imagePaths: 2705 }, [2679, 2681, 2683, 2686, 2689, 2692, 2695, 2698, 2701], { depth: 25, slug: 2680, text: 2669 }, "executing-a-command-from-the-command-palette", { depth: 29, slug: 744, text: 2682 }, "Data Flow", { depth: 29, slug: 2684, text: 2685 }, "phase-1-opening-the-command-palette-windsky", "Phase 1: Opening the Command Palette (Wind/Sky)", { depth: 29, slug: 2687, text: 2688 }, "phase-2-fetching-command-list-mountain", "Phase 2: Fetching Command List (Mountain)", { depth: 29, slug: 2690, text: 2691 }, "phase-3-displaying-and-selecting-windsky", "Phase 3: Displaying and Selecting (Wind/Sky)", { depth: 29, slug: 2693, text: 2694 }, "phase-4a-native-command-wind---mountain", "Phase 4A: Native Command (Wind -> Mountain)", { depth: 29, slug: 2696, text: 2697 }, "phase-4b-extension-command-wind---mountain---cocoon", "Phase 4B: Extension Command (Wind -> Mountain -> Cocoon)", { depth: 29, slug: 2699, text: 2700 }, "key-source-files", "Key Source Files", { depth: 29, slug: 446, text: 447 }, [], [], { title: 2669, section: 2670, order: 1841, description: 2671 }, [], "workflow-extension-tests", { id: 2706, data: 2708, body: 2711, filePath: 2712, digest: 2713, rendered: 2714 }, { title: 2709, section: 2670, order: 1554, description: 2710 }, "Running Extension Tests", "Extension Development Host model: second isolated instance launches to run tests with test Cocoon remote-controlling main UI.", '# Running Extension Tests\n\nExplains the "Extension Development Host" model: a second, isolated instance of\nthe application launches to run tests, with the test Cocoon instance\nremote-controlling the main UI.\n\nThe actual implementation details should be verified against\n`Mountain/Source/Testing/` and `Cocoon/Source/Services/Extension.ts`.\n\n## Data Flow\n\n```\nDeveloper launches Mountain with --extensionDevelopmentPath\n  -> Extension activated in development mode\n  -> Developer runs "Run Tests" command (Ctrl+Shift+P)\n  -> Test Runner Service constructs special arguments:\n       --extensionDevelopmentPath\n       --extensionTestsPath\n       VSCODE_IPC_HOOK_CLI env var\n  -> Spawn new Mountain instance (Extension Development Host)\n\nTest Host:\n  -> Detects --extension... flags, knows it is a test instance\n  -> Launches Cocoon sidecar with special env vars\n  -> Test Cocoon detects VSCODE_IPC_HOOK_CLI\n  -> Enters CLI test runner mode (no normal gRPC server)\n  -> Requires and executes script from --extensionTestsPath (e.g. mocha)\n  -> Mocha loads extension test files\n\nTest Execution:\n  -> Tests call vscode.commands.executeCommand(\'my-extension.doSomething\')\n  -> Lightweight test vscode shim intercepts require(\'vscode\')\n  -> Shim connects to MAIN Mountain instance (not test instance)\n  -> gRPC: executeCommand sent to main Mountain\n  -> Main Mountain executes command, updates UI, returns result\n  -> Test proceeds: vscode.workspace.textDocuments[0]\n  -> Another gRPC call to main Mountain for document state\n  -> assert checks if main application state matches expected\n\nTest Completion:\n  -> Mocha completes all tests, aggregates pass/fail counts\n  -> Prints results to stdout, exits with code (0/1)\n  -> Main Mountain\'s Test Runner Service monitored stdout\n  -> Parses results, displays notification: "10 passed, 0 failed"\n```\n\n## Phase 1: Test Discovery and Command Registration\n\n1. Extension under development includes a `test` script and contributes a\n   command for its test runner.\n2. Developer launches Mountain with\n   `--extensionDevelopmentPath /path/to/my-extension`. Extension activates in\n   development mode.\n\n## Phase 2: Initiating the Test Run\n\n3. Developer executes "Run Tests" from the Command Palette in the main Mountain\n   window.\n4. Test Runner Service prepares special arguments and environment variables for\n   a new instance: `--extensionDevelopmentPath`, `--extensionTestsPath`, and\n   `VSCODE_IPC_HOOK_CLI`.\n5. Spawns a new Mountain process -- the "Extension Development Host".\n\n## Phase 3: Test Execution in the Development Host\n\n6. New Mountain instance starts, sees the `--extension...` flags, knows it is a\n   test instance.\n7. Launches its own Cocoon sidecar with special environment variables.\n8. Test Cocoon detects `VSCODE_IPC_HOOK_CLI` and enters CLI test runner mode (no\n   normal gRPC server, no `initExtensionHost` handshake).\n9. Executes the test runner script from `--extensionTestsPath` (typically\n   mocha).\n10. Test files execute. The `require(\'vscode\')` call is intercepted by a\n    lightweight `RequireInterceptor` that connects back to the **main Mountain\n    instance**.\n\n## Phase 4: Remote Control and Result Reporting\n\n11. Test code calls `vscode.commands.executeCommand(...)`. Lightweight shim\n    sends gRPC request to the original Mountain instance.\n12. Main Mountain executes the command, UI updates, files open. Result returned\n    to test Cocoon.\n13. Test proceeds to assertions (e.g. checking document state via another gRPC\n    call to main Mountain).\n14. Mocha completes, aggregates results, prints to stdout, exits with code.\n15. Main Mountain\'s Test Runner Service parses the output and displays a\n    notification.\n\n## Key Source Files\n\n- `Mountain/Source/Testing/` -- test runner service\n- `Cocoon/Source/Services/Extension.ts` -- extension activation\n\n---\n\n## See Also\n\n- [Extension Development](https://Editor.Land/Doc/extension-development)\n- [Application Startup and Handshake](https://Editor.Land/Doc/workflow-startup)', "Source/Content/doc/workflow-extension-tests.md", "03125a2a6761ab62", { html: 2715, metadata: 2716 }, `<h1 id="running-extension-tests">Running Extension Tests</h1>
<p>Explains the “Extension Development Host” model: a second, isolated instance of
the application launches to run tests, with the test Cocoon instance
remote-controlling the main UI.</p>
<p>The actual implementation details should be verified against
<code>Mountain/Source/Testing/</code> and <code>Cocoon/Source/Services/Extension.ts</code>.</p>
<h2 id="data-flow">Data Flow</h2>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>Developer launches Mountain with --extensionDevelopmentPath</span></span>
<span class="line"><span>  -> Extension activated in development mode</span></span>
<span class="line"><span>  -> Developer runs "Run Tests" command (Ctrl+Shift+P)</span></span>
<span class="line"><span>  -> Test Runner Service constructs special arguments:</span></span>
<span class="line"><span>       --extensionDevelopmentPath</span></span>
<span class="line"><span>       --extensionTestsPath</span></span>
<span class="line"><span>       VSCODE_IPC_HOOK_CLI env var</span></span>
<span class="line"><span>  -> Spawn new Mountain instance (Extension Development Host)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Test Host:</span></span>
<span class="line"><span>  -> Detects --extension... flags, knows it is a test instance</span></span>
<span class="line"><span>  -> Launches Cocoon sidecar with special env vars</span></span>
<span class="line"><span>  -> Test Cocoon detects VSCODE_IPC_HOOK_CLI</span></span>
<span class="line"><span>  -> Enters CLI test runner mode (no normal gRPC server)</span></span>
<span class="line"><span>  -> Requires and executes script from --extensionTestsPath (e.g. mocha)</span></span>
<span class="line"><span>  -> Mocha loads extension test files</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Test Execution:</span></span>
<span class="line"><span>  -> Tests call vscode.commands.executeCommand('my-extension.doSomething')</span></span>
<span class="line"><span>  -> Lightweight test vscode shim intercepts require('vscode')</span></span>
<span class="line"><span>  -> Shim connects to MAIN Mountain instance (not test instance)</span></span>
<span class="line"><span>  -> gRPC: executeCommand sent to main Mountain</span></span>
<span class="line"><span>  -> Main Mountain executes command, updates UI, returns result</span></span>
<span class="line"><span>  -> Test proceeds: vscode.workspace.textDocuments[0]</span></span>
<span class="line"><span>  -> Another gRPC call to main Mountain for document state</span></span>
<span class="line"><span>  -> assert checks if main application state matches expected</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Test Completion:</span></span>
<span class="line"><span>  -> Mocha completes all tests, aggregates pass/fail counts</span></span>
<span class="line"><span>  -> Prints results to stdout, exits with code (0/1)</span></span>
<span class="line"><span>  -> Main Mountain's Test Runner Service monitored stdout</span></span>
<span class="line"><span>  -> Parses results, displays notification: "10 passed, 0 failed"</span></span></code></pre>
<h2 id="phase-1-test-discovery-and-command-registration">Phase 1: Test Discovery and Command Registration</h2>
<ol>
<li>Extension under development includes a <code>test</code> script and contributes a
command for its test runner.</li>
<li>Developer launches Mountain with
<code>--extensionDevelopmentPath /path/to/my-extension</code>. Extension activates in
development mode.</li>
</ol>
<h2 id="phase-2-initiating-the-test-run">Phase 2: Initiating the Test Run</h2>
<ol start="3">
<li>Developer executes “Run Tests” from the Command Palette in the main Mountain
window.</li>
<li>Test Runner Service prepares special arguments and environment variables for
a new instance: <code>--extensionDevelopmentPath</code>, <code>--extensionTestsPath</code>, and
<code>VSCODE_IPC_HOOK_CLI</code>.</li>
<li>Spawns a new Mountain process — the “Extension Development Host”.</li>
</ol>
<h2 id="phase-3-test-execution-in-the-development-host">Phase 3: Test Execution in the Development Host</h2>
<ol start="6">
<li>New Mountain instance starts, sees the <code>--extension...</code> flags, knows it is a
test instance.</li>
<li>Launches its own Cocoon sidecar with special environment variables.</li>
<li>Test Cocoon detects <code>VSCODE_IPC_HOOK_CLI</code> and enters CLI test runner mode (no
normal gRPC server, no <code>initExtensionHost</code> handshake).</li>
<li>Executes the test runner script from <code>--extensionTestsPath</code> (typically
mocha).</li>
<li>Test files execute. The <code>require('vscode')</code> call is intercepted by a
lightweight <code>RequireInterceptor</code> that connects back to the <strong>main Mountain
instance</strong>.</li>
</ol>
<h2 id="phase-4-remote-control-and-result-reporting">Phase 4: Remote Control and Result Reporting</h2>
<ol start="11">
<li>Test code calls <code>vscode.commands.executeCommand(...)</code>. Lightweight shim
sends gRPC request to the original Mountain instance.</li>
<li>Main Mountain executes the command, UI updates, files open. Result returned
to test Cocoon.</li>
<li>Test proceeds to assertions (e.g. checking document state via another gRPC
call to main Mountain).</li>
<li>Mocha completes, aggregates results, prints to stdout, exits with code.</li>
<li>Main Mountain’s Test Runner Service parses the output and displays a
notification.</li>
</ol>
<h2 id="key-source-files">Key Source Files</h2>
<ul>
<li><code>Mountain/Source/Testing/</code> — test runner service</li>
<li><code>Cocoon/Source/Services/Extension.ts</code> — extension activation</li>
</ul>
<hr>
<h2 id="see-also">See Also</h2>
<ul>
<li><a href="https://Editor.Land/Doc/extension-development">Extension Development</a></li>
<li><a href="https://Editor.Land/Doc/workflow-startup">Application Startup and Handshake</a></li>
</ul>`, { headings: 2717, localImagePaths: 2735, remoteImagePaths: 2736, frontmatter: 2737, imagePaths: 2738 }, [2718, 2720, 2721, 2724, 2727, 2730, 2733, 2734], { depth: 25, slug: 2719, text: 2709 }, "running-extension-tests", { depth: 29, slug: 744, text: 2682 }, { depth: 29, slug: 2722, text: 2723 }, "phase-1-test-discovery-and-command-registration", "Phase 1: Test Discovery and Command Registration", { depth: 29, slug: 2725, text: 2726 }, "phase-2-initiating-the-test-run", "Phase 2: Initiating the Test Run", { depth: 29, slug: 2728, text: 2729 }, "phase-3-test-execution-in-the-development-host", "Phase 3: Test Execution in the Development Host", { depth: 29, slug: 2731, text: 2732 }, "phase-4-remote-control-and-result-reporting", "Phase 4: Remote Control and Result Reporting", { depth: 29, slug: 2699, text: 2700 }, { depth: 29, slug: 446, text: 447 }, [], [], { title: 2709, section: 2670, order: 1554, description: 2710 }, [], "workflow-hover-provider", { id: 2739, data: 2741, body: 2744, filePath: 2745, digest: 2746, rendered: 2747 }, { title: 2742, section: 2670, order: 2084, description: 2743 }, "Invoking a Language Feature (Hover Provider)", "Bi-directional communication: extension registers a feature, Mountain orchestrates the request, result displayed in Wind UI.", "# Invoking a Language Feature (Hover Provider)\n\nA key example of bi-directional communication: an extension in Cocoon registers\na hover provider, Mountain orchestrates the request, and the result is displayed\nin the Wind UI.\n\n## Data Flow\n\n```\nPhase 1: Registration\n  Extension calls vscode.languages.registerHoverProvider('mylang', provider)\n  -> Cocoon stores provider with unique handle (e.g. 123)\n  -> Cocoon sends $registerHoverProvider gRPC to Mountain\n  -> Mountain creates ProviderRegistrationDto\n  -> Mountain stores in AppState.LanguageProviders\n\nPhase 2: User Hover Request\n  User hovers over a word in a \"mylang\" file\n  -> Monaco hover controller triggers\n  -> LanguageFeaturesService.getHover()\n  -> TauriInvoke('mountain://language-feature/provide-hover')\n\nPhase 3: Host Orchestration\n  -> Mountain queries AppState.LanguageProviders for \"mylang\" hover providers\n  -> Finds registration (handle 123) belonging to cocoon-main\n  -> Mountain sends $provideHover gRPC to Cocoon (handle, URI, position)\n\nPhase 4: Extension Execution\n  -> Cocoon looks up provider by handle\n  -> Calls provider.provideHover(document, position, token)\n  -> Extension returns Hover object { contents: ['Hello World'] }\n  -> Cocoon serializes to HoverResultDto\n  -> Returns to Mountain\n\nPhase 5: UI Update\n  -> Mountain returns hover data to Wind\n  -> Wind passes data to Monaco controller\n  -> Monaco renders tooltip widget\n  -> User sees \"Hello World\" tooltip\n```\n\n## Phase 1: Extension Registration (Cocoon)\n\n1. Extension is activated by Cocoon. Its `activate()` function runs.\n2. Extension calls `vscode.languages.registerHoverProvider('mylang', provider)`.\n3. `LanguageFeaturesProvider` in Cocoon stores the provider in a local map with\n   a unique handle.\n4. Sends `$registerHoverProvider` gRPC request to Mountain with the handle and\n   metadata.\n\n## Phase 2: Host-Side Provider Registration (Mountain)\n\n5. gRPC server receives `$registerHoverProvider` and dispatches to `track`.\n6. `LanguageFeatureProvider.RegisterProvider()` executes via `AppRuntime`.\n7. `Registration.register_provider()` creates a `ProviderRegistrationDto` and\n   stores it in `AppState.LanguageProviders`. Mountain now knows that for\n   language \"mylang\", `cocoon-main` has a hover provider with handle `123`.\n\n## Phase 3: User Interaction and UI Request (Wind/Sky)\n\n8. User hovers over a word in a \"mylang\" file. Monaco's hover controller\n   triggers.\n9. `ILanguageFeaturesService.getHover()` creates an Effect that calls\n   `TauriInvoke('mountain://language-feature/provide-hover')`.\n\n## Phase 4: Host-Side Orchestration (Mountain -> Cocoon)\n\n10. `LanguageFeatureProvider.ProvideHover()` queries\n    `AppState.LanguageProviders` and finds handle `123` belonging to\n    `cocoon-main`.\n11. Makes `$provideHover` gRPC request to Cocoon with document URI, position,\n    and provider handle.\n\n## Phase 5: Extension Execution and Response (Cocoon)\n\n12. Cocoon's gRPC server dispatches to the handler, which looks up the provider\n    by handle.\n13. Calls `provider.provideHover(document, position, token)`. Extension code\n    executes.\n14. Extension returns `Hover` object. Handler serializes to `HoverResultDto` and\n    returns to Mountain.\n\n## Phase 6: Final UI Update (Mountain -> Wind/Sky)\n\n15. gRPC call resolves with `HoverResultDto`. Result sent back as response to\n    the original Tauri invoke.\n16. Wind receives hover data, passes to Monaco's hover controller.\n17. Monaco renders the tooltip widget on screen.\n\n## Key Source Files\n\n- `Cocoon/src/Service/LanguageFeatures.ts` -- language feature provider\n- `Mountain/Source/Environment/LanguageFeatureProvider/Registration.rs` --\n  provider registration\n- `Mountain/Source/Environment/LanguageFeatureProvider/FeatureMethods.rs` --\n  feature execution\n- `Mountain/Source/track/TrackLogic.rs` -- track dispatcher\n\n---\n\n## See Also\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [VS Code API Coverage](https://Editor.Land/Doc/vscode-api-coverage)", "Source/Content/doc/workflow-hover-provider.md", "a6bf584cd12844e6", { html: 2748, metadata: 2749 }, `<h1 id="invoking-a-language-feature-hover-provider">Invoking a Language Feature (Hover Provider)</h1>
<p>A key example of bi-directional communication: an extension in Cocoon registers
a hover provider, Mountain orchestrates the request, and the result is displayed
in the Wind UI.</p>
<h2 id="data-flow">Data Flow</h2>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>Phase 1: Registration</span></span>
<span class="line"><span>  Extension calls vscode.languages.registerHoverProvider('mylang', provider)</span></span>
<span class="line"><span>  -> Cocoon stores provider with unique handle (e.g. 123)</span></span>
<span class="line"><span>  -> Cocoon sends $registerHoverProvider gRPC to Mountain</span></span>
<span class="line"><span>  -> Mountain creates ProviderRegistrationDto</span></span>
<span class="line"><span>  -> Mountain stores in AppState.LanguageProviders</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Phase 2: User Hover Request</span></span>
<span class="line"><span>  User hovers over a word in a "mylang" file</span></span>
<span class="line"><span>  -> Monaco hover controller triggers</span></span>
<span class="line"><span>  -> LanguageFeaturesService.getHover()</span></span>
<span class="line"><span>  -> TauriInvoke('mountain://language-feature/provide-hover')</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Phase 3: Host Orchestration</span></span>
<span class="line"><span>  -> Mountain queries AppState.LanguageProviders for "mylang" hover providers</span></span>
<span class="line"><span>  -> Finds registration (handle 123) belonging to cocoon-main</span></span>
<span class="line"><span>  -> Mountain sends $provideHover gRPC to Cocoon (handle, URI, position)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Phase 4: Extension Execution</span></span>
<span class="line"><span>  -> Cocoon looks up provider by handle</span></span>
<span class="line"><span>  -> Calls provider.provideHover(document, position, token)</span></span>
<span class="line"><span>  -> Extension returns Hover object { contents: ['Hello World'] }</span></span>
<span class="line"><span>  -> Cocoon serializes to HoverResultDto</span></span>
<span class="line"><span>  -> Returns to Mountain</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Phase 5: UI Update</span></span>
<span class="line"><span>  -> Mountain returns hover data to Wind</span></span>
<span class="line"><span>  -> Wind passes data to Monaco controller</span></span>
<span class="line"><span>  -> Monaco renders tooltip widget</span></span>
<span class="line"><span>  -> User sees "Hello World" tooltip</span></span></code></pre>
<h2 id="phase-1-extension-registration-cocoon">Phase 1: Extension Registration (Cocoon)</h2>
<ol>
<li>Extension is activated by Cocoon. Its <code>activate()</code> function runs.</li>
<li>Extension calls <code>vscode.languages.registerHoverProvider('mylang', provider)</code>.</li>
<li><code>LanguageFeaturesProvider</code> in Cocoon stores the provider in a local map with
a unique handle.</li>
<li>Sends <code>$registerHoverProvider</code> gRPC request to Mountain with the handle and
metadata.</li>
</ol>
<h2 id="phase-2-host-side-provider-registration-mountain">Phase 2: Host-Side Provider Registration (Mountain)</h2>
<ol start="5">
<li>gRPC server receives <code>$registerHoverProvider</code> and dispatches to <code>track</code>.</li>
<li><code>LanguageFeatureProvider.RegisterProvider()</code> executes via <code>AppRuntime</code>.</li>
<li><code>Registration.register_provider()</code> creates a <code>ProviderRegistrationDto</code> and
stores it in <code>AppState.LanguageProviders</code>. Mountain now knows that for
language “mylang”, <code>cocoon-main</code> has a hover provider with handle <code>123</code>.</li>
</ol>
<h2 id="phase-3-user-interaction-and-ui-request-windsky">Phase 3: User Interaction and UI Request (Wind/Sky)</h2>
<ol start="8">
<li>User hovers over a word in a “mylang” file. Monaco’s hover controller
triggers.</li>
<li><code>ILanguageFeaturesService.getHover()</code> creates an Effect that calls
<code>TauriInvoke('mountain://language-feature/provide-hover')</code>.</li>
</ol>
<h2 id="phase-4-host-side-orchestration-mountain---cocoon">Phase 4: Host-Side Orchestration (Mountain -> Cocoon)</h2>
<ol start="10">
<li><code>LanguageFeatureProvider.ProvideHover()</code> queries
<code>AppState.LanguageProviders</code> and finds handle <code>123</code> belonging to
<code>cocoon-main</code>.</li>
<li>Makes <code>$provideHover</code> gRPC request to Cocoon with document URI, position,
and provider handle.</li>
</ol>
<h2 id="phase-5-extension-execution-and-response-cocoon">Phase 5: Extension Execution and Response (Cocoon)</h2>
<ol start="12">
<li>Cocoon’s gRPC server dispatches to the handler, which looks up the provider
by handle.</li>
<li>Calls <code>provider.provideHover(document, position, token)</code>. Extension code
executes.</li>
<li>Extension returns <code>Hover</code> object. Handler serializes to <code>HoverResultDto</code> and
returns to Mountain.</li>
</ol>
<h2 id="phase-6-final-ui-update-mountain---windsky">Phase 6: Final UI Update (Mountain -> Wind/Sky)</h2>
<ol start="15">
<li>gRPC call resolves with <code>HoverResultDto</code>. Result sent back as response to
the original Tauri invoke.</li>
<li>Wind receives hover data, passes to Monaco’s hover controller.</li>
<li>Monaco renders the tooltip widget on screen.</li>
</ol>
<h2 id="key-source-files">Key Source Files</h2>
<ul>
<li><code>Cocoon/src/Service/LanguageFeatures.ts</code> — language feature provider</li>
<li><code>Mountain/Source/Environment/LanguageFeatureProvider/Registration.rs</code> —
provider registration</li>
<li><code>Mountain/Source/Environment/LanguageFeatureProvider/FeatureMethods.rs</code> —
feature execution</li>
<li><code>Mountain/Source/track/TrackLogic.rs</code> — track dispatcher</li>
</ul>
<hr>
<h2 id="see-also">See Also</h2>
<ul>
<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>
<li><a href="https://Editor.Land/Doc/vscode-api-coverage">VS Code API Coverage</a></li>
</ul>`, { headings: 2750, localImagePaths: 2774, remoteImagePaths: 2775, frontmatter: 2776, imagePaths: 2777 }, [2751, 2753, 2754, 2757, 2760, 2763, 2766, 2769, 2772, 2773], { depth: 25, slug: 2752, text: 2742 }, "invoking-a-language-feature-hover-provider", { depth: 29, slug: 744, text: 2682 }, { depth: 29, slug: 2755, text: 2756 }, "phase-1-extension-registration-cocoon", "Phase 1: Extension Registration (Cocoon)", { depth: 29, slug: 2758, text: 2759 }, "phase-2-host-side-provider-registration-mountain", "Phase 2: Host-Side Provider Registration (Mountain)", { depth: 29, slug: 2761, text: 2762 }, "phase-3-user-interaction-and-ui-request-windsky", "Phase 3: User Interaction and UI Request (Wind/Sky)", { depth: 29, slug: 2764, text: 2765 }, "phase-4-host-side-orchestration-mountain---cocoon", "Phase 4: Host-Side Orchestration (Mountain -> Cocoon)", { depth: 29, slug: 2767, text: 2768 }, "phase-5-extension-execution-and-response-cocoon", "Phase 5: Extension Execution and Response (Cocoon)", { depth: 29, slug: 2770, text: 2771 }, "phase-6-final-ui-update-mountain---windsky", "Phase 6: Final UI Update (Mountain -> Wind/Sky)", { depth: 29, slug: 2699, text: 2700 }, { depth: 29, slug: 446, text: 447 }, [], [], { title: 2742, section: 2670, order: 2084, description: 2743 }, [], "workflow-open-file", { id: 2778, data: 2780, body: 2783, filePath: 2784, digest: 2785, rendered: 2786 }, { title: 2781, section: 2670, order: 194, description: 2782 }, "Opening a File from the UI", "Flow from user clicking a file in explorer to content loaded from disk by Mountain and rendered in editor by Wind.", "# Opening a File from the UI\n\nDetails the flow from a user clicking a file in the File Explorer to the file's\ncontent being loaded into an editor.\n\n## Data Flow\n\n```\nUser clicks file in File Explorer\n  -> IEditorService.openEditor({ resource: fileUri })\n  -> TextEditorService resolves EditorInput\n  -> EditorGroupsService finds target editor group\n  -> TextFileEditorModel.load()\n  -> IFileService.readFile(fileUri)\n  -> TauriDiskFileSystemProvider.lookup(file: scheme)\n  -> ReadFile Effect executes\n  -> TauriInvoke('plugin:fs|ReadFile', { path: fileUri.fsPath })\n  -> Mountain: tokio::fs::read(path)\n  -> File content returned as Vec<u8>\n  -> Uint8Array returned up the chain\n  -> TextFileEditorModel hydrated\n  -> TextEditorPane created in editor group\n  -> Monaco editor receives text model\n  -> User sees file open in editor\n```\n\n## Phase 1: UI Interaction (Wind/Sky)\n\n1. User clicks a file `<div>` in the File Explorer. The `onClick` handler knows\n   the file's URI.\n2. `IEditorService.openEditor()` is called with `{ resource: fileUri }`. The\n   effect resolves the input into a concrete `EditorInput` via\n   `TextEditorService`, then finds the target editor group.\n\n## Phase 2: Editor and Filesystem Logic (Wind -> Mountain)\n\n3. `EditorGroupsService` checks if the file is already open. If not, it calls\n   `EditorInput.resolve()` to load the underlying model.\n4. `TextFileEditorModel.load()` needs file content. It calls\n   `IFileService.readFile(fileUri)`.\n5. `IFileService` looks up the provider for the `file:` scheme and finds\n   `TauriDiskFileSystemProvider`.\n6. `TauriDiskFileSystemProvider.readFile()` executes the `ReadFile` Effect from\n   the Tauri Integration layer: `Effect.runPromise(ReadFile(fileUri))`.\n7. The `ReadFile` Effect calls\n   `TauriInvoke('plugin:fs|ReadFile', { path: fileUri.fsPath })`, sending the\n   request from the webview to Mountain.\n\n## Phase 3: Native File I/O (Mountain)\n\n8. Tauri routes the command to `tauri-plugin-fs`. The plugin performs\n   `tokio::fs::read(path)`.\n9. File content (`Vec<u8>`) is read from disk, serialized, and returned as the\n   promise resolution.\n\n## Phase 4: UI Rendering (Wind)\n\n10. The `TauriInvoke` promise resolves with file content as a `Uint8Array`.\n11. `TextFileEditorModel.load()` succeeds. The model is hydrated.\n12. `EditorGroupsService` creates a `TextEditorPane` and sets the\n    `TextFileEditorModel` on the Monaco editor instance.\n13. Monaco renders the text content to screen.\n\n## Key Source Files\n\n- `Wind/Source/Application/Editor/Definition.ts` -- editor service\n- `Wind/Source/Application/FileSystem/Definition.ts` -- filesystem provider\n- `Wind/Integration/Tauri/Wrap/ReadFile.ts` -- Tauri invoke effect\n- `Mountain/src/main.rs` -- Tauri fs plugin\n\n---\n\n## See Also\n\n- [Application Startup and Handshake](https://Editor.Land/Doc/workflow-startup)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)", "Source/Content/doc/workflow-open-file.md", "f8fd6a6378fe9d3b", { html: 2787, metadata: 2788 }, `<h1 id="opening-a-file-from-the-ui">Opening a File from the UI</h1>
<p>Details the flow from a user clicking a file in the File Explorer to the file’s
content being loaded into an editor.</p>
<h2 id="data-flow">Data Flow</h2>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>User clicks file in File Explorer</span></span>
<span class="line"><span>  -> IEditorService.openEditor({ resource: fileUri })</span></span>
<span class="line"><span>  -> TextEditorService resolves EditorInput</span></span>
<span class="line"><span>  -> EditorGroupsService finds target editor group</span></span>
<span class="line"><span>  -> TextFileEditorModel.load()</span></span>
<span class="line"><span>  -> IFileService.readFile(fileUri)</span></span>
<span class="line"><span>  -> TauriDiskFileSystemProvider.lookup(file: scheme)</span></span>
<span class="line"><span>  -> ReadFile Effect executes</span></span>
<span class="line"><span>  -> TauriInvoke('plugin:fs|ReadFile', { path: fileUri.fsPath })</span></span>
<span class="line"><span>  -> Mountain: tokio::fs::read(path)</span></span>
<span class="line"><span>  -> File content returned as Vec&#x3C;u8></span></span>
<span class="line"><span>  -> Uint8Array returned up the chain</span></span>
<span class="line"><span>  -> TextFileEditorModel hydrated</span></span>
<span class="line"><span>  -> TextEditorPane created in editor group</span></span>
<span class="line"><span>  -> Monaco editor receives text model</span></span>
<span class="line"><span>  -> User sees file open in editor</span></span></code></pre>
<h2 id="phase-1-ui-interaction-windsky">Phase 1: UI Interaction (Wind/Sky)</h2>
<ol>
<li>User clicks a file <code>&#x3C;div></code> in the File Explorer. The <code>onClick</code> handler knows
the file’s URI.</li>
<li><code>IEditorService.openEditor()</code> is called with <code>{ resource: fileUri }</code>. The
effect resolves the input into a concrete <code>EditorInput</code> via
<code>TextEditorService</code>, then finds the target editor group.</li>
</ol>
<h2 id="phase-2-editor-and-filesystem-logic-wind---mountain">Phase 2: Editor and Filesystem Logic (Wind -> Mountain)</h2>
<ol start="3">
<li><code>EditorGroupsService</code> checks if the file is already open. If not, it calls
<code>EditorInput.resolve()</code> to load the underlying model.</li>
<li><code>TextFileEditorModel.load()</code> needs file content. It calls
<code>IFileService.readFile(fileUri)</code>.</li>
<li><code>IFileService</code> looks up the provider for the <code>file:</code> scheme and finds
<code>TauriDiskFileSystemProvider</code>.</li>
<li><code>TauriDiskFileSystemProvider.readFile()</code> executes the <code>ReadFile</code> Effect from
the Tauri Integration layer: <code>Effect.runPromise(ReadFile(fileUri))</code>.</li>
<li>The <code>ReadFile</code> Effect calls
<code>TauriInvoke('plugin:fs|ReadFile', { path: fileUri.fsPath })</code>, sending the
request from the webview to Mountain.</li>
</ol>
<h2 id="phase-3-native-file-io-mountain">Phase 3: Native File I/O (Mountain)</h2>
<ol start="8">
<li>Tauri routes the command to <code>tauri-plugin-fs</code>. The plugin performs
<code>tokio::fs::read(path)</code>.</li>
<li>File content (<code>Vec&#x3C;u8></code>) is read from disk, serialized, and returned as the
promise resolution.</li>
</ol>
<h2 id="phase-4-ui-rendering-wind">Phase 4: UI Rendering (Wind)</h2>
<ol start="10">
<li>The <code>TauriInvoke</code> promise resolves with file content as a <code>Uint8Array</code>.</li>
<li><code>TextFileEditorModel.load()</code> succeeds. The model is hydrated.</li>
<li><code>EditorGroupsService</code> creates a <code>TextEditorPane</code> and sets the
<code>TextFileEditorModel</code> on the Monaco editor instance.</li>
<li>Monaco renders the text content to screen.</li>
</ol>
<h2 id="key-source-files">Key Source Files</h2>
<ul>
<li><code>Wind/Source/Application/Editor/Definition.ts</code> — editor service</li>
<li><code>Wind/Source/Application/FileSystem/Definition.ts</code> — filesystem provider</li>
<li><code>Wind/Integration/Tauri/Wrap/ReadFile.ts</code> — Tauri invoke effect</li>
<li><code>Mountain/src/main.rs</code> — Tauri fs plugin</li>
</ul>
<hr>
<h2 id="see-also">See Also</h2>
<ul>
<li><a href="https://Editor.Land/Doc/workflow-startup">Application Startup and Handshake</a></li>
<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>
</ul>`, { headings: 2789, localImagePaths: 2807, remoteImagePaths: 2808, frontmatter: 2809, imagePaths: 2810 }, [2790, 2792, 2793, 2796, 2799, 2802, 2805, 2806], { depth: 25, slug: 2791, text: 2781 }, "opening-a-file-from-the-ui", { depth: 29, slug: 744, text: 2682 }, { depth: 29, slug: 2794, text: 2795 }, "phase-1-ui-interaction-windsky", "Phase 1: UI Interaction (Wind/Sky)", { depth: 29, slug: 2797, text: 2798 }, "phase-2-editor-and-filesystem-logic-wind---mountain", "Phase 2: Editor and Filesystem Logic (Wind -> Mountain)", { depth: 29, slug: 2800, text: 2801 }, "phase-3-native-file-io-mountain", "Phase 3: Native File I/O (Mountain)", { depth: 29, slug: 2803, text: 2804 }, "phase-4-ui-rendering-wind", "Phase 4: UI Rendering (Wind)", { depth: 29, slug: 2699, text: 2700 }, { depth: 29, slug: 446, text: 447 }, [], [], { title: 2781, section: 2670, order: 194, description: 2782 }, [], "workflow-save-participants", { id: 2811, data: 2813, body: 2816, filePath: 2817, digest: 2818, rendered: 2819 }, { title: 2814, section: 2670, order: 382, description: 2815 }, "Saving a File with Save Participants", "Intercepting a save event, allowing an extension to modify a file (e.g. formatting) before Mountain writes it to disk.", "# Saving a File with Save Participants\n\nExplains the process of intercepting a save event, allowing an extension in\nCocoon to modify a file (e.g. for formatting) before Mountain writes it to disk.\n\n## Data Flow\n\n```\nUser presses Ctrl+S (editor is dirty)\n  -> workbench.action.files.save command\n  -> IEditorService.save()\n  -> TextFileEditorModelManager.save()\n  -> WorkingCopyFileService.runSaveParticipants()\n  -> Gather ISaveParticipants\n  -> ExtHostSaveParticipant.participate()\n  -> Wind sends $participateInSave gRPC to Cocoon\n\nCocoon:\n  -> ExtHostDocumentSaveParticipant fires onWillSaveTextDocument\n  -> Prettier extension calculates formatting edits\n  -> Extension returns Promise<TextEdit[]>\n  -> All extensions resolve, edits collected\n  -> Serialize TextEdits to DTOs\n  -> Return array of TextEdit DTOs to Wind\n\nWind/UI:\n  -> BulkEditService applies TextEdits to document model in memory\n  -> TextFileEditorModelManager proceeds to save\n  -> IFileService.writeFile()\n  -> TauriDiskFileSystemProvider -> WriteFile Effect -> TauriInvoke\n\nMountain:\n  -> FsWriter receives call\n  -> tokio::fs::write(path, content)\n  -> Success returned up the chain\n\nWind/UI:\n  -> TextFileEditorModel no longer dirty\n  -> UI removes filled-circle indicator from editor tab\n  -> Save complete\n```\n\n## Phase 1: User Action and Initial Save Trigger (Wind/Sky)\n\n1. User presses `Ctrl+S` in a dirty editor. `workbench.action.files.save`\n   dispatched.\n2. `IEditorService.save()` identifies the active editor and calls `save` on its\n   `EditorInput`.\n3. `TextFileEditorModelManager` does not immediately write to disk. It enters\n   the Save Participants phase.\n4. Emits a `willSave` event, orchestrated by `IWorkingCopyFileService`.\n\n## Phase 2: Orchestration via Save Participants (Wind -> Cocoon -> Wind)\n\n5. `WorkingCopyFileService.runSaveParticipants()` gathers all registered\n   `ISaveParticipants`. One is `ExtHostSaveParticipant`, responsible for\n   communicating with extensions in Cocoon.\n6. Calls `participate()` on `ExtHostSaveParticipant`, which makes a\n   `$participateInSave` gRPC request to Cocoon with the document URI and save\n   reason.\n\n## Phase 3: Extension Execution (Cocoon)\n\n7. Cocoon's gRPC server dispatches to `ExtHostDocumentSaveParticipant`.\n8. Service fires `onWillSaveTextDocument` event to all subscribed extensions.\n9. Prettier extension calculates formatting edits, returns\n   `Promise<TextEdit[]>`.\n10. Service collects all results, serializes `TextEdit` objects to DTOs via\n    `TypeConverter`, returns to Wind.\n\n## Phase 4: Applying Edits and Final Save (Wind -> Mountain -> Disk)\n\n11. gRPC call resolves with text edits from extensions.\n12. `BulkEditService` applies the `TextEdit`s to the document model in memory.\n    Editor content now reflects formatted code.\n13. `TextFileEditorModelManager` proceeds to call `IFileService.writeFile()`.\n14. Save follows the same path as Opening a File (Workflow #2): `IFileService`\n    -> `TauriDiskFileSystemProvider` -> `WriteFile` Effect -> `TauriInvoke` ->\n    Mountain.\n15. `tokio::fs::write(path, content)` writes the formatted document to disk.\n16. `TextFileEditorModel` is no longer dirty. UI removes the dirty indicator.\n\n## Key Source Files\n\n- `Wind/Source/Application/BulkEdit/Live.ts` -- bulk edit service\n- `Wind/Source/Application/File/Live.ts` -- file service\n- `Mountain/Source/FileSystem/` -- filesystem providers\n\n---\n\n## See Also\n\n- [Opening a File from the UI](https://Editor.Land/Doc/workflow-open-file)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)", "Source/Content/doc/workflow-save-participants.md", "bd389efc88c8c3bc", { html: 2820, metadata: 2821 }, '<h1 id="saving-a-file-with-save-participants">Saving a File with Save Participants</h1>\n<p>Explains the process of intercepting a save event, allowing an extension in\nCocoon to modify a file (e.g. for formatting) before Mountain writes it to disk.</p>\n<h2 id="data-flow">Data Flow</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>User presses Ctrl+S (editor is dirty)</span></span>\n<span class="line"><span>  -> workbench.action.files.save command</span></span>\n<span class="line"><span>  -> IEditorService.save()</span></span>\n<span class="line"><span>  -> TextFileEditorModelManager.save()</span></span>\n<span class="line"><span>  -> WorkingCopyFileService.runSaveParticipants()</span></span>\n<span class="line"><span>  -> Gather ISaveParticipants</span></span>\n<span class="line"><span>  -> ExtHostSaveParticipant.participate()</span></span>\n<span class="line"><span>  -> Wind sends $participateInSave gRPC to Cocoon</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>Cocoon:</span></span>\n<span class="line"><span>  -> ExtHostDocumentSaveParticipant fires onWillSaveTextDocument</span></span>\n<span class="line"><span>  -> Prettier extension calculates formatting edits</span></span>\n<span class="line"><span>  -> Extension returns Promise&#x3C;TextEdit[]></span></span>\n<span class="line"><span>  -> All extensions resolve, edits collected</span></span>\n<span class="line"><span>  -> Serialize TextEdits to DTOs</span></span>\n<span class="line"><span>  -> Return array of TextEdit DTOs to Wind</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>Wind/UI:</span></span>\n<span class="line"><span>  -> BulkEditService applies TextEdits to document model in memory</span></span>\n<span class="line"><span>  -> TextFileEditorModelManager proceeds to save</span></span>\n<span class="line"><span>  -> IFileService.writeFile()</span></span>\n<span class="line"><span>  -> TauriDiskFileSystemProvider -> WriteFile Effect -> TauriInvoke</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>Mountain:</span></span>\n<span class="line"><span>  -> FsWriter receives call</span></span>\n<span class="line"><span>  -> tokio::fs::write(path, content)</span></span>\n<span class="line"><span>  -> Success returned up the chain</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>Wind/UI:</span></span>\n<span class="line"><span>  -> TextFileEditorModel no longer dirty</span></span>\n<span class="line"><span>  -> UI removes filled-circle indicator from editor tab</span></span>\n<span class="line"><span>  -> Save complete</span></span></code></pre>\n<h2 id="phase-1-user-action-and-initial-save-trigger-windsky">Phase 1: User Action and Initial Save Trigger (Wind/Sky)</h2>\n<ol>\n<li>User presses <code>Ctrl+S</code> in a dirty editor. <code>workbench.action.files.save</code>\ndispatched.</li>\n<li><code>IEditorService.save()</code> identifies the active editor and calls <code>save</code> on its\n<code>EditorInput</code>.</li>\n<li><code>TextFileEditorModelManager</code> does not immediately write to disk. It enters\nthe Save Participants phase.</li>\n<li>Emits a <code>willSave</code> event, orchestrated by <code>IWorkingCopyFileService</code>.</li>\n</ol>\n<h2 id="phase-2-orchestration-via-save-participants-wind---cocoon---wind">Phase 2: Orchestration via Save Participants (Wind -> Cocoon -> Wind)</h2>\n<ol start="5">\n<li><code>WorkingCopyFileService.runSaveParticipants()</code> gathers all registered\n<code>ISaveParticipants</code>. One is <code>ExtHostSaveParticipant</code>, responsible for\ncommunicating with extensions in Cocoon.</li>\n<li>Calls <code>participate()</code> on <code>ExtHostSaveParticipant</code>, which makes a\n<code>$participateInSave</code> gRPC request to Cocoon with the document URI and save\nreason.</li>\n</ol>\n<h2 id="phase-3-extension-execution-cocoon">Phase 3: Extension Execution (Cocoon)</h2>\n<ol start="7">\n<li>Cocoon’s gRPC server dispatches to <code>ExtHostDocumentSaveParticipant</code>.</li>\n<li>Service fires <code>onWillSaveTextDocument</code> event to all subscribed extensions.</li>\n<li>Prettier extension calculates formatting edits, returns\n<code>Promise&#x3C;TextEdit[]></code>.</li>\n<li>Service collects all results, serializes <code>TextEdit</code> objects to DTOs via\n<code>TypeConverter</code>, returns to Wind.</li>\n</ol>\n<h2 id="phase-4-applying-edits-and-final-save-wind---mountain---disk">Phase 4: Applying Edits and Final Save (Wind -> Mountain -> Disk)</h2>\n<ol start="11">\n<li>gRPC call resolves with text edits from extensions.</li>\n<li><code>BulkEditService</code> applies the <code>TextEdit</code>s to the document model in memory.\nEditor content now reflects formatted code.</li>\n<li><code>TextFileEditorModelManager</code> proceeds to call <code>IFileService.writeFile()</code>.</li>\n<li>Save follows the same path as Opening a File (Workflow #2): <code>IFileService</code>\n-> <code>TauriDiskFileSystemProvider</code> -> <code>WriteFile</code> Effect -> <code>TauriInvoke</code> ->\nMountain.</li>\n<li><code>tokio::fs::write(path, content)</code> writes the formatted document to disk.</li>\n<li><code>TextFileEditorModel</code> is no longer dirty. UI removes the dirty indicator.</li>\n</ol>\n<h2 id="key-source-files">Key Source Files</h2>\n<ul>\n<li><code>Wind/Source/Application/BulkEdit/Live.ts</code> — bulk edit service</li>\n<li><code>Wind/Source/Application/File/Live.ts</code> — file service</li>\n<li><code>Mountain/Source/FileSystem/</code> — filesystem providers</li>\n</ul>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/workflow-open-file">Opening a File from the UI</a></li>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n</ul>', { headings: 2822, localImagePaths: 2840, remoteImagePaths: 2841, frontmatter: 2842, imagePaths: 2843 }, [2823, 2825, 2826, 2829, 2832, 2835, 2838, 2839], { depth: 25, slug: 2824, text: 2814 }, "saving-a-file-with-save-participants", { depth: 29, slug: 744, text: 2682 }, { depth: 29, slug: 2827, text: 2828 }, "phase-1-user-action-and-initial-save-trigger-windsky", "Phase 1: User Action and Initial Save Trigger (Wind/Sky)", { depth: 29, slug: 2830, text: 2831 }, "phase-2-orchestration-via-save-participants-wind---cocoon---wind", "Phase 2: Orchestration via Save Participants (Wind -> Cocoon -> Wind)", { depth: 29, slug: 2833, text: 2834 }, "phase-3-extension-execution-cocoon", "Phase 3: Extension Execution (Cocoon)", { depth: 29, slug: 2836, text: 2837 }, "phase-4-applying-edits-and-final-save-wind---mountain---disk", "Phase 4: Applying Edits and Final Save (Wind -> Mountain -> Disk)", { depth: 29, slug: 2699, text: 2700 }, { depth: 29, slug: 446, text: 447 }, [], [], { title: 2814, section: 2670, order: 382, description: 2815 }, [], "workflow-scm", { id: 2844, data: 2846, body: 2849, filePath: 2850, digest: 2851, rendered: 2852 }, { title: 2847, section: 2670, order: 2308, description: 2848 }, "Source Control Management (SCM)", "Git extension in Cocoon uses Mountain to run native git commands and populate the SCM view in the UI.", "# Source Control Management (SCM)\n\nOutlines how the built-in Git extension in Cocoon uses Mountain as a service to\nrun native `git` commands and populate the SCM view in the UI.\n\nVerify against `Cocoon/Source/Services/Extension.ts` for extension activation\nand Mountain's Git provider implementation.\n\n## Data Flow\n\n```\nPhase 1: Registration and Discovery\n  Git Extension activate()\n  -> vscode.scm.createSourceControl('git', 'Git')\n  -> ScmProvider service in Cocoon sends $registerScmProvider gRPC to Mountain\n  -> vscode.workspace.workspaceFolders API to get root path\n  -> vscode.workspace.fs.stat(Uri.joinPath(root, '.git')) -- gRPC to Mountain\n  -> Mountain: tokio::fs::metadata -- .git directory confirmed\n\nPhase 2: Populate SCM View\n  -> Git extension runs: git status --porcelain -z\n  -> $gitExec gRPC to Mountain\n  -> Mountain GitProvider spawns git process, captures stdout\n  -> Returns raw output to Cocoon\n  -> Git extension parses output, builds list of changed files\n  -> Creates SourceControlResourceState objects (URI + decorations)\n  -> Updates SourceControl resourceGroups\n  -> $updateScmGroup gRPC notification to Mountain\n  -> Mountain emits sky://scm/update-group Tauri event\n\nPhase 3: UI Rendering\n  -> SCM View component receives update-group event\n  -> UI re-renders file list (e.g. \"M src/main.ts\")\n\nPhase 4: Diff View\n  User clicks changed file\n  -> vscode.open with git: URI (e.g. git:/src/main.ts?{\"ref\":\"HEAD\"})\n  -> EditorService creates DiffEditorInput\n  -> Modified side: file:// URI, IFileService.read (standard read)\n  -> Original side: git: URI, Content Provider calls $gitExec\n  -> Mountain: git show HEAD:file\n  -> Returns original content to Wind\n  -> DiffEditor opens with both sides\n  -> User sees side-by-side diff\n```\n\n## Phase 1: Extension Registration and Repository Discovery (Cocoon)\n\n1. Cocoon activates the built-in Git extension.\n2. Git extension calls `vscode.scm.createSourceControl('git', 'Git')`.\n   `ScmProvider` service manages state and sends `$registerScmProvider` gRPC to\n   Mountain.\n3. Git extension uses `vscode.workspace.workspaceFolders` to get root path. Then\n   checks for `.git` directory via `vscode.workspace.fs.stat(...)`, which routes\n   through Cocoon's FileSystemProvider over gRPC to Mountain's\n   `tokio::fs::metadata`.\n\n## Phase 2: Populating the SCM View (Cocoon -> Mountain)\n\n4. Git extension needs file status. Uses a custom `vscode.git` API exposed by\n   Mountain, which translates to a `$gitExec` gRPC request.\n5. Mountain's `GitProvider` safely spawns a native `git` child process with the\n   provided arguments, captures stdout, returns raw string.\n6. Git extension parses `git status` output, creates\n   `SourceControlResourceState` objects, updates `resourceGroups`.\n7. `ScmProvider` in Cocoon serializes changes and sends `$updateScmGroup` gRPC\n   notification to Mountain.\n8. Mountain updates `AppState.ActiveScmProviders` and emits\n   `sky://scm/update-group` Tauri event.\n\n## Phase 3: Rendering the SCM View (Wind/Sky)\n\n9. SCM View component in Wind receives the `sky://scm/update-group` event and\n   re-renders with the list of modified files.\n\n## Phase 4: Viewing a Diff\n\n10. User clicks a changed file in the SCM view. `vscode.open` executes with a\n    `git:` scheme URI.\n11. `EditorService` creates a `DiffEditorInput`:\n    - Modified side: loads from workspace `file://` URI via `IFileService`\n      (standard read).\n    - Original side: `git:` scheme Content Provider sends `$gitExec` to Mountain\n      with `git show HEAD:path/to/file`.\n12. Mountain runs the command, returns original content. DiffEditor opens with\n    both sides.\n\n## Key Source Files\n\n- `Cocoon/Source/Services/Extension.ts` -- extension activation\n- `Mountain/Source/Environment/SynchronizationProvider.rs` -- Git provider\n  (verify)\n- `Mountain/Source/Environment/LanguageFeatureProvider/Registration.rs` --\n  provider registry\n\n---\n\n## See Also\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)\n- [VS Code API Coverage](https://Editor.Land/Doc/vscode-api-coverage)", "Source/Content/doc/workflow-scm.md", "3e7fb998bf3d1651", { html: 2853, metadata: 2854 }, `<h1 id="source-control-management-scm">Source Control Management (SCM)</h1>
<p>Outlines how the built-in Git extension in Cocoon uses Mountain as a service to
run native <code>git</code> commands and populate the SCM view in the UI.</p>
<p>Verify against <code>Cocoon/Source/Services/Extension.ts</code> for extension activation
and Mountain’s Git provider implementation.</p>
<h2 id="data-flow">Data Flow</h2>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>Phase 1: Registration and Discovery</span></span>
<span class="line"><span>  Git Extension activate()</span></span>
<span class="line"><span>  -> vscode.scm.createSourceControl('git', 'Git')</span></span>
<span class="line"><span>  -> ScmProvider service in Cocoon sends $registerScmProvider gRPC to Mountain</span></span>
<span class="line"><span>  -> vscode.workspace.workspaceFolders API to get root path</span></span>
<span class="line"><span>  -> vscode.workspace.fs.stat(Uri.joinPath(root, '.git')) -- gRPC to Mountain</span></span>
<span class="line"><span>  -> Mountain: tokio::fs::metadata -- .git directory confirmed</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Phase 2: Populate SCM View</span></span>
<span class="line"><span>  -> Git extension runs: git status --porcelain -z</span></span>
<span class="line"><span>  -> $gitExec gRPC to Mountain</span></span>
<span class="line"><span>  -> Mountain GitProvider spawns git process, captures stdout</span></span>
<span class="line"><span>  -> Returns raw output to Cocoon</span></span>
<span class="line"><span>  -> Git extension parses output, builds list of changed files</span></span>
<span class="line"><span>  -> Creates SourceControlResourceState objects (URI + decorations)</span></span>
<span class="line"><span>  -> Updates SourceControl resourceGroups</span></span>
<span class="line"><span>  -> $updateScmGroup gRPC notification to Mountain</span></span>
<span class="line"><span>  -> Mountain emits sky://scm/update-group Tauri event</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Phase 3: UI Rendering</span></span>
<span class="line"><span>  -> SCM View component receives update-group event</span></span>
<span class="line"><span>  -> UI re-renders file list (e.g. "M src/main.ts")</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Phase 4: Diff View</span></span>
<span class="line"><span>  User clicks changed file</span></span>
<span class="line"><span>  -> vscode.open with git: URI (e.g. git:/src/main.ts?{"ref":"HEAD"})</span></span>
<span class="line"><span>  -> EditorService creates DiffEditorInput</span></span>
<span class="line"><span>  -> Modified side: file:// URI, IFileService.read (standard read)</span></span>
<span class="line"><span>  -> Original side: git: URI, Content Provider calls $gitExec</span></span>
<span class="line"><span>  -> Mountain: git show HEAD:file</span></span>
<span class="line"><span>  -> Returns original content to Wind</span></span>
<span class="line"><span>  -> DiffEditor opens with both sides</span></span>
<span class="line"><span>  -> User sees side-by-side diff</span></span></code></pre>
<h2 id="phase-1-extension-registration-and-repository-discovery-cocoon">Phase 1: Extension Registration and Repository Discovery (Cocoon)</h2>
<ol>
<li>Cocoon activates the built-in Git extension.</li>
<li>Git extension calls <code>vscode.scm.createSourceControl('git', 'Git')</code>.
<code>ScmProvider</code> service manages state and sends <code>$registerScmProvider</code> gRPC to
Mountain.</li>
<li>Git extension uses <code>vscode.workspace.workspaceFolders</code> to get root path. Then
checks for <code>.git</code> directory via <code>vscode.workspace.fs.stat(...)</code>, which routes
through Cocoon’s FileSystemProvider over gRPC to Mountain’s
<code>tokio::fs::metadata</code>.</li>
</ol>
<h2 id="phase-2-populating-the-scm-view-cocoon---mountain">Phase 2: Populating the SCM View (Cocoon -> Mountain)</h2>
<ol start="4">
<li>Git extension needs file status. Uses a custom <code>vscode.git</code> API exposed by
Mountain, which translates to a <code>$gitExec</code> gRPC request.</li>
<li>Mountain’s <code>GitProvider</code> safely spawns a native <code>git</code> child process with the
provided arguments, captures stdout, returns raw string.</li>
<li>Git extension parses <code>git status</code> output, creates
<code>SourceControlResourceState</code> objects, updates <code>resourceGroups</code>.</li>
<li><code>ScmProvider</code> in Cocoon serializes changes and sends <code>$updateScmGroup</code> gRPC
notification to Mountain.</li>
<li>Mountain updates <code>AppState.ActiveScmProviders</code> and emits
<code>sky://scm/update-group</code> Tauri event.</li>
</ol>
<h2 id="phase-3-rendering-the-scm-view-windsky">Phase 3: Rendering the SCM View (Wind/Sky)</h2>
<ol start="9">
<li>SCM View component in Wind receives the <code>sky://scm/update-group</code> event and
re-renders with the list of modified files.</li>
</ol>
<h2 id="phase-4-viewing-a-diff">Phase 4: Viewing a Diff</h2>
<ol start="10">
<li>User clicks a changed file in the SCM view. <code>vscode.open</code> executes with a
<code>git:</code> scheme URI.</li>
<li><code>EditorService</code> creates a <code>DiffEditorInput</code>:
<ul>
<li>Modified side: loads from workspace <code>file://</code> URI via <code>IFileService</code>
(standard read).</li>
<li>Original side: <code>git:</code> scheme Content Provider sends <code>$gitExec</code> to Mountain
with <code>git show HEAD:path/to/file</code>.</li>
</ul>
</li>
<li>Mountain runs the command, returns original content. DiffEditor opens with
both sides.</li>
</ol>
<h2 id="key-source-files">Key Source Files</h2>
<ul>
<li><code>Cocoon/Source/Services/Extension.ts</code> — extension activation</li>
<li><code>Mountain/Source/Environment/SynchronizationProvider.rs</code> — Git provider
(verify)</li>
<li><code>Mountain/Source/Environment/LanguageFeatureProvider/Registration.rs</code> —
provider registry</li>
</ul>
<hr>
<h2 id="see-also">See Also</h2>
<ul>
<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>
<li><a href="https://Editor.Land/Doc/vscode-api-coverage">VS Code API Coverage</a></li>
</ul>`, { headings: 2855, localImagePaths: 2873, remoteImagePaths: 2874, frontmatter: 2875, imagePaths: 2876 }, [2856, 2858, 2859, 2862, 2865, 2868, 2871, 2872], { depth: 25, slug: 2857, text: 2847 }, "source-control-management-scm", { depth: 29, slug: 744, text: 2682 }, { depth: 29, slug: 2860, text: 2861 }, "phase-1-extension-registration-and-repository-discovery-cocoon", "Phase 1: Extension Registration and Repository Discovery (Cocoon)", { depth: 29, slug: 2863, text: 2864 }, "phase-2-populating-the-scm-view-cocoon---mountain", "Phase 2: Populating the SCM View (Cocoon -> Mountain)", { depth: 29, slug: 2866, text: 2867 }, "phase-3-rendering-the-scm-view-windsky", "Phase 3: Rendering the SCM View (Wind/Sky)", { depth: 29, slug: 2869, text: 2870 }, "phase-4-viewing-a-diff", "Phase 4: Viewing a Diff", { depth: 29, slug: 2699, text: 2700 }, { depth: 29, slug: 446, text: 447 }, [], [], { title: 2847, section: 2670, order: 2308, description: 2848 }, [], "workflow-startup", { id: 2877, data: 2879, body: 2882, filePath: 2883, digest: 2884, rendered: 2885 }, { title: 2880, section: 2670, order: 691, description: 2881 }, "Application Startup and Handshake", "End-to-end process of launching Mountain, spawning Cocoon, and establishing initialized state for UI and extension host.", "# Application Startup and Handshake\n\nLaunches the Land application from native `Mountain` backend through `Wind` UI\nand `Cocoon` extension host. This is the foundational workflow that enables all\nothers.\n\n## Startup Sequence\n\n```\nUser -> Mountain: Launch Application\nMountain: Create AppState and AppRuntime\nMountain: Load configuration from settings.json\nMountain: Scan extensions and load manifests\nMountain: Start gRPC server\nMountain: Spawn Node.js process (bootstrap-fork.js)\nCocoon: Run process patches (console piping, etc.)\nCocoon: Start gRPC client\nCocoon: Send $initialHandshake gRPC notification to Mountain\nMountain: Gather InitData from AppState\nMountain: Send initExtensionHost gRPC request to Cocoon\nCocoon: Create InitDataLayer\nCocoon: Run FullAppInitialization effect\nCocoon: Install RequireInterceptor (patch require())\nCocoon: Activate startup extensions\nUI: Preload.ts shims window.vscode with Tauri IPC + process\nUI: Create AppLayer services (Clipboard, Dialog, Editor)\nUI: Instantiate VS Code Workbench class\nUI: Render UI parts (Activity Bar, Sidebar, etc.)\n```\n\n## Phase 1: Mountain Startup\n\n1. Tauri's `Builder` is created, `.setup()` hook configured.\n2. `AppState` and `MountainEnvironment` created. The environment implements all\n   `Common` traits.\n3. `AppRuntime` wraps the environment as the effect engine.\n4. Background task loads `settings.json`, scans extensions, starts gRPC server\n   (`vine::server::Initialize`).\n5. `InitializeCocoon` handler constructs the sidecar environment and spawns\n   `node ./Element/Cocoon/Scripts/cocoon/bootstrap-fork.js`.\n\n## Phase 2: Cocoon Handshake\n\n1. Cocoon runs `RunProcessPatches`, starts its gRPC client via `IpcProvider`.\n2. On connection, sends `$initialHandshake` gRPC notification to Mountain.\n3. Mountain receives the handshake, gathers `InitializationData` from `AppState`\n   (workspace info, extensions, configuration).\n4. Mountain sends `initExtensionHost` gRPC request with the full initialization\n   payload.\n5. Cocoon creates `InitDataLayer`, runs `FullAppInitialization`, installs\n   `RequireInterceptor`.\n6. `ExtensionHostProvider` activates startup extensions (`*` activation event).\n\n## Phase 3: UI Launch\n\n1. Tauri window opens, loads `index.html`.\n2. `Preload.ts` executes, shimming `window.vscode` global with Tauri-backed\n   `ipcRenderer` and `process`.\n3. Main UI script creates the `AppLayer` composing all Wind services.\n4. `Workbench.startup()` is called, rendering Activity Bar, Status Bar, Sidebar,\n   Editor Part.\n5. Application is fully initialized and ready for user interaction.\n\n## Key Source Files\n\n- `Mountain/Source/Binary/Main/Entry.rs` -- main entry\n- `Mountain/Source/ProcessManagement/CocoonManagement.rs` -- sidecar spawning\n- `Mountain/Source/ProcessManagement/InitializationData.rs` -- init payload\n- `Cocoon/Scripts/cocoon/bootstrap-fork.js` -- extension host bootstrap\n- `Wind/Source/Preload.ts` -- VS Code global shim\n\n---\n\n## See Also\n\n- [Opening a File from the UI](https://Editor.Land/Doc/workflow-open-file)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)", "Source/Content/doc/workflow-startup.md", "75c2e37f330094f2", { html: 2886, metadata: 2887 }, '<h1 id="application-startup-and-handshake">Application Startup and Handshake</h1>\n<p>Launches the Land application from native <code>Mountain</code> backend through <code>Wind</code> UI\nand <code>Cocoon</code> extension host. This is the foundational workflow that enables all\nothers.</p>\n<h2 id="startup-sequence">Startup Sequence</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>User -> Mountain: Launch Application</span></span>\n<span class="line"><span>Mountain: Create AppState and AppRuntime</span></span>\n<span class="line"><span>Mountain: Load configuration from settings.json</span></span>\n<span class="line"><span>Mountain: Scan extensions and load manifests</span></span>\n<span class="line"><span>Mountain: Start gRPC server</span></span>\n<span class="line"><span>Mountain: Spawn Node.js process (bootstrap-fork.js)</span></span>\n<span class="line"><span>Cocoon: Run process patches (console piping, etc.)</span></span>\n<span class="line"><span>Cocoon: Start gRPC client</span></span>\n<span class="line"><span>Cocoon: Send $initialHandshake gRPC notification to Mountain</span></span>\n<span class="line"><span>Mountain: Gather InitData from AppState</span></span>\n<span class="line"><span>Mountain: Send initExtensionHost gRPC request to Cocoon</span></span>\n<span class="line"><span>Cocoon: Create InitDataLayer</span></span>\n<span class="line"><span>Cocoon: Run FullAppInitialization effect</span></span>\n<span class="line"><span>Cocoon: Install RequireInterceptor (patch require())</span></span>\n<span class="line"><span>Cocoon: Activate startup extensions</span></span>\n<span class="line"><span>UI: Preload.ts shims window.vscode with Tauri IPC + process</span></span>\n<span class="line"><span>UI: Create AppLayer services (Clipboard, Dialog, Editor)</span></span>\n<span class="line"><span>UI: Instantiate VS Code Workbench class</span></span>\n<span class="line"><span>UI: Render UI parts (Activity Bar, Sidebar, etc.)</span></span></code></pre>\n<h2 id="phase-1-mountain-startup">Phase 1: Mountain Startup</h2>\n<ol>\n<li>Tauri’s <code>Builder</code> is created, <code>.setup()</code> hook configured.</li>\n<li><code>AppState</code> and <code>MountainEnvironment</code> created. The environment implements all\n<code>Common</code> traits.</li>\n<li><code>AppRuntime</code> wraps the environment as the effect engine.</li>\n<li>Background task loads <code>settings.json</code>, scans extensions, starts gRPC server\n(<code>vine::server::Initialize</code>).</li>\n<li><code>InitializeCocoon</code> handler constructs the sidecar environment and spawns\n<code>node ./Element/Cocoon/Scripts/cocoon/bootstrap-fork.js</code>.</li>\n</ol>\n<h2 id="phase-2-cocoon-handshake">Phase 2: Cocoon Handshake</h2>\n<ol>\n<li>Cocoon runs <code>RunProcessPatches</code>, starts its gRPC client via <code>IpcProvider</code>.</li>\n<li>On connection, sends <code>$initialHandshake</code> gRPC notification to Mountain.</li>\n<li>Mountain receives the handshake, gathers <code>InitializationData</code> from <code>AppState</code>\n(workspace info, extensions, configuration).</li>\n<li>Mountain sends <code>initExtensionHost</code> gRPC request with the full initialization\npayload.</li>\n<li>Cocoon creates <code>InitDataLayer</code>, runs <code>FullAppInitialization</code>, installs\n<code>RequireInterceptor</code>.</li>\n<li><code>ExtensionHostProvider</code> activates startup extensions (<code>*</code> activation event).</li>\n</ol>\n<h2 id="phase-3-ui-launch">Phase 3: UI Launch</h2>\n<ol>\n<li>Tauri window opens, loads <code>index.html</code>.</li>\n<li><code>Preload.ts</code> executes, shimming <code>window.vscode</code> global with Tauri-backed\n<code>ipcRenderer</code> and <code>process</code>.</li>\n<li>Main UI script creates the <code>AppLayer</code> composing all Wind services.</li>\n<li><code>Workbench.startup()</code> is called, rendering Activity Bar, Status Bar, Sidebar,\nEditor Part.</li>\n<li>Application is fully initialized and ready for user interaction.</li>\n</ol>\n<h2 id="key-source-files">Key Source Files</h2>\n<ul>\n<li><code>Mountain/Source/Binary/Main/Entry.rs</code> — main entry</li>\n<li><code>Mountain/Source/ProcessManagement/CocoonManagement.rs</code> — sidecar spawning</li>\n<li><code>Mountain/Source/ProcessManagement/InitializationData.rs</code> — init payload</li>\n<li><code>Cocoon/Scripts/cocoon/bootstrap-fork.js</code> — extension host bootstrap</li>\n<li><code>Wind/Source/Preload.ts</code> — VS Code global shim</li>\n</ul>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/workflow-open-file">Opening a File from the UI</a></li>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n</ul>', { headings: 2888, localImagePaths: 2904, remoteImagePaths: 2905, frontmatter: 2906, imagePaths: 2907 }, [2889, 2891, 2893, 2896, 2899, 2902, 2903], { depth: 25, slug: 2890, text: 2880 }, "application-startup-and-handshake", { depth: 29, slug: 957, text: 2892 }, "Startup Sequence", { depth: 29, slug: 2894, text: 2895 }, "phase-1-mountain-startup", "Phase 1: Mountain Startup", { depth: 29, slug: 2897, text: 2898 }, "phase-2-cocoon-handshake", "Phase 2: Cocoon Handshake", { depth: 29, slug: 2900, text: 2901 }, "phase-3-ui-launch", "Phase 3: UI Launch", { depth: 29, slug: 2699, text: 2700 }, { depth: 29, slug: 446, text: 447 }, [], [], { title: 2880, section: 2670, order: 691, description: 2881 }, [], "workflow-terminal", { id: 2908, data: 2910, body: 2913, filePath: 2914, digest: 2915, rendered: 2916 }, { title: 2911, section: 2670, order: 548, description: 2912 }, "Creating and Interacting with an Integrated Terminal", "Mountain spawns a PTY process and streams I/O to the Wind frontend and Cocoon extension host.", "# Creating and Interacting with an Integrated Terminal\n\nDeep dive into native process management: Mountain spawns a PTY process and\nstreams its I/O to both the Wind frontend and the Cocoon extension host.\n\n## Data Flow\n\n```\nExtension calls vscode.window.createTerminal()\n  OR user executes terminal:new command\n  -> Request routed to Mountain TerminalProvider\n\nMountain:\n  -> TerminalProvider.CreateTerminal()\n  -> Generate unique TerminalId\n  -> Determine shell (from options or system default)\n  -> portable-pty: PtySystem.openpty() creates master/slave PTY pair\n  -> Spawn shell process (e.g. bash) as PTY child\n  -> Create TerminalStateDto (TerminalId, pid, reader/writer handles)\n  -> Spawn I/O tasks:\n       Writer Task: waits on mpsc channel, writes to PTY\n       Reader Task: reads PTY output, sends notifications\n       Waiter Task: monitors shell exit\n  -> Send $acceptTerminalOpened gRPC to Cocoon\n  -> Send $acceptTerminalProcessId gRPC to Cocoon\n\nCocoon:\n  -> Creates Terminal proxy object for extensions\n  -> Fires onDidWriteData events when data arrives\n\nMountain -> Wind/UI:\n  -> Reader Task emits Tauri event: sky://terminal/data\n\nUser Input Loop:\n  User types \"ls -la\" in terminal UI\n  -> Xterm.js captures keystrokes\n  -> TauriInvoke('mountain://terminal/send-text', { Id, Text })\n  -> Mountain: SendTextToTerminalLogic\n  -> Send text to mpsc sender channel\n  -> Writer Task writes bytes to PTY master\n  -> Shell executes command, writes to stdout\n  -> Reader Task captures output\n  -> Output sent to Cocoon and Wind/UI\n  -> xterm.write(data) displays output\n```\n\n## Phase 1: Terminal Creation Request (Cocoon or Wind)\n\n1. Extension calls `vscode.window.createTerminal(...)` or user runs\n   `workbench.action.terminal.new`.\n2. Request is routed to Mountain's `TerminalProvider` as\n   `Common::terminal::CreateTerminal`.\n\n## Phase 2: Native PTY Spawning (Mountain)\n\n3. `TerminalProvider.CreateTerminal()` gets a unique `TerminalId`, determines\n   the shell.\n4. Uses `portable-pty` to spawn a pseudo-terminal (`PtySystem.openpty`),\n   creating a master/slave PTY pair.\n5. Spawns the shell process as a child of the PTY slave.\n6. Creates `TerminalStateDto` with TerminalId, OS pid, and clones of the PTY\n   master's reader/writer handles.\n7. Spawns three tokio tasks:\n    - **Writer Task**: waits on `mpsc` channel, writes received strings to PTY\n      master.\n    - **Reader Task**: reads PTY output, sends `$acceptTerminalProcessData` gRPC\n      to Cocoon.\n    - **Waiter Task**: awaits shell termination, sends `$acceptTerminalClosed`\n      gRPC and cleans up.\n\n## Phase 3: UI Rendering and State Sync (Cocoon -> Wind/Sky)\n\n8. Terminal service in Cocoon creates a local `Terminal` proxy object for\n   extensions.\n9. Mountain's Reader Task emits `sky://terminal/data` Tauri event to Sky.\n10. Terminal component (wrapping Xterm.js) receives the event and calls\n    `xterm.write(data)`.\n\n## Phase 4: User Input (Wind/Sky -> Mountain -> Shell)\n\n11. User types in terminal UI. Xterm.js captures keystrokes.\n12. `TauriInvoke('mountain://terminal/send-text', { Id, Text })` sends input to\n    Mountain.\n13. `SendTextToTerminalLogic` looks up `TerminalStateDto` and sends text to the\n    mpsc channel.\n14. Writer Task writes bytes to PTY master. Shell executes command and produces\n    output.\n15. Reader Task captures output, loop continues.\n\n## Key Source Files\n\n- `Mountain/Source/Environment/TerminalProvider.rs` -- terminal creation and PTY\n  management\n- `Mountain/Source/handlers/terminal/TerminalLogic.rs` -- send text handler\n\n---\n\n## See Also\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)", "Source/Content/doc/workflow-terminal.md", "e7a10dca4ebb7b64", { html: 2917, metadata: 2918 }, `<h1 id="creating-and-interacting-with-an-integrated-terminal">Creating and Interacting with an Integrated Terminal</h1>
<p>Deep dive into native process management: Mountain spawns a PTY process and
streams its I/O to both the Wind frontend and the Cocoon extension host.</p>
<h2 id="data-flow">Data Flow</h2>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>Extension calls vscode.window.createTerminal()</span></span>
<span class="line"><span>  OR user executes terminal:new command</span></span>
<span class="line"><span>  -> Request routed to Mountain TerminalProvider</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Mountain:</span></span>
<span class="line"><span>  -> TerminalProvider.CreateTerminal()</span></span>
<span class="line"><span>  -> Generate unique TerminalId</span></span>
<span class="line"><span>  -> Determine shell (from options or system default)</span></span>
<span class="line"><span>  -> portable-pty: PtySystem.openpty() creates master/slave PTY pair</span></span>
<span class="line"><span>  -> Spawn shell process (e.g. bash) as PTY child</span></span>
<span class="line"><span>  -> Create TerminalStateDto (TerminalId, pid, reader/writer handles)</span></span>
<span class="line"><span>  -> Spawn I/O tasks:</span></span>
<span class="line"><span>       Writer Task: waits on mpsc channel, writes to PTY</span></span>
<span class="line"><span>       Reader Task: reads PTY output, sends notifications</span></span>
<span class="line"><span>       Waiter Task: monitors shell exit</span></span>
<span class="line"><span>  -> Send $acceptTerminalOpened gRPC to Cocoon</span></span>
<span class="line"><span>  -> Send $acceptTerminalProcessId gRPC to Cocoon</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Cocoon:</span></span>
<span class="line"><span>  -> Creates Terminal proxy object for extensions</span></span>
<span class="line"><span>  -> Fires onDidWriteData events when data arrives</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Mountain -> Wind/UI:</span></span>
<span class="line"><span>  -> Reader Task emits Tauri event: sky://terminal/data</span></span>
<span class="line"><span></span></span>
<span class="line"><span>User Input Loop:</span></span>
<span class="line"><span>  User types "ls -la" in terminal UI</span></span>
<span class="line"><span>  -> Xterm.js captures keystrokes</span></span>
<span class="line"><span>  -> TauriInvoke('mountain://terminal/send-text', { Id, Text })</span></span>
<span class="line"><span>  -> Mountain: SendTextToTerminalLogic</span></span>
<span class="line"><span>  -> Send text to mpsc sender channel</span></span>
<span class="line"><span>  -> Writer Task writes bytes to PTY master</span></span>
<span class="line"><span>  -> Shell executes command, writes to stdout</span></span>
<span class="line"><span>  -> Reader Task captures output</span></span>
<span class="line"><span>  -> Output sent to Cocoon and Wind/UI</span></span>
<span class="line"><span>  -> xterm.write(data) displays output</span></span></code></pre>
<h2 id="phase-1-terminal-creation-request-cocoon-or-wind">Phase 1: Terminal Creation Request (Cocoon or Wind)</h2>
<ol>
<li>Extension calls <code>vscode.window.createTerminal(...)</code> or user runs
<code>workbench.action.terminal.new</code>.</li>
<li>Request is routed to Mountain’s <code>TerminalProvider</code> as
<code>Common::terminal::CreateTerminal</code>.</li>
</ol>
<h2 id="phase-2-native-pty-spawning-mountain">Phase 2: Native PTY Spawning (Mountain)</h2>
<ol start="3">
<li><code>TerminalProvider.CreateTerminal()</code> gets a unique <code>TerminalId</code>, determines
the shell.</li>
<li>Uses <code>portable-pty</code> to spawn a pseudo-terminal (<code>PtySystem.openpty</code>),
creating a master/slave PTY pair.</li>
<li>Spawns the shell process as a child of the PTY slave.</li>
<li>Creates <code>TerminalStateDto</code> with TerminalId, OS pid, and clones of the PTY
master’s reader/writer handles.</li>
<li>Spawns three tokio tasks:
<ul>
<li><strong>Writer Task</strong>: waits on <code>mpsc</code> channel, writes received strings to PTY
master.</li>
<li><strong>Reader Task</strong>: reads PTY output, sends <code>$acceptTerminalProcessData</code> gRPC
to Cocoon.</li>
<li><strong>Waiter Task</strong>: awaits shell termination, sends <code>$acceptTerminalClosed</code>
gRPC and cleans up.</li>
</ul>
</li>
</ol>
<h2 id="phase-3-ui-rendering-and-state-sync-cocoon---windsky">Phase 3: UI Rendering and State Sync (Cocoon -> Wind/Sky)</h2>
<ol start="8">
<li>Terminal service in Cocoon creates a local <code>Terminal</code> proxy object for
extensions.</li>
<li>Mountain’s Reader Task emits <code>sky://terminal/data</code> Tauri event to Sky.</li>
<li>Terminal component (wrapping Xterm.js) receives the event and calls
<code>xterm.write(data)</code>.</li>
</ol>
<h2 id="phase-4-user-input-windsky---mountain---shell">Phase 4: User Input (Wind/Sky -> Mountain -> Shell)</h2>
<ol start="11">
<li>User types in terminal UI. Xterm.js captures keystrokes.</li>
<li><code>TauriInvoke('mountain://terminal/send-text', { Id, Text })</code> sends input to
Mountain.</li>
<li><code>SendTextToTerminalLogic</code> looks up <code>TerminalStateDto</code> and sends text to the
mpsc channel.</li>
<li>Writer Task writes bytes to PTY master. Shell executes command and produces
output.</li>
<li>Reader Task captures output, loop continues.</li>
</ol>
<h2 id="key-source-files">Key Source Files</h2>
<ul>
<li><code>Mountain/Source/Environment/TerminalProvider.rs</code> — terminal creation and PTY
management</li>
<li><code>Mountain/Source/handlers/terminal/TerminalLogic.rs</code> — send text handler</li>
</ul>
<hr>
<h2 id="see-also">See Also</h2>
<ul>
<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>
</ul>`, { headings: 2919, localImagePaths: 2937, remoteImagePaths: 2938, frontmatter: 2939, imagePaths: 2940 }, [2920, 2922, 2923, 2926, 2929, 2932, 2935, 2936], { depth: 25, slug: 2921, text: 2911 }, "creating-and-interacting-with-an-integrated-terminal", { depth: 29, slug: 744, text: 2682 }, { depth: 29, slug: 2924, text: 2925 }, "phase-1-terminal-creation-request-cocoon-or-wind", "Phase 1: Terminal Creation Request (Cocoon or Wind)", { depth: 29, slug: 2927, text: 2928 }, "phase-2-native-pty-spawning-mountain", "Phase 2: Native PTY Spawning (Mountain)", { depth: 29, slug: 2930, text: 2931 }, "phase-3-ui-rendering-and-state-sync-cocoon---windsky", "Phase 3: UI Rendering and State Sync (Cocoon -> Wind/Sky)", { depth: 29, slug: 2933, text: 2934 }, "phase-4-user-input-windsky---mountain---shell", "Phase 4: User Input (Wind/Sky -> Mountain -> Shell)", { depth: 29, slug: 2699, text: 2700 }, { depth: 29, slug: 446, text: 447 }, [], [], { title: 2911, section: 2670, order: 548, description: 2912 }, [], "workflow-user-data-sync", { id: 2941, data: 2943, body: 2946, filePath: 2947, digest: 2948, rendered: 2949 }, { title: 2944, section: 2670, order: 1663, description: 2945 }, "User Data Synchronization", "End-to-end sync of user settings: authentication, remote fetch, three-way merge, local apply, and notification of all components.", '# User Data Synchronization\n\nDescribes the process of syncing user settings: user authentication, fetching\ndata from a remote store, three-way merge, applying changes locally, and\nnotifying all parts of the application.\n\nVerify against `Mountain/Source/Environment/SynchronizationProvider.rs` for the\nactual implementation.\n\n## Data Flow\n\n```\nPhase 1: Authentication\n  User clicks "Sign In"\n  -> AuthenticationService triggers OAuth session request\n  -> Cocoon AuthenticationProvider sends $getSession gRPC to Mountain\n  -> Mountain handles OAuth flow (browser redirect, callback)\n  -> Token stored securely in keyring via SecretsProvider\n  -> User signed in, UserDataSyncAccountService has identity + token\n\nPhase 2: Triggering Synchronization\n  -> UserDataAutoSyncService triggered (after sign-in, manual, or interval)\n  -> Calls UserDataSyncService.sync()\n  -> Iterates through registered synchronizers (settings, extensions, etc.)\n\nPhase 3: Synchronizing Settings\n  -> SettingsSynchronizer.fetches remote settings via UserDataSyncStoreService\n  -> GET request to cloud endpoint with OAuth token\n  -> Remote server returns stored settings.json\n  -> Mountain reads local settings.json via FsReader\n  -> Three-way merge: local + remote + base (last sync state)\n  -> Resolves conflicts intelligently\n\nPhase 4: Applying Merged State\n  -> FsWriter writes merged settings.json to disk\n  -> ConfigurationService.reloadConfiguration()\n  -> Re-reads config files, reconstructs effective configuration\n  -> Updates AppState.Configuration\n  -> $acceptConfigurationChanged gRPC to Cocoon\n  -> sky://configuration/changed Tauri event to Wind\n\nPhase 5: UI and Extension Host React\n  -> Cocoon ConfigurationProvider updates cache\n  -> Fires onDidChangeConfiguration (vscode.workspace.onDidChangeConfiguration)\n  -> Wind settings UI re-renders with new values\n  -> Editor applies settings like fontSize\n  -> Extensions receive new settings, adjust behavior\n```\n\n## Phase 1: User Authentication (Wind/Sky -> Cocoon -> Mountain)\n\n1. User clicks "Sign In" in the Account menu.\n   `workbench.action.authentication.signIn` dispatched.\n2. `AuthenticationService` in Wind triggers a request to Cocoon\'s\n   `AuthenticationProvider`.\n3. Cocoon makes `$getSession` gRPC to Mountain. Mountain handles OAuth flow\n   (browser window, callback URI).\n4. OAuth token stored in system keyring via `SecretsProvider`.\n   `UserDataSyncAccountService` receives identity and token.\n\n## Phase 2: Triggering Synchronization (Mountain)\n\n5. `UserDataAutoSyncService` triggers (post-sign-in, manual, or scheduled).\n   Orchestrates what to sync and in order.\n6. Calls `UserDataSyncService.sync()`, which iterates through registered\n   synchronizers (settings, extensions, keybindings, snippets).\n\n## Phase 3: Three-Way Merge (Mountain)\n\n7. `SettingsSynchronizer` fetches remote `settings.json` via authenticated GET\n   to a cloud endpoint.\n8. Reads local `settings.json` via `FsReader`.\n9. Performs three-way merge: compares local, remote, and base versions (state\n   from last successful sync, stored locally).\n10. Intelligently combines changes: if user added settings on both machines,\n    merge contains both. Direct conflicts flagged for user resolution.\n\n## Phase 4: Applying Changes (Mountain)\n\n11. `FsWriter` writes merged content back to local `settings.json`.\n12. `ConfigurationService.reloadConfiguration()` re-reads all settings files,\n    reconstructs effective configuration, updates `Configuration` in `AppState`.\n\n## Phase 5: Notify Cocoon and Wind (Mountain -> All)\n\n13. `ConfigurationService` sends `$acceptConfigurationChanged` gRPC to Cocoon\n    with changed key summary.\n14. Emits `sky://configuration/changed` Tauri event to Wind/Sky.\n15. Cocoon\'s `ConfigurationProvider` updates its cache, fires\n    `onDidChangeConfiguration` to extensions.\n16. Wind components re-render with new values.\n\n## Key Source Files\n\n- `Mountain/Source/Environment/SynchronizationProvider.rs` -- sync\n  implementation (verify)\n- `vs/platform/userDataSync/common/settingsMerge.ts` -- merge logic\n\n---\n\n## See Also\n\n- [Application Startup and Handshake](https://Editor.Land/Doc/workflow-startup)\n- [Architecture Overview](https://Editor.Land/Doc/architecture)', "Source/Content/doc/workflow-user-data-sync.md", "e69bdfc38043cbb2", { html: 2950, metadata: 2951 }, '<h1 id="user-data-synchronization">User Data Synchronization</h1>\n<p>Describes the process of syncing user settings: user authentication, fetching\ndata from a remote store, three-way merge, applying changes locally, and\nnotifying all parts of the application.</p>\n<p>Verify against <code>Mountain/Source/Environment/SynchronizationProvider.rs</code> for the\nactual implementation.</p>\n<h2 id="data-flow">Data Flow</h2>\n<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>Phase 1: Authentication</span></span>\n<span class="line"><span>  User clicks "Sign In"</span></span>\n<span class="line"><span>  -> AuthenticationService triggers OAuth session request</span></span>\n<span class="line"><span>  -> Cocoon AuthenticationProvider sends $getSession gRPC to Mountain</span></span>\n<span class="line"><span>  -> Mountain handles OAuth flow (browser redirect, callback)</span></span>\n<span class="line"><span>  -> Token stored securely in keyring via SecretsProvider</span></span>\n<span class="line"><span>  -> User signed in, UserDataSyncAccountService has identity + token</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>Phase 2: Triggering Synchronization</span></span>\n<span class="line"><span>  -> UserDataAutoSyncService triggered (after sign-in, manual, or interval)</span></span>\n<span class="line"><span>  -> Calls UserDataSyncService.sync()</span></span>\n<span class="line"><span>  -> Iterates through registered synchronizers (settings, extensions, etc.)</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>Phase 3: Synchronizing Settings</span></span>\n<span class="line"><span>  -> SettingsSynchronizer.fetches remote settings via UserDataSyncStoreService</span></span>\n<span class="line"><span>  -> GET request to cloud endpoint with OAuth token</span></span>\n<span class="line"><span>  -> Remote server returns stored settings.json</span></span>\n<span class="line"><span>  -> Mountain reads local settings.json via FsReader</span></span>\n<span class="line"><span>  -> Three-way merge: local + remote + base (last sync state)</span></span>\n<span class="line"><span>  -> Resolves conflicts intelligently</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>Phase 4: Applying Merged State</span></span>\n<span class="line"><span>  -> FsWriter writes merged settings.json to disk</span></span>\n<span class="line"><span>  -> ConfigurationService.reloadConfiguration()</span></span>\n<span class="line"><span>  -> Re-reads config files, reconstructs effective configuration</span></span>\n<span class="line"><span>  -> Updates AppState.Configuration</span></span>\n<span class="line"><span>  -> $acceptConfigurationChanged gRPC to Cocoon</span></span>\n<span class="line"><span>  -> sky://configuration/changed Tauri event to Wind</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>Phase 5: UI and Extension Host React</span></span>\n<span class="line"><span>  -> Cocoon ConfigurationProvider updates cache</span></span>\n<span class="line"><span>  -> Fires onDidChangeConfiguration (vscode.workspace.onDidChangeConfiguration)</span></span>\n<span class="line"><span>  -> Wind settings UI re-renders with new values</span></span>\n<span class="line"><span>  -> Editor applies settings like fontSize</span></span>\n<span class="line"><span>  -> Extensions receive new settings, adjust behavior</span></span></code></pre>\n<h2 id="phase-1-user-authentication-windsky---cocoon---mountain">Phase 1: User Authentication (Wind/Sky -> Cocoon -> Mountain)</h2>\n<ol>\n<li>User clicks “Sign In” in the Account menu.\n<code>workbench.action.authentication.signIn</code> dispatched.</li>\n<li><code>AuthenticationService</code> in Wind triggers a request to Cocoon’s\n<code>AuthenticationProvider</code>.</li>\n<li>Cocoon makes <code>$getSession</code> gRPC to Mountain. Mountain handles OAuth flow\n(browser window, callback URI).</li>\n<li>OAuth token stored in system keyring via <code>SecretsProvider</code>.\n<code>UserDataSyncAccountService</code> receives identity and token.</li>\n</ol>\n<h2 id="phase-2-triggering-synchronization-mountain">Phase 2: Triggering Synchronization (Mountain)</h2>\n<ol start="5">\n<li><code>UserDataAutoSyncService</code> triggers (post-sign-in, manual, or scheduled).\nOrchestrates what to sync and in order.</li>\n<li>Calls <code>UserDataSyncService.sync()</code>, which iterates through registered\nsynchronizers (settings, extensions, keybindings, snippets).</li>\n</ol>\n<h2 id="phase-3-three-way-merge-mountain">Phase 3: Three-Way Merge (Mountain)</h2>\n<ol start="7">\n<li><code>SettingsSynchronizer</code> fetches remote <code>settings.json</code> via authenticated GET\nto a cloud endpoint.</li>\n<li>Reads local <code>settings.json</code> via <code>FsReader</code>.</li>\n<li>Performs three-way merge: compares local, remote, and base versions (state\nfrom last successful sync, stored locally).</li>\n<li>Intelligently combines changes: if user added settings on both machines,\nmerge contains both. Direct conflicts flagged for user resolution.</li>\n</ol>\n<h2 id="phase-4-applying-changes-mountain">Phase 4: Applying Changes (Mountain)</h2>\n<ol start="11">\n<li><code>FsWriter</code> writes merged content back to local <code>settings.json</code>.</li>\n<li><code>ConfigurationService.reloadConfiguration()</code> re-reads all settings files,\nreconstructs effective configuration, updates <code>Configuration</code> in <code>AppState</code>.</li>\n</ol>\n<h2 id="phase-5-notify-cocoon-and-wind-mountain---all">Phase 5: Notify Cocoon and Wind (Mountain -> All)</h2>\n<ol start="13">\n<li><code>ConfigurationService</code> sends <code>$acceptConfigurationChanged</code> gRPC to Cocoon\nwith changed key summary.</li>\n<li>Emits <code>sky://configuration/changed</code> Tauri event to Wind/Sky.</li>\n<li>Cocoon’s <code>ConfigurationProvider</code> updates its cache, fires\n<code>onDidChangeConfiguration</code> to extensions.</li>\n<li>Wind components re-render with new values.</li>\n</ol>\n<h2 id="key-source-files">Key Source Files</h2>\n<ul>\n<li><code>Mountain/Source/Environment/SynchronizationProvider.rs</code> — sync\nimplementation (verify)</li>\n<li><code>vs/platform/userDataSync/common/settingsMerge.ts</code> — merge logic</li>\n</ul>\n<hr>\n<h2 id="see-also">See Also</h2>\n<ul>\n<li><a href="https://Editor.Land/Doc/workflow-startup">Application Startup and Handshake</a></li>\n<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>\n</ul>', { headings: 2952, localImagePaths: 2973, remoteImagePaths: 2974, frontmatter: 2975, imagePaths: 2976 }, [2953, 2955, 2956, 2959, 2962, 2965, 2968, 2971, 2972], { depth: 25, slug: 2954, text: 2944 }, "user-data-synchronization", { depth: 29, slug: 744, text: 2682 }, { depth: 29, slug: 2957, text: 2958 }, "phase-1-user-authentication-windsky---cocoon---mountain", "Phase 1: User Authentication (Wind/Sky -> Cocoon -> Mountain)", { depth: 29, slug: 2960, text: 2961 }, "phase-2-triggering-synchronization-mountain", "Phase 2: Triggering Synchronization (Mountain)", { depth: 29, slug: 2963, text: 2964 }, "phase-3-three-way-merge-mountain", "Phase 3: Three-Way Merge (Mountain)", { depth: 29, slug: 2966, text: 2967 }, "phase-4-applying-changes-mountain", "Phase 4: Applying Changes (Mountain)", { depth: 29, slug: 2969, text: 2970 }, "phase-5-notify-cocoon-and-wind-mountain---all", "Phase 5: Notify Cocoon and Wind (Mountain -> All)", { depth: 29, slug: 2699, text: 2700 }, { depth: 29, slug: 446, text: 447 }, [], [], { title: 2944, section: 2670, order: 1663, description: 2945 }, [], "workflow-webview", { id: 2977, data: 2979, body: 2982, filePath: 2983, digest: 2984, rendered: 2985 }, { title: 2980, section: 2670, order: 2625, description: 2981 }, "Creating and Interacting with a Webview Panel", "Extension-contributed webview lifecycle: Cocoon requests panel, Mountain manages native webview, messages proxy back and forth.", "# Creating and Interacting with a Webview Panel\n\nDetails the full lifecycle of extension-contributed UI, from Cocoon requesting a\npanel to Mountain managing the native webview and proxying messages.\n\n## Lifecycle\n\n```\nExtension calls createWebviewPanel(viewType, title, viewColumn, options)\n  -> Cocoon WebviewPanelProvider.CreateWebviewPanel Effect\n  -> IpcProvider sends $createWebviewPanel gRPC to Mountain\n  -> Mountain generates unique handle (UUID)\n  -> Mountain creates WebviewStateDto, stores in AppState.ActiveWebviews\n  -> Mountain emits Tauri event: sky://webview/create\n  -> Mountain sends handle back to Cocoon as gRPC response\n\nCocoon:\n  -> Creates WebviewPanelShim and WebviewShim, stores handle\n  -> Returns WebviewPanelShim to extension\n\nWind/UI:\n  -> WebviewManagementService receives sky://webview/create\n  -> Dynamically creates TauriWebviewWindow or iframe, associates with handle\n\nExtension sets content:\n  -> panel.webview.html = \"<h1>Hello</h1>\"\n  -> WebviewShim sends $setWebviewHtml gRPC to Mountain\n  -> Mountain emits sky://webview/set-html event\n  -> Wind sets inner HTML of webview element\n  -> User sees content\n\nUser interaction:\n  -> User clicks button in webview\n  -> vscode.postMessage({ command: 'doSomething' })\n  -> Wind sends TauriInvoke('mountain://webview/on-message')\n  -> Mountain looks up webview's owner sidecar\n  -> Mountain sends $onDidReceiveMessage gRPC to Cocoon\n  -> Cocoon WebviewPanelProvider fires onDidReceiveMessage event\n  -> Extension receives message payload\n```\n\n## Phase 1: Extension Creates the Webview (Cocoon)\n\n1. Extension's `activate()` runs. Calls\n   `window.createWebviewPanel(viewType, title, viewColumn, options)`.\n2. `WebviewPanelProvider` in Cocoon constructs a DTO with all panel options,\n   extension ID, and `localResourceRoots` location.\n3. Sends `$createWebviewPanel` gRPC request to Mountain, awaits a unique handle.\n\n## Phase 2: Host Creates the Native Webview (Mountain)\n\n4. `$createWebviewPanel` request dispatched to `WebviewProvider` trait.\n5. `WebviewProvider.CreateWebviewPanel()` generates a UUID handle, creates\n   `WebviewStateDto`, stores in `AppState.ActiveWebviews`.\n6. Emits `sky://webview/create` Tauri event to Sky frontend.\n7. Sends the handle back to Cocoon as the gRPC response.\n\n## Phase 3: UI Renders the Webview (Wind/Sky)\n\n8. `WebviewManagementService` receives the `sky://webview/create` event.\n9. Creates a new `TauriWebviewWindow` or iframe within the main window's DOM,\n   associated with the handle.\n\n## Phase 4: Content and Interaction (Cocoon <-> Mountain <-> Wind)\n\n10. Cocoon creates `WebviewPanelShim` and `WebviewShim` with the handle, returns\n    it to the extension.\n11. Extension sets `panel.webview.html`. The Shim sends `$setWebviewHtml` gRPC\n    to Mountain.\n12. Mountain emits `sky://webview/set-html` event. Wind sets the inner HTML of\n    the webview.\n13. User clicks a button in the webview, calling `vscode.postMessage()`.\n14. Wind sends\n    `TauriInvoke('mountain://webview/on-message', { Handle, Message })`.\n15. Mountain looks up the webview's owner sidecar and sends\n    `$onDidReceiveMessage` gRPC to Cocoon.\n16. Cocoon fires `onDidReceiveMessage` event. Extension receives the message\n    payload.\n\n## Key Source Files\n\n- `Mountain/Source/Environment/WebviewProvider.rs` -- webview creation and HTML\n  management\n- `Cocoon/src/Service/WebviewPanel.ts` -- webview panel provider\n- `Cocoon/src/Service/Ipc.ts` -- IPC provider\n\n---\n\n## See Also\n\n- [Architecture Overview](https://Editor.Land/Doc/architecture)", "Source/Content/doc/workflow-webview.md", "eb122442f08a0048", { html: 2986, metadata: 2987 }, `<h1 id="creating-and-interacting-with-a-webview-panel">Creating and Interacting with a Webview Panel</h1>
<p>Details the full lifecycle of extension-contributed UI, from Cocoon requesting a
panel to Mountain managing the native webview and proxying messages.</p>
<h2 id="lifecycle">Lifecycle</h2>
<pre class="astro-code github-dark" style="background-color:#24292e;color:#e1e4e8; overflow-x: auto;" tabindex="0" data-language="plaintext"><code><span class="line"><span>Extension calls createWebviewPanel(viewType, title, viewColumn, options)</span></span>
<span class="line"><span>  -> Cocoon WebviewPanelProvider.CreateWebviewPanel Effect</span></span>
<span class="line"><span>  -> IpcProvider sends $createWebviewPanel gRPC to Mountain</span></span>
<span class="line"><span>  -> Mountain generates unique handle (UUID)</span></span>
<span class="line"><span>  -> Mountain creates WebviewStateDto, stores in AppState.ActiveWebviews</span></span>
<span class="line"><span>  -> Mountain emits Tauri event: sky://webview/create</span></span>
<span class="line"><span>  -> Mountain sends handle back to Cocoon as gRPC response</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Cocoon:</span></span>
<span class="line"><span>  -> Creates WebviewPanelShim and WebviewShim, stores handle</span></span>
<span class="line"><span>  -> Returns WebviewPanelShim to extension</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Wind/UI:</span></span>
<span class="line"><span>  -> WebviewManagementService receives sky://webview/create</span></span>
<span class="line"><span>  -> Dynamically creates TauriWebviewWindow or iframe, associates with handle</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Extension sets content:</span></span>
<span class="line"><span>  -> panel.webview.html = "&#x3C;h1>Hello&#x3C;/h1>"</span></span>
<span class="line"><span>  -> WebviewShim sends $setWebviewHtml gRPC to Mountain</span></span>
<span class="line"><span>  -> Mountain emits sky://webview/set-html event</span></span>
<span class="line"><span>  -> Wind sets inner HTML of webview element</span></span>
<span class="line"><span>  -> User sees content</span></span>
<span class="line"><span></span></span>
<span class="line"><span>User interaction:</span></span>
<span class="line"><span>  -> User clicks button in webview</span></span>
<span class="line"><span>  -> vscode.postMessage({ command: 'doSomething' })</span></span>
<span class="line"><span>  -> Wind sends TauriInvoke('mountain://webview/on-message')</span></span>
<span class="line"><span>  -> Mountain looks up webview's owner sidecar</span></span>
<span class="line"><span>  -> Mountain sends $onDidReceiveMessage gRPC to Cocoon</span></span>
<span class="line"><span>  -> Cocoon WebviewPanelProvider fires onDidReceiveMessage event</span></span>
<span class="line"><span>  -> Extension receives message payload</span></span></code></pre>
<h2 id="phase-1-extension-creates-the-webview-cocoon">Phase 1: Extension Creates the Webview (Cocoon)</h2>
<ol>
<li>Extension’s <code>activate()</code> runs. Calls
<code>window.createWebviewPanel(viewType, title, viewColumn, options)</code>.</li>
<li><code>WebviewPanelProvider</code> in Cocoon constructs a DTO with all panel options,
extension ID, and <code>localResourceRoots</code> location.</li>
<li>Sends <code>$createWebviewPanel</code> gRPC request to Mountain, awaits a unique handle.</li>
</ol>
<h2 id="phase-2-host-creates-the-native-webview-mountain">Phase 2: Host Creates the Native Webview (Mountain)</h2>
<ol start="4">
<li><code>$createWebviewPanel</code> request dispatched to <code>WebviewProvider</code> trait.</li>
<li><code>WebviewProvider.CreateWebviewPanel()</code> generates a UUID handle, creates
<code>WebviewStateDto</code>, stores in <code>AppState.ActiveWebviews</code>.</li>
<li>Emits <code>sky://webview/create</code> Tauri event to Sky frontend.</li>
<li>Sends the handle back to Cocoon as the gRPC response.</li>
</ol>
<h2 id="phase-3-ui-renders-the-webview-windsky">Phase 3: UI Renders the Webview (Wind/Sky)</h2>
<ol start="8">
<li><code>WebviewManagementService</code> receives the <code>sky://webview/create</code> event.</li>
<li>Creates a new <code>TauriWebviewWindow</code> or iframe within the main window’s DOM,
associated with the handle.</li>
</ol>
<h2 id="phase-4-content-and-interaction-cocoon---mountain---wind">Phase 4: Content and Interaction (Cocoon &#x3C;-> Mountain &#x3C;-> Wind)</h2>
<ol start="10">
<li>Cocoon creates <code>WebviewPanelShim</code> and <code>WebviewShim</code> with the handle, returns
it to the extension.</li>
<li>Extension sets <code>panel.webview.html</code>. The Shim sends <code>$setWebviewHtml</code> gRPC
to Mountain.</li>
<li>Mountain emits <code>sky://webview/set-html</code> event. Wind sets the inner HTML of
the webview.</li>
<li>User clicks a button in the webview, calling <code>vscode.postMessage()</code>.</li>
<li>Wind sends
<code>TauriInvoke('mountain://webview/on-message', { Handle, Message })</code>.</li>
<li>Mountain looks up the webview’s owner sidecar and sends
<code>$onDidReceiveMessage</code> gRPC to Cocoon.</li>
<li>Cocoon fires <code>onDidReceiveMessage</code> event. Extension receives the message
payload.</li>
</ol>
<h2 id="key-source-files">Key Source Files</h2>
<ul>
<li><code>Mountain/Source/Environment/WebviewProvider.rs</code> — webview creation and HTML
management</li>
<li><code>Cocoon/src/Service/WebviewPanel.ts</code> — webview panel provider</li>
<li><code>Cocoon/src/Service/Ipc.ts</code> — IPC provider</li>
</ul>
<hr>
<h2 id="see-also">See Also</h2>
<ul>
<li><a href="https://Editor.Land/Doc/architecture">Architecture Overview</a></li>
</ul>`, { headings: 2988, localImagePaths: 3008, remoteImagePaths: 3009, frontmatter: 3010, imagePaths: 3011 }, [2989, 2991, 2994, 2997, 3e3, 3003, 3006, 3007], { depth: 25, slug: 2990, text: 2980 }, "creating-and-interacting-with-a-webview-panel", { depth: 29, slug: 2992, text: 2993 }, "lifecycle", "Lifecycle", { depth: 29, slug: 2995, text: 2996 }, "phase-1-extension-creates-the-webview-cocoon", "Phase 1: Extension Creates the Webview (Cocoon)", { depth: 29, slug: 2998, text: 2999 }, "phase-2-host-creates-the-native-webview-mountain", "Phase 2: Host Creates the Native Webview (Mountain)", { depth: 29, slug: 3001, text: 3002 }, "phase-3-ui-renders-the-webview-windsky", "Phase 3: UI Renders the Webview (Wind/Sky)", { depth: 29, slug: 3004, text: 3005 }, "phase-4-content-and-interaction-cocoon---mountain---wind", "Phase 4: Content and Interaction (Cocoon <-> Mountain <-> Wind)", { depth: 29, slug: 2699, text: 2700 }, { depth: 29, slug: 446, text: 447 }, [], [], { title: 2980, section: 2670, order: 2625, description: 2981 }, [], "meta::meta", ["Map", 3014, 3015, 3016, 3017, 3018, 3019], "astro-config-digest", '{"root":{},"srcDir":{},"publicDir":{},"outDir":{},"cacheDir":{},"site":"https://Editor.Land","compressHTML":true,"base":"/","trailingSlash":"ignore","output":"static","scopedStyleStrategy":"attribute","build":{"format":"directory","client":{},"server":{},"assets":"_astro","serverEntry":"entry.mjs","redirects":true,"inlineStylesheets":"auto","concurrency":9999},"server":{"open":false,"host":true,"port":9999,"allowedHosts":[]},"redirects":{},"prefetch":{"prefetchAll":true,"defaultStrategy":"hover"},"image":{"endpoint":{"route":"/_image"},"service":{"entrypoint":"astro/assets/services/sharp","config":{}},"dangerouslyProcessSVG":false,"domains":[],"remotePatterns":[],"responsiveStyles":false},"devToolbar":{"enabled":false},"markdown":{"syntaxHighlight":{"type":"shiki","excludeLangs":["math"]},"shikiConfig":{"langs":[],"langAlias":{},"theme":"github-dark","themes":{},"wrap":false,"transformers":[]},"remarkPlugins":[],"rehypePlugins":[],"remarkRehype":{},"processor":{"name":"unified","options":{"remarkPlugins":[],"rehypePlugins":[],"remarkRehype":{}}}},"security":{"checkOrigin":true,"allowedDomains":[],"csp":false,"actionBodySizeLimit":1048576,"serverIslandBodySizeLimit":1048576},"env":{"schema":{},"validateSecrets":false},"prerenderConflictBehavior":"warn","experimental":{"advancedRouting":false,"clientPrerender":true,"contentIntellisense":true,"chromeDevtoolsWorkspace":false,"rustCompiler":false,"queuedRendering":{"enabled":false}},"legacy":{"collectionsBackwardsCompat":false}}', "astro-version", "6.4.2", "content-config-digest", "d036989aed09eaaa"];

export { _astro_dataLayerContent as default };
